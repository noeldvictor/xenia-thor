<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="jp.xenia.emulator">

    <uses-feature
        android:name="android.hardware.vulkan.level"
        android:required="true"
        android:version="0" />

    <uses-feature
        android:name="android.hardware.vulkan.version"
        android:required="true"
        android:version="0x400000" />

    <!-- Granted automatically - guest sockets. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Research debug builds load local ISO/XEX content from removable storage. -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:allowNativeHeapPointerTagging="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.XeniaThor">

        <meta-data
            android:name="com.android.graphics.injectLayers.enable"
            android:value="true" />

        <activity
            android:name="jp.xenia.emulator.LauncherActivity"
            android:exported="true"
            android:process=":launcher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="jp.xenia.emulator.SettingsActivity"
            android:exported="false"
            android:label="@string/activity_label_settings"
            android:screenOrientation="sensorLandscape"
            android:theme="@style/Theme.XeniaThor" />

        <activity
            android:name="jp.xenia.emulator.ControllerMappingActivity"
            android:exported="false"
            android:label="@string/activity_label_controller_mapping"
            android:screenOrientation="sensorLandscape"
            android:theme="@style/Theme.XeniaThor" />

        <activity
            android:name="jp.xenia.emulator.GpuTraceViewerActivity"
            android:exported="true"
            android:label="@string/activity_label_gpu_trace_viewer"
            android:screenOrientation="sensorLandscape"
            android:theme="@style/Theme.XeniaThor" />

        <activity
            android:name="jp.xenia.emulator.EmulatorActivity"
            android:exported="true"
            android:label="@string/activity_label_emulator"
            android:screenOrientation="sensorLandscape"
            android:theme="@style/Theme.XeniaThor" />

        <activity
            android:name="jp.xenia.emulator.WindowDemoActivity"
            android:exported="true"
            android:label="@string/activity_label_window_demo"
            android:theme="@style/Theme.XeniaThor" />

    </application>

</manifest>
