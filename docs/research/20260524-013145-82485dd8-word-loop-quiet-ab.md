# 82485DD8 Word Loop Quiet A/B

Date: 2026-05-24 01:31 EDT

Branch: `master`

Commit: `11427e90c`

APK SHA:

`CB234AD454F2F458BB22E436F16CCDC7DBD8EA7510DDD8005B8AC2E2B9E0CA3C`

## Question

The profiled `82485DD8:82485E70` word copy-loop fastpath looked positive in
`docs/research/20260524-011500-82485dd8-word-copy-loop-fastpath.md`, but that
run still had block body-time instrumentation. This capture asks whether the
same patch is still route-positive in the quieter speed lane.

## Method

Both runs used the same APK and same delayed body-time comparators:

- `82490030`
- `82486178`
- `82485DD8`
- `82486018`

Both runs disabled disassembly dumps and block body-time profiling. Both kept
`arm64_blue_dragon_vmx_copy_loop_fastpath=false` and
`arm64_context_promotion_gpr_livein_r1=false`.

Control off:

- Capture: `scratch/thor-debug/20260524-012352-*`
- Cvar: `arm64_blue_dragon_word_copy_loop_fastpath=false`
- Screenshot: visible Blue Dragon opening sky/wing route
- Fatal-marker search: clean

Fastpath on:

- Capture: `scratch/thor-debug/20260524-012713-*`
- Cvar: `arm64_blue_dragon_word_copy_loop_fastpath=true`
- Screenshot: visible Blue Dragon opening sky/wing route
- Fatal-marker search: clean

## Result

Final delayed body-time rows:

| Function | Off Total | On Total | Read |
| --- | ---: | ---: | --- |
| `82490030` | `3923146` | `3573598` | parent down about 9% |
| `82486178` | `1591407` | `1584856` | effectively flat |
| `82485DD8` | `1010794` | `774505` | target down about 23% |
| `82486018` | `647412` | `375573` | secondary path also lower |

Final thread samples still show a CPU/JIT wall, not a GPU wall:

- Off: Main Thread `92.3%`, XMA Decoder `46.1%`, GPU Commands `7.6%`,
  Draw Thread `0.0%`.
- On: Main Thread `96.1%`, XMA Decoder `50.0%`, GPU Commands `7.6%`,
  Draw Thread `3.8%`.

Android UI frame stats remain unsuitable as game FPS proof.

## Decision

Promote `arm64_blue_dragon_word_copy_loop_fastpath` into the Blue Dragon Thor
speed preset only. The backend cvar remains default-off globally because this
is still a title/address-specific research shortcut.

Keep `arm64_blue_dragon_vmx_copy_loop_fastpath=false`; its earlier same-APK A/B
did not prove speed. Do not combine the two copy-loop toggles without a
separate matrix.

## Next

Use the word-loop fastpath as part of the normal Blue Dragon speed lane, then
run a fresh delayed body-time rebaseline to see what replaces `82485DD8` under
the `82490030` parent. Likely next choices are:

- `82486018:82486124` if it remains locally hot;
- a fresh `82490030` child split with the word-loop toggle on;
- return to the larger `82282490` opening-scene wall if `82490030` is no longer
  the limiting branch.
