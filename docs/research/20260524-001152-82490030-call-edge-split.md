# 82490030 Call-Edge Split

Date: 2026-05-24 00:11 EDT

## Question

After fixing the `Processor::OnThreadDestroyed` global-lock black-idle, does
the restored `8227F1D8 -> 82490030` route point to a local `82490030` codegen
peephole, or to a child function?

## Inputs

- Commit: `0fe85f640`
- APK SHA-256:
  `FE5CBCF23E832807E51547D66387C3680DF6DB4B4802C9DB5BB64ACB46B8489B`
- Device: AYN Thor over ADB serial `c3ca0370`
- Target:
  `/storage/2664-21DE/roms/xbox360/Blue Dragon.m3u/Blue Dragon (USA, Europe) (En,Fr) (Disc 1).iso`
- Capture:
  `scratch/thor-debug/20260524-000643-*`
- Parent HIR/block capture:
  `scratch/thor-debug/20260524-000047-*`

## Commands

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 25 -Arm64SpeedProfileMinDelta 1 -A64LseKernelLockFastpaths true -A64RtlLeaveFastpathAudit false -Arm64PermuteI32ZipFastpath true -Arm64ContextPromotionGprLiveInR1 false -Arm64SpeedProfileBodyTimeFilter "82490030,82486178,826BFC40,82485DD8,82486018" -Arm64SpeedProfileBodyTimeAfterMs 120000 -Arm64SpeedProfileCallEdgeFilter 82490030 -Arm64SpeedProfileThreadSnapshotOnIdle true -StopAppAfterCapture true

powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_call_path_report.ps1 -LogPath scratch\thor-debug\20260524-000643-speed-logcat.txt -Function 82490030 -Phase OptHIR -BlockProfileLog scratch\thor-debug\20260524-000643-speed-logcat.txt -Top 20
```

## Route Health

- Final screenshot reached the visible Blue Dragon sky/wing opening route:
  `scratch/thor-debug/20260524-000643-screenshot.png`.
- Fatal-marker search found no `Fatal signal`, `AndroidRuntime`,
  `VK_ERROR_DEVICE_LOST`, GPU hang, assertion, or fatal log lines.
- No `A64 thread snapshot skipped`, `global_lock_owner`, or `owner_hint` lines
  appeared.
- This keeps the `ObjectTable` release-outside-lock fix route-clean for this
  lane.

Final perf snapshot remained CPU/JIT-heavy:

- Main Thread: `88.4%`
- XMA Decoder: `42.3%`
- GPU Commands: `7.6%`
- Draw Thread: `3.8%`

Android UI frame stats still are not game FPS proof.

## Parent HIR/Block Context

The earlier route-clean `82490030` filtered HIR/block capture
`scratch/thor-debug/20260524-000047-*` showed:

- HIR lines: `894`
- `store_context=235`, `load_context=113`, `context_barrier=44`
- Calls: `18`
- Dynamic block body-time leaders:
  - `82490030`: `body_total=629838`, calls to `0x82486178` and `0x826BFC40`
  - `8249016C`: `body_total=465936`, call to `0x82485DD8`
  - `82490120`: `body_total=352957`, call to `0x82486018`
  - `824903D8`: `body_total=330897`, local memory/store loop, no calls

This made a call-edge split more defensible than a local state-traffic patch.

## Dynamic Call-Edge Result

`tools/thor/thor_hir_call_path_report.ps1` now supports dynamic-only
call-edge logs where no filtered HIR dump was requested. On the
`20260524-000643` capture it reported:

```text
hir_dump_found=false

target=0x82486178 edges=2,1 blocks=2 block_guests=82490084,82490030 calls_total=33218 body_ticks_total=2302621 body_ticks_peak_delta=626104 ticks_per_call_peak=203
target=0x82485DD8 edges=10,13,8,7,9,5,12 blocks=7 block_guests=824901BC,8249023C,8249016C,82490140,8249019C,82490100,82490214 calls_total=39467 body_ticks_total=804023 body_ticks_peak_delta=295463 ticks_per_call_peak=72
target=0x82486018 edges=6 blocks=1 block_guests=82490120 calls_total=35462 body_ticks_total=679426 body_ticks_peak_delta=382151 ticks_per_call_peak=42
```

Final function body-time rows:

```text
82490030 body_ticks_total=4650455 body_ticks_delta=1229703 entries_delta=8901 ticks_per_entry=138 code_size=8380
82486178 body_ticks_total=2333170 body_ticks_delta=443461 entries_delta=9238 ticks_per_entry=48 code_size=2100
82486018 body_ticks_total=642359 body_ticks_delta=376213 entries_delta=11482 ticks_per_entry=32 code_size=3700
82485DD8 body_ticks_total=887723 body_ticks_delta=307698 entries_delta=15536 ticks_per_entry=19 code_size=2720
```

Top final call-edge rows:

```text
82490030 edge=6 block=82490120 target=82486018 calls_total=35462 body_ticks_total=679426 ticks_per_call=42
82490030 edge=8 block=8249016C target=82485DD8 calls_total=18745 body_ticks_total=746121 ticks_per_call=72
82490030 edge=2 block=82490084 target=82486178 calls_total=13410 body_ticks_total=1531533 ticks_per_call=90
82490030 edge=1 block=82490030 target=82486178 calls_total=19808 body_ticks_total=771088 ticks_per_call=40
```

## Interpretation

`82490030` is not ready for a local generated-code peephole. Its measured wall
is still mostly child work.

The strongest next target is `82486178` because it has the largest aggregated
child body time from `82490030` (`2302621`) and remains second in final
function body-time (`2333170`). `82485DD8` and `82486018` are worth keeping in
the comparator filter, but they are not the first HIR/block dump target.

`826BFC40` appeared as a static call in the HIR-backed parent report, but it
did not survive the dynamic call-edge split as a meaningful body-time target in
this route.

## Tooling Change

`tools/thor/thor_hir_call_path_report.ps1` now:

- does not throw when no HIR dump exists if dynamic call-edge rows are present;
- prints `hir_dump_found=true/false`;
- adds a dynamic target summary that aggregates call-edge rows by callee.

This is useful because quiet call-edge captures should not need a disassembly
dump just to identify the next callee.

Validation:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_call_path_report.ps1 -LogPath scratch\thor-debug\20260524-000643-speed-logcat.txt -Function 82490030 -Phase OptHIR -BlockProfileLog scratch\thor-debug\20260524-000643-speed-logcat.txt -Top 20
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_call_path_report.ps1 -LogPath scratch\thor-debug\20260524-000047-speed-logcat.txt -Function 82490030 -Phase OptHIR -BlockProfileLog scratch\thor-debug\20260524-000047-speed-logcat.txt -Top 12
git diff --check
```

## Decision

Keep call-edge cvars default-off outside targeted captures. Do not patch local
`82490030` codegen from this evidence.

Next run should be a route-stabilized filtered HIR plus delayed body/block-time
capture for `82486178`, keeping `82490030`, `82485DD8`, and `82486018` in the
body-time comparator filter:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 25 -Arm64SpeedProfileMinDelta 1 -A64LseKernelLockFastpaths true -A64RtlLeaveFastpathAudit false -Arm64PermuteI32ZipFastpath true -Arm64ContextPromotionGprLiveInR1 false -DisassembleFunctions true -DisassembleFunctionFilter 82486178 -Arm64SpeedProfileBodyTimeFilter "82490030,82486178,82485DD8,82486018" -Arm64SpeedProfileBodyTimeAfterMs 120000 -Arm64SpeedProfileBlockFilter 82486178 -Arm64SpeedProfileBlockBodyTime true -Arm64SpeedProfileThreadSnapshotOnIdle true -StopAppAfterCapture true
```

