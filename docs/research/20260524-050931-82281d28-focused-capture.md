# 2026-05-24 05:09 EDT - 82281D28 Focused Capture

## Question

After `821CE028` proved real but too small for the next immediate codegen
patch, does the larger `82281D28` body-time row expose a local block we can
optimize safely?

## Capture

Command:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 25 -Arm64SpeedProfileMinDelta 1 -DisassembleFunctions true -DisassembleFunctionFilter 82281D28 -Arm64SpeedProfileBodyTimeFilter "82282490,82281D28,82287788,821CE028" -Arm64SpeedProfileBodyTimeAfterMs 120000 -Arm64SpeedProfileBlockFilter 82281D28 -Arm64SpeedProfileBlockBodyTime true
```

Artifacts:

- Capture prefix: `scratch/thor-debug/20260524-050427-*`
- Log: `scratch/thor-debug/20260524-050427-speed-logcat.txt`
- Filtered log: `scratch/thor-debug/20260524-050427-speed-logcat-filtered.txt`
- Metadata: `scratch/thor-debug/20260524-050427-meta.txt`
- Screenshot: `scratch/thor-debug/20260524-050427-screenshot.png`
- APK SHA-256:
  `A4CB9C0E8F4CBB63EB09F3DA38640BEA5410DFE5916A155627C18F3B90C39BD8`
- Commit: `281217dbb`

The final screenshot reached the visible opening sky/wing route. Fatal-marker
search found no `FATAL`, `SIGSEGV`, `SIGABRT`, `AndroidRuntime`,
`VK_ERROR_DEVICE_LOST`, `GPU is hung`, assert, or searched guest-crash marker.

## Body-Time Result

Final filtered body rows:

- `82281D28`: `body_ticks_total=65346716`, `entries_delta=27612`,
  `ticks_per_entry=803`, `code_size=119908`
- `82282490`: `body_ticks_total=31615045`, `entries_delta=57876`,
  `ticks_per_entry=174`, `code_size=87168`
- `82282388`: `body_ticks_total=18965773`, `entries_delta=5265`,
  `ticks_per_entry=1222`, `code_size=88088`
- `82282410`: `body_ticks_total=17323669`, `entries_delta=3861`,
  `ticks_per_entry=1522`, `code_size=88088`
- `82287788`: `body_ticks_total=3277477`, `entries_delta=229485`,
  `ticks_per_entry=4`, `code_size=35172`
- `821CE028`: `body_ticks_total=716173`, `entries_delta=113421`,
  `ticks_per_entry=2`, `code_size=3756`

The dynamic block profiler puts nearly all local `82281D28` body time in block
`20`, guest `8228233C`:

- `8228233C`: `body_ticks_total=46564347`, `entries_delta=22347`,
  `ticks_per_entry=707`
- Next blocks are much smaller:
  - `82281D88`: `2471139`
  - `8228210C`: `2424580`
  - `82281FFC`: `1092503`
  - `822822EC`: `729131`

## HIR Shape

`tools/thor/thor_hir_hotpath_report.ps1` on the filtered dump reports:

- `hir_lines=6855`
- `store_context=1438`
- `load_context=779`
- `dot_product_4=136`
- `permutes=381`
- `permute.2=236`
- `context_barriers=216`
- `calls=62`

Context traffic is close to the older `82282490` shape:

- Loads: `GPR=562`, `VMX=155`, `FPR=38`, `FP/VMX_STATUS=12`,
  `LR/CTR=10`, `CR=2`
- Stores: `GPR=583`, `VMX=373`, `CR=343`, `LR/CTR=70`, `FPR=52`,
  `FP/VMX_STATUS=12`, `XER=5`

The state-span report ranks GPR cache candidates as:

- `r[1]` score `224`
- `r[11]` score `169`
- `r[10]` score `98`
- `r[31]` score `95`
- `r[29]` score `81`
- `r[30]` score `74`
- `r[28]` score `72`

## Important Caveat

The generated-code block profiler reports hot guest block `8228233C`, but the
warning-level filtered HIR log starts at `82282398`. The dynamic block slice and
A64 codegen audit tools therefore cannot map `8228233C` to an exact OptHIR span
from this capture alone.

The visible HIR immediately after the missing region shows `822823E0` calling
`0x826BF770`, then `82282404` recursively calling `0x82281D28`. The final body
rows also show sibling/alias-looking functions `82282388` and `82282410` with
large body time. That makes this lane look call-edge/recursive-path heavy, not
yet a safe local state/vector peephole.

## Decision

Do not patch `82281D28` generated code from this capture alone.

Next run should be a route-stabilized `82281D28` call-edge split with delayed
body-time comparators, not another broad HIR dump and not a codegen experiment:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 25 -Arm64SpeedProfileMinDelta 1 -Arm64SpeedProfileBodyTimeFilter "82281D28,82282388,82282410,82282490,826BF770,82287788,821CE028" -Arm64SpeedProfileBodyTimeAfterMs 120000 -Arm64SpeedProfileCallEdgeFilter 82281D28
```

If the edge split proves `8228233C` is mostly child work, follow the dominant
callee. If it proves parent-exclusive time remains high, rerun a narrower HIR
dump or improve the HIR/block mapper so `8228233C` can be audited before a
default-off function/block-gated patch.
