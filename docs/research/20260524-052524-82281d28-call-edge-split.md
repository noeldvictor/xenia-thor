# 2026-05-24 05:25 EDT - 82281D28 Call-Edge Split

## Question

The previous focused capture made `82281D28` look like the next large lane, but
its hottest internal dynamic block (`8228233C`) could not be mapped to exact
OptHIR because the warning-level dump started at `82282398`. Is `8228233C`
mostly local generated-code work, or mostly child/recursive call work?

## Capture

Command:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 25 -Arm64SpeedProfileMinDelta 1 -Arm64SpeedProfileBodyTimeFilter "82281D28,82282388,82282410,82282490,826BF770,82287788,821CE028" -Arm64SpeedProfileBodyTimeAfterMs 120000 -Arm64SpeedProfileCallEdgeFilter 82281D28
```

Artifacts:

- Capture prefix: `scratch/thor-debug/20260524-052031-*`
- Log: `scratch/thor-debug/20260524-052031-speed-logcat.txt`
- Filtered log: `scratch/thor-debug/20260524-052031-speed-logcat-filtered.txt`
- Metadata: `scratch/thor-debug/20260524-052031-meta.txt`
- Screenshot: `scratch/thor-debug/20260524-052031-screenshot.png`
- Commit: `dc26cf2a4`
- APK SHA-256:
  `A4CB9C0E8F4CBB63EB09F3DA38640BEA5410DFE5916A155627C18F3B90C39BD8`

The run reached the visible opening sky/wing route. Fatal-marker search was
clean for the usual Android, Vulkan, assert, signal, and searched guest-crash
markers.

## Parser

Command:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_call_path_report.ps1 -LogPath scratch\thor-debug\20260524-052031-speed-logcat.txt -Function 82281D28 -BlockProfileLog scratch\thor-debug\20260524-052031-speed-logcat.txt -Top 20
```

The parser found no filtered HIR dump in this no-disassembly run, but its
dynamic-only call-edge summary worked.

## Body-Time Result

Final body-time rows:

- `82282490`: `body_ticks_total=30214411`, `entries_delta=59360`,
  `ticks_per_entry=136`, `code_size=87168`
- `82281D28`: `body_ticks_total=10212419`, `entries_delta=28320`,
  `ticks_per_entry=126`, `code_size=103872`
- `82287788`: `body_ticks_total=3195362`, `entries_delta=235579`,
  `ticks_per_entry=4`, `code_size=35172`
- `82282388`: `body_ticks_total=3020644`, `entries_delta=5400`,
  `ticks_per_entry=195`, `code_size=88088`
- `82282410`: `body_ticks_total=2902097`, `entries_delta=3960`,
  `ticks_per_entry=256`, `code_size=88088`
- `821CE028`: `body_ticks_total=674464`, `entries_delta=116539`,
  `ticks_per_entry=2`, `code_size=3756`
- `826BF770`: `body_ticks_total=563064`, `entries_delta=166998`,
  `ticks_per_entry=1`, `code_size=324`

`82282490` returned to the top body row when the heavy HIR/block dump was
removed.

## Dynamic Edge Result

Top dynamic call-edge targets for `82281D28`:

- `8228233C -> 82281D28`: `body_ticks_total=7350722`,
  `calls_total=69445`, `ticks_per_call_peak=116`
- `82281FFC/8228207C/82281F38 -> 82275C08`: `body_ticks_total=779200`,
  `calls_total=132497`
- `822822EC -> 82274E38`: `body_ticks_total=487108`,
  `calls_total=421433`
- `82281E0C/82281EC4 -> 822753E0`: `body_ticks_total=327352`,
  `calls_total=156812`
- `8228233C -> 826BF770` plus small helper edges:
  `body_ticks_total=146059`, `calls_total=70107`

The direct dynamic edge total is approximately `9096992` ticks, or about
`89.1%` of the final `82281D28` body total. The self-recursive
`8228233C -> 82281D28` edge alone is about `72.0%` of `82281D28` body time.
Approximate parent-exclusive work left after the reported direct edges is
`1115427` ticks.

## Decision

Do not patch local `82281D28` generated code yet.

This capture says the tempting `8228233C` block is mostly recursive callee
work, and the no-disassembly lane puts `82282490` back on top. The right next
step is not another unchanged `82281D28` HIR/block run. Use the current evidence
to collapse or reason about the recursive SCC and return to body-backed
`82282490` work, especially local spans with real exclusive time. If `82281D28`
is revisited, do it with a mapper/tooling improvement that can recover
`8228233C` static span context, not a broad local codegen experiment.
