# 20260524-120704 - 822824B8 Branch-State Audit

## Goal

Audit the fresh non-call-heavy `82282490:822824B8-822824E8` span before any
codegen patch. The previous lane-closure report selected this span because it
has `approx_exclusive=1099164`, `exclusive_pct=100`, and CR/GPR
compare-and-branch traffic instead of inclusive child-call time.

## Starting Point

Starting HEAD: `e5d8ad788`

Relevant prior note:

- `docs/research/20260524-115538-8228252c-lane-closure-next-target.md`

## Commands

```powershell
git status --short --branch
Test-Path scratch\thor-debug\latest-blue-dragon-full-speed-proof.json
Get-Content AGENTS.md -TotalCount 80
Get-Content scratch\thor-debug\codex-goal-loop.json -Raw
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_dynamic_block_slice_report.ps1 -LogPath scratch\thor-debug\20260521-170941-speed-logcat.txt -Function 82282490 -BlockProfileLog scratch\thor-debug\20260524-030450-speed-logcat.txt -BlockGuests 822824B8 -Top 12 -ContextTop 20
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_a64_codegen_audit.ps1 -LogPath scratch\thor-debug\20260521-170941-speed-logcat.txt -Function 82282490 -StartGuest 822824B8 -EndGuest 822824E8 -BlockProfileLog scratch\thor-debug\20260524-030450-speed-logcat.txt -Top 24
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_branch_state_audit.ps1 -LogPath scratch\thor-debug\20260521-170941-speed-logcat.txt -Function 82282490 -StartGuest 822824B8 -EndGuest 822824E8 -BlockProfileLog scratch\thor-debug\20260524-030450-speed-logcat.txt -Top 20
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_branch_state_audit.ps1 -LogPath scratch\thor-debug\20260521-170941-speed-logcat.txt -Function 82282490 -StartGuest 822825F4 -EndGuest 82282600 -BlockProfileLog scratch\thor-debug\20260524-030450-speed-logcat.txt -Top 16
```

## Tooling

Added `tools/thor/thor_hir_branch_state_audit.ps1`. It parses a filtered HIR
slice, reports context loads/stores by PPC context field, identifies branch
predicates that were also stored to context before the branch, and reports
same-context reloads that would require a fallthrough-only value carrier across
`context_barrier` / branch boundaries.

## 822824B8 Findings

`822824B8-822824E8` remains real local work:

- `body_ticks_total=1099164`
- `entries_delta=209429`
- `ppc_count=12`
- `hir_count=56`
- `class_stores=cr:9,gpr:7`
- `class_loads=gpr:4`
- `context_stores=cr6.1:2,cr6.all_equal:2,cr6.none_equal:2,r[10]:2,r[11]:2,r[9]:2,cr0.eq:1,cr0.gt:1,cr0.lt:1,r[30]:1`
- `context_loads=r[11]:2,r[31]:2`

The branch predicates are exactly the tempting CR-store shape, but they are not
safe to skip from this local view:

- `822824C8` branches on `v48`, which was just stored to
  `cr6.none_equal` at `822824C4`.
- `822824D8` branches on `v66`, which was just stored to
  `cr6.none_equal` at `822824D4`.
- `822824E0` branches on `v81`, which was just stored to `cr0.eq` at
  `822824DC`.

The remaining local reload opportunities are small and fallthrough-only:

- `r[11]` reload at `822824CC`, sourced from the `822824B8` store.
- `r[11]` reload at `822824DC`, sourced from the `822824B8` store.
- `r[31]` reload at `822824E4`, sourced from the `822824B8` load.

All three cross a `context_barrier` / branch boundary. Any replacement needs a
branch-aware fallthrough carrier and still must keep guest-visible state for
the branch targets.

## Loop Tail Check

The companion loop-tail span `822825F4-82282600` is also local but has no GPR
reload opportunity:

- `body_ticks_total=1033964`
- `ppc_count=3`
- `hir_count=12`
- `class_stores=cr:3,gpr:1`
- branch predicate store: `cr0.eq@822825F8`
- `reloads_after_same_context_access=0`

It is the same known-risk CR predicate-store lane, not an independent patch
target.

## Decision

Do not implement a `822824B8-822824E8` codegen behavior patch next.

The CR store / compare / barrier shape is exactly the lane that previously
crashed or regressed Blue Dragon when broadened. The GPR side offers only three
fallthrough-only reload replacements in the span, which is too small for a
standalone patch after similar branch-preserving carrier experiments missed
speed proof elsewhere.

Next useful work should either:

- broaden the branch-state audit across the `822824B8 <-> 822825F4` loop and
  other branchy local spans to see whether a CFG-aware GPR carrier has a real
  aggregate upper bound; or
- return to a broader CFG-aware/interprocedural state-carrier design for
  higher-traffic state rather than another local CR peephole.
