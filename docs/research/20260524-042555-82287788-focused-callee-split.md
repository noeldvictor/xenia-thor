# Blue Dragon 82287788 Focused Callee Split

Date: 2026-05-24 04:25 EDT

## Question

After the `8228252C` lane-fold probe failed to prove speed, is the child path
`8228252C -> 82287788` a better next target for Blue Dragon on AYN Thor?

## Captures

Route/filtered HIR/block-body capture:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 25 -Arm64SpeedProfileMinDelta 1 -DisassembleFunctions true -DisassembleFunctionFilter 82287788 -Arm64SpeedProfileBodyTimeFilter "82282490,82287788" -Arm64SpeedProfileBodyTimeAfterMs 120000 -Arm64SpeedProfileBlockFilter 82287788 -Arm64SpeedProfileBlockBodyTime true
```

- Capture: `scratch/thor-debug/20260524-041413-*`
- HEAD: `05601d81d`
- APK SHA-256:
  `A4CB9C0E8F4CBB63EB09F3DA38640BEA5410DFE5916A155627C18F3B90C39BD8`
- Screenshot: visible opening sky/wing route.
- Fatal-marker search: clean.

Lower-noise call-edge split:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 25 -Arm64SpeedProfileMinDelta 1 -Arm64SpeedProfileBodyTimeFilter "82282490,82287788,821CE028,82287EE0,826BFC78,826BFCC8" -Arm64SpeedProfileBodyTimeAfterMs 120000 -Arm64SpeedProfileCallEdgeFilter 82287788
```

- Capture: `scratch/thor-debug/20260524-042031-*`
- HEAD: `05601d81d`
- APK SHA-256:
  `A4CB9C0E8F4CBB63EB09F3DA38640BEA5410DFE5916A155627C18F3B90C39BD8`
- Screenshot: visible opening sky/wing route.
- Fatal-marker search: clean.

## Findings

`82287788` is real, but the first block-body capture inflated its apparent
cost. With block-body instrumentation on, final rows showed:

- `82282490`: `body_ticks_total=65625046`, `ticks_per_entry=365`,
  `code_size=87168`
- `82287788`: `body_ticks_total=13383638`, `ticks_per_entry=20`,
  `code_size=46484`

The lower-noise call-edge run is more useful for speed direction:

- `82282490`: `body_ticks_total=31584286`, `ticks_per_entry=148`,
  `code_size=87168`
- `82287788`: `body_ticks_total=3659269`, `ticks_per_entry=5`,
  `code_size=35380`
- `821CE028`: `body_ticks_total=675182`, `ticks_per_entry=2`,
  `code_size=3756`
- `82287788 -> 821CE028`: `body_ticks_total=1147798`,
  `calls_total=340310`, `ticks_per_call=3`

So `82287788` is still a meaningful child lane, but it is not bigger than the
recursive `82282490` wall. Its only dynamic direct child captured here is
`821CE028`, and that edge explains about 31% of `82287788` body time in the
lower-noise run. The remainder points back to local state/vector/FPR lowering.

The focused HIR report for `82287788`:

- `hir_lines=3043`
- `store_context=568`
- `load_context=455`
- `permutes=132`
- `dot_product_4=12`
- `context_barriers=52`
- `branches=112`
- `calls=6`

The dynamic block-slice/codegen audit says the top local span is:

- slice: `822877BC-82287B38`
- instrumented block body: `1173620`
- entries in final interval: `230521`
- PPC shape: `addi:59`, `addis:23`, `lfs:13`, `lvlx:10`, `lvrx:10`,
  `vor:10`, `stfs:8`, `stvx:8`, `lvx128:6`, `vmaddfp:6`, `vspltw:6`
- HIR shape: `store_context:186`, `load_context:145`, `and:105`, `add:93`,
  `truncate:46`, `load.1:44`, `permute:33`, `store.1:22`,
  `context_barrier:15`
- lowering floor: `extract=6` all constant, `splat=10`, `mul_add_v128=6`,
  `load1=44`, `store1=22`

The static direct-call sites in `82287788` are:

- `8228778C -> 826BFC78`
- `82287854 -> 821CE028`
- `8228786C -> 82287EE0`
- `82287ED4 -> 821CE028`
- `82287EE4 -> 826BFC78`
- tail branches to `826BFCC8`

Only the `822877BC -> 821CE028` edge appeared in the dynamic edge rows for the
opening route, so do not assume the other static calls are important without a
new route capture.

## Decision

Do not restart the exact `stvewx` lane-fold A/B, broad `822824F0` vector
peephole, broad VMX dot fastpath, or CR compare/branch fusion. The CR
compare/barrier fusion and CR-store elision cvars are known Blue Dragon crash
lanes from `docs/research/20260521-153300-a64-context-cache-cr-branch-negative.md`.

Next safe lane:

1. Keep `arm64_blue_dragon_stvewx_stack_lane_fastpath` default-off.
2. Keep `arm64_vmx_dot_f32_fastpath`,
   `arm64_cr_compare_branch_across_context_barrier`, and
   `arm64_cr_store_elide_for_fused_branch` default-off.
3. Before codegen edits, create or run an exact `822877BC-82287B38` state/vector
   reduction audit that separates:
   - CR6 compare/store/barrier traffic that cannot use the rejected fusion path;
   - constant `extract`/`splat` shapes;
   - `lvlx/lvrx/vor/stvx` stack/vector shuffle pairs;
   - `vmaddfp` cost that must not use the rejected broad dot shortcut.
4. If patching next, make it default-off and function/span-gated to `82287788`
   / `822877BC-82287B38`, with route-clean proof before any quiet A/B.

