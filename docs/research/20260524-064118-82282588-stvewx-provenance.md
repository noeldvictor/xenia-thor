# 2026-05-24 06:41 EDT - 82282588 STVEWX Provenance

## Goal

Close the remaining `82282588` dynamic-`EXTRACT_I32` provenance gap in the
body-backed `8228252C-822825C4` Blue Dragon span without changing generated
code yet.

## Inputs

- HIR dump: `scratch/thor-debug/20260521-170941-speed-logcat.txt`
- Route-clean block/call-edge profile:
  `scratch/thor-debug/20260524-030450-speed-logcat.txt`
- Prior lane note:
  `docs/research/20260524-034103-8228252c-stvewx-lane-provenance.md`
- Current span estimate:
  `docs/research/20260524-063109-82282490-span-instruction-estimate.md`

## Tool Update

`tools/thor/thor_hir_stvewx_lane_audit.ps1` now accepts:

```powershell
-ProvenanceStartGuest <guest_pc>
```

This lets the audit keep the measured target slice as `8228252C-822825C4`
while starting symbolic provenance earlier. The tool now also forwards simple
`store_context` -> later `load_context` expressions inside the provenance
window, with conservative kills on `context_barrier` and `call*`.

## Commands

Baseline target-only audit, unchanged:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_stvewx_lane_audit.ps1 -LogPath scratch\thor-debug\20260521-170941-speed-logcat.txt -Function 82282490 -StartGuest 8228252C -EndGuest 822825C4 -BlockProfileLog scratch\thor-debug\20260524-030450-speed-logcat.txt
```

Provenance-aware audit:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_stvewx_lane_audit.ps1 -LogPath scratch\thor-debug\20260521-170941-speed-logcat.txt -Function 82282490 -StartGuest 8228252C -ProvenanceStartGuest 82282514 -EndGuest 822825C4 -BlockProfileLog scratch\thor-debug\20260524-030450-speed-logcat.txt
```

## Result

The target-only audit still reports `82282588` as unknown, as expected:

```text
pc=82282588 result=unknown lane=- address=(r[6]+0x8)&~3
```

The provenance-aware audit proves all three `stvewx` dynamic extract lanes:

| PC | Result | Lane | Provenance |
| --- | --- | ---: | --- |
| `82282580` | proven | `0` | `r1 + 0x50`, masked with `~3` |
| `82282584` | proven | `1` | `r1 + 0x54`, masked with `~3` |
| `82282588` | proven | `2` | `r6 = r1 + 0x50`, then `r6 + 0x8`, masked with `~3` |

Exact `82282588` chain:

```text
v127.i64<gpr1> = load_context +40
v148.i64<gpr1> = add v127.i64<gpr1>, 50
store_context +80, v148.i64<gpr1>
v270.i64<gpr0> = load_context +80
v272.i64<gpr0> = add v270.i64<gpr0>, 8
v274.i64<gpr0> = and v272.i64<gpr0>, -4
```

Under the normal PPC stack-pointer 16-byte alignment assumption, that makes
`82282588` lane `2`.

## Decision

This is stronger provenance for the larger `8228252C-822825C4` lane, not a
speed win yet. The old two-site `stvewx` fold A/B should still not be repeated
unchanged.

The next behavior-changing experiment, if chosen, should be a new default-off
Blue-Dragon/function/PC-gated `EXTRACT_I32` fastpath that covers all three
proven sites:

- `82282580 -> lane 0`
- `82282584 -> lane 1`
- `82282588 -> lane 2`

It should keep the existing audit/counter style, prove route-clean behavior
first, and only then run a quiet same-APK speed A/B. If this still misses, the
next lane should move to `MUL_ADD_V128` cost rather than another narrow
`stvewx` tweak.
