# 8227F1D8 Call-Edge Split

Date: 2026-05-23 15:32 EDT

## Goal

Split the call-heavy `8227F1D8` entry block after the prior filtered HIR and
block body-time capture showed its wall was inclusive call work rather than a
safe local peephole.

## Capture

- Capture prefix: `scratch/thor-debug/20260523-152754-*`
- Logcat: `scratch/thor-debug/20260523-152754-speed-logcat.txt`
- Screenshot: `scratch/thor-debug/20260523-152754-screenshot.png`
- HEAD: `42ba0c59a`
- APK SHA-256:
  `962D3086F4030D9BD5A9D46AF5E8DFA4A320A13BFCD14135B8B077AECDC31CC5`
- Route: visible opening sky/wing scene
- Fatal-marker search: clean

Launch lane:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 25 -Arm64SpeedProfileMinDelta 1 -A64LseKernelLockFastpaths true -A64RtlLeaveFastpathAudit false -Arm64PermuteI32ZipFastpath true -Arm64ContextPromotionGprLiveInR1 false -Arm64SpeedProfileBodyTimeFilter "8227F1D8,82490030,826BFC7C" -Arm64SpeedProfileBodyTimeAfterMs 120000 -Arm64SpeedProfileCallEdgeFilter "8227F1D8" -Arm64SpeedProfileThreadSnapshotOnIdle true -StopAppAfterCapture true
```

## Results

The capture reached the same visible opening route and the dynamic edge rows
landed. The active child edge is `8227F1D8 -> 82490030`; `826BFC7C` did not
appear as a meaningful body-time or call-edge row in this route.

Final delayed body-time rows:

```text
8227F1D8 body_ticks_delta=712900 body_ticks_total=4117139 entries_delta=9610 ticks_per_entry=74 code_size=24756
82490030 body_ticks_delta=678424 body_ticks_total=4007328 entries_delta=9610 ticks_per_entry=70 code_size=6716
8227EFC8 body_ticks_delta=11228 body_ticks_total=35676 entries_delta=2138 ticks_per_entry=5 code_size=25752
```

Final dynamic call-edge row:

```text
8227F1D8 edge=1 block=8227F1D8 target=82490030 calls_delta=9610 calls_total=32107 body_ticks_delta=693438 body_ticks_total=4054641 ticks_per_call=72
```

Earlier intervals show the same shape:

```text
target=82490030 calls_total=5783 body_ticks_total=643708 ticks_per_call=149
target=82490030 calls_total=13329 body_ticks_total=1823790 ticks_per_call=156
target=82490030 calls_total=22497 body_ticks_total=3361203 ticks_per_call=167
```

Final perf still points at CPU/JIT rather than a saturated GPU path:

```text
Main Thread 103%
XMA Decoder 11.5%
Audio Worker 7.6%
GPU Commands 3.8%
Draw Thread 0.0%
```

Android UI frame stats are not reliable game FPS here. The final `gfxinfo`
sample only reported `Total frames rendered: 3`, and the logcat Choreographer
lines are Android UI scheduling symptoms, not a Xenia route FPS counter.

## Decision

Do not write a local `8227F1D8` peephole yet. The child call edge almost fully
explains the parent body-time:

- Parent final `8227F1D8 body_ticks_total`: `4117139`
- Child final `82490030 body_ticks_total`: `4007328`
- Edge final `8227F1D8 -> 82490030 body_ticks_total`: `4054641`

Next useful slice:

1. Run a route-stabilized filtered HIR plus delayed body/block-time capture for
   `82490030`.
2. Keep `8227F1D8` in the body-time filter as the parent comparator.
3. Do not chase `826BFC7C` until a fresh route shows it dynamically hot.
4. Keep broad GPR/cache/vector/CR experiments default-off.

Suggested capture:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 25 -Arm64SpeedProfileMinDelta 1 -A64LseKernelLockFastpaths true -A64RtlLeaveFastpathAudit false -Arm64PermuteI32ZipFastpath true -Arm64ContextPromotionGprLiveInR1 false -DisassembleFunctions true -DisassembleFunctionFilter 82490030 -Arm64SpeedProfileBodyTimeFilter "8227F1D8,82490030" -Arm64SpeedProfileBodyTimeAfterMs 120000 -Arm64SpeedProfileBlockFilter "82490030" -Arm64SpeedProfileBlockBodyTime true -Arm64SpeedProfileThreadSnapshotOnIdle true -StopAppAfterCapture true
```
