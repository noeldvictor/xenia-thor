# 82490030 Control Black-Idle

Date: 2026-05-23 15:50 EDT

## Goal

Run the no-disassembly delayed body-time control for `8227F1D8,82490030` on
the same APK and route after the filtered `82490030` capture black-idled. This
tests whether the previous stall was caused by the HIR/block dump path or by a
broader route-stability / owner-thread issue.

## Capture

- Capture prefix: `scratch/thor-debug/20260523-154626-*`
- Starting HEAD: `f6f4c13ca`
- APK SHA-256:
  `962D3086F4030D9BD5A9D46AF5E8DFA4A320A13BFCD14135B8B077AECDC31CC5`
- Target:
  `/storage/2664-21DE/roms/xbox360/Blue Dragon.m3u/Blue Dragon (USA, Europe) (En,Fr) (Disc 1).iso`
- Route result: black-idle before visible opening route progress
- Fatal-marker search: clean

Command:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 25 -Arm64SpeedProfileMinDelta 1 -A64LseKernelLockFastpaths true -A64RtlLeaveFastpathAudit false -Arm64PermuteI32ZipFastpath true -Arm64ContextPromotionGprLiveInR1 false -Arm64SpeedProfileBodyTimeFilter "8227F1D8,82490030" -Arm64SpeedProfileBodyTimeAfterMs 120000 -Arm64SpeedProfileThreadSnapshotOnIdle true -StopAppAfterCapture true
```

## Evidence

- No `8227F1D8` or `82490030` body-time rows landed after the delayed
  body-time profiler activated.
- Final screenshot was black.
- Speed counters showed early activity, then a hard flatline:
  - `15:46:45`: `entry_delta=3843047`
  - `15:47:00`: `entry_delta=189129`
  - `15:47:15` onward: `entry_delta=0`
- Body-time activated at `15:46:30` for `8227F1D8,82490030`, but the run
  never reached the comparable parent/child rows seen in the previous
  route-clean call-edge capture.
- Idle attribution repeated the zombied owner-hint signature:
  - `last_global_owner_sys_tid=20126`
  - `last_global_owner_thread_id=F80002E8`
  - `owner_hint=hit`
  - `owner_hint_source=thread_id_or_handle`
  - `owner_hint_sys_tid=946810032`
  - `owner_hint_tid=00000016`
  - `owner_hint_handle=F80002E8`
  - `owner_hint_state=zombie`
- Final perf sample was idle, not speed evidence: Main Thread `0.0%`,
  GPU Commands `0.0%`, Draw Thread `0.0%`.

## Decision

The no-disassembly control black-idled with the same `F80002E8` zombied owner
signature as the filtered `82490030` run. That makes the current blocker route
stability / idle attribution, not `82490030` codegen.

Do not rerun the exact filtered `82490030` capture or the exact no-disassembly
control unchanged. Do not patch generated code from this evidence.

Next useful slice: improve zombie owner/native TID attribution before changing
guest generated-code behavior. The next patch should make the idle skip line
answer whether `last_global_owner_sys_tid` maps to a live Android native thread
under `/proc/<pid>/task/<tid>` and should separate that live native owner from
the possibly stale `owner_hint_sys_tid` cached on the zombied XThread.
