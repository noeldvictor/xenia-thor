# 82282490 Top Span Comparison

Date: 2026-05-24 05:51 EDT

## Question

The current `82282490` local/exclusive ranking put `8228252C-822825C4`
first and `822824F0-82282528` second. This note compares both spans before a
new codegen patch so we do not repeat the narrow `stvewx` lane-fold A/B or
patch inclusive recursive time.

## Commands

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_a64_codegen_audit.ps1 -LogPath scratch\thor-debug\20260521-170941-speed-logcat.txt -Function 82282490 -StartGuest 8228252C -EndGuest 822825C4 -BlockProfileLog scratch\thor-debug\20260524-030450-speed-logcat.txt -Top 16
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_a64_codegen_audit.ps1 -LogPath scratch\thor-debug\20260521-170941-speed-logcat.txt -Function 82282490 -StartGuest 822824F0 -EndGuest 82282528 -BlockProfileLog scratch\thor-debug\20260524-030450-speed-logcat.txt -Top 16
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_span_reduction_audit.ps1 -LogPath scratch\thor-debug\20260521-170941-speed-logcat.txt -Function 82282490 -StartGuest 8228252C -EndGuest 822825C4 -BlockProfileLog scratch\thor-debug\20260524-030450-speed-logcat.txt -EdgeProfileLog scratch\thor-debug\20260524-030450-speed-logcat.txt -Top 16
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_span_reduction_audit.ps1 -LogPath scratch\thor-debug\20260521-170941-speed-logcat.txt -Function 82282490 -StartGuest 822824F0 -EndGuest 82282528 -BlockProfileLog scratch\thor-debug\20260524-030450-speed-logcat.txt -EdgeProfileLog scratch\thor-debug\20260524-030450-speed-logcat.txt -Top 16
```

## 8228252C-822825C4

The larger absolute local candidate still has the best approximate exclusive
total, but the safe single-lane patch is not obvious.

- `body_ticks_total=8530471`
- `edge_targets=82287788:5653971`
- `approx_exclusive=2876500`
- `exclusive_pct=33.72`
- `entries_delta=170343`
- `ticks_per_entry=6`
- static shape: `vspltw:6`, `lvx128:4`, `stvewx:3`, `vmaddfp:3`,
  `store_context:27`, `load_context:14`, `extract:9`, `splat:6`

The span auditor still recommends `next_lane=no_patch_yet`. The main tempting
lanes are either already rejected or too broad for a safe one-variable patch:

- CR6 branch-chain fusion is closed for now because previous CR
  compare/barrier fusion and CR-store elision crashed or regressed this title.
- The exact `stvewx` stack-lane fastpath for `82282580` and `82282584` was
  route-clean but did not prove speed.
- The remaining state/vector/FPR surface is real, but it needs more
  provenance before a function/block-gated codegen toggle.

## 822824F0-82282528

The second-ranked span is smaller in absolute terms but has cleaner parent-side
share and a simpler shape.

- `body_ticks_total=3501617`
- `edge_targets=82274DB0:1480443`
- `approx_exclusive=2021174`
- `exclusive_pct=57.72`
- `entries_delta=209429`
- `ticks_per_entry=2`
- static shape: `lvlx:1`, `lvrx:1`, `load_vector_shl:2`, `permute:2`,
  `store_context:16`, `load_context:8`, `context_barrier:2`

The span auditor also recommends `next_lane=no_patch_yet`. The best narrow
question here is not another broad vector peephole; it is a provenance audit
for the exact `lvlx/lvrx -> load_vector_shl + permute -> v[0]` join in
`822824F0-82282528`.

## Decision

Do not patch generated-code behavior from this comparison alone.

`8228252C-822825C4` remains the top absolute local target, but the already
tested `stvewx` lane is closed and the remaining state/vector/FPR traffic needs
more proof. `822824F0-82282528` is now the cleaner offline audit target because
it has only two vector loads and two permutes around a concrete `lvlx/lvrx`
join. The next slice should add or run a focused provenance audit for that join
before a default-off function/span-gated codegen experiment.

Guardrails:

- Keep `arm64_blue_dragon_stvewx_stack_lane_fastpath` default-off and do not
  repeat the exact `82282580/82282584` A/B unchanged.
- Keep CR compare/barrier fusion and CR-store elision closed unless a new
  route-clean proof explains the old failures.
- Keep broad VMX-dot, broad GPR caches, and stale `822824F0` broad peepholes
  default-off outside targeted captures.
