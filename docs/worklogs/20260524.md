# Worklog 2026-05-24

## 00:00-00:12 EDT - 82490030 HIR/Block And Call-Edge Split

Starting HEAD: `0fe85f640`

Goal: resume route-stable hotpath profiling after the `ObjectTable` release
fix restored the Blue Dragon opening route, and decide whether `82490030` needs
a local codegen patch or another callee split.

Commands run:

```powershell
git status --short --branch
Get-CimInstance Win32_Process | Where-Object { $_.CommandLine -match 'thor_xenia_debug|gradle|ninja|adb|xenia|codex' } | Select-Object ProcessId,Name,CommandLine | Format-List
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_call_path_report.ps1 -LogPath scratch\thor-debug\20260524-000047-speed-logcat.txt -Function 82490030 -Phase OptHIR -BlockProfileLog scratch\thor-debug\20260524-000047-speed-logcat.txt -Top 30
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 25 -Arm64SpeedProfileMinDelta 1 -A64LseKernelLockFastpaths true -A64RtlLeaveFastpathAudit false -Arm64PermuteI32ZipFastpath true -Arm64ContextPromotionGprLiveInR1 false -Arm64SpeedProfileBodyTimeFilter "82490030,82486178,826BFC40,82485DD8,82486018" -Arm64SpeedProfileBodyTimeAfterMs 120000 -Arm64SpeedProfileCallEdgeFilter 82490030 -Arm64SpeedProfileThreadSnapshotOnIdle true -StopAppAfterCapture true
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_call_path_report.ps1 -LogPath scratch\thor-debug\20260524-000643-speed-logcat.txt -Function 82490030 -Phase OptHIR -BlockProfileLog scratch\thor-debug\20260524-000643-speed-logcat.txt -Top 20
git diff --check
```

Files changed:

- `tools/thor/thor_hir_call_path_report.ps1`
- `docs/research/20260524-001152-82490030-call-edge-split.md`
- `docs/worklogs/20260524.md`
- `AGENTS.md`
- `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md`
- `.agents/skills/xenia-working-fast-autonomy/SKILL.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `tools/thor/thor_codex_goal_loop.ps1`

Artifacts:

- `scratch/thor-debug/20260524-000047-*`
- `scratch/thor-debug/20260524-000643-*`

Findings:

- No overlapping xenia-thor build or capture was active at slice start.
- The prior `82490030` HIR/block capture reached the visible sky/wing route
  and showed `82490030` is state-traffic and call-heavy:
  `store_context=235`, `load_context=113`, `context_barrier=44`, and `18`
  calls.
- The new call-edge capture `scratch/thor-debug/20260524-000643-*` also
  reached the visible Blue Dragon sky/wing opening route.
- Fatal-marker search was clean.
- No idle-snapshot skip or global-owner line appeared.
- Final perf stayed CPU/JIT-heavy: Main Thread `88.4%`, XMA Decoder `42.3%`,
  GPU Commands `7.6%`, Draw Thread `3.8%`.
- `82490030` final body-time was `4650455` ticks, but child edges explain a
  large part of that wall.
- Dynamic target summary from `82490030`:
  - `0x82486178`: `body_ticks_total=2302621`, `calls_total=33218`
  - `0x82485DD8`: `body_ticks_total=804023`, `calls_total=39467`
  - `0x82486018`: `body_ticks_total=679426`, `calls_total=35462`
- `826BFC40` was present statically in the HIR-backed report but did not show
  as a meaningful dynamic body-time target in the route-clean call-edge split.
- Updated `tools/thor/thor_hir_call_path_report.ps1` so dynamic-only call-edge
  captures without HIR dumps produce a report instead of throwing.

Validation:

- `thor_hir_call_path_report.ps1` works on the no-disassembly dynamic-only
  `20260524-000643` capture.
- It still works on the HIR-backed `20260524-000047` capture.
- `git diff --check` produced only the existing Windows line-ending warning for
  the edited PowerShell script.

Decision:

- Do not patch local `82490030` generated code yet.
- Next target is a route-stabilized filtered HIR plus delayed block/body-time
  capture for `82486178`, with `82490030`, `82485DD8`, and `82486018` kept in
  the comparator body-time filter.

Blockers:

- No full-speed proof yet.
- Android UI frame stats remain unusable as game FPS; use quiet route captures
  and Xenia profiler data.

Next actions:

- Run the `82486178` filtered HIR/block capture from
  `docs/research/20260524-001152-82490030-call-edge-split.md`.
- If `82486178` is itself call-heavy, split its callees before a codegen patch.
- If `82486178` has a local block wall, gate the first experiment by function
  and block and keep rollback cvars default-off.

## 00:19-00:52 EDT - 82486178 VMX Copy-Loop Fastpath Probe

Starting HEAD: `52a8d4c6a`

Goal: test whether the local `82486178:8248627C` VMX copy loop from the
route-clean `82490030` child split can be replaced with a narrow default-off A64
fastpath.

Commands run:

```powershell
git status --short --branch
Test-Path scratch\thor-debug\latest-blue-dragon-full-speed-proof.json
Get-Content -Path AGENTS.md -TotalCount 220
Get-Content -Path .agents\skills\xenia-a64-speed-hotpath\SKILL.md -TotalCount 220
git diff --stat
git diff -- src/xenia/cpu/backend/a64/a64_emitter.cc
git diff -- src/xenia/cpu/backend/a64/a64_backend.cc tools/thor/thor_xenia_debug.ps1 android/android_studio_project/app/src/main/java/jp/xenia/emulator/EmulatorActivity.java src/xenia/cpu/backend/a64/a64_emitter.h
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 25 -Arm64SpeedProfileMinDelta 1 -A64LseKernelLockFastpaths true -A64RtlLeaveFastpathAudit false -Arm64PermuteI32ZipFastpath true -Arm64ContextPromotionGprLiveInR1 false -Arm64BlueDragonVmxCopyLoopFastpath true -Arm64SpeedProfileBodyTimeFilter "82490030,82486178,82485DD8,82486018" -Arm64SpeedProfileBodyTimeAfterMs 120000 -Arm64SpeedProfileBlockFilter 82486178 -Arm64SpeedProfileBlockBodyTime true -Arm64SpeedProfileThreadSnapshotOnIdle true -StopAppAfterCapture true
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 25 -Arm64SpeedProfileMinDelta 1 -A64LseKernelLockFastpaths true -A64RtlLeaveFastpathAudit false -Arm64PermuteI32ZipFastpath true -Arm64ContextPromotionGprLiveInR1 false -Arm64BlueDragonVmxCopyLoopFastpath false -Arm64SpeedProfileBodyTimeFilter "82490030,82486178,82485DD8,82486018" -Arm64SpeedProfileBodyTimeAfterMs 120000 -Arm64SpeedProfileBlockFilter 82486178 -Arm64SpeedProfileBlockBodyTime true -Arm64SpeedProfileThreadSnapshotOnIdle true -StopAppAfterCapture true
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 25 -Arm64SpeedProfileMinDelta 1 -A64LseKernelLockFastpaths true -A64RtlLeaveFastpathAudit false -Arm64PermuteI32ZipFastpath true -Arm64ContextPromotionGprLiveInR1 false -Arm64BlueDragonVmxCopyLoopFastpath true -Arm64SpeedProfileBodyTimeFilter "82490030,82486178,82485DD8,82486018" -Arm64SpeedProfileBodyTimeAfterMs 120000 -Arm64SpeedProfileThreadSnapshotOnIdle true -StopAppAfterCapture true
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 25 -Arm64SpeedProfileMinDelta 1 -A64LseKernelLockFastpaths true -A64RtlLeaveFastpathAudit false -Arm64PermuteI32ZipFastpath true -Arm64ContextPromotionGprLiveInR1 false -Arm64BlueDragonVmxCopyLoopFastpath false -Arm64SpeedProfileBodyTimeFilter "82490030,82486178,82485DD8,82486018" -Arm64SpeedProfileBodyTimeAfterMs 120000 -Arm64SpeedProfileThreadSnapshotOnIdle true -StopAppAfterCapture true
Get-ChildItem -Path scratch\thor-debug -File | Where-Object { $_.Name -match '^20260524-(003437|003840|004420|004802)-' } | Select-String -Pattern 'A64 body-time|A64 block body-time|Fatal signal|AndroidRuntime|VK_ERROR_DEVICE_LOST|GPU is hung|idle snapshot|last_global_owner|top_threads|Main Thread|GPU Commands|XMA Decoder|arm64_blue_dragon_vmx_copy_loop_fastpath|apk_sha|git_head'
```

Files changed:

- `android/android_studio_project/app/src/main/java/jp/xenia/emulator/EmulatorActivity.java`
- `src/xenia/cpu/backend/a64/a64_backend.cc`
- `src/xenia/cpu/backend/a64/a64_emitter.cc`
- `src/xenia/cpu/backend/a64/a64_emitter.h`
- `tools/thor/thor_xenia_debug.ps1`
- `docs/research/20260524-005156-82486178-vmx-copy-loop-fastpath.md`
- `docs/worklogs/20260524.md`
- `AGENTS.md`
- `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md`
- `.agents/skills/xenia-working-fast-autonomy/SKILL.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `tools/thor/thor_codex_goal_loop.ps1`

Artifacts:

- `scratch/thor-debug/20260524-003437-*`
- `scratch/thor-debug/20260524-003840-*`
- `scratch/thor-debug/20260524-004420-*`
- `scratch/thor-debug/20260524-004802-*`

Findings:

- The new cvar and Thor launcher flag are `arm64_blue_dragon_vmx_copy_loop_fastpath`
  / `-Arm64BlueDragonVmxCopyLoopFastpath`.
- The default-off fastpath is function/block gated to `82486178:8248627C`.
- NativeCore and FullDeploy passed.
- The one-trip version reached the visible opening route both on and off, and
  block `8248627C` was lower with the fastpath on (`2049969` vs `2485871`),
  but that version still re-entered the HIR block path per trip.
- The inner-loop version also reached the visible opening route both on and off
  on APK SHA
  `215678AC4C57B0D4DB7847311777CBF2A39910CE2A13000D668F0867A6A99019`.
- Inner-loop fastpath on final body-time: `82490030=3728643`,
  `82486178=2050839`, `82485DD8=631854`, `82486018=438763`.
- Inner-loop same-APK control off final body-time: `82490030=3468843`,
  `82486178=1727468`, `82485DD8=499551`, `82486018=571513`.
- Both inner-loop screenshots showed the visible Blue Dragon opening route, and
  fatal-marker searches were clean.

Validation:

- `NativeCore` passed.
- `FullDeploy -DeviceSerial c3ca0370` passed.
- Route-clean screenshots verified for both inner-loop fastpath on/off.

Decision:

- Keep `arm64_blue_dragon_vmx_copy_loop_fastpath` default-off.
- Do not repeat this exact `82486178` fastpath A/B unchanged.
- The route-clean result is useful, but the same-APK inner-loop A/B did not
  prove speed because the control had lower final `82486178` and `82490030`
  body-time totals.

Blockers:

- No full-speed proof marker yet.
- Current captures remain CPU/JIT-heavy; Android UI frame stats are still not
  reliable game FPS.

Next actions:

- Move to fresh evidence on remaining `82490030` children, especially
  `82485DD8` and `82486018`, before another codegen shortcut.
- Keep `arm64_blue_dragon_vmx_copy_loop_fastpath=false` outside explicit
  research rechecks.

## 00:57-01:15 EDT - 82485DD8 / 82486018 Capture And Word Loop Probe

Starting HEAD: `50da5a0a9`

Goal: use fresh evidence on the remaining `82490030` children and test the
simplest local loop if the capture identifies one.

Commands run:

```powershell
git status --short --branch
rg -n "DisassembleFunctionFilter|Arm64SpeedProfileBlockFilter|Split\(|Contains\(|filter" tools\thor\thor_xenia_debug.ps1 src\xenia\cpu\backend\a64
Get-CimInstance Win32_Process | Where-Object { $_.CommandLine -match 'thor_xenia_debug|thor_build|gradle|ninja|adb|xenia|codex' } | Select-Object ProcessId,Name,CommandLine | Format-List
adb devices
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 25 -Arm64SpeedProfileMinDelta 1 -A64LseKernelLockFastpaths true -A64RtlLeaveFastpathAudit false -Arm64PermuteI32ZipFastpath true -Arm64ContextPromotionGprLiveInR1 false -Arm64BlueDragonVmxCopyLoopFastpath false -DisassembleFunctions true -DisassembleFunctionFilter "82485DD8,82486018" -Arm64SpeedProfileBodyTimeFilter "82490030,82486178,82485DD8,82486018" -Arm64SpeedProfileBodyTimeAfterMs 120000 -Arm64SpeedProfileBlockFilter "82485DD8,82486018" -Arm64SpeedProfileBlockBodyTime true -Arm64SpeedProfileThreadSnapshotOnIdle true -StopAppAfterCapture true
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_call_path_report.ps1 -LogPath scratch\thor-debug\20260524-010013-speed-logcat.txt -Function 82485DD8 -Phase OptHIR -BlockProfileLog scratch\thor-debug\20260524-010013-speed-logcat.txt -Top 20
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_call_path_report.ps1 -LogPath scratch\thor-debug\20260524-010013-speed-logcat.txt -Function 82486018 -Phase OptHIR -BlockProfileLog scratch\thor-debug\20260524-010013-speed-logcat.txt -Top 20
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_hotpath_report.ps1 -LogPath scratch\thor-debug\20260524-010013-speed-logcat.txt -Function 82485DD8 -Phase OptHIR -Top 20
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_hotpath_report.ps1 -LogPath scratch\thor-debug\20260524-010013-speed-logcat.txt -Function 82486018 -Phase OptHIR -Top 20
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 25 -Arm64SpeedProfileMinDelta 1 -A64LseKernelLockFastpaths true -A64RtlLeaveFastpathAudit false -Arm64PermuteI32ZipFastpath true -Arm64ContextPromotionGprLiveInR1 false -Arm64BlueDragonVmxCopyLoopFastpath false -Arm64BlueDragonWordCopyLoopFastpath true -Arm64SpeedProfileBodyTimeFilter "82490030,82486178,82485DD8,82486018" -Arm64SpeedProfileBodyTimeAfterMs 120000 -Arm64SpeedProfileBlockFilter "82485DD8,82486018" -Arm64SpeedProfileBlockBodyTime true -Arm64SpeedProfileThreadSnapshotOnIdle true -StopAppAfterCapture true
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 25 -Arm64SpeedProfileMinDelta 1 -A64LseKernelLockFastpaths true -A64RtlLeaveFastpathAudit false -Arm64PermuteI32ZipFastpath true -Arm64ContextPromotionGprLiveInR1 false -Arm64BlueDragonVmxCopyLoopFastpath false -Arm64BlueDragonWordCopyLoopFastpath false -Arm64SpeedProfileBodyTimeFilter "82490030,82486178,82485DD8,82486018" -Arm64SpeedProfileBodyTimeAfterMs 120000 -Arm64SpeedProfileBlockFilter "82485DD8,82486018" -Arm64SpeedProfileBlockBodyTime true -Arm64SpeedProfileThreadSnapshotOnIdle true -StopAppAfterCapture true
```

Files changed:

- `android/android_studio_project/app/src/main/java/jp/xenia/emulator/EmulatorActivity.java`
- `src/xenia/cpu/backend/a64/a64_backend.cc`
- `src/xenia/cpu/backend/a64/a64_emitter.cc`
- `src/xenia/cpu/backend/a64/a64_emitter.h`
- `tools/thor/thor_xenia_debug.ps1`
- `docs/research/20260524-011500-82485dd8-word-copy-loop-fastpath.md`
- `docs/worklogs/20260524.md`
- `AGENTS.md`
- `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md`
- `.agents/skills/xenia-working-fast-autonomy/SKILL.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `tools/thor/thor_codex_goal_loop.ps1`

Artifacts:

- `scratch/thor-debug/20260524-010013-*`
- `scratch/thor-debug/20260524-010729-*`
- `scratch/thor-debug/20260524-011110-*`

Findings:

- Fresh HIR/body/block capture `20260524-010013` reached the visible opening
  route with clean fatal search. It showed `82485DD8` stronger than `82486018`:
  final `82485DD8=2039478`, `82486018=954041`.
- `82485DD8` block `82485E70` is a local word copy/update loop:
  `lwzu r10,4(r29)`, `addic. r11,-1`, `rldicr r31,1,62`,
  `stwu r10,4(r4)`, `bne cr0,82485E70`.
- Added `arm64_blue_dragon_word_copy_loop_fastpath`, default-off and gated to
  `82485DD8:82485E70`.
- Post-capture code review tightened the loop exit to branch on `w11` instead
  of `x11`, matching the HIR branch on the low 32-bit CR0 EQ result from
  `addic.`.
- NativeCore and FullDeploy passed.
- NativeCore and FullDeploy passed again after the `w11` branch-condition fix.
  The rebuilt APK SHA is
  `CB234AD454F2F458BB22E436F16CCDC7DBD8EA7510DDD8005B8AC2E2B9E0CA3C`.
- Same-APK fastpath-on capture `20260524-010729` reached the visible route with
  clean fatal search and APK SHA
  `B240F237AC9ED68637BA5152699A742F75A4F6AD7555CF61845F78FB50992965`.
- Same-APK control `20260524-011110` also reached the visible route with clean
  fatal search.
- Positive A/B:
  - `82485DD8`: `2307804` off -> `695645` on.
  - `82485E70`: `1122726` off -> `75310` on.
  - `82490030`: `5508640` off -> `4351286` on.

Validation:

- `NativeCore` passed.
- `FullDeploy -DeviceSerial c3ca0370` passed.
- Both fastpath-on and fastpath-off captures reached the visible opening route
  and had clean fatal-marker searches.

Decision:

- Keep `arm64_blue_dragon_word_copy_loop_fastpath` default-off for now, but
  treat it as a positive speed candidate.
- Do not combine it with `arm64_blue_dragon_vmx_copy_loop_fastpath` yet.

Blockers:

- Still no full-speed proof marker.
- Need a quieter same-APK A/B without block body-time profiler before promoting
  the word-loop toggle.

Next actions:

- Run quiet same-APK A/B for
  `arm64_blue_dragon_word_copy_loop_fastpath=false` then `true`, keeping body
  comparators but disabling block body-time and disassembly.
- If quiet evidence holds, consider enabling this in the Blue Dragon speed
  preset or making it default-on only under a title/route gate.

## 01:22-01:34 EDT - Quiet Word Loop A/B And Speed Preset Promotion

Starting HEAD: `11427e90c`

Goal: confirm whether the `82485DD8:82485E70` word copy-loop fastpath remains a
route-wide win without block body-time or disassembly noise.

Commands run:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 25 -Arm64SpeedProfileMinDelta 1 -A64LseKernelLockFastpaths true -A64RtlLeaveFastpathAudit false -Arm64PermuteI32ZipFastpath true -Arm64ContextPromotionGprLiveInR1 false -Arm64BlueDragonVmxCopyLoopFastpath false -Arm64BlueDragonWordCopyLoopFastpath false -Arm64SpeedProfileBodyTimeFilter "82490030,82486178,82485DD8,82486018" -Arm64SpeedProfileBodyTimeAfterMs 120000 -Arm64SpeedProfileThreadSnapshotOnIdle true -StopAppAfterCapture true
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 25 -Arm64SpeedProfileMinDelta 1 -A64LseKernelLockFastpaths true -A64RtlLeaveFastpathAudit false -Arm64PermuteI32ZipFastpath true -Arm64ContextPromotionGprLiveInR1 false -Arm64BlueDragonVmxCopyLoopFastpath false -Arm64BlueDragonWordCopyLoopFastpath true -Arm64SpeedProfileBodyTimeFilter "82490030,82486178,82485DD8,82486018" -Arm64SpeedProfileBodyTimeAfterMs 120000 -Arm64SpeedProfileThreadSnapshotOnIdle true -StopAppAfterCapture true
```

Files changed:

- `tools/thor/thor_xenia_debug.ps1`
- `docs/research/20260524-013145-82485dd8-word-loop-quiet-ab.md`
- `docs/worklogs/20260524.md`
- `AGENTS.md`
- `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md`
- `.agents/skills/xenia-working-fast-autonomy/SKILL.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `tools/thor/thor_codex_goal_loop.ps1`

Artifacts:

- `scratch/thor-debug/20260524-012352-*`
- `scratch/thor-debug/20260524-012713-*`

Findings:

- Both quiet captures reached the visible Blue Dragon opening sky/wing route.
- Fatal-marker search was clean across both captures.
- Control off final body-time:
  `82490030=3923146`, `82486178=1591407`, `82485DD8=1010794`,
  `82486018=647412`.
- Fastpath on final body-time:
  `82490030=3573598`, `82486178=1584856`, `82485DD8=774505`,
  `82486018=375573`.
- Final perf still shows CPU/JIT pressure, not a GPU wall: fastpath-on final
  sample had Main Thread `96.1%`, XMA Decoder `50.0%`, GPU Commands `7.6%`,
  Draw Thread `3.8%`.

Validation:

- Same APK SHA for both quiet runs:
  `CB234AD454F2F458BB22E436F16CCDC7DBD8EA7510DDD8005B8AC2E2B9E0CA3C`.
- Both screenshots show the same visible opening route.
- No fatal markers or idle owner lines were found.

Decision:

- Promote `Arm64BlueDragonWordCopyLoopFastpath` to the Blue Dragon A64 speed
  pack in `tools/thor/thor_xenia_debug.ps1`.
- Keep the backend cvar globally default-off.
- Keep `arm64_blue_dragon_vmx_copy_loop_fastpath=false`; do not combine the two
  copy-loop toggles without a separate matrix.

Blockers:

- Still no full-speed proof marker.
- Android UI frame stats still do not prove game FPS.

Next actions:

- Run a fresh delayed body-time rebaseline with the word-loop speed preset on.
- If `82490030` remains hot, re-split its children under the new preset.
- Otherwise move back to the larger `82282490` opening-scene wall.

## 01:36-01:42 EDT - Word Loop Preset Rebaseline

Starting HEAD: `bb20d68ee`

Goal: use the promoted Blue Dragon speed preset without an explicit word-loop
override and identify the next CPU/JIT wall.

Commands run:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 30 -Arm64SpeedProfileMinDelta 1 -A64LseKernelLockFastpaths true -A64RtlLeaveFastpathAudit false -Arm64PermuteI32ZipFastpath true -Arm64ContextPromotionGprLiveInR1 false -Arm64BlueDragonVmxCopyLoopFastpath false -Arm64SpeedProfileBodyTimeFilter "82282490,82281D28,82490030,82486178,82485DD8,82486018" -Arm64SpeedProfileBodyTimeAfterMs 120000 -Arm64SpeedProfileThreadSnapshotOnIdle true -StopAppAfterCapture true
```

Files changed:

- `docs/research/20260524-014050-word-loop-preset-rebaseline.md`
- `docs/worklogs/20260524.md`
- `AGENTS.md`
- `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md`
- `.agents/skills/xenia-working-fast-autonomy/SKILL.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `tools/thor/thor_codex_goal_loop.ps1`

Artifacts:

- `scratch/thor-debug/20260524-013649-*`

Findings:

- Metadata confirmed the speed preset now sends
  `arm64_blue_dragon_word_copy_loop_fastpath=true`.
- Screenshot reached the visible Blue Dragon opening sky/wing route.
- Fatal-marker search was clean and no idle owner lines were emitted.
- Final body-time rows:
  `82282490=25383515`, `82281D28=7175068`, `82490030=3480696`,
  `82282388=2082550`, `82282410=2004094`, `82486178=1358411`,
  `82485DD8=618538`, `82486018=457433`.
- Final perf still points at CPU/JIT: Main Thread `96.1%`, XMA Decoder `7.6%`,
  GPU Commands `3.8%`, Draw Thread `3.8%`.

Validation:

- Same APK SHA:
  `CB234AD454F2F458BB22E436F16CCDC7DBD8EA7510DDD8005B8AC2E2B9E0CA3C`.
- Route-clean screenshot checked.
- No fatal markers.

Decision:

- The word-loop preset is working; `82485DD8` is not the next main wall.
- Move back to the `82282490` opening-scene cluster under the current preset.

Blockers:

- Still no full-speed proof marker.

Next actions:

- Run delayed `82282490` block body-time under the current preset.
- If `822825E0 -> 82282490` still dominates, use call-edge/HIR callee analysis
  instead of returning to stale `82490030` children.

## 01:44-02:11 EDT - 82282490 Recursive Edge And Stack Sync Probe

Starting HEAD: `c702bd0c4`

Goal: rebaseline the `82282490` opening-scene wall under the word-loop speed
preset, then expose and test A64 host/guest stack synchronization as a
one-variable generated-code overhead probe.

Commands run:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 30 -Arm64SpeedProfileMinDelta 1 -A64LseKernelLockFastpaths true -A64RtlLeaveFastpathAudit false -Arm64PermuteI32ZipFastpath true -Arm64ContextPromotionGprLiveInR1 false -Arm64BlueDragonVmxCopyLoopFastpath false -Arm64SpeedProfileBodyTimeFilter "82282490,82281D28" -Arm64SpeedProfileBodyTimeAfterMs 120000 -Arm64SpeedProfileBlockFilter "82282490" -Arm64SpeedProfileBlockBodyTime true -Arm64SpeedProfileThreadSnapshotOnIdle true -StopAppAfterCapture true
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 30 -Arm64SpeedProfileMinDelta 1 -A64LseKernelLockFastpaths true -A64RtlLeaveFastpathAudit false -Arm64PermuteI32ZipFastpath true -Arm64ContextPromotionGprLiveInR1 false -Arm64BlueDragonVmxCopyLoopFastpath false -Arm64SpeedProfileBodyTimeFilter "82282490,82281D28" -Arm64SpeedProfileBodyTimeAfterMs 120000 -Arm64SpeedProfileCallEdgeFilter "82282490" -Arm64SpeedProfileThreadSnapshotOnIdle true -StopAppAfterCapture true
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_block_mix_report.ps1 -LogPath scratch\thor-debug\20260521-170941-speed-logcat.txt -Function 82282490 -Phase OptHIR -BlockProfileLog scratch\thor-debug\20260524-014437-speed-logcat.txt -Top 12
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_call_path_report.ps1 -LogPath scratch\thor-debug\20260521-170941-speed-logcat.txt -Function 82282490 -Phase OptHIR -BlockProfileLog scratch\thor-debug\20260524-014858-speed-logcat.txt -Top 12
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode ApkShellDeploy -DeviceSerial c3ca0370
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 30 -Arm64SpeedProfileMinDelta 1 -A64LseKernelLockFastpaths true -A64RtlLeaveFastpathAudit false -Arm64PermuteI32ZipFastpath true -Arm64ContextPromotionGprLiveInR1 false -Arm64BlueDragonVmxCopyLoopFastpath false -A64EnableHostGuestStackSynchronization false -Arm64SpeedProfileBodyTimeFilter "82282490,82281D28" -Arm64SpeedProfileBodyTimeAfterMs 120000 -Arm64SpeedProfileThreadSnapshotOnIdle true -StopAppAfterCapture true
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 30 -Arm64SpeedProfileMinDelta 1 -A64LseKernelLockFastpaths true -A64RtlLeaveFastpathAudit false -Arm64PermuteI32ZipFastpath true -Arm64ContextPromotionGprLiveInR1 false -Arm64BlueDragonVmxCopyLoopFastpath false -A64EnableHostGuestStackSynchronization true -Arm64SpeedProfileBodyTimeFilter "82282490,82281D28" -Arm64SpeedProfileBodyTimeAfterMs 120000 -Arm64SpeedProfileThreadSnapshotOnIdle true -StopAppAfterCapture true
```

Files changed:

- `android/android_studio_project/app/src/main/java/jp/xenia/emulator/EmulatorActivity.java`
- `tools/thor/thor_xenia_debug.ps1`
- `docs/research/20260524-021116-82282490-recursive-stack-sync-probe.md`
- `docs/worklogs/20260524.md`
- `AGENTS.md`
- `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md`
- `.agents/skills/xenia-working-fast-autonomy/SKILL.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `tools/thor/thor_codex_goal_loop.ps1`

Artifacts:

- `scratch/thor-debug/20260524-014437-*`
- `scratch/thor-debug/20260524-014858-*`
- `scratch/thor-debug/20260524-020019-*`
- `scratch/thor-debug/20260524-020408-*`

Findings:

- Block body-time confirmed the current wall is the recursive lane again:
  `822825E0` inside `82282490` reached `body_ticks_total=36823133`, while
  stale vector-ish `822824F0` was only `1394125`.
- Call-edge profiling split that lane: `822825E0 -> 82282490` reached
  `body_ticks_total=28602334`, `calls_total=117425`, and peak
  `ticks_per_call=1244`. The secondary edge was
  `822825C8 -> 8227FEE8` at `body_ticks_total=11951162`.
- Source inspection showed direct guest calls use generated-code `blr` when the
  target has machine code, so the hot recursive edge is not repeatedly entering
  through the host-to-guest thunk.
- Exposed `a64_enable_host_guest_stack_synchronization` through Android launch
  extras and Thor metadata while preserving the Blue Dragon speed-pack default
  as `true`.
- Same-APK stack-sync-off capture `20260524-020019` reached the visible opening
  sky/wing route with clean fatal search. Final `82282490=26462740`,
  `ticks_per_entry=119`, `code_size=85104`.
- Same-APK stack-sync-on control `20260524-020408` reached the same visible
  route with clean fatal search. Final `82282490=27192906`,
  `ticks_per_entry=131`, `code_size=87168`.

Validation:

- `ApkShellDeploy -DeviceSerial c3ca0370` passed.
- Both stack-sync A/B screenshots show the visible opening sky/wing route.
- Fatal-marker searches were clean for the block, call-edge, stack-sync-off,
  and stack-sync-on captures.

Decision:

- Keep `a64_enable_host_guest_stack_synchronization` exposed for Thor captures.
- Keep the speed-pack default at `true`; do not globally disable this correctness
  guard from a single modest A/B.
- Treat stack-sync-off as a promising default-off Blue Dragon route experiment,
  not a proven FPS fix.

Blockers:

- Still no full-speed proof marker.
- The current wall remains CPU/JIT-heavy in recursive `82282490`; Android UI
  frame stats still are not reliable game FPS proof.

Next actions:

- Run one route-matched stack-sync control sandwich or add a stackpoint/prolog
  overhead profiler before changing the Blue Dragon speed pack.
- If stack-sync-off repeats cleanly, consider a Blue Dragon-route-gated preset
  toggle with a rollback flag.
- Otherwise continue reducing recursive `822825E0 -> 82282490` call/prolog
  cost instead of returning to stale VMX/vector, r1 live-in, or call-edge-only
  probes.

## 02:18-02:23 EDT - Stack Sync Sandwich Follow-Up

Starting HEAD: `7f271cb5c`

Goal: avoid over-promoting the first stack-sync-off A/B by repeating the
experiment side of the sandwich on the same installed APK.

Commands run:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 30 -Arm64SpeedProfileMinDelta 1 -A64LseKernelLockFastpaths true -A64RtlLeaveFastpathAudit false -Arm64PermuteI32ZipFastpath true -Arm64ContextPromotionGprLiveInR1 false -Arm64BlueDragonVmxCopyLoopFastpath false -A64EnableHostGuestStackSynchronization false -Arm64SpeedProfileBodyTimeFilter "82282490,82281D28" -Arm64SpeedProfileBodyTimeAfterMs 120000 -Arm64SpeedProfileThreadSnapshotOnIdle true -StopAppAfterCapture true
```

Files changed:

- `docs/research/20260524-021116-82282490-recursive-stack-sync-probe.md`
- `docs/worklogs/20260524.md`
- `AGENTS.md`
- `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md`
- `.agents/skills/xenia-working-fast-autonomy/SKILL.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `tools/thor/thor_codex_goal_loop.ps1`

Artifacts:

- `scratch/thor-debug/20260524-021858-*`

Findings:

- Repeat stack-sync-off reached the visible opening sky/wing route with clean
  fatal-marker search.
- Metadata used HEAD `7f271cb5c`, APK SHA
  `5FD5488026AA7152ED86E352864EC7FA160BE9ADBE1E0D4B7B2827FF554E85CF`,
  `a64_enable_host_guest_stack_synchronization=false`, and
  `arm64_blue_dragon_word_copy_loop_fastpath=true`.
- Final `82282490=27192157`, `ticks_per_entry=146`, `code_size=85104`.
- Final `82281D28=7809349`, `ticks_per_entry=88`, `code_size=94020`.
- Final perf sample still points at CPU/JIT: Main Thread `96.1%`, XMA Decoder
  `42.3%`, GPU Commands `11.5%`, Draw Thread `3.8%`.

Decision:

- Stack-sync-off is route-clean and reduces generated code size, but the repeat
  did not preserve the first run's body-time win. Do not promote it to the Blue
  Dragon speed pack yet.
- Stop repeating unchanged stack-sync A/B captures.

Blockers:

- Still no full-speed proof marker.

Next actions:

- Add direct stackpoint/prolog overhead attribution, or continue with
  recursive `822825E0 -> 82282490` call/prolog cost reduction.
- Keep stack sync default-on until a lower-risk profiler proves a repeatable
  speed win.

## 02:34-02:40 EDT - Thor Launcher Android HID Default Fix

Starting HEAD: `f4bd054a5`

Goal: fix normal AYN Thor launcher game launches so the built-in controller
works through Xenia's Android HID bridge instead of the research-only `nop`
controller.

Commands run:

```powershell
adb devices
adb -s c3ca0370 shell getprop ro.product.model
adb -s c3ca0370 shell dumpsys input
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode ApkShellDeploy -DeviceSerial c3ca0370
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragon -DeviceSerial c3ca0370 -HidDriver android -LogLevel 2 -HideAndroidOsd false
adb -s c3ca0370 shell input keyevent 108
adb -s c3ca0370 shell input keyevent 96
```

Files changed:

- `android/android_studio_project/app/src/main/java/jp/xenia/emulator/LauncherActivity.java`
- `docs/research/20260524-023710-thor-launcher-android-hid-fix.md`
- `docs/worklogs/20260524.md`
- `AGENTS.md`
- `README.md`

Artifacts:

- `scratch/thor-debug/20260524-023537-thor-controller-android-hid-*`
- `scratch/thor-debug/20260524-023633-thor-controller-android-hid-info-*`

Findings:

- Android still reports the Thor built-in pad as `Odin Controller`, vendor
  `0x2020`, product `0x0111`, with `KEYBOARD | GAMEPAD | JOYSTICK` classes and
  `/system/usr/keylayout/Vendor_2020_Product_0111.kl`.
- `EmulatorActivity` already forwards Android gamepad key and motion events to
  native, and `AndroidInputDriver` already maps those events to XInput user 0.
- The normal launcher game-picker path was still forcing `hid=nop`, hiding the
  real Thor controls from guest `XamInput`.
- The launcher now uses `hid=android` for normal game launches.

Validation:

- `ApkShellDeploy -DeviceSerial c3ca0370` passed and streamed install
  succeeded.
- `LaunchBlueDragon -HidDriver android -LogLevel 2` logged the info line
  `Android HID: active as XInput controller 1`.
- Synthetic Android START/A keyevents logged native `Android HID: key` down/up
  rows.
- Fatal-marker search was clean:
  `Fatal signal=0`, `AndroidRuntime=0`, `VK_ERROR_DEVICE_LOST=0`,
  `GPU is hung=0`.

Decision:

- Normal handheld play tests should use `hid=android`.
- Keep `hid=nop` only for deterministic scripted input captures.

## 02:42-03:03 EDT - A64 Entry/Exit Profiler For Recursive Wall

Starting HEAD: `f400988b5`

Goal: add direct stackpoint/prolog/epilog attribution before changing the
stack synchronization default or chasing recursive call/prolog cost in
`822825E0 -> 82282490`.

Commands run:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 30 -Arm64SpeedProfileMinDelta 1 -A64LseKernelLockFastpaths true -A64RtlLeaveFastpathAudit false -Arm64PermuteI32ZipFastpath true -Arm64ContextPromotionGprLiveInR1 false -Arm64BlueDragonVmxCopyLoopFastpath false -Arm64SpeedProfileBodyTimeFilter "82282490,82281D28" -Arm64SpeedProfileEntryExitTimeFilter "82282490,82281D28" -Arm64SpeedProfileBodyTimeAfterMs 120000 -Arm64SpeedProfileThreadSnapshotOnIdle true -StopAppAfterCapture true
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_a64_entry_exit_report.ps1 -LogPath scratch\thor-debug\20260524-025000-speed-logcat.txt
```

Files changed:

- `src/xenia/cpu/backend/a64/a64_backend.cc`
- `src/xenia/cpu/backend/a64/a64_backend.h`
- `src/xenia/cpu/backend/a64/a64_emitter.cc`
- `src/xenia/cpu/backend/a64/a64_emitter.h`
- `src/xenia/cpu/backend/a64/a64_function.h`
- `android/android_studio_project/app/src/main/java/jp/xenia/emulator/EmulatorActivity.java`
- `tools/thor/thor_xenia_debug.ps1`
- `tools/thor/thor_continual_harness_review.ps1`
- `tools/thor/thor_a64_entry_exit_report.ps1`
- `docs/research/20260524-025544-a64-entry-exit-profiler.md`
- `docs/worklogs/20260524.md`
- `AGENTS.md`
- `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md`
- `.agents/skills/xenia-working-fast-autonomy/SKILL.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `tools/thor/thor_codex_goal_loop.ps1`

Artifacts:

- `scratch/thor-debug/20260524-025000-*`

Findings:

- Added default-off cvar `arm64_speed_profile_entry_exit_time_filter` and Thor
  flag `-Arm64SpeedProfileEntryExitTimeFilter`.
- Added entry/exit rows to the A64 speed profile and parser
  `tools/thor/thor_a64_entry_exit_report.ps1`.
- Route-clean capture `20260524-025000` reached the visible opening sky/wing
  scene with clean fatal-marker search.
- `82282490` entry/exit combined total was `273393` ticks over `186010`
  delayed-route entries, or `1.47` ticks/call. Body total was `29486579`
  ticks, so entry/exit was only `0.93%` of body total.
- `82281D28` entry/exit combined total was `134782` ticks over `87115`
  entries, or `1.547` ticks/call. Body total was `8624393` ticks, so
  entry/exit was only `1.56%` of body total.

Validation:

- `NativeCore` passed.
- `FullDeploy -DeviceSerial c3ca0370` passed and streamed install succeeded.
- Post-format `NativeCore` passed after changing entry/exit log output to
  per-1000-entry fields.
- Fatal-marker search was clean for capture `20260524-025000`.

Decision:

- Do not disable `a64_enable_host_guest_stack_synchronization` from the mixed
  stack-sync A/B evidence.
- Do not keep chasing stackpoint/prolog/epilog as the main Blue Dragon opening
  wall. The measured overhead is tiny versus body time.
- Next focus should be exclusive attribution inside `82282490` body/recursive
  child work, or fresh body-backed state-traffic reduction.

## 03:04-03:15 EDT - 82282490 Exclusive Callee Attribution

Starting HEAD: `079795cb8`

Goal: run same-route block body-time and call-edge timing together for
`82282490` so the recursive wall can be separated into child-call body time
versus local block work.

Commands run:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 30 -Arm64SpeedProfileMinDelta 1 -A64LseKernelLockFastpaths true -A64RtlLeaveFastpathAudit false -Arm64PermuteI32ZipFastpath true -Arm64ContextPromotionGprLiveInR1 false -Arm64BlueDragonVmxCopyLoopFastpath false -Arm64SpeedProfileBodyTimeFilter "82282490,82281D28,8227FEE8" -Arm64SpeedProfileBodyTimeAfterMs 120000 -Arm64SpeedProfileBlockFilter "82282490" -Arm64SpeedProfileBlockBodyTime true -Arm64SpeedProfileCallEdgeFilter "82282490" -Arm64SpeedProfileThreadSnapshotOnIdle true -StopAppAfterCapture true
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_call_path_report.ps1 -LogPath scratch\thor-debug\20260521-170941-speed-logcat.txt -Function 82282490 -BlockProfileLog scratch\thor-debug\20260524-030450-speed-logcat.txt -Top 12
```

Files changed:

- `docs/research/20260524-031017-82282490-exclusive-callee-attribution.md`
- `docs/worklogs/20260524.md`
- `AGENTS.md`
- `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md`
- `.agents/skills/xenia-working-fast-autonomy/SKILL.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `tools/thor/thor_codex_goal_loop.ps1`

Artifacts:

- `scratch/thor-debug/20260524-030450-*`

Findings:

- Capture reached the visible opening sky/wing route with clean fatal-marker
  search.
- Final `82282490` body total was `101054711` ticks, with profiler-inflated
  `code_size=112080`.
- Same-run dynamic direct-call edges account for `84969304` ticks, or
  `84.08%` of final `82282490` body ticks.
- The self-recursive `822825E0 -> 82282490` edge alone accounts for
  `72194274` ticks, about `71.44%` of final `82282490` body ticks and
  `98.32%` of the `822825E0` block body.
- `822825C8 -> 8227FEE8` is also almost fully callee work.
- The meaningful local candidates are now smaller: dynamic `8228252C` has
  about `2876500` approximate exclusive ticks and `822824F0` has about
  `2021174`, but `8228252C` needs a dynamic HIR/PPC slice because it lands
  inside the old `loc_822824F0` HIR region rather than as its own label.

Decision:

- Do not rerun this exact block/call-edge capture unchanged.
- Do not chase stackpoint/prolog/epilog or a generic recursive-call overhead
  fix from this evidence.
- Next add/run a dynamic block-slice report mapping block-body guests such as
  `8228252C` to exact `82282490` HIR/PPC spans, then choose a default-off
  function/block-gated state-traffic or lowering experiment from those
  body-backed local spans.

## 03:16-03:25 EDT - 82282490 Dynamic Block Slice Tool

Starting HEAD: `ff4f76cb5`

Goal: convert the latest `82282490` block-body guests into exact OptHIR/PPC
slices so the next A64 experiment can target a real local span instead of a
coarse HIR label.

Commands run:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_dynamic_block_slice_report.ps1 -LogPath scratch\thor-debug\20260521-170941-speed-logcat.txt -Function 82282490 -BlockProfileLog scratch\thor-debug\20260524-030450-speed-logcat.txt -Top 12
```

Files changed:

- `tools/thor/thor_hir_dynamic_block_slice_report.ps1`
- `docs/research/20260524-032005-82282490-dynamic-block-slice-report.md`
- `docs/worklogs/20260524.md`
- `AGENTS.md`
- `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md`
- `.agents/skills/xenia-working-fast-autonomy/SKILL.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `tools/thor/thor_codex_goal_loop.ps1`

Findings:

- New script maps generated-code block-body guests to exact HIR/PPC slices and
  joins body ticks, call-edge ticks, approximate exclusive ticks, PPC/HIR op
  counts, context slots, and memory/branch/call/barrier counts.
- `822825E0` remains the biggest inclusive block, but `98.32%` of it is the
  self-recursive `822825E0 -> 82282490` child edge.
- Top local slice is now `8228252C-822825C4`: body total `8530471`, edge body
  `5653971`, approximate exclusive `2876500`, with vector/FPR/state traffic
  (`vspltw:6`, `lvx128:4`, `stvewx:3`, `vmaddfp:3`, `store_context:27`,
  `load_context:14`, `extract:9`, `splat:6`).
- `822824F0-82282528` is the next local slice: body total `3501617`, edge body
  `1480443`, approximate exclusive `2021174`.

Decision:

- Do not patch from broad `loc_822824F0` evidence.
- Next run/generated-code audit should focus on the `8228252C-822825C4`
  vector/state-traffic slice before choosing a default-off function/block-gated
  codegen experiment.

## 03:26-03:40 EDT - 8228252C A64 Codegen Audit

Starting HEAD: `fa6e0ad41`

Goal: turn the top body-backed local `82282490` slice into a concrete A64
lowering audit before changing generated code.

Commands run:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_a64_codegen_audit.ps1 -LogPath scratch\thor-debug\20260521-170941-speed-logcat.txt -Function 82282490 -StartGuest 8228252C -EndGuest 822825C4 -BlockProfileLog scratch\thor-debug\20260524-030450-speed-logcat.txt -Top 12
```

Files changed:

- `tools/thor/thor_hir_a64_codegen_audit.ps1`
- `docs/research/20260524-033116-8228252c-a64-codegen-audit.md`
- `docs/worklogs/20260524.md`
- `AGENTS.md`
- `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md`
- `.agents/skills/xenia-working-fast-autonomy/SKILL.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `tools/thor/thor_codex_goal_loop.ps1`

Findings:

- New audit tool maps an exact HIR slice to A64 lowering anchors and counts the
  expensive generated-code shapes.
- `8228252C-822825C4` remains body-backed: body total `8530471`, child edge
  `8228252C -> 82287788` body `5653971`, approximate exclusive local work
  `2876500` (`33.72%`), final interval `ticks_per_entry=6`.
- Local lowering shape: `store_context:27`, `load_context:14`, `extract:9`,
  `splat:6`, `load.1:6`, `load_offset.1:4`, `stvewx:3`, and `vmaddfp:3`.
- All three `stvewx` instructions (`82282580`, `82282584`, `82282588`) lower as
  masked-address plus dynamic extract plus `store.1`. The first two appear to
  be stack-address stores from `r1 + 0x50` and `r1 + 0x54`; the third still
  needs `r6` provenance proof.

Decision:

- Do not restart the stale broad `822824F0` vector peephole.
- Next slice should either add an offline stack-lane/provenance audit for
  `82282580`, `82282584`, and `82282588`, or implement a default-off
  function/block-gated `stvewx` lane-folding probe only for the two proven
  `r1 + constant` stores, with explicit rollback and correctness logging.

## 03:38-03:49 EDT - 8228252C STVEWX Lane Provenance

Starting HEAD: `7ad22dbab`

Goal: prove which `stvewx` dynamic extract lanes in `8228252C-822825C4` are
constant before changing A64 generated-code behavior.

Commands run:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_stvewx_lane_audit.ps1 -LogPath scratch\thor-debug\20260521-170941-speed-logcat.txt -Function 82282490 -StartGuest 8228252C -EndGuest 822825C4 -BlockProfileLog scratch\thor-debug\20260524-030450-speed-logcat.txt
```

Files changed:

- `tools/thor/thor_hir_stvewx_lane_audit.ps1`
- `docs/research/20260524-034103-8228252c-stvewx-lane-provenance.md`
- `docs/worklogs/20260524.md`
- `AGENTS.md`
- `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md`
- `.agents/skills/xenia-working-fast-autonomy/SKILL.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `tools/thor/thor_codex_goal_loop.ps1`

Findings:

- The new offline audit proves two of the three `stvewx` dynamic extracts under
  the normal PPC `r1` 16-byte stack-alignment assumption.
- `82282580` is `r1 + 0x50`, masked with `~3`, so the extracted lane is `0`.
- `82282584` is `r1 + 0x54`, masked with `~3`, so the extracted lane is `1`.
- `82282588` remains unknown because its address is `r6 + 0x8`; do not fold
  this one without separate provenance proof or runtime audit.

Decision:

- Next code slice can implement a default-off, Blue-Dragon/function/PC-gated
  A64 `EXTRACT_I32` fastpath for only `82282580 -> lane 0` and
  `82282584 -> lane 1`.
- Include a rollback cvar and correctness/audit evidence before a quiet speed
  A/B. Do not include `82282588` in the first probe.

## 03:49-04:04 EDT - 8228252C STVEWX Lane Fastpath Probe

Starting HEAD: `c8240eafd`

Goal: turn the two proven stack-lane `stvewx` dynamic extracts into a
default-off A64 `EXTRACT_I32` fastpath with audit counters, then check whether
the quiet route shows a speed win.

Commands run:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 25 -Arm64SpeedProfileMinDelta 1 -Arm64SpeedProfileBodyTimeFilter "82282490" -Arm64SpeedProfileBodyTimeAfterMs 120000 -Arm64SpeedProfileBlockFilter "82282490" -Arm64SpeedProfileBlockBodyTime true -Arm64BlueDragonStvewxStackLaneFastpath true -Arm64BlueDragonStvewxStackLaneAudit true
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 25 -Arm64SpeedProfileMinDelta 1 -Arm64SpeedProfileBodyTimeFilter "82282490" -Arm64SpeedProfileBodyTimeAfterMs 120000 -Arm64BlueDragonStvewxStackLaneFastpath false -Arm64BlueDragonStvewxStackLaneAudit false
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 25 -Arm64SpeedProfileMinDelta 1 -Arm64SpeedProfileBodyTimeFilter "82282490" -Arm64SpeedProfileBodyTimeAfterMs 120000 -Arm64BlueDragonStvewxStackLaneFastpath true -Arm64BlueDragonStvewxStackLaneAudit false
```

Files changed:

- `src/xenia/cpu/backend/a64/a64_backend.cc`
- `src/xenia/cpu/backend/a64/a64_backend.h`
- `src/xenia/cpu/backend/a64/a64_seq_vector.cc`
- `tools/thor/thor_xenia_debug.ps1`
- `android/android_studio_project/app/src/main/java/jp/xenia/emulator/EmulatorActivity.java`
- `docs/research/20260524-040404-blue-dragon-stvewx-lane-fastpath.md`
- `docs/worklogs/20260524.md`
- `AGENTS.md`
- `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md`
- `.agents/skills/xenia-working-fast-autonomy/SKILL.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `tools/thor/thor_codex_goal_loop.ps1`

Findings:

- NativeCore and FullDeploy passed. APK SHA:
  `A4CB9C0E8F4CBB63EB09F3DA38640BEA5410DFE5916A155627C18F3B90C39BD8`.
- Audit capture `scratch/thor-debug/20260524-035227-*` reached the visible
  opening sky/wing route with clean fatal-marker search.
- Final audit row was `fastpath=446272/1338814 fallback=0/0`; the folded lanes
  are dynamically stable for the two proven PCs.
- Quiet control `scratch/thor-debug/20260524-035623-*` reached
  `Microsoft Game Studios Presents`, had clean fatal-marker search, and kept
  `82282490 code_size=87168`.
- Quiet fastpath-on `scratch/thor-debug/20260524-035944-*` reached an earlier
  opening sky/wing frame, had clean fatal-marker search, and shrank
  `82282490 code_size` to `87088`.

Decision:

- Keep `arm64_blue_dragon_stvewx_stack_lane_fastpath` and audit default-off.
- Count this as route-clean/code-size evidence only, not a speed win.
- Do not repeat the exact quiet A/B unchanged.
- Next target should be larger body-backed work in `8228252C-822825C4`,
  especially the `8228252C -> 82287788` child path or broader state/vector/FPR
  traffic, rather than another tiny lane-fold run.

## 04:07-04:25 EDT - 82287788 Focused Capture And Callee Split

Starting HEAD: `05601d81d`

Goal: follow the body-backed `8228252C -> 82287788` child path after the
`stvewx` lane fastpath proved route-clean but not faster.

Commands run:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 25 -Arm64SpeedProfileMinDelta 1 -DisassembleFunctions true -DisassembleFunctionFilter 82287788 -Arm64SpeedProfileBodyTimeFilter "82282490,82287788" -Arm64SpeedProfileBodyTimeAfterMs 120000 -Arm64SpeedProfileBlockFilter 82287788 -Arm64SpeedProfileBlockBodyTime true
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_hotpath_report.ps1 -LogPath scratch\thor-debug\20260524-041413-speed-logcat.txt -Function 82287788 -Phase OptHIR -Top 25
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_dynamic_block_slice_report.ps1 -LogPath scratch\thor-debug\20260524-041413-speed-logcat.txt -Function 82287788 -BlockProfileLog scratch\thor-debug\20260524-041413-speed-logcat.txt -Top 12
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 25 -Arm64SpeedProfileMinDelta 1 -Arm64SpeedProfileBodyTimeFilter "82282490,82287788,821CE028,82287EE0,826BFC78,826BFCC8" -Arm64SpeedProfileBodyTimeAfterMs 120000 -Arm64SpeedProfileCallEdgeFilter 82287788
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_a64_codegen_audit.ps1 -LogPath scratch\thor-debug\20260524-041413-speed-logcat.txt -Function 82287788 -StartGuest 822877BC -EndGuest 82287B38 -BlockProfileLog scratch\thor-debug\20260524-041413-speed-logcat.txt -Top 16
```

Files changed:

- `docs/research/20260524-042555-82287788-focused-callee-split.md`
- `docs/worklogs/20260524.md`
- `AGENTS.md`
- `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md`
- `.agents/skills/xenia-working-fast-autonomy/SKILL.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `tools/thor/thor_codex_goal_loop.ps1`

Artifacts:

- `scratch/thor-debug/20260524-041413-*`
- `scratch/thor-debug/20260524-042031-*`

Findings:

- Both captures reached the visible opening sky/wing route with clean
  fatal-marker searches on APK SHA
  `A4CB9C0E8F4CBB63EB09F3DA38640BEA5410DFE5916A155627C18F3B90C39BD8`.
- The block-body run showed `82287788 body_ticks_total=13383638`, but this was
  instrumentation-heavy (`code_size=46484`).
- The lower-noise call-edge split showed `82287788 body_ticks_total=3659269`,
  `ticks_per_entry=5`, and `code_size=35380`, while `82282490` remained the
  main wall at `31584286`.
- Dynamic call-edge rows showed only one meaningful child:
  `82287788 -> 821CE028` with `body_ticks_total=1147798`,
  `calls_total=340310`, and `ticks_per_call=3`.
- The top local span is `822877BC-82287B38`: `store_context=186`,
  `load_context=145`, `context_barrier=15`, `lvlx/lvrx/vor/stvx` vector
  assembly, `lvx128:6`, `vmaddfp:6`, `vspltw:6`, `extract=6` all constant,
  and `splat=10`.

Decision:

- `82287788` is a real secondary child lane, but not bigger than recursive
  `82282490`.
- Do not revive the known-crashy CR compare/barrier fusion or CR-store elision
  cvars, and do not restart broad VMX-dot or stale `822824F0` peepholes.
- Next patch should be preceded by a focused `822877BC-82287B38`
  state/vector-reduction audit, then a default-off function/span-gated
  experiment only if the audit identifies a semantics-safe lowering.

## 04:35-04:46 EDT - 822877BC Span Reduction Audit

Starting HEAD: `9d8f46262`

Goal: decide whether the body-backed `82287788` parent span
`822877BC-82287B38` deserves a local A64 state/vector patch, or whether its
body time is actually inclusive callee work.

Commands run:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_span_reduction_audit.ps1 -LogPath scratch\thor-debug\20260524-041413-speed-logcat.txt -Function 82287788 -StartGuest 822877BC -EndGuest 82287B38 -BlockProfileLog scratch\thor-debug\20260524-041413-speed-logcat.txt -EdgeProfileLog scratch\thor-debug\20260524-042031-speed-logcat.txt -Top 20
```

Files changed:

- `tools/thor/thor_hir_span_reduction_audit.ps1`
- `docs/research/20260524-044223-822877bc-span-reduction-audit.md`
- `docs/worklogs/20260524.md`
- `AGENTS.md`
- `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md`
- `.agents/skills/xenia-working-fast-autonomy/SKILL.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `tools/thor/thor_codex_goal_loop.ps1`

Findings:

- The new audit combines filtered HIR, block-body rows, and separate call-edge
  rows so inclusive parent block time is not mistaken for local generated-code
  work.
- The exact span has `body_ticks_total=1173620`, while the dynamic child edge
  `82287788 -> 821CE028` accounts for `1147798` body ticks over `340310`
  calls.
- Approximate parent-exclusive body is only `25822` ticks, or `2.2%`.
- The parent span still has large visible traffic (`store_context=186`,
  `load_context=145`, CR6 stores, `lvlx/lvrx/vor/stvx`, `load_vector_shl=20`,
  `load_vector_shr=12`, `permute=33`, `permute.2=11`), but this is not enough
  exclusive-body proof for a parent-side patch.

Decision:

- Do not patch `822877BC-82287B38` locally yet.
- Keep CR compare/barrier fusion, CR-store elision, broad VMX-dot, stale
  `822824F0`, broad GPR caches, and the exact stvewx lane-fold A/B closed
  unless new evidence changes the premise.
- Next useful lane is a route-stabilized filtered HIR plus delayed
  body/block-time capture for `821CE028`, keeping `82282490` and `82287788`
  as body-time comparators.

## 04:49-05:00 EDT - 821CE028 Focused Capture

Starting HEAD: `3ac6b0e34`

Goal: follow the dynamic `82287788 -> 821CE028` edge with a route-stabilized
filtered HIR plus delayed body/block-time capture.

Commands run:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 25 -Arm64SpeedProfileMinDelta 1 -DisassembleFunctions true -DisassembleFunctionFilter 821CE028 -Arm64SpeedProfileBodyTimeFilter "82282490,82287788,821CE028" -Arm64SpeedProfileBodyTimeAfterMs 120000 -Arm64SpeedProfileBlockFilter 821CE028 -Arm64SpeedProfileBlockBodyTime true
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_hotpath_report.ps1 -LogPath scratch\thor-debug\20260524-045022-speed-logcat.txt -Function 821CE028 -Phase OptHIR -Top 25
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_dynamic_block_slice_report.ps1 -LogPath scratch\thor-debug\20260524-045022-speed-logcat.txt -Function 821CE028 -BlockProfileLog scratch\thor-debug\20260524-045022-speed-logcat.txt -Top 12
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_a64_codegen_audit.ps1 -LogPath scratch\thor-debug\20260524-045022-speed-logcat.txt -Function 821CE028 -StartGuest 821CE048 -BlockProfileLog scratch\thor-debug\20260524-045022-speed-logcat.txt -Top 16
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_span_reduction_audit.ps1 -LogPath scratch\thor-debug\20260524-045022-speed-logcat.txt -Function 821CE028 -StartGuest 821CE048 -EndGuest 821CE1CC -BlockProfileLog scratch\thor-debug\20260524-045022-speed-logcat.txt -Top 20
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_state_span_report.ps1 -LogPath scratch\thor-debug\20260524-045022-speed-logcat.txt -Function 821CE028 -Phase OptHIR -Top 20
```

Files changed:

- `tools/thor/thor_hir_span_reduction_audit.ps1`
- `docs/research/20260524-045602-821ce028-focused-capture.md`
- `docs/worklogs/20260524.md`
- `AGENTS.md`
- `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md`
- `.agents/skills/xenia-working-fast-autonomy/SKILL.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `tools/thor/thor_codex_goal_loop.ps1`

Artifacts:

- `scratch/thor-debug/20260524-045022-*`

Findings:

- Capture reached the visible opening sky/wing route with clean fatal-marker
  search on APK SHA
  `A4CB9C0E8F4CBB63EB09F3DA38640BEA5410DFE5916A155627C18F3B90C39BD8`.
- Final body rows were `82282490=37190996`, `82281D28=7446911`,
  `82287788=4992080`, and `821CE028=2523574`.
- `821CE028` emitted OptHIR and block-body rows. Top block
  `821CE048-821CE1CC` had `body_total=607573`, `entries_delta=125068`, and no
  child edge hiding inside it.
- The block is a repeated VMX compare/dot/CR reduction shape:
  `vmsum4fp128*:6`, `vcmpgefp.:6`, `vcmpgtfp.:6`, `vslw:6`, `vxor:6`,
  `mfcr:12`, and `rlwinm:13`.
- Span/state reports point at a possible narrow `r10` state-traffic audit
  (`score=26`), but CR store/barrier work and broad VMX-dot remain rejected
  lanes.

Decision:

- Do not patch `821CE028` immediately. It is route-clean and real, but it is
  the fourth body row and the best local block is much smaller than
  `82282490` and `82281D28`.
- If staying on `821CE028`, do only an offline/function-gated `r10` lifetime
  audit first.
- Prefer a larger next target: route-stabilized filtered HIR plus delayed
  body/block-time capture for `82281D28`, keeping `82282490`, `82287788`, and
  `821CE028` as comparators.

## 05:03-05:12 EDT - 82281D28 Focused Capture

Starting HEAD: `281217dbb`

Goal: run the larger `82281D28` filtered HIR plus delayed body/block-time
capture requested by the previous slice, keeping `82282490`, `82287788`, and
`821CE028` as body-time comparators.

Commands run:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 25 -Arm64SpeedProfileMinDelta 1 -DisassembleFunctions true -DisassembleFunctionFilter 82281D28 -Arm64SpeedProfileBodyTimeFilter "82282490,82281D28,82287788,821CE028" -Arm64SpeedProfileBodyTimeAfterMs 120000 -Arm64SpeedProfileBlockFilter 82281D28 -Arm64SpeedProfileBlockBodyTime true
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_hotpath_report.ps1 -LogPath scratch\thor-debug\20260524-050427-speed-logcat.txt -Function 82281D28 -Phase OptHIR -Top 30
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_dynamic_block_slice_report.ps1 -LogPath scratch\thor-debug\20260524-050427-speed-logcat.txt -Function 82281D28 -BlockProfileLog scratch\thor-debug\20260524-050427-speed-logcat.txt -Top 16
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_a64_codegen_audit.ps1 -LogPath scratch\thor-debug\20260524-050427-speed-logcat.txt -Function 82281D28 -StartGuest 8228233C -BlockProfileLog scratch\thor-debug\20260524-050427-speed-logcat.txt -Top 20
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_state_span_report.ps1 -LogPath scratch\thor-debug\20260524-050427-speed-logcat.txt -Function 82281D28 -Phase OptHIR -Top 25
```

Files changed:

- `docs/research/20260524-050931-82281d28-focused-capture.md`
- `docs/worklogs/20260524.md`
- `AGENTS.md`
- `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md`
- `.agents/skills/xenia-working-fast-autonomy/SKILL.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `tools/thor/thor_codex_goal_loop.ps1`

Artifacts:

- `scratch/thor-debug/20260524-050427-*`

Findings:

- Capture reached the visible opening sky/wing route with clean fatal-marker
  search on APK SHA
  `A4CB9C0E8F4CBB63EB09F3DA38640BEA5410DFE5916A155627C18F3B90C39BD8`.
- Final body rows put `82281D28` first at `body_ticks_total=65346716`,
  followed by `82282490=31615045`, `82282388=18965773`,
  `82282410=17323669`, `82287788=3277477`, and `821CE028=716173`.
- The top internal `82281D28` block row is `8228233C` with
  `body_ticks_total=46564347`, `entries_delta=22347`, and
  `ticks_per_entry=707`.
- The filtered HIR dump is large and `82282490`-like:
  `store_context=1438`, `load_context=779`, `dot_product_4=136`,
  `permutes=381`, `context_barriers=216`, and `calls=62`.
- The warning-level HIR log starts at `82282398`, while the hottest block is
  `8228233C`, so current offline slice/codegen tools cannot map the exact hot
  dynamic block from this capture.
- Visible HIR after the missing region shows a call to `0x826BF770` and a
  recursive call back to `0x82281D28`; large sibling/alias body rows
  `82282388` and `82282410` reinforce that this should be split by dynamic
  edge evidence before patching.

Decision:

- Do not patch `82281D28` generated code yet.
- Next run should be a route-stabilized call-edge split with delayed body-time:
  `-Arm64SpeedProfileBodyTimeFilter "82281D28,82282388,82282410,82282490,826BF770,82287788,821CE028"`
  and `-Arm64SpeedProfileCallEdgeFilter 82281D28`.

## 05:19-05:28 EDT - 82281D28 Call-Edge Split

Starting HEAD: `dc26cf2a4`

Goal: split the `82281D28` hot block/callee shape before any codegen patch,
because the previous HIR dump started after the hot `8228233C` block.

Commands run:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 25 -Arm64SpeedProfileMinDelta 1 -Arm64SpeedProfileBodyTimeFilter "82281D28,82282388,82282410,82282490,826BF770,82287788,821CE028" -Arm64SpeedProfileBodyTimeAfterMs 120000 -Arm64SpeedProfileCallEdgeFilter 82281D28
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_call_path_report.ps1 -LogPath scratch\thor-debug\20260524-052031-speed-logcat.txt -Function 82281D28 -BlockProfileLog scratch\thor-debug\20260524-052031-speed-logcat.txt -Top 20
```

Files changed:

- `docs/research/20260524-052524-82281d28-call-edge-split.md`
- `docs/worklogs/20260524.md`
- `AGENTS.md`
- `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md`
- `.agents/skills/xenia-working-fast-autonomy/SKILL.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `tools/thor/thor_codex_goal_loop.ps1`

Artifacts:

- `scratch/thor-debug/20260524-052031-*`

Findings:

- Capture reached the visible opening sky/wing route with clean fatal-marker
  search on APK SHA
  `A4CB9C0E8F4CBB63EB09F3DA38640BEA5410DFE5916A155627C18F3B90C39BD8`.
- With the HIR/block dump removed, `82282490` returned to the top body row:
  `82282490=30214411`, `82281D28=10212419`, `82287788=3195362`,
  `82282388=3020644`, `82282410=2902097`, `821CE028=674464`, and
  `826BF770=563064`.
- Dynamic call-edge rows show `82281D28` is mostly recursive child work:
  `8228233C -> 82281D28` has `body_ticks_total=7350722`,
  `calls_total=69445`, and `ticks_per_call_peak=116`.
- Reported dynamic call edges account for about `9096992` ticks, or `89.1%`
  of final `82281D28` body time. The self-recursive edge alone accounts for
  about `72.0%`.
- Approximate `82281D28` parent-exclusive work after reported direct edges is
  only `1115427` ticks.

Decision:

- Do not patch local `82281D28` generated code yet, and do not rerun the exact
  HIR/block capture unchanged.
- Next step should collapse/reason about the recursive SCC and return to
  body-backed `82282490` work, or improve the mapper enough to recover the
  missing `8228233C` static span before any local `82281D28` codegen patch.

## 05:32-05:41 EDT - Call-Path Recursive Heuristic

Starting HEAD: `72820697c`

Goal: turn the manual recursive-edge math from the `82281D28` split into a
deterministic report so future slices do not patch inclusive recursive blocks
by mistake.

Commands run:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_call_path_report.ps1 -LogPath scratch\thor-debug\20260524-052031-speed-logcat.txt -Function 82281D28 -BlockProfileLog scratch\thor-debug\20260524-052031-speed-logcat.txt -Top 20
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_call_path_report.ps1 -LogPath scratch\thor-debug\20260524-030450-speed-logcat.txt -Function 82282490 -BlockProfileLog scratch\thor-debug\20260524-030450-speed-logcat.txt -Top 12
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_call_path_report.ps1 -LogPath scratch\thor-debug\20260523-152754-speed-logcat.txt -Function 8227F1D8 -BlockProfileLog scratch\thor-debug\20260523-152754-speed-logcat.txt -Top 8
```

Files changed:

- `tools/thor/thor_hir_call_path_report.ps1`
- `docs/research/20260524-053502-call-path-recursive-heuristic.md`
- `docs/worklogs/20260524.md`
- `AGENTS.md`
- `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md`
- `.agents/skills/xenia-working-fast-autonomy/SKILL.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `tools/thor/thor_codex_goal_loop.ps1`

Findings:

- The call-path report now prints a `Dynamic Recursive/Exclusive Heuristic`
  section when body-time rows are present in `-BlockProfileLog`.
- Validation on `82281D28` reports dynamic direct edges at `89.08%` of final
  function body and self-recursion at `71.98%`, leaving only `10.92%`
  approximate parent-exclusive body.
- Validation on `82282490` reports dynamic direct edges at `84.08%` and
  self-recursion at `71.44%`, leaving `15.92%` approximate parent-exclusive
  body.
- Validation on old `8227F1D8` evidence reports the known `82490030` child at
  `98.48%`, which matches the previous manual conclusion.

Decision:

- Do not patch local `82281D28`.
- Use the updated report before patching call-heavy recursive blocks.
- Next useful speed work should pick body-backed local/exclusive spans from
  `82282490`, such as the previously mapped `8228252C-822825C4` area, rather
  than inclusive recursive block totals.

## 05:40-05:46 EDT - 82282490 Exclusive Candidate Ranking

Starting HEAD: `4528093b0`

Goal: rank `82282490` dynamic block slices by approximate exclusive body time
so the next speed patch targets local generated-code work instead of recursive
or child-edge totals.

Commands run:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_dynamic_block_slice_report.ps1 -LogPath scratch\thor-debug\20260521-170941-speed-logcat.txt -Function 82282490 -BlockProfileLog scratch\thor-debug\20260524-030450-speed-logcat.txt -Top 12
```

Files changed:

- `tools/thor/thor_hir_dynamic_block_slice_report.ps1`
- `docs/research/20260524-054204-82282490-exclusive-candidate-ranking.md`
- `docs/worklogs/20260524.md`
- `AGENTS.md`
- `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md`
- `.agents/skills/xenia-working-fast-autonomy/SKILL.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `tools/thor/thor_codex_goal_loop.ps1`

Findings:

- The dynamic block slice report now prints a `Local Exclusive Candidate
  Ranking`.
- `8228252C-822825C4` remains the top local/exclusive candidate:
  `approx_exclusive=2876500`, `exclusive_pct=33.72`.
- `822824F0-82282528` is second: `approx_exclusive=2021174`,
  `exclusive_pct=57.72`.
- `822825E0` still has a high absolute exclusive estimate, but only `1.68%`
  exclusive share because it is dominated by the recursive child edge.
- The prior narrow `stvewx` stack-lane fold for `82282580/82282584` was
  route-clean but did not prove speed, so do not repeat that exact A/B.

Decision:

- Keep `8228252C-822825C4` as the best local span, but the next patch should
  target broader state/vector/FPR traffic rather than the already-tested
  narrow `stvewx` lane fold.
- If no safe patch is obvious, compare `8228252C-822825C4` against
  `822824F0-82282528` with a focused offline audit before changing codegen.

## 05:51-05:59 EDT - 82282490 Top Span Comparison

Starting HEAD: `e7bb7602f`

Goal: compare the top two `82282490` local/exclusive spans before writing a
new A64 codegen patch.

Commands run:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_a64_codegen_audit.ps1 -LogPath scratch\thor-debug\20260521-170941-speed-logcat.txt -Function 82282490 -StartGuest 8228252C -EndGuest 822825C4 -BlockProfileLog scratch\thor-debug\20260524-030450-speed-logcat.txt -Top 16
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_a64_codegen_audit.ps1 -LogPath scratch\thor-debug\20260521-170941-speed-logcat.txt -Function 82282490 -StartGuest 822824F0 -EndGuest 82282528 -BlockProfileLog scratch\thor-debug\20260524-030450-speed-logcat.txt -Top 16
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_span_reduction_audit.ps1 -LogPath scratch\thor-debug\20260521-170941-speed-logcat.txt -Function 82282490 -StartGuest 8228252C -EndGuest 822825C4 -BlockProfileLog scratch\thor-debug\20260524-030450-speed-logcat.txt -EdgeProfileLog scratch\thor-debug\20260524-030450-speed-logcat.txt -Top 16
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_span_reduction_audit.ps1 -LogPath scratch\thor-debug\20260521-170941-speed-logcat.txt -Function 82282490 -StartGuest 822824F0 -EndGuest 82282528 -BlockProfileLog scratch\thor-debug\20260524-030450-speed-logcat.txt -EdgeProfileLog scratch\thor-debug\20260524-030450-speed-logcat.txt -Top 16
```

Files changed:

- `docs/research/20260524-055108-82282490-top-span-comparison.md`
- `docs/worklogs/20260524.md`
- `AGENTS.md`
- `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md`
- `.agents/skills/xenia-working-fast-autonomy/SKILL.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `tools/thor/thor_codex_goal_loop.ps1`

Findings:

- `8228252C-822825C4` remains the larger absolute local candidate:
  `body_ticks_total=8530471`, child edge to `82287788=5653971`,
  `approx_exclusive=2876500`, and `exclusive_pct=33.72`.
- Its local surface is real but noisy: `store_context=27`,
  `load_context=14`, `extract=9`, `splat=6`, `stvewx=3`, and
  `vmaddfp=3`. The already-tested `stvewx` lane fold is route-clean but not a
  proven speed win.
- `822824F0-82282528` is smaller but cleaner: `body_ticks_total=3501617`,
  child edge to `82274DB0=1480443`, `approx_exclusive=2021174`, and
  `exclusive_pct=57.72`.
- Its exact vector shape is narrow: one `lvlx`, one `lvrx`, two
  `load_vector_shl`, two `permute`, and two stores to `v[0]`.

Decision:

- Do not patch generated-code behavior from this comparison alone.
- Next slice should add or run focused provenance for the exact
  `822824F0-82282528` `lvlx/lvrx -> load_vector_shl + permute -> v[0]`
  join before a default-off function/span-gated codegen experiment.
- Keep CR compare/barrier fusion, CR-store elision, broad VMX-dot, broad GPR
  caches, and the exact `stvewx` lane-fold A/B closed unless new route-clean
  evidence reopens them.

## 06:00-06:12 EDT - 822824F0 Vector-Load Provenance Auditor

Starting HEAD: `373c8a268`

Goal: turn the tempting `822824F0-82282528` `lvlx/lvrx` vector-load shape into
a deterministic provenance report before touching A64 codegen.

Commands run:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_vector_load_join_audit.ps1 -LogPath scratch\thor-debug\20260521-170941-speed-logcat.txt -Function 82282490 -StartGuest 822824F0 -EndGuest 82282528 -BlockProfileLog scratch\thor-debug\20260524-030450-speed-logcat.txt -Top 16
```

Files changed:

- `tools/thor/thor_hir_vector_load_join_audit.ps1`
- `docs/research/20260524-060827-822824f0-vector-load-provenance.md`
- `docs/worklogs/20260524.md`
- `AGENTS.md`
- `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md`
- `.agents/skills/xenia-working-fast-autonomy/SKILL.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `tools/thor/thor_codex_goal_loop.ps1`

Findings:

- New tool `tools/thor/thor_hir_vector_load_join_audit.ps1` parses filtered
  HIR/PPC, block body rows, simple PPC GPR provenance, vector load rows, and
  lookahead vector joins.
- `822824F0-82282528` remains body-backed local work:
  `body_ticks_total=3501617`, child edge `82274DB0=1480443`, approximate
  exclusive `2021174`.
- `82282520 lvlx vr13,r0,r11` loads from `r30+0x14`, stores `v[13]`, and
  lowers to one `load_vector_shl`, one `load.1`, and one `permute`.
- `82282528 lvrx vr0,r11,r10` loads from `r30+0x20`, stores `v[0]`, and has
  the expected zero path plus one `load_vector_shl`, one `load.1`, and one
  `permute`.
- The first matching `vor vr0,vr13,vr0` join is at `8228254C`, outside the
  requested span in the next region.

Decision:

- Do not patch local `822824F0-82282528` codegen yet.
- Next step should audit the cross-span `lvlx/lvrx -> vor -> extract/splat ->
  vmaddfp` consumer chain through at least `8228254C`, or return to the larger
  `8228252C-822825C4` state/vector/FPR target with better provenance.

## 06:18-06:25 EDT - 822824F0 Cross-Span Consumer Audit

Starting HEAD: `396794d15`

Goal: extend the vector-load provenance tool through the downstream
`vor/extract/splat/vmaddfp` users so we do not turn the `822824F0` hunch into
an unsafe local-only codegen patch.

Commands run:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_vector_load_join_audit.ps1 -LogPath scratch\thor-debug\20260521-170941-speed-logcat.txt -Function 82282490 -StartGuest 822824F0 -EndGuest 82282574 -BlockProfileLog scratch\thor-debug\20260524-030450-speed-logcat.txt -Top 16
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_a64_codegen_audit.ps1 -LogPath scratch\thor-debug\20260521-170941-speed-logcat.txt -Function 82282490 -StartGuest 822824F0 -EndGuest 82282574 -BlockProfileLog scratch\thor-debug\20260524-030450-speed-logcat.txt -Top 16
```

Files changed:

- `tools/thor/thor_hir_vector_load_join_audit.ps1`
- `docs/research/20260524-062037-822824f0-cross-span-consumer-audit.md`
- `docs/worklogs/20260524.md`
- `AGENTS.md`
- `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md`
- `.agents/skills/xenia-working-fast-autonomy/SKILL.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `tools/thor/thor_codex_goal_loop.ps1`

Findings:

- The updated auditor now prints vector consumer rows.
- The full `822824F0-82282574` chain is self-contained inside the larger span:
  `lvlx/lvrx`, `vsldoi`, four `lvx128`-style loads, `vor`, three
  `extract+splat` pairs, and three `vmaddfp` lowerings before the later
  `stvewx` store area.
- Body-backed profile remains `body_ticks_total=3501617`, child edge
  `82274DB0=1480443`, and approximate exclusive `2021174`.
- A64 audit says this is not a single cheap peephole: the three
  `MUL_ADD_V128` lowerings include VMX FPCR handling, scratch-stack saves,
  `fmla`, PPC NaN fixup, and denormal handling.

Decision:

- Do not implement a quick local-only `822824F0` vector peephole.
- Next useful output is a generated-instruction estimate/source-review for the
  exact chain, especially `MUL_ADD_V128`, `PERMUTE_V128`,
  `LOAD_VECTOR_SHL`, and `EXTRACT/SPLAT`, then compare against the larger
  `8228252C-822825C4` state/vector/FPR target before a default-off codegen
  experiment.

## 06:31-06:39 EDT - 82282490 Span Instruction Estimate

Starting HEAD: `713f19ad6`

Goal: add a source-reviewed A64 instruction-floor estimate to the existing HIR
audit and use it to compare the `822824F0-82282574` vector/FMA chain against
the larger `8228252C-822825C4` local span.

Commands run:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_a64_codegen_audit.ps1 -LogPath scratch\thor-debug\20260521-170941-speed-logcat.txt -Function 82282490 -StartGuest 822824F0 -EndGuest 82282574 -BlockProfileLog scratch\thor-debug\20260524-030450-speed-logcat.txt -Top 16
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_a64_codegen_audit.ps1 -LogPath scratch\thor-debug\20260521-170941-speed-logcat.txt -Function 82282490 -StartGuest 8228252C -EndGuest 822825C4 -BlockProfileLog scratch\thor-debug\20260524-030450-speed-logcat.txt -Top 16
```

Files changed:

- `tools/thor/thor_hir_a64_codegen_audit.ps1`
- `docs/research/20260524-063109-82282490-span-instruction-estimate.md`
- `docs/worklogs/20260524.md`
- `AGENTS.md`
- `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md`
- `.agents/skills/xenia-working-fast-autonomy/SKILL.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `tools/thor/thor_codex_goal_loop.ps1`

Findings:

- `tools/thor/thor_hir_a64_codegen_audit.ps1` now prints a
  `Source-Reviewed A64 Floor Estimate` section. It is a heuristic floor, not
  exact generated instruction count.
- `822824F0-82282574` estimated floor is `164` with approximate exclusive
  `2021174`, or `12324.23` exclusive ticks per estimated instruction. Top rows
  are `mul_add_v128=30`, `store_context=25`, `permute=24`,
  `load_context=14`, `add=13`, `load.1=12`, and `load_vector_shl=12`.
- `8228252C-822825C4` estimated floor is `192` with approximate exclusive
  `2876500`, or `14981.77` exclusive ticks per estimated instruction. Top rows
  are `extract_dynamic_i32=33`, `mul_add_v128=30`, `store_context=27`,
  `load_context=14`, `load.1=12`, `and=11`, and `add=10`.
- `8228252C-822825C4` remains the better next target by both absolute local
  exclusive time and estimated density.

Decision:

- Do not patch local-only `822824F0` vector codegen from the cross-span audit.
- Next slice should target the larger `8228252C-822825C4` lane or a shared
  `MUL_ADD_V128` cost lane with a default-off, function/span-gated audit first.
  Good concrete next steps are either a `MUL_ADD_V128` source/audit lane for
  `82282568/8228256C/82282570`, or better `82282588` dynamic-EXTRACT/stvewx
  provenance before another lane-fold experiment.

## 06:41-06:47 EDT - 82282588 STVEWX Provenance

Starting HEAD: `82d56ba06`

Goal: close the remaining `82282588` dynamic-`EXTRACT_I32` provenance gap in
the measured `8228252C-822825C4` span without changing generated code.

Commands run:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_stvewx_lane_audit.ps1 -LogPath scratch\thor-debug\20260521-170941-speed-logcat.txt -Function 82282490 -StartGuest 8228252C -EndGuest 822825C4 -BlockProfileLog scratch\thor-debug\20260524-030450-speed-logcat.txt
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_stvewx_lane_audit.ps1 -LogPath scratch\thor-debug\20260521-170941-speed-logcat.txt -Function 82282490 -StartGuest 8228252C -ProvenanceStartGuest 82282514 -EndGuest 822825C4 -BlockProfileLog scratch\thor-debug\20260524-030450-speed-logcat.txt
```

Files changed:

- `tools/thor/thor_hir_stvewx_lane_audit.ps1`
- `docs/research/20260524-064118-82282588-stvewx-provenance.md`
- `docs/worklogs/20260524.md`
- `AGENTS.md`
- `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md`
- `.agents/skills/xenia-working-fast-autonomy/SKILL.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `tools/thor/thor_codex_goal_loop.ps1`

Findings:

- `tools/thor/thor_hir_stvewx_lane_audit.ps1` now supports
  `-ProvenanceStartGuest` and simple `store_context` -> `load_context`
  expression forwarding inside the provenance window, with kills on
  `context_barrier` and `call*`.
- Target-only `8228252C-822825C4` audit still reports `82282588` unknown.
- Provenance-aware audit with `-ProvenanceStartGuest 82282514` proves
  `82282588 -> lane 2`: `r6 = r1 + 0x50`, then `r6 + 0x8`, masked with `~3`.
- All three dynamic `stvewx` extract sites in the span now have provenance:
  `82282580 -> lane 0`, `82282584 -> lane 1`, and `82282588 -> lane 2`.

Decision:

- This is stronger provenance, not a speed win yet.
- Do not repeat the old two-site `stvewx` lane-fold A/B unchanged.
- If the next slice patches generated code, make it a new default-off,
  Blue-Dragon/function/PC-gated all-three-site `EXTRACT_I32` fastpath with
  audit counters first. If that misses, move to `MUL_ADD_V128` cost rather than
  another narrow `stvewx` tweak.

## 06:48-06:56 EDT - All-Three STVEWX Fastpath Audit

Starting HEAD: `87b06f518`

Goal: extend the existing default-off Blue Dragon `stvewx` stack-lane
`EXTRACT_I32` fastpath to include the newly proven `82282588 -> lane 2` site,
then prove the all-three-site fold is route-clean under audit before any quiet
speed A/B.

Commands run:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 25 -Arm64SpeedProfileMinDelta 1 -Arm64SpeedProfileBodyTimeFilter "82282490" -Arm64SpeedProfileBodyTimeAfterMs 120000 -Arm64SpeedProfileBlockFilter "82282490" -Arm64SpeedProfileBlockBodyTime true -Arm64BlueDragonStvewxStackLaneFastpath true -Arm64BlueDragonStvewxStackLaneAudit true
```

Files changed:

- `src/xenia/cpu/backend/a64/a64_seq_vector.cc`
- `src/xenia/cpu/backend/a64/a64_backend.cc`
- `docs/research/20260524-065602-blue-dragon-all-three-stvewx-fastpath.md`
- `docs/worklogs/20260524.md`
- `AGENTS.md`
- `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md`
- `.agents/skills/xenia-working-fast-autonomy/SKILL.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `tools/thor/thor_codex_goal_loop.ps1`

Findings:

- `NativeCore` passed.
- `FullDeploy` passed and installed on `c3ca0370`.
- APK SHA-256:
  `8A3DA22B2208AA67DE13C07383490F3BEBA14E2DC1538AAB5CE08FD035626771`.
- Capture `scratch/thor-debug/20260524-065132-*` reached the visible opening
  sky/dragon-wing route and had a clean fatal-marker search.
- Final audit row:
  `fastpath=722256/2008221 fallback=0/0`.
- Final delayed `82282490` body row:
  `body_ticks_total=50120439`, `entries_delta=60844`,
  `ticks_per_entry=276`, `code_size=107420`.
- Final `82282490` block body rows kept the known recursive shape:
  `822825E0` first, `8228252C` second, `822825C8` third, and `822824F0`
  fourth.

Decision:

- The all-three-site fold is route-clean under audit, but this is not a speed
  win because audit and block body-time instrumentation were enabled.
- Keep `arm64_blue_dragon_stvewx_stack_lane_fastpath` default-off in presets.
- Next useful step is a quiet same-APK A/B or control sandwich with audit off.
  If that is inconclusive again, stop spending more slices on narrow `stvewx`
  lane folds and move to `MUL_ADD_V128` cost for `82282568/8228256C/82282570`.

## 07:01-07:10 EDT - All-Three STVEWX Quiet A/B

Starting HEAD: `a924aba56`

Goal: test the all-three `stvewx` lane fold without audit counters,
body-time, block body-time, or disassembly so the result can be judged as a
quiet same-APK A/B.

Commands run:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 25 -Arm64SpeedProfileMinDelta 1 -Arm64BlueDragonStvewxStackLaneFastpath false -Arm64BlueDragonStvewxStackLaneAudit false
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 25 -Arm64SpeedProfileMinDelta 1 -Arm64BlueDragonStvewxStackLaneFastpath true -Arm64BlueDragonStvewxStackLaneAudit false
```

Files changed:

- `docs/research/20260524-071045-blue-dragon-all-three-stvewx-quiet-ab.md`
- `docs/worklogs/20260524.md`
- `AGENTS.md`
- `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md`
- `.agents/skills/xenia-working-fast-autonomy/SKILL.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `tools/thor/thor_codex_goal_loop.ps1`

Findings:

- Control `scratch/thor-debug/20260524-070253-*` and fastpath-on
  `scratch/thor-debug/20260524-070613-*` both used commit `a924aba56`, APK
  SHA `8A3DA22B2208AA67DE13C07383490F3BEBA14E2DC1538AAB5CE08FD035626771`,
  no audit, no body-time, and no block body-time.
- Both reached a visually matching opening sky/dragon-wing frame and had clean
  fatal-marker searches.
- Final speed-profile interval was close: control `entry_delta=3640519`,
  fastpath-on `entry_delta=3537838`.
- Final `82282490` rows had identical total entries at `183498`; code size
  shrank from `86828` to `86708`, but final delta only moved from `54908` to
  `51940`.
- Final perf remained CPU/JIT-heavy, not GPU-heavy: Main Thread was `96.1%`
  control and `100%` fastpath-on; GPU Commands was `11.5%` in both.

Decision:

- The all-three fold is route-clean and shrinks code, but quiet A/B does not
  prove a speed win.
- Keep `arm64_blue_dragon_stvewx_stack_lane_fastpath` default-off.
- Stop spending more sprint slices on narrow `stvewx` lane folds unless a
  broader state/vector experiment reopens them.
- Next useful lane is `MUL_ADD_V128` source/cost audit for
  `82282568/8228256C/82282570`, or broader `8228252C-822825C4`
  state/vector/FPR reduction.

## 07:18-07:22 EDT - MUL_ADD_V128 Source/Cost Audit

Starting HEAD: `2cb4dd954`

Goal: move past the inconclusive all-three `stvewx` lane fold and turn the
shared `MUL_ADD_V128` lowering at `82282568/8228256C/82282570` into a
repeatable offline audit before any generated-code shortcut.

Commands run:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_a64_codegen_audit.ps1 -LogPath scratch\thor-debug\20260521-170941-speed-logcat.txt -Function 82282490 -StartGuest 8228252C -EndGuest 822825C4 -BlockProfileLog scratch\thor-debug\20260524-030450-speed-logcat.txt -Top 16
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_a64_codegen_audit.ps1 -LogPath scratch\thor-debug\20260521-170941-speed-logcat.txt -Function 82282490 -StartGuest 822824F0 -EndGuest 82282574 -BlockProfileLog scratch\thor-debug\20260524-030450-speed-logcat.txt -Top 16
```

Files changed:

- `tools/thor/thor_hir_a64_codegen_audit.ps1`
- `docs/research/20260524-072228-mul-add-v128-source-cost-audit.md`
- `docs/worklogs/20260524.md`
- `AGENTS.md`
- `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md`
- `.agents/skills/xenia-working-fast-autonomy/SKILL.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `tools/thor/thor_codex_goal_loop.ps1`

Findings:

- `tools/thor/thor_hir_a64_codegen_audit.ps1` now prints a
  `MUL_ADD_V128 Source-Cost Audit` section for slices containing `mul_add` HIR
  or `vmaddfp` PPC rows.
- Both candidate spans hit the same three VMX FMA PCs:
  `82282568`, `8228256C`, and `82282570`.
- `8228252C-822825C4` remains the larger body-backed local target:
  `approx_exclusive=2876500`, `mul_add_v128=3`, and
  `approx_exclusive_ticks_per_estimated_instr=14981.77`.
- `MUL_ADD_V128` is not a one-instruction shortcut target. The A64 lowering
  enters VMX FPCR mode, optionally flushes denormals, saves flushed sources to
  scratch, emits `fmla`, runs the PPC NaN fast-path/repair helper, optionally
  flushes output denormals, and copies the result to the destination.
- x64 intentionally avoids fused host FMA for `MUL_ADD_V128` because the
  `vfmadd` path differed from `vmulps` plus `vaddps` tests, so any A64
  shortcut needs semantics evidence as well as speed evidence.

Decision:

- Do not patch `MUL_ADD_V128` generated-code behavior yet.
- Next useful lane is a default-off function/span/PC-gated runtime audit for
  `82282568/8228256C/82282570` that counts denormal flush need, NaN fixup
  entry/per-lane repair, FPCR mode switches, and source/dest alias copies.
- If those expensive paths are cold in the Blue Dragon opening route, test a
  guarded fastpath. If they are hot, return to broader `8228252C-822825C4`
  state/vector/FPR traffic reduction.

## 07:29-07:44 EDT - MUL_ADD_V128 Runtime Audit Patch

Starting HEAD: `64221eb06`

Goal: implement the default-off runtime audit requested by the source/cost
report so the next Thor run can measure real `MUL_ADD_V128` slow-path pressure
before any shortcut.

Commands run:

```powershell
git diff --check
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore
```

Files changed:

- `src/xenia/cpu/backend/a64/a64_backend.cc`
- `src/xenia/cpu/backend/a64/a64_backend.h`
- `src/xenia/cpu/backend/a64/a64_seq_util.h`
- `src/xenia/cpu/backend/a64/a64_sequences.cc`
- `tools/thor/thor_xenia_debug.ps1`
- `docs/research/20260524-074442-blue-dragon-mul-add-v128-runtime-audit.md`
- `docs/worklogs/20260524.md`
- `AGENTS.md`
- `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md`
- `.agents/skills/xenia-working-fast-autonomy/SKILL.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `tools/thor/thor_codex_goal_loop.ps1`

Findings:

- Added default-off cvar `arm64_blue_dragon_mul_add_v128_audit`.
- The audit is gated to Blue Dragon guest function `82282490` and PCs
  `82282568`, `8228256C`, and `82282570`.
- Logged counters cover total/per-PC calls, FPCR switches, software flush-path
  eligibility, input/output denormal sightings, NaN-fixup entry and repaired
  lanes, plus source/destination vector copies.
- `tools/thor/thor_xenia_debug.ps1` now exposes
  `-Arm64BlueDragonMulAddV128Audit`.
- `git diff --check` passed.
- `NativeCore` passed for Android `arm64-v8a` and `x86_64`.

Decision:

- This is an audit patch, not a speed win yet.
- Keep `arm64_blue_dragon_mul_add_v128_audit` default-off in presets.
- Next step is `FullDeploy` plus a route-stabilized
  `LaunchBlueDragonSpeedCapture` with `-Arm64BlueDragonMulAddV128Audit true`
  and delayed `82282490` body-time. If expensive counters are cold, design a
  guarded fastpath; if they are hot, return to broader `8228252C-822825C4`
  state/vector/FPR traffic reduction.

## 07:50-08:01 EDT - MUL_ADD_V128 Audit Capture And Android Plumbing

Starting HEAD: `172df9cf7`

Goal: deploy the runtime audit patch, confirm the Android launcher actually
passes the audit cvar to native, and collect route-clean counters for
`82282568/8228256C/82282570`.

Commands run:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 25 -Arm64SpeedProfileMinDelta 1 -Arm64SpeedProfileBodyTimeFilter "82282490" -Arm64SpeedProfileBodyTimeAfterMs 120000 -Arm64BlueDragonMulAddV128Audit true -StopAppAfterCapture true
```

Files changed:

- `android/android_studio_project/app/src/main/java/jp/xenia/emulator/EmulatorActivity.java`
- `docs/research/20260524-080156-blue-dragon-mul-add-v128-audit-capture.md`
- `docs/worklogs/20260524.md`
- `AGENTS.md`
- `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md`
- `.agents/skills/xenia-working-fast-autonomy/SKILL.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `tools/thor/thor_codex_goal_loop.ps1`

Findings:

- First deployed capture `scratch/thor-debug/20260524-075059-*` reached the
  visible opening sky/dragon-wing route with clean fatal search, but emitted no
  `A64 Blue Dragon MUL_ADD_V128 audit` rows.
- Root cause: `tools/thor/thor_xenia_debug.ps1` sent
  `arm64_blue_dragon_mul_add_v128_audit`, but `EmulatorActivity.java` did not
  copy that extra into the native launch argument cvar bundle.
- Added the missing `copyBooleanExtra` bridge and reran `FullDeploy`, which
  passed and installed on `c3ca0370`.
- Rerun `scratch/thor-debug/20260524-075713-*` used APK SHA
  `7BBAB603A9931918867E9AAE0869193F3A2049329ED7FB448D36C4E66B97EE85`,
  reached the visible opening sky/dragon-wing route, and had a clean
  fatal-marker search.
- Final audit counters were `total=722256/2131533`, with each target PC at
  `240752/710511`. `sw_flush_path`, `input_denorm`, `output_denorm`,
  `nan_entry`, and `nan_lane` all stayed `0/0`.
- Final audit counters also showed `src_copy=2166768/6394599` and
  `dest_copy=722256/2131533`, so the hot cost left in this lane is mostly
  scratch/copy/NaN-fixup structure rather than actually taken NaN or denormal
  handling.
- Final route remained CPU/JIT-heavy: Main Thread `96.1%`, XMA Decoder
  `42.3%`, GPU Commands `11.5%`, Draw Thread `0.0%`.

Decision:

- Keep `arm64_blue_dragon_mul_add_v128_audit` default-off.
- The next useful patch is a default-off Blue-Dragon/function/PC-gated
  `MUL_ADD_V128` fastpath for `82282568`, `8228256C`, and `82282570`, proven
  with route-clean capture first and quiet same-APK A/B second.
- Do not generalize this to `MUL_ADD_V128` globally and do not treat the audit
  capture itself as speed evidence because the audit build increased generated
  code size.

## 08:08-08:23 EDT - MUL_ADD_V128 Fastpath Route Proof And Quiet A/B

Starting HEAD: `5d554c8af`

Goal: implement the default-off `MUL_ADD_V128` fastpath justified by the
runtime audit, prove route safety, then run a quiet same-APK A/B.

Commands run:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 25 -Arm64SpeedProfileMinDelta 1 -Arm64SpeedProfileBodyTimeFilter "82282490" -Arm64SpeedProfileBodyTimeAfterMs 120000 -Arm64BlueDragonMulAddV128Fastpath true -StopAppAfterCapture true
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 25 -Arm64SpeedProfileMinDelta 1 -Arm64BlueDragonMulAddV128Fastpath false -Arm64BlueDragonMulAddV128Audit false -StopAppAfterCapture true
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 25 -Arm64SpeedProfileMinDelta 1 -Arm64BlueDragonMulAddV128Fastpath true -Arm64BlueDragonMulAddV128Audit false -StopAppAfterCapture true
```

Files changed:

- `src/xenia/cpu/backend/a64/a64_backend.cc`
- `src/xenia/cpu/backend/a64/a64_sequences.cc`
- `android/android_studio_project/app/src/main/java/jp/xenia/emulator/EmulatorActivity.java`
- `tools/thor/thor_xenia_debug.ps1`
- `docs/research/20260524-082324-blue-dragon-mul-add-v128-fastpath-ab.md`
- `docs/worklogs/20260524.md`
- `AGENTS.md`
- `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md`
- `.agents/skills/xenia-working-fast-autonomy/SKILL.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `tools/thor/thor_codex_goal_loop.ps1`

Findings:

- Added default-off cvar `arm64_blue_dragon_mul_add_v128_fastpath`, gated to
  Blue Dragon function `82282490`, PCs `82282568/8228256C/82282570`, and Thor
  hardware denormal-input flushing.
- `NativeCore` and `FullDeploy` passed. Tested APK SHA:
  `127CFD98C6AFE2AFD5C7EC8D6D0E0A8DB2B9DE7893334ED3F85B7894621B3688`.
- Route-safety capture `scratch/thor-debug/20260524-081212-*` reached visible
  opening sky/dragon-wing route with clean fatal-marker search.
- Quiet control `scratch/thor-debug/20260524-081601-*` and quiet fastpath-on
  `scratch/thor-debug/20260524-081929-*` both reached visually matching
  opening route frames with clean fatal-marker searches.
- Quiet A/B shrank `82282490` code size from `86828` to `85424`.
- Final `82282490` speed rows were mixed: control `delta=65296`,
  `total=170142`; fastpath-on `delta=60844`, `total=177562`.
- Final thread samples did not prove route-wide speed: Main Thread was
  `92.3%` control and `96.1%` fastpath-on, with XMA/GPU samples also varying.

Decision:

- Keep `arm64_blue_dragon_mul_add_v128_fastpath` default-off and out of the
  Blue Dragon speed preset.
- Treat this as route-clean code-size scaffolding, not a speed win.
- Next useful lane is broader `8228252C-822825C4` local-span state/vector/FPR
  reduction; do not spend another immediate slice on this narrow three-PC FMA
  skip unless a broader experiment reopens it.

## 08:29-08:36 EDT - 8228252C Call-Boundary State Audit

Starting HEAD: `2a484d8cb`

Goal: stop circling narrow `stvewx` and `MUL_ADD_V128` probes, then identify
whether the broader `8228252C-822825C4` cost is really call-boundary state
traffic around the hot `8228252C -> 82287788` child edge.

Commands run:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_span_reduction_audit.ps1 -LogPath scratch\thor-debug\20260521-170941-speed-logcat.txt -Function 82282490 -StartGuest 8228252C -EndGuest 822825C4 -BlockProfileLog scratch\thor-debug\20260524-030450-speed-logcat.txt -EdgeProfileLog scratch\thor-debug\20260524-030450-speed-logcat.txt -Top 24
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_call_boundary_state_audit.ps1 -ParentLogPath scratch\thor-debug\20260521-170941-speed-logcat.txt -ParentFunction 82282490 -ParentStartGuest 8228252C -ParentEndGuest 822825C4 -CalleeLogPath scratch\thor-debug\20260524-041413-speed-logcat.txt -CalleeFunction 82287788 -Phase OptHIR -Top 40
```

Files changed:

- `tools/thor/thor_hir_call_boundary_state_audit.ps1`
- `docs/research/20260524-083644-8228252c-call-boundary-state-audit.md`
- `docs/worklogs/20260524.md`
- `AGENTS.md`
- `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md`
- `.agents/skills/xenia-working-fast-autonomy/SKILL.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `tools/thor/thor_codex_goal_loop.ps1`

Findings:

- Added a deterministic parent/callee HIR state audit for direct-call
  boundaries.
- For parent span `8228252C-822825C4`, the audit found `17` pre-call
  `store_context` rows before `82282598 -> 82287788`, about `188` approximate
  state bytes.
- Only `4` stores are live into `82287788`: `r[3]`, `f[1]`, `fpscr`, and `lr`.
- The other `13` stores, about `160` approximate state bytes, are
  `callee_dead_parent_dead_linear` in this HIR audit. Most are VMX stores:
  `v[11]`, `v[9]`, `v[8]`, `v[10]`, `v[13]`, `v[12]`, and `v[0]`.
- This is not proof that stores can be removed. It is a better next target than
  another narrow vector peephole because it explains why the local span stays
  expensive even after the lane-fold and FMA shortcuts missed speed proof.

Decision:

- Do not patch behavior yet.
- Next useful slice is a default-off, function/span/call-gated state-store
  suppression audit/counter for the `82282598 -> 82287788` boundary, counting
  the 13 candidate stores before attempting to skip any of them.
- Keep `arm64_blue_dragon_stvewx_stack_lane_fastpath` and
  `arm64_blue_dragon_mul_add_v128_fastpath` default-off.

## 08:43-08:55 EDT - Call-Boundary State Runtime Audit

Starting HEAD: `2a6dadb6e`

Goal: add the default-off runtime counter for the `8228252C-822825C4`
call-boundary state-store candidates and prove the counters on Thor before any
store-suppression behavior change.

Commands run:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 25 -Arm64SpeedProfileMinDelta 1 -Arm64SpeedProfileBodyTimeFilter "82282490" -Arm64SpeedProfileBodyTimeAfterMs 120000 -Arm64BlueDragonCallBoundaryStateAudit true -StopAppAfterCapture true
rg -n "Fatal signal|AndroidRuntime|VK_ERROR_DEVICE_LOST|GPU is hung|SIGSEGV|SIGABRT" scratch\thor-debug\20260524-084805-speed-logcat.txt
```

Files changed:

- `src/xenia/cpu/backend/a64/a64_backend.cc`
- `src/xenia/cpu/backend/a64/a64_backend.h`
- `src/xenia/cpu/backend/a64/a64_sequences.cc`
- `android/android_studio_project/app/src/main/java/jp/xenia/emulator/EmulatorActivity.java`
- `tools/thor/thor_xenia_debug.ps1`
- `docs/research/20260524-085451-blue-dragon-call-boundary-state-runtime-audit.md`
- `docs/worklogs/20260524.md`
- `AGENTS.md`
- `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md`
- `.agents/skills/xenia-working-fast-autonomy/SKILL.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `tools/thor/thor_codex_goal_loop.ps1`

Findings:

- Added cvar `arm64_blue_dragon_call_boundary_state_audit`, default-off.
- Android launch plumbing and Thor debug script metadata now pass/record the
  cvar.
- The audit is counter-only. It counts exact Blue Dragon `82282490` PC/offset
  store sites and does not suppress generated stores.
- `NativeCore` and `FullDeploy` passed. Deployed APK SHA:
  `4E2A9728C1DDDCB4FD562843CB94CA4711EC96118992CDD44B79BCB49A40C36C`.
- Capture `scratch/thor-debug/20260524-084805-*` reached the visible opening
  sky/dragon-wing route and had a clean fatal-marker search.
- Final audit totals matched the static model exactly: `dead=8778627`,
  `dead_vmx=4726953`, `dead_gpr=2701116`, `dead_fpr=1350558`,
  `live=2701116`, which is `675279` dynamic boundary hits at `13` candidate
  dead stores and `4` live-in stores per hit.
- Final delayed body-time still had `82282490` first:
  `body_ticks_total=32598747`, `entries_delta=62328`, `ticks_per_entry=171`,
  `code_size=88008`.
- Final perf sample remained CPU/JIT-heavy: Main Thread `96.1%`, XMA Decoder
  `42.3%`, GPU Commands `7.6%`, Draw Thread `0.0%`.

Decision:

- This proves the audit counters and candidate ratio on the route, not speed.
  Audit instrumentation increased code size, so this capture is not a quiet
  FPS comparison.
- Keep `arm64_blue_dragon_call_boundary_state_audit` default-off and out of
  presets.
- Next useful slice is a separate default-off function/PC-gated suppression
  probe for only the `13` candidate-dead store sites, with route-clean proof
  before any quiet A/B. Do not skip the `4` live-in stores: `r[3]`, `f[1]`,
  `fpscr`, and `lr`.

## 09:02-09:30 EDT - Call-Boundary Store Suppression Probe

Starting HEAD: `087cbdc77`

Goal: implement the default-off behavior probe for the `13` candidate-dead
pre-call stores at the Blue Dragon `82282490` / `82282598 -> 82287788`
boundary, prove route safety, and run a quiet same-APK A/B.

Commands run:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 25 -Arm64SpeedProfileMinDelta 1 -Arm64SpeedProfileBodyTimeFilter "82282490" -Arm64SpeedProfileBodyTimeAfterMs 120000 -Arm64BlueDragonCallBoundaryStateAudit true -Arm64BlueDragonCallBoundaryStateSuppressDeadStores true -StopAppAfterCapture true
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 25 -Arm64SpeedProfileMinDelta 1 -Arm64BlueDragonCallBoundaryStateAudit false -Arm64BlueDragonCallBoundaryStateSuppressDeadStores false -StopAppAfterCapture true
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 25 -Arm64SpeedProfileMinDelta 1 -Arm64BlueDragonCallBoundaryStateAudit false -Arm64BlueDragonCallBoundaryStateSuppressDeadStores true -StopAppAfterCapture true
```

Files changed:

- `src/xenia/cpu/backend/a64/a64_backend.cc`
- `src/xenia/cpu/backend/a64/a64_backend.h`
- `src/xenia/cpu/backend/a64/a64_sequences.cc`
- `android/android_studio_project/app/src/main/java/jp/xenia/emulator/EmulatorActivity.java`
- `tools/thor/thor_xenia_debug.ps1`
- `docs/research/20260524-093001-blue-dragon-call-boundary-store-suppression-probe.md`
- `docs/worklogs/20260524.md`
- `AGENTS.md`
- `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md`
- `.agents/skills/xenia-working-fast-autonomy/SKILL.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `tools/thor/thor_codex_goal_loop.ps1`

Findings:

- Added default-off cvar
  `arm64_blue_dragon_call_boundary_state_suppress_dead_stores`.
- The cvar skips only the `13` candidate-dead Blue Dragon `82282490`
  PC/offset sites. It does not skip live-in `r[3]`, `f[1]`, `fpscr`, or `lr`.
- Audit counters/log rows are emitted only when
  `arm64_blue_dragon_call_boundary_state_audit=true`; suppression-only is now
  quiet and emits zero call-boundary audit rows.
- Corrected APK SHA:
  `9551FA105672594452461EB4E42DE6F13FCEEC8940F064CB7B40F6AF427351FF`.
- Route-safety capture `scratch/thor-debug/20260524-091813-*` reached the
  visible opening sky/dragon-wing route with clean fatal-marker search.
- Final route-safety audit row had `suppressed=9007635`, exactly matching
  `dead=9007635`; live-in stores remained separate at `live=2771580`.
- Quiet same-APK control `scratch/thor-debug/20260524-092153-*` and
  suppression-on `scratch/thor-debug/20260524-092523-*` both reached visible
  opening sky/dragon-wing frames with clean fatal-marker searches.
- Quiet control final `82282490` code size was `86828`; suppression-on was
  `86768`, a small `60` byte shrink.
- Quiet final `82282490` rows were mixed: control `delta=62328 total=169959`,
  suppression-on `delta=56392 total=173110`. Thread samples also did not prove
  a speed win.
- The earlier pair `20260524-090930-*` / `20260524-091252-*` is discarded for
  speed judgment because suppression was still emitting atomic counters before
  the quiet-path fix.

Decision:

- Keep `arm64_blue_dragon_call_boundary_state_suppress_dead_stores`
  default-off and out of presets.
- Treat this as route-clean code-size scaffolding, not speed proof.
- Do not repeat the exact same store-suppression A/B next. Move to broader
  `8228252C-822825C4` state/vector/FPR reduction or a larger state-cache /
  lowering change backed by fresh offline evidence.

## 09:36-09:48 EDT - HIR Dead State Store DCE Audit

Starting HEAD: `951dc364d`

Goal: test whether moving the route-proven `13` call-boundary dead-store
suppression earlier into HIR would unlock recursive `DeadCodeEliminationPass`
removal of upstream `8228252C-822825C4` vector/FPR work.

Commands run:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_dead_state_store_dce_audit.ps1 -LogPath scratch\thor-debug\20260521-170941-speed-logcat.txt -Function 82282490 -StartGuest 8228252C -EndGuest 822825C4 -TargetMode BlueDragonCallBoundaryDead -Top 32
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_dead_state_store_dce_audit.ps1 -LogPath scratch\thor-debug\20260521-170941-speed-logcat.txt -Function 82282490 -StartGuest 8228252C -EndGuest 822825C4 -TargetMode AllSpanStoreContext -Top 32
Select-String -Path scratch\thor-debug\20260524-041413-speed-logcat.txt -Pattern 'Filtered function dump 82287788 OptHIR:.*(load_context \+296|load_context \+2628|store_context \+296|store_context \+2628|f\[1\]|fpscr)' -Context 4,8
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_hotpath_report.ps1 -LogPath scratch\thor-debug\20260524-041413-speed-logcat.txt -Function 82287788 -Phase OptHIR -Top 30
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_state_span_report.ps1 -LogPath scratch\thor-debug\20260524-041413-speed-logcat.txt -Function 82287788 -Phase OptHIR -Top 20
```

Files changed:

- `tools/thor/thor_hir_dead_state_store_dce_audit.ps1`
- `docs/research/20260524-094213-hir-dead-state-store-dce-audit.md`
- `docs/worklogs/20260524.md`
- `AGENTS.md`
- `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md`
- `.agents/skills/xenia-working-fast-autonomy/SKILL.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `tools/thor/thor_codex_goal_loop.ps1`

Findings:

- Added `tools/thor/thor_hir_dead_state_store_dce_audit.ps1`, an offline tool
  that parses filtered HIR, removes target `store_context` rows virtually, and
  recursively estimates which assignment-producing HIR rows would become dead.
- Exact `13` candidate-dead call-boundary stores produced
  `dead_assignments=0`. Moving the same suppression from A64 emission to HIR
  would not delete upstream `lvx128`, `vspltw`, `vmaddfp`, `lfs`, or `fmuls`
  work.
- Unsafe all-span `store_context` removal had an upper bound of
  `dead_assignments=22`, mostly the live FPR/fpscr path around `8228258C`,
  `82282590`, and `82282594`.
- The focused `82287788` HIR proves that path is live: the callee loads
  `f[1]` (`+296`) and `fpscr` (`+2628`), including immediate `stfs fr1`
  stores and later FP compare/arithmetic state updates.

Decision:

- Do not implement a HIR-level version of the exact `13`-store suppression.
  It would only repeat the tiny backend store skip that already missed speed.
- Do not skip live `f[1]` / `fpscr` state. The larger-looking DCE opportunity
  is a real callee argument/state path into `82287788`.
- Next useful lane is an interprocedural argument/state-roundtrip audit for
  `82282598 -> 82287788`, focused on whether `f[1]` / `fpscr` can be carried
  through a guarded function-pair mechanism or callee-local promotion without
  hiding guest-visible PPC state.

## 09:49-09:56 EDT - 82282598 -> 82287788 State Roundtrip Audit

Starting HEAD: `aaa76c0c5`

Goal: turn the HIR DCE audit's live `f[1]` / `fpscr` warning into a reusable
interprocedural audit before any state-carrier or callee-local promotion patch.

Commands run:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_interproc_state_roundtrip_audit.ps1 -ParentLogPath scratch\thor-debug\20260521-170941-speed-logcat.txt -ParentFunction 82282490 -ParentCallPc 82282598 -CalleeLogPath scratch\thor-debug\20260524-041413-speed-logcat.txt -CalleeFunction 82287788 -EdgeProfileLog scratch\thor-debug\20260524-030450-speed-logcat.txt -Offsets "56,296,2628,16" -Top 24
```

Files changed:

- `tools/thor/thor_hir_interproc_state_roundtrip_audit.ps1`
- `docs/research/20260524-095137-82282598-82287788-state-roundtrip-audit.md`
- `docs/worklogs/20260524.md`
- `AGENTS.md`
- `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md`
- `.agents/skills/xenia-working-fast-autonomy/SKILL.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `tools/thor/thor_codex_goal_loop.ps1`

Findings:

- Added `tools/thor/thor_hir_interproc_state_roundtrip_audit.ps1`, which joins
  parent/callee filtered HIR context access with optional dynamic call-edge rows.
- The dynamic `82282490` block `8228252C -> 82287788` edge is very hot:
  `calls_total=1691272`, `body_ticks_total=5653971`, and `ticks_per_call=4`.
- Parent `82282490` stores `r[3]` at `82282550`, `f[1]` and `fpscr` at
  `82282594`, and `lr` at `82282598` before or at the call.
- Callee `82287788` loads all tracked fields: `r[3] loads=12`, `f[1] loads=10`,
  `fpscr loads=26/stores=26`, and `lr loads=2`.
- This confirms the larger-looking DCE lane is live parent-to-callee state, not
  safe dead-store cleanup.

Decision:

- Do not skip live `r[3]`, `f[1]`, `fpscr`, or `lr` stores at this boundary.
- Next useful lane is a default-off, Blue-Dragon/function-pair carrier or
  callee-local promotion probe for `82282490 -> 82287788`, with explicit
  helper/exit/exception/alias flush rules and route proof before any quiet A/B.

## 09:58-10:07 EDT - 82287788 Callee-Local Promotion Audit

Starting HEAD: `a55844ac1`

Goal: check whether the live `f[1]` / `fpscr` state in callee `82287788` has
safe strict local reuse windows before any carrier or callee-local promotion
patch.

Commands run:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_callee_local_promotion_audit.ps1 -LogPath scratch\thor-debug\20260524-041413-speed-logcat.txt -Function 82287788 -Offsets "296,2628" -Top 20
```

Files changed:

- `tools/thor/thor_hir_callee_local_promotion_audit.ps1`
- `docs/research/20260524-100409-82287788-callee-local-promotion-audit.md`
- `docs/worklogs/20260524.md`
- `AGENTS.md`
- `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md`
- `.agents/skills/xenia-working-fast-autonomy/SKILL.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `tools/thor/thor_codex_goal_loop.ps1`

Findings:

- Added `tools/thor/thor_hir_callee_local_promotion_audit.ps1`, which splits a
  filtered HIR dump into conservative promotion windows broken by labels,
  context barriers, calls, branches, and returns.
- For `82287788`, `f[1]` has `loads=10`, `stores=0`, `regions=10`,
  `strict_redundant_loads=0`, `cross_boundary_reload_after_load=9`, and
  `risky_call_crossings=2`.
- For `fpscr`, the callee has `loads=26`, `stores=26`, `regions=26`,
  `strict_wins=0`, `cross_boundary_reload_after_store=25`, and
  `risky_call_crossings=1`.
- Strict same-region callee-local promotion has no win here. Useful reuse is
  across labels, branches, context barriers, and some calls.

Decision:

- Do not implement a blind callee-local context cache for `82287788`.
- Keep `f[1]` as the best candidate because the callee never stores it, but the
  next experiment must be a CFG/function-pair carrier audit/probe that proves
  call-clobber and flush rules.
- Treat `fpscr` as mutable/high-risk until a source review proves exact dirty
  update forwarding. Do not patch `fpscr` first.

## 10:08-10:18 EDT - 82287788 f[1] Carrier Safety Audit

Starting HEAD: `2bf7cde4e`

Goal: follow the `82282490 -> 82287788` live `f[1]` lane without guessing by
checking whether an entry carrier can survive helper calls and where real child
calls block reuse.

Commands run:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_fpr_carrier_safety_audit.ps1 -LogPath scratch\thor-debug\20260524-041413-speed-logcat.txt -Function 82287788 -Offset 296 -Top 20
Select-String -Path scratch\thor-debug\20260524-041413-speed-logcat.txt -Pattern "82287854|82287ED4|8228778C|82287EDC|82287EE4|82288220"
Get-Content src\xenia\cpu\backend\a64\a64_emitter.cc -TotalCount 2925 | Select-Object -Last 170
Get-Content src\xenia\cpu\xex_module.cc -TotalCount 1360 | Select-Object -Last 120
```

Files changed:

- `tools/thor/thor_hir_fpr_carrier_safety_audit.ps1`
- `docs/research/20260524-101421-82287788-f1-carrier-safety-audit.md`
- `docs/worklogs/20260524.md`
- `AGENTS.md`
- `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md`
- `.agents/skills/xenia-working-fast-autonomy/SKILL.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `tools/thor/thor_codex_goal_loop.ps1`

Findings:

- Added `tools/thor/thor_hir_fpr_carrier_safety_audit.ps1`, an offline HIR
  audit for target FPR context loads across helper/unknown calls.
- For `82287788` offset `296` (`f[1]`), the audit found `target_loads=10`,
  `target_stores=0`, `helper_whitelist=2`, and `unknown_call_blocked=8`.
- The only helper-whitelist loads are at `82287798` and `82287828`.
- The unknown calls are real PPC calls to `0x821CE028` at `82287854` and
  `82287ED4`.
- Source review of `TryEmitGprLrHelperCall` shows the `__savegprlr_28` /
  `__restgprlr_28` helpers touch GPR/LR state but not FPR, VMX, or fpscr
  state; `xex_module.cc` also detects GPR/LR helpers separately from FPR/VMX
  helpers.

Decision:

- Do not implement a speed patch from this audit alone.
- A helper-whitelist `f[1]` carrier currently covers only 2 of 10 static
  callee loads; the other 8 cross `821CE028` and need separate clobber/use
  proof.
- Keep `fpscr` out of this lane. It remains mutable/high-risk.
- Next useful slice is either a default-off runtime counter for dynamic
  helper-whitelist versus unknown-call-blocked `f[1]` hits, or a focused
  `821CE028` `f[1]` clobber/use audit.

## 10:20-10:27 EDT - 821CE028 f[1] Clobber Audit

Starting HEAD: `36c40a372`

Goal: resolve the `821CE028` unknown-call blocker from the `82287788` `f[1]`
carrier safety audit.

Commands run:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_fpr_carrier_safety_audit.ps1 -LogPath scratch\thor-debug\20260524-045022-speed-logcat.txt -Function 821CE028 -Offset 296 -Top 20
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_hotpath_report.ps1 -LogPath scratch\thor-debug\20260524-045022-speed-logcat.txt -Function 821CE028 -Phase OptHIR -Top 30
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_state_span_report.ps1 -LogPath scratch\thor-debug\20260524-045022-speed-logcat.txt -Function 821CE028 -Phase OptHIR -Top 20
Select-String -Path scratch\thor-debug\20260524-045022-speed-logcat.txt -Pattern 'Filtered function dump 821CE028 OptHIR:.*(load_context \+296|store_context \+296|load_context \+2628|store_context \+2628|call|call_indirect)' -Context 2,3
```

Files changed:

- `tools/thor/thor_hir_fpr_carrier_safety_audit.ps1`
- `docs/research/20260524-102113-821ce028-f1-clobber-audit.md`
- `docs/worklogs/20260524.md`
- `AGENTS.md`
- `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md`
- `.agents/skills/xenia-working-fast-autonomy/SKILL.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `tools/thor/thor_codex_goal_loop.ps1`

Findings:

- Updated `tools/thor/thor_hir_fpr_carrier_safety_audit.ps1` so a zero
  target-access function reports `decision=no_target_context_access_observed`
  instead of a misleading generic no-patch line.
- `821CE028` has `target_loads=0` and `target_stores=0` for `f[1]`
  (`+296`).
- The only calls in `821CE028` OptHIR are the two `call_indirect.6` return
  paths at `821CE1CC` and `821CE1D4`.
- The hotpath report shows the only FPR context traffic in `821CE028` is
  `f[0]` (`+288`) load/store; no `f[1]` or `fpscr` context traffic appears.

Decision:

- Treat `821CE028` as `f[1]`-preserving for the narrow parent `82287788`
  carrier lane.
- Do not patch `821CE028` or promote it as a speed target from this evidence;
  this only removes the child-call clobber uncertainty for `f[1]`.
- Next useful slice is a default-off `82287788` runtime carrier audit/probe
  that treats `__savegprlr_28`, `__restgprlr_28`, and direct calls to
  `0x821CE028` as `f[1]`-preserving and counts dynamic replacement
  opportunities for all 10 static `f[1]` loads before any quiet A/B.

## 10:26-10:45 EDT - 82287788 f[1] Carrier Runtime Audit

Starting HEAD: `b9ad485bf`

Goal: add and validate a default-off runtime counter for the `82287788`
`f[1]` carrier lane before changing generated behavior.

Commands run:

```powershell
git status --short --branch
Test-Path scratch\thor-debug\latest-blue-dragon-full-speed-proof.json
Get-Content -Raw scratch\thor-debug\codex-goal-loop.json
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370
Get-FileHash -Algorithm SHA256 android\android_studio_project\app\build\outputs\apk\github\debug\app-github-debug.apk
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 25 -Arm64SpeedProfileMinDelta 1 -Arm64SpeedProfileBodyTimeFilter "82282490,82287788" -Arm64SpeedProfileBodyTimeAfterMs 120000 -Arm64BlueDragonF1CarrierAudit true -StopAppAfterCapture true
rg -n "(?i)(Fatal signal|AndroidRuntime|FATAL EXCEPTION|VK_ERROR_DEVICE_LOST|GPU is hung|tombstone|backtrace|SIGSEGV|SIGABRT|crash)" scratch\thor-debug\20260524-103438-speed-logcat.txt scratch\thor-debug\20260524-103438-speed-logcat-filtered.txt
rg -n "A64 Blue Dragon f1 carrier audit|A64 speed profile body top" scratch\thor-debug\20260524-103438-speed-logcat.txt
```

Files changed:

- `src/xenia/cpu/backend/a64/a64_backend.cc`
- `src/xenia/cpu/backend/a64/a64_backend.h`
- `src/xenia/cpu/backend/a64/a64_sequences.cc`
- `android/android_studio_project/app/src/main/java/jp/xenia/emulator/EmulatorActivity.java`
- `tools/thor/thor_xenia_debug.ps1`
- `docs/research/20260524-104145-blue-dragon-f1-carrier-runtime-audit.md`
- `docs/worklogs/20260524.md`
- `AGENTS.md`
- `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md`
- `.agents/skills/xenia-working-fast-autonomy/SKILL.md`
- `tools/thor/thor_codex_goal_loop.ps1`

Findings:

- Added default-off `arm64_blue_dragon_f1_carrier_audit` and Android/Thor
  launch plumbing.
- NativeCore and FullDeploy passed; APK SHA is
  `514C6A8C0C14820BFE0ED48CF280D00371B30602AEEE5CD86AE06A82E05D139F`.
- Targeted capture `scratch/thor-debug/20260524-103438-*` reached visible
  opening sky/dragon-wing route with clean fatal-marker search.
- Final audit counters: `total=755910/2159587`,
  `helper_preserved=379058/1081707`, and
  `child_preserved=376852/1077880`.
- Final body-time still keeps `82282490` as the wall
  (`body_ticks_total=32144132`), while `82287788` is high-entry but cheap
  (`body_ticks_total=3578711`, `ticks_per_entry=5`).

Decision:

- The `f[1]` carrier lane is dynamically real, but the capture is audit-heavy
  and not speed proof.
- Keep `arm64_blue_dragon_f1_carrier_audit` default-off and out of presets.
- Next useful slice is a default-off `82287788` `f[1]` carrier replacement
  probe for offset `296`, preserving only across `__savegprlr_28`,
  `__restgprlr_28`, and direct calls to `0x821CE028`, with explicit kills for
  unknown calls, indirect calls, exits, exceptions, and overlapping stores.

## 10:44-11:12 EDT - 82287788 f[1] Carrier Fastpath A/B

Starting HEAD: `1bd6d0a5f`

Goal: implement the default-off `82287788` `f[1]` replacement probe suggested by
the runtime audit, then prove whether it is route-safe and faster on Thor.

Commands run:

```powershell
git status --short --branch
Test-Path scratch\thor-debug\latest-blue-dragon-full-speed-proof.json
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 25 -Arm64SpeedProfileMinDelta 1 -Arm64SpeedProfileBodyTimeFilter "82282490,82287788" -Arm64SpeedProfileBodyTimeAfterMs 120000 -Arm64BlueDragonF1CarrierFastpath true -Arm64BlueDragonF1CarrierAudit true -StopAppAfterCapture true
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 25 -Arm64SpeedProfileMinDelta 1 -Arm64BlueDragonF1CarrierFastpath false -Arm64BlueDragonF1CarrierAudit false -StopAppAfterCapture true
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 25 -Arm64SpeedProfileMinDelta 1 -Arm64BlueDragonF1CarrierFastpath true -Arm64BlueDragonF1CarrierAudit false -StopAppAfterCapture true
rg -n "(?i)(Fatal signal|AndroidRuntime|FATAL EXCEPTION|VK_ERROR_DEVICE_LOST|GPU is hung|tombstone|backtrace|SIGSEGV|SIGABRT|crash)" scratch\thor-debug\20260524-105424-speed-logcat.txt scratch\thor-debug\20260524-105424-speed-logcat-filtered.txt scratch\thor-debug\20260524-105813-speed-logcat.txt scratch\thor-debug\20260524-105813-speed-logcat-filtered.txt scratch\thor-debug\20260524-110156-speed-logcat.txt scratch\thor-debug\20260524-110156-speed-logcat-filtered.txt
```

Files changed:

- `src/xenia/cpu/backend/a64/a64_backend.cc`
- `src/xenia/cpu/backend/a64/a64_backend.h`
- `src/xenia/cpu/backend/a64/a64_emitter.cc`
- `src/xenia/cpu/backend/a64/a64_emitter.h`
- `src/xenia/cpu/backend/a64/a64_sequences.cc`
- `android/android_studio_project/app/src/main/java/jp/xenia/emulator/EmulatorActivity.java`
- `tools/thor/thor_xenia_debug.ps1`
- `docs/research/20260524-110545-blue-dragon-f1-carrier-fastpath-ab.md`
- `docs/worklogs/20260524.md`
- `AGENTS.md`
- `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md`
- `.agents/skills/xenia-working-fast-autonomy/SKILL.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `tools/thor/thor_codex_goal_loop.ps1`

Findings:

- Added `arm64_blue_dragon_f1_carrier_fastpath`, default-off and title-gated,
  for the audited `82287788` `f[1]` (`+296`) load sites.
- The fastpath uses a function-local A64 stack slot instead of pinning a host
  FPR: seed at `82287798`, then reload from the slot at the nine audited
  helper/`821CE028`-preserved PCs.
- NativeCore and FullDeploy passed; tested APK SHA is
  `A2A59F7777F77B52526ABF5E401194A4A7490D71B4F7A878ED8099B0CA7557A9`.
- Audit capture `scratch/thor-debug/20260524-105424-*` reached visible opening
  sky/dragon-wing route with clean fatal-marker search. Final counters were
  `seed=223805/688280` and `reuse=443749/1365365`; final body-time still put
  `82282490` first at `body_ticks_total=30401688` and `82287788` cheap at
  `body_ticks_total=3636251`, `ticks_per_entry=5`.
- Quiet same-APK control `scratch/thor-debug/20260524-105813-*` and
  fastpath-on `scratch/thor-debug/20260524-110156-*` were route-clean, but the
  fastpath did not prove speed. Control reached a later opening frame; fastpath
  changed `82287788` code size `35568 -> 35572`, and `82282490` remained the
  real wall.

Decision:

- Keep `arm64_blue_dragon_f1_carrier_fastpath` and
  `arm64_blue_dragon_f1_carrier_audit` default-off and out of presets.
- Do not repeat the exact f1 carrier A/B unchanged.
- Next useful lane is broader `8228252C-822825C4` parent/callee
  state/vector/FPR reduction, with an offline audit proving how to reduce live
  `82282490 -> 82287788` state round-trips without hiding guest-visible `r[3]`,
  `f[1]`, `fpscr`, or `lr` state.

## 11:15-11:24 EDT - 8228252C State Forwarding Plan

Starting HEAD: `aa41ceb28`

Goal: finish the broader `8228252C-822825C4` parent/callee state audit after
the narrow `82287788` `f[1]` fastpath missed speed proof.

Commands run:

```powershell
git status --short --branch
Test-Path scratch\thor-debug\latest-blue-dragon-full-speed-proof.json
Get-Content -Raw scratch\thor-debug\codex-goal-loop.json
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_interproc_state_roundtrip_audit.ps1 -ParentLogPath scratch\thor-debug\20260521-170941-speed-logcat.txt -ParentFunction 82282490 -ParentCallPc 82282598 -CalleeLogPath scratch\thor-debug\20260524-041413-speed-logcat.txt -CalleeFunction 82287788 -EdgeProfileLog scratch\thor-debug\20260524-030450-speed-logcat.txt -Offsets "56,296,2628,16" -Top 24
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_callee_local_promotion_audit.ps1 -LogPath scratch\thor-debug\20260524-041413-speed-logcat.txt -Function 82287788 -Offsets "56,296,2628,16" -Top 30
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_hotpath_report.ps1 -LogPath scratch\thor-debug\20260524-041413-speed-logcat.txt -Function 82287788
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_a64_codegen_audit.ps1 -LogPath scratch\thor-debug\20260521-170941-speed-logcat.txt -Function 82282490 -StartGuest 8228252C -EndGuest 822825C4 -BlockProfileLog scratch\thor-debug\20260524-030450-speed-logcat.txt -Top 16
```

Files changed:

- `tools/thor/thor_hir_interproc_state_roundtrip_audit.ps1`
- `docs/research/20260524-111940-8228252c-state-forwarding-plan.md`
- `docs/worklogs/20260524.md`
- `AGENTS.md`
- `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md`
- `.agents/skills/xenia-working-fast-autonomy/SKILL.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `tools/thor/thor_codex_goal_loop.ps1`

Findings:

- The interprocedural state audit now prints a `Forwarding Plan` with risk and
  static dynamic-edge upper bounds.
- The live `82282490` `82282598 -> 82287788` edge remains hot:
  `calls_total=1691272`, `body_ticks_total=5653971`, and
  `ticks_per_call=4`.
- `f[1]` is the only medium-risk read-only forwarding candidate:
  `loads=10`, `stores=0`, `static_load_upper=16912720`. The previous
  stack-slot carrier A/B missed speed proof, so the next version should be a
  function-pair/callee-variant design audit instead of the same local reload
  probe.
- `fpscr` has the largest static upper bound
  (`static_load_upper=43973072`, `static_store_upper=43973072`), but it is
  mutable and very high risk without exact dirty writeback rules.
- `r[3]` is mutable/high-risk, and `lr` is call-link state. Do not forward
  either from this evidence.

Decision:

- No generated-code behavior patch in this slice.
- Next useful lane is a default-off `82282490 -> 82287788`
  function-pair/callee-variant audit/design for `f[1]`, while separately
  modeling `fpscr` dirty caching before any `fpscr` patch.

## 11:24-11:34 EDT - 82287788 Function-Pair Variant Audit

Starting HEAD: `205f51c41`

Goal: turn the forwarding plan into a deterministic pair-variant audit before
any generated-code behavior patch.

Commands run:

```powershell
git status --short --branch
Test-Path scratch\thor-debug\latest-blue-dragon-full-speed-proof.json
Get-Content scratch\thor-debug\codex-goal-loop.json -TotalCount 80
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_function_pair_variant_audit.ps1 -ParentLogPath scratch\thor-debug\20260521-170941-speed-logcat.txt -ParentFunction 82282490 -ParentCallPc 82282598 -CalleeLogPath scratch\thor-debug\20260524-041413-speed-logcat.txt -CalleeFunction 82287788 -Offset 296 -EdgeProfileLog scratch\thor-debug\20260524-030450-speed-logcat.txt -PreservedCallPcs "82287854,82287ED4" -Top 24
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_function_pair_variant_audit.ps1 -ParentLogPath scratch\thor-debug\20260521-170941-speed-logcat.txt -ParentFunction 82282490 -ParentCallPc 82282598 -CalleeLogPath scratch\thor-debug\20260524-041413-speed-logcat.txt -CalleeFunction 82287788 -Offset 2628 -EdgeProfileLog scratch\thor-debug\20260524-030450-speed-logcat.txt -PreservedCallPcs "82287854,82287ED4" -Top 12
rg -n "void A64Emitter::Call\(|Pass the next call's guest return address in x0|ldr\(x0, ptr\(sp, static_cast<uint32_t>\(StackLayout::GUEST_CALL_RET_ADDR\)\)\)" src\xenia\cpu\backend\a64\a64_emitter.cc
```

Files changed:

- `tools/thor/thor_hir_function_pair_variant_audit.ps1`
- `docs/research/20260524-112944-82287788-function-pair-variant-audit.md`
- `docs/worklogs/20260524.md`
- `AGENTS.md`
- `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md`
- `.agents/skills/xenia-working-fast-autonomy/SKILL.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `tools/thor/thor_codex_goal_loop.ps1`

Findings:

- New tool confirms `f[1]` is a pair-specific entry-seed candidate for
  `82282490:82282598 -> 82287788`: parent seed store exists at `82282594`,
  callee has `loads=10`, `stores=0`, `replaceable_loads=10`,
  `unsafe_loads=0`, and `static_replace_upper=16912720`.
- The `fpscr` control is blocked: `loads=26`, `stores=26`,
  `variant_shape=blocked_by_callee_writes`.
- Source review shows the current A64 direct-call path passes guest return in
  `x0`, so a real `f[1]` pair variant needs a default-off custom thunk/entry
  seed or caller-seeded callee-slot proof. It should not casually alter the
  generic call ABI or normal `82287788` entry semantics.

Decision:

- No generated-code behavior patch in this slice.
- Next useful lane is source-designing the default-off pair-specific
  `f[1]` call/entry seed path, with parent context visibility preserved.

## 11:34-11:39 EDT - 82287788 Pair-Entry ROI Check

Starting HEAD: `7be884402`

Goal: decide whether a pair-specific `f[1]` entry seed is worth implementing
as the next behavior patch.

Commands run:

```powershell
git status --short --branch
Test-Path scratch\thor-debug\latest-blue-dragon-full-speed-proof.json
Get-Content scratch\thor-debug\codex-goal-loop.json -TotalCount 60
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_function_pair_variant_audit.ps1 -ParentLogPath scratch\thor-debug\20260521-170941-speed-logcat.txt -ParentFunction 82282490 -ParentCallPc 82282598 -CalleeLogPath scratch\thor-debug\20260524-041413-speed-logcat.txt -CalleeFunction 82287788 -Offset 296 -EdgeProfileLog scratch\thor-debug\20260524-030450-speed-logcat.txt -PreservedCallPcs "82287854,82287ED4" -CompareExistingStackCarrier -Top 10
```

Files changed:

- `tools/thor/thor_hir_function_pair_variant_audit.ps1`
- `docs/research/20260524-113615-82287788-pair-entry-roi.md`
- `docs/worklogs/20260524.md`
- `AGENTS.md`
- `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md`
- `.agents/skills/xenia-working-fast-autonomy/SKILL.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `tools/thor/thor_codex_goal_loop.ps1`

Findings:

- Added `-CompareExistingStackCarrier` to the function-pair audit.
- Raw pair-entry candidate still looks clean: `replaceable_loads=10`,
  `unsafe_loads=0`, and `static_replace_upper=16912720`.
- Compared to the existing stack-slot carrier, the incremental gain is only the
  first context load per callee call:
  `pair_incremental_static_upper=1691272`.

Decision:

- Do not implement a standalone `f[1]` pair-entry behavior patch next. Its
  extra ABI/prolog/thunk complexity is too high for the remaining one-load
  seed win after the stack-slot carrier A/B missed speed proof.
- Next useful lane is broader `8228252C-822825C4` state-cache/callee-variant
  design, or an `fpscr` dirty-cache audit with exact writeback points.

## 11:41-11:49 EDT - 82287788 FPSCR Dirty-Cache Audit

Starting HEAD: `b1212341d`

Goal: decide whether the larger `fpscr` upper bound from the
`82282490 -> 82287788` state-forwarding plan has cheap exact writeback points,
or whether it requires a full CFG/visibility model before any behavior patch.

Commands run:

```powershell
git status --short --branch
Test-Path scratch\thor-debug\latest-blue-dragon-full-speed-proof.json
Get-Content AGENTS.md -TotalCount 220
Get-Content scratch\thor-debug\codex-goal-loop.json -Raw
Get-Content .agents\skills\xenia-blue-dragon-full-speed-autopilot\SKILL.md -TotalCount 220
Get-Content .agents\skills\xenia-working-fast-autonomy\SKILL.md -TotalCount 220
Get-Content .agents\skills\xenia-codex-goal-loop\SKILL.md -TotalCount 220
Get-Content .agents\skills\xenia-continual-harness-refiner\SKILL.md -TotalCount 220
Get-Content .agents\skills\xenia-a64-speed-hotpath\SKILL.md -TotalCount 220
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_callee_local_promotion_audit.ps1 -LogPath scratch\thor-debug\20260524-041413-speed-logcat.txt -Function 82287788 -Offsets "2628" -Top 20
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_fpscr_dirty_cache_audit.ps1 -LogPath scratch\thor-debug\20260524-041413-speed-logcat.txt -Function 82287788 -EdgeProfileLog scratch\thor-debug\20260524-030450-speed-logcat.txt -ParentFunction 82282490 -CalleeFunction 82287788 -Top 30
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_fpscr_dirty_cache_audit.ps1 -LogPath scratch\thor-debug\20260521-170941-speed-logcat.txt -Function 82282490 -Top 20
```

Files changed:

- `tools/thor/thor_hir_fpscr_dirty_cache_audit.ps1`
- `docs/research/20260524-114614-82287788-fpscr-dirty-cache-audit.md`
- `docs/worklogs/20260524.md`
- `AGENTS.md`
- `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md`
- `.agents/skills/xenia-working-fast-autonomy/SKILL.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `tools/thor/thor_codex_goal_loop.ps1`

Findings:

- Added `tools/thor/thor_hir_fpscr_dirty_cache_audit.ps1`, which pairs
  `fpscr` (`+2628`) load/update/store rows and classifies each store-to-next
  load transition as strict local, CFG/PHI or stack-carrier, external
  visibility writeback, or exit flush.
- On `82287788`, the apparent `fpscr` opportunity is large but not local:
  `loads=26`, `stores=26`, `paired_load_store_updates=26`, dynamic upper
  `loads=43973072` and `stores=43973072`, but `strict_same_window=0`.
- The useful `82287788` transitions are cross-boundary:
  `cfg_phi_or_stack_carrier_required=24` and
  `external_visibility_flush_required=2`. Call-visible writeback PCs are
  `82287ED4`, `82287EDC`, `82287EE4`, and `82288220`.
- Parent `82282490` is worse for local `fpscr` caching:
  `loads=12`, `stores=12`, and all 12 transitions require external call
  visibility.

Decision:

- Do not implement an `fpscr` dirty-cache behavior patch next. It needs a
  CFG-aware dirty carrier with exact call/exit/helper visibility and writeback
  rules, not a Blue Dragon-only peephole.
- Next useful lane is broader body-backed `8228252C-822825C4`
  state/vector/FPR work, or a generic CFG-aware dirty-state carrier audit
  before any `fpscr` patch.

## 11:55-12:07 EDT - 8228252C Lane Closure And Next Target

Starting HEAD: `d409a4c26`

Goal: recheck the broader `82282490:8228252C-822825C4` span after the
`fpscr` dirty-cache audit, then decide whether to patch that span immediately
or pivot to the next body-backed local target.

Commands run:

```powershell
git status --short --branch
Test-Path scratch\thor-debug\latest-blue-dragon-full-speed-proof.json
Get-Content scratch\thor-debug\codex-goal-loop.json -Raw
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_call_boundary_state_audit.ps1 -ParentLogPath scratch\thor-debug\20260521-170941-speed-logcat.txt -ParentFunction 82282490 -ParentStartGuest 8228252C -ParentEndGuest 822825C4 -CalleeLogPath scratch\thor-debug\20260524-041413-speed-logcat.txt -CalleeFunction 82287788 -Phase OptHIR -Top 40
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_dead_state_store_dce_audit.ps1 -LogPath scratch\thor-debug\20260521-170941-speed-logcat.txt -Function 82282490 -StartGuest 8228252C -EndGuest 822825C4 -TargetMode BlueDragonCallBoundaryDead -Top 32
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_a64_codegen_audit.ps1 -LogPath scratch\thor-debug\20260521-170941-speed-logcat.txt -Function 82282490 -StartGuest 8228252C -EndGuest 822825C4 -BlockProfileLog scratch\thor-debug\20260524-030450-speed-logcat.txt -Top 24
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_dynamic_block_slice_report.ps1 -LogPath scratch\thor-debug\20260521-170941-speed-logcat.txt -Function 82282490 -BlockProfileLog scratch\thor-debug\20260524-030450-speed-logcat.txt -Top 20 -ContextTop 12
```

Files changed:

- `docs/research/20260524-115538-8228252c-lane-closure-next-target.md`
- `docs/worklogs/20260524.md`
- `AGENTS.md`
- `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md`
- `.agents/skills/xenia-working-fast-autonomy/SKILL.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `tools/thor/thor_codex_goal_loop.ps1`

Findings:

- `8228252C-822825C4` is still the largest local/exclusive span:
  `approx_exclusive=2876500`, `store_context=27`, `load_context=14`,
  `mul_add=3`, and `stvewx=3`.
- The current isolated sub-lanes are closed for immediate patching:
  call-boundary candidate-dead stores have no HIR DCE payoff, `f[1]` pair-entry
  only saves the seed load after the stack-slot carrier A/B missed, `fpscr`
  needs CFG-aware dirty writebacks, all-three `stvewx` was not speed-positive,
  and the three-PC `MUL_ADD_V128` fastpath was mixed.
- The corrected dynamic block slice report exposes `822824B8-822824E8` as a
  fresh non-call-heavy local candidate: `approx_exclusive=1099164`,
  `exclusive_pct=100`, `store_context=16`, and `load_context=4`, with CR/GPR
  compare-and-branch traffic.

Decision:

- Do not patch `8228252C-822825C4` behavior next unless the slice is a broader
  CFG-aware/interprocedural state-carrier design.
- Next low-risk local lane is an offline `822824B8-822824E8` CR/GPR
  compare-and-branch audit before any default-off function/span-gated codegen
  experiment.

## 12:02-12:16 EDT - 822824B8 Branch-State Audit

Starting HEAD: `e5d8ad788`

Goal: audit the fresh non-call-heavy `82282490:822824B8-822824E8` local span
before any codegen patch.

Commands run:

```powershell
git status --short --branch
Test-Path scratch\thor-debug\latest-blue-dragon-full-speed-proof.json
Get-Content AGENTS.md -TotalCount 80
Get-Content scratch\thor-debug\codex-goal-loop.json -Raw
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_dynamic_block_slice_report.ps1 -LogPath scratch\thor-debug\20260521-170941-speed-logcat.txt -Function 82282490 -BlockProfileLog scratch\thor-debug\20260524-030450-speed-logcat.txt -BlockGuests 822824B8 -Top 12 -ContextTop 20
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_a64_codegen_audit.ps1 -LogPath scratch\thor-debug\20260521-170941-speed-logcat.txt -Function 82282490 -StartGuest 822824B8 -EndGuest 822824E8 -BlockProfileLog scratch\thor-debug\20260524-030450-speed-logcat.txt -Top 24
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_branch_state_audit.ps1 -LogPath scratch\thor-debug\20260521-170941-speed-logcat.txt -Function 82282490 -StartGuest 822824B8 -EndGuest 822824E8 -BlockProfileLog scratch\thor-debug\20260524-030450-speed-logcat.txt -Top 20
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_branch_state_audit.ps1 -LogPath scratch\thor-debug\20260521-170941-speed-logcat.txt -Function 82282490 -StartGuest 822825F4 -EndGuest 82282600 -BlockProfileLog scratch\thor-debug\20260524-030450-speed-logcat.txt -Top 16
```

Files changed:

- `tools/thor/thor_hir_branch_state_audit.ps1`
- `docs/research/20260524-120704-822824b8-branch-state-audit.md`
- `docs/worklogs/20260524.md`
- `AGENTS.md`
- `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md`
- `.agents/skills/xenia-working-fast-autonomy/SKILL.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `tools/thor/thor_codex_goal_loop.ps1`

Findings:

- Added `tools/thor/thor_hir_branch_state_audit.ps1`, which reports context
  loads/stores, branch predicates stored to context, and same-context reloads
  that would need a fallthrough-only carrier across branch barriers.
- `822824B8-822824E8` is local and body-backed:
  `body_ticks_total=1099164`, `class_stores=cr:9,gpr:7`, and
  `class_loads=gpr:4`.
- The three branch predicates are the risky CR-store shape:
  `cr6.none_equal@822824C4`, `cr6.none_equal@822824D4`, and `cr0.eq@822824DC`
  are stored before `context_barrier` / branch.
- The only GPR reload opportunities are `r[11]` at `822824CC`, `r[11]` at
  `822824DC`, and `r[31]` at `822824E4`; all cross a branch barrier and are
  fallthrough-only.
- The loop tail `822825F4-82282600` is also local but has only the same CR
  predicate-store shape and no reload opportunity.

Decision:

- Do not implement a `822824B8-822824E8` behavior patch next. It is either the
  known-negative CR store/barrier lane or a three-load GPR carrier upper bound,
  which is too small for another standalone probe.
- Next useful output is a broader CFG-aware branch/GPR carrier audit across the
  `822824B8 <-> 822825F4` loop and nearby branchy spans, or a return to
  higher-traffic CFG-aware/interprocedural state-carrier design.

## 12:12-12:22 EDT - 82282490 Branch Loop Aggregate Audit

Starting HEAD: `ddfef0ce6`

Goal: broaden the fresh `822824B8-822824E8` branch-state audit across the
nearby entry/setup and loop tail before changing generated-code behavior.

Commands run:

```powershell
git status --short --branch
Test-Path scratch\thor-debug\latest-blue-dragon-full-speed-proof.json
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_dynamic_block_slice_report.ps1 -LogPath scratch\thor-debug\20260521-170941-speed-logcat.txt -Function 82282490 -BlockProfileLog scratch\thor-debug\20260524-030450-speed-logcat.txt -Top 8 -ContextTop 12
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_branch_state_audit.ps1 -LogPath scratch\thor-debug\20260521-170941-speed-logcat.txt -Function 82282490 -StartGuest 82282490 -EndGuest 822824B8 -BlockProfileLog scratch\thor-debug\20260524-030450-speed-logcat.txt -Top 16
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_branch_state_audit.ps1 -LogPath scratch\thor-debug\20260521-170941-speed-logcat.txt -Function 82282490 -StartGuest 822824B8 -EndGuest 822824E8 -BlockProfileLog scratch\thor-debug\20260524-030450-speed-logcat.txt -Top 20
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_branch_state_audit.ps1 -LogPath scratch\thor-debug\20260521-170941-speed-logcat.txt -Function 82282490 -StartGuest 822825F4 -EndGuest 82282600 -BlockProfileLog scratch\thor-debug\20260524-030450-speed-logcat.txt -Top 16
```

Files changed:

- `docs/research/20260524-121503-82282490-branch-loop-aggregate-audit.md`
- `docs/worklogs/20260524.md`
- `AGENTS.md`
- `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md`
- `.agents/skills/xenia-working-fast-autonomy/SKILL.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `tools/thor/thor_codex_goal_loop.ps1`

Findings:

- The dynamic local-exclusive ranking still favors bigger state/vector spans:
  `8228252C-822825C4` remains first at `approx_exclusive=2876500`, then
  `822824F0-82282528` at `2021174`.
- The branch loop is real but narrow: `822824B8-822824E8` contributes
  `1099164`, and loop tail `822825F4-82282600` contributes `1033964`, or
  `2133128` combined local-exclusive ticks.
- The nearby entry/setup span `82282490-822824B8` contributes only `249802`
  ticks and has no GPR reload opportunity.
- Across the branch loop there are four CR predicate stores before
  `context_barrier` / branch, but only three GPR reload opportunities, all
  fallthrough-only and all in `822824B8-822824E8`.

Decision:

- Do not patch CR store/barrier fusion or a narrow branch-local GPR carrier
  next. The CR lane is known-negative/risky and the GPR upper bound is too
  small for another standalone speed probe.
- No Thor run was needed for this offline lane closure.
- Next useful output should return to higher-traffic CFG-aware or
  interprocedural state-carrier work around `8228252C-822825C4` and the hot
  `82282490 -> 82287788` state round-trip, with exact call/helper/barrier/exit
  flush rules.

## 12:22-12:30 EDT - Thor Controller Device-Aware Input Fix

Starting HEAD: `ddfef0ce6`

Goal: fix the Thor built-in controller path for normal emulator launches after
the launcher had already been switched to `hid=android`.

Commands run:

```powershell
adb devices
adb shell getprop ro.product.model
adb shell dumpsys input
adb shell cat /system/usr/keylayout/Vendor_2020_Product_0111.kl
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode ApkShellDeploy -DeviceSerial c3ca0370
adb logcat -c
adb shell am force-stop jp.xenia.emulator.github.debug
adb shell am start -n jp.xenia.emulator.github.debug/jp.xenia.emulator.EmulatorActivity --es gpu vulkan --es cpu arm64 --es apu nop --es hid android --ez android_hide_osd true
adb shell input keyevent 19
adb shell input keyevent 96
adb shell input keyevent 98
adb logcat -d -v time
```

Files changed:

- `android/android_studio_project/app/src/main/java/jp/xenia/emulator/EmulatorActivity.java`
- `src/xenia/hid/android/android_input_driver.cc`
- `docs/research/20260524-122605-thor-controller-device-aware-input.md`
- `docs/worklogs/20260524.md`
- `AGENTS.md`
- `README.md`

Findings:

- Thor still exposes the built-in pad as `Odin Controller`, vendor `0x2020`,
  product `0x0111`, with `KEYBOARD | GAMEPAD | JOYSTICK` sources and axes
  `X/Y/Z/RZ/HAT_X/HAT_Y/GAS/BRAKE`.
- The keylayout includes `BUTTON_C` and `BUTTON_Z`, which the native Android
  HID mapper did not previously handle.
- The Activity key filter could still drop D-pad-style events when Android
  delivered them as keyboard/virtual-source key events.

Patch:

- `EmulatorActivity` now forwards recognized D-pad/gamepad keycodes regardless
  of source, uses an `Odin Controller` device fallback for motion, retries axis
  ranges through joystick/gamepad sources, and logs the first few controller
  bridge events with tag `XeniaInput`.
- Native Android HID maps `BUTTON_C -> X` and `BUTTON_Z -> Y`.

Validation:

- `FullDeploy -DeviceSerial c3ca0370` passed and streamed install succeeded.
- `ApkShellDeploy -DeviceSerial c3ca0370` passed after the Java-only tightening.
- Launching `EmulatorActivity` with `hid=android` logged
  `Android HID: active as XInput controller 1`.
- Synthetic `DPAD_UP`, `BUTTON_A`, and `BUTTON_C` keyevents reached Java
  `XeniaInput` and native `Android HID: key` down/up logs.
- Filtered validation logs showed no `AndroidRuntime`, `UnsatisfiedLinkError`,
  or native fatal lines.

## 13:14-13:19 EDT - 8228252C State-Carrier Design Constraints

Starting HEAD: `83971c568`

Goal: follow the fresh goal-loop priority after the branch-loop closure and
decide whether `8228252C-822825C4` is ready for a behavior patch, or whether it
needs a broader state-carrier audit first.

Commands run:

```powershell
git status --short --branch
Test-Path scratch\thor-debug\latest-blue-dragon-full-speed-proof.json
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_dynamic_block_slice_report.ps1 -LogPath scratch\thor-debug\20260521-170941-speed-logcat.txt -Function 82282490 -BlockProfileLog scratch\thor-debug\20260524-030450-speed-logcat.txt -Top 8 -ContextTop 12
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_interproc_state_roundtrip_audit.ps1 -ParentLogPath scratch\thor-debug\20260521-170941-speed-logcat.txt -ParentFunction 82282490 -ParentCallPc 82282598 -CalleeLogPath scratch\thor-debug\20260524-041413-speed-logcat.txt -CalleeFunction 82287788 -EdgeProfileLog scratch\thor-debug\20260524-030450-speed-logcat.txt -Top 40
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_function_pair_variant_audit.ps1 -ParentLogPath scratch\thor-debug\20260521-170941-speed-logcat.txt -ParentFunction 82282490 -ParentCallPc 82282598 -CalleeLogPath scratch\thor-debug\20260524-041413-speed-logcat.txt -CalleeFunction 82287788 -Offset 296 -EdgeProfileLog scratch\thor-debug\20260524-030450-speed-logcat.txt -PreservedCallPcs 82287854,82287ED4 -CompareExistingStackCarrier -Top 40
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_fpscr_dirty_cache_audit.ps1 -LogPath scratch\thor-debug\20260524-041413-speed-logcat.txt -Function 82287788 -EdgeProfileLog scratch\thor-debug\20260524-030450-speed-logcat.txt -ParentFunction 82282490 -CalleeFunction 82287788 -Top 40
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_codex_goal_loop.ps1 -Mode EnableBlueDragonFullSpeed -MaxAttempts 48
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_codex_goal_loop.ps1 -Mode Status
git diff --check
python "C:\Users\leanerdesigner\.codex\skills\.system\skill-creator\scripts\quick_validate.py" .agents\skills\xenia-blue-dragon-full-speed-autopilot
python "C:\Users\leanerdesigner\.codex\skills\.system\skill-creator\scripts\quick_validate.py" .agents\skills\xenia-working-fast-autonomy
python "C:\Users\leanerdesigner\.codex\skills\.system\skill-creator\scripts\quick_validate.py" .agents\skills\xenia-a64-speed-hotpath
```

Files changed:

- `docs/research/20260524-131709-8228252c-state-carrier-design-constraints.md`
- `docs/worklogs/20260524.md`
- `AGENTS.md`
- `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md`
- `.agents/skills/xenia-working-fast-autonomy/SKILL.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `tools/thor/thor_codex_goal_loop.ps1`

Findings:

- `8228252C-822825C4` remains the top `82282490` local/exclusive target:
  `body_total=8530471`, child edge `8228252C -> 82287788` body `5653971`,
  `edge_calls_total=1691272`, and approximate exclusive `2876500`.
- Parent `82282490` stores `r[3]`, `f[1]`, `fpscr`, and `lr` before or at
  `82282598 -> 82287788`; callee `82287788` loads all four.
- `f[1]` is read-only in the callee, but the standalone pair-entry ROI is only
  one seed context load over the already-tested stack-slot carrier.
- `fpscr` has a much larger access upper, but `strict_same_window=0`; it needs
  CFG-aware dirty state plus call-visible writebacks at `82287ED4`,
  `82287EDC`, `82287EE4`, and `82288220`.
- `r[3]` is mutable and `lr` is call-link state.

Decision:

- Do not patch generated-code behavior next from a local PC fold, a standalone
  `f[1]` thunk, or an `fpscr` shortcut.
- No Thor run was needed for this offline decision.
- Next useful output is a deterministic CFG/interprocedural state-carrier audit
  that prints exact seed, kill, and writeback requirements for
  `8228252C-822825C4` and `82282598 -> 82287788`.

Validation:

- `git diff --check` passed with only the existing CRLF conversion warnings.
- Repo-local skill validation passed for the updated Blue Dragon autopilot,
  working-fast autonomy, and A64 speed-hotpath skills.
- Goal loop was re-armed for `blue-dragon-full-speed`.

## 13:26-13:33 EDT - 8228252C State-Carrier Design Audit Tool

Starting HEAD: `7c6a2525a`

Goal: turn the previous state-carrier constraints into a deterministic audit
tool that prints seed, kill, and writeback requirements for the top
`82282490` local span and hot callee edge before any behavior patch.

Commands run:

```powershell
git status --short --branch
Test-Path scratch\thor-debug\latest-blue-dragon-full-speed-proof.json
Get-Content scratch\thor-debug\codex-goal-loop.json -Raw
Get-Content docs\worklogs\20260524.md -Tail 140
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_state_carrier_design_audit.ps1 -ParentLogPath scratch\thor-debug\20260521-170941-speed-logcat.txt -ParentFunction 82282490 -ParentStartGuest 8228252C -ParentEndGuest 822825C4 -ParentCallPc 82282598 -CalleeLogPath scratch\thor-debug\20260524-041413-speed-logcat.txt -CalleeFunction 82287788 -EdgeProfileLog scratch\thor-debug\20260524-030450-speed-logcat.txt -PreservedCallPcs 82287854,82287ED4 -Top 20
git diff --check
python "C:\Users\leanerdesigner\.codex\skills\.system\skill-creator\scripts\quick_validate.py" .agents\skills\xenia-blue-dragon-full-speed-autopilot
python "C:\Users\leanerdesigner\.codex\skills\.system\skill-creator\scripts\quick_validate.py" .agents\skills\xenia-working-fast-autonomy
python "C:\Users\leanerdesigner\.codex\skills\.system\skill-creator\scripts\quick_validate.py" .agents\skills\xenia-a64-speed-hotpath
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_codex_goal_loop.ps1 -Mode EnableBlueDragonFullSpeed -MaxAttempts 48
```

Files changed:

- `tools/thor/thor_hir_state_carrier_design_audit.ps1`
- `docs/research/20260524-133027-8228252c-state-carrier-design-audit.md`
- `docs/worklogs/20260524.md`
- `AGENTS.md`
- `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md`
- `.agents/skills/xenia-working-fast-autonomy/SKILL.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `tools/thor/thor_codex_goal_loop.ps1`

Findings:

- The new tool joins parent HIR span, callee HIR, and dynamic edge rows.
- `8228252C -> 82287788` remains hot:
  `calls_total=1691272`, `body_ticks_total=5653971`, `ticks_per_call=4`.
- `f[1]` is a medium-risk `read_only_entry_seed`: parent seed at `82282594`,
  callee `loads=10`, `stores=0`, dynamic load upper `16912720`.
- `fpscr` is the large but very-high-risk `dirty_cfg_carrier`: parent seed at
  `82282594`, callee `loads=26`, `stores=26`, dynamic load/store upper
  `43973072/43973072`, and required writebacks at `82287ED4`, `82287EDC`,
  `82287EE4`, and `82288220`.
- `r[3]` is mutable/high-risk, and `lr` is blocked as call-link state.

Decision:

- Do not patch generated behavior or run a speed A/B from this directly.
- Next useful output is a default-off carrier-design counter/probe that reports
  dynamic seed hits, reads, dirty writes, writebacks, kills, and fallbacks for
  `f[1]` and/or `fpscr` without changing normal generated-code behavior.

Validation:

- The new state-carrier design audit runs successfully on the current
  `82282490` parent and `82287788` callee logs.
- `git diff --check` passed with only the existing CRLF conversion warnings.
- Repo-local skill validation passed for the updated Blue Dragon autopilot,
  working-fast autonomy, and A64 speed-hotpath skills.
- Goal loop was re-armed for `blue-dragon-full-speed`.

## 13:41-13:55 EDT - State-Carrier Design Runtime Probe

Starting HEAD: `984458da8`

Goal: implement the default-off counter/probe requested by the state-carrier
design audit, without changing generated behavior or running a speed A/B.

Commands run:

```powershell
git status --short --branch
Test-Path scratch\thor-debug\latest-blue-dragon-full-speed-proof.json
Get-Content scratch\thor-debug\codex-goal-loop.json -Raw
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode ApkShell
Get-FileHash android\android_studio_project\app\build\outputs\apk\github\debug\app-github-debug.apk -Algorithm SHA256
git diff --check
python "C:\Users\leanerdesigner\.codex\skills\.system\skill-creator\scripts\quick_validate.py" .agents\skills\xenia-blue-dragon-full-speed-autopilot
python "C:\Users\leanerdesigner\.codex\skills\.system\skill-creator\scripts\quick_validate.py" .agents\skills\xenia-working-fast-autonomy
python "C:\Users\leanerdesigner\.codex\skills\.system\skill-creator\scripts\quick_validate.py" .agents\skills\xenia-a64-speed-hotpath
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_continual_harness_review.ps1 -Mode Window
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_codex_goal_loop.ps1 -Mode EnableBlueDragonFullSpeed -MaxAttempts 48
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_codex_goal_loop.ps1 -Mode Status
```

Files changed:

- `src/xenia/cpu/backend/a64/a64_backend.cc`
- `src/xenia/cpu/backend/a64/a64_backend.h`
- `src/xenia/cpu/backend/a64/a64_emitter.cc`
- `src/xenia/cpu/backend/a64/a64_sequences.cc`
- `android/android_studio_project/app/src/main/java/jp/xenia/emulator/EmulatorActivity.java`
- `tools/thor/thor_xenia_debug.ps1`
- `docs/research/20260524-135034-blue-dragon-state-carrier-design-probe.md`
- `docs/worklogs/20260524.md`
- `AGENTS.md`
- `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md`
- `.agents/skills/xenia-working-fast-autonomy/SKILL.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `tools/thor/thor_codex_goal_loop.ps1`

Patch:

- Added default-off cvar
  `arm64_blue_dragon_state_carrier_design_audit`.
- The cvar is counter-only and title/function/PC/offset gated to the audited
  `82282490 -> 82287788` path.
- The speed-profile row now reports `f1_seed`, `f1_read`,
  `f1_helper_read`, `f1_child_read`, `f1_child_call`, `f1_fallback`,
  `fpscr_seed`, `fpscr_read`, `fpscr_dirty_write`,
  `fpscr_required_writeback`, `fpscr_call_kill`, and `fpscr_fallback`.
- Android intent bridging and `tools/thor/thor_xenia_debug.ps1` expose
  `-Arm64BlueDragonStateCarrierDesignAudit`.

Validation:

- `NativeCore` passed.
- `ApkShell` passed.
- Packaged APK SHA-256:
  `5A80DD15AF4471FABE16E7370D6CA5EB25FC8582993F0208BE98DB33EFCFA2FD`.
- `git diff --check` passed with only the existing CRLF conversion warnings.
- Repo-local skill validation passed for the updated Blue Dragon autopilot,
  working-fast autonomy, and A64 speed-hotpath skills.
- Goal loop was re-armed for `blue-dragon-full-speed`.

Decision:

- No Thor route capture was run in this slice; this is a committed probe patch.
- Do not judge FPS from the next audit run and do not enable the probe in
  presets.
- Next useful output is a route-stabilized audit capture with
  `-Arm64BlueDragonStateCarrierDesignAudit true`,
  `-Arm64SpeedProfileBodyTimeFilter "82282490,82287788"`, and
  `-Arm64SpeedProfileBodyTimeAfterMs 120000`.

## 13:56-14:05 EDT - Mature Vulkan Port Design Research

Starting HEAD: `ea86aa8a9`

Goal: answer how mature emulator Vulkan ports are built and upgrade
xenia-thor's GPU design guidance.

Commands run:

```powershell
git status --short --branch
Get-Process | Where-Object { $_.ProcessName -match 'adb|gradle|java|git|xenia|codex|powershell' }
Get-CimInstance Win32_Process | Where-Object { $_.Name -match 'adb|gradle|java|git|powershell|xenia|codex' }
Get-Content AGENTS.md -TotalCount 220
Get-Content .agents\skills\xenia-vulkan-adreno-renderdoc\SKILL.md -TotalCount 260
Get-Content docs\worklogs\20260524.md -Tail 180
Get-Content scratch\thor-debug\codex-goal-loop.json -TotalCount 120
Get-Content docs\gpu.md -TotalCount 220
rg -n "vulkan|Vulkan|Adreno|RenderDoc|AGI|pipeline|shader cache|EDRAM|resolve|present|frame" docs\research AGENTS.md .agents\skills\xenia-vulkan-adreno-renderdoc\SKILL.md -g "*.md"
rg --files src\xenia\gpu\vulkan src\xenia\ui\vulkan tools\thor | rg "vulkan|renderdoc|xenia_debug|gpu"
rg -n "PipelineCache|pipeline cache|VkPipelineCache|cache|async|thread|deferred|descriptor|timeline|completion|trace|EDRAM|resolve|barrier|layout|swap|present" src\xenia\gpu\vulkan\vulkan_pipeline_cache.cc src\xenia\gpu\vulkan\vulkan_command_processor.cc src\xenia\gpu\vulkan\vulkan_texture_cache.cc src\xenia\gpu\vulkan\vulkan_render_target_cache.cc src\xenia\ui\vulkan\vulkan_gpu_completion_timeline.cc src\xenia\ui\vulkan\vulkan_presenter.cc
adb devices -l
git diff --check
python "C:\Users\leanerdesigner\.codex\skills\.system\skill-creator\scripts\quick_validate.py" .agents\skills\xenia-vulkan-adreno-renderdoc
```

Files changed:

- `docs/research/20260524-135929-mature-vulkan-port-design.md`
- `AGENTS.md`
- `.agents/skills/xenia-vulkan-adreno-renderdoc/SKILL.md`
- `docs/worklogs/20260524.md`

Findings:

- Mature emulator Vulkan ports converge around infrastructure, not one magic
  renderer switch: device profiles, shader/pipeline cache systems, deterministic
  trace/replay, explicit sync attribution, EDRAM/resolve correctness, and
  separate validation/RenderDoc/AGI/perf lanes.
- Dolphin, RPCS3, Cemu, and PPSSPP all treat shader/pipeline stutter as a
  runtime system with correctness tradeoffs, not as a simple async toggle.
- Android/Adreno work needs a device/driver bug profile and mobile tiler
  bandwidth discipline before optional format, cache, or presentation changes.
- For Blue Dragon now, the quiet evidence still says CPU/JIT is the wall; GPU
  Commands around the latest quiet A/B were only about 11.5%, so "push more GPU"
  is not the current speed lever.

Decision:

- Added a repo design ladder for future Vulkan maturity work.
- Updated the Vulkan/Adreno skill so future GPU slices require fresh GPU-wall
  evidence before RenderDoc, AGI, async pipeline compilation, persistent cache
  work, or Adreno-specific workarounds.
- No build, deploy, or Thor capture was required for this docs-only design pass.
- User correction: unrelated Windows-only PS3/Eternal Sonata tests are not an
  active xenia-thor slice by themselves. Future xenia-thor runtime changes
  should still prefer Thor proof unless a real same-device, same-repo, same-git,
  or host-saturation conflict exists.

Validation:

- `adb devices -l` showed Thor `c3ca0370` online as `AYN_Thor`.
- `git diff --check` passed with only existing LF-to-CRLF warnings.
- Repo-local `xenia-vulkan-adreno-renderdoc` skill validation passed.

## 14:05-14:15 EDT - State-Carrier Audit Capture Log-Level Fix

Starting HEAD: `93da0d55d`

Goal: run the first Thor route capture for
`arm64_blue_dragon_state_carrier_design_audit` and leave usable evidence or a
concrete blocker before any carrier behavior patch.

Commands run:

```powershell
git status --short --branch
Test-Path scratch\thor-debug\latest-blue-dragon-full-speed-proof.json
Get-Process | Where-Object { $_.ProcessName -match 'gradle|java|adb|git|xenia|codex|powershell' }
Get-CimInstance Win32_Process | Where-Object { $_.ProcessId -in 10004,18080,18124,21520,26444,10584,10696,21492,16440 }
Get-Content scratch\thor-debug\codex-goal-loop.json -Raw
Get-FileHash android\android_studio_project\app\build\outputs\apk\github\debug\app-github-debug.apk -Algorithm SHA256
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode Install -DeviceSerial c3ca0370
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 210 -PerfSampleSeconds "60,120,180,210" -Arm64BlueDragonStateCarrierDesignAudit true -Arm64SpeedProfileBodyTimeFilter "82282490,82287788" -Arm64SpeedProfileBodyTimeAfterMs 120000
rg -n -i "AndroidRuntime|FATAL EXCEPTION|Fatal signal|tombstone|backtrace|SIGSEGV|assert|GPU is hung|device lost|ANR|crash|Xenia guest fatal" scratch\thor-debug\20260524-140649-speed-logcat.txt scratch\thor-debug\20260524-140649-speed-logcat-filtered.txt
rg -n -i "state.*carrier|carrier.*audit|body.*time|speed profile|82282490|82287788|main thread|gpu commands|xenia|A64|arm64" scratch\thor-debug\20260524-140649-speed-logcat.txt scratch\thor-debug\20260524-140649-speed-logcat-filtered.txt scratch\thor-debug\20260524-140649-perf-final.txt
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 5 -PerfSampleSeconds "5" -Arm64BlueDragonStateCarrierDesignAudit true -Arm64SpeedProfileBodyTimeFilter "82282490,82287788" -Arm64SpeedProfileBodyTimeAfterMs 120000
Select-String -Path scratch\thor-debug\20260524-141830-meta.txt -Pattern 'timestamp=|head=|apk_sha256=|log_level=|arm64_blue_dragon_state_carrier_design_audit=|arm64_speed_profile_body_time_filter='
rg -n -i "AndroidRuntime|FATAL EXCEPTION|Fatal signal|tombstone|backtrace|SIGSEGV|assert|GPU is hung|device lost|ANR|crash|Xenia guest fatal" scratch\thor-debug\20260524-141830-speed-logcat.txt scratch\thor-debug\20260524-141830-speed-logcat-filtered.txt
powershell -NoProfile -ExecutionPolicy Bypass -Command "`$tokens=`$null; `$errors=`$null; [void][System.Management.Automation.Language.Parser]::ParseFile('tools\thor\thor_xenia_debug.ps1', [ref]`$tokens, [ref]`$errors); if (`$errors.Count) { `$errors | ForEach-Object { `$_.Message }; exit 1 } else { 'thor_xenia_debug parse-ok' }"
powershell -NoProfile -ExecutionPolicy Bypass -Command "`$tokens=`$null; `$errors=`$null; [void][System.Management.Automation.Language.Parser]::ParseFile('tools\thor\thor_codex_goal_loop.ps1', [ref]`$tokens, [ref]`$errors); if (`$errors.Count) { `$errors | ForEach-Object { `$_.Message }; exit 1 } else { 'goal_loop parse-ok' }"
git diff --check
python "C:\Users\leanerdesigner\.codex\skills\.system\skill-creator\scripts\quick_validate.py" .agents\skills\xenia-blue-dragon-full-speed-autopilot
python "C:\Users\leanerdesigner\.codex\skills\.system\skill-creator\scripts\quick_validate.py" .agents\skills\xenia-working-fast-autonomy
python "C:\Users\leanerdesigner\.codex\skills\.system\skill-creator\scripts\quick_validate.py" .agents\skills\xenia-a64-speed-hotpath
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_continual_harness_review.ps1 -Mode Window
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_codex_goal_loop.ps1 -Mode EnableBlueDragonFullSpeed -MaxAttempts 48
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_codex_goal_loop.ps1 -Mode Status
```

Files changed:

- `tools/thor/thor_xenia_debug.ps1`
- `docs/research/20260524-141502-state-carrier-audit-route-capture.md`
- `docs/worklogs/20260524.md`
- `AGENTS.md`
- `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md`
- `.agents/skills/xenia-working-fast-autonomy/SKILL.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `tools/thor/thor_codex_goal_loop.ps1`

Capture:

- `scratch/thor-debug/20260524-140649-speed-logcat.txt`
- `scratch/thor-debug/20260524-140649-speed-logcat-filtered.txt`
- `scratch/thor-debug/20260524-140649-meta.txt`
- `scratch/thor-debug/20260524-140649-screenshot.png`
- `scratch/thor-debug/20260524-140649-perf-final.txt`

Findings:

- The APK SHA was
  `5A80DD15AF4471FABE16E7370D6CA5EB25FC8582993F0208BE98DB33EFCFA2FD`.
- The capture reached the visible opening sky / dragon-wing route with the
  Microsoft Game Studios presentation frame.
- Fatal-marker search was clean for AndroidRuntime, fatal exceptions, fatal
  signals, tombstones, backtraces, SIGSEGV, asserts, ANRs, device-lost,
  GPU-hung, and Xenia guest fatal markers.
- Metadata confirmed `arm64_blue_dragon_state_carrier_design_audit=true`,
  `arm64_speed_profile_body_time_filter=82282490,82287788`, and
  `arm64_speed_profile_body_time_after_ms=120000`, but also `log_level=0`.
- No `A64 Blue Dragon state-carrier design audit` row and no `82282490` /
  `82287788` body-time rows landed.
- Final thread sample still looks CPU/JIT plus XMA-heavy: Main Thread `103%`,
  XMA Decoder `50.0%`, GPU Commands `3.8%`, GPU VSync `3.8%`, Draw Thread
  `0.0%`.

Patch:

- `tools/thor/thor_xenia_debug.ps1` now auto-lifts Blue Dragon speed captures
  to `LogLevel=1` when body-time, block/call-edge, thread-snapshot, or audit
  rows are requested.
- Quiet no-instrumentation captures still default to `LogLevel=0` unless the
  caller explicitly passes `-LogLevel`.
- Short validation capture `scratch/thor-debug/20260524-141830-*` was not a
  route run, but confirmed the new default: metadata recorded
  `arm64_blue_dragon_state_carrier_design_audit=true`,
  `arm64_speed_profile_body_time_filter=82282490,82287788`, and `log_level=1`
  without an explicit `-LogLevel` argument. Its fatal-marker search was clean.
- `tools/thor/thor_continual_harness_review.ps1 -Mode Window` regenerated the
  local scratch window and confirmed the pre-rearm goal prompt was stale.
- The goal loop was re-armed for `blue-dragon-full-speed`; its current prompt
  now points at this log-level blocker and the `LogLevel=1` rerun.

Decision:

- Treat `scratch/thor-debug/20260524-140649-*` as route-clean proof for the
  current APK and cvars, but not as a successful state-carrier counter capture.
- Do not patch generated-code behavior from this no-row capture.
- Do not rerun the exact same command unchanged.
- Next useful output is the same route-stabilized audit with the fixed script
  default or explicit `-LogLevel 1`, then use the counter row before any carrier
  behavior patch.

## 14:26-14:45 EDT - State-Carrier Audit Interval Fix And Counter Proof

Starting HEAD: `4669170b1`

Goal: rerun the state-carrier audit with the fixed log level, prove whether the
counter/body rows land, and leave the next lane without patching generated-code
behavior from audit-only evidence.

Commands run:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode Install -DeviceSerial c3ca0370
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 210 -PerfSampleSeconds "60,120,180,210" -Arm64BlueDragonStateCarrierDesignAudit true -Arm64SpeedProfileBodyTimeFilter "82282490,82287788" -Arm64SpeedProfileBodyTimeAfterMs 120000
rg -n -i "AndroidRuntime|FATAL EXCEPTION|Fatal signal|tombstone|backtrace|SIGSEGV|assert|GPU is hung|device lost|ANR|crash|Xenia guest fatal" scratch\thor-debug\20260524-142827-speed-logcat.txt scratch\thor-debug\20260524-142827-speed-logcat-filtered.txt
rg -n "state|carrier|audit|speed profile|body_ticks|822|A64|Blue Dragon|profile" scratch\thor-debug\20260524-142827-speed-logcat.txt scratch\thor-debug\20260524-142827-speed-logcat-filtered.txt
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 210 -PerfSampleSeconds "60,120,180,210" -Arm64BlueDragonStateCarrierDesignAudit true -Arm64SpeedProfileBodyTimeFilter "82282490,82287788" -Arm64SpeedProfileBodyTimeAfterMs 120000
rg -n -i "AndroidRuntime|FATAL EXCEPTION|Fatal signal|tombstone|backtrace|SIGSEGV|assert|GPU is hung|device lost|ANR|crash|Xenia guest fatal" scratch\thor-debug\20260524-143436-speed-logcat.txt scratch\thor-debug\20260524-143436-speed-logcat-filtered.txt
rg -n "A64 speed profile enabled|A64 body-time profile|A64 body-time profile activated|A64 Blue Dragon state-carrier design audit|A64 speed profile body top|82282490|82287788|Main Thread|GPU Commands|XMA Decoder" scratch\thor-debug\20260524-143436-speed-logcat.txt scratch\thor-debug\20260524-143436-speed-logcat-filtered.txt scratch\thor-debug\20260524-143436-perf-180s.txt scratch\thor-debug\20260524-143436-perf-210s.txt scratch\thor-debug\20260524-143436-perf-final.txt
```

Files changed:

- `tools/thor/thor_xenia_debug.ps1`
- `docs/research/20260524-143436-state-carrier-audit-interval-fix.md`
- `docs/worklogs/20260524.md`
- `AGENTS.md`
- `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md`
- `.agents/skills/xenia-working-fast-autonomy/SKILL.md`
- `tools/thor/thor_codex_goal_loop.ps1`

Findings:

- First rerun `scratch/thor-debug/20260524-142827-*` reached the Microsoft Game
  Studios sky / dragon-wing frame with clean fatal-marker search and
  `log_level=1`, but still emitted no state-carrier/body rows. Source review
  showed both rows are emitted by `A64Backend::LogSpeedProfile`, so the real
  remaining blocker was missing `arm64_speed_profile_interval_ms`.
- `tools/thor/thor_xenia_debug.ps1` now defaults
  `Arm64SpeedProfileIntervalMs=15000` when row-producing speed-profiler
  instrumentation is requested and the caller did not pass an explicit interval.
  Quiet no-instrumentation captures are unchanged.
- Second rerun `scratch/thor-debug/20260524-143436-*` proved the fix without an
  explicit interval. Metadata recorded `arm64_speed_profile_interval_ms=15000`,
  `arm64_blue_dragon_state_carrier_design_audit=true`,
  `arm64_speed_profile_body_time_filter=82282490,82287788`, and `log_level=1`.
  It reached the same visible opening frame with clean fatal-marker search.
- Final state-carrier counters were `f1_read=3477646`,
  `f1_helper_read=1742466`, `f1_child_read=1735180`,
  `f1_child_call=576589`, `f1_fallback=0`, `fpscr_read=3640919`,
  `fpscr_dirty_write=3640919`, `fpscr_required_writeback=1182090`,
  `fpscr_call_kill=2924556`, and `fpscr_fallback=0`.
- Final body rows kept `82282490` dominant at `61133316`, followed by
  `82281D28=13198164` and `82287788=7477399`.
- Final thread sample still points away from a GPU wall: Main Thread `103%`,
  XMA Decoder `42.3%`, GPU Commands `7.6%`.

Decision:

- Treat this as successful audit/counter proof, not a quiet FPS verdict.
- Do not repeat the no-interval or log-level-only captures.
- Do not patch a standalone `f[1]` carrier or fpscr shortcut directly from this
  row. The `f[1]` counter volume is real and fallback-free, but the previous
  narrow carrier A/B missed speed; the next useful `f[1]` lane needs a broader
  parent/callee design with helper, child-call, barrier, exit, exception, and
  fallback rules.
- Keep fpscr in audit/design only for now: its dynamic volume is larger, but
  `fpscr_dirty_write=3640919`, `fpscr_required_writeback=1182090`, and
  `fpscr_call_kill=2924556` keep it a CFG-aware dirty-carrier problem.

## 14:47-14:58 EDT - State-Carrier Follow-up Planner

Starting HEAD: `9a34369a4`

Goal: turn the latest route-clean state-carrier counter row into a deterministic
offline next-step planner, instead of starting another stale stvewx, standalone
`f[1]`, pair-entry, `fpscr`, or three-PC `MUL_ADD_V128` experiment.

Commands run:

```powershell
git status --short --branch
Test-Path scratch\thor-debug\latest-blue-dragon-full-speed-proof.json
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_state_carrier_followup_plan.ps1 -CounterLogPath scratch\thor-debug\20260524-143436-speed-logcat.txt -StaticAuditPath docs\research\20260524-133027-8228252c-state-carrier-design-audit.md -PairEntryRoiPath docs\research\20260524-113615-82287788-pair-entry-roi.md -F1FastpathAbPath docs\research\20260524-110545-blue-dragon-f1-carrier-fastpath-ab.md -FpscrDirtyCacheAuditPath docs\research\20260524-114614-82287788-fpscr-dirty-cache-audit.md
powershell -NoProfile -ExecutionPolicy Bypass -Command "`$tokens=`$null; `$errors=`$null; [void][System.Management.Automation.Language.Parser]::ParseFile('tools\thor\thor_hir_state_carrier_followup_plan.ps1', [ref]`$tokens, [ref]`$errors); if (`$errors.Count) { `$errors | ForEach-Object { `$_.Message }; exit 1 } else { 'state_carrier_followup_plan parse-ok' }"
powershell -NoProfile -ExecutionPolicy Bypass -Command "`$tokens=`$null; `$errors=`$null; [void][System.Management.Automation.Language.Parser]::ParseFile('tools\thor\thor_codex_goal_loop.ps1', [ref]`$tokens, [ref]`$errors); if (`$errors.Count) { `$errors | ForEach-Object { `$_.Message }; exit 1 } else { 'goal_loop parse-ok' }"
git diff --check
python "C:\Users\leanerdesigner\.codex\skills\.system\skill-creator\scripts\quick_validate.py" .agents\skills\xenia-blue-dragon-full-speed-autopilot
python "C:\Users\leanerdesigner\.codex\skills\.system\skill-creator\scripts\quick_validate.py" .agents\skills\xenia-working-fast-autonomy
python "C:\Users\leanerdesigner\.codex\skills\.system\skill-creator\scripts\quick_validate.py" .agents\skills\xenia-a64-speed-hotpath
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_continual_harness_review.ps1 -Mode Window
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_codex_goal_loop.ps1 -Mode EnableBlueDragonFullSpeed -MaxAttempts 48
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_codex_goal_loop.ps1 -Mode Status
```

Files changed:

- `tools/thor/thor_hir_state_carrier_followup_plan.ps1`
- `docs/research/20260524-145241-state-carrier-followup-plan.md`
- `docs/worklogs/20260524.md`
- `AGENTS.md`
- `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md`
- `.agents/skills/xenia-working-fast-autonomy/SKILL.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `tools/thor/thor_codex_goal_loop.ps1`

Findings:

- New offline tool `tools/thor/thor_hir_state_carrier_followup_plan.ps1`
  parses the latest `A64 Blue Dragon state-carrier design audit` row and joins
  it with the prior static audits.
- It confirms `f[1]` has real volume (`3477646` reads, zero fallbacks), but the
  split is `50.10%` helper-preserved and `49.90%` child-spanning, and previous
  stack-slot / pair-entry evidence leaves it as a broader parent/callee design
  lane, not another narrow behavior patch.
- It confirms `fpscr` is still audit-only: `3640919` reads and dirty writes,
  `1182090` required writebacks, and `2924556` call kills keep it a
  CFG-visible dirty-carrier problem.
- PowerShell parse validation passed for the new planner and the re-armed goal
  loop script.
- Skill validation passed for the three touched repo-local skills.
- `git diff --check` passed with only existing LF-to-CRLF warnings.
- `tools/thor/thor_codex_goal_loop.ps1 -Mode Status` shows the
  `blue-dragon-full-speed` prompt now starts from this planner and the proof
  marker remains absent.

Decision:

- No Thor runtime capture was needed for this offline planner.
- Do not patch generated behavior yet.
- Next useful slice should design a broader `82282490 -> 82287788`
  parent/callee carrier with compile-time/runtime counters, or prove exact
  CFG writeback rules before any quiet speed A/B.

## 16:29-16:36 EDT - A64 State-Carrier ABI Contract Audit

Starting HEAD: `414f3edc0`

Goal: source-review the A64 direct-call ABI before another state-carrier speed
probe, because the current `f[1]` and `fpscr` evidence is high-volume but not
yet an implementable parent/callee payload.

Commands run:

```powershell
git status --short --branch
git log -1 --oneline
Test-Path scratch\thor-debug\latest-blue-dragon-full-speed-proof.json
Get-Process | Where-Object { $_.ProcessName -match 'adb|gradle|java|git|powershell|msbuild|ninja|cmake|python' } | Select-Object Id,ProcessName,CPU,StartTime,Path | Format-Table -AutoSize
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_a64_state_carrier_abi_audit.ps1
powershell -NoProfile -ExecutionPolicy Bypass -Command "`$tokens=`$null; `$errors=`$null; [void][System.Management.Automation.Language.Parser]::ParseFile('tools\thor\thor_a64_state_carrier_abi_audit.ps1', [ref]`$tokens, [ref]`$errors); if (`$errors.Count) { `$errors | ForEach-Object { `$_.Message }; exit 1 } else { 'a64_state_carrier_abi_audit parse-ok' }"
powershell -NoProfile -ExecutionPolicy Bypass -Command "`$tokens=`$null; `$errors=`$null; [void][System.Management.Automation.Language.Parser]::ParseFile('tools\thor\thor_codex_goal_loop.ps1', [ref]`$tokens, [ref]`$errors); if (`$errors.Count) { `$errors | ForEach-Object { `$_.Message }; exit 1 } else { 'goal_loop parse-ok' }"
git diff --check
python "C:\Users\leanerdesigner\.codex\skills\.system\skill-creator\scripts\quick_validate.py" .agents\skills\xenia-blue-dragon-full-speed-autopilot
python "C:\Users\leanerdesigner\.codex\skills\.system\skill-creator\scripts\quick_validate.py" .agents\skills\xenia-working-fast-autonomy
python "C:\Users\leanerdesigner\.codex\skills\.system\skill-creator\scripts\quick_validate.py" .agents\skills\xenia-a64-speed-hotpath
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_codex_goal_loop.ps1 -Mode EnableBlueDragonFullSpeed -MaxAttempts 48
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_codex_goal_loop.ps1 -Mode Status
```

Files changed:

- `tools/thor/thor_a64_state_carrier_abi_audit.ps1`
- `docs/research/20260524-163338-a64-state-carrier-abi-audit.md`
- `docs/worklogs/20260524.md`
- `AGENTS.md`
- `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md`
- `.agents/skills/xenia-working-fast-autonomy/SKILL.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `tools/thor/thor_codex_goal_loop.ps1`

Findings:

- No full-speed proof marker was present, and the worktree started clean.
- Only usual ADB, Gradle, MSBuild node-reuse, and short git-status processes
  were visible; no overlapping Thor capture/build/deploy was started.
- New tool `tools/thor/thor_a64_state_carrier_abi_audit.ps1` confirms the
  current direct-call ABI is `guest_return_only_in_x0`.
- The existing `arm64_blue_dragon_f1_carrier_fastpath` is a
  callee-local stack-slot carrier for `82287788`, not a parent/callee payload
  ABI.
- Existing counter coverage is enough for design audit, but the current ABI
  blocks a broader `82282490 -> 82287788` payload without either an explicit
  call-ABI extension or an edge-specific compiled entry/variant.
- PowerShell parse validation passed for the new ABI audit tool and the
  re-armed goal-loop script.
- Skill validation passed for the three touched repo-local skills.
- `git diff --check` passed with only existing LF-to-CRLF warnings.
- Continual harness window review regenerated
  `scratch/thor-debug/continual-harness-window.md` so stale heartbeat lanes can
  be compared against the latest ABI audit.
- `tools/thor/thor_codex_goal_loop.ps1 -Mode Status` now starts from the ABI
  audit and still reports no full-speed proof marker.

Decision:

- Do not make another quiet speed A/B from the current state-carrier evidence.
- `f[1]` remains the first plausible read-only payload candidate only after the
  ABI shape is explicit.
- Keep `fpscr` audit-only until exact CFG writeback and external flush points
  are proven.
- Next useful patch should add an edge-specific ABI/variant design audit with
  compile-time/runtime counters, or prove exact `fpscr` writeback rules before
  any generated behavior change.

## 16:46-16:51 EDT - A64 Edge Variant Design Audit

Starting HEAD: `9d24d9307`

Goal: turn the ABI audit's "edge-specific entry/variant required" conclusion
into an executable design gate before any generated-code behavior patch.

Commands run:

```powershell
git status --short --branch
Test-Path scratch\thor-debug\latest-blue-dragon-full-speed-proof.json
Get-Process | Where-Object { $_.ProcessName -match 'gradle|adb|java|git|xenia|msbuild|powershell|dotnet' } | Select-Object Id,ProcessName,CPU,StartTime
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_a64_edge_variant_design_audit.ps1
powershell -NoProfile -ExecutionPolicy Bypass -Command "& { `$tokens=`$null; `$errors=`$null; `$null=[System.Management.Automation.Language.Parser]::ParseFile('tools\thor\thor_a64_edge_variant_design_audit.ps1',[ref]`$tokens,[ref]`$errors); if (`$errors.Count -gt 0) { `$errors | ForEach-Object { `$_.Message }; exit 1 } else { 'parse-ok' } }"
powershell -NoProfile -ExecutionPolicy Bypass -Command "& { `$tokens=`$null; `$errors=`$null; `$null=[System.Management.Automation.Language.Parser]::ParseFile('tools\thor\thor_codex_goal_loop.ps1',[ref]`$tokens,[ref]`$errors); if (`$errors.Count -gt 0) { `$errors | ForEach-Object { `$_.Message }; exit 1 } else { 'goal_loop parse-ok' } }"
git diff --check
python "C:\Users\leanerdesigner\.codex\skills\.system\skill-creator\scripts\quick_validate.py" .agents\skills\xenia-blue-dragon-full-speed-autopilot
python "C:\Users\leanerdesigner\.codex\skills\.system\skill-creator\scripts\quick_validate.py" .agents\skills\xenia-working-fast-autonomy
python "C:\Users\leanerdesigner\.codex\skills\.system\skill-creator\scripts\quick_validate.py" .agents\skills\xenia-a64-speed-hotpath
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_continual_harness_review.ps1 -Mode Window
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_codex_goal_loop.ps1 -Mode EnableBlueDragonFullSpeed -MaxAttempts 48
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_codex_goal_loop.ps1 -Mode Status
```

Files changed:

- `tools/thor/thor_a64_edge_variant_design_audit.ps1`
- `docs/research/20260524-165127-a64-edge-variant-design-audit.md`
- `docs/worklogs/20260524.md`
- `AGENTS.md`
- `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md`
- `.agents/skills/xenia-working-fast-autonomy/SKILL.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `tools/thor/thor_codex_goal_loop.ps1`

Findings:

- No full-speed proof marker was present, and the worktree started clean.
- No overlapping Thor build, deploy, Blue Dragon capture, git operation, or
  active Codex slice was detected beyond long-lived ADB/MSBuild daemons and
  short probes from this slice.
- New tool `tools/thor/thor_a64_edge_variant_design_audit.ps1` confirms the
  normal entry is a singleton: host entry, direct calls, unresolved resolution,
  the guest-address indirection table, and `A64Function::machine_code()` all
  point to one normal entry per guest function.
- The hot `82282490:82282598 -> 82287788` state-carrier path therefore cannot
  safely use plain caller register preloading, replace `82287788`'s normal
  machine-code pointer, or change the global indirection slot.
- The safe next patch is counter-only: default-off compile/runtime counters for
  exact edge eligibility, payload materialization, normal-entry fallbacks,
  child-call kills, and variant misses.
- PowerShell parse validation passed for the new audit tool and the re-armed
  goal-loop script.
- Skill validation passed for the three touched repo-local skills.
- `git diff --check` passed with only existing LF-to-CRLF warnings.
- Continual harness window review regenerated
  `scratch/thor-debug/continual-harness-window.md`.
- `tools/thor/thor_codex_goal_loop.ps1 -Mode Status` now starts from this
  edge-variant audit and still reports no full-speed proof marker.

Decision:

- Do not patch generated behavior or run a quiet speed A/B from the current
  state-carrier evidence.
- Next useful behavior-adjacent slice is a default-off edge-variant counter
  probe for `82282490:82282598 -> 82287788`, or exact `fpscr` CFG writeback
  proof if the sprint pivots to dirty-state carrying.

## 16:57-17:02 EDT - Mature Vulkan Port Design Research

Starting HEAD: `9d24d9307`

Goal: answer the user's concern that other emulator Vulkan ports should inform
xenia-thor, and convert the external research into a concrete diagnostic design
lane without misreading the current Blue Dragon CPU/JIT evidence.

Sources checked:

- Dolphin Ubershaders:
  <https://dolphin-emu.org/blog/2017/07/30/ubershaders/>
- RPCS3 asynchronous shader implementation:
  <https://blog.rpcs3.net/2018/08/08/eliminating-stutter-with-asynchronous-shader-implementation/>
- PCSX2 performance counters:
  <https://pcsx2.net/docs/troubleshooting/performance/>
- PCSX2 2.0 Vulkan/blending overview:
  <https://pcsx2.net/blog/2024/pcsx2-2-release/>
- Khronos Vulkan pipeline-cache sample:
  <https://github.khronos.org/Vulkan-Site/samples/latest/samples/performance/pipeline_cache/README.html>
- Android Frame Pacing library:
  <https://developer.android.com/games/sdk/frame-pacing>
- Android Vulkan frame pacing integration:
  <https://developer.android.com/games/sdk/frame-pacing/vulkan/add-functions>
- Arm GPU Best Practices:
  <https://documentation-service.arm.com/static/67a62b17091bfc3e0a947695>

Files changed:

- `docs/research/20260524-165737-mature-vulkan-port-speed-design.md`
- `AGENTS.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`

Findings:

- Mature emulator Vulkan ports do not just "use Vulkan"; they add diagnostics
  for the real GPU costs: shader/pipeline stutter, barriers, render passes,
  readbacks, uploads/copies, descriptor churn, and present/frame pacing.
- PCSX2's OSD-style counter categories are the most directly portable to
  xenia-thor's Thor speed reports.
- Dolphin/RPCS3 shader-stutter work suggests a correctness fallback plus
  asynchronous shader/pipeline preparation, not a permanent skip-render hack.
- Khronos/Roblox pipeline-cache guidance suggests adding cold/warm pipeline
  cache metadata and robust cache validation before treating pipeline caching as
  solved.
- Android Frame Pacing / Swappy should be evaluated only after present timing
  shows queue-stuffing or present-bound behavior.

Decision:

- Keep the active Blue Dragon speed lane on A64 while captures show Main Thread
  is the wall.
- Start a parallel Vulkan diagnostic lane: add PCSX2-style GPU counters and
  pipeline-cache/readback/barrier/upload/present diagnostics before any broad
  Vulkan rewrite.
