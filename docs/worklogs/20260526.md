# 2026-05-26 Worklog

## A64 Register Allocation Audit Capture

- Started from the reset plan and confirmed no full-speed proof marker,
  clean `master` worktree, no active build/deploy/capture/git operation, and
  connected Thor `c3ca0370`.
- Ran FullDeploy:
  `powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`.
  The build/install passed with only the existing Android Gradle compileSdk
  warning.
- Ran a 180-second Blue Dragon route capture:
  `powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -Arm64RegisterAllocationAudit true -Arm64RegisterAllocationAuditFunction 0x82282490 -Arm64SpeedProfileBodyTimeFilter "82282490,82281D28,82287788" -Arm64SpeedProfileBodyTimeAfterMs 120000 -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 25 -Arm64SpeedProfileMinDelta 1 -StopAppAfterCapture true`.
- Capture prefix: `scratch/thor-debug/20260526-000910-*`.
  APK SHA256:
  `4B3B48CD3DB69B093CF3D47FA84BBC211DD09D23063F08DDD0064496FF6AC356`.
- Screenshot reached the visible opening sky / dragon-wing route. Fatal-marker
  scan found no Xenia, AndroidRuntime, tombstone, assert, or backtrace failure;
  the only signal marker was an unrelated Google Play services unstable
  start-timeout kill.
- Register allocation audit for `0x82282490`:
  `blocks=164`, `blocks_with_spills=0`, `instructions=9016`,
  `dest_values=4517`, `locals_before=0`, `locals_after=0`,
  `local_slots_added=0`, `behavior_changed=0`.
- Per set: INT `dest_values=2877`, `spill_requests=0`,
  `max_active_registers=6`; FLOAT `dest_values=350`, `spill_requests=0`,
  `max_active_registers=16`; VEC `dest_values=1290`,
  `spill_requests=0`, `max_active_registers=16`.
- Delayed final body-time rows:
  `82282490=29894735`, `82281D28=8077356`, `82287788=3611977`.
  Final perf remained CPU/JIT-heavy: Main Thread `92.3%`, GPU Commands
  `11.5%`, Draw Thread `3.8%`.
- Research note:
  `docs/research/20260526-001500-a64-register-allocation-audit-capture.md`.

Next: close the narrow allocator-spill-pressure lane for current `0x82282490`
evidence. Do not patch allocator behavior or run a quiet speed A/B from this
capture. Prefer helper ABI/block-linking audit for the wall cluster, or current
PERMUTE / `LOAD_VECTOR_SHL` / `LOAD_VECTOR_SHR` route counters before VMX128
behavior work.

## A64 Helper / Block-Link Offline Audit

- Added `tools/thor/thor_a64_helper_block_link_audit.ps1`, a deterministic
  source/log audit for the `82282490` / `82281D28` / `82287788` wall cluster.
- Ran parser validation:
  `$tokens=$null; $errors=$null; [System.Management.Automation.PSParser]::Tokenize((Get-Content -Raw tools\thor\thor_a64_helper_block_link_audit.ps1), [ref]$errors)`.
- Ran:
  `powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_a64_helper_block_link_audit.ps1`.
- Findings from `scratch/thor-debug/20260526-000910-*`: final interval had
  `entry_delta=3831037`, `direct_delta=3357831`,
  `indirect_delta=469505`, `extern_delta=389034`,
  `dispatch_delta_total=4216370`, `dispatch_delta_per_entry_delta=1.10`,
  `resolves_delta=5`, and `resolve_misses_total=0`.
- Source checks confirmed direct compiled calls, indirection fallback,
  runtime resolve fallback, extern guest-to-host thunk, stackpoint/post-call
  stack sync, call-edge profiling support, singleton `A64Function::machine_code_`,
  and guest-address-only code-cache indirection.
- Research note:
  `docs/research/20260526-004800-a64-helper-block-link-audit.md`.

Next: run a Thor route capture using existing call-edge profiling for
`82282490,82281D28,82287788` before any helper ABI or block-linking behavior
patch. Resolver-first work is closed for the warmed route because the latest
capture has zero resolve misses and only five resolves in the final interval.

## A64 Call-Edge Recursion Capture

- Ran the planned Thor route capture with call-edge profiling:
  `powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -Arm64SpeedProfileCallEdgeFilter "82282490,82281D28,82287788" -Arm64SpeedProfileBodyTimeFilter "82282490,82281D28,82287788" -Arm64SpeedProfileBodyTimeAfterMs 120000 -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 25 -Arm64SpeedProfileMinDelta 1 -StopAppAfterCapture true`.
- Capture prefix: `scratch/thor-debug/20260526-005803-*`.
  Commit `2131e7922`, APK SHA256
  `4B3B48CD3DB69B093CF3D47FA84BBC211DD09D23063F08DDD0064496FF6AC356`.
- Screenshot reached the visible opening sky / dragon-wing route. Fatal-marker
  search found no Xenia, AndroidRuntime, tombstone, assert, backtrace, fatal
  exception, SIGSEGV, SIGABRT, crash, or ANR lines; the normal Adreno Vulkan
  application-name line was the only `Xenia` hit.
- Final perf remained CPU/JIT-heavy: Main Thread `88.4%`, GPU Commands
  `11.5%`, XMA Decoder `7.6%`, Draw Thread `0.0%`.
- Final body-time rows:
  `82282490=32874047`, `82281D28=9434976`, `82287788=3264491`.
- `tools/thor/thor_hir_call_path_report.ps1` dynamic call-edge summaries:
  `82282490` dynamic edges were `94.24%` of body time, with
  self-recursive `822825E0 -> 82282490` at `69.62%`; `82281D28` dynamic edges
  were `88.95%`, with self-recursive `8228233C -> 82281D28` at `72.22%`;
  `82287788` was mostly parent-exclusive (`79.27%`).
- Research note:
  `docs/research/20260526-011000-a64-call-edge-recursion-capture.md`.

Next at that point: do not rerun unchanged call-edge captures or patch
resolver/direct recursive dispatch from this evidence. The next speed lane was
the VMX128 route-counter check recorded below; that follow-up now supersedes
this next-action note.

## VMX128 Route-Stabilized Counter Closure

- Ran the continual harness window before another Thor route run:
  `powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_continual_harness_review.ps1 -Mode Window`.
  It refreshed `scratch/thor-debug/continual-harness-window.md`; this scratch
  window is not committed.
- Ran a 180-second Blue Dragon route capture with function disassembly,
  block-body, and delayed body-time profiling:
  `powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -DisassembleFunctionFilter "82282490,82281D28,82287788" -Arm64SpeedProfileBlockFilter "82282490,82281D28,82287788" -Arm64SpeedProfileBlockBodyTime true -Arm64SpeedProfileBodyTimeFilter "82282490,82281D28,82287788" -Arm64SpeedProfileBodyTimeAfterMs 120000 -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 30 -Arm64SpeedProfileMinDelta 1 -StopAppAfterCapture true`.
- Capture prefix: `scratch/thor-debug/20260526-011255-*`.
  Commit `7aa4b90d4`, APK SHA256
  `4B3B48CD3DB69B093CF3D47FA84BBC211DD09D23063F08DDD0064496FF6AC356`.
- Screenshot reached visible opening sky / dragon-wing. Fatal-marker search
  found no AndroidRuntime, tombstone, assert, backtrace, fatal exception,
  SIGSEGV, SIGABRT, crash, or ANR lines.
- Final perf remained CPU/JIT-heavy: Main Thread `92.3%`, GPU Commands
  `11.5%`, Draw Thread `7.6%`. This was trace-heavy and is not a quiet speed
  proof.
- Final delayed function body-time rows:
  `82282490=98058145`, `82281D28=67536752`, `82287788=14590679`.
- Final hot block rows were still dominated by scalar/control or call-heavy
  blocks: `82282490:822825E0=70762934`,
  `82281D28:8228233C=48139529`, and
  `82282490:8228252C=16310678`.
- Ran route-stabilized vector shape reports. Because all-three-function HIR
  dumping overflowed logcat, `82282490` and `82281D28` used older full HIR
  dumps joined to the current block profile; `82287788` used current HIR.
- Weighted counters:
  `82282490` PERMUTE/SHL/SHR/PACK/UNPACK =
  `2524413/1682942/0/0/0`; `82281D28` =
  `1317189/878126/0/0/0`; `82287788` =
  `7353192/700304/2801216/0/0`.
- Interpretation: `PACK` remains absent, `UNPACK` remains zero weighted, and
  the active `82282490` / `82281D28` vector volume is concentrated in closed
  stvewx/vmaddfp local shapes. `82287788` has real `PERMUTE` /
  `LOAD_VECTOR_SHR` volume, but its vector block `82287D10` was only
  `531338` body ticks compared with scalar/control blocks.
- Ran codegen-floor checks for `822825E0`, `8228252C-822825C4`,
  `822877BC-82287868`, and `82287D10-82287D84`. `822825E0` is mostly
  `store_context`, barriers, compares, and a recursive call; `822877BC` is
  scalar/control/context-heavy with `context_barriers=9`.
- `82281D28:8228233C` still could not be mapped from the available OptHIR:
  `StartGuest 8228233C not found in OptHIR dump for 82281D28`.
- Research note:
  `docs/research/20260526-012000-vmx128-route-stabilized-counters.md`.

Next: close broad VMX128 `PERMUTE` / `LOAD_VECTOR_SHL` /
`LOAD_VECTOR_SHR` behavior for now. The next useful slice is a hot-block
codegen-floor/source audit with reliable one-function or file-backed HIR dumps,
especially to recover `82281D28:8228233C`, then target a reusable scalar,
context-barrier, helper ABI, or direct-call stackpoint rule only if the audit
supports it.

## 82281D28 HIR / Block-Profile Join Audit

- Started from clean `master` at `13c098afe`; no full-speed proof marker was
  present. Thor `c3ca0370` was connected. A separate Windows Eternal Sonata
  PowerShell process was running, but it was Windows-only PS3 work and not a
  Thor build/deploy/capture/git operation.
- Ran a one-function 180-second Blue Dragon route capture to avoid all-three
  function HIR logcat overflow:
  `powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -DisassembleFunctionFilter "82281D28" -Arm64SpeedProfileBlockFilter "82281D28" -Arm64SpeedProfileBlockBodyTime true -Arm64SpeedProfileBodyTimeFilter "82282490,82281D28,82287788" -Arm64SpeedProfileBodyTimeAfterMs 120000 -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 30 -Arm64SpeedProfileMinDelta 1 -StopAppAfterCapture true`.
- Capture prefix: `scratch/thor-debug/20260526-013226-*`.
  APK SHA256:
  `4B3B48CD3DB69B093CF3D47FA84BBC211DD09D23063F08DDD0064496FF6AC356`.
- Screenshot reached the visible opening sky / dragon-wing route. Fatal-marker
  search found no AndroidRuntime, fatal exception, tombstone, assert,
  backtrace, SIGSEGV, SIGABRT, crash, or ANR rows.
- Final perf remained CPU/JIT-heavy and trace-heavy: Main Thread `100%`,
  XMA Decoder `53.8%`, GPU Commands `7.6%`, Draw Thread `0.0%`.
- Final delayed body-time rows:
  `82281D28=66940160`, `82282490=28800287`, `82287788=3559908`.
  Final `82281D28` block-body top was
  `block=20 guest=8228233C body_ticks_total=47699966`.
- The one-function OptHIR dump recovered `82281D28` static HIR, but the exact
  runtime hot guest `8228233C` still did not appear as a printed HIR label or
  PPC comment. The first printed PPC comment was `822823B0`.
- Added `tools/thor/thor_hir_block_profile_join_audit.ps1` to detect unsafe
  joins between runtime block profile rows and printed HIR labels/comments.
  Parser validation passed.
- Ran:
  `powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_block_profile_join_audit.ps1 -LogPath scratch\thor-debug\20260526-013226-speed-logcat.txt -Function 82281D28 -Phase OptHIR -BlockProfileLog scratch\thor-debug\20260526-013226-speed-logcat.txt -ProfileKind Body -Top 20`.
  Result: `hir_labels=117`, `ppc_comments=2184`, `profile_rows=88`,
  `guest_hir_label_matches=0`, `guest_ppc_comment_matches=0`,
  `ordinal_label_matches=0`, `unmatched_profile_guests=88`,
  `ordinal_fallback_mismatches=88`, `join_status=unsafe`.
- Source review: block profile rows log `profile_block_address(i)` from
  `A64Function`; `A64Emitter::FindBlockGuestAddress` fills that with the first
  `SOURCE_OFFSET` before falling back to `GuestAddressFor()`. This can produce
  runtime profile guests that are not printed as HIR labels/comments.
- Research note:
  `docs/research/20260526-014000-82281d28-hir-block-profile-join.md`.

Next: do not patch behavior from current weighted HIR/block-profile joins for
`82281D28`. Build a deterministic block-profile-to-HIR mapper or metadata dump
that records runtime ordinal, `FindBlockGuestAddress`, first source offset,
first printable PPC comment, HIR label if any, and source span; then make HIR
analysis scripts refuse ordinal fallback unless the metadata proves it safe.

## A64 Block-Profile Metadata Mapper

- Continued from the reset plan with no full-speed proof marker, clean
  `master`, connected Thor `c3ca0370`, and no active Thor build/deploy/capture
  or real git operation.
- Implemented behavior-neutral A64 profile block metadata in
  `src/xenia/cpu/backend/a64/a64_function.*`,
  `src/xenia/cpu/backend/a64/a64_emitter.*`, and
  `src/xenia/cpu/backend/a64/a64_backend.cc`.
- New speed-profile block and block-body rows now append:
  `first_source`, `last_source`, `first_guest`, `last_guest`,
  `first_comment`, `last_comment`, `label`, and `hir_instrs`.
- Updated HIR/profile analysis tools:
  `tools/thor/thor_hir_block_profile_join_audit.ps1`,
  `tools/thor/thor_hir_vector_shape_report.ps1`,
  `tools/thor/thor_hir_block_mix_report.ps1`, and
  `tools/thor/thor_hir_call_path_report.ps1`.
- The vector, block-mix, and call-path reports now refuse ordinal fallback by
  default and report match/fallback/unmatched counts. `-AllowOrdinalFallback`
  remains only for explicit legacy comparison.
- Parser validation passed for all four PowerShell tools.
- Old-log checks on `scratch/thor-debug/20260526-013226-speed-logcat.txt`
  correctly kept `join_status=unsafe` and prevented fake weighted rows:
  vector shape reported `profile_unmatched=154`; block-mix and call-path
  reported `profile_unmatched=242`.
- Native build passed:
  `powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore`.
- Research note:
  `docs/research/20260526-015900-a64-block-profile-metadata-mapper.md`.

Next: FullDeploy and run a route-clean one-function `82281D28` capture with
block body-time/disassembly enabled. Then run
`tools/thor/thor_hir_block_profile_join_audit.ps1` on the new log and require
metadata-backed mapping before any `82281D28` behavior patch. This patch is not
speed proof and should not be followed by a quiet speed A/B.

## 82281D28 Metadata Capture Blocker

- Started from clean `master` at `d39f3460e`; no full-speed proof marker was
  present, Thor `c3ca0370` was connected, and no active Thor build/deploy/
  capture or real git operation was running.
- Ran FullDeploy:
  `powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`.
  Build/install passed with the existing Android Gradle compileSdk warning.
- Ran the planned one-function capture:
  `powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -DisassembleFunctionFilter "82281D28" -Arm64SpeedProfileBlockFilter "82281D28" -Arm64SpeedProfileBlockBodyTime true -Arm64SpeedProfileBodyTimeFilter "82282490,82281D28,82287788" -Arm64SpeedProfileBodyTimeAfterMs 120000 -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 30 -Arm64SpeedProfileMinDelta 1 -StopAppAfterCapture true`.
- Capture prefix: `scratch/thor-debug/20260526-021159-*`.
  APK SHA256:
  `963CE83618F947C979BFAB3558CC2ED44A86055C6F8AC5077F5F0E1A7DDF5504`.
- Screenshot reached the visible opening sky / dragon-wing route. Focused
  fatal-marker search found no Xenia/AndroidRuntime/tombstone/assert/
  backtrace/fatal exception/SIGSEGV/SIGABRT/crash/ANR rows.
- Final perf remained CPU/JIT-heavy and trace-heavy: Main Thread `92.5%`,
  XMA Decoder `48.1%`, GPU Commands `11.1%`, Draw Thread `3.7%`.
- Final delayed body-time rows:
  `82281D28=66528341`, `82282490=36273569`, `82287788=3535771`.
- Hot block metadata now exists for the wall block:
  `82281D28:block20 guest=8228233C body_ticks_total=47409053`,
  `first_source=8228233C`, `last_source=82282370`,
  `first_comment=8228233C`, `last_comment=82282370`,
  `label=8228233C`, `hir_instrs=34`.
- Fixed `tools/thor/thor_hir_block_profile_join_audit.ps1` after finding that
  the metadata regex overwrote PowerShell `$Matches` and zeroed parsed totals.
  Parser validation passed.
- Corrected join audit on the new log reports:
  `metadata_rows=88`, `metadata_mappable_rows=0`,
  `active_metadata_unmappable_rows=88`,
  `top_active_metadata_unmappable=block=20,guest=8228233C,total=47409053`,
  and `join_status=unsafe`.
- Research note:
  `docs/research/20260526-022000-82281d28-metadata-capture-blocker.md`.

Next: do not patch `82281D28` behavior from weighted HIR/profile joins. Emit
file-backed or log-backed per-block HIR text/source spans for active unmappable
rows, starting with `82281D28` block `20` (`8228233C-82282370`), then rerun the
join audit.

## HIR Block Profile Stamps

- Continued from clean `master` at `bc1189517`; no full-speed proof marker was
  present, Thor `c3ca0370` was connected, and no active Thor build/deploy/
  capture or real git operation was running.
- Implemented behavior-neutral per-block HIR dump stamps in
  `src/xenia/cpu/hir/hir_builder.cc`. Each `HIRBuilder::Dump()` block now emits
  a `block_profile` comment with real `block->ordinal`, source span, guest span,
  first printable PPC comment span, label address, and visible HIR instruction
  count.
- Updated `tools/thor/thor_hir_block_profile_join_audit.ps1` to parse the new
  stamps as `hir_block_*` fields and to mark profile rows mappable when the
  runtime guest address falls inside the stamped source or guest span. Ordinal
  fallback remains disallowed for behavior decisions.
- Validation:
  - `git diff --check` passed.
  - PowerShell parser validation for
    `tools/thor/thor_hir_block_profile_join_audit.ps1` passed.
  - Old-log audit on `scratch/thor-debug/20260526-021159-speed-logcat.txt`
    still reports `join_status=unsafe` with `hir_block_metadata_rows=0`, so the
    tool does not invent mappings for stale captures.
  - `powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore`
    passed.
- Re-armed the repo goal loop with
  `powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_codex_goal_loop.ps1 -Mode EnableBlueDragonFullSpeed -MaxAttempts 48`.
- Updated heartbeat automation `blue-dragon-full-speed-sprint` to point at the
  new HIR block-stamp capture/deploy step.
- Research note:
  `docs/research/20260526-023500-hir-block-profile-stamps.md`.

Next: FullDeploy, run a one-function `82281D28` Blue Dragon route capture with
disassembly and block body-time enabled, then rerun
`tools/thor/thor_hir_block_profile_join_audit.ps1`. Use weighted `82281D28`
HIR evidence only if the new log has `hir_block_mappable_rows > 0` and
`join_status` no longer reports `unsafe`.

## 82281D28 Block Map Capture

- Continued from clean `master` at `be55b0009`; no full-speed proof marker was
  present, Thor `c3ca0370` was connected, and no active build/deploy/capture or
  real git operation was running.
- Ran FullDeploy:
  `powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`.
  Build/install passed with the existing Android Gradle compileSdk warning.
- Ran the planned one-function `82281D28` capture with disassembly, block
  body-time, and delayed body-time comparators. First capture prefix:
  `scratch/thor-debug/20260526-024838-*`. It reached visible opening
  sky/dragon-wing and had a clean focused fatal-marker search, but the join
  audit stayed unsafe because Thor logcat was still 256 KiB and retained only
  the tail of the one-function OptHIR dump:
  `hir_block_metadata_rows=0`, `hir_block_mappable_rows=0`,
  `active_metadata_unmappable_rows=88`, `join_status=unsafe`.
- Increased Thor logcat buffers to 64 MiB with
  `adb -s c3ca0370 logcat -G 64M`, then reran the same capture.
- Corrected capture prefix: `scratch/thor-debug/20260526-025401-*`.
  Capture commit `be55b0009`; APK SHA256
  `32AB811EF1A77BF57EE93F0E079A5B73FD2FAC15CFD3239A47EA91CA2D315AEC`.
  Screenshot again reached visible opening sky/dragon-wing and the focused
  fatal-marker search found no Xenia/AndroidRuntime/tombstone/assert/
  backtrace/fatal exception/SIGSEGV/SIGABRT/crash/ANR rows.
- Final perf stayed CPU/JIT-heavy: Main Thread `103%`, XMA Decoder `7.6%`,
  GPU Commands `3.8%`, Draw Thread `0.0%`. Final delayed body-time rows were
  `82281D28=62542369`, `82282490=28704115`, and `82287788=3280726`.
- Join audit on `scratch/thor-debug/20260526-025401-speed-logcat.txt` now
  reports safe metadata-backed mapping: `metadata_rows=88`,
  `metadata_mappable_rows=88`, `hir_block_metadata_rows=88`,
  `hir_block_mappable_rows=88`, `active_metadata_unmappable_rows=0`, and
  `join_status=metadata_required`.
- Top hot row is now mapped:
  `82281D28:block20 guest=8228233C total=44572243 delta=16609748 entries=23493 tpe=707`,
  source/guest span `8228233C-82282370`, label `8228233C`, `hir_instrs=34`.
- Hot block detail shows scalar call setup, not vector/GPU work:
  `context_loads=5`, `context_stores=13`, `memory_loads=6`,
  `memory_stores=1`, `calls=2`, `context_barriers=2`, `ppc_ops=ld:6,addi:3,bl:2,or:2,stw:1`,
  and call targets `bl->0x826BF770` plus recursive `bl->0x82281D28`.
- Research note:
  `docs/research/20260526-030200-82281d28-block-map-capture.md`.

Next: do not rerun the unchanged HIR mapper capture. Use the mapped
`82281D28:8228233C-82282370` block for an offline/source call-setup audit:
classify the `r3-r10`/`lr` context stores around `0x826BF770` and recursive
`0x82281D28`, compare against existing stack-sync and memcpy fastpaths, and
only patch behavior if the audit proves a reusable helper ABI/direct-call or
stack-argument rule.

## 82281D28 Call Setup Audit

- Continued from clean `master` at `928bc09a6`; no full-speed proof marker was
  present, Thor was connected, the emulator was not running, and no active
  build/deploy/capture or real git operation was blocking the slice.
- Stayed offline as requested by the reset plan; no new Thor capture was run.
- Re-read the mapped block with
  `tools/thor/thor_hir_block_detail_report.ps1` and confirmed
  `82281D28:8228233C-82282370` is scalar call setup:
  `context_loads=5`, `context_stores=13`, `memory_loads=6`,
  `memory_stores=1`, `calls=2`, `context_barriers=2`, with call targets
  `0x826BF770` and recursive `0x82281D28`.
- Ran call-boundary audits:
  - `8228233C-8228234C -> 826BF770` with the legacy `826BF770` OptHIR log:
    `parent_pre_call_store_context=4`, `classification_counts=callee_live_in:4`.
    Stores to `r3`, `r4`, `r5`, and `lr` are all live under the current ABI.
  - `82282350-82282370 -> 82281D28` on the current mapped log:
    `parent_pre_call_store_context=9`, `classification_counts=callee_live_in:9`.
    Stores to `r3-r10` and `lr` are all live into the recursive callee.
- Ran `thor_hir_callee_local_promotion_audit.ps1` for `r3-r10/lr`; every target
  had `strict_redundant_loads=0`, `strict_load_after_store=0`, and `risk=high`.
- Source audit of `A64Emitter::Call`, `A64Function::CallImpl`, and
  `TryEmitBlueDragonMemcpyFunctionBody` shows direct guest calls pass only the
  return address in `x0`; guest arguments are still transferred through
  `PPCContext`. The memcpy body fastpath still reads `r3/r4/r5` from
  `PPCContext`, so it does not remove caller-side setup stores.
- Research note:
  `docs/research/20260526-032000-82281d28-call-setup-audit.md`.

Decision: close local `82281D28:8228233C-82282370` store elision and another
unchanged call/setup capture. The reusable path is a generic, default-off
guest-call argument handoff audit that counts direct guest calls where parent
stores `r3-r10/lr` and callee immediately reloads them, including normal-entry
fallback, exception/debug visibility, indirection, tail-call, and stackpoint
constraints.

## Guest-Call Argument Handoff Audit Tool

- Continued from clean `master` at `ac823dab8`; no full-speed proof marker was
  present, Thor `c3ca0370` was connected, the Xenia app was not running, and no
  active Thor build/deploy/capture or real git operation was blocking the
  slice. An unrelated PS3/Eternal Sonata Windows test process was running
  outside this Thor/Xenia lane and was not treated as a blocker.
- Added `tools/thor/thor_hir_guest_call_arg_handoff_audit.ps1`, a
  behavior-neutral log-backed audit for direct PPC guest calls. It parses HIR
  dumps and block body-time rows, finds parent `r3-r10/lr` stores before `bl`
  calls, classifies callee first-use reloads when callee HIR is available, and
  reports missing-callee coverage plus normal-entry fallback pressure.
- Validation:
  - PowerShell parser validation for
    `tools/thor/thor_hir_guest_call_arg_handoff_audit.ps1` passed.
  - Ran the tool on `scratch/thor-debug/20260526-025401-speed-logcat.txt` with
    extra callee HIR from `scratch/thor-debug/20260519-213553-live-logcat.txt`.
- Audit result for `82281D28`:
  `direct_ppc_bl_calls=81`, `calls_with_argument_stores=81`,
  `argument_store_fields=439`, `argument_store_bytes=3512`,
  `callee_first_load_stores=62`, `callee_first_load_bytes=496`,
  `callee_hir_missing_stores=364`, and
  `normal_entry_fallback_required=81`.
- The two known body-weighted hot calls stay ABI-live:
  `82282370 -> 0x82281D28` has nine setup stores and all are
  `callee_first_load`; `8228234C -> 0x826BF770` has four setup stores and all
  are `callee_first_load`.
- Decision: the generic audit surface exists, but current HIR coverage is not
  enough for a guest-call fast-entry behavior patch. Most candidate stores are
  blocked by missing callee HIR. Next loop should collect broader route HIR or
  file-backed HIR for missing direct-call-heavy callees, rerun this audit, and
  only design a fast-entry variant if body-weighted callee-first-load volume is
  broad and normal-entry fallback constraints are explicit.
- Research note:
  `docs/research/20260526-033000-guest-call-arg-handoff-audit.md`.

## Guest-Call HIR Coverage Capture

- Continued from clean `master` at `682043bb0`; no full-speed proof marker was
  present, Thor `c3ca0370` was connected, Xenia was not running, and no active
  Thor build/deploy/capture or real git operation was blocking the slice.
  Read-only git status probes were ignored. The unrelated PS3/Eternal Sonata
  Windows process stayed outside this Thor/Xenia lane.
- Checked old logs for the missing direct-call-heavy callees from the first
  guest-call handoff audit. Existing logs only covered `82287788` and
  `8227FEE8`, reducing missing stores from `364` to `326`, so a targeted Thor
  route capture was justified.
- Raised Thor logcat buffers to 64 MiB and ran a 180-second Blue Dragon route
  capture with targeted disassembly filter:
  `82281D28,826BF770,82168610,822750F8,82274DB0,82275C08,82277100,82286BC0,82287788,82198088,822753E0,8227FEE8,8228AC30,8228AEF0`.
- Capture prefix: `scratch/thor-debug/20260526-034119-*`.
  Commit `682043bb0`; APK SHA256
  `32AB811EF1A77BF57EE93F0E079A5B73FD2FAC15CFD3239A47EA91CA2D315AEC`.
  Screenshot reached the visible opening sky / dragon-wing frame, and focused
  fatal-marker search found no Xenia/AndroidRuntime/tombstone/assert/backtrace/
  SIGSEGV/SIGABRT/crash/ANR rows.
- Final perf remained CPU/JIT-heavy: Main Thread `92.3%`, XMA Decoder
  `42.3%`, GPU Commands `7.6%`. Final delayed body-time rows were
  `82282490=33831839`, `82281D28=7930867`, and `82287788=3525919`.
- Fixed `tools/thor/thor_hir_guest_call_arg_handoff_audit.ps1` so the primary
  log supplies caller functions and block body-time while `-ExtraLogPath`
  supplies callee HIR only. Parser validation passed.
- Reran the audit with `scratch/thor-debug/20260526-025401-speed-logcat.txt`
  as body-time primary and `scratch/thor-debug/20260526-034119-speed-logcat.txt`
  as callee-HIR extra. Corrected result:
  `direct_ppc_bl_calls=81`, `argument_store_fields=439`,
  `callee_first_load_stores=247`, `callee_hir_missing_stores=126`,
  `callee_first_store=51`, `callee_absent=15`, and
  `normal_entry_fallback_required=81`.
- Decision: the guest-call argument lane has material ABI round-trip traffic,
  especially in the body-weighted top call boundaries, but it is not a local
  store-elision patch. Next useful slice is a source-level fast-entry
  feasibility audit for A64 direct guest calls and normal-entry fallback before
  any runtime behavior skeleton.
- Research note:
  `docs/research/20260526-034500-guest-call-hir-coverage-capture.md`.

## A64 Guest-Call Fast-Entry Feasibility Audit

- Continued from clean `master` with no full-speed proof marker, Thor
  `c3ca0370` connected, Xenia not running, and no active Thor build/deploy/
  capture or real git operation. Read-only git status probes and the unrelated
  PS3/Eternal Sonata Windows process were not blockers.
- Added `tools/thor/thor_a64_guest_call_fast_entry_feasibility.ps1`, a
  deterministic source audit for the direct guest-call fast-entry lane.
- Validation:
  - PowerShell parser validation passed for the new tool.
  - Ran
    `powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_a64_guest_call_fast_entry_feasibility.ps1`.
- Source findings:
  - normal guest entry is a singleton `A64Function::machine_code()` plus one
    guest-address indirection slot;
  - host-to-guest and direct guest-to-guest calls pass only the guest PPC
    return address in `x0`;
  - tail, indirect, unresolved, and extern calls require normal fallback;
  - stackpoint/longjmp recovery, debug/trap visibility, exception mapping, and
    guest-to-host callback save/restore make hidden payload state unsafe unless
    explicit flush points exist;
  - no generic fast-entry skeleton exists yet.
- Decision: direct guest-call fast-entry is feasible only as a separate guarded
  entry path or stub. Do not replace `A64Function::machine_code()` or the
  global indirection slot, and do not patch runtime behavior yet.
- Research note:
  `docs/research/20260526-040500-a64-guest-call-fast-entry-feasibility.md`.

Next: add a default-off counter-only
`arm64_guest_call_fast_entry_audit` skeleton that changes no generated
behavior and counts eligible direct calls, fallback blockers, callee first-use
coverage, normal-entry fallback pressure, estimated avoidable context
store/load traffic, and required dirty flush points. No quiet speed A/B until
that audit proves route-wide volume with manageable flush pressure.

## A64 Guest-Call Fast-Entry Audit Skeleton

- Continued from clean `master` at `6272e2b9f`; no full-speed proof marker was
  present, Thor `c3ca0370` was connected, and no active Thor build/deploy/
  capture or real git operation was blocking the slice. Read-only git status
  probes were ignored.
- Added default-off native cvars:
  `arm64_guest_call_fast_entry_audit`,
  `arm64_guest_call_fast_entry_audit_function`, and
  `arm64_guest_call_fast_entry_audit_budget`.
- Added `A64Emitter::MaybeLogGuestCallFastEntryAudit`, a compile-time HIR
  summary row for direct guest-call fast-entry eligibility. It counts direct
  calls, conditional calls, eligible regular calls, tail/indirect/extern/
  unresolved/helper blockers, normal-entry fallback pressure, stackpoint
  sensitivity, parent `r3-r10/lr` store traffic, and dirty flush points.
- The audit is behavior-neutral: `payload_materializations_allowed=0`,
  `behavior_changed=0`, `alternate_codegen=0`, `normal_entry=unchanged`, and
  `global_indirection=unchanged`.
- Added Android `EmulatorActivity` cvar pass-through and Thor launcher
  parameters:
  `-Arm64GuestCallFastEntryAudit`,
  `-Arm64GuestCallFastEntryAuditFunction`, and
  `-Arm64GuestCallFastEntryAuditBudget`.
- Validation:
  - `git diff --check` passed with existing CRLF warnings only.
  - PowerShell parser validation passed for `tools/thor/thor_xenia_debug.ps1`.
  - PowerShell parser validation passed for
    `tools/thor/thor_codex_goal_loop.ps1`.
  - `powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore`
    passed.
  - `powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode ApkShell`
    passed with the existing Android Gradle compileSdk warning.
- Re-armed the repo goal loop with
  `powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_codex_goal_loop.ps1 -Mode EnableBlueDragonFullSpeed -MaxAttempts 48`.
- Research note:
  `docs/research/20260526-041500-a64-guest-call-fast-entry-audit-skeleton.md`.

Next: commit and push. The next runtime slice should FullDeploy and run a
route-clean capture with `-Arm64GuestCallFastEntryAudit true
-Arm64GuestCallFastEntryAuditFunction 0x82281D28
-Arm64GuestCallFastEntryAuditBudget 16`; no quiet speed A/B from this
counter-only patch.

## A64 Guest-Call Fast-Entry Audit Capture

- Continued from clean `master` at `45d43abe8`; no full-speed proof marker was
  present, Thor `c3ca0370` was connected, and no active build/deploy/capture or
  real git operation was blocking the slice.
- Read the reset skills, worklog, and goal-loop state, then raised Thor logcat
  buffers to 64 MiB with `adb -s c3ca0370 logcat -G 64M`.
- Ran FullDeploy:
  `powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`.
  Build/install passed with the existing Android Gradle compileSdk warning.
- Ran a 180-second Blue Dragon route capture with:
  `-Arm64GuestCallFastEntryAudit true`,
  `-Arm64GuestCallFastEntryAuditFunction 0x82281D28`,
  `-Arm64GuestCallFastEntryAuditBudget 16`, and delayed body-time comparators
  `82282490,82281D28,82287788`.
- Capture prefix: `scratch/thor-debug/20260526-043446-*`.
  APK SHA256:
  `86B1C890FB6C63775F5D20B99001F36E62BD088F308AD9C21D9F963F37B68F83`.
  Screenshot reached visible opening sky / dragon-wing, and the focused
  fatal-marker search found no AndroidRuntime/tombstone/assert/backtrace/
  fatal exception/SIGSEGV/SIGABRT/crash/ANR rows.
- Audit row for `82281D28`:
  `direct_calls=84`, `eligible_regular=67`, `tail_blockers=8`,
  `unresolved_direct_targets=52`, `helper_blockers=17`,
  `normal_entry_fallback=67`, `stackpoint_sensitive=67`,
  `arg_store_calls=78`, `arg_store_fields=421`,
  `arg_store_bytes=3368`, `callee_first_use_known=0`,
  `callee_first_use_missing=421`, `dirty_flush_points=268`,
  `flush_context_barrier=260`, `already_compiled_targets=32`,
  `payload_materializations_allowed=0`, `behavior_changed=0`,
  `alternate_codegen=0`, `normal_entry=unchanged`, and
  `global_indirection=unchanged`.
- Final perf remained CPU/JIT-heavy: Main Thread `103%`, XMA Decoder `50.0%`,
  GPU Commands `3.8%`, Draw Thread `0.0%`.
- Final delayed body-time rows:
  `82282490=31015975`, `82281D28=7992965`, `82287788=3534990`.
- Decision: do not implement fast-entry behavior and do not rerun the same
  `0x82281D28` audit unchanged. The current row proves caller argument traffic
  exists, but callee safety is still unknown (`callee_first_use_known=0`) and
  dirty flush pressure is high. Next useful slice is a callee-aware audit
  extension or planner; if that cannot produce body-weighted known first-use
  volume with manageable flush pressure, close the lane.
- Research note:
  `docs/research/20260526-043900-a64-guest-call-fast-entry-audit-capture.md`.

## Guest-Call Callee-Aware Audit

- Continued from `849b4d7e4` with no full-speed proof marker and no active
  build/deploy/capture or real git operation blocking the slice.
- Ran the continual-harness window review:
  `powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_continual_harness_review.ps1 -Mode Window`.
  Output: `scratch/thor-debug/continual-harness-window.md`.
- Updated `tools/thor/thor_hir_guest_call_arg_handoff_audit.ps1` with
  `-FastEntryAuditLogPath`, compile-audit row ingestion, and per-target
  body-weighted summaries for live-in, first-store, absent, and missing callee
  first-use classifications.
- Validation: PowerShell parser validation passed for
  `tools/thor/thor_hir_guest_call_arg_handoff_audit.ps1`.
- Ran the callee-aware offline audit:
  `powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_guest_call_arg_handoff_audit.ps1 -LogPath scratch\thor-debug\20260526-025401-speed-logcat.txt -ExtraLogPath scratch\thor-debug\20260526-034119-speed-logcat.txt -FastEntryAuditLogPath scratch\thor-debug\20260526-043446-speed-logcat.txt -Function 82281D28 -Phase OptHIR -Top 16`.
  Output: `scratch/thor-debug/20260526-044800-guest-call-callee-aware-audit.txt`.
- The offline join reports material body-weighted known live-in traffic:
  `argument_store_fields=439`, `callee_first_load_stores=247`,
  `callee_first_store_stores=51`, `callee_absent_stores=15`,
  `callee_hir_missing_stores=126`,
  `body_weighted_live_in_fields=690421033`, and
  `body_weighted_missing_fields=30011199`.
- Top known live-in targets are recursive `0x82281D28`
  (`body_weighted_live_in=401150187`) and `0x826BF770`
  (`body_weighted_live_in=206739640`). Top missing weighted target is
  `0x82276E70` (`body_weighted_missing=20915802`).
- The joined compile-time fast-entry row still blocks behavior:
  `callee_first_use_known=0`, `callee_first_use_missing=421`,
  `normal_entry_fallback=67`, `stackpoint_sensitive=67`,
  `dirty_flush_points=268`, and `flush_context_barrier=260`.
- Decision: do not implement guest-call fast-entry behavior and do not run a
  quiet speed A/B. If this lane continues, the next slice must add default-off
  runtime per-target fast-entry rows for direct guest calls and close the lane if
  those rows do not show broad known first-use traffic with manageable flush
  pressure.
- Research note:
  `docs/research/20260526-045000-guest-call-callee-aware-audit.md`.

## A64 Guest-Call Fast-Entry Target Rows

- Continued from clean `master` at `0f5b10158`; no full-speed proof marker was
  present and no active build/deploy/capture or real git operation blocked the
  slice.
- Added bounded runtime target rows to the existing default-off
  `arm64_guest_call_fast_entry_audit` path in
  `src/xenia/cpu/backend/a64/a64_emitter.cc`.
- New rows have prefix `A64 guest-call fast-entry target` and include direct
  target address, call counts, conditional/tail counts, eligible regular calls,
  already-compiled/unresolved state, helper blockers, normal-entry fallback,
  stackpoint sensitivity, parent `r3-r10/lr` store fields/bytes,
  pre-call flush pressure, first call/block guest PCs, and `arg_store_top`.
- The existing function-level row now includes `target_rows` and
  `parent_pre_call_flush_points`.
- Behavior remains unchanged:
  `payload_materializations_allowed=0`, `behavior_changed=0`,
  `alternate_codegen=0`, `normal_entry=unchanged`, and
  `global_indirection=unchanged`.
- Updated `tools/thor/thor_hir_guest_call_arg_handoff_audit.ps1` to parse future
  target rows from `-FastEntryAuditLogPath` and print a
  `Fast-Entry Runtime Target Rows` section.
- Validation:
  - PowerShell parser validation passed for
    `tools/thor/thor_hir_guest_call_arg_handoff_audit.ps1`.
  - `git diff --check` passed with existing CRLF warnings only.
  - `powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore`
    passed.
  - Parser smoke on the existing fast-entry logs passed; as expected, old logs
    contain no runtime target rows from this new patch.
- Decision: do not implement guest-call fast-entry behavior and do not run a
  quiet speed A/B from this patch. Next useful slice is FullDeploy plus a
  route-clean `0x82281D28` fast-entry target-row capture, then join those rows
  with the offline callee-aware HIR audit. Close the fast-entry behavior lane if
  target rows do not show broad known first-use traffic with manageable flush
  pressure.
- Research note:
  `docs/research/20260526-051000-a64-guest-call-fast-entry-target-rows.md`.

## A64 Guest-Call Fast-Entry Target-Row Capture

- Continued from clean `master` at `bc00a8eaa`; no full-speed proof marker was
  present and no active build/deploy/capture or real git operation blocked the
  slice.
- Thor `c3ca0370` was connected as `AYN Thor`, logcat was raised to 64 MiB, and
  FullDeploy passed with the existing Android Gradle compileSdk warning.
- Ran a 180-second Blue Dragon route capture with
  `-Arm64GuestCallFastEntryAudit true`,
  `-Arm64GuestCallFastEntryAuditFunction 0x82281D28`,
  `-Arm64GuestCallFastEntryAuditBudget 16`, and delayed body-time comparators
  `82282490,82281D28,82287788`.
- Capture prefix: `scratch/thor-debug/20260526-052805-*`.
  APK SHA256:
  `555838EB4F235D1FF1AA1B63B1D9E1A7F115B74DB541DD6479F52C6C5FFBBBA2`.
  Screenshot reached visible opening sky / dragon-wing, and focused
  fatal-marker search found no AndroidRuntime/tombstone/assert/backtrace/fatal
  exception/SIGSEGV/SIGABRT/crash/ANR rows.
- Runtime audit row for `82281D28`:
  `target_rows=32`, `direct_calls=84`, `eligible_regular=67`,
  `unresolved_direct_targets=52`, `helper_blockers=17`,
  `normal_entry_fallback=67`, `stackpoint_sensitive=67`,
  `arg_store_fields=421`, `dirty_flush_points=268`,
  `flush_context_barrier=260`, `parent_pre_call_flush_points=195`,
  `already_compiled_targets=32`, `payload_materializations_allowed=0`,
  `behavior_changed=0`, `alternate_codegen=0`, `normal_entry=unchanged`, and
  `global_indirection=unchanged`.
- Joined the target rows with the callee-aware HIR audit:
  `scratch/thor-debug/20260526-052805-guest-call-fast-entry-target-row-join.txt`.
  It reports real broad known live-in volume:
  `callee_first_load_stores=247`,
  `body_weighted_live_in_fields=690421033`, and
  `body_weighted_missing_fields=30011199`.
- Top body-weighted known targets are recursive `0x82281D28`
  (`body_weighted_live_in=401150187`) and `0x826BF770`
  (`body_weighted_live_in=206739640`), but runtime flush and unresolved-target
  pressure remain too high for a behavior patch.
- Final perf stayed CPU/JIT-heavy: Main Thread `96.1%`, XMA Decoder `7.6%`,
  GPU Commands `7.6%`, Draw Thread `3.8%`. No sustained 30 FPS proof exists.
- Decision: do not implement guest-call fast-entry behavior, do not run a quiet
  speed A/B, and do not rerun the same target-row capture unchanged. Close the
  current target-row capture lane. Reopen fast-entry only with a source-level
  guarded-stub / late-bound-entry design that explicitly handles normal-entry
  fallback, global indirection, stackpoint/debug/exception visibility, and dirty
  flush points for the top body-weighted targets. Otherwise move to a hot-block
  A64 codegen-floor/disassembly audit.
- Research note:
  `docs/research/20260526-053600-a64-guest-call-fast-entry-target-row-capture.md`.

## 82281D28 Hot-Block Codegen Floor

- Continued from clean `master` at `38f7d319a`; no full-speed proof marker was
  present and no active build/deploy/capture or real git operation blocked the
  slice.
- Ran the deterministic mapped-block HIR/source codegen-floor audit for
  `82281D28:8228233C-82282370` using
  `tools/thor/thor_hir_a64_codegen_audit.ps1` and
  `tools/thor/thor_hir_block_detail_report.ps1` on
  `scratch/thor-debug/20260526-025401-speed-logcat.txt`.
- Reports:
  - `scratch/thor-debug/20260526-054200-82281d28-hot-block-codegen-audit.txt`
  - `scratch/thor-debug/20260526-054200-82281d28-hot-block-detail.txt`
- The block is scalar call/stack-argument setup, not VMX/vector/GPU work:
  `store_context=13`, `load_offset.1=6`, `load_context=5`, `calls=2`,
  `context_barriers=2`, with calls to `0x826BF770` and recursive
  `0x82281D28`.
- Literal A64 machine-code disassembly remains blocked because the capture says
  ARM64 Capstone disassembly is unavailable. This is therefore a HIR/source
  codegen-floor audit.
- Decision: do not patch local store elision, guest-call fast-entry behavior,
  VMX128, or GPU/Vulkan from this block. The next useful slice is a
  default-off counter-only guest-stack argument handoff audit for direct guest
  calls, focused on `LOAD_OFFSET` from `r1 + constant` feeding
  `STORE_CONTEXT r3-r10/lr` before calls, with body-time weighting and blocker
  rows.
- Research note:
  `docs/research/20260526-054200-82281d28-hot-block-codegen-floor.md`.

## Guest-Stack Argument Handoff Audit Skeleton

- Continued from clean `master` at `9c507747b`; no full-speed proof marker was
  present and no active build/deploy/capture or real git operation blocked the
  slice.
- Added default-off, counter-only native cvars for the stack-argument handoff
  lane:
  `arm64_guest_stack_arg_handoff_audit`,
  `arm64_guest_stack_arg_handoff_audit_function`, and
  `arm64_guest_stack_arg_handoff_audit_budget`.
- Added native audit rows for `LOAD_OFFSET` from `r1 + constant` feeding
  `STORE_CONTEXT r3-r10/lr` before direct guest calls. Rows include target and
  callsite metadata, stack offset counts, argument-slot counts, resolved /
  compiled state, normal-entry fallback pressure, stackpoint sensitivity,
  context-barrier and other blocker counts, estimated avoidable bytes, and
  behavior-neutral markers.
- Added Android intent extra plumbing and Thor launcher/capture metadata
  parameters:
  `-Arm64GuestStackArgHandoffAudit`,
  `-Arm64GuestStackArgHandoffAuditFunction`, and
  `-Arm64GuestStackArgHandoffAuditBudget`.
- Extended `tools/thor/thor_hir_guest_call_arg_handoff_audit.ps1` with
  `-GuestStackArgHandoffAuditLogPath` so future captures can print
  body-weighted target rows from the new runtime log prefix.
- Validation:
  - `git diff --check` passed with existing CRLF warnings only.
  - `tools/thor/thor_xenia_debug.ps1` parser check passed.
  - `tools/thor/thor_hir_guest_call_arg_handoff_audit.ps1` parser check passed.
  - Old-log parser smoke passed on the latest `82281D28` HIR / fast-entry logs.
  - First `NativeCore` build failed on a missing `hir` namespace alias in the
    new anonymous-namespace helpers; fixed.
  - `NativeCore` passed after the fix.
  - `ApkShell` passed with the existing Android Gradle compileSdk warning.
- No Thor route capture was run and no generated behavior changed:
  `payload_materializations_allowed=0`, `behavior_changed=0`,
  `alternate_codegen=0`, `normal_entry=unchanged`, and
  `global_indirection=unchanged`.
- Next useful slice is FullDeploy plus a route-clean
  `0x82281D28` stack-argument handoff capture using
  `-Arm64GuestStackArgHandoffAudit true`,
  `-Arm64GuestStackArgHandoffAuditFunction 0x82281D28`, and
  `-Arm64GuestStackArgHandoffAuditBudget 16`, then join with
  `tools/thor/thor_hir_guest_call_arg_handoff_audit.ps1`.
- Research note:
  `docs/research/20260526-055500-guest-stack-arg-handoff-audit-skeleton.md`.

## Guest-Stack Argument Handoff Capture

- Continued from clean `master` at `baf5a6b92`; no full-speed proof marker was
  present, Thor `c3ca0370` was connected as `AYN Thor`, and no active
  build/deploy/capture or real git operation blocked the slice.
- FullDeploy passed and installed APK SHA256
  `E543483604CF4EE3B550D5A58A56F56F3505EEFB5E733CB6BE28778C93670BB2`.
- Ran a 180-second Blue Dragon route capture with
  `-Arm64GuestStackArgHandoffAudit true`,
  `-Arm64GuestStackArgHandoffAuditFunction 0x82281D28`,
  `-Arm64GuestStackArgHandoffAuditBudget 16`, filtered `82281D28`
  disassembly/block body-time metadata, and delayed body-time comparators
  `82282490,82281D28,82287788`.
- Capture prefix: `scratch/thor-debug/20260526-061637-*`.
  Screenshot reached visible opening sky / dragon-wing, and the focused
  fatal-marker search found no AndroidRuntime/tombstone/assert/backtrace/fatal
  exception/SIGSEGV/SIGABRT/crash/ANR rows.
- Fixed a parser bug in
  `tools/thor/thor_hir_guest_call_arg_handoff_audit.ps1` where `$Matches` was
  overwritten while tokenizing guest-stack audit rows. The parser now caches row
  identity before reading key/value tokens.
- Join report:
  `scratch/thor-debug/20260526-061637-guest-stack-arg-handoff-join.txt`.
  Runtime stack-specific counters were narrower than the broader callee-aware
  live-in evidence: `stack_arg_store_calls=17`, `stack_arg_store_fields=87`,
  `stack_arg_store_bytes=680`, `estimated_avoidable_bytes=1360`,
  `unresolved_direct_targets=52`, `normal_entry_fallback=67`,
  `stackpoint_sensitive=67`, `dirty_flush_points=268`, and
  `flush_context_barrier=260`.
- The largest stack-specific body-weighted row was recursive `0x82281D28`, but
  it had only one stack-argument call with six fields and unresolved /
  normal-entry fallback pressure. `0x826BF770` was already compiled and known,
  but only had one stack-specific field.
- Final perf stayed CPU/JIT-heavy: Main Thread `100%`, GPU Commands `11.5%`,
  Draw Thread `7.6%`, XMA Decoder `7.6%`. Final delayed body-time rows were
  `82281D28=65318256`, `82282490=29545224`, and `82287788=3471859`.
- Decision: close the current guest-stack argument handoff behavior lane. Do
  not patch behavior or run a quiet speed A/B from this evidence. Reopen only
  under a broader guarded-stub / late-bound-entry design that handles normal
  entry, global indirection, stackpoint/debug/exception visibility, dirty
  flushes, and unresolved targets.
- Next useful structural lane is fastmem/addressing or A64 load/store
  codegen-floor work for hot `LOAD_OFFSET` / `STORE_OFFSET` forms in
  `82281D28`, with default-off counters and body-time weighting before any
  behavior patch.
- Research note:
  `docs/research/20260526-061637-guest-stack-arg-handoff-capture.md`.

## 82281D28 Load/Store Offset Audit

- Continued from clean `master` at `0b49a1fce`; no full-speed proof marker was
  present and no xenia-thor build/deploy/capture or real git operation blocked
  the offline slice. A separate PS3 speed run was active in another repo, so no
  Thor capture was started.
- Added `tools/thor/thor_hir_load_store_offset_audit.ps1`, an offline
  behavior-neutral HIR/profile join for `LOAD_OFFSET` / `STORE_OFFSET` rows.
  It parses stamped filtered HIR plus A64 block body-time rows and separates
  guest-stack `r1` traffic from other context-GPR bases.
- Ran it against the latest route-clean `82281D28` capture:
  `scratch/thor-debug/20260526-061637-speed-logcat.txt`.
- Report:
  `scratch/thor-debug/20260526-063600-82281d28-load-store-offset-audit.txt`.
- Result: `82281D28` has `offset_ops=365`, `load_offset_ops=253`,
  `store_offset_ops=112`, and `mapped_body_ops=117`. Body-weighted offset
  traffic is dominated by guest-stack forms:
  `body_weighted_offset_ops=450860314`,
  `body_weighted_guest_stack_ops=411865334`,
  `body_weighted_context_gpr_ops=38994980`, and
  `body_weighted_unknown_base_ops=0`.
- Dominant block remains `82281D28:8228233C-82282370`, with body-weight
  contribution `325888766` from the `stw r11,0x64(r1)` and
  `ld r5-r10,0x170-0x198(r1)` call/setup sequence.
- Decision: do not patch generated behavior and do not run a quiet speed A/B
  from this offline audit. The next useful lane is a source-level A64 memory
  lowering feasibility audit for `ComputeMemoryAddress`,
  `AddGuestMemoryOffset`, and `OPCODE_LOAD_OFFSET` / `OPCODE_STORE_OFFSET`,
  especially `r1 + small constant`, while preserving 32-bit guest wrap,
  membase, byte-swap, MMIO/exception visibility, and fallback behavior.
- Research note:
  `docs/research/20260526-063600-82281d28-load-store-offset-audit.md`.

## A64 Memory Lowering Feasibility

- Continued from clean `master` at `a7e759eb9`; no full-speed proof marker was
  present and no xenia-thor build/deploy/capture or real git operation blocked
  the offline slice. A separate PS3 speed run was active in another repo, so no
  Thor capture was started.
- Added `tools/thor/thor_a64_memory_lowering_feasibility.ps1`, a deterministic
  source audit for A64 `ComputeMemoryAddress`, `AddGuestMemoryOffset`, and
  `OPCODE_LOAD_OFFSET` / `OPCODE_STORE_OFFSET`, cross-checking x64's
  `ComputeMemoryAddressOffset` reference and POSIX allocation granularity.
- Ran the audit against the latest load/store offset report:
  `scratch/thor-debug/20260526-063600-82281d28-load-store-offset-audit.txt`.
- Report:
  `scratch/thor-debug/20260526-070000-a64-memory-lowering-feasibility.txt`.
- Result: a legal reusable A64 lowering candidate exists, but only with strict
  constraints. The first safe shape is an offset-aware helper for normal
  non-MMIO constant-offset memory paths that keeps `x0` as the final 32-bit
  guest address before applying membase. It can plausibly fold
  `mov w0, Wsrc` + `add w0, w0, #imm` into an `add w0, Wsrc, #imm` where the
  existing granularity and fallback rules are preserved.
- Blocked shortcuts: host pointer plus load/store immediate addressing is not
  generally legal because it can change guest 32-bit wrap into host 64-bit
  pointer arithmetic. Unconditional MMIO/inline-MMIO folds are also blocked, and
  store-watch paths still require final guest address in `x0`.
- No generated behavior changed:
  `payload_materializations_allowed=0`, `behavior_changed=0`.
- Next useful slice is a default-off A64 offset-aware address helper prototype
  or source-tested codegen audit for constant-offset normal memory paths,
  preserving 32-bit wrap, byte swap, MMIO / exception visibility, store-watch
  `x0`, and fallback behavior.
- Research note:
  `docs/research/20260526-070000-a64-memory-lowering-feasibility.md`.

## A64 Offset Address Helper Prototype

- Continued from clean `master` at `6f4b0b1dc`; no full-speed proof marker was
  present and no xenia-thor build/deploy/capture or real git operation blocked
  the slice.
- Added default-off `arm64_offset_memory_address_fastpath`.
- Added `ComputeMemoryAddressOffset` for A64 normal memory paths. The helper
  folds constant offset guest-address math while preserving `x0` as the final
  32-bit guest address before membase. Non-constant offsets and
  `allocation_granularity() > 0x1000` use the old two-step fallback.
- Updated `OPCODE_LOAD_OFFSET` / `OPCODE_STORE_OFFSET` lowering to route
  through the new helper only when the cvar is enabled. Default behavior remains
  unchanged.
- Added Android intent forwarding and Thor launcher/capture metadata support for
  `-Arm64OffsetMemoryAddressFastpath`.
- Updated `tools/thor/thor_a64_memory_lowering_feasibility.ps1` so it recognizes
  the new default-off prototype and keeps rejecting unsafe host-pointer plus
  immediate-addressing shortcuts.
- Validation:
  - `tools/thor/thor_a64_memory_lowering_feasibility.ps1` passed with
    `legal_offset_helper_candidate=1`,
    `default_off_offset_helper_prototype=1`, and
    `behavior_changed=default_off_only`.
  - `tools/thor/thor_xenia_debug.ps1` parser check passed.
  - `tools/thor/thor_build.ps1 -Mode ApkShell` passed.
  - `tools/thor/thor_build.ps1 -Mode NativeCore` passed.
- No Thor route capture was run in this slice, and no speed claim exists.
- Next useful slice is FullDeploy plus a route-clean Thor capture with
  `-Arm64OffsetMemoryAddressFastpath true` and delayed body-time comparators
  `82282490,82281D28,82287788`. Treat that as route-safety proof only; do not
  run a quiet speed A/B until the enabled path reaches visible opening
  sky / dragon-wing with clean fatal markers.
- Research note:
  `docs/research/20260526-071500-a64-offset-address-helper-prototype.md`.

## A64 Offset Address Fastpath Route Capture

- Continued from clean `master` at `1ed1dec66`; no full-speed proof marker was
  present and no xenia-thor build/deploy/capture or real git operation blocked
  the slice.
- Confirmed Thor device `c3ca0370` was connected as `AYN Thor` on Android SDK
  `33`, and confirmed the legal Blue Dragon Disc 1 SD-card path.
- FullDeploy passed and installed APK SHA256
  `3195B62E66ABCE289187F6A0C4A67E621AE2A32931BBD98B790CD32CB0E33AB9`.
- Ran a 180-second Blue Dragon speed capture with
  `-Arm64OffsetMemoryAddressFastpath true`, delayed body-time comparators
  `82282490,82281D28,82287788`, and audit/disassembly/block tracing off.
- Capture prefix: `scratch/thor-debug/20260526-073440-*`.
  Screenshot reached the visible opening sky / dragon-wing frame, and focused
  fatal-marker search found no AndroidRuntime/tombstone/assert/backtrace/fatal
  exception/SIGSEGV/SIGABRT/crash/ANR rows.
- Final perf stayed CPU/JIT-heavy: Main Thread `100%`, XMA Decoder `11.5%`,
  GPU Commands `7.6%`, Draw Thread `3.8%`.
- Enabled-path generated code sizes were `82281D28=95724`,
  `82282490=86452`, and `82287788=35712`. A follow-up same-APK A/B is needed
  for authoritative control deltas because the latest route-clean baseline had
  audit instrumentation enabled.
- Final enabled body-time rows were `82282490=29593983`,
  `82281D28=7630474`, and `82287788=3430942`.
- Decision: route safety is proven for the enabled cvar, but speed is not. Keep
  `arm64_offset_memory_address_fastpath` default-off. Next useful slice is a
  same-APK quiet A/B or control sandwich on APK SHA
  `3195B62E66ABCE289187F6A0C4A67E621AE2A32931BBD98B790CD32CB0E33AB9`,
  comparing `-Arm64OffsetMemoryAddressFastpath false` versus `true`.
- Research note:
  `docs/research/20260526-073440-a64-offset-address-fastpath-route-capture.md`.

## A64 Offset Address Fastpath Quiet A/B

- Continued from clean `master` at `5ffe7d20b`; no full-speed proof marker was
  present and no xenia-thor build/deploy/capture or real git operation blocked
  the slice.
- Ran a same-APK ON/OFF/ON sandwich for
  `arm64_offset_memory_address_fastpath` using APK SHA256
  `3195B62E66ABCE289187F6A0C4A67E621AE2A32931BBD98B790CD32CB0E33AB9`.
- Captures:
  - ON-1: `scratch/thor-debug/20260526-073440-*`.
  - OFF: `scratch/thor-debug/20260526-074634-*`.
  - ON-2: `scratch/thor-debug/20260526-075015-*`.
- All captures ran 180 seconds, used delayed body-time comparators
  `82282490,82281D28,82287788`, kept audit/disassembly/block/call-edge tracing
  off, reached the visible opening sky / dragon-wing frame, and had clean
  focused fatal-marker searches.
- Final body-time / code-size signal:
  - `82282490`: OFF `31252631` body ticks and `87168` code bytes; ON captures
    `29593983` / `27182211` body ticks and `86452` code bytes.
  - `82281D28`: OFF `8049998` body ticks and `96908` code bytes; ON captures
    `7630474` / `7527649` body ticks and `95724` code bytes.
  - `82287788`: OFF `3540296` body ticks and `35732` code bytes; ON captures
    `3430942` / `3338470` body ticks and `35712` code bytes.
- Final perf still showed a CPU/JIT wall: Main Thread was `100%` on ON-1,
  `92.3%` on OFF, and `92.3%` on ON-2; GPU Commands stayed at `7.6%` or
  `11.5%`.
- Decision: the offset helper is route-clean and locally positive for generated
  code/body-time, but it is not sustained 30 FPS proof and not a route-wide FPS
  claim. Keep the cvar default-off globally, enable
  `-Arm64OffsetMemoryAddressFastpath true` explicitly in future Blue Dragon
  speed captures, and do not repeat this unchanged A/B.
- Next useful lane is broader A64 normal-memory/addressing or another
  route-backed codegen-floor audit, not another narrow offset-helper proof.
- Research note:
  `docs/research/20260526-075500-a64-offset-address-fastpath-quiet-ab.md`.

## 82282490 / 82287788 Codegen-Floor Capture

- Continued from clean `master` at `97d06641e`; no full-speed proof marker was
  present and no xenia-thor build/deploy/capture or real git operation blocked
  the slice.
- Ran a 180-second route-backed HIR / block-body capture with
  `-Arm64OffsetMemoryAddressFastpath true`, filtered OptHIR for
  `82282490,82287788`, block body-time for `82282490,82287788`, and delayed
  body-time comparators `82282490,82281D28,82287788`.
- Capture prefix: `scratch/thor-debug/20260526-080921-*`; APK SHA256
  `3195B62E66ABCE289187F6A0C4A67E621AE2A32931BBD98B790CD32CB0E33AB9`.
  Screenshot reached the visible opening sky / dragon-wing frame, and focused
  fatal-marker search was clean.
- Final perf remained CPU/JIT-heavy: Main Thread `88.4%`, XMA Decoder `53.8%`,
  GPU Commands `15.3%`, Draw Thread `7.6%`.
- HIR dumps were usable: `82282490` had `9863` OptHIR rows and `82287788` had
  `4169`.
- Top mapped body rows were `82282490:822825E0-822825F0`
  (`body_ticks_total=65574732`, `ticks_per_entry=80`),
  `82282490:8228252C-822825C4` (`15467751`, `23`), and
  `82287788:822877BC-82287864` (`1265302`, `1`).
- Offline load/store offset audits found `82282490` still has material offset
  traffic (`offset_ops=241`, `body_weighted_offset_ops=133143493`) but it is
  dominated by context-GPR offset loads and the already-closed local
  stvewx / MUL_ADD_V128 region. `82287788` had only `3` offset ops and
  `body_weighted_offset_ops=1209223`.
- Decision: do not patch generated behavior from this capture, and do not
  rerun unchanged `82282490` / `82287788` HIR codegen-floor captures. Keep using
  `-Arm64OffsetMemoryAddressFastpath true` explicitly in future Blue Dragon
  speed captures. The next fastmem/addressing slice must prove a broader
  reusable rule, such as no-wrap / normal-memory eligibility for hot
  `LOAD_OFFSET` / `STORE_OFFSET` forms across body-dominant route blocks, or
  close the current fastmem/addressing lane.
- Research note:
  `docs/research/20260526-081300-82282490-82287788-codegen-floor-capture.md`.

## A64 No-Wrap Memory Eligibility Audit

- Continued from clean `master` at `07c8cd86f`; no full-speed proof marker was
  present and no active build/deploy/capture or real git operation blocked the
  slice.
- Added `tools/thor/thor_a64_no_wrap_memory_eligibility_audit.ps1` to join the
  current route-backed load/store offset reports for `82281D28`, `82282490`,
  and `82287788` with A64/x64 memory-lowering source checks.
- Ran the tool and wrote
  `scratch/thor-debug/20260526-083000-a64-no-wrap-memory-eligibility-audit.txt`.
- The current offset-helper source checks are good:
  `compute_memory_address_offset=1`, `offset_helper_keeps_x0_guest_address=1`,
  `offset_helper_uses_w_register_add=1`, `load_offset_uses_membase_plus_x0=1`,
  and `store_watch_uses_final_x0=1`.
- Route offset volume remains material across the three reports:
  `offset_ops_total=609`, `load_offset_ops_total=409`,
  `store_offset_ops_total=200`, and
  `body_weighted_offset_ops_total=585213030`.
- Host-pointer-plus-immediate behavior is blocked:
  `runtime_no_wrap_counter_present=0`, `static_range_analysis_present=0`,
  `static_no_wrap_provable_rows=0`, and `runtime_no_wrap_proven_rows=0`.
  Top weighted bases are guest-GPR derived (`r1`, `r31`, `r27`, `r29`, `r28`),
  so HIR/profile evidence does not prove that guest address + offset cannot
  wrap 32-bit address space.
- Decision: close the current fastmem/addressing lane. Keep using
  `-Arm64OffsetMemoryAddressFastpath true` explicitly in future Blue Dragon
  captures because the helper is route-clean and locally positive, but do not
  add another memory-addressing behavior patch without a new no-wrap counter or
  static range-analysis proof.
- Validation:
  - `tools/thor/thor_a64_no_wrap_memory_eligibility_audit.ps1` passed.
- Research note:
  `docs/research/20260526-083000-a64-no-wrap-memory-eligibility-audit.md`.

## A64 Guarded-Stub Entry Design Audit

- Continued from clean `master` at `35683f614`; no full-speed proof marker was
  present, the worktree was clean, Thor `c3ca0370` was connected, and no Xenia
  app/build/deploy/capture or real git operation was active.
- Added `tools/thor/thor_a64_guarded_stub_entry_design_audit.ps1` to turn the
  guarded-stub / late-bound-entry fast-entry question into a deterministic
  source contract audit.
- Ran the tool and wrote
  `scratch/thor-debug/20260526-084500-a64-guarded-stub-entry-design-audit.txt`.
- The audit confirms the normal-entry contract is still singular:
  `normal_entry_singleton=true`, `global_indirection_singleton=true`,
  `normal_entry_abi_only_return_x0=true`,
  `direct_calls_use_normal_machine_code=true`,
  `late_bound_unresolved_stays_normal=true`,
  `stackpoint_reentry_contract=true`, and
  `host_debug_exception_boundaries=true`.
- It also confirms the missing behavior contracts:
  `alternate_entry_storage_present=false`,
  `payload_abi_storage_present=false`, and
  `behavior_fast_entry_present=false`.
- Joined with the prior target-row capture, runtime blockers remain material:
  `unresolved_direct_targets=52`, `normal_entry_fallback=67`,
  `stackpoint_sensitive=67`, `dirty_flush_points=268`, and
  `callee_first_use_known=0`.
- Decision: do not implement guest-call fast-entry behavior from the current
  source shape. Do not replace `A64Function::machine_code`, do not rewrite the
  global indirection slot, and do not materialize an `r3-r10/lr` payload without
  a dirty-flush ABI.
- Next useful default lane is another structural A64 lane, with guest-state /
  cache design only if it avoids the closed `r1/r11/f1/fpscr/barrier` lanes.
  Reopen fast-entry only as an explicit source/data-model patch for separate
  fast-entry storage plus payload/flush contracts, with generated behavior still
  unchanged until counters prove it.
- Validation:
  - PowerShell parser check passed for
    `tools/thor/thor_a64_guarded_stub_entry_design_audit.ps1`.
  - `tools/thor/thor_a64_guarded_stub_entry_design_audit.ps1` passed.
- Research note:
  `docs/research/20260526-084500-a64-guarded-stub-entry-design.md`.

## A64 Non-Closed Guest-State Cache Audit

- Continued from clean `master` at `9cc49e3a3`; no full-speed proof marker was
  present, Thor `c3ca0370` was connected, and no Xenia app/build/deploy/capture
  or real git operation was active.
- Added `tools/thor/thor_a64_nonclosed_guest_state_cache_audit.ps1` to rank
  guest-state/cache candidates while excluding closed `r1` / `r11` slots and
  separating `r3-r10` call-argument overlap.
- Ran the tool and wrote
  `scratch/thor-debug/20260526-085500-a64-nonclosed-guest-state-cache-audit.txt`.
- Source checks confirm behavior should not be patched yet:
  `same_block_context_promotion_exists=True`,
  `context_promotion_per_block=True`, `register_allocation_per_block=True`,
  `spills_to_locals=True`, `emit_time_context_cache_resets_per_block=True`,
  `host_gpr_pressure=True`, and `existing_pre_promotion_audit_hook=True`.
- Raw scores are still led by closed `r1=567` and `r11=446`. Call-argument
  slots also have volume (`r10=251`, `r9=186`, `r3=155`, `r8=135`, `r7=125`,
  `r6=115`) but overlap the closed fast-entry / handoff lanes.
- Strict non-closed targets exist: `r31=237`, `r30=175`, `r29=168`,
  `r28=152`, `r27=94`, and `r26=58`, aggregated across `82281D28`,
  `82282490`, and `82287788`.
- Decision: do not patch generated behavior. The useful next guest-state slice
  is a default-off counter-only post-promotion non-closed GPR state-cache audit
  for `r31,r30,r29,r28,r27` with no store elision, no context-load replacement,
  no payload materialization, explicit miss/flush reasons, and spill-pressure
  estimates.
- Validation:
  - PowerShell parser check passed for
    `tools/thor/thor_a64_nonclosed_guest_state_cache_audit.ps1`.
  - `tools/thor/thor_a64_nonclosed_guest_state_cache_audit.ps1` passed.
- Research note:
  `docs/research/20260526-085500-a64-nonclosed-guest-state-cache-audit.md`.

## A64 Non-Closed GPR Cache Audit Skeleton

- Continued from clean `master` at `ff737cace`; no full-speed proof marker was
  present, Thor `c3ca0370` was connected, and no active Xenia
  build/deploy/capture or real git operation was running.
- Added default-off `arm64_guest_state_nonclosed_cache_audit` and
  `arm64_guest_state_nonclosed_cache_audit_function`.
- Added a post-promotion, counter-only ContextPromotionPass audit for
  `r31,r30,r29,r28,r27`.
- The audit changes no generated behavior: no store elision, no context-load
  replacement, no payload materialization, normal fallback preserved.
- Logged rows include candidate loads/stores, clean/dirty hits possible,
  miss/flush reasons, per-slot totals, normal fallback, peak live slots, and a
  conservative spill-pressure estimate.
- Added Android intent forwarding and Thor launcher/capture metadata for:
  `-Arm64GuestStateNonclosedCacheAudit` and
  `-Arm64GuestStateNonclosedCacheAuditFunction`.
- Validation:
  - `tools/thor/thor_xenia_debug.ps1` parser check passed.
  - `git diff --check` passed with CRLF conversion warnings only.
  - `tools/thor/thor_build.ps1 -Mode ApkShell` passed.
  - `tools/thor/thor_build.ps1 -Mode NativeCore` passed.
- No Thor route capture was run in this slice, and no speed claim exists.
- Next useful slice is FullDeploy plus a route-clean audit capture for one wall
  function, starting with `0x82281D28`, with
  `-Arm64OffsetMemoryAddressFastpath true`,
  `-Arm64GuestStateNonclosedCacheAudit true`, and delayed body-time comparators
  `82282490,82281D28,82287788`.
- Research note:
  `docs/research/20260526-090500-a64-nonclosed-gpr-cache-audit-skeleton.md`.

## A64 Non-Closed GPR Cache Audit Capture

- Continued from clean `master` at `a3bb4442e`; no full-speed proof marker was
  present, Thor `c3ca0370` was connected, and no active Xenia
  build/deploy/capture or real git operation was running.
- FullDeploy passed and installed APK SHA256
  `75E498D1038C5E1CFD45FC4036182C51EF4506E35FC8DC91EF1DFFFBCBB8D109`.
- Ran a 180-second Blue Dragon route capture with
  `-Arm64OffsetMemoryAddressFastpath true`,
  `-Arm64GuestStateNonclosedCacheAudit true`,
  `-Arm64GuestStateNonclosedCacheAuditFunction 0x82281D28`, and delayed
  body-time comparators `82282490,82281D28,82287788`.
- Capture prefix: `scratch/thor-debug/20260526-091832-*`.
  Screenshot reached the visible opening sky / dragon-wing frame, and focused
  fatal-marker search found no crash/assert/tombstone rows.
- Final perf remained CPU/JIT-heavy: Main Thread `100%`, GPU Commands `7.6%`,
  XMA Decoder `7.6%`, Draw Thread `0.0%`.
- Final body-time rows were `82282490=28864797`, `82281D28=7161895`, and
  `82287788=3187095`.
- Audit result for `0x82281D28`: `candidate_loads=181`,
  `candidate_stores=75`, `clean_hits_possible=0`, `dirty_hits_possible=0`,
  `normal_fallback=256`, `peak_live_slots=5`,
  `estimated_spill_pressure=3`, with high multi-pred/label/branch/external
  visibility pressure.
- Decision: close the current non-closed post-promotion GPR cache behavior
  lane. Do not run a quiet speed A/B, do not patch generated behavior, and do
  not rerun unchanged nonclosed-cache captures for `0x82282490` or
  `0x82287788`.
- Next useful work is a harness rerank / different structural lane, not another
  guest-state cache audit.
- Research note:
  `docs/research/20260526-092500-a64-nonclosed-gpr-cache-capture.md`.

## A64 Fast-Entry Data Model Rerank

- Continued from clean `master` at `18fe015b5`; no full-speed proof marker was
  present, Thor `c3ca0370` was connected, and no active Xenia
  build/deploy/capture or real git operation was running.
- Ran the continual harness window:
  `powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_continual_harness_review.ps1 -Mode Window`.
  It refreshed `scratch/thor-debug/continual-harness-window.md`; the scratch
  window is not committed.
- Rerank decision: do not start another Thor capture from the closed
  nonclosed-cache lane. The only still-open speed-adjacent lane is guest-call
  fast-entry as behavior-neutral source/data-model work.
- Added separate `A64Function` fast-entry storage and contract metadata:
  `guest_call_fast_entry_code`, length, payload mask bits for `r3-r10/lr`,
  dirty-flush mask bits, `A64GuestCallFastEntryContract`, and
  `SetupGuestCallFastEntry`.
- Updated:
  `tools/thor/thor_a64_guest_call_fast_entry_feasibility.ps1` and
  `tools/thor/thor_a64_guarded_stub_entry_design_audit.ps1`.
- Generated behavior remains unchanged: normal `machine_code()`, host-to-guest
  entry, direct-call `fn->machine_code()` behavior, and global indirection are
  still the active paths.
- Audit reports:
  `scratch/thor-debug/20260526-093500-a64-fast-entry-feasibility.txt` and
  `scratch/thor-debug/20260526-093500-a64-guarded-stub-data-model-audit.txt`.
  They report `fast_entry_data_model_present=true`,
  `existing_fast_entry_behavior_absent=true`,
  `alternate_entry_storage_present=true`,
  `payload_abi_storage_present=true`, `behavior_fast_entry_present=false`, and
  `decision=data_model_present_behavior_unchanged`.
- Prior runtime blockers remain:
  `unresolved_direct_targets=52`, `normal_entry_fallback=67`,
  `stackpoint_sensitive=67`, `dirty_flush_points=268`, and
  `callee_first_use_known=0`.
- Validation:
  - PowerShell parser checks passed for both updated audit scripts.
  - Both updated audit scripts passed.
  - `tools/thor/thor_build.ps1 -Mode NativeCore` passed.
- No Thor route capture was run and no speed claim exists.
- Next useful slice: a behavior-neutral dirty-flush/payload protocol audit or
  default-off stub skeleton. Do not patch direct callsite behavior until direct
  guard/fallback, payload population, dirty flushes, late-bound target behavior,
  and stackpoint resume are explicit.
- Research note:
  `docs/research/20260526-093500-a64-fast-entry-data-model-rerank.md`.

## A64 Fast-Entry Dirty-Flush Protocol Audit

- Continued from clean `master` at `d5f241464`; no full-speed proof marker was
  present and no active Thor build/deploy/capture or real git operation was
  running.
- Added `tools/thor/thor_a64_fast_entry_dirty_flush_protocol_audit.ps1`.
- Ran:
  `powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_a64_fast_entry_dirty_flush_protocol_audit.ps1`.
- Wrote report:
  `scratch/thor-debug/20260526-094500-a64-fast-entry-dirty-flush-protocol-audit.txt`.
- Findings:
  - payload mask covers `r3-r10/lr` (`9/9`);
  - dirty-flush mask covers context barrier, helper call, host call,
    debug trap, tail call, return, exception, and unresolved target (`8/8`);
  - normal entry, direct calls, and indirection remain unchanged;
  - payload population, dirty flush codegen, fast-entry stub codegen,
    direct-call guards, and fast-entry stackpoint resume protocol are still
    absent.
- Prior runtime blockers still apply:
  `unresolved_direct_targets=52`, `normal_entry_fallback=67`,
  `stackpoint_sensitive=67`, `dirty_flush_points=268`,
  `flush_context_barrier=260`, `parent_pre_call_flush_points=195`, and
  `callee_first_use_known=0`.
- Decision: data model is enough for more source/protocol work, but behavior is
  still blocked. A safe next patch is a default-off stub skeleton or no-op
  codegen protocol helpers only; do not patch direct callsites,
  `machine_code()`, global indirection, payload materialization, or run a
  quiet speed A/B from this evidence.
- Validation:
  - PowerShell parser check passed for the new tool.
  - The new tool passed.
  - Existing `thor_a64_guarded_stub_entry_design_audit.ps1` and
    `thor_a64_guest_call_fast_entry_feasibility.ps1` still passed.
  - `git diff --check` passed with CRLF conversion warnings only.
- Re-armed the repo goal loop with the new `094500` decision note and updated
  the `blue-dragon-full-speed-sprint` heartbeat automation to the same next
  lane.
- No Thor route capture was run and no speed claim exists.
- Research note:
  `docs/research/20260526-094500-a64-fast-entry-dirty-flush-protocol.md`.

## A64 Fast-Entry Stub Skeleton

- Continued from clean `master` at `8f78a84bb`; no full-speed proof marker was
  present and no active Thor build/deploy/capture or real git operation was
  running.
- Added a behavior-neutral fast-entry skeleton:
  - `A64GuestCallFastEntryFlags` now separates skeleton metadata from future
    behavior enablement.
  - `MakeA64GuestCallFastEntryStubSkeletonContract` and validation helpers make
    the full `r3-r10/lr` payload plus dirty-flush boundary contract explicit.
  - `A64Function::SetupGuestCallFastEntryStubSkeleton` exists, but no generated
    path calls it yet.
  - Added default-off `arm64_guest_call_fast_entry_stub_skeleton` plus
    Android/Thor launch plumbing; enabling it only logs
    `alternate_codegen=0`, direct calls unchanged, global indirection
    unchanged, and `behavior_changed=0`.
- Extended `tools/thor/thor_a64_fast_entry_dirty_flush_protocol_audit.ps1` to
  require the skeleton/cvar while still requiring generated behavior to be
  absent.
- Tightened `tools/thor/thor_a64_guest_call_fast_entry_feasibility.ps1` so the
  default-off skeleton cvar is not counted as generated behavior.
- Audit report:
  `scratch/thor-debug/20260526-101000-a64-fast-entry-stub-skeleton-audit.txt`.
  It reports `stub_skeleton_protocol_present=true`,
  `stub_skeleton_default_off_cvar_present=true`,
  `direct_calls_still_use_normal_entry=true`,
  `late_bound_paths_still_use_normal_entry=true`,
  `payload_population_codegen_absent=true`, `dirty_flush_codegen_absent=true`,
  `fast_entry_stub_codegen_absent=true`, and
  `decision=stub_skeleton_source_ready_but_behavior_blocked`.
- No Thor route capture was run and no speed claim exists.
- Validation:
  - PowerShell parser checks passed for the updated Thor scripts.
  - Updated protocol audit passed and wrote
    `scratch/thor-debug/20260526-101000-a64-fast-entry-stub-skeleton-audit.txt`.
  - `thor_a64_guest_call_fast_entry_feasibility.ps1` passed after the
    skeleton-cvar behavior classification fix.
  - `thor_a64_guarded_stub_entry_design_audit.ps1` passed.
  - `git diff --check` passed with CRLF conversion warnings only.
  - `tools/thor/thor_build.ps1 -Mode NativeCore` passed.
  - `tools/thor/thor_build.ps1 -Mode ApkShell` passed.
- Research note:
  `docs/research/20260526-101000-a64-fast-entry-stub-skeleton.md`.

## A64 Fast-Entry Guard Protocol

- Continued from clean `master` at `5db8b216e`; no full-speed proof marker was
  present, Thor `c3ca0370` was connected, the app was not running, and no
  active Thor build/deploy/capture or real git operation was running.
- Added behavior-neutral direct-call guard protocol source:
  - `A64GuestCallFastEntryGuardBlocker` names blockers for behavior disabled,
    incomplete contract, missing fast-entry code, non-direct call, unresolved
    target, missing payload population, missing dirty flush, incomplete dirty
    flush coverage, missing late-bound fallback, missing stackpoint resume, and
    debug/exception visibility.
  - `A64GuestCallFastEntryGuardInputs` models the runtime/source inputs a
    future guard needs.
  - `A64GuestCallFastEntryGuardDecision` plus
    `EvaluateA64GuestCallFastEntryGuard` conservatively block if any required
    protocol is missing.
- Added `tools/thor/thor_a64_fast_entry_guard_protocol_audit.ps1`.
- Audit report:
  `scratch/thor-debug/20260526-102500-a64-fast-entry-guard-protocol-audit.txt`.
  It reports `guard_blockers_cover_required_classes=true`,
  `guard_inputs_cover_runtime_contracts=true`,
  `guard_evaluator_blocks_missing_protocols=true`,
  `guard_model_unused_by_codegen=true`, `direct_callsite_guard_absent=true`,
  `payload_population_codegen_absent=true`, `dirty_flush_codegen_absent=true`,
  `fast_entry_stub_codegen_absent=true`, and
  `decision=guard_protocol_source_ready_behavior_blocked`.
- Generated behavior remains unchanged: no callsite guard, payload
  materialization, dirty flush codegen, stub codegen, normal-entry replacement,
  or global-indirection rewrite.
- No Thor route capture was run and no speed claim exists.
- Validation:
  - PowerShell parser checks passed for the new guard audit and updated Thor
    scripts.
  - `tools/thor/thor_a64_fast_entry_guard_protocol_audit.ps1` passed and wrote
    `scratch/thor-debug/20260526-102500-a64-fast-entry-guard-protocol-audit.txt`.
  - `tools/thor/thor_a64_fast_entry_dirty_flush_protocol_audit.ps1` passed.
  - `tools/thor/thor_a64_guest_call_fast_entry_feasibility.ps1` passed.
  - `git diff --check` passed with CRLF conversion warnings only.
  - `tools/thor/thor_build.ps1 -Mode NativeCore` passed.
- Research note:
  `docs/research/20260526-102500-a64-fast-entry-guard-protocol.md`.
