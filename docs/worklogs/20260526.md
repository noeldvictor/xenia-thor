# 2026-05-26 Worklog

## A64 Register Allocation Audit Capture

- Started from the reset plan and confirmed no full-speed proof marker,
  clean `master` worktree, no active build/deploy/capture/git operation, and
  connected Thor `c3ca0370`.
- Ran FullDeploy:
  `powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`.
  The build/install passed with only the existing Android Gradle compileSdk
  warning.
- Ran a 180-second Blue Dragon route capture:
  `powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -Arm64RegisterAllocationAudit true -Arm64RegisterAllocationAuditFunction 0x82282490 -Arm64SpeedProfileBodyTimeFilter "82282490,82281D28,82287788" -Arm64SpeedProfileBodyTimeAfterMs 120000 -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 25 -Arm64SpeedProfileMinDelta 1 -StopAppAfterCapture true`.
- Capture prefix: `scratch/thor-debug/20260526-000910-*`.
  APK SHA256:
  `4B3B48CD3DB69B093CF3D47FA84BBC211DD09D23063F08DDD0064496FF6AC356`.
- Screenshot reached the visible opening sky / dragon-wing route. Fatal-marker
  scan found no Xenia, AndroidRuntime, tombstone, assert, or backtrace failure;
  the only signal marker was an unrelated Google Play services unstable
  start-timeout kill.
- Register allocation audit for `0x82282490`:
  `blocks=164`, `blocks_with_spills=0`, `instructions=9016`,
  `dest_values=4517`, `locals_before=0`, `locals_after=0`,
  `local_slots_added=0`, `behavior_changed=0`.
- Per set: INT `dest_values=2877`, `spill_requests=0`,
  `max_active_registers=6`; FLOAT `dest_values=350`, `spill_requests=0`,
  `max_active_registers=16`; VEC `dest_values=1290`,
  `spill_requests=0`, `max_active_registers=16`.
- Delayed final body-time rows:
  `82282490=29894735`, `82281D28=8077356`, `82287788=3611977`.
  Final perf remained CPU/JIT-heavy: Main Thread `92.3%`, GPU Commands
  `11.5%`, Draw Thread `3.8%`.
- Research note:
  `docs/research/20260526-001500-a64-register-allocation-audit-capture.md`.

Next: close the narrow allocator-spill-pressure lane for current `0x82282490`
evidence. Do not patch allocator behavior or run a quiet speed A/B from this
capture. Prefer helper ABI/block-linking audit for the wall cluster, or current
PERMUTE / `LOAD_VECTOR_SHL` / `LOAD_VECTOR_SHR` route counters before VMX128
behavior work.

## A64 Helper / Block-Link Offline Audit

- Added `tools/thor/thor_a64_helper_block_link_audit.ps1`, a deterministic
  source/log audit for the `82282490` / `82281D28` / `82287788` wall cluster.
- Ran parser validation:
  `$tokens=$null; $errors=$null; [System.Management.Automation.PSParser]::Tokenize((Get-Content -Raw tools\thor\thor_a64_helper_block_link_audit.ps1), [ref]$errors)`.
- Ran:
  `powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_a64_helper_block_link_audit.ps1`.
- Findings from `scratch/thor-debug/20260526-000910-*`: final interval had
  `entry_delta=3831037`, `direct_delta=3357831`,
  `indirect_delta=469505`, `extern_delta=389034`,
  `dispatch_delta_total=4216370`, `dispatch_delta_per_entry_delta=1.10`,
  `resolves_delta=5`, and `resolve_misses_total=0`.
- Source checks confirmed direct compiled calls, indirection fallback,
  runtime resolve fallback, extern guest-to-host thunk, stackpoint/post-call
  stack sync, call-edge profiling support, singleton `A64Function::machine_code_`,
  and guest-address-only code-cache indirection.
- Research note:
  `docs/research/20260526-004800-a64-helper-block-link-audit.md`.

Next: run a Thor route capture using existing call-edge profiling for
`82282490,82281D28,82287788` before any helper ABI or block-linking behavior
patch. Resolver-first work is closed for the warmed route because the latest
capture has zero resolve misses and only five resolves in the final interval.

## A64 Call-Edge Recursion Capture

- Ran the planned Thor route capture with call-edge profiling:
  `powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -Arm64SpeedProfileCallEdgeFilter "82282490,82281D28,82287788" -Arm64SpeedProfileBodyTimeFilter "82282490,82281D28,82287788" -Arm64SpeedProfileBodyTimeAfterMs 120000 -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 25 -Arm64SpeedProfileMinDelta 1 -StopAppAfterCapture true`.
- Capture prefix: `scratch/thor-debug/20260526-005803-*`.
  Commit `2131e7922`, APK SHA256
  `4B3B48CD3DB69B093CF3D47FA84BBC211DD09D23063F08DDD0064496FF6AC356`.
- Screenshot reached the visible opening sky / dragon-wing route. Fatal-marker
  search found no Xenia, AndroidRuntime, tombstone, assert, backtrace, fatal
  exception, SIGSEGV, SIGABRT, crash, or ANR lines; the normal Adreno Vulkan
  application-name line was the only `Xenia` hit.
- Final perf remained CPU/JIT-heavy: Main Thread `88.4%`, GPU Commands
  `11.5%`, XMA Decoder `7.6%`, Draw Thread `0.0%`.
- Final body-time rows:
  `82282490=32874047`, `82281D28=9434976`, `82287788=3264491`.
- `tools/thor/thor_hir_call_path_report.ps1` dynamic call-edge summaries:
  `82282490` dynamic edges were `94.24%` of body time, with
  self-recursive `822825E0 -> 82282490` at `69.62%`; `82281D28` dynamic edges
  were `88.95%`, with self-recursive `8228233C -> 82281D28` at `72.22%`;
  `82287788` was mostly parent-exclusive (`79.27%`).
- Research note:
  `docs/research/20260526-011000-a64-call-edge-recursion-capture.md`.

Next at that point: do not rerun unchanged call-edge captures or patch
resolver/direct recursive dispatch from this evidence. The next speed lane was
the VMX128 route-counter check recorded below; that follow-up now supersedes
this next-action note.

## VMX128 Route-Stabilized Counter Closure

- Ran the continual harness window before another Thor route run:
  `powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_continual_harness_review.ps1 -Mode Window`.
  It refreshed `scratch/thor-debug/continual-harness-window.md`; this scratch
  window is not committed.
- Ran a 180-second Blue Dragon route capture with function disassembly,
  block-body, and delayed body-time profiling:
  `powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -DisassembleFunctionFilter "82282490,82281D28,82287788" -Arm64SpeedProfileBlockFilter "82282490,82281D28,82287788" -Arm64SpeedProfileBlockBodyTime true -Arm64SpeedProfileBodyTimeFilter "82282490,82281D28,82287788" -Arm64SpeedProfileBodyTimeAfterMs 120000 -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 30 -Arm64SpeedProfileMinDelta 1 -StopAppAfterCapture true`.
- Capture prefix: `scratch/thor-debug/20260526-011255-*`.
  Commit `7aa4b90d4`, APK SHA256
  `4B3B48CD3DB69B093CF3D47FA84BBC211DD09D23063F08DDD0064496FF6AC356`.
- Screenshot reached visible opening sky / dragon-wing. Fatal-marker search
  found no AndroidRuntime, tombstone, assert, backtrace, fatal exception,
  SIGSEGV, SIGABRT, crash, or ANR lines.
- Final perf remained CPU/JIT-heavy: Main Thread `92.3%`, GPU Commands
  `11.5%`, Draw Thread `7.6%`. This was trace-heavy and is not a quiet speed
  proof.
- Final delayed function body-time rows:
  `82282490=98058145`, `82281D28=67536752`, `82287788=14590679`.
- Final hot block rows were still dominated by scalar/control or call-heavy
  blocks: `82282490:822825E0=70762934`,
  `82281D28:8228233C=48139529`, and
  `82282490:8228252C=16310678`.
- Ran route-stabilized vector shape reports. Because all-three-function HIR
  dumping overflowed logcat, `82282490` and `82281D28` used older full HIR
  dumps joined to the current block profile; `82287788` used current HIR.
- Weighted counters:
  `82282490` PERMUTE/SHL/SHR/PACK/UNPACK =
  `2524413/1682942/0/0/0`; `82281D28` =
  `1317189/878126/0/0/0`; `82287788` =
  `7353192/700304/2801216/0/0`.
- Interpretation: `PACK` remains absent, `UNPACK` remains zero weighted, and
  the active `82282490` / `82281D28` vector volume is concentrated in closed
  stvewx/vmaddfp local shapes. `82287788` has real `PERMUTE` /
  `LOAD_VECTOR_SHR` volume, but its vector block `82287D10` was only
  `531338` body ticks compared with scalar/control blocks.
- Ran codegen-floor checks for `822825E0`, `8228252C-822825C4`,
  `822877BC-82287868`, and `82287D10-82287D84`. `822825E0` is mostly
  `store_context`, barriers, compares, and a recursive call; `822877BC` is
  scalar/control/context-heavy with `context_barriers=9`.
- `82281D28:8228233C` still could not be mapped from the available OptHIR:
  `StartGuest 8228233C not found in OptHIR dump for 82281D28`.
- Research note:
  `docs/research/20260526-012000-vmx128-route-stabilized-counters.md`.

Next: close broad VMX128 `PERMUTE` / `LOAD_VECTOR_SHL` /
`LOAD_VECTOR_SHR` behavior for now. The next useful slice is a hot-block
codegen-floor/source audit with reliable one-function or file-backed HIR dumps,
especially to recover `82281D28:8228233C`, then target a reusable scalar,
context-barrier, helper ABI, or direct-call stackpoint rule only if the audit
supports it.

## 82281D28 HIR / Block-Profile Join Audit

- Started from clean `master` at `13c098afe`; no full-speed proof marker was
  present. Thor `c3ca0370` was connected. A separate Windows Eternal Sonata
  PowerShell process was running, but it was Windows-only PS3 work and not a
  Thor build/deploy/capture/git operation.
- Ran a one-function 180-second Blue Dragon route capture to avoid all-three
  function HIR logcat overflow:
  `powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -DisassembleFunctionFilter "82281D28" -Arm64SpeedProfileBlockFilter "82281D28" -Arm64SpeedProfileBlockBodyTime true -Arm64SpeedProfileBodyTimeFilter "82282490,82281D28,82287788" -Arm64SpeedProfileBodyTimeAfterMs 120000 -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 30 -Arm64SpeedProfileMinDelta 1 -StopAppAfterCapture true`.
- Capture prefix: `scratch/thor-debug/20260526-013226-*`.
  APK SHA256:
  `4B3B48CD3DB69B093CF3D47FA84BBC211DD09D23063F08DDD0064496FF6AC356`.
- Screenshot reached the visible opening sky / dragon-wing route. Fatal-marker
  search found no AndroidRuntime, fatal exception, tombstone, assert,
  backtrace, SIGSEGV, SIGABRT, crash, or ANR rows.
- Final perf remained CPU/JIT-heavy and trace-heavy: Main Thread `100%`,
  XMA Decoder `53.8%`, GPU Commands `7.6%`, Draw Thread `0.0%`.
- Final delayed body-time rows:
  `82281D28=66940160`, `82282490=28800287`, `82287788=3559908`.
  Final `82281D28` block-body top was
  `block=20 guest=8228233C body_ticks_total=47699966`.
- The one-function OptHIR dump recovered `82281D28` static HIR, but the exact
  runtime hot guest `8228233C` still did not appear as a printed HIR label or
  PPC comment. The first printed PPC comment was `822823B0`.
- Added `tools/thor/thor_hir_block_profile_join_audit.ps1` to detect unsafe
  joins between runtime block profile rows and printed HIR labels/comments.
  Parser validation passed.
- Ran:
  `powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_block_profile_join_audit.ps1 -LogPath scratch\thor-debug\20260526-013226-speed-logcat.txt -Function 82281D28 -Phase OptHIR -BlockProfileLog scratch\thor-debug\20260526-013226-speed-logcat.txt -ProfileKind Body -Top 20`.
  Result: `hir_labels=117`, `ppc_comments=2184`, `profile_rows=88`,
  `guest_hir_label_matches=0`, `guest_ppc_comment_matches=0`,
  `ordinal_label_matches=0`, `unmatched_profile_guests=88`,
  `ordinal_fallback_mismatches=88`, `join_status=unsafe`.
- Source review: block profile rows log `profile_block_address(i)` from
  `A64Function`; `A64Emitter::FindBlockGuestAddress` fills that with the first
  `SOURCE_OFFSET` before falling back to `GuestAddressFor()`. This can produce
  runtime profile guests that are not printed as HIR labels/comments.
- Research note:
  `docs/research/20260526-014000-82281d28-hir-block-profile-join.md`.

Next: do not patch behavior from current weighted HIR/block-profile joins for
`82281D28`. Build a deterministic block-profile-to-HIR mapper or metadata dump
that records runtime ordinal, `FindBlockGuestAddress`, first source offset,
first printable PPC comment, HIR label if any, and source span; then make HIR
analysis scripts refuse ordinal fallback unless the metadata proves it safe.

## A64 Block-Profile Metadata Mapper

- Continued from the reset plan with no full-speed proof marker, clean
  `master`, connected Thor `c3ca0370`, and no active Thor build/deploy/capture
  or real git operation.
- Implemented behavior-neutral A64 profile block metadata in
  `src/xenia/cpu/backend/a64/a64_function.*`,
  `src/xenia/cpu/backend/a64/a64_emitter.*`, and
  `src/xenia/cpu/backend/a64/a64_backend.cc`.
- New speed-profile block and block-body rows now append:
  `first_source`, `last_source`, `first_guest`, `last_guest`,
  `first_comment`, `last_comment`, `label`, and `hir_instrs`.
- Updated HIR/profile analysis tools:
  `tools/thor/thor_hir_block_profile_join_audit.ps1`,
  `tools/thor/thor_hir_vector_shape_report.ps1`,
  `tools/thor/thor_hir_block_mix_report.ps1`, and
  `tools/thor/thor_hir_call_path_report.ps1`.
- The vector, block-mix, and call-path reports now refuse ordinal fallback by
  default and report match/fallback/unmatched counts. `-AllowOrdinalFallback`
  remains only for explicit legacy comparison.
- Parser validation passed for all four PowerShell tools.
- Old-log checks on `scratch/thor-debug/20260526-013226-speed-logcat.txt`
  correctly kept `join_status=unsafe` and prevented fake weighted rows:
  vector shape reported `profile_unmatched=154`; block-mix and call-path
  reported `profile_unmatched=242`.
- Native build passed:
  `powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore`.
- Research note:
  `docs/research/20260526-015900-a64-block-profile-metadata-mapper.md`.

Next: FullDeploy and run a route-clean one-function `82281D28` capture with
block body-time/disassembly enabled. Then run
`tools/thor/thor_hir_block_profile_join_audit.ps1` on the new log and require
metadata-backed mapping before any `82281D28` behavior patch. This patch is not
speed proof and should not be followed by a quiet speed A/B.

## 82281D28 Metadata Capture Blocker

- Started from clean `master` at `d39f3460e`; no full-speed proof marker was
  present, Thor `c3ca0370` was connected, and no active Thor build/deploy/
  capture or real git operation was running.
- Ran FullDeploy:
  `powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`.
  Build/install passed with the existing Android Gradle compileSdk warning.
- Ran the planned one-function capture:
  `powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -DisassembleFunctionFilter "82281D28" -Arm64SpeedProfileBlockFilter "82281D28" -Arm64SpeedProfileBlockBodyTime true -Arm64SpeedProfileBodyTimeFilter "82282490,82281D28,82287788" -Arm64SpeedProfileBodyTimeAfterMs 120000 -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 30 -Arm64SpeedProfileMinDelta 1 -StopAppAfterCapture true`.
- Capture prefix: `scratch/thor-debug/20260526-021159-*`.
  APK SHA256:
  `963CE83618F947C979BFAB3558CC2ED44A86055C6F8AC5077F5F0E1A7DDF5504`.
- Screenshot reached the visible opening sky / dragon-wing route. Focused
  fatal-marker search found no Xenia/AndroidRuntime/tombstone/assert/
  backtrace/fatal exception/SIGSEGV/SIGABRT/crash/ANR rows.
- Final perf remained CPU/JIT-heavy and trace-heavy: Main Thread `92.5%`,
  XMA Decoder `48.1%`, GPU Commands `11.1%`, Draw Thread `3.7%`.
- Final delayed body-time rows:
  `82281D28=66528341`, `82282490=36273569`, `82287788=3535771`.
- Hot block metadata now exists for the wall block:
  `82281D28:block20 guest=8228233C body_ticks_total=47409053`,
  `first_source=8228233C`, `last_source=82282370`,
  `first_comment=8228233C`, `last_comment=82282370`,
  `label=8228233C`, `hir_instrs=34`.
- Fixed `tools/thor/thor_hir_block_profile_join_audit.ps1` after finding that
  the metadata regex overwrote PowerShell `$Matches` and zeroed parsed totals.
  Parser validation passed.
- Corrected join audit on the new log reports:
  `metadata_rows=88`, `metadata_mappable_rows=0`,
  `active_metadata_unmappable_rows=88`,
  `top_active_metadata_unmappable=block=20,guest=8228233C,total=47409053`,
  and `join_status=unsafe`.
- Research note:
  `docs/research/20260526-022000-82281d28-metadata-capture-blocker.md`.

Next: do not patch `82281D28` behavior from weighted HIR/profile joins. Emit
file-backed or log-backed per-block HIR text/source spans for active unmappable
rows, starting with `82281D28` block `20` (`8228233C-82282370`), then rerun the
join audit.

## HIR Block Profile Stamps

- Continued from clean `master` at `bc1189517`; no full-speed proof marker was
  present, Thor `c3ca0370` was connected, and no active Thor build/deploy/
  capture or real git operation was running.
- Implemented behavior-neutral per-block HIR dump stamps in
  `src/xenia/cpu/hir/hir_builder.cc`. Each `HIRBuilder::Dump()` block now emits
  a `block_profile` comment with real `block->ordinal`, source span, guest span,
  first printable PPC comment span, label address, and visible HIR instruction
  count.
- Updated `tools/thor/thor_hir_block_profile_join_audit.ps1` to parse the new
  stamps as `hir_block_*` fields and to mark profile rows mappable when the
  runtime guest address falls inside the stamped source or guest span. Ordinal
  fallback remains disallowed for behavior decisions.
- Validation:
  - `git diff --check` passed.
  - PowerShell parser validation for
    `tools/thor/thor_hir_block_profile_join_audit.ps1` passed.
  - Old-log audit on `scratch/thor-debug/20260526-021159-speed-logcat.txt`
    still reports `join_status=unsafe` with `hir_block_metadata_rows=0`, so the
    tool does not invent mappings for stale captures.
  - `powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore`
    passed.
- Re-armed the repo goal loop with
  `powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_codex_goal_loop.ps1 -Mode EnableBlueDragonFullSpeed -MaxAttempts 48`.
- Updated heartbeat automation `blue-dragon-full-speed-sprint` to point at the
  new HIR block-stamp capture/deploy step.
- Research note:
  `docs/research/20260526-023500-hir-block-profile-stamps.md`.

Next: FullDeploy, run a one-function `82281D28` Blue Dragon route capture with
disassembly and block body-time enabled, then rerun
`tools/thor/thor_hir_block_profile_join_audit.ps1`. Use weighted `82281D28`
HIR evidence only if the new log has `hir_block_mappable_rows > 0` and
`join_status` no longer reports `unsafe`.

## 82281D28 Block Map Capture

- Continued from clean `master` at `be55b0009`; no full-speed proof marker was
  present, Thor `c3ca0370` was connected, and no active build/deploy/capture or
  real git operation was running.
- Ran FullDeploy:
  `powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`.
  Build/install passed with the existing Android Gradle compileSdk warning.
- Ran the planned one-function `82281D28` capture with disassembly, block
  body-time, and delayed body-time comparators. First capture prefix:
  `scratch/thor-debug/20260526-024838-*`. It reached visible opening
  sky/dragon-wing and had a clean focused fatal-marker search, but the join
  audit stayed unsafe because Thor logcat was still 256 KiB and retained only
  the tail of the one-function OptHIR dump:
  `hir_block_metadata_rows=0`, `hir_block_mappable_rows=0`,
  `active_metadata_unmappable_rows=88`, `join_status=unsafe`.
- Increased Thor logcat buffers to 64 MiB with
  `adb -s c3ca0370 logcat -G 64M`, then reran the same capture.
- Corrected capture prefix: `scratch/thor-debug/20260526-025401-*`.
  Capture commit `be55b0009`; APK SHA256
  `32AB811EF1A77BF57EE93F0E079A5B73FD2FAC15CFD3239A47EA91CA2D315AEC`.
  Screenshot again reached visible opening sky/dragon-wing and the focused
  fatal-marker search found no Xenia/AndroidRuntime/tombstone/assert/
  backtrace/fatal exception/SIGSEGV/SIGABRT/crash/ANR rows.
- Final perf stayed CPU/JIT-heavy: Main Thread `103%`, XMA Decoder `7.6%`,
  GPU Commands `3.8%`, Draw Thread `0.0%`. Final delayed body-time rows were
  `82281D28=62542369`, `82282490=28704115`, and `82287788=3280726`.
- Join audit on `scratch/thor-debug/20260526-025401-speed-logcat.txt` now
  reports safe metadata-backed mapping: `metadata_rows=88`,
  `metadata_mappable_rows=88`, `hir_block_metadata_rows=88`,
  `hir_block_mappable_rows=88`, `active_metadata_unmappable_rows=0`, and
  `join_status=metadata_required`.
- Top hot row is now mapped:
  `82281D28:block20 guest=8228233C total=44572243 delta=16609748 entries=23493 tpe=707`,
  source/guest span `8228233C-82282370`, label `8228233C`, `hir_instrs=34`.
- Hot block detail shows scalar call setup, not vector/GPU work:
  `context_loads=5`, `context_stores=13`, `memory_loads=6`,
  `memory_stores=1`, `calls=2`, `context_barriers=2`, `ppc_ops=ld:6,addi:3,bl:2,or:2,stw:1`,
  and call targets `bl->0x826BF770` plus recursive `bl->0x82281D28`.
- Research note:
  `docs/research/20260526-030200-82281d28-block-map-capture.md`.

Next: do not rerun the unchanged HIR mapper capture. Use the mapped
`82281D28:8228233C-82282370` block for an offline/source call-setup audit:
classify the `r3-r10`/`lr` context stores around `0x826BF770` and recursive
`0x82281D28`, compare against existing stack-sync and memcpy fastpaths, and
only patch behavior if the audit proves a reusable helper ABI/direct-call or
stack-argument rule.

## 82281D28 Call Setup Audit

- Continued from clean `master` at `928bc09a6`; no full-speed proof marker was
  present, Thor was connected, the emulator was not running, and no active
  build/deploy/capture or real git operation was blocking the slice.
- Stayed offline as requested by the reset plan; no new Thor capture was run.
- Re-read the mapped block with
  `tools/thor/thor_hir_block_detail_report.ps1` and confirmed
  `82281D28:8228233C-82282370` is scalar call setup:
  `context_loads=5`, `context_stores=13`, `memory_loads=6`,
  `memory_stores=1`, `calls=2`, `context_barriers=2`, with call targets
  `0x826BF770` and recursive `0x82281D28`.
- Ran call-boundary audits:
  - `8228233C-8228234C -> 826BF770` with the legacy `826BF770` OptHIR log:
    `parent_pre_call_store_context=4`, `classification_counts=callee_live_in:4`.
    Stores to `r3`, `r4`, `r5`, and `lr` are all live under the current ABI.
  - `82282350-82282370 -> 82281D28` on the current mapped log:
    `parent_pre_call_store_context=9`, `classification_counts=callee_live_in:9`.
    Stores to `r3-r10` and `lr` are all live into the recursive callee.
- Ran `thor_hir_callee_local_promotion_audit.ps1` for `r3-r10/lr`; every target
  had `strict_redundant_loads=0`, `strict_load_after_store=0`, and `risk=high`.
- Source audit of `A64Emitter::Call`, `A64Function::CallImpl`, and
  `TryEmitBlueDragonMemcpyFunctionBody` shows direct guest calls pass only the
  return address in `x0`; guest arguments are still transferred through
  `PPCContext`. The memcpy body fastpath still reads `r3/r4/r5` from
  `PPCContext`, so it does not remove caller-side setup stores.
- Research note:
  `docs/research/20260526-032000-82281d28-call-setup-audit.md`.

Decision: close local `82281D28:8228233C-82282370` store elision and another
unchanged call/setup capture. The reusable path is a generic, default-off
guest-call argument handoff audit that counts direct guest calls where parent
stores `r3-r10/lr` and callee immediately reloads them, including normal-entry
fallback, exception/debug visibility, indirection, tail-call, and stackpoint
constraints.

## Guest-Call Argument Handoff Audit Tool

- Continued from clean `master` at `ac823dab8`; no full-speed proof marker was
  present, Thor `c3ca0370` was connected, the Xenia app was not running, and no
  active Thor build/deploy/capture or real git operation was blocking the
  slice. An unrelated PS3/Eternal Sonata Windows test process was running
  outside this Thor/Xenia lane and was not treated as a blocker.
- Added `tools/thor/thor_hir_guest_call_arg_handoff_audit.ps1`, a
  behavior-neutral log-backed audit for direct PPC guest calls. It parses HIR
  dumps and block body-time rows, finds parent `r3-r10/lr` stores before `bl`
  calls, classifies callee first-use reloads when callee HIR is available, and
  reports missing-callee coverage plus normal-entry fallback pressure.
- Validation:
  - PowerShell parser validation for
    `tools/thor/thor_hir_guest_call_arg_handoff_audit.ps1` passed.
  - Ran the tool on `scratch/thor-debug/20260526-025401-speed-logcat.txt` with
    extra callee HIR from `scratch/thor-debug/20260519-213553-live-logcat.txt`.
- Audit result for `82281D28`:
  `direct_ppc_bl_calls=81`, `calls_with_argument_stores=81`,
  `argument_store_fields=439`, `argument_store_bytes=3512`,
  `callee_first_load_stores=62`, `callee_first_load_bytes=496`,
  `callee_hir_missing_stores=364`, and
  `normal_entry_fallback_required=81`.
- The two known body-weighted hot calls stay ABI-live:
  `82282370 -> 0x82281D28` has nine setup stores and all are
  `callee_first_load`; `8228234C -> 0x826BF770` has four setup stores and all
  are `callee_first_load`.
- Decision: the generic audit surface exists, but current HIR coverage is not
  enough for a guest-call fast-entry behavior patch. Most candidate stores are
  blocked by missing callee HIR. Next loop should collect broader route HIR or
  file-backed HIR for missing direct-call-heavy callees, rerun this audit, and
  only design a fast-entry variant if body-weighted callee-first-load volume is
  broad and normal-entry fallback constraints are explicit.
- Research note:
  `docs/research/20260526-033000-guest-call-arg-handoff-audit.md`.

## Guest-Call HIR Coverage Capture

- Continued from clean `master` at `682043bb0`; no full-speed proof marker was
  present, Thor `c3ca0370` was connected, Xenia was not running, and no active
  Thor build/deploy/capture or real git operation was blocking the slice.
  Read-only git status probes were ignored. The unrelated PS3/Eternal Sonata
  Windows process stayed outside this Thor/Xenia lane.
- Checked old logs for the missing direct-call-heavy callees from the first
  guest-call handoff audit. Existing logs only covered `82287788` and
  `8227FEE8`, reducing missing stores from `364` to `326`, so a targeted Thor
  route capture was justified.
- Raised Thor logcat buffers to 64 MiB and ran a 180-second Blue Dragon route
  capture with targeted disassembly filter:
  `82281D28,826BF770,82168610,822750F8,82274DB0,82275C08,82277100,82286BC0,82287788,82198088,822753E0,8227FEE8,8228AC30,8228AEF0`.
- Capture prefix: `scratch/thor-debug/20260526-034119-*`.
  Commit `682043bb0`; APK SHA256
  `32AB811EF1A77BF57EE93F0E079A5B73FD2FAC15CFD3239A47EA91CA2D315AEC`.
  Screenshot reached the visible opening sky / dragon-wing frame, and focused
  fatal-marker search found no Xenia/AndroidRuntime/tombstone/assert/backtrace/
  SIGSEGV/SIGABRT/crash/ANR rows.
- Final perf remained CPU/JIT-heavy: Main Thread `92.3%`, XMA Decoder
  `42.3%`, GPU Commands `7.6%`. Final delayed body-time rows were
  `82282490=33831839`, `82281D28=7930867`, and `82287788=3525919`.
- Fixed `tools/thor/thor_hir_guest_call_arg_handoff_audit.ps1` so the primary
  log supplies caller functions and block body-time while `-ExtraLogPath`
  supplies callee HIR only. Parser validation passed.
- Reran the audit with `scratch/thor-debug/20260526-025401-speed-logcat.txt`
  as body-time primary and `scratch/thor-debug/20260526-034119-speed-logcat.txt`
  as callee-HIR extra. Corrected result:
  `direct_ppc_bl_calls=81`, `argument_store_fields=439`,
  `callee_first_load_stores=247`, `callee_hir_missing_stores=126`,
  `callee_first_store=51`, `callee_absent=15`, and
  `normal_entry_fallback_required=81`.
- Decision: the guest-call argument lane has material ABI round-trip traffic,
  especially in the body-weighted top call boundaries, but it is not a local
  store-elision patch. Next useful slice is a source-level fast-entry
  feasibility audit for A64 direct guest calls and normal-entry fallback before
  any runtime behavior skeleton.
- Research note:
  `docs/research/20260526-034500-guest-call-hir-coverage-capture.md`.

## A64 Guest-Call Fast-Entry Feasibility Audit

- Continued from clean `master` with no full-speed proof marker, Thor
  `c3ca0370` connected, Xenia not running, and no active Thor build/deploy/
  capture or real git operation. Read-only git status probes and the unrelated
  PS3/Eternal Sonata Windows process were not blockers.
- Added `tools/thor/thor_a64_guest_call_fast_entry_feasibility.ps1`, a
  deterministic source audit for the direct guest-call fast-entry lane.
- Validation:
  - PowerShell parser validation passed for the new tool.
  - Ran
    `powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_a64_guest_call_fast_entry_feasibility.ps1`.
- Source findings:
  - normal guest entry is a singleton `A64Function::machine_code()` plus one
    guest-address indirection slot;
  - host-to-guest and direct guest-to-guest calls pass only the guest PPC
    return address in `x0`;
  - tail, indirect, unresolved, and extern calls require normal fallback;
  - stackpoint/longjmp recovery, debug/trap visibility, exception mapping, and
    guest-to-host callback save/restore make hidden payload state unsafe unless
    explicit flush points exist;
  - no generic fast-entry skeleton exists yet.
- Decision: direct guest-call fast-entry is feasible only as a separate guarded
  entry path or stub. Do not replace `A64Function::machine_code()` or the
  global indirection slot, and do not patch runtime behavior yet.
- Research note:
  `docs/research/20260526-040500-a64-guest-call-fast-entry-feasibility.md`.

Next: add a default-off counter-only
`arm64_guest_call_fast_entry_audit` skeleton that changes no generated
behavior and counts eligible direct calls, fallback blockers, callee first-use
coverage, normal-entry fallback pressure, estimated avoidable context
store/load traffic, and required dirty flush points. No quiet speed A/B until
that audit proves route-wide volume with manageable flush pressure.
