# 2026-05-27 Worklog

## Blue Dragon Route-Engine Sampler Capture

- Continued from the route-engine reset on branch
  `blue-dragon-route-engine-reset`, with no sustained 30 FPS proof marker and
  no active build/deploy/capture/git operation.
- Ran FullDeploy:
  `powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`.
- Ran a 180-second quiet Blue Dragon route with
  `-Arm64OffsetMemoryAddressFastpath true`,
  `-Arm64SpeedProfileBodyTimeFilter "82282490,82281D28,82287788"`,
  `-Arm64SpeedProfileBodyTimeAfterMs 120000`, and
  `-FrameCpuSampler true`.
- Capture prefix: `scratch/thor-debug/20260527-105906-*`.
  APK SHA256:
  `5320F722699CE8B9659BC3A942E5E892CCD01622AE2233F4C1E4D86CB7E3158B`.
- Screenshot was manually checked and reached the visible opening sky /
  dragon-wing route.
- Updated `tools/thor/thor_blue_dragon_speed_capture_summary.ps1` so known
  non-Xenia fatal-marker noise is excluded from the focused fatal count while
  raw/excluded counts are still reported.
- Updated `tools/thor/thor_blue_dragon_route_engine_reset_report.ps1` so the
  route report joins `*-frame-cpu-sampler.txt`, emits per-thread/core sampler
  rows, handles CRLF raw sampler text, and changes the post-sampler decision to
  FrameTimeline/present attribution before kernel/HLE churn behavior work.
- Proof summary:
  `fatal_marker_count=0`, `fatal_marker_raw_count=3`,
  `fatal_marker_excluded_count=3`, `fps_marker_count=0`,
  `sustained_30fps_proof=false`, `proof_reason=no_fps_markers`,
  `choreographer_skipped_max_frames=117`.
- Final perf remained CPU/JIT-heavy: Main Thread `96.1%`, GPU Commands
  `11.5%`, XMA Decoder `50.0%`, Draw Thread `3.8%`.
- Final body-time rows:
  `82282490=32940122`, `82281D28=8122412`, `82287788=3528896`.
- Route report:
  `kernel_hle_churn_share=0.0546`, `tiny_helper_share=0.1592`,
  `route_engine_gap=missing_frametimeline_present_attribution`,
  `decision=add_frametimeline_present_attribution_then_kernel_hle_churn_audit`.
- Sampler result: Main Thread dominated CPU jiffies and was sampled mostly on
  core 7 (`49/59` samples), so Android core placement is not the first suspect.
  `gfxinfo` only reported three rendered frames, so it is not enough to
  explain Vulkan SurfaceView frame pacing.
- Research note:
  `docs/research/20260527-110500-blue-dragon-route-engine-sampler-capture.md`.
- Validation: PowerShell parser checks passed for both updated report scripts;
  `git diff --check` passed with CRLF replacement warnings only; the updated
  route report emits Main Thread sampler rows and the FrameTimeline/present
  attribution decision.

Next: add a route-clean FrameTimeline / SurfaceFlinger present-attribution
capture path, then audit kernel/HLE churn only if the frame/present trace shows
the lock/IRQL helpers align with missed frames. Do not rerun this same quiet
capture or reopen closed A64 micro-lanes from this evidence.

## Surface Latency Capture Path

- Added automatic SurfaceFlinger latency capture to
  `tools/thor/thor_android_frame_cpu_sampler.ps1`.
- The sampler now defaults `-AutoSurfaceLayer true`, prefers the actual
  `SurfaceView[...](BLAST)#...` layer, clears latency at capture start, records
  raw `dumpsys SurfaceFlinger --latency` rows at capture end, and summarizes
  valid frames, interval count, span, average/max interval, and intervals over
  33 ms / 50 ms.
- Updated `tools/thor/thor_xenia_debug.ps1` to pass
  `-FrameCpuSamplerAutoSurfaceLayer` through `LaunchBlueDragonSpeedCapture` and
  record it in metadata.
- Updated `tools/thor/thor_blue_dragon_route_engine_reset_report.ps1` to parse
  surface latency rows and move the next decision to
  `join_surface_latency_with_kernel_hle_churn` only after valid present
  intervals exist.
- Updated current planning surfaces so the next loop targets present
  attribution instead of repeating the completed route sampler capture:
  `AGENTS.md`, repo-local Blue Dragon/A64/goal-loop/refiner skills, and
  `tools/thor/thor_codex_goal_loop.ps1`.
- Validation: parser checks passed for the three changed PowerShell scripts;
  the route report still parses `scratch/thor-debug/20260527-105906-*` and now
  reports
  `decision=capture_surface_latency_or_frametimeline_then_kernel_hle_churn_audit`;
  sampler smoke against a stopped app exits cleanly with
  `decision=process_not_running`.
- Research note:
  `docs/research/20260527-123500-blue-dragon-surface-latency-capture-path.md`.

Next: FullDeploy/current-branch route capture with
`-FrameCpuSampler true -FrameCpuSamplerAutoSurfaceLayer true`, then rerun the
proof summary and route-engine report. Treat it as present-attribution evidence,
not a speed A/B. The repo-local plan and goal-loop defaults were advanced to
that runtime slice after the tooling commit.

## Surface Latency Route Capture

- Ran FullDeploy on branch `blue-dragon-route-engine-reset`, then ran a
  180-second quiet Blue Dragon route with
  `-Arm64OffsetMemoryAddressFastpath true`,
  `-FrameCpuSampler true`, and
  `-FrameCpuSamplerAutoSurfaceLayer true`.
- Capture prefix: `scratch/thor-debug/20260527-125840-*`.
  Capture commit: `e5425d408`. APK SHA256:
  `5320F722699CE8B9659BC3A942E5E892CCD01622AE2233F4C1E4D86CB7E3158B`.
- Screenshot was manually checked and reached the visible opening sky /
  dragon-wing route.
- Proof summary remains negative:
  `fatal_marker_count=0`, `fatal_marker_raw_count=2`,
  `fatal_marker_excluded_count=2`, `fps_marker_count=0`,
  `sustained_30fps_proof=false`, `proof_reason=no_fps_markers`,
  `choreographer_skipped_max_frames=149`.
- Final perf stayed CPU/JIT-heavy: Main Thread `96.1%`, GPU Commands `11.5%`,
  Draw Thread `3.8%`, XMA Decoder `46.1%`.
- Final body rows:
  `82282490=31126351`, `82281D28=8135606`, `82287788=3507105`.
- Fixed SurfaceFlinger latency parsing after the first report included a
  sentinel row with `9223372036854775807` actual/ready timestamps. The sampler
  and route report now reject sentinel rows, trim layer names, use non-grouped
  fixed-point millisecond formatting, and the route report recomputes latency
  from raw `surface_latency=` rows so stale summaries do not poison evidence.
- Corrected route report:
  `scratch/thor-debug/20260527-125840-route-engine-reset-report.txt`.
  Surface latency is now valid for the game SurfaceView layer:
  `valid_frames=126`, `interval_count=125`, `span_ms=55149.561`,
  `interval_avg_ms=441.196`, `interval_max_ms=2361.047`,
  `intervals_over_33ms=125`, `intervals_over_50ms=125`,
  `rejected_rows=1`.
- Route report decision:
  `kernel_hle_churn_share=0.0547`, `tiny_helper_share=0.1594`,
  `route_engine_gap=missing_frametimeline_jank_source_attribution`,
  `decision=join_surface_latency_with_kernel_hle_churn`.
- Research note:
  `docs/research/20260527-131500-blue-dragon-surface-latency-route-capture.md`.
- Validation: PowerShell parser checks passed for both changed scripts; the
  corrected route report reran successfully on the new route capture.

Next: do not rerun the same capture or patch kernel/HLE from aggregate
evidence. Add a time-aligned present/jank attribution join, or a Perfetto /
FrameTimeline path if SurfaceFlinger latency cannot identify whether long
present gaps align with kernel/HLE helpers, XMA, A64 wall bursts, or Vulkan
frame pacing.

## Present/Jank Attribution Instrumentation

- Confirmed the worktree was clean on `blue-dragon-route-engine-reset`, no
  full-speed proof marker existed, and no active build/deploy/capture/git slice
  was running.
- Inspected the `scratch/thor-debug/20260527-125840-*` sampler and speed logs.
  The existing capture has SurfaceFlinger actual-present timestamps in device
  monotonic nanoseconds, but sampler rows only had host wall time and elapsed
  stopwatch milliseconds. That blocks a rigorous time join.
- Updated `tools/thor/thor_android_frame_cpu_sampler.ps1` so future captures
  write `start_device_monotonic_ns`, per-sample `device_monotonic_ns`, and
  `surface_latency_capture_device_monotonic_ns` using `/proc/uptime`.
- Added `tools/thor/thor_blue_dragon_present_jank_attribution.ps1`. It parses
  raw SurfaceFlinger latency, sampler thread deltas, and A64 speed-profile
  buckets. On captures with monotonic samples it will bucket long present
  intervals against Main Thread, XMA Decoder, GPU Commands, Draw Thread,
  guest XThreads, kernel/HLE deltas, and wall-cluster deltas. On old captures
  it fails closed instead of making an approximate claim.
- Ran the attribution report on the old capture:
  `scratch/thor-debug/20260527-125840-present-jank-attribution.txt`.
  Result: `sample_count=59`, `device_monotonic_sample_count=0`,
  `surface_latency_valid_frames=126`, `surface_latency_interval_count=125`,
  `long_present_interval_count=125`,
  `attribution_status=missing_device_monotonic_samples`,
  `decision=rerun_capture_with_device_monotonic_sampler`.
- Sampler smoke against a stopped app wrote
  `start_device_monotonic_ns=1622120270000000` and exited with
  `decision=process_not_running`.
- Research note:
  `docs/research/20260527-132500-blue-dragon-present-jank-attribution.md`.
- Validation: PowerShell parser checks passed for the updated sampler and new
  attribution tool.

Next: FullDeploy current branch and run a new quiet 180-second route capture
with the timestamped sampler, then run the proof summary, route-engine report,
and new present/jank attribution report. Do not patch kernel/HLE, Vulkan, or
A64 behavior until the attribution report identifies which subsystem aligns
with the long present gaps.

## Android Game Launch and Controller Mapping Pass

- Reproduced user-facing launch/crash issues across several non-Blue Dragon
  titles on Thor:
  - Wallace & Gromit helper launch failed before app launch due apostrophe path
    quoting; not a game compatibility result.
  - Back to the Future aborted on `NtQueryVolumeInformationFile` /
    `XFileFsDeviceInformation`.
  - Banjo-Kazooie: Nuts & Bolts aborted on an XAudio render-driver pointer
    assertion and repeatedly queried `XFileXctdCompressionInformation`.
  - Burnout Revenge stayed alive for 60s but remained in loading/black-screen
    state with no focused fatal marker.
  - Project Sylpheed stayed alive but showed repeated physical heap
    release-from-non-region-start failures.
- Fixed the app-launch path mismatch by resolving external-storage document
  URIs to raw `/storage/<volume>/<path>` targets when possible and corrected the
  Thor SD-card launcher default to `Roms/xbox360`.
- Fixed Android mapped-memory failure detection by checking `MAP_FAILED`.
- Changed the two observed assert-abort compatibility classes to conservative
  runtime behavior: `XFileFsDeviceInformation` /
  `XFileXctdCompressionInformation` return minimal payloads, unknown file-info
  classes return `X_STATUS_INVALID_INFO_CLASS`, and unexpected XAudio
  render-driver pointers return `X_ERROR_INVALID_PARAMETER`.
- Added a visual controller remapping path under Settings:
  `View / remap controller`. It draws a tappable Xbox controller, shows current
  physical Thor button mappings, tracks last input, stores button remaps in
  preferences, swaps displaced button bindings, remaps Android key events before
  native HID, and supports reset.
- On-device verification:
  - FullDeploy succeeded on Thor.
  - `git diff --check` passed with existing CRLF warnings only.
  - Visual UI proof screenshots are in
    `scratch/thor-debug/20260527-controller-ui/`.
  - Remap smoke selected Xbox `A`, sent Android `BUTTON_B`, saw `A mapped to B`,
    then reset mappings to defaults.
- Research note:
  `docs/research/20260527-144000-android-game-launch-crash-and-controller-mapping.md`.

Next: use the app picker path for future title tests so results reflect the
actual user flow. Prioritize remaining crashes by hard-abort first, then
black-screen/loading stalls with clean fatal searches.

## Windows PowerShell Command Hygiene Guardrail

- Added repo-local skill
  `.agents/skills/xenia-windows-powershell-command-hygiene/SKILL.md` plus
  `agents/openai.yaml`.
- Root cause: a Windows PowerShell status probe used Bash-style `&&` command
  chaining, which PowerShell 5.1 rejects. This failure class can also affect
  quoting, paths with spaces, redirection, and mixed-shell file operations.
- Updated `AGENTS.md`, `xenia-working-fast-autonomy`,
  `xenia-blue-dragon-full-speed-autopilot`, `xenia-codex-goal-loop`, and
  `tools/thor/thor_codex_goal_loop.ps1` so future goal-loop/heartbeat work
  reads the command-hygiene skill before command-heavy slices.
- The rule is now explicit: assume Windows PowerShell 5.1, do not use Bash
  `&&`/`||`/here-docs/`export`/`rm -rf` patterns, prefer one command per tool
  call, use `multi_tool_use.parallel` for independent reads, and use
  `$LASTEXITCODE` checks for dependent multi-step PowerShell commands.
- Research note:
  `docs/research/20260527-150500-windows-powershell-command-hygiene-skill.md`.
- Validation: `git diff --check` passed with normal CRLF warnings only;
  `tools/thor/thor_codex_goal_loop.ps1 -Mode Status` parsed successfully; then
  `-Mode EnableBlueDragonFullSpeed -MaxAttempts 48` re-armed the live
  `scratch/thor-debug/codex-goal-loop.json` prompt with the new PowerShell
  hygiene skill in the startup read list.

Next: future command-heavy continuations should read the PowerShell hygiene
skill before shell composition and avoid Bash-style command chaining.

## Android In-Game Menu and Overlay Cleanup

- Investigated the user's report that game content still had UI covering it.
  Screenshot `scratch/thor-debug/20260527-user-screen/current-screen.png`
  showed Project Sylpheed running under Xenia, with the app's own launch OSD top
  card and donor-fork bottom badge covering the game.
- Changed normal Android launches to hide the launch OSD by default, migrated
  existing non-research preferences to the new default, and kept the Research
  profile able to show the launch OSD.
- Rebranded APK-visible strings to Xenia Thor and removed donor-fork wording
  from current Android/runtime memory messages.
- Added immersive sticky fullscreen to `WindowedAppActivity`; the final clean
  screenshot no longer shows Android status/nav bars or the launch OSD:
  `scratch/thor-debug/20260527-user-screen/clean-game-view.png`.
- Reproduced the Back complaint. The pre-fix log did not show a Java/native
  fatal crash; Back finished the emulator activity and returned to launcher,
  which looked like a crash to the user.
- Added an in-game Xenia Thor menu in `EmulatorActivity` inspired by the Cemu
  Thor experiment's OSD/controller-help pattern:
  - Back now toggles the menu instead of finishing the emulator activity.
  - Menu buttons open Resume, Controller map, and Settings.
  - Controller Help shows current physical-to-XInput bindings and the last
    observed input.
- Added a top-left FPS badge plus persistent Settings and in-game OSD controls:
  `Show FPS overlay` in Settings and `Show FPS` in the in-game menu. The first
  implementation uses Android `Choreographer` display callbacks; it is useful
  visual feedback but not exact guest `VdSwap` FPS yet.
- Added `KEYCODE_MENU` as a bindable Xbox Back/Select fallback because the Thor
  Menu button should act as Select.
- On-device verification:
  - `ApkShell` and FullDeploy passed on Thor.
  - Direct Project Sylpheed launch reached visible game content.
  - Synthetic `KEYCODE_BUTTON_START` logged
    `KEYCODE_BUTTON_START->KEYCODE_BUTTON_START` and reached native Android HID.
  - Synthetic `KEYCODE_MENU` logged `KEYCODE_MENU->KEYCODE_BUTTON_SELECT` and
    reached native Android HID.
  - Back opened the in-game menu and the process stayed alive; screenshot:
    `scratch/thor-debug/20260527-user-screen/ingame-menu.png`.
  - FPS badge proof:
    `scratch/thor-debug/20260527-user-screen/choreographer-fps-top-left.png`.
  - Final FPS/menu/select proof:
    `scratch/thor-debug/20260527-user-screen/final-fps-menu-select-v2.png`.
- Research note:
  `docs/research/20260527-151500-android-ingame-menu-overlay-controller-start.md`.

Next: expand the in-game OSD into richer performance/graphics/audio/tools
sections only after this first usability layer proves comfortable on physical
Thor controls. A later native metric pass can replace the display-callback FPS
badge with exact guest-present/VdSwap FPS.

## Android OSD Exit To Menu

- Added an explicit `Exit to menu` button to the in-game OSD so Back can remain
  a pause/menu toggle while leaving the game is an intentional menu action.
- The exit action starts `LauncherActivity` with `NEW_TASK` / `CLEAR_TASK` and
  finishes `EmulatorActivity`, so direct emulator launches return to a fresh
  Xenia Thor menu instead of Android home or stale DocumentsUI picker state.
- Device testing showed direct launcher start was killed by native emulator
  teardown after `EmulatorActivity.finish()`. A deferred `AlarmManager`
  relaunch was blocked by Android background-activity rules, so the final
  implementation runs `LauncherActivity` in a separate `:launcher` process and
  starts it directly before finishing the game.
- Research note:
  `docs/research/20260527-152100-android-osd-exit-to-menu.md`.
- Validation:
  - `ApkShell` and FullDeploy passed on Thor.
  - OSD button proof:
    `scratch/thor-debug/20260527-user-screen/menu-exit-game-v5.png`.
  - Exit proof:
    `scratch/thor-debug/20260527-user-screen/after-exit-to-menu-v5.png`.
  - `dumpsys activity` confirmed `LauncherActivity` resumed after tapping
    `Exit to menu`.
  - Focused fatal-marker search was clean in
    `scratch/thor-debug/20260527-user-screen/exit-to-menu-v5-logcat.txt`.

## Android App Picker and Quick Launch Proof

- Continued the Android usability and compatibility sprint rather than the old
  Blue Dragon speed loop.
- Found `scratch/thor-debug/codex-goal-loop.json` still pointed at the stale
  Blue Dragon full-speed prompt. Added `EnableAndroidUsability` to
  `tools/thor/thor_codex_goal_loop.ps1` and re-armed the scratch loop as
  `android-usability-compat`.
- Added a `Launch Last Game` card to the Android launcher:
  - Disabled until a game is picked once through Android DocumentsUI.
  - Persists the selected game URI and display name in
    `xenia_android_settings`.
  - Relaunches the saved game without reopening the picker.
- Fixed two controller/landscape usability issues found during follow-up:
  - The launcher is now scrollable in landscape, so D-pad navigation can reach
    Settings and Window Demo instead of leaving lower rows clipped.
  - Launcher and OSD focused controls can be activated with controller `A`,
    `Start`, D-pad center, or Enter.
- Added native Android HID `GetKeystroke` support. Android key events already
  updated XInput state, but `GetKeystroke` previously always returned empty;
  titles that look for Start through `XamInputGetKeystroke` could ignore Start
  even when the state path was alive.
- Validation:
  - `tools/thor/thor_build.ps1 -Mode ApkShell` passed.
  - `tools/thor/thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`
    passed.
  - Actual app-picker flow opened DocumentsUI at `disk > Roms > xbox360`.
  - Selected `Burnout Revenge (USA).iso` from DocumentsUI, not by direct ADB
    launch.
  - Burnout reached a visible loading screen with the FPS overlay:
    `scratch/thor-debug/20260527-app-picker-proof/burnout-after-picker-launch.png`.
  - Back opened the in-game OSD and `Exit to menu` returned to
    `LauncherActivity`.
  - The launcher then showed `Launch Last Game` with
    `Burnout Revenge (USA).iso`; tapping it relaunched Burnout without
    reopening DocumentsUI.
  - D-pad Down scrolled launcher focus to the lower `Window Demo` row:
    `scratch/thor-debug/20260527-start-ui-proof/launcher-focus-bottom.png`.
  - Synthetic `KEYCODE_BUTTON_START` reached Java as
    `KEYCODE_BUTTON_START->KEYCODE_BUTTON_START` and native Android HID as key
    down/up in
    `scratch/thor-debug/20260527-start-ui-proof/start-button-logcat.txt`.
  - Focused fatal-marker search returned `focused_fatal_marker_count=0`.
  - Compatibility marker search found two non-fatal
    `XAudioGetVoiceCategoryVolumeChangeMask` rows, so this is still a
    game-compatibility/stall class rather than a launcher crash.
- Research note:
  `docs/research/20260527-160500-android-app-picker-quick-launch-proof.md`.

Next: keep using the actual app picker path for title tests. A useful next
frontend slice is a recent-games list with per-title last status/crash notes so
the launcher can explain "launched but stuck" versus "crashed with known
reason" directly in the app.

## Project Sylpheed Crash And OSD Fit

- Continued the Android usability and compatibility sprint after the user asked
  to try Project Sylpheed and reported that the game view still had app UI
  covering it.
- Found the SD-card target:
  `/storage/2664-21DE/roms/xbox360/Project Sylpheed - Arc of Deception (USA, Europe) (En,Ja).iso`.
- Direct launch reached Xenia runtime and loaded `game:\default.xex`, but the
  title hit Xenia's guest crash dialog rather than an Android process crash.
- Capture:
  `scratch/thor-debug/20260527-161538-*`.
- Crash markers:
  - `BaseHeap::Release failed because address is not a region start`
  - `PhysicalHeap::Release failed due to parent heap failure`
  - `BaseHeap::Alloc page count too big for requested range`
  - `PhysicalHeap::Alloc unable to alloc physical memory in parent heap`
  - `RtlRaiseException(702DF8D0(E06D7363))`
  - Crash PC `0x8245BDEC`, function `0x8245BD80-0x8245BE64`
- The screenshot proved the visible fit problem was app overlay, not Android
  system bars: the SurfaceView was fullscreen, but the large title/path card
  and bottom `Xenia Thor Android build` badge covered the game view.
- Patched live gameplay overlay behavior so the title/path card and bottom
  build badge stay hidden during gameplay; FPS remains the only live default
  overlay and the fuller controls stay in the OSD menu.
- Changed generic `thor_xenia_debug.ps1` direct launches to hide Android OSD by
  default, matching the user-facing app behavior.
- Validation:
  - `tools/thor/thor_build.ps1 -Mode ApkShell` passed.
  - `tools/thor/thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`
    passed.
  - Post-patch capture `scratch/thor-debug/20260527-161935-*` uses
    `hide_android_osd=true` and shows only the top-left FPS badge over the
    guest crash dialog; the title/path card and bottom build badge are gone.
  - The same Project Sylpheed crash reproduced, so this title remains a
    physical-heap / guest-exception compatibility issue rather than a launcher
    or Android UI fit issue.
- Research note:
  `docs/research/20260527-162500-project-sylpheed-crash-osd-fit.md`.

## Android Internal Resolution Setting

- Added a real Android `Internal resolution` setting with 480p, 720p, and
  1080p choices.
- Wired the setting through `XeniaAndroidSettings` launch arguments as
  `kernel_display_resolution`; direct `EmulatorActivity` launches can also pass
  that cvar.
- Added `kernel_display_resolution` to the kernel video-mode stub so
  `VdQueryVideoMode` can report:
  - 480p as 720x480.
  - 720p as 1280x720.
  - 1080p as 1920x1080.
- Kept Vulkan draw-resolution scaling separate because Android Vulkan still
  hardcodes the render-target/texture-cache scale to 1x.
- Device proof:
  - Settings picker:
    `scratch/thor-debug/20260527-resolution-setting-proof/settings-resolution-scrolled.png`.
  - Project Sylpheed 480p sanity:
    `scratch/thor-debug/20260527-project-sylpheed-480p-sanity/project-sylpheed-480p.png`.
- The Project Sylpheed 480p sanity launch fixed the visible fit symptom for the
  Square Enix logo, which is now centered/full-width instead of stuck as a
  lower-right quarter image.
- Remaining compatibility blocker is unchanged: Project Sylpheed still logs
  `BaseHeap::Release failed because address is not a region start`,
  `PhysicalHeap::Release failed due to parent heap failure`, and
  `RtlRaiseException`.
- Validation:
  - `git diff --check` passed with expected CRLF warnings.
  - `tools/thor/thor_build.ps1 -Mode NativeCore` passed.
  - `tools/thor/thor_build.ps1 -Mode ApkShell` passed.
  - `tools/thor/thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`
    passed.
- Research note:
  `docs/research/20260527-164500-android-internal-resolution-and-sylpheed-480p.md`.

## Android Launch Status UI

- Continued the Android usability and compatibility sprint rather than the old
  Blue Dragon speed loop.
- Added launcher/emulator shared last-run state to `XeniaAndroidSettings`:
  title, target, state, start time, and finish time.
- `LauncherActivity` now records game launches as `running` before starting the
  emulator, and `EmulatorActivity` records direct launches with a target too.
- `Exit to menu` now records `exited_to_menu` before returning to the separate
  launcher process.
- Added a compact launcher status line under `Launch Last Game` so the user can
  immediately distinguish:
  - a previous launch still marked running:
    `Last run: may have crashed or closed - <title>`.
  - an intentional OSD exit:
    `Last run: exited to menu - <title>`.
- Device proof:
  - `scratch/thor-debug/20260527-launch-status-proof/last-run-maybe-crashed.png`.
  - `scratch/thor-debug/20260527-launch-status-proof/last-run-exited-to-menu.png`.
  - `scratch/thor-debug/20260527-launch-status-proof/menu.xml`.
- Validation:
  - `git diff --check` passed with expected CRLF warnings only.
  - `tools/thor/thor_build.ps1 -Mode ApkShell` passed.
  - `tools/thor/thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`
    passed.
- Research note:
  `docs/research/20260527-170000-android-launch-status-ui.md`.

Next: expand this into a recent-games/status list with per-title status/crash
classes. Project Sylpheed still needs a physical-heap / guest-exception
compatibility audit, and Burnout remains a loading/stall class.

## Android Recent Games Status List

- Continued the Android usability and compatibility sprint.
- Added persisted recent-games JSON under `XeniaAndroidSettings`, capped to
  five entries.
- Each entry stores launch URI, display title, resolved target path, last state,
  start time, and finish time.
- `recordLaunchStarted` now updates both the last-run chip and the recent-game
  row as `running`.
- `recordLaunchExitedToMenu` updates the last-run chip and matching recent-game
  row as `exited_to_menu`.
- Added a launcher `Recent games` section under the last-run chip. Rows are
  focusable/clickable and relaunch the stored URI/path.
- Device proof:
  - `scratch/thor-debug/20260527-recent-games-proof/recent-games-status.png`.
  - `scratch/thor-debug/20260527-recent-games-proof/recent-games-status-scrolled.png`.
  - `scratch/thor-debug/20260527-recent-games-proof/recent-games-status.xml`.
- Proof result:
  - Project Sylpheed row shows `Exited to menu`.
  - Burnout Revenge row shows `May have crashed/closed`.
- Validation:
  - `tools/thor/thor_build.ps1 -Mode ApkShell` passed.
  - `tools/thor/thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`
    passed.
- Research note:
  `docs/research/20260527-171500-android-recent-games-status-list.md`.

Next: feed richer crash/stall classifications into the recent-game rows from
host-side captures, or run the focused Project Sylpheed physical-heap /
`RtlRaiseException` compatibility audit.

## Context Compaction Reset

- Time: 2026-05-27T17:57:51-04:00.
- Reduced prompt/context churn that was causing frequent thread compaction.
- Replaced the oversized `AGENTS.md` chronology with concise current guidance.
- Shortened `scratch/thor-debug/codex-goal-loop.json`.
- Updated `docs/research/20260527-164500-android-internal-resolution-and-sylpheed-480p.md`
  to mark the Square Enix logo as movie evidence, not title/menu proof.
- Added `docs/research/20260527-175800-context-compaction-reset.md`.
- Next: keep heartbeat prompts short and focus Project Sylpheed on the actual
  title/menu geometry bug plus the guest heap/RtlRaiseException crash class.

## Project Sylpheed Title Geometry Fix

- Fixed the title/menu lower-right quadrant geometry path.
- Corrected VD packed width/height decoding, removed Project Sylpheed-specific
  Android 480p/scaler hardcodes, kept Vulkan swap output at source size, and
  forced Android Vulkan QuadList draws through the existing CPU triangle-list
  conversion instead of the Adreno geometry-shader path.
- Added Android app stable launch overrides so stale research cvars from
  `xenia.config.toml` do not poison normal launcher starts.
- Device/user proof:
  - Bad-title proof:
    `scratch/thor-debug/20260527-sylpheed-vd-scaler-output-fix/user-says-still-bad-now.png`.
  - Fullscreen presenter proof:
    `scratch/thor-debug/20260527-project-sylpheed-solid-presenter-probe/solid-probe.png`.
  - App-path fullscreen proof:
    `scratch/thor-debug/20260527-sylpheed-app-stable-launch/screenshot-30s.png`.
  - User confirmed: "title screen fixed".
- Validation:
  - `git diff --check` passed with expected CRLF warnings only.
  - `tools/thor/thor_build.ps1 -Mode NativeCore` passed.
  - `tools/thor/thor_build.ps1 -Mode ApkShell` passed.
  - `tools/thor/thor_xenia_debug.ps1 -DeviceSerial c3ca0370 -Mode Install`
    passed.
- Research note:
  `docs/research/20260527-184700-project-sylpheed-title-geometry-fix.md`.

Next: Project Sylpheed work should move to the guest heap/RtlRaiseException and
black-frame compatibility class, not title positioning.
