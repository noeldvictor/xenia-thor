# 2026-05-27 Worklog

## Blue Dragon Route-Engine Sampler Capture

- Continued from the route-engine reset on branch
  `blue-dragon-route-engine-reset`, with no sustained 30 FPS proof marker and
  no active build/deploy/capture/git operation.
- Ran FullDeploy:
  `powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`.
- Ran a 180-second quiet Blue Dragon route with
  `-Arm64OffsetMemoryAddressFastpath true`,
  `-Arm64SpeedProfileBodyTimeFilter "82282490,82281D28,82287788"`,
  `-Arm64SpeedProfileBodyTimeAfterMs 120000`, and
  `-FrameCpuSampler true`.
- Capture prefix: `scratch/thor-debug/20260527-105906-*`.
  APK SHA256:
  `5320F722699CE8B9659BC3A942E5E892CCD01622AE2233F4C1E4D86CB7E3158B`.
- Screenshot was manually checked and reached the visible opening sky /
  dragon-wing route.
- Updated `tools/thor/thor_blue_dragon_speed_capture_summary.ps1` so known
  non-Xenia fatal-marker noise is excluded from the focused fatal count while
  raw/excluded counts are still reported.
- Updated `tools/thor/thor_blue_dragon_route_engine_reset_report.ps1` so the
  route report joins `*-frame-cpu-sampler.txt`, emits per-thread/core sampler
  rows, handles CRLF raw sampler text, and changes the post-sampler decision to
  FrameTimeline/present attribution before kernel/HLE churn behavior work.
- Proof summary:
  `fatal_marker_count=0`, `fatal_marker_raw_count=3`,
  `fatal_marker_excluded_count=3`, `fps_marker_count=0`,
  `sustained_30fps_proof=false`, `proof_reason=no_fps_markers`,
  `choreographer_skipped_max_frames=117`.
- Final perf remained CPU/JIT-heavy: Main Thread `96.1%`, GPU Commands
  `11.5%`, XMA Decoder `50.0%`, Draw Thread `3.8%`.
- Final body-time rows:
  `82282490=32940122`, `82281D28=8122412`, `82287788=3528896`.
- Route report:
  `kernel_hle_churn_share=0.0546`, `tiny_helper_share=0.1592`,
  `route_engine_gap=missing_frametimeline_present_attribution`,
  `decision=add_frametimeline_present_attribution_then_kernel_hle_churn_audit`.
- Sampler result: Main Thread dominated CPU jiffies and was sampled mostly on
  core 7 (`49/59` samples), so Android core placement is not the first suspect.
  `gfxinfo` only reported three rendered frames, so it is not enough to
  explain Vulkan SurfaceView frame pacing.
- Research note:
  `docs/research/20260527-110500-blue-dragon-route-engine-sampler-capture.md`.
- Validation: PowerShell parser checks passed for both updated report scripts;
  `git diff --check` passed with CRLF replacement warnings only; the updated
  route report emits Main Thread sampler rows and the FrameTimeline/present
  attribution decision.

Next: add a route-clean FrameTimeline / SurfaceFlinger present-attribution
capture path, then audit kernel/HLE churn only if the frame/present trace shows
the lock/IRQL helpers align with missed frames. Do not rerun this same quiet
capture or reopen closed A64 micro-lanes from this evidence.
