# 2026-05-28

## Project Sylpheed Reenter Audit Skeleton

- Added default-off `xboxkrnl_reenter_audit` instrumentation for the Project
  Sylpheed New Game/default-difficulty native abort path.
- `KeSetCurrentStackPointers` now logs behavior-neutral reenter rows with
  current/target thread IDs, LR/CTR/r1/r13, stack pointer inputs, fiber/same
  thread state, APC disable count, and explicit `behavior_changed=0` markers.
- `XThread::Reenter` now logs the actual exception throw target when the audit
  is enabled; it still throws exactly as before.
- Added Android launch extras, stable-launch defaulting, and Thor PowerShell
  parameters for the audit cvars.
- Validation passed: Thor PowerShell parser check, `git diff --check` with
  existing CRLF conversion warnings only, `NativeCore`, and `ApkShell`.
- Research note:
  `docs/research/20260528-095500-project-sylpheed-reenter-audit-skeleton.md`.

Next: deploy and reproduce Project Sylpheed from the game library with
`-XboxkrnlReenterAudit true`, then use the logged rows to design a guarded
non-throw Android/A64 reenter path. Keep physical-free diagnostics separate.

## Project Sylpheed Reenter Repro Blocked By ADB

- Tried `tools/thor/thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`.
- Build completed successfully, but install failed after ADB reported the Thor
  transport closed and then `device 'c3ca0370' not found`.
- Restarted the ADB server; `adb devices` still returned no attached devices.
- Did not clear logcat, force-stop, or run the Sylpheed repro with the device
  absent.
- Research note:
  `docs/research/20260528-101500-project-sylpheed-reenter-repro-adb-blocker.md`.

Next: once Thor is visible in `adb devices`, deploy current branch and run the
Project Sylpheed game-library New Game repro with
`-XboxkrnlReenterAudit true -XboxkrnlReenterAuditBudget 128`.

## Project Sylpheed Android Reenter Longjmp

- ADB returned for `c3ca0370`; `NativeCore` and `FullDeploy` succeeded.
- Added Android-only `xboxkrnl_android_reenter_longjmp`, default-on for Android,
  so same-thread guest reenter returns through a jump state instead of throwing
  `reenter_exception` through A64 generated code.
- Kept the original exception path as fallback and left non-Android behavior
  unchanged.
- Added debug-only launcher override forwarding for reenter audit cvars, so the
  real launcher/library path can be tested without direct ISO launch.
- Reproduced Project Sylpheed through the launcher `Launch Last Game` row,
  reached title/menu/difficulty, selected New Game / Normal, and captured:
  `throws_exception 0`, `longjmp_reenter 1`, `behavior_changed 1`.
- The old native abort disappeared: final status had `android_runtime_count=0`,
  `native_signal_count=0`, and `guest_crash_count=0`.
- Remaining blocker is now a live black frame plus physical heap release
  failures, classified as
  `project_sylpheed_heap_release_failures_no_crash_marker`.
- Research note:
  `docs/research/20260528-114500-project-sylpheed-android-reenter-longjmp.md`.

Next: classify the black-frame state and physical heap release behavior without
blindly rounding physical frees. Separately, Android audio still needs a real
backend because `apu=any` currently falls through to `NopAudioSystem`.

## Project Sylpheed Black Frame Classifier

- Added `tools/thor/thor_android_packet_visual_status.ps1`, a packet-level
  visual classifier that joins screenshot near-black analysis with
  `status-report.txt`, reenter audit rows, heap release rows, VdSwap rows,
  native abort markers, and silent-audio fallback rows.
- Ran it on the post-New-Game black-frame packet:
  `scratch/thor-debug/remote-debug-20260528-114503`.
- Result:
  `classification=project_sylpheed_black_frame_after_reenter_no_crash`,
  `near_black_share=1.000000`, `longjmp_reenter_count=6`,
  `throws_exception_count=0`, `native_abort_marker_count=0`,
  `heap_release_after_longjmp=3`.
- Ran it on the menu/title control packet:
  `scratch/thor-debug/remote-debug-20260528-114147`.
- Result:
  `classification=visual_not_black_frame`, `near_black_share=0.554675`.
- Research note:
  `docs/research/20260528-121600-project-sylpheed-black-frame-classifier.md`.

Next: classify physical heap release semantics and whether the failing interior
frees correlate with loss of guest presentation. Do not round down the physical
free blindly.

## Project Sylpheed Physical Heap Release Semantics

- Added `tools/thor/thor_project_sylpheed_physical_heap_release_audit.ps1` to
  parse BaseHeap / PhysicalHeap / MmFreePhysicalMemory triplets from packet
  logcat and compute region-base, interior-offset, page-alignment, repeat, and
  post-reenter / post-present timing facts.
- Ran it on the black-frame packet:
  `scratch/thor-debug/remote-debug-20260528-114503`.
- Result:
  `classification=project_sylpheed_interior_physical_heap_frees_after_reenter_no_present_swaps`,
  `parsed_triplet_count=18`, `parsed_triplets_after_longjmp=1`,
  `inside_region_count=18`, `region_start_count=0`,
  `parent_page_aligned_count=0`, and `vd_swap_after_longjmp=0`.
- Ran the same audit on the visible menu control packet:
  `scratch/thor-debug/remote-debug-20260528-114147`.
- The control packet already has 17 interior physical free failures while the
  menu is visible and `VdSwap` continues, so interior free failures alone are
  not sufficient to explain the black frame.
- The failing frees all map into the same large parent region
  `1C220000-1FCA0000`; blind round-down would release `0x03A80000` bytes and
  remains unsafe.
- Research note:
  `docs/research/20260528-123000-project-sylpheed-physical-heap-release-semantics.md`.

Next: trace physical allocation/free ownership or audit post-reenter execution
and presentation; keep Android audio as a separate backend task.

## Project Sylpheed Physical Memory Ownership Audit

- Added default-off `xboxkrnl_physical_memory_audit` plus
  `xboxkrnl_physical_memory_audit_budget`.
- Instrumented `MmAllocatePhysicalMemoryEx` and `MmFreePhysicalMemory` with
  behavior-neutral ownership rows: guest thread/LR/CTR/r1, physical and parent
  allocation bases/sizes, region-start flags, page-alignment flags, release
  result, returned region size, and `behavior_changed 0`.
- Added Android direct-emulator and real launcher/library debug plumbing, plus
  Thor helper parameters:
  `-XboxkrnlPhysicalMemoryAudit` and
  `-XboxkrnlPhysicalMemoryAuditBudget`.
- Extended the Sylpheed physical heap packet audit to count the new rows while
  preserving old-packet parsing.
- Validation passed: PowerShell parser checks, old-packet parser smoke,
  `NativeCore`, `ApkShell`, `FullDeploy -DeviceSerial c3ca0370`, and
  `git diff --check` with CRLF warnings only.
- Deployed APK SHA256:
  `7B10B60782D1D420BE39E5F4340E7E79B118598175498B54FC87D86638958E89`.
- Research note:
  `docs/research/20260528-125000-project-sylpheed-physical-memory-ownership-audit.md`.

Next: reproduce Project Sylpheed through the game library with the physical
memory audit and reenter audit enabled, then decide whether the failing free is
an expected interior allocation contract or a post-reenter presentation stall.
Keep Android audio separate.

## Project Sylpheed Pthread Join Abort

- Reproduced Project Sylpheed through the real game library with
  `-XboxkrnlPhysicalMemoryAudit true -XboxkrnlPhysicalMemoryAuditBudget 512`
  and `-XboxkrnlReenterAudit true -XboxkrnlReenterAuditBudget 128`.
- Packet `scratch/thor-debug/remote-debug-20260528-132027` reached New Game,
  Normal, and save selection, then aborted with
  `invalid pthread_t ... passed to pthread_join`.
- Symbolized stack points to
  `PosixCondition<Thread>::post_execution -> Wait -> XObject::Wait ->
  NtWaitForSingleObjectEx_entry`, not the old uncaught reenter exception.
- Fixed POSIX thread wait handles by consuming the pthread handle before the
  one legal `pthread_join`, so repeated waits on a completed Xenia thread do not
  call bionic with a stale pthread.
- Added a regression test that waits a completed thread twice.
- Updated `thor_android_game_status_report.ps1` to classify old packets as
  `android_pthread_join_invalid_thread_abort` instead of heap-native abort when
  the abort message is `invalid pthread_t ... pthread_join`.
- Validation passed: `NativeCore`, `FullDeploy -DeviceSerial c3ca0370`,
  PowerShell parser check, old-packet status parser smoke, and
  `git diff --check` with CRLF warnings only.
- Deployed APK SHA256:
  `6FB80504BD99231F8A38BDC4F05DB5C00B4BAD9246C4474C773C041D48E3A5DF`.
- Post-fix packet `scratch/thor-debug/remote-debug-20260528-133426` no longer
  shows native abort, `invalid_pthread_join_count`, reenter rows, or heap rows;
  it remains live at a near-black/loading screen with `vd_swap_count=19473`.
- Research note:
  `docs/research/20260528-133500-project-sylpheed-pthread-join-and-loading-stall.md`.

Next: classify the remaining Sylpheed loading/black stall with a fresh
game-library packet that reaches the audited New Game path again, or join guest
execution/kernel wait/presentation evidence from the current live loading state.

## Project Sylpheed Live Loading VdSwap Loop

- Captured the current live Project Sylpheed state before clearing the running
  app: `scratch/thor-debug/remote-debug-20260528-134556`.
- Status report has no scoped crash markers:
  `android_runtime_count=0`, `native_signal_count=0`,
  `guest_crash_count=0`, `invalid_pthread_join_count=0`, and no BaseHeap /
  PhysicalHeap rows.
- Extended `tools/thor/thor_android_packet_visual_status.ps1` to classify a
  live near-black loading/presentation loop separately from the older
  post-reenter black-frame class.
- Current packet now reports
  `classification=project_sylpheed_live_loading_vdswap_loop_no_crash`,
  `near_black_share=0.998900`, `vd_swap_count=19975`,
  `longjmp_reenter_count=0`, `heap_release_count=0`,
  `native_abort_marker_count=0`, and `invalid_pthread_join_count=0`.
- The previous post-fix packet
  `scratch/thor-debug/remote-debug-20260528-133426` now reports the same
  class with `vd_swap_count=19473`.
- Research note:
  `docs/research/20260528-135500-project-sylpheed-live-loading-vdswap-loop.md`.

Next: join guest execution, kernel wait, and presentation evidence for the live
loading loop. This is not the reenter abort, pthread abort, or physical-free
crash lane.

## Project Sylpheed Loading Loop Join Audit

- Added `tools/thor/thor_project_sylpheed_loading_loop_audit.ps1` to join
  packet visual/status rows, `VdSwap` presentation rows, optional Android
  `top -H` thread snapshots, and optional `Xboxkrnl wait trace` rows.
- Captured a live thread snapshot for the still-running packet:
  `scratch/thor-debug/remote-debug-20260528-134556/top-threads-live.txt`.
- Audit result:
  `classification=project_sylpheed_live_loading_guest_cpu_vdswap_no_crash`,
  `presentation_evidence=vdswap_continuing`,
  `guest_execution_evidence=guest_xthreads_consuming_cpu`, and
  `kernel_wait_evidence=missing_xboxkrnl_thread_wait_trace`.
- Key numbers: `vdswap_count=19975`, `vdswap_span_seconds=157.212`,
  `vdswap_rate_per_second=127.058`, `guest_thread_rows=17`,
  `guest_running_rows=4`, `guest_cpu_sum=126.5`,
  `guest_max_cpu=61.5`, and top guest thread `XThread3ACD6CB0`.
- No known crash markers in this joined view:
  `longjmp_reenter_count=0`, `heap_release_count=0`,
  `native_abort_marker_count=0`, and `invalid_pthread_join_count=0`.
- Research note:
  `docs/research/20260528-141500-project-sylpheed-loading-loop-join-audit.md`.

Next: add or enable focused wait/progress evidence for the CPU-consuming guest
XThreads, starting with `-XboxkrnlThreadWaitTrace true` and a guest PC/progress
sampler if available.

## Project Sylpheed Wait/Progress Launch Plumbing

- Confirmed existing native/EmulatorActivity support for
  `arm64_speed_profile_thread_snapshot` and `xboxkrnl_thread_wait_trace`, but
  the real launcher/game-library path only forwarded reenter and physical-memory
  debug extras.
- Updated `LauncherActivity` and `tools/thor/thor_xenia_debug.ps1
  -Mode LaunchLauncher` so game-library launches can preserve A64 thread
  snapshot and xboxkrnl wait trace cvars.
- Added stable default-off launcher overrides for
  `arm64_speed_profile_thread_snapshot`,
  `arm64_speed_profile_thread_snapshot_on_idle`, and
  `xboxkrnl_thread_wait_trace`.
- Extended `tools/thor/thor_project_sylpheed_loading_loop_audit.ps1` to parse
  `A64 thread snapshot` rows and report last-function / return-address
  progress summaries.
- Old packet smoke against
  `scratch/thor-debug/remote-debug-20260528-134556` still reports
  `guest_progress_evidence=missing_a64_thread_snapshot`, as expected.
- Validation passed: PowerShell parser checks, old-packet smoke, `ApkShell`,
  and `git diff --check` with CRLF warnings only.
- Research note:
  `docs/research/20260528-142500-project-sylpheed-library-wait-progress-launch-plumbing.md`.

Next: FullDeploy and reproduce Sylpheed through the real game library with
`-Arm64SpeedProfileIntervalMs 5000`,
`-Arm64SpeedProfileThreadSnapshot true`, and
`-XboxkrnlThreadWaitTrace true -XboxkrnlThreadWaitTraceBudget 256`.

## Project Sylpheed XMA Decoder Abort

- FullDeployed the wait/progress plumbing build and reproduced Project
  Sylpheed through the real game library.
- Pre-fix packet `scratch/thor-debug/remote-debug-20260528-150232` reached the
  New Game/cutscene path, then returned to launcher with a native abort on the
  `XMA Decoder (F8)` thread.
- Updated the status classifier; rerunning it on that packet reports
  `classification=project_sylpheed_xma_decoder_native_abort` with native abort
  message `xma_context.cc:786: XmaContext::GetNextFrame(...): assertion
  "false" failed`.
- Patched `XmaContext` frame-boundary parsing so invalid/truncated frame lengths
  return packet-end fallback instead of Android-killing assertions.
- Post-deploy packets `scratch/thor-debug/remote-debug-20260528-150754` and
  `scratch/thor-debug/remote-debug-20260528-151705` have no scoped XMA/native
  abort marker; the exact New Game retest is currently blocked at the save-file
  selector with blank rows under debug input.
- Validation passed: `git diff --check` with CRLF warnings only, FullDeploy to
  `c3ca0370`, APK SHA256
  `DA3CB667C638BA6CA9616BBB862C540FDE3AE9FFE809A18B0E96BD62032E738D`, and
  classifier smoke on the old packet.
- Research note:
  `docs/research/20260528-151500-project-sylpheed-xma-decoder-abort.md`.

Next: either drive past Project Sylpheed `SELECT DATA` with physical controls /
clean save state for a stronger XMA proof, or start the separate Android audio
backend lane because output still falls through to `NopAudioSystem`.

## Android AAudio Backend

- Added a native Android AAudio backend and registered it before NOP so
  `apu=any` no longer falls through to silent audio on Android.
- Added an explicit `Android AAudio` settings option, raised the Android
  min/native platform to API 26, and updated `thor_xenia_debug.ps1` so scripted
  launches default to `-ApuDriver any` instead of hard-coded `nop`.
- Fixed `thor_android_remote_debug.ps1` ADB stderr handling after a successful
  screenshot pull was being treated as a PowerShell error.
- Validation passed: `python xenia-build premake --target_os=android`,
  `NativeCore`, `FullDeploy -DeviceSerial c3ca0370`, APK SHA256
  `42D64598CEB87F4FD05E5C95DEECF92A9E5F995935799AE938D9001B0E93C39E`, and a
  remote-debug screenshot packet at
  `scratch/thor-debug/remote-debug-20260528-160025`.
- Audio proof packet
  `scratch/thor-debug/remote-debug-20260528-161000-android-aaudio-proof`
  logged `AndroidAudioSystem selected native Android audio backend` and no
  `NopAudioSystem` row.
- The direct Project Sylpheed launch used for audio-selection proof still hit
  `project_sylpheed_guest_heap_rtlraiseexception`; that is separate from audio
  backend selection.
- Research note:
  `docs/research/20260528-160500-android-aaudio-backend.md`.

Next: get game-library audible-output proof by reaching a route that opens an
Android audio driver, or continue the Project Sylpheed guest heap compatibility
lane from the new direct-launch status packet.

## Android AAudio Game-Library Proof

- Launched the real Android launcher and tapped the Project Sylpheed
  `Launch Last Game` card after confirming the UI in
  `scratch/thor-debug/remote-debug-20260528-162119`.
- Captured `scratch/thor-debug/remote-debug-20260528-162241`, which shows the
  Project Sylpheed opening route and logs `AndroidAudioSystem selected native
  Android audio backend`, `AndroidAudioSystem creating Android audio driver 0`,
  and `AndroidAudioDriver opened AAudio output: sample_rate=48000 channels=2`.
- The packet has no `NopAudioSystem` row, so the app-library path now reaches
  native Android audio instead of silent NOP.
- Status remains `project_sylpheed_heap_release_failures_no_crash_marker` with
  15 BaseHeap / PhysicalHeap release failures, no scoped Android/native crash,
  and no guest crash marker.
- Research note:
  `docs/research/20260528-162500-android-aaudio-game-library-proof.md`.

Next: continue Project Sylpheed heap ownership compatibility, or confirm
audible output by ear with the user present on a game-library route.

## Project Sylpheed Physical Ownership Route Audit

- Reproduced Project Sylpheed through the real launcher/game-library route with
  physical memory and reenter audits enabled, then captured
  `scratch/thor-debug/remote-debug-20260528-163432`.
- Packet is live/no-crash: `project_sylpheed_heap_release_failures_no_crash_marker`,
  `android_runtime_count=0`, `native_signal_count=0`, and `vulkan_swapchain_count=2`.
- The same packet keeps the Android audio route open:
  `AndroidAudioDriver opened AAudio output: sample_rate=48000 channels=2`.
- Extended `thor_project_sylpheed_physical_heap_release_audit.ps1` to parse and
  join physical-memory audit free request/result rows to the heap failure
  triplets.
- Fresh report
  `scratch/thor-debug/20260528-163432-sylpheed-physical-ownership-audit.txt`
  classifies the issue as
  `project_sylpheed_single_owner_interior_physical_heap_frees`.
- Key split: 5 region-start frees succeed, 15 interior frees fail, and all 15
  failures belong to owner allocation `BC220000:03A80000`.
- Keep rejecting blind round-down: the parent region is
  `1C220000-1FCA0000` / `03A80000` bytes.
- Validation so far: PowerShell parser check, fresh packet audit, and old
  packet audit smoke.
- Research note:
  `docs/research/20260528-164500-project-sylpheed-physical-ownership-route-audit.md`.

Next: design an explicit physical suballocation free contract or conservative
owner-refcount rule, with behavior still guarded until double-free/leak/GPU
memory hazards are understood.

## Project Sylpheed Physical Free Contract Design

- Extended the physical heap audit again so allocation-return rows are parsed
  and joined against physical free requests.
- Fresh report
  `scratch/thor-debug/20260528-163432-sylpheed-physical-ownership-audit.txt`
  now shows `physical_memory_audit_parsed_allocate_count=43`,
  `physical_memory_audit_allocation_result_count=42`, and
  `physical_memory_audit_failed_interior_exact_allocation_result_count=0`.
- The owner allocation is a real `MmAllocatePhysicalMemoryEx` result:
  `BC220000:03A80000`, requested as `03A723D0` from LR `824A88B4`.
- The failing frees come from LR `821507BC` / varying CTR targets, are not exact
  allocation results, and are interior offsets into that owner allocation.
- Source design conclusion: do not patch behavior yet. `MmFreePhysicalMemory` is
  `void`, so hiding failures is diagnostic-only, while freeing the owner region
  would release 58.5 MiB. A safe behavior path needs guest free-site size/lifetime
  proof or a conservative physical suballocation ownership model.
- Research note:
  `docs/research/20260528-170000-project-sylpheed-physical-free-contract-design.md`.

Next: map the guest free-site LR/CTR targets or switch back to live
loading/presentation evidence if the heap free-site cannot provide size/lifetime
data.

## Project Sylpheed Physical Free-Site Audit

- Added `tools/thor/thor_project_sylpheed_physical_free_site_audit.ps1` to map
  behavior-neutral physical-memory audit rows into runtime LR/CTR free-site
  groups.
- Fresh report
  `scratch/thor-debug/20260528-170900-sylpheed-physical-free-site-audit.txt`
  classifies the packet as
  `project_sylpheed_single_owner_guest_suballocator_free_sites`.
- Counts: 43 allocation rows, 20 free requests/results, 5 successful frees, 15
  failed interior frees, 0 failed interior frees matching exact allocation
  results.
- All failed frees share owner allocation `BC220000:03A80000` and runtime LR
  `821507BC`.
- CTR targets to map next: `821511D0`, `821512F0`, `82151320`, `821513B0`,
  `82151470`, `821514A0`, `82151530`, `82151560`, `821515C0`, `82151680`,
  `821516B0`, `82151740`, `82151770`, `821517A0`, and `821517D0`.
- Validation passed: PowerShell parser check, fresh packet audit, and old
  pre-audit packet smoke.
- Research note:
  `docs/research/20260528-171000-project-sylpheed-physical-free-site-audit.md`.

Next: map guest code around LR `821507BC` and the CTR cluster for a true
size/lifetime contract, or switch back to live loading/presentation evidence if
the guest-code path cannot produce one.

## Project Sylpheed Guest Free-Site Disassembly Plumbing

- Found that direct `EmulatorActivity` launches already accepted
  `disassemble_functions` and `disassemble_function_filter`, but the real
  launcher/game-library path did not forward those debug extras.
- Patched `LauncherActivity` and `tools/thor/thor_xenia_debug.ps1 -Mode
  LaunchLauncher` so focused PPC/HIR/machine-code dumps can be requested while
  still launching Project Sylpheed from the product game-library UI.
- Kept this behavior-neutral: dumps are only enabled when the debug cvars are
  explicitly passed.
- Research note:
  `docs/research/20260528-173000-project-sylpheed-guest-free-site-disassembly-plumbing.md`.

Next: run a focused library-route Sylpheed repro with the LR/CTR filter from the
research note, then map whether the interior frees represent a real guest
suballocator contract or a presentation/loading red herring.

## Android Library UI And Sylpheed New Game State

- Captured the current Project Sylpheed New Game state before touching the app:
  `scratch/thor-debug/remote-debug-20260528-173237`.
- Status remains live/no scoped crash:
  `project_sylpheed_heap_release_failures_no_crash_marker`, scoped
  `android_runtime_count=0`, scoped `native_signal_count=0`,
  `guest_crash_count=0`, and `vulkan_swapchain_count=4`.
- Visual/status audit says the screenshot is not clean black-frame proof because
  the Thor burn-in overlay is visible, but `VdSwap` continues heavily
  (`vd_swap_count=25747`).
- Fresh free-site audit reports 17 failed interior frees, all still owned by
  `BC220000:03A80000`, with shared LR `821507BC`; new CTR contexts include
  `82151590` and `82151650`.
- Reworked the launcher to feel more like a game library: Game library / Recent
  games are promoted above utility actions, first game gets focus, rows are now
  game tiles with type cover placeholders, compact paths, and status badges.
- Validation: `compileGithubDebugJavaWithJavac`, `git diff --check` with CRLF
  warnings only, direct `assembleGithubDebug`, install to `c3ca0370`, and UI
  screenshot proof at `scratch/thor-debug/remote-debug-20260528-173943/screen.png`.
- Research note:
  `docs/research/20260528-174000-android-library-ui-and-sylpheed-new-game-state.md`.

Next: launch Project Sylpheed from the game library with focused disassembly,
thread wait tracing, and thread snapshots to join the LR/CTR free-site cluster
with the live loading loop before any heap behavior patch.

## Android OSD Exit Game Wording

- Renamed the in-game OSD exit action from `Exit to menu` to `Exit game`.
- Updated the OSD helper note and launcher recent-game status copy to say
  `exited game`, while leaving the existing internal `exited_to_menu` state key
  unchanged for compatibility with saved launcher status.
- This is a wording/UX cleanup only; the existing action still closes the
  running title and returns to the launcher process.
- Validation: Android resource/Java build, `git diff --check`, direct
  `assembleGithubDebug`, and install to `c3ca0370`.

Next: keep using `Exit game` in OSD copy and only revisit behavior if the user
asks for a different close/quit flow.

## Project Sylpheed Library Wait Evidence And APK Native Guard

- Found and fixed a deploy-path regression: a resource-only APK install could
  omit `lib/arm64-v8a/libxenia-app.so`, causing immediate
  `UnsatisfiedLinkError` on game launch.
- Patched `tools/thor/thor_build.ps1` so `ApkShell` / `ApkShellDeploy` stage
  existing native outputs into temporary `jniLibs` while skipping native
  rebuild, assert the APK contains the arm64 native library, and remove staging
  afterward.
- Repaired the Thor install and relaunched Project Sylpheed from the real game
  library with focused free-site disassembly, physical-memory audit, wait
  trace, and A64 thread snapshots.
- Packet `scratch/thor-debug/remote-debug-20260528-180311` is a live black-frame
  state, not a scoped process crash: `near_black_share=1.000000`,
  `vdswap_count=2443`, `guest_cpu_sum=130.3`, `GPU Commands=42.3%`,
  `XMA Decoder=38.4%`, `wait_trace_count=256`, and
  `a64_thread_snapshot_count=350`.
- Physical free-site audit still says no blind heap patch: 16 failed interior
  frees, one owner `BC220000:03A80000`, LR `821507BC`, and zero failed frees
  matching exact allocation results.
- Validation: `ApkShell -NoSubst`, APK zip native-lib inspection, temporary
  staging cleanup, `Install -NoSubst` to `c3ca0370`, Sylpheed library-route
  packet, visual/loading/free-site audits, and `git diff --check`.
- Research note:
  `docs/research/20260528-180300-project-sylpheed-library-wait-and-apk-native-guard.md`.

Next: extract the filtered PPC/HIR dump around `821511D0-82151914` and map the
CTR free-site cases before any heap ownership behavior patch.

## Project Sylpheed Free-Site Flow Join

- Added `tools/thor/thor_project_sylpheed_free_site_flow_join.ps1` to join
  physical-free rows, filtered PPC/HIR table blocks, wait trace, VdSwap rows,
  and A64 thread snapshots from a Project Sylpheed packet.
- Ran it on `scratch/thor-debug/remote-debug-20260528-180311`; report:
  `scratch/thor-debug/20260528-180311-sylpheed-free-site-flow-join.txt`.
- Classification:
  `project_sylpheed_guest_suballocator_offsets_with_unattributed_live_black_loop`.
- All 16 failed interior frees map to filtered PPC/HIR table cases:
  `ppc_target_match_count=16`, `offset_match_count=16`, and
  `next_case_offset_match_count=16`.
- Nuance: each failed row's `ctr` points at the previous table case; the failed
  owner offset matches the next case (`ctr + 0x30`). Example `ctr=821514A0`
  frees owner offset `018F8CC0`, which is computed by source case `821514D0`.
- Wait trace is too early for final causality: 256 rows end at `18:02:12.984`,
  while the final failed free is `18:03:02.018`. VdSwap spans
  `18:02:08.337` to `18:03:01.481`, and 87 A64 snapshots continue after the
  final failed free.
- Decision: do not hide or round down physical frees; next evidence should map
  late guest PCs `824AB214`, `824AC578`, `8272D048`, and `82762158`, ideally
  with delayed/thread-filtered wait trace.
- Research note:
  `docs/research/20260528-181500-project-sylpheed-free-site-flow-join.md`.

Next: map the late wait/progress guest PCs or run a focused delayed wait-trace
capture before any heap ownership behavior patch.

## Android Library Frontend Cover Art

- Reworked the launcher toward a PPSSPP/Dolphin-style frontend: header, top
  category tabs, game library as the main surface, and a right-side action rail.
- Added cover-style game tiles and asynchronous cover lookup/cache via
  `XeniaCoverArt`.
- Default online cover database:
  `https://raw.githubusercontent.com/xenia-manager/x360db/main/games.json`.
- Added scoped network security for `download.xbox.com` cover art and app-cache
  storage for `x360db-games.json` plus cover JPEGs.
- Updated the PowerShell hygiene skill with the faster UI build loop:
  `tools\thor\thor_build.ps1 -Mode ApkShell -NoSubst`.
- Validated with `ApkShellDeploy -NoSubst` to `c3ca0370`.
- Proof screenshots:
  `scratch/thor-debug/remote-debug-20260528-183628/screen.png` and
  `scratch/thor-debug/remote-debug-20260528-184134/screen.png`.
- Follow-up density pass switches the library from tall rows to a four-column
  cover shelf, tightens the header/action rail, and adds visible disc badges
  such as `Disc 1` / `Disc 2` for multi-disc games.
- Dense shelf proof:
  `scratch/thor-debug/remote-debug-20260528-184731/screen.png`.
- Research note:
  `docs/research/20260528-184100-android-library-frontend-cover-art.md`.

Next: keep iterating on controller navigation and cover metadata quality, or
return to Project Sylpheed late-PC/wait attribution.
