# 2026-05-29 Worklog

## 01:05 EDT - Project Sylpheed Late-PC Wait Map

- Read current Android usability instructions, PowerShell/remote-debug skills,
  and `docs/research/20260528-181500-project-sylpheed-free-site-flow-join.md`.
- Confirmed branch/device state: `blue-dragon-route-engine-reset` was clean and
  synced, and AYN Thor `c3ca0370` was connected.
- Added `tools/thor/thor_project_sylpheed_late_pc_wait_map.ps1` to classify the
  late wait/progress PCs in packet-local logcat evidence.
- Ran:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_project_sylpheed_late_pc_wait_map.ps1 -PacketDir scratch\thor-debug\remote-debug-20260528-180311 -OutPath scratch\thor-debug\20260528-180311-sylpheed-late-pc-wait-map.txt
```

- Report classified the packet as
  `project_sylpheed_late_pc_wait_map_needs_capture`: `824AB214` and `824AC578`
  are live wait return PCs, `8272D048` and `82762158` appear as late main-thread
  `last_fn` snapshots, and all four still lack filtered PPC/HIR dumps.
- Wrote `docs/research/20260529-010500-project-sylpheed-late-pc-wait-map.md`.
- Next: real game-library capture with filtered disassembly/HIR for
  `824AB214,824AC578,8272D048,82762158` plus wait trace that reaches the
  black/loading window after failed physical frees.

## 08:51 EDT - Burnout Black 0 FPS Stall

- Captured current screen/log before clearing or force-stopping:
  `scratch/thor-debug/remote-debug-20260529-085115`.
- The active title is Burnout Revenge
  (`Burnout4_External.pe`, title ID `454107DC`, media ID `223759FA`), not
  Project Sylpheed.
- Screenshot shows a black surface with the top-left OSD at `0.0 FPS`.
- Targeted marker scans found `1238159` historical `VdSwap(` rows, no scoped
  fatal/AndroidRuntime/guest-crash markers, and no heap/RtlRaiseException
  markers.
- Last `VdSwap` is `05-29 05:03:44.322`; screenshot packet time is
  `2026-05-29T08:51:16.8979743-04:00`, so the guest had stopped swapping for
  roughly 3h47m.
- Odin Controller A input still reached Android at `08:38:59`, with no later
  guest swaps.
- Wrote
  `docs/research/20260529-085115-burnout-black-0fps-stall.md`.
- Decision: the current `0.0 FPS` badge is correct guest-swap evidence. The
  next Burnout lane is focused guest progress/wait evidence, not another FPS OSD
  source patch.

## 09:15 EDT - Burnout No-Present Audit Tool

- Added `tools/thor/thor_burnout_no_present_stall_audit.ps1` so future Burnout
  black-screen packets can be classified without manual marker scans.
- The tool streams `logcat-focused.txt`, counts runtime `VdSwap(` rows, parses
  title/media/PE metadata, checks crash/heap markers, measures screenshot
  blackness while ignoring the OSD corner, and reports input after the final
  guest swap.
- Ran it on `scratch/thor-debug/remote-debug-20260529-085115`:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_burnout_no_present_stall_audit.ps1 -PacketDir scratch\thor-debug\remote-debug-20260529-085115 -OutPath scratch\thor-debug\20260529-085115-burnout-no-present-stall-audit.txt
```

- Result:
  `classification=burnout_live_black_no_present_stall_no_scoped_crash`,
  `vdswap_count=1238159`, `seconds_since_last_vdswap=13652.576`,
  `crash_marker_count=0`, `heap_marker_count=0`,
  `input_after_last_vdswap_count=2`, `near_black_share=0.998051`.
- Wrote
  `docs/research/20260529-091500-burnout-no-present-audit-tool.md`.

## 09:30 EDT - Remote Debug Tail Logcat

- Hardened `tools/thor/thor_android_remote_debug.ps1` so fresh "check now"
  packets default to bounded `logcat -t 50000` instead of dumping stale full
  device logcat into PowerShell memory.
- Added `-LogcatTailLines` and `-FullLogcat`, streamed ADB logcat output directly
  to disk, recorded the mode in `meta.txt`, and tightened focused crash filtering
  so Wi-Fi signal rows do not dominate `logcat-focused.txt`.
- Validation:

```powershell
$errors = $null; [System.Management.Automation.Language.Parser]::ParseFile('tools\thor\thor_android_remote_debug.ps1', [ref]$null, [ref]$errors)
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_android_remote_debug.ps1 -DeviceSerial c3ca0370 -Mode Screenshot -OutDir scratch\thor-debug\remote-debug-20260529-094000-tail-stream-proof
```

- Proof packet `scratch/thor-debug/remote-debug-20260529-094000-tail-stream-proof`
  completed quickly with `logcat.txt=6384198` bytes,
  `logcat-focused.txt=219367` bytes, `full_logcat=False`,
  `logcat_tail_lines=50000`, and no scoped crash/heap markers.
- Wrote `docs/research/20260529-093000-remote-debug-tail-logcat.md`.

## 09:45 EDT - Project Sylpheed Late-PC Capture Plan

- Updated `tools/thor/thor_project_sylpheed_late_pc_wait_map.ps1` to emit an
  exact delayed/thread-filtered capture recipe for the remaining Sylpheed
  black/loading evidence gap.
- Validation:

```powershell
$errors = $null; [System.Management.Automation.Language.Parser]::ParseFile('tools\thor\thor_project_sylpheed_late_pc_wait_map.ps1', [ref]$null, [ref]$errors)
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_project_sylpheed_late_pc_wait_map.ps1 -PacketDir scratch\thor-debug\remote-debug-20260528-180311 -OutPath scratch\thor-debug\20260528-180311-sylpheed-late-pc-wait-map-capture-plan.txt
```

- Proof report still classifies the packet as
  `project_sylpheed_late_pc_wait_map_needs_capture`, but now gives
  `recommended_disassemble_function_filter=824AB214,824AC578,8272D048,82762158`,
  `recommended_wait_trace_after_ms=45000`,
  `recommended_wait_trace_guest_tids=00000006,00000009,00000008,0000000A`,
  `recommended_wait_trace_budget=2048`, and
  `recommended_remote_debug_logcat_tail_lines=200000`.
- Wrote
  `docs/research/20260529-094500-project-sylpheed-late-pc-capture-plan.md`.

## 10:38 EDT - Project Sylpheed Filtered Late Window

- Fixed a real launcher failure where selecting Start game while an old
  emulator process was alive could stack a second `EmulatorActivity` and abort
  in `xe::Memory::Memory()` with `active_memory_`.
- Improved filtered PPC/HIR diagnostics by rechecking the function filter after
  scanner end-address discovery and by making PPC decoder misses return
  `PPCOpcode::kInvalid` instead of killing diagnostic captures on embedded data.
- Ran the real game-library Project Sylpheed route with filtered PPC/HIR,
  delayed wait trace, A64 thread snapshots, and physical memory audit.
- Proof packet:
  `scratch/thor-debug/remote-debug-20260529-103800-sylpheed-filtered-late-window`.
  Screen is black with guest-swap FPS at `61.3`, `xenia_pid_count=1`,
  `native_signal_count=0`, and status
  `project_sylpheed_heap_release_failures_no_crash_marker`.
- Updated `tools/thor/thor_project_sylpheed_late_pc_wait_map.ps1` so inner PCs
  count as covered when the filtered dump label is the containing function
  range. The new report
  `scratch/thor-debug/20260529-103800-sylpheed-late-pc-wait-map.txt`
  classifies `project_sylpheed_late_pc_wait_map_has_filtered_dumps`.
- Free-site and heap reports now join: 19/19 failed interior frees have PPC/HIR
  table matches, wait trace is after the last failed free, VdSwap continues
  afterward, and all failed frees belong to owner group `BC220000:03A80000`.
- Wrote
  `docs/research/20260529-103800-project-sylpheed-filtered-late-window.md`.
- Decision: do not round down physical frees. Next slice should inspect the
  `821511D0-821518C0` free table with the `824AB190`/`824AC540` wait/retry
  wrappers before any heap ownership or presentation behavior patch.

## 10:43 EDT - Project Sylpheed Heap/Wait Contract

- Fixed `tools/thor/thor_project_sylpheed_free_site_flow_join.ps1` so the
  completed late-window packet no longer reports stale `incomplete` status when
  all failed interior frees map to the PPC/HIR free table but the final table
  case lacks a next-case offset.
- Regenerated
  `scratch/thor-debug/20260529-103800-sylpheed-free-site-flow-join.txt`:
  `classification=project_sylpheed_guest_suballocator_table_with_live_wait_loop`,
  `failed_interior_free_count=19`, `ppc_target_match_count=19`,
  `offset_match_count=18`, `offset_unmatched_count=1`,
  `wait_trace_after_last_failed_free_count=2048`, and
  `vdswap_after_last_failed_free_count=14880`.
- Audited `MmFreePhysicalMemory_entry`, `PhysicalHeap::Release`, and
  `BaseHeap::Release`: current release behavior is region-start only, so
  interior frees fail before physical callbacks or physical page-table release.
- Wrote
  `docs/research/20260529-104300-project-sylpheed-heap-wait-contract.md`.
- Decision: next slice should add a default-off physical-suballocation
  ownership ledger/audit before any behavior patch. Do not round down the
  `1C220000-1FCA0000` parent region.

## 10:53 EDT - Project Sylpheed Physical Suballocation Audit

- Added default-off `xboxkrnl_physical_suballocation_audit` and
  `xboxkrnl_physical_suballocation_audit_budget`.
- The audit is behavior-neutral: `MmFreePhysicalMemory` release behavior is
  unchanged, while request rows group interior physical frees under the owner
  allocation and report child offset counts, duplicate offsets, min/max bounds,
  owner-full-release-by-bounds, current parent/physical release reachability,
  and unsafe child callback-query shape.
- Added Android launcher/settings forwarding and `tools/thor/thor_xenia_debug.ps1`
  parameters:
  `-XboxkrnlPhysicalSuballocationAudit true` and
  `-XboxkrnlPhysicalSuballocationAuditBudget 512`.
- Updated `tools/thor/thor_project_sylpheed_late_pc_wait_map.ps1` so its next
  Sylpheed recipe includes the new audit cvars.
- Validation passed: PowerShell parser checks for touched scripts, regenerated
  the late-PC wait-map report, `NativeCore`, and `ApkShell -NoSubst`.
- Wrote
  `docs/research/20260529-105300-project-sylpheed-physical-suballocation-audit.md`.

## 11:17 EDT - Project Sylpheed Suballocation Ledger Capture

- FullDeploy split was required on Windows: `FullDeploy` hit Gradle
  `!directory.isDirectory` under subst, while `FullDeploy -NoSubst` hit the
  NDK space-path makefile issue. Used `NativeCore`, then `ApkShellDeploy
  -NoSubst`.
- Deployed APK SHA256
  `8FA0750C1D87C4FFBB1C7740C0C116E8C81F66AD032B1AA00D5CF4E5B422971F`.
- Launched Project Sylpheed through the real cover-shelf library with physical
  memory audit, physical suballocation audit, thread wait trace, and A64 thread
  snapshots enabled.
- Proof packet:
  `scratch/thor-debug/remote-debug-20260529-111744`; final screen is black
  with guest-swap FPS still around `61`.
- Added `tools/thor/thor_project_sylpheed_physical_suballocation_audit.ps1` and
  generated
  `scratch/thor-debug/20260529-111744-sylpheed-physical-suballocation-audit.txt`.
- Result:
  `classification=project_sylpheed_suballocation_children_seen_no_owner_release`,
  `interior_request_count=19`, `owner_top=BC220000:03A80000:19`,
  `ledger_owner_fully_released_by_bounds_count=0`,
  `callback_query_unsafe_for_child_count=19`, and `behavior_changed_count=0`.
- Wrote
  `docs/research/20260529-111744-project-sylpheed-suballocation-ledger-capture.md`.
- Decision: do not round down, hide, or owner-release the interior frees from
  this evidence. Next useful slice is delayed post-free wait/progress capture
  or source-only owner/child contract design preserving callbacks.

## 11:37 EDT - Project Sylpheed Delayed Wait / No-Present

- Launched Project Sylpheed through the real library with
  `-XboxkrnlThreadWaitTraceAfterMs 80000`, guest TIDs
  `00000009,00000006,0000000A`, physical memory audit, suballocation audit, and
  A64 snapshots enabled.
- Proof packet: `scratch/thor-debug/remote-debug-20260529-113736`; screenshot
  is black with `0.0 FPS`.
- Updated `tools/thor/thor_project_sylpheed_loading_loop_audit.ps1` to read
  packet `meta.txt`, emit first/last `VdSwap` times, and classify stale swap
  streams as no-present stalls.
- Result:
  `classification=project_sylpheed_black_no_present_stall_no_scoped_crash`,
  `vdswap_last_time=11:35:19.199`,
  `seconds_since_last_vdswap=138.541`,
  `native_abort_marker_count=0`, and delayed wait rows after the final failed
  physical free.
- Physical ledger remains behavior-blocking:
  `interior_request_count=21`, `owner_top=BC220000:03A80000:21`,
  `unique_children=33`, `ledger_owner_fully_released_by_bounds_count=0`,
  and `callback_query_unsafe_for_child_count=21`.
- Wrote
  `docs/research/20260529-113736-project-sylpheed-delayed-wait-no-present.md`.
- Decision: next useful slice is to join the presentation stop to wait objects
  or GPU/presenter state; do not patch physical frees yet.

## 13:40 EDT - Project Sylpheed Presentation Stop Join

- Continued with packet `scratch/thor-debug/remote-debug-20260529-113736`.
- Enhanced `tools/thor/thor_project_sylpheed_late_pc_wait_map.ps1` to join
  presentation-stop context to wait-handle identity:
  - parse/report `xeRtlNtStatusToDosError` lines around last `VdSwap`,
  - report first post-stop wait row with handle/object/thread/API,
  - emit per-target wait-handle maps with guest-object/status/thread/name
    breakdowns.
- Validation commands:

```powershell
$errors = $null
[void][System.Management.Automation.Language.Parser]::ParseFile((Resolve-Path 'tools/thor/thor_project_sylpheed_late_pc_wait_map.ps1'), [ref]$null, [ref]$errors)
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_project_sylpheed_late_pc_wait_map.ps1 -PacketDir 'scratch/thor-debug/remote-debug-20260529-113736' -OutPath 'scratch/thor-debug/20260529-113736-sylpheed-late-pc-wait-map.txt'
```

- Current proof shows:
  `vdswap_last_status_before=caller=F800025C status=00000103 mapped=000003E5 line=14930...`,
  `vdswap_last_status_after=caller=F8000028 status=C0000001 mapped=0000001F line=14941...`,
  and handles:
  `target_pc[0]_wait_handle[0]=handle=F8000054...`,
  `target_pc[0]_wait_handle[1]=handle=F8000068...`,
  `target_pc[1]_wait_handle[0]=handle=F8000260...`.
- Next action: targeted post-free capture focusing on wait handles
  `F8000260`, `F8000054`, and `F8000068` plus GPU/presenter traces before any
  physical-free behavior change.

## 13:45 EDT - Project Sylpheed Post-Free Handle Sampler

- Added a targeted post-failed-free sampler block to
  `tools/thor/thor_project_sylpheed_late_pc_wait_map.ps1`:
  - enumerate handle-level summaries for all target PCs strictly after the final
    failed physical free,
  - compute per-handle thread/status/api/phase summaries, and
  - emit compact sample rows from the beginning and end of the post-free loop.
- Ran and validated parser health before execution:

```powershell
$errors = $null
[void][System.Management.Automation.Language.Parser]::ParseFile((Resolve-Path 'tools\\thor\\thor_project_sylpheed_late_pc_wait_map.ps1'), [ref]$null, [ref]$errors)
```

- Produced sampler proof:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\\thor\\thor_project_sylpheed_late_pc_wait_map.ps1 -PacketDir 'scratch\\thor-debug\\remote-debug-20260529-113736' -OutPath 'scratch\\thor-debug\\20260529-134500-sylpheed-late-pc-wait-map-post-free.txt'
```

- Report findings:
  `post_failed_target_wait_handle_count=3`, with handle summaries for
  `F8000260`, `F8000054`, and `F8000068` and status split
  `00000000/00000102` on each post-free loop.
- Wrote
  `docs/research/20260529-134500-project-sylpheed-post-free-handle-sampler.md`.
- Decision: keep physical free behavior untouched; next slice should join
  these three handles to presenter/GPU state around `VdSwap` shutdown.

## 14:15 EDT - Project Sylpheed Handle Lifecycle Join

- Updated `tools/thor/thor_project_sylpheed_late_pc_wait_map.ps1` to map
  kernel object lifecycle rows (Added/Removed handle events) into both
  `target_pc[_]wait_handle` and `post_failed_target_handle` summaries/samples.
- Re-ran on packet `scratch/thor-debug/remote-debug-20260529-113736`:
  `scratch/thor-debug/20260529-134500-sylpheed-late-pc-wait-map-post-free.txt`.
- Produced actionable findings:
  `post_failed_target_handle[0]` (`F8000260`) `lifecycle_rows=3` with
  `ADDED:2,REMOVED:1`; `post_failed_target_handle[1]` (`F8000054`) and
  `post_failed_target_handle[2]` (`F8000068`) each `lifecycle_rows=1`.
- Next slice: trace `824AC578`/`824AB214` waiter progression against presenter
  and GPU present state using the same handle IDs (`F8000260`, `F8000054`,
  `F8000068`).
- Wrote `docs/research/20260529-141500-project-sylpheed-handle-lifecycle-join.md`.
- Validation: PowerShell parser passed for touched script, and regenerated post-free
  wait-map command executed successfully.

## 15:10 EDT - Project Sylpheed Presenter/GPU Stop Join

- Extended `tools/thor/thor_project_sylpheed_late_pc_wait_map.ps1` to report
  `VdSwap` caller counts, same-caller status rows, presenter/GPU rows around the
  final swap, and snapshot `r3` values for target wait PCs.
- Regenerated
  `scratch/thor-debug/20260529-151000-sylpheed-presenter-gpu-stop-join.txt`
  from packet `scratch/thor-debug/remote-debug-20260529-113736`.
- Proof now says
  `presentation_stop_context=last_vdswap_has_no_same_caller_status_and_no_later_presenter_gpu_rows`,
  `vdswap_last_caller=F8000084`,
  `vdswap_last_caller_status_count=0`, and
  `gpu_presenter_after_last_vdswap_count=0`.
- The first post-stop wait is still
  `pc=824AC578 handle=F8000260` at `11:35:48.819`, `29.62s` after the last
  `VdSwap`; `824AC578` snapshots also carry `F8000260` in `r3` after the final
  failed free.
- Wrote
  `docs/research/20260529-151000-project-sylpheed-presenter-gpu-stop-join.md`.
- Decision: keep physical frees behavior-neutral; next slice should add
  default-off `VdSwap` enter/exit and compact presenter/GPU state instrumentation
  for a fresh packet.

## 16:00 EDT - Project Sylpheed VdSwap Probe Live Presenter

- Added launch plumbing for default-off `gpu_trace_vd_swap` across
  `tools/thor/thor_xenia_debug.ps1`, `EmulatorActivity`, `LauncherActivity`, and
  stable Android launcher settings.
- Validated `thor_xenia_debug.ps1` with the PowerShell parser and built the
  debug APK with `tools\thor\thor_build.ps1 -Mode ApkShell -NoSubst`.
- Installed APK
  `09A886D7F4D12FF15B1B9F4B48D696A5E964B2FC1ADFDA398B28E8306430161D` on
  `c3ca0370`.
- Launched Project Sylpheed from the real game library card with
  `-GpuTraceVdSwap true`, wait tracing, physical/suballocation audit, and A64
  snapshots.
- Captured packet:
  `scratch/thor-debug/remote-debug-20260529-160000-vdswap-probe`.
- Screenshot is black but reports `59.2 FPS`, unlike the prior stale `0.0 FPS`
  packet.
- Parser proof:
  `vdswap_last_caller=F8000084`,
  `vdswap_caller_counts=F8000084:19550,F8000028:2`,
  `vdswap_last_caller_status_count=9775`, and
  `gpu_presenter_after_last_vdswap_count=1`; presenter rows continue at capture
  end.
- Physical frees remain behavior-blocked:
  `interior_request_count=15`,
  `ledger_owner_fully_released_by_bounds_count=0`,
  `current_physical_release_would_succeed_count=0`, and
  `behavior_changed_count=0`.
- Wrote
  `docs/research/20260529-160000-project-sylpheed-vdswap-probe-live-presenter.md`.

## 16:29 EDT - Banjo-Kazooie File-Status Dirty Disc

- Switched focus to Banjo-Kazooie: Nuts & Bolts (`4D5307ED`, media
  `3E567DFF`) after the user asked to get Banjo working.
- Captured the current device before reset:
  `scratch/thor-debug/remote-debug-20260529-banjo-current-before-reset`; it was
  Burnout at a save/load screen, not Banjo.
- Launched Banjo directly from:
  `/storage/2664-21DE/Roms/xbox360/Banjo-Kazooie - Nuts & Bolts (USA) (En,Ja,Fr,De,Es,It,Nl,Sv,No,Zh,Ko,Pl,Ru,Cs).iso`.
- Added behavior-neutral file-I/O probes:
  default-off full `xboxkrnl_file_io_trace`, budgeted
  `xboxkrnl_file_io_status_log_budget`, Thor helper launch plumbing, and a
  `XamShowDirtyDiscErrorUI` marker.
- Validation:

```powershell
$errors = $null
[void][System.Management.Automation.Language.Parser]::ParseFile((Resolve-Path 'tools\\thor\\thor_xenia_debug.ps1'), [ref]$null, [ref]$errors)
git diff --check
powershell -NoProfile -ExecutionPolicy Bypass -File tools\\thor\\thor_build.ps1 -Mode NativeCore
powershell -NoProfile -ExecutionPolicy Bypass -File tools\\thor\\thor_build.ps1 -Mode ApkShell -NoSubst
adb -s c3ca0370 install -r android\\android_studio_project\\app\\build\\outputs\\apk\\github\\debug\\app-github-debug.apk
```

- `FullDeploy` still hits the known subst-path
  `:app:mergeGithubDebugResources` `!directory.isDirectory` failure; the
  validated deploy path was `NativeCore` plus `ApkShell -NoSubst` and manual
  install.
- Installed APK SHA256:
  `A2306E04D185351877E97949F6149A1A66FF7E886509CD069BD0FB0F4DF0740E`.
- Final packet:
  `scratch/thor-debug/remote-debug-20260529-banjo-file-status-final`.
- Proof: screenshot shows the `Disc Read Error` dialog at `0.0 FPS`, with no
  Android runtime, native signal, guest crash, heap release, physical heap
  release, or invalid pthread join marker.
- Rendering is alive in the log: `270` guest-output callback submissions and
  `540` `XE_SWAP` rows.
- New blocker evidence:
  `NtQueryFullAttributesFile status: path='GAME:\loctext\englishus\' status=C000000F missing=1`,
  immediately followed by `xeRtlNtStatusToDosError C000000F => 2`; dirty-disc
  is logged `0.725s` later.
- Wrote
  `docs/research/20260529-162930-banjo-nuts-bolts-file-status-dirty-disc.md`.

## 18:55 EDT - Library Speed/Quality Sweep + Crash Overlap

- Added `tools/thor/thor_game_speed_quality_pass.ps1`: launches each title,
  taps Start(108)/A(96) to skip intro movies, settles, then captures screenshot
  + VdSwap-based FPS + near-black + crash markers, classifying each.
- Swept 8 plain ISOs: packet `scratch/thor-debug/game-pass-all-20260529-184405`.
- Findings: Banjo renders ~30fps (NOT dirty disc - prior note stale); Burnout
  reaches in-game 60fps (clean city-highway frame); all 4 Gears render ~28-30fps;
  Back to the Future and Project Sylpheed do not render.
- Crash taxonomy corrected: `RtlRaiseException` is routine/non-fatal. Real fatal
  signals: Sylpheed guest CRASH DUMP (PC 0x8245BDEC); native `abort`/`__assert2`
  asserts in Back to the Future + Gears 1/3/Judgment. Gears 3 == Gears: Judgment
  share identical crash PCs (`0xdcb6b4/0xdcaa6c/0xdc9d40/0x9e9f34`) -> one fix
  likely covers both.
- Wrote `docs/research/20260529-185500-game-library-speed-quality-sweep.md`.

## 19:00 EDT - UMA Direct-Write A/B

- Built NativeCore + ApkShell with the pre-existing `gpu_uma_direct_shared_memory`
  path; wired the cvar through the Android launch-intent allow-list and
  `thor_xenia_debug.ps1`. APK `B7C4A17F...`.
- Burnout off-vs-on, same route: 59.8 vs 58.2 FPS (vsync-capped), both clean
  frames. Path confirmed active (memory type 6, host-coherent). Correctness PASS,
  no corruption; perf neutral because scene is vsync-bound (active work tens of
  us/frame). UMA path forces a 512 MB non-sparse buffer.
- Packets `scratch/thor-debug/game-pass-uma-off-20260529-185858` and
  `...uma-on-20260529-190014`.

## 19:38 EDT - Live-Cvar Fast Loop + Frame-Time

- Added a runtime cvar setter (`cvar::SetCommandVarFromString` /
  `ICommandVar::SetValueFromString`), JNI `nativeSetConfigVar`, an
  `EmulatorActivity` `SET_CVAR` broadcast, and `tools/thor/thor_cvar.ps1`.
- Proven on a running Burnout (no relaunch): flipped `vulkan_trace_perf_counters`
  true->false live; perf-counter frame-time snapshots streamed on enable. This
  turns the tuning loop from minutes into ~1s and gives the frame-time telemetry
  (#7) the FPS cap was hiding.
- Validation: NativeCore + ApkShell exit 0; on-device broadcast applied both ways.
- Wrote `docs/research/20260529-194000-uma-ab-and-live-cvar-fast-loop.md`.
- Note: `kernel_flags.*`, `xam_ui.cc`, `xboxkrnl_io.cc` remain pre-existing dirty
  work, intentionally left untouched and uncommitted.

## 20:00 EDT - Autonomous: symbolize + soften Gears shader-interpreter assert

- Iter 1: symbolized the sweep's native asserts with NDK addr2line vs the
  unstripped arm64 libxenia-app.so. Gears 3 == Gears: Judgment share the shader
  interpreter ALU path (`ShaderInterpreter::ExecuteAluInstruction`); Gears 1
  (XmaDecoder) and Back to the Future (cond-var timed wait) are separate.
  Wrote `docs/research/20260529-200000-gears-native-assert-symbolication.md`.
- Iter 2: converted the interpreter's unhandled-ALU-opcode aborts
  (`shader_interpreter.cc` vector ~611 / scalar ~898) to log-once + safe continue
  (zero result / retain previous). NativeCore + ApkShell exit 0, APK
  `0E74E70B...`.
- Gears 3 verify (`game-pass-gears3-fix-20260529-200208`): did NOT reach shaders
  (`vdswap=0`); aborted earlier on a transient disc-mount FatalError
  ("Unable to mount disc image; file not found or corrupt", `emulator.cc:993`).
  The earlier sweep mounted+rendered this ISO, so it is a flaky-mount, unrelated
  to the fix. ALU fix is sound but UNPROVEN on Gears - no behavioral claim.
- Next: retry Gears 3 to exercise the ALU fix; consider making disc-mount failure
  non-fatal. Wrote
  `docs/research/20260529-200300-shader-interpreter-nonfatal-alu-and-gears3-mount.md`.

## 20:20 EDT - Autonomous: Gears 3 zero-stride vfetch fix (DEVICE PROVEN)

- The earlier "disc-mount FatalError" on Gears 3 was a self-inflicted wrong ISO
  filename (dropped ",Hu"); use exact `ls` filenames.
- With the correct name, the authoritative Gears 3 abort was
  `shader_translator.cc:429 GatherVertexFetchInformation assertion
  "(fetch_instr.attributes.stride) != 0" failed` - a vfetch creating a NEW
  binding with stride 0 (fetch constant 93). The ALU interpreter path (iter 1/2)
  was an addr2line mis-attribution, not the real cause.
- Fix: allow the zero-stride new binding + warn once (the existing-binding path
  already tolerates zero stride). `src/xenia/gpu/shader_translator.cc`.
- Built (APK `0E30244D...`), deployed, ran Gears 3 (correct filename):
  packet `scratch/thor-debug/game-pass-gears3-stridefix-20260529-201836`.
  PROOF: Gears 3 title screen ("PRESS START") renders @ 29.6 FPS, `vdswap=2209`,
  the fix's XELOGW present, no CRASH DUMP/abort/stride assertion. Gears 3 now
  boots past the crash.
- Wrote `docs/research/20260529-202000-gears3-zero-stride-vfetch-fix.md`.
- Next: confirm Gears: Judgment (shared assert) reaches title; push Gears 3 past
  PRESS START; continue RPG sweep + UMA quantification.

## 20:27 EDT - Autonomous: Gears: Judgment also reaches title (cluster fixed)

- Ran Gears: Judgment on the stride-fix APK
  (`game-pass-judgment-stridefix-20260529-202613`). PROOF: title screen
  ("GEARS OF WAR JUDGMENT / PRESS START") @ ~21.7 FPS, `vdswap=1821` (was 829),
  the `zero-stride new binding (fetch constant 93)` XELOGW fired (same constant as
  Gears 3), no CRASH DUMP/abort/stride assertion.
- One `shader_translator.cc` fix unblocks both UE3 Gears titles. Appended
  confirmation to `docs/research/20260529-202000-gears3-zero-stride-vfetch-fix.md`.
- Aside: transient `adb: device not found` blips mid-capture on both runs - Thor
  USB ADB intermittently dropping; captures still complete. Cable/port check
  suggested (not a code issue).
- Next: try advancing Gears 3 past PRESS START; then RPG sweep / UMA quantify.
