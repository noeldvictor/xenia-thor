# xenia-thor Worklog - 2026-05-23

## 23:53-00:10 EDT - 8227FEE8 Block Body-Time Black-Idle And Owner Attribution

Starting HEAD: `05dbeb7d1`

Goal: run the delayed `8227FEE8` block body-time capture. If the route
black-idles again, improve idle-owner attribution before changing generated
code.

Commands run:

```powershell
git status --short --branch
adb devices
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 25 -Arm64SpeedProfileMinDelta 1 -A64LseKernelLockFastpaths true -A64RtlLeaveFastpathAudit false -Arm64PermuteI32ZipFastpath true -Arm64ContextPromotionGprLiveInR1 false -Arm64SpeedProfileBodyTimeFilter "8227FEE8" -Arm64SpeedProfileBodyTimeAfterMs 120000 -Arm64SpeedProfileBlockFilter "8227FEE8" -Arm64SpeedProfileBlockBodyTime true -Arm64SpeedProfileThreadSnapshotOnIdle true -StopAppAfterCapture true
git diff --check
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 90 -PerfSampleSeconds "60,85" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 25 -Arm64SpeedProfileMinDelta 1 -A64LseKernelLockFastpaths true -A64RtlLeaveFastpathAudit false -Arm64PermuteI32ZipFastpath true -Arm64ContextPromotionGprLiveInR1 false -Arm64SpeedProfileThreadSnapshotOnIdle true -StopAppAfterCapture true
```

Files changed:

- `src/xenia/base/mutex.h`
- `src/xenia/base/mutex.cc`
- `src/xenia/cpu/processor.h`
- `src/xenia/cpu/processor.cc`
- `src/xenia/cpu/backend/a64/a64_backend.cc`
- `docs/research/20260523-001018-a64-owner-thread-id-attribution.md`
- `docs/worklogs/20260523.md`
- `AGENTS.md`
- `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md`
- `.agents/skills/xenia-working-fast-autonomy/SKILL.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `tools/thor/thor_codex_goal_loop.ps1`

Findings:

- Proof marker is still absent.
- Capture `scratch/thor-debug/20260522-235449-*` attempted delayed
  `8227FEE8` function and block body-time.
- It black-idled before useful route progress: screenshot black,
  `entry_delta=0` after the early samples, no searched fatal markers, and no
  `8227FEE8` body/block rows after body-time activation.
- The idle snapshot was blocked by the processor debug lock and reported
  `last_global_owner_sys_tid=14186`, `global_lock_count=0`, and
  `owner_hint=miss`.
- Patch added diagnostic attribution only:
  `global_critical_region` now records both native system TID and
  `xe::threading::current_thread_id()` for the last owner, `Processor` can
  query the lock-free hint cache by guest thread ID or handle, and the A64 idle
  skip line logs `last_global_owner_thread_id`, `owner_hint_source`, and
  `owner_hint_sys_tid`.
- NativeCore passed.
- FullDeploy passed and installed patched APK SHA
  `962D3086F4030D9BD5A9D46AF5E8DFA4A320A13BFCD14135B8B077AECDC31CC5`.
- Short validation capture `scratch/thor-debug/20260523-000506-*` stayed
  active at the loading spinner with clean fatal search. It did not exercise
  the new idle owner line because counters kept advancing.

Validation:

- `git diff --check` passed.
- `tools/thor/thor_build.ps1 -Mode NativeCore` passed.
- `tools/thor/thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370` passed.
- Device capture wrote metadata, logcat, perf samples, and screenshot.

Blockers:

- We still do not have `8227FEE8` block body-time rows.
- The new owner attribution path is build/deploy validated but not yet
  exercised in a black-idle capture.
- No FPS win or full-speed proof happened in this slice.

Next actions:

- Repeat the delayed `8227FEE8` block body-time capture on the patched APK.
- If route-clean, use real block rows to choose between `82280A68`/`82280E1C`
  vector load/store work or a tighter GPR/CR state-traffic patch.
- If it black-idles, inspect `last_global_owner_thread_id`,
  `owner_hint_source`, and `owner_hint_sys_tid` before making a guest codegen
  change.

## 12:21-12:27 EDT - 8227FEE8 Route-Clean Block Body-Time

Starting HEAD: `1f6d75624`

Goal: repeat the delayed `8227FEE8` block body-time capture on the patched APK
and choose the next evidence-backed hotpath.

Commands run:

```powershell
git status --short --branch
adb devices
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 25 -Arm64SpeedProfileMinDelta 1 -A64LseKernelLockFastpaths true -A64RtlLeaveFastpathAudit false -Arm64PermuteI32ZipFastpath true -Arm64ContextPromotionGprLiveInR1 false -Arm64SpeedProfileBodyTimeFilter "8227FEE8" -Arm64SpeedProfileBodyTimeAfterMs 120000 -Arm64SpeedProfileBlockFilter "8227FEE8" -Arm64SpeedProfileBlockBodyTime true -Arm64SpeedProfileThreadSnapshotOnIdle true -StopAppAfterCapture true
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_block_mix_report.ps1 -LogPath scratch\thor-debug\20260522-233545-speed-logcat.txt -Function 8227FEE8 -Phase OptHIR -BlockProfileLog scratch\thor-debug\20260523-122223-speed-logcat.txt -Top 16
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_call_path_report.ps1 -LogPath scratch\thor-debug\20260522-233545-speed-logcat.txt -Function 8227FEE8 -Phase OptHIR -BlockProfileLog scratch\thor-debug\20260523-122223-speed-logcat.txt -Top 12
```

Files changed:

- `docs/research/20260523-122718-8227fee8-block-body-time-route-clean.md`
- `docs/worklogs/20260523.md`
- `AGENTS.md`
- `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md`
- `.agents/skills/xenia-working-fast-autonomy/SKILL.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `tools/thor/thor_codex_goal_loop.ps1`

Findings:

- Proof marker is still absent.
- Capture `scratch/thor-debug/20260523-122223-*` reached the visible
  opening sky/wing route and had a clean fatal-marker search.
- It used APK SHA
  `962D3086F4030D9BD5A9D46AF5E8DFA4A320A13BFCD14135B8B077AECDC31CC5`.
- Delayed body-time produced `8227FEE8` rows:
  `body_ticks_total=6914659`, final `body_ticks_delta=1871176`,
  `entries_delta=2041`, `ticks_per_entry=916`, `code_size=71868`.
- Internal block body-time says the next target is not the static
  `82280A68` / `82280E1C` vector blocks yet.
- The dynamic wall inside `8227FEE8` is call-heavy block `822809F4`
  (`body_total=1937201`, peak `665665`, peak `ticks_per_entry=128`) with
  direct calls to `0x8227F1D8` and `0x8247BE20`.
- Final perf sample still showed a CPU/JIT wall: Main Thread `96.1%`, GPU
  Commands `11.5%`, XMA Decoder `11.5%`, Draw Thread `3.8%`.

Validation:

- Device capture wrote metadata, logcat, perf samples, and visible route
  screenshot.
- `tools/thor/thor_hir_block_mix_report.ps1` parsed the joined block profile.
- `tools/thor/thor_hir_call_path_report.ps1` mapped charged body-time to
  `8227FEE8` call targets.

Blockers:

- No FPS win or full-speed proof happened in this slice.
- We still need to split `822809F4` inclusive callee cost between
  `0x8227F1D8` and `0x8247BE20` before writing a codegen patch.

Next actions:

- Run a route-stabilized callee/call-edge capture for
  `8227FEE8,8227F1D8,8247BE20` with delayed body-time and
  `-Arm64SpeedProfileCallEdgeFilter 8227FEE8`.
- If route-clean, choose the next default-off function-gated experiment from
  the callee split.
- Do not start the stale `82280A68` / `82280E1C` vector peephole or broad GPR
  cache from this capture alone.

## 12:33-12:45 EDT - 8227FEE8 Callee Call-Edge Split

Starting HEAD: `b26dfa100`

Goal: run the route-stabilized `8227FEE8` callee/call-edge capture and decide
whether `8227F1D8` or `8247BE20` is the next measured speed target.

Commands run:

```powershell
git status --short --branch
adb devices
adb shell getprop ro.product.model
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 25 -Arm64SpeedProfileMinDelta 1 -A64LseKernelLockFastpaths true -A64RtlLeaveFastpathAudit false -Arm64PermuteI32ZipFastpath true -Arm64ContextPromotionGprLiveInR1 false -Arm64SpeedProfileBodyTimeFilter "8227FEE8,8227F1D8,8247BE20" -Arm64SpeedProfileBodyTimeAfterMs 120000 -Arm64SpeedProfileCallEdgeFilter "8227FEE8" -Arm64SpeedProfileThreadSnapshotOnIdle true -StopAppAfterCapture true
rg -n "Fatal signal|AndroidRuntime|VK_ERROR_DEVICE_LOST|GPU is hung|Unhandled|ASSERT|CHECK failed|FATAL" scratch\thor-debug\20260523-123406-speed-logcat.txt scratch\thor-debug\20260523-123406-speed-logcat-filtered.txt
rg -n "A64 speed profile body top 0[1-4]|edge=21 block=822809F4 target=8227F1D8|target=8247BE20" scratch\thor-debug\20260523-123406-speed-logcat.txt
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_call_path_report.ps1 -LogPath scratch\thor-debug\20260522-233545-speed-logcat.txt -Function 8227FEE8 -Phase OptHIR -BlockProfileLog scratch\thor-debug\20260523-123406-speed-logcat.txt -Top 16
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_hotpath_report.ps1 -LogPath scratch\thor-debug\20260523-123406-speed-logcat.txt -Function 8227F1D8 -Phase OptHIR -Top 20
```

Files changed:

- `docs/research/20260523-124029-8227fee8-callee-call-edge-split.md`
- `docs/worklogs/20260523.md`
- `AGENTS.md`
- `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md`
- `.agents/skills/xenia-working-fast-autonomy/SKILL.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `tools/thor/thor_codex_goal_loop.ps1`

Findings:

- Proof marker is still absent.
- Capture `scratch/thor-debug/20260523-123406-*` reached the visible opening
  sky/wing route and had a clean fatal-marker search.
- It used HEAD `b26dfa100` and APK SHA
  `962D3086F4030D9BD5A9D46AF5E8DFA4A320A13BFCD14135B8B077AECDC31CC5`.
- Final delayed body-time showed `8227F1D8` taking the top interval:
  `body_ticks_delta=844204`, `body_ticks_total=3714635`,
  `entries_delta=9941`, `ticks_per_entry=84`, `code_size=20180`.
- Parent `8227FEE8` ended at `body_ticks_total=4265926`, but the final
  interval was lower than `8227F1D8`.
- `8247BE20` is not the next target for this route: it ended at only
  `body_ticks_total=27747`, `ticks_per_entry=2`, `code_size=796`.
- Dynamic call-edge rows make the split clear: `822809F4 -> 8227F1D8`
  reached `body_ticks_total=2031295`, peak delta `1137492`, and peak
  `ticks_per_call=216`.
- The next dynamic edge under `8227FEE8` was much smaller
  (`82281938 -> 82285308`, `body_ticks_total=105077`).
- `tools/thor/thor_hir_hotpath_report.ps1` found no `8227F1D8` HIR in this
  capture because no filtered dump was enabled for the callee.
- Final perf still points at CPU/JIT: Main Thread `92.3%`, XMA Decoder
  `11.5%`, GPU Commands `7.6%`, Draw Thread `0.0%`.

Validation:

- Device capture wrote metadata, logcat, perf samples, and visible route
  screenshot.
- Fatal-marker search returned no matches.
- `tools/thor/thor_hir_call_path_report.ps1` parsed dynamic call-edge rows.
- `tools/thor/thor_hir_hotpath_report.ps1` confirmed the next run needs a
  filtered `8227F1D8` HIR dump.

Blockers:

- No FPS win or full-speed proof happened in this slice.
- We do not yet have `8227F1D8` HIR or internal block body-time rows.

Next actions:

- Run a route-stabilized filtered HIR plus delayed body/block-time capture for
  `8227F1D8`, keeping `8227FEE8` in the body-time filter as the parent
  comparator.
- If route-clean, choose a default-off function/block-gated codegen experiment
  from the `8227F1D8` HIR and block body-time.
- If it black-idles, inspect `last_global_owner_thread_id`,
  `owner_hint_source`, and `owner_hint_sys_tid` before changing generated-code
  behavior.

## 12:59-13:10 EDT - Continual Harness Adaptation

Starting HEAD: `2e45cfda4`

Goal: inspect `sethkarten/continual-harness` and adapt the portable process
ideas into the xenia-thor autonomous sprint without importing Pokemon-specific
runtime code.

Commands run:

```powershell
git status --short --branch
Get-Content .agents\skills\xenia-codex-goal-loop\SKILL.md -TotalCount 220
Get-Content .agents\skills\xenia-working-fast-autonomy\SKILL.md -TotalCount 140
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_continual_harness_review.ps1 -Mode Window
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_codex_goal_loop.ps1 -Mode EnableBlueDragonFullSpeed -MaxAttempts 48
git diff --check
```

Files changed:

- `.agents/skills/xenia-continual-harness-refiner/SKILL.md`
- `tools/thor/thor_continual_harness_review.ps1`
- `docs/research/20260523-125940-continual-harness-adaptation.md`
- `AGENTS.md`
- `.agents/skills/xenia-codex-goal-loop/SKILL.md`
- `.agents/skills/xenia-working-fast-autonomy/SKILL.md`
- `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md`
- `docs/worklogs/20260523.md`
- `tools/thor/thor_codex_goal_loop.ps1`

Findings:

- Continual Harness is useful as a process pattern, not as drop-in emulator
  code for Xenia.
- The portable loop is recent trajectory window -> refiner -> CRUD edits over
  prompt, subagents, skills, and memory -> persisted bootstrap for the next
  continuation.
- For xenia-thor, the trajectory window is recent worklogs, research notes,
  goal-loop state, git state, and capture metadata.
- First local adaptation is a repo-local skill plus a deterministic window
  builder:
  `tools/thor/thor_continual_harness_review.ps1 -Mode Window`.
- Stop-hook and heartbeat prompts now mention the continual-harness refiner as
  the corrective path when the loop repeats stale lanes.

Validation:

- `tools/thor/thor_continual_harness_review.ps1 -Mode Window` generated
  `scratch/thor-debug/continual-harness-window.md`.
- PowerShell parser validation passed for
  `tools/thor/thor_continual_harness_review.ps1`.
- PowerShell parser validation passed for
  `tools/thor/thor_codex_goal_loop.ps1`.
- `git diff --check` passed.

Next actions:

- Use the refiner skill when the loop repeats stale experiments or a fresh
  capture changes the next target.
- Keep the current Blue Dragon next action unchanged: filtered `8227F1D8` HIR
  plus delayed body/block-time.

## 13:08-13:22 EDT - 8227F1D8 Filtered HIR And Block Body-Time

Starting HEAD: `a1e62d275`

Goal: run the route-stabilized filtered HIR plus delayed body/block-time
capture for `8227F1D8`.

Commands run:

```powershell
git status --short --branch
adb devices
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 25 -Arm64SpeedProfileMinDelta 1 -A64LseKernelLockFastpaths true -A64RtlLeaveFastpathAudit false -Arm64PermuteI32ZipFastpath true -Arm64ContextPromotionGprLiveInR1 false -DisassembleFunctions true -DisassembleFunctionFilter 8227F1D8 -Arm64SpeedProfileBodyTimeFilter "8227FEE8,8227F1D8" -Arm64SpeedProfileBodyTimeAfterMs 120000 -Arm64SpeedProfileBlockFilter "8227F1D8" -Arm64SpeedProfileBlockBodyTime true -Arm64SpeedProfileThreadSnapshotOnIdle true -StopAppAfterCapture true
rg -n "Fatal signal|AndroidRuntime|VK_ERROR_DEVICE_LOST|GPU is hung|Unhandled|ASSERT|CHECK failed|FATAL" scratch\thor-debug\20260523-130934-speed-logcat.txt scratch\thor-debug\20260523-130934-speed-logcat-filtered.txt
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_hotpath_report.ps1 -LogPath scratch\thor-debug\20260523-130934-speed-logcat.txt -Function 8227F1D8 -Phase OptHIR -Top 25
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_block_mix_report.ps1 -LogPath scratch\thor-debug\20260523-130934-speed-logcat.txt -Function 8227F1D8 -Phase OptHIR -BlockProfileLog scratch\thor-debug\20260523-130934-speed-logcat.txt -Top 20
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_hir_call_path_report.ps1 -LogPath scratch\thor-debug\20260523-130934-speed-logcat.txt -Function 8227F1D8 -Phase OptHIR -BlockProfileLog scratch\thor-debug\20260523-130934-speed-logcat.txt -Top 20
```

Files changed:

- `docs/research/20260523-131338-8227f1d8-filtered-hir-block-body-time.md`
- `docs/worklogs/20260523.md`
- `AGENTS.md`
- `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md`
- `.agents/skills/xenia-working-fast-autonomy/SKILL.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `tools/thor/thor_codex_goal_loop.ps1`

Findings:

- Proof marker is still absent.
- Capture `scratch/thor-debug/20260523-130934-*` reached the visible opening
  sky/wing route and had a clean fatal-marker search.
- It used APK SHA
  `962D3086F4030D9BD5A9D46AF5E8DFA4A320A13BFCD14135B8B077AECDC31CC5`.
- The warning-level filtered `8227F1D8` OptHIR dump landed.
- Final body rows: `8227F1D8 body_ticks_total=4238549`,
  `body_ticks_delta=907049`, `entries_delta=9675`, `ticks_per_entry=93`,
  `code_size=28180`.
- Final `8227F1D8` block body-time was almost entirely block `8227F1D8`
  itself: `body_ticks_total=4152240`, peak delta `1764204`, peak
  `ticks_per_entry=262`.
- Block `8227F1D8` is small but call-heavy: `instr=69`, `ctx=7/20`,
  `mem=4/3`, `branches=2`, `calls=2`, `barriers=4`, calling `0x82490030`
  and `0x826BFC7C`.
- Whole-function HIR is state-traffic heavy:
  `store_context=546`, `load_context=289`, `context_barrier=115`, `calls=52`.
- Top context loads include `r[1]=43`, `r[31]=36`, `r[30]=20`, and
  `r[11]=17`; top stores include `r[11]=60`, `lr=50`, `r[3]=48`,
  `r[4]=46`, and CR6 triplets at `37` each.
- Final perf still points at CPU/JIT: Main Thread `103%`, XMA Decoder `7.6%`,
  GPU Commands `7.6%`, Draw Thread `0.0%`.

Validation:

- Device capture wrote metadata, logcat, perf samples, and visible route
  screenshot.
- Fatal-marker search returned no matches.
- `thor_hir_hotpath_report.ps1`, `thor_hir_block_mix_report.ps1`, and
  `thor_hir_call_path_report.ps1` all parsed the capture.

Blockers:

- No FPS win or full-speed proof happened in this slice.
- The current evidence still identifies inclusive call work, not a safe
  function-local codegen peephole.

Next actions:

- Run a route-stabilized call-edge split for `8227F1D8`, with delayed
  body-time for `8227F1D8,82490030,826BFC7C` and
  `-Arm64SpeedProfileCallEdgeFilter 8227F1D8`.
- If route-clean, choose the next target from dynamic edge rows.
- Keep broad GPR/cache/vector/CR experiments default-off until that edge split
  names a real generated-code shape.

## 15:27-15:39 EDT - 8227F1D8 Call-Edge Split

Starting HEAD: `42ba0c59a`

Goal: split the two direct child calls from the hot `8227F1D8` entry block
before writing any local codegen peephole.

Commands run:

```powershell
git status --short --branch
adb devices
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 25 -Arm64SpeedProfileMinDelta 1 -A64LseKernelLockFastpaths true -A64RtlLeaveFastpathAudit false -Arm64PermuteI32ZipFastpath true -Arm64ContextPromotionGprLiveInR1 false -Arm64SpeedProfileBodyTimeFilter "8227F1D8,82490030,826BFC7C" -Arm64SpeedProfileBodyTimeAfterMs 120000 -Arm64SpeedProfileCallEdgeFilter "8227F1D8" -Arm64SpeedProfileThreadSnapshotOnIdle true -StopAppAfterCapture true
rg -n "Fatal signal|AndroidRuntime|VK_ERROR_DEVICE_LOST|GPU is hung|Unhandled|ASSERT|CHECK failed|FATAL" scratch\thor-debug\20260523-152754-speed-logcat.txt scratch\thor-debug\20260523-152754-speed-logcat-filtered.txt
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_continual_harness_review.ps1 -Mode Window
```

Files changed:

- `docs/research/20260523-153235-8227f1d8-call-edge-split.md`
- `docs/worklogs/20260523.md`
- `AGENTS.md`
- `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md`
- `.agents/skills/xenia-working-fast-autonomy/SKILL.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `tools/thor/thor_codex_goal_loop.ps1`

Findings:

- Proof marker is still absent.
- Capture `scratch/thor-debug/20260523-152754-*` reached the visible opening
  sky/wing route and had a clean fatal-marker search.
- It used APK SHA
  `962D3086F4030D9BD5A9D46AF5E8DFA4A320A13BFCD14135B8B077AECDC31CC5`.
- Final body-time rows: `8227F1D8 body_ticks_total=4117139`,
  `body_ticks_delta=712900`, `entries_delta=9610`, `ticks_per_entry=74`,
  `code_size=24756`; `82490030 body_ticks_total=4007328`,
  `body_ticks_delta=678424`, `entries_delta=9610`, `ticks_per_entry=70`,
  `code_size=6716`.
- Final dynamic edge row: `8227F1D8 -> 82490030 calls_total=32107`,
  `body_ticks_total=4054641`, `body_ticks_delta=693438`,
  `ticks_per_call=72`.
- `826BFC7C` did not appear as a meaningful dynamic body-time or edge row.
- Final perf remains CPU/JIT-heavy: Main Thread `103%`, XMA Decoder `11.5%`,
  Audio Worker `7.6%`, GPU Commands `3.8%`, Draw Thread `0.0%`.
- Android UI frame stats are still not reliable game FPS. The final sample
  showed `Total frames rendered: 3`, which is not Blue Dragon route FPS.

Validation:

- Device capture wrote metadata, logcat, perf samples, and visible route
  screenshot.
- Fatal-marker search returned no matches.
- Dynamic call-edge/body-time rows landed and are internally consistent.
- The generic HIR call-path report was not used for this run because this
  capture intentionally did not include a fresh OptHIR dump.

Blockers:

- No FPS win or full-speed proof happened in this slice.
- We still need `82490030` HIR and internal block body-time before choosing a
  codegen experiment.

Next actions:

- Run a route-stabilized filtered HIR plus delayed body/block-time capture for
  `82490030`, keeping `8227F1D8` in the body-time filter as parent comparator.
- Suggested flags: `-DisassembleFunctions true -DisassembleFunctionFilter
  82490030 -Arm64SpeedProfileBodyTimeFilter "8227F1D8,82490030"
  -Arm64SpeedProfileBodyTimeAfterMs 120000 -Arm64SpeedProfileBlockFilter
  82490030 -Arm64SpeedProfileBlockBodyTime true`.
- Keep broad GPR/cache/vector/CR experiments default-off until the `82490030`
  HIR/block evidence names a real generated-code shape.

## 15:36-15:48 EDT - 82490030 Filtered Capture Black-Idle

Starting HEAD: `b06530e75`

Goal: run the first filtered `82490030` HIR plus delayed block body-time
capture after `8227F1D8 -> 82490030` proved to be the hot child edge.

Commands run:

```powershell
git status --short --branch
adb devices
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 25 -Arm64SpeedProfileMinDelta 1 -A64LseKernelLockFastpaths true -A64RtlLeaveFastpathAudit false -Arm64PermuteI32ZipFastpath true -Arm64ContextPromotionGprLiveInR1 false -DisassembleFunctions true -DisassembleFunctionFilter 82490030 -Arm64SpeedProfileBodyTimeFilter "8227F1D8,82490030" -Arm64SpeedProfileBodyTimeAfterMs 120000 -Arm64SpeedProfileBlockFilter "82490030" -Arm64SpeedProfileBlockBodyTime true -Arm64SpeedProfileThreadSnapshotOnIdle true -StopAppAfterCapture true
rg -n "Fatal signal|AndroidRuntime|VK_ERROR_DEVICE_LOST|GPU is hung|Unhandled|ASSERT|CHECK failed|FATAL" scratch\thor-debug\20260523-153726-speed-logcat.txt scratch\thor-debug\20260523-153726-speed-logcat-filtered.txt
rg -n -i "idle|owner_hint|last_global_owner|thread snapshot|processor debug|debug lock|A64 speed profile|Filtered function dump|82490030|8227F1D8" scratch\thor-debug\20260523-153726-speed-logcat.txt scratch\thor-debug\20260523-153726-speed-logcat-filtered.txt
```

Files changed:

- `docs/research/20260523-154117-82490030-filtered-capture-black-idle.md`
- `docs/worklogs/20260523.md`
- `AGENTS.md`
- `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md`
- `.agents/skills/xenia-working-fast-autonomy/SKILL.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `tools/thor/thor_codex_goal_loop.ps1`

Findings:

- Proof marker is still absent.
- Capture `scratch/thor-debug/20260523-153726-*` black-idled before opening
  route progress.
- It used APK SHA
  `962D3086F4030D9BD5A9D46AF5E8DFA4A320A13BFCD14135B8B077AECDC31CC5`.
- Fatal-marker search returned no matches.
- No `82490030` HIR, body-time row, or block body-time row landed.
- Counters went flat after early activity: `entry_delta=3803069` at
  `15:37:45`, `entry_delta=230348` at `15:38:00`, then `entry_delta=0` from
  `15:38:15` through the end. Body-time activated at `15:39:30` with
  `entry_delta=0`.
- Idle attribution fired: `last_global_owner_sys_tid=15739`,
  `last_global_owner_thread_id=F80002E8`, `owner_hint=hit`,
  `owner_hint_source=thread_id_or_handle`, `owner_hint_tid=00000016`,
  `owner_hint_handle=F80002E8`, `owner_hint_state=zombie`.
- Final perf is idle, not speed evidence: Main Thread `0.0%`, GPU Commands
  `0.0%`, Draw Thread `0.0%`.

Validation:

- Device capture wrote metadata, logcat, perf samples, and final screenshot.
- Fatal-marker search returned no matches.
- Idle-owner diagnostics identified a zombied XThread owner-hint path.

Blockers:

- This run is not usable `82490030` HIR/body-time evidence.
- The black-idle route needs a control before any generated-code experiment.

Next actions:

- Do not repeat the exact filtered `82490030` capture unchanged.
- Run a no-disassembly delayed body-time control for `8227F1D8,82490030` on the
  same APK and route.
- If the control reaches visible opening and logs comparable body rows, rerun
  the filtered `82490030` HIR/block capture.
- If the control also black-idles, improve zombie owner/native TID attribution
  before changing generated-code behavior.

## 15:46-15:56 EDT - 82490030 No-Disassembly Control Black-Idle

Starting HEAD: `f6f4c13ca`

Goal: run the no-disassembly delayed body-time control for `8227F1D8,82490030`
on the same APK and route, to test whether the prior filtered `82490030`
black-idle was caused by the disassembly/block-profile path.

Commands run:

```powershell
git status --short --branch
adb devices
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 25 -Arm64SpeedProfileMinDelta 1 -A64LseKernelLockFastpaths true -A64RtlLeaveFastpathAudit false -Arm64PermuteI32ZipFastpath true -Arm64ContextPromotionGprLiveInR1 false -Arm64SpeedProfileBodyTimeFilter "8227F1D8,82490030" -Arm64SpeedProfileBodyTimeAfterMs 120000 -Arm64SpeedProfileThreadSnapshotOnIdle true -StopAppAfterCapture true
rg -n "Fatal signal|AndroidRuntime|VK_ERROR_DEVICE_LOST|GPU is hung|Unhandled|ASSERT|CHECK failed|FATAL" scratch\thor-debug\20260523-154626-speed-logcat.txt scratch\thor-debug\20260523-154626-speed-logcat-filtered.txt
rg -n -i "idle|owner_hint|last_global_owner|thread snapshot|processor debug|debug lock|A64 speed profile|82490030|8227F1D8|body top" scratch\thor-debug\20260523-154626-speed-logcat.txt scratch\thor-debug\20260523-154626-speed-logcat-filtered.txt
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_continual_harness_review.ps1 -Mode Window
```

Files changed:

- `docs/research/20260523-155012-82490030-control-black-idle.md`
- `docs/worklogs/20260523.md`
- `AGENTS.md`
- `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md`
- `.agents/skills/xenia-working-fast-autonomy/SKILL.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `tools/thor/thor_codex_goal_loop.ps1`

Findings:

- Proof marker is still absent.
- Capture `scratch/thor-debug/20260523-154626-*` black-idled before visible
  opening route progress.
- It used the same APK SHA
  `962D3086F4030D9BD5A9D46AF5E8DFA4A320A13BFCD14135B8B077AECDC31CC5`.
- Fatal-marker search returned no matches.
- No `8227F1D8` or `82490030` body-time rows landed after delayed body-time
  activation.
- Counters showed early activity, then flatlined: `entry_delta=3843047` at
  `15:46:45`, `entry_delta=189129` at `15:47:00`, then `entry_delta=0` from
  `15:47:15` through the end.
- Idle attribution repeated the zombied owner signature:
  `last_global_owner_sys_tid=20126`, `last_global_owner_thread_id=F80002E8`,
  `owner_hint=hit`, `owner_hint_source=thread_id_or_handle`,
  `owner_hint_sys_tid=946810032`, `owner_hint_tid=00000016`,
  `owner_hint_handle=F80002E8`, `owner_hint_state=zombie`.
- Final perf is idle, not speed evidence: Main Thread `0.0%`, GPU Commands
  `0.0%`, Draw Thread `0.0%`.

Validation:

- Device capture wrote metadata, logcat, perf samples, and final screenshot.
- Fatal-marker search returned no matches.
- The continual-harness window was refreshed so the next loop sees both
  82490030 black-idle captures.

Blockers:

- This control reproduced the black-idle, so it is not safe to proceed to an
  `82490030` codegen patch.
- The current blocker is poor owner attribution for a stuck/zombied XThread
  path while the processor debug lock is busy.

Next actions:

- Do not rerun the exact filtered `82490030` capture or the exact
  no-disassembly control unchanged.
- Improve zombie owner/native TID attribution before changing generated code:
  report whether `last_global_owner_sys_tid` maps to a live Android thread
  under `/proc/<pid>/task/<tid>`, include native thread name/state if available,
  and clearly separate live native-owner data from stale XThread hint fields.

## 15:57-16:12 EDT - A64 Owner Native TID Attribution

Starting HEAD: `64a915c52`

Goal: improve the black-idle owner diagnostics before changing any generated
code, especially the stale-looking Android `pthread_t` value in
`owner_hint_sys_tid`.

Commands run:

```powershell
git status --short --branch
Test-Path scratch\thor-debug\latest-blue-dragon-full-speed-proof.json
rg -n "A64 thread snapshot skipped|owner_hint|last_global_owner|thread snapshot" src tools -S
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370
Get-FileHash android\android_studio_project\app\build\outputs\apk\github\debug\app-github-debug.apk -Algorithm SHA256
adb -s c3ca0370 shell cmd appops get jp.xenia.emulator.github.debug MANAGE_EXTERNAL_STORAGE
powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 100 -PerfSampleSeconds "60,95" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 25 -Arm64SpeedProfileMinDelta 1 -A64LseKernelLockFastpaths true -A64RtlLeaveFastpathAudit false -Arm64PermuteI32ZipFastpath true -Arm64ContextPromotionGprLiveInR1 false -Arm64SpeedProfileBodyTimeFilter "8227F1D8,82490030" -Arm64SpeedProfileBodyTimeAfterMs 120000 -Arm64SpeedProfileThreadSnapshotOnIdle true -StopAppAfterCapture true
rg -n "Fatal signal|AndroidRuntime|VK_ERROR_DEVICE_LOST|GPU is hung|Unhandled|ASSERT|CHECK failed|FATAL" scratch\thor-debug\20260523-160357-speed-logcat.txt scratch\thor-debug\20260523-160357-speed-logcat-filtered.txt
rg -n -i "A64 thread snapshot|idle|owner_hint|last_global_owner|native_owner|owner_hint_native|A64 speed profile summary|body top|8227F1D8|82490030" scratch\thor-debug\20260523-160357-speed-logcat.txt scratch\thor-debug\20260523-160357-speed-logcat-filtered.txt
```

Files changed:

- `src/xenia/cpu/backend/a64/a64_backend.cc`
- `src/xenia/cpu/processor.cc`
- `src/xenia/cpu/processor.h`
- `src/xenia/kernel/xthread.cc`
- `docs/research/20260523-160650-a64-owner-native-tid-attribution.md`
- `docs/worklogs/20260523.md`
- `AGENTS.md`
- `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md`
- `.agents/skills/xenia-working-fast-autonomy/SKILL.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `tools/thor/thor_codex_goal_loop.ps1`

Findings:

- Proof marker is still absent.
- `Thread::system_id()` on Android is `pthread_t`-shaped, while
  `last_global_owner_sys_tid` records Linux `gettid()`.
- Added `Processor::OnThreadNativeStarted()` so XThread startup refreshes the
  debug hint with the real native TID.
- Added `/proc/self/task/<tid>` liveness to the A64 idle skip log for the lock
  owner and hint TIDs.
- `NativeCore` passed.
- `FullDeploy` passed and installed APK SHA
  `2E3D88F46BB709AA3A869634C24219FBBA0568695C1F4902693132701CF9EBE5`.
- `MANAGE_EXTERNAL_STORAGE` remained allowed.
- Validation capture `scratch/thor-debug/20260523-160357-*` black-idled again
  with a black final screenshot and clean fatal-marker search.
- Counters went flat by `16:04:46`.
- New owner line: `last_global_owner_sys_tid=28245`,
  `last_global_owner_thread_id=F80002EC`, `owner_hint=hit`,
  `owner_hint_source=system_tid`, `owner_hint_sys_tid=28245`,
  `owner_hint_tid=00000016`, `owner_hint_handle=F80002EC`,
  `owner_hint_state=zombie`, `native_owner_live=false`, and
  `owner_hint_native_live=false`.

Validation:

- Native build passed.
- Full deploy passed.
- Device capture wrote metadata, logcat, perf samples, and final screenshot.
- Fatal-marker search returned no matches.
- The new fields appeared in the black-idle log and eliminated the stale
  `pthread_t`/native-TID ambiguity.

Blockers:

- The lock owner now appears to be a dead native thread / zombied XThread, not
  a live stuck generated-code thread.
- The current A64 idle line still cannot prove whether this is stale
  last-owner bookkeeping or a thread exiting while holding the global critical
  region.

Next actions:

- Do not rerun the exact `82490030` captures unchanged.
- Do not patch `8227F1D8` or `82490030` generated code from this evidence.
- Add diagnostic global critical-region ownership/lifetime attribution around
  the processor debug lock, so the next black-idle can distinguish stale
  last-owner bookkeeping from a real lock lifetime bug.
