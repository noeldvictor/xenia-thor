# Worklog 2026-05-19

## 2026-05-19 00:00-00:02 EDT - Shader Constant Trace

Scope:

- Add `vulkan_trace_shader_constants` and a budgeted trace counter.
- Route the knob through Android activity extras and the Thor debug script.
- Log active pixel shader constant maps and raw/float values next to Blue
  Dragon draw, texture, source, and resolve traces.

Files changed:

- `src/xenia/gpu/gpu_flags.cc`
- `src/xenia/gpu/gpu_flags.h`
- `src/xenia/gpu/vulkan/vulkan_command_processor.cc`
- `src/xenia/gpu/vulkan/vulkan_command_processor.h`
- `android/android_studio_project/app/src/main/java/jp/xenia/emulator/EmulatorActivity.java`
- `tools/thor/thor_xenia_debug.ps1`
- `AGENTS.md`
- `docs/research/20260519-000221-blue-dragon-shader-constant-trace.md`

Commands:

- `git diff --check`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonLiveCapture -DeviceSerial c3ca0370 -Arm64MiniJit false -MmapAddressHigh 8 -GpuTraceSwap true -GpuTracePacketBudget 120 -VulkanTraceDrawState true -VulkanTraceDrawStateBudget 280 -VulkanTraceShaderConstants true -VulkanTraceShaderConstantsBudget 64 -VulkanTraceTextureSourceChecksum true -VulkanTraceTextureSourceChecksumBudget 64 -VulkanTraceCopyState true -VulkanTraceCopyStateBudget 32 -VulkanTraceResolveChecksum true -VulkanTraceResolveChecksumBudget 20 -VulkanPresentScoredResolveOnSwap true -VulkanPresentScoredResolveRejectClearLike true -VulkanPresentScoredResolveBudget 120 -LiveCaptureSeconds 65`

Validation:

- `git diff --check` passed with only normal LF/CRLF warnings.
- Native core build passed.
- Full deploy passed and installed on Thor.
- Capture:
  - `scratch/thor-debug/20260519-000002-*`
- APK SHA-256:
  `198B4AAEAB8406EB820284CCD3C3C2A28C96801A5FE005991F5BBB05F4AC03EF`.
- No fatal signal, AndroidRuntime crash, GPU-hung line, or ARM64 fallback.

Findings:

- Shader constants were captured for the visible fullscreen pixel shader
  family.
- `05775DE8A2B0B3F5` uses only three float constants and no bool or loop
  constants.
- `0ABADD9DA4373CBA` uses thirteen float constants and no bool or loop
  constants.
- `1E70EB9513D670C9` has no float, bool, or loop constants.
- The constants are not globally missing or zero, while the output remains
  clear-like.

Conclusion:

- The current Blue Dragon wall is now past descriptor setup, source memory
  readback, and gross constant upload.
- Next lane: dump and inspect Xenos microcode / translated SPIR-V for the
  visible fullscreen shader hashes.

## 2026-05-19 00:08-00:15 EDT - Shader Dump Automation And Presenter Proof

Scope:

- Route `dump_shaders` from ADB extras into the native app.
- Dump Vulkan translated shader binaries when `dump_shaders` is enabled.
- Add `-DumpShaders auto` to the Thor debug script and pull shader dumps into
  the matching capture folder.
- Run a solid guest-output presenter sanity check.

Files changed:

- `android/android_studio_project/app/src/main/java/jp/xenia/emulator/EmulatorActivity.java`
- `src/xenia/gpu/vulkan/vulkan_pipeline_cache.cc`
- `tools/thor/thor_xenia_debug.ps1`
- `docs/research/20260519-001514-blue-dragon-shader-dump-presenter-proof.md`
- `AGENTS.md`

Commands:

- `git diff --check`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonLiveCapture -DeviceSerial c3ca0370 -Arm64MiniJit false -MmapAddressHigh 8 -DumpShaders auto -GpuTraceSwap true -GpuTracePacketBudget 120 -VulkanTraceDrawState true -VulkanTraceDrawStateBudget 260 -VulkanTraceShaderConstants true -VulkanTraceShaderConstantsBudget 48 -VulkanTraceTextureSourceChecksum true -VulkanTraceTextureSourceChecksumBudget 48 -VulkanTraceCopyState true -VulkanTraceCopyStateBudget 24 -VulkanTraceResolveChecksum true -VulkanTraceResolveChecksumBudget 16 -VulkanPresentScoredResolveOnSwap true -VulkanPresentScoredResolveRejectClearLike true -VulkanPresentScoredResolveBudget 100 -LiveCaptureSeconds 60`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonLiveCapture -DeviceSerial c3ca0370 -Arm64MiniJit false -MmapAddressHigh 8 -GpuTraceSwap true -GpuTracePacketBudget 40 -VulkanDebugSolidGuestOutput true -LiveCaptureSeconds 25`

Validation:

- Native core build passed.
- Full deploy passed and installed on Thor.
- Shader dump capture:
  - `scratch/thor-debug/20260519-001033-*`
  - `scratch/thor-debug/20260519-001033-shader-dumps`
- Solid presenter capture:
  - `scratch/thor-debug/20260519-001412-*`
- APK SHA-256:
  `9B11EE7998FECBCE637B7866A0E73CB9C70B06E2E00BD185C4313A0A6747CCB2`.
- No fatal signal, AndroidRuntime crash, GPU-hung line, or ARM64 fallback.

Findings:

- The shader dump pull produced 41 shader files: Xenos ucode binaries,
  ucode text disassembly, and Vulkan SPIR-V binaries.
- The `0ABADD9DA4373CBA` shader branch ladder can sample non-clear DXT fetch
  7, but the captured `c26.x = 2.0` path exits before fetch 7 contributes.
- The final fullscreen composite path is therefore mostly sampling already-flat
  render-chain inputs.
- `vulkan_debug_solid_guest_output=true` produced a visible magenta frame on
  the Thor screenshot and logged 123 solid guest-output submissions.

Conclusion:

- Android/Vulkan guest-output presentation is alive.
- The current Blue Dragon black-frame wall is upstream of presentation:
  earlier render-target generation, resolve behavior, EDRAM state, or guest
  correctness before the final composite.

## 2026-05-19 00:28-00:36 EDT - Targeted Vertex Fetch Trace

Scope:

- Add a budgeted vertex fetch trace for Vulkan draws.
- Add a vertex shader hash filter so focused Blue Dragon probes can skip early
  setup draws and reach the final fullscreen blit.
- Route the new flags through Android launch extras and the Thor debug script.

Files changed:

- `src/xenia/gpu/gpu_flags.cc`
- `src/xenia/gpu/gpu_flags.h`
- `src/xenia/gpu/vulkan/vulkan_command_processor.cc`
- `src/xenia/gpu/vulkan/vulkan_command_processor.h`
- `android/android_studio_project/app/src/main/java/jp/xenia/emulator/EmulatorActivity.java`
- `tools/thor/thor_xenia_debug.ps1`
- `AGENTS.md`
- `docs/research/20260519-003650-blue-dragon-targeted-vertex-fetch-trace.md`

Commands:

- `git diff --check`
- `[System.Management.Automation.PSParser]::Tokenize((Get-Content -Raw tools\thor\thor_xenia_debug.ps1), [ref]$null)`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonLiveCapture -DeviceSerial c3ca0370 -Arm64MiniJit false -MmapAddressHigh 8 -MountCache true -TimeScalar 1.0 -HidNopConnected true -HidNopButtons 'START,A' -HidNopButtonsDelayMs 45000 -HidNopButtonsHoldMs 5000 -GpuTraceSwap true -GpuTracePacketBudget 120 -GpuTraceSwapFrontbufferChecksum true -GpuTraceSwapFrontbufferChecksumBudget 12 -GpuTraceSwapRenderTargets true -GpuTraceSwapRenderTargetsBudget 18 -VulkanTraceDrawState true -VulkanTraceDrawStateBudget 140 -VulkanTraceVertexFetchChecksum true -VulkanTraceVertexFetchChecksumBudget 90 -VulkanTraceTextureSourceChecksum true -VulkanTraceTextureSourceChecksumBudget 20 -VulkanTraceCopyState true -VulkanTraceCopyStateBudget 80 -VulkanTraceResolveChecksum true -VulkanTraceResolveChecksumBudget 42 -VulkanPresentScoredResolveOnSwap true -VulkanPresentScoredResolveRejectClearLike true -VulkanPresentScoredResolveMinWidth 80 -VulkanPresentScoredResolveMinHeight 45 -VulkanPresentScoredResolveBudget 160 -LiveCaptureSeconds 75`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonLiveCapture -DeviceSerial c3ca0370 -Arm64MiniJit false -MmapAddressHigh 8 -MountCache true -TimeScalar 1.0 -HidNopConnected true -HidNopButtons 'START,A' -HidNopButtonsDelayMs 50000 -HidNopButtonsHoldMs 5000 -GpuTraceSwap true -GpuTracePacketBudget 120 -GpuTraceSwapFrontbufferChecksum true -GpuTraceSwapFrontbufferChecksumBudget 12 -GpuTraceSwapRenderTargets true -GpuTraceSwapRenderTargetsBudget 18 -VulkanTraceVertexFetchChecksum true -VulkanTraceVertexFetchShaderFilter 5CF1EE2E3C82546B -VulkanTraceVertexFetchChecksumBudget 24 -VulkanTraceCopyState true -VulkanTraceCopyStateBudget 70 -VulkanTraceResolveChecksum true -VulkanTraceResolveChecksumBudget 36 -VulkanPresentScoredResolveOnSwap true -VulkanPresentScoredResolveRejectClearLike true -VulkanPresentScoredResolveMinWidth 80 -VulkanPresentScoredResolveMinHeight 45 -VulkanPresentScoredResolveBudget 220 -LiveCaptureSeconds 110`

Validation:

- `git diff --check` passed with only normal LF/CRLF warnings.
- PowerShell parser check passed for `tools/thor/thor_xenia_debug.ps1`.
- Native core build passed.
- Full deploy passed and installed on Thor.
- Captures:
  - `scratch/thor-debug/20260519-002829-*`
  - `scratch/thor-debug/20260519-003312-*`
- Latest APK SHA-256:
  `1D66CE51565B842A3C2A203E066AFF1C5F14BE7CFBA1ABBB8423907F7A5E38BE`.
- No fatal signal, AndroidRuntime crash, GPU-hung line, or ARM64 fallback.

Findings:

- The broad trace proved the need for a shader filter because early
  `0A6D1DD7767FDF27` setup draws burned the budget before the final blit.
- The focused trace reached the final `5CF1EE2E3C82546B` vertex shader paired
  with pixel shader `9567C79307ACC6F5`.
- The final blit's vertex stream has sane position and UV data.
- The final blit's color multiplier is not zero; it appears as `FFFFFFFF` in
  the first pass and later as values like `FFFFFF00` / big-endian `00FFFFFF`.
- The source render-chain texture feeding the final pass is already zero or
  clear-like before the final blit writes to `1D14C000` and then `1CA1C000`.

Conclusion:

- The final fullscreen quad setup is not the current Blue Dragon black-frame
  wall.
- Next lane: targeted draw-state and texture-source shader filters for the
  upstream render passes that populate `1DC14000`, `1D14C000`, `1CA1C000`, and
  the scored-present candidates before the final composite.

## 2026-05-19 00:44-00:55 EDT - Targeted Shader Filter Trace

Scope:

- Add shader-hash filters for Vulkan draw-state, shader-constant, and
  texture-source tracing.
- Route the filters through Android launch extras and the Thor debug script.
- Use focused Blue Dragon captures to chase the render-chain pass that zeros
  `1DC14000` before the final fullscreen blit.

Files changed:

- `src/xenia/gpu/gpu_flags.cc`
- `src/xenia/gpu/gpu_flags.h`
- `src/xenia/gpu/vulkan/vulkan_command_processor.cc`
- `android/android_studio_project/app/src/main/java/jp/xenia/emulator/EmulatorActivity.java`
- `tools/thor/thor_xenia_debug.ps1`
- `AGENTS.md`
- `docs/research/20260519-005540-blue-dragon-targeted-shader-filter-trace.md`
- `docs/worklogs/20260519.md`

Commands:

- `git diff --check`
- `[System.Management.Automation.PSParser]::Tokenize((Get-Content -Raw tools\thor\thor_xenia_debug.ps1), [ref]$errors)`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonLiveCapture -DeviceSerial c3ca0370 -Arm64MiniJit false -MmapAddressHigh 8 -MountCache true -TimeScalar 1.0 -HidNopConnected true -HidNopButtons 'START,A' -HidNopButtonsDelayMs 50000 -HidNopButtonsHoldMs 5000 -GpuTraceSwap true -GpuTracePacketBudget 140 -GpuTraceSwapFrontbufferChecksum true -GpuTraceSwapFrontbufferChecksumBudget 12 -GpuTraceSwapRenderTargets true -GpuTraceSwapRenderTargetsBudget 18 -VulkanTraceDrawState true -VulkanTraceDrawShaderFilter '05775DE8A2B0B3F5,0ABADD9DA4373CBA,1B132051B5504DA9,9567C79307ACC6F5' -VulkanTraceDrawStateBudget 140 -VulkanTraceTextureSourceChecksum true -VulkanTraceTextureSourceShaderFilter '05775DE8A2B0B3F5,0ABADD9DA4373CBA,1B132051B5504DA9,9567C79307ACC6F5' -VulkanTraceTextureSourceChecksumBudget 80 -VulkanTraceCopyState true -VulkanTraceCopyStateBudget 90 -VulkanTraceResolveChecksum true -VulkanTraceResolveChecksumBudget 60 -VulkanPresentScoredResolveOnSwap true -VulkanPresentScoredResolveRejectClearLike true -VulkanPresentScoredResolveMinWidth 80 -VulkanPresentScoredResolveMinHeight 45 -VulkanPresentScoredResolveBudget 220 -LiveCaptureSeconds 95`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonLiveCapture -DeviceSerial c3ca0370 -Arm64MiniJit false -MmapAddressHigh 8 -MountCache true -TimeScalar 1.0 -HidNopConnected true -HidNopButtons 'START,A' -HidNopButtonsDelayMs 50000 -HidNopButtonsHoldMs 5000 -DumpShaders auto -GpuTraceSwap true -GpuTracePacketBudget 140 -GpuTraceSwapFrontbufferChecksum true -GpuTraceSwapFrontbufferChecksumBudget 12 -GpuTraceSwapRenderTargets true -GpuTraceSwapRenderTargetsBudget 18 -VulkanTraceDrawState true -VulkanTraceDrawShaderFilter '05775DE8A2B0B3F5,0ABADD9DA4373CBA,2A0674C564A8A8C5,57B736C8B5D4E953,B02CC5F55AD0D140,9567C79307ACC6F5' -VulkanTraceDrawStateBudget 180 -VulkanTraceShaderConstants true -VulkanTraceShaderConstantsShaderFilter '05775DE8A2B0B3F5,0ABADD9DA4373CBA,2A0674C564A8A8C5,57B736C8B5D4E953,B02CC5F55AD0D140,9567C79307ACC6F5' -VulkanTraceShaderConstantsBudget 96 -VulkanTraceTextureSourceChecksum true -VulkanTraceTextureSourceShaderFilter '05775DE8A2B0B3F5,0ABADD9DA4373CBA,2A0674C564A8A8C5,57B736C8B5D4E953,B02CC5F55AD0D140,9567C79307ACC6F5' -VulkanTraceTextureSourceChecksumBudget 120 -VulkanTraceCopyState true -VulkanTraceCopyStateBudget 100 -VulkanTraceResolveChecksum true -VulkanTraceResolveChecksumBudget 70 -VulkanPresentScoredResolveOnSwap true -VulkanPresentScoredResolveRejectClearLike true -VulkanPresentScoredResolveMinWidth 80 -VulkanPresentScoredResolveMinHeight 45 -VulkanPresentScoredResolveBudget 240 -LiveCaptureSeconds 95`

Validation:

- `git diff --check` passed with only normal LF/CRLF warnings.
- PowerShell parser check passed for `tools/thor/thor_xenia_debug.ps1`.
- Full deploy passed and installed on Thor.
- Captures:
  - `scratch/thor-debug/20260519-004401-*`
  - `scratch/thor-debug/20260519-004826-*`
  - `scratch/thor-debug/20260519-004826-shader-dumps`
- Latest APK SHA-256:
  `03F2DD4495DD8E558473377871C3C5232D38BD10768CB639925DFDBB7A91A4D1`.
- No fatal signal, AndroidRuntime crash, GPU hang, or ARM64 execution fallback.

Findings:

- `05775DE8A2B0B3F5` writes nonzero clear-like `1DC14000` content.
- The following `0ABADD9DA4373CBA` pass samples that nonzero source and resolves
  `1DC14000` back to all zero.
- Later post passes sample `1DC14000` after it is already zero, and the final
  `9567C79307ACC6F5` blit receives zero input.
- The `0ABADD9DA4373CBA` ucode path is gated by `c26.x`; with `c26.x = 2.0`,
  the non-clear packed DXT fetch 7 is described but not on the observed output
  path.
- Runtime also shows successful `D:\pack\!necessity.ipk` access followed by
  failed loose `D:\!necessity\font\...` probes and `cache:\pack` activity,
  making VFS/cache behavior a parallel suspect.

Conclusion:

- The first concrete GPU zeroing point is now around `0ABADD9DA4373CBA`, not
  the final presenter or final blit.
- Next lane: audit Blue Dragon VFS/cache behavior, then return to a minimal
  `0ABADD9DA4373CBA` shader-output probe if file/cache behavior looks normal.

## 2026-05-19 01:07-01:23 EDT - Pixel Output Override Probe

Scope:

- Add a research-only pixel shader output override for hash-targeted Vulkan
  shader-export probes.
- Route the override through Android launch extras and the Thor debug script.
- Use it to split the `0ABADD9DA4373CBA` shader/export path from later
  render-chain zeroing.

Files changed:

- `src/xenia/gpu/gpu_flags.cc`
- `src/xenia/gpu/gpu_flags.h`
- `src/xenia/gpu/spirv_shader_translator.cc`
- `src/xenia/gpu/vulkan/vulkan_command_processor.cc`
- `android/android_studio_project/app/src/main/java/jp/xenia/emulator/EmulatorActivity.java`
- `tools/thor/thor_xenia_debug.ps1`
- `AGENTS.md`
- `docs/research/20260519-012358-blue-dragon-pixel-output-override-probe.md`
- `docs/worklogs/20260519.md`

Commands:

- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonLiveCapture -DeviceSerial c3ca0370 -Arm64MiniJit false -MmapAddressHigh 8 -MountCache true -TimeScalar 1.0 -HidNopConnected true -HidNopButtons 'START,A' -HidNopButtonsDelayMs 50000 -HidNopButtonsHoldMs 5000 -VulkanDebugPixelShaderOutputFilter 0ABADD9DA4373CBA -VulkanDebugPixelShaderOutputMode 1 -VulkanForceSigned2101010UnormFallback true -GpuTraceSwap true -GpuTracePacketBudget 120 -GpuTraceSwapFrontbufferChecksum true -GpuTraceSwapFrontbufferChecksumBudget 10 -GpuTraceSwapRenderTargets true -GpuTraceSwapRenderTargetsBudget 16 -VulkanTraceDrawState true -VulkanTraceDrawShaderFilter '05775DE8A2B0B3F5,0ABADD9DA4373CBA,9567C79307ACC6F5' -VulkanTraceDrawStateBudget 160 -VulkanTraceTextureSourceChecksum true -VulkanTraceTextureSourceShaderFilter '0ABADD9DA4373CBA,9567C79307ACC6F5' -VulkanTraceTextureSourceChecksumBudget 64 -VulkanTraceCopyState true -VulkanTraceCopyStateBudget 100 -VulkanTraceResolveChecksum true -VulkanTraceResolveChecksumBudget 120 -VulkanPresentScoredResolveOnSwap true -VulkanPresentScoredResolveRejectClearLike false -VulkanPresentScoredResolveMinWidth 80 -VulkanPresentScoredResolveMinHeight 45 -VulkanPresentScoredResolveBudget 260 -LiveCaptureSeconds 90`

Validation:

- Full deploy passed and installed on Thor.
- Capture:
  - `scratch/thor-debug/20260519-011906-*`
- APK SHA-256:
  `B92175A0BC538E8284C2C033B595AD9FBF1F9A959D217CDB82EEF98C7796E633`.
- No fatal signal, AndroidRuntime crash, GPU hang, or ARM64 execution fallback.

Findings:

- The new cvars reached the native side:
  `debug_ps_output_filter=0ABADD9DA4373CBA debug_ps_output_mode=1`.
- `VulkanTextureCache` signed `2_10_10_10` UNORM fallback engaged, but the
  preceding run showed that fallback alone does not fix the black output.
- Forcing `0ABADD9DA4373CBA` to magenta makes the `1DC14000` resolve nonzero:
  `first_sample=FF03F0FF`.
- A later copy/draw/resolve sequence to the same `raw_dest_base=1DC14000`
  changes the target back to all zero before the final `9567C79307ACC6F5` blit.

Conclusion:

- The targeted shader/export path can write nonzero color; it is not the
  immediate black-frame wall.
- Next lane: instrument copy/resolve logs with recent draw sequence and shader
  hashes so the exact post-`0ABADD9DA4373CBA` zeroing writer can be identified.

## 2026-05-19 01:24-01:41 EDT - Copy/Draw Trace Tags And B02 Split

Scope:

- Add copy and draw sequence tags to Vulkan copy trace lines.
- Build and deploy the trace-tagged native core to Thor.
- Use forced shader-output captures to identify the post-`0ABADD9DA4373CBA`
  zeroing chain.

Files changed:

- `src/xenia/gpu/vulkan/vulkan_command_processor.cc`
- `src/xenia/gpu/vulkan/vulkan_command_processor.h`
- `AGENTS.md`
- `docs/research/20260519-014135-blue-dragon-post-abadd-shader-chain.md`
- `docs/worklogs/20260519.md`

Commands:

- `git diff --check`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonLiveCapture -DeviceSerial c3ca0370 -Arm64MiniJit false -MmapAddressHigh 8 -MountCache true -TimeScalar 1.0 -HidNopConnected true -HidNopButtons 'START,A' -HidNopButtonsDelayMs 50000 -HidNopButtonsHoldMs 5000 -VulkanDebugPixelShaderOutputFilter 0ABADD9DA4373CBA -VulkanDebugPixelShaderOutputMode 1 -GpuTraceSwap true -GpuTracePacketBudget 120 -GpuTraceSwapFrontbufferChecksum true -GpuTraceSwapFrontbufferChecksumBudget 4 -GpuTraceSwapRenderTargets true -GpuTraceSwapRenderTargetsBudget 8 -VulkanTraceDrawState true -VulkanTraceDrawShaderFilter '05775DE8A2B0B3F5,0ABADD9DA4373CBA,57B736C8B5D4E953,B02CC5F55AD0D140,9567C79307ACC6F5' -VulkanTraceDrawStateBudget 130 -VulkanTraceShaderConstants true -VulkanTraceShaderConstantsShaderFilter '57B736C8B5D4E953,B02CC5F55AD0D140' -VulkanTraceShaderConstantsBudget 80 -VulkanTraceTextureSourceChecksum true -VulkanTraceTextureSourceShaderFilter '0ABADD9DA4373CBA,57B736C8B5D4E953,B02CC5F55AD0D140,9567C79307ACC6F5' -VulkanTraceTextureSourceChecksumBudget 100 -VulkanTraceCopyState true -VulkanTraceCopyStateBudget 90 -VulkanTraceResolveChecksum true -VulkanTraceResolveChecksumBudget 120 -VulkanPresentScoredResolveOnSwap true -VulkanPresentScoredResolveRejectClearLike false -VulkanPresentScoredResolveBudget 180 -LiveCaptureSeconds 70`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonLiveCapture -DeviceSerial c3ca0370 -Arm64MiniJit false -MmapAddressHigh 8 -MountCache true -TimeScalar 1.0 -HidNopConnected true -HidNopButtons 'START,A' -HidNopButtonsDelayMs 50000 -HidNopButtonsHoldMs 5000 -VulkanDebugPixelShaderOutputFilter 57B736C8B5D4E953 -VulkanDebugPixelShaderOutputMode 1 -GpuTraceSwap true -GpuTracePacketBudget 120 -GpuTraceSwapFrontbufferChecksum true -GpuTraceSwapFrontbufferChecksumBudget 4 -GpuTraceSwapRenderTargets true -GpuTraceSwapRenderTargetsBudget 8 -VulkanTraceDrawState true -VulkanTraceDrawShaderFilter '05775DE8A2B0B3F5,0ABADD9DA4373CBA,57B736C8B5D4E953,B02CC5F55AD0D140,9567C79307ACC6F5' -VulkanTraceDrawStateBudget 130 -VulkanTraceShaderConstants true -VulkanTraceShaderConstantsShaderFilter '57B736C8B5D4E953,B02CC5F55AD0D140' -VulkanTraceShaderConstantsBudget 80 -VulkanTraceTextureSourceChecksum true -VulkanTraceTextureSourceShaderFilter '57B736C8B5D4E953,B02CC5F55AD0D140,9567C79307ACC6F5' -VulkanTraceTextureSourceChecksumBudget 100 -VulkanTraceCopyState true -VulkanTraceCopyStateBudget 90 -VulkanTraceResolveChecksum true -VulkanTraceResolveChecksumBudget 120 -VulkanPresentScoredResolveOnSwap true -VulkanPresentScoredResolveRejectClearLike false -VulkanPresentScoredResolveBudget 180 -LiveCaptureSeconds 70`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonLiveCapture -DeviceSerial c3ca0370 -Arm64MiniJit false -MmapAddressHigh 8 -MountCache true -TimeScalar 1.0 -HidNopConnected true -HidNopButtons 'START,A' -HidNopButtonsDelayMs 50000 -HidNopButtonsHoldMs 5000 -VulkanDebugPixelShaderOutputFilter B02CC5F55AD0D140 -VulkanDebugPixelShaderOutputMode 1 -GpuTraceSwap true -GpuTracePacketBudget 120 -GpuTraceSwapFrontbufferChecksum true -GpuTraceSwapFrontbufferChecksumBudget 4 -GpuTraceSwapRenderTargets true -GpuTraceSwapRenderTargetsBudget 8 -VulkanTraceDrawState true -VulkanTraceDrawShaderFilter '05775DE8A2B0B3F5,0ABADD9DA4373CBA,57B736C8B5D4E953,B02CC5F55AD0D140,9567C79307ACC6F5' -VulkanTraceDrawStateBudget 130 -VulkanTraceShaderConstants true -VulkanTraceShaderConstantsShaderFilter '57B736C8B5D4E953,B02CC5F55AD0D140' -VulkanTraceShaderConstantsBudget 80 -VulkanTraceTextureSourceChecksum true -VulkanTraceTextureSourceShaderFilter 'B02CC5F55AD0D140,9567C79307ACC6F5' -VulkanTraceTextureSourceChecksumBudget 100 -VulkanTraceCopyState true -VulkanTraceCopyStateBudget 90 -VulkanTraceResolveChecksum true -VulkanTraceResolveChecksumBudget 120 -VulkanPresentScoredResolveOnSwap true -VulkanPresentScoredResolveRejectClearLike false -VulkanPresentScoredResolveBudget 180 -LiveCaptureSeconds 70`

Validation:

- `git diff --check` passed with only normal LF/CRLF warnings.
- Native core build passed.
- Full deploy passed and installed on Thor.
- Captures:
  - `scratch/thor-debug/20260519-013208-*`
  - `scratch/thor-debug/20260519-013500-*`
  - `scratch/thor-debug/20260519-013719-*`
- Latest APK SHA-256:
  `44B87E30B1141C339D5CC18C9BDBDCFE866D85F1B1E2EB2DA1250D2343708A19`.
- No fatal signal, AndroidRuntime crash, GPU hang, or ARM64 execution fallback.

Findings:

- Forced `0ABADD9DA4373CBA` writes `1DC14000` nonzero, then
  `57B736C8B5D4E953` writes it back to zero.
- Forced `57B736C8B5D4E953` writes `1DC14000` nonzero and produces a visible
  dark-blue debug frame, then `B02CC5F55AD0D140` writes it back to zero.
- Forced `B02CC5F55AD0D140` writes `1DC14000` nonzero; the final
  `9567C79307ACC6F5` pass samples it, writes `1D14C000` nonzero, and then
  writes the official frontbuffer nonzero.
- The final fullscreen pass, resolve-to-frontbuffer chain, and Android
  presentation path can carry nonzero data on Thor.
- The generic frontbuffer checksum reads CPU guest memory and is not enough by
  itself to judge Vulkan shared-memory state.

Conclusion:

- The immediate Blue Dragon black-frame wall is the unforced
  `B02CC5F55AD0D140` shader pass, or the texture/constant/boolean state feeding
  it.
- Next lane: inspect B02 translation and format 7 source semantics, especially
  `2_10_10_10` fetch normalization, DXT input, branch booleans, log/exp/NaN
  handling, and export clamping.

## 2026-05-19 01:51-02:23 EDT - B02 Fetch Probes And Title Prompt

Scope:

- Add secondary shader-output filters and richer texture-fetch debug modes.
- Add Adreno `2_10_10_10` fallback probes.
- Test whether B02 samples black or whether post-fetch processing crushes the
  value.
- Add a default-off texture exponent-adjust bypass to prove the title-screen
  hypothesis.

Files changed:

- `android/android_studio_project/app/src/main/java/jp/xenia/emulator/EmulatorActivity.java`
- `src/xenia/gpu/gpu_flags.cc`
- `src/xenia/gpu/gpu_flags.h`
- `src/xenia/gpu/spirv_shader_translator.cc`
- `src/xenia/gpu/spirv_shader_translator.h`
- `src/xenia/gpu/spirv_shader_translator_fetch.cc`
- `src/xenia/gpu/vulkan/vulkan_command_processor.cc`
- `src/xenia/gpu/vulkan/vulkan_texture_cache.cc`
- `tools/thor/thor_xenia_debug.ps1`
- `AGENTS.md`
- `README.md`
- `docs/research/20260519-022333-blue-dragon-title-exp-adjust-probe.md`
- `docs/worklogs/20260519.md`

Commands:

- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonLiveCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 75 -EmitInlineMmioChecks true -GpuTraceSwap true -GpuTracePacketBudget 260 -GpuInterruptOnRingIdle false -GpuBlueDragonKickWaitToken false -VulkanTraceCopyState true -VulkanTraceCopyStateBudget 80 -VulkanTraceDrawState true -VulkanTraceDrawStateBudget 120 -VulkanTraceDrawShaderFilter "57B736C8B5D4E953,B02CC5F55AD0D140,9567C79307ACC6F5" -VulkanTraceTextureSourceChecksum true -VulkanTraceTextureSourceChecksumBudget 120 -VulkanTraceTextureSourceShaderFilter "57B736C8B5D4E953,B02CC5F55AD0D140,9567C79307ACC6F5" -VulkanTraceSwapSharedMemoryChecksum true -VulkanTraceSwapSharedMemoryChecksumBudget 30 -VulkanForceSigned2101010UnormFallback true -VulkanForce2101010Rgba8Fallback false -VulkanDebugPixelShaderOutputFilter 57B736C8B5D4E953 -VulkanDebugPixelShaderOutputMode 1 -VulkanDebugPixelShaderOutputSecondaryFilter B02CC5F55AD0D140 -VulkanDebugPixelShaderOutputSecondaryMode 10`
- Same launch shape with `-VulkanDebugPixelShaderOutputSecondaryMode 30`.
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonLiveCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 75 -EmitInlineMmioChecks true -GpuTraceSwap true -GpuTracePacketBudget 260 -GpuInterruptOnRingIdle false -GpuBlueDragonKickWaitToken false -VulkanTraceCopyState true -VulkanTraceCopyStateBudget 90 -VulkanTraceDrawState true -VulkanTraceDrawStateBudget 160 -VulkanTraceDrawShaderFilter "0ABADD9DA4373CBA,57B736C8B5D4E953,B02CC5F55AD0D140,9567C79307ACC6F5" -VulkanTraceTextureSourceChecksum true -VulkanTraceTextureSourceChecksumBudget 140 -VulkanTraceTextureSourceShaderFilter "0ABADD9DA4373CBA,57B736C8B5D4E953,B02CC5F55AD0D140,9567C79307ACC6F5" -VulkanTraceSwapSharedMemoryChecksum true -VulkanTraceSwapSharedMemoryChecksumBudget 40 -VulkanForceSigned2101010UnormFallback true -VulkanForce2101010Rgba8Fallback false -VulkanDebugTextureFetchDisableExpAdjust true`

Validation:

- Native core build passed.
- Full deploy passed and installed on Thor.
- APK SHA-256 for the title-reaching build:
  `6B283A738F3BAE475965BF48FAB63366ED630D2604048EB4F2837D28BD6251B7`.
- Captures:
  - `scratch/thor-debug/20260519-021201-*`
  - `scratch/thor-debug/20260519-021355-*`
  - `scratch/thor-debug/20260519-022127-*`
- Title screenshot:
  `scratch/thor-debug/20260519-022127-screenshot.png`

Findings:

- B02 mode 10 stayed black and did not show the yellow missing-var sentinel, so
  the post-processed texture fetch result was genuinely near zero.
- B02 mode 30 showed nonzero raw unsigned sampling and produced a visible red
  debug frame, proving raw Adreno sampling was not the immediate failure.
- B02 fetch word `003E0003` carries a result exponent adjustment of `-16`.
- The temporary `vulkan_debug_texture_fetch_disable_exp_adjust=true` run reached
  Blue Dragon's `press START` title prompt without shader-output forcing.
- The title-reaching run had nonzero official swap shared-memory checksum:
  `first_sample=FF000000`, `nonzero=1780`.

Conclusion:

- The immediate title-screen wall was the `2_10_10_10_FLOAT` render-chain
  resolve/fetch decode path combined with result exponent adjustment, not final
  presentation or raw texture sampling.
- `vulkan_debug_texture_fetch_disable_exp_adjust` is a research-only proof
  knob, not a compatibility fix. Next step is a targeted 7e3-aware decode path
  for the relevant `2_10_10_10` render-chain textures.

## 2026-05-19 13:19-13:35 EDT - Dword3 Exp Fix, Clean Title, Codex Hook Research

Scope:

- Replace the broad title-screen texture exponent bypass with a focused SPIR-V
  fetch-constant fix.
- Hide the Android emulator OSD during proof captures so game output is
  unobscured.
- Research Codex hooks / automations for bounded autonomous Blue Dragon
  continuation.

Files changed:

- `.codex/hooks.json`
- `.codex/hooks/blue_dragon_stop.ps1`
- `android/android_studio_project/app/src/main/java/jp/xenia/emulator/EmulatorActivity.java`
- `android/android_studio_project/app/src/main/res/layout/activity_emulator.xml`
- `src/xenia/gpu/spirv_shader_translator_fetch.cc`
- `tools/thor/thor_xenia_debug.ps1`
- `AGENTS.md`
- `README.md`
- `docs/research/20260519-133516-blue-dragon-dword3-title-fix.md`
- `docs/research/20260519-133516-codex-hooks-blue-dragon-loop.md`
- `docs/worklogs/20260519.md`

Commands:

- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonLiveCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 75 -HideAndroidOsd true -EmitInlineMmioChecks true -GpuTraceSwap true -GpuTracePacketBudget 260 -GpuInterruptOnRingIdle false -GpuBlueDragonKickWaitToken false -VulkanTraceCopyState true -VulkanTraceCopyStateBudget 90 -VulkanTraceDrawState true -VulkanTraceDrawStateBudget 160 -VulkanTraceDrawShaderFilter "0ABADD9DA4373CBA,57B736C8B5D4E953,B02CC5F55AD0D140,9567C79307ACC6F5" -VulkanTraceTextureSourceChecksum true -VulkanTraceTextureSourceChecksumBudget 140 -VulkanTraceTextureSourceShaderFilter "0ABADD9DA4373CBA,57B736C8B5D4E953,B02CC5F55AD0D140,9567C79307ACC6F5" -VulkanTraceSwapSharedMemoryChecksum true -VulkanTraceSwapSharedMemoryChecksumBudget 40 -VulkanForceSigned2101010UnormFallback true -VulkanForce2101010Rgba8Fallback false`

Validation:

- Native core build passed.
- Full deploy passed and installed on Thor.
- APK SHA-256 for the clean title build:
  `09F480292F913D6132F3A288C7FCAB758E02FF6BAD519B1DF089BB3EF40B1224`.
- Clean title capture:
  - `scratch/thor-debug/20260519-132943-screenshot.png`
  - `scratch/thor-debug/20260519-132943-meta.txt`
  - `scratch/thor-debug/20260519-132943-live-logcat-filtered.txt`
- Proof marker written:
  `scratch/thor-debug/latest-title-proof.json`.
- Filtered log search found no `AndroidRuntime`, `FATAL EXCEPTION`, native fatal
  signal, `VK_ERROR_DEVICE_LOST`, or GPU-hung lines in the title capture.
- Device still reported process `12743` alive after capture.

Findings:

- The SPIR-V texture fetch path was using fetch constant dword 4 for result
  exponent adjustment. That is wrong for this field.
- Blue Dragon's B02 title-chain fetch constant has dword 4 bits 13:18 decoding
  to signed `-16`, but dword 3 decodes to result exponent `0`.
- The previous global bypass reached title because it skipped the bad dword 4
  adjustment.
- After switching result exponent adjustment to dword 3, Blue Dragon reaches
  the visible `press START` title prompt with
  `vulkan_debug_texture_fetch_disable_exp_adjust=false`.
- The validated run still uses `vulkan_force_signed_2101010_unorm_fallback=true`.
- Codex has official hooks. The `Stop` hook can return `{"decision":"block",
  "reason":"..."}` to continue with a new prompt, and `continue:false` can stop
  the loop. Codex app automations can do scheduled thread wakeups.

Conclusion:

- Blue Dragon now has a clean Thor title-screen proof without the old global
  texture exponent bypass.
- This is still a research fork and not a compatibility/playability claim.
- Next lane: press START / menu path, Android input, and removal or narrowing of
  remaining research-only Vulkan fallback knobs.

## 2026-05-19 14:18-14:21 EDT - Post-Push Title Reproduction

Scope:

- Rebuild/deploy current pushed `master` and reproduce the Blue Dragon title
  screen after commit `d9576ec4c`.

Commands:

- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonLiveCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 75 -HideAndroidOsd true -EmitInlineMmioChecks true -GpuTraceSwap true -GpuTracePacketBudget 260 -GpuInterruptOnRingIdle false -GpuBlueDragonKickWaitToken false -VulkanTraceCopyState true -VulkanTraceCopyStateBudget 90 -VulkanTraceDrawState true -VulkanTraceDrawStateBudget 160 -VulkanTraceDrawShaderFilter "0ABADD9DA4373CBA,57B736C8B5D4E953,B02CC5F55AD0D140,9567C79307ACC6F5" -VulkanTraceTextureSourceChecksum true -VulkanTraceTextureSourceChecksumBudget 140 -VulkanTraceTextureSourceShaderFilter "0ABADD9DA4373CBA,57B736C8B5D4E953,B02CC5F55AD0D140,9567C79307ACC6F5" -VulkanTraceSwapSharedMemoryChecksum true -VulkanTraceSwapSharedMemoryChecksumBudget 40 -VulkanForceSigned2101010UnormFallback true -VulkanForce2101010Rgba8Fallback false`

Validation:

- Full deploy passed and installed.
- Capture: `scratch/thor-debug/20260519-141908-*`.
- Screenshot: `scratch/thor-debug/20260519-141908-screenshot.png`.
- Meta shows `head=d9576ec4c`.
- APK SHA-256:
  `09F480292F913D6132F3A288C7FCAB758E02FF6BAD519B1DF089BB3EF40B1224`.
- Cvar line shows `force_signed_2101010_unorm=true` and
  `disable_fetch_exp_adjust=false`.
- Filtered log search found no `AndroidRuntime`, `FATAL EXCEPTION`, native fatal
  signal, `VK_ERROR_DEVICE_LOST`, or GPU-hung lines.
- Device still reported process `5368` alive after capture.

Conclusion:

- The pushed `master` state reproduces the clean Blue Dragon `press START` title
  screen on Thor.

## 2026-05-19 14:40-14:49 EDT - Nop HID Sequence To Opening Scene

Scope:

- Test whether the existing nop HID START pulse can advance Blue Dragon from
  the title prompt.
- Add a research-only nop HID sequence cvar for multiple timed button pulses.
- Use the sequence to drive START and language-selection A presses.

Files changed:

- `android/android_studio_project/app/src/main/java/jp/xenia/emulator/EmulatorActivity.java`
- `src/xenia/hid/hid_flags.cc`
- `src/xenia/hid/hid_flags.h`
- `src/xenia/hid/nop/nop_input_driver.cc`
- `src/xenia/hid/nop/nop_input_driver.h`
- `src/xenia/kernel/xam/xam_input.cc`
- `tools/thor/thor_xenia_debug.ps1`
- `AGENTS.md`
- `README.md`
- `docs/research/20260519-144858-blue-dragon-nop-hid-sequence.md`
- `docs/worklogs/20260519.md`

Commands:

- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonLiveCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 120 -HideAndroidOsd true -HidNopConnected true -HidNopButtons "start,a" -HidNopButtonsDelayMs 45000 -HidNopButtonsHoldMs 7000 -EmitInlineMmioChecks true -GpuTraceSwap true -GpuTracePacketBudget 260 -GpuInterruptOnRingIdle false -GpuBlueDragonKickWaitToken false -VulkanTraceCopyState true -VulkanTraceCopyStateBudget 90 -VulkanTraceDrawState true -VulkanTraceDrawStateBudget 160 -VulkanTraceDrawShaderFilter "0ABADD9DA4373CBA,57B736C8B5D4E953,B02CC5F55AD0D140,9567C79307ACC6F5" -VulkanTraceTextureSourceChecksum true -VulkanTraceTextureSourceChecksumBudget 140 -VulkanTraceTextureSourceShaderFilter "0ABADD9DA4373CBA,57B736C8B5D4E953,B02CC5F55AD0D140,9567C79307ACC6F5" -VulkanTraceSwapSharedMemoryChecksum true -VulkanTraceSwapSharedMemoryChecksumBudget 40 -VulkanForceSigned2101010UnormFallback true -VulkanForce2101010Rgba8Fallback false`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonLiveCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 150 -HideAndroidOsd true -HidNopConnected true -HidNopButtonSequence "start@45000:3000;a@68000:3000;a@86000:3000;a@106000:3000" -EmitInlineMmioChecks true -GpuTraceSwap true -GpuTracePacketBudget 260 -GpuInterruptOnRingIdle false -GpuBlueDragonKickWaitToken false -VulkanTraceCopyState true -VulkanTraceCopyStateBudget 90 -VulkanTraceDrawState true -VulkanTraceDrawStateBudget 160 -VulkanTraceDrawShaderFilter "0ABADD9DA4373CBA,57B736C8B5D4E953,B02CC5F55AD0D140,9567C79307ACC6F5" -VulkanTraceTextureSourceChecksum true -VulkanTraceTextureSourceChecksumBudget 140 -VulkanTraceTextureSourceShaderFilter "0ABADD9DA4373CBA,57B736C8B5D4E953,B02CC5F55AD0D140,9567C79307ACC6F5" -VulkanTraceSwapSharedMemoryChecksum true -VulkanTraceSwapSharedMemoryChecksumBudget 40 -VulkanForceSigned2101010UnormFallback true -VulkanForce2101010Rgba8Fallback false`

Validation:

- Native core build passed.
- Full deploy passed and installed.
- `scratch/thor-debug/20260519-144021-*` reached the `Voice Language` menu with
  English selected using the old single-pulse nop HID path.
- `scratch/thor-debug/20260519-144529-*` reached the opening scene using
  `hid_nop_button_sequence`.
- APK SHA-256 for the sequence build:
  `8F3CF8C24DDD76CA1BFD11116A3B186BCF5D74145129A17A0DA2049B82D91556`.
- The sequence run showed `disable_fetch_exp_adjust=false` and
  `force_signed_2101010_unorm=true`.
- Filtered log search found no `AndroidRuntime`, `FATAL EXCEPTION`, native fatal
  signal, `VK_ERROR_DEVICE_LOST`, or GPU-hung lines.
- Device still reported process `18239` alive after capture.

Findings:

- Existing nop HID START/A is enough to leave the title prompt and reach the
  language menu.
- A single held button set is awkward for later menus, so
  `hid_nop_button_sequence` was added.
- The XAM input wrappers were already normalizing `actual_user_index`; the
  normalized index is now passed into `GetState`, `SetState`, and
  `GetKeystroke`.

Conclusion:

- Blue Dragon now advances past title and language selection into the opening
  scene on Thor with scripted nop HID input.
- This is still not a playability or compatibility claim. Next lane is real
  Android controller mapping and longer scripted progression toward controllable
  gameplay/menu state.

## 2026-05-19 14:58-15:13 EDT - Opening Scene And Performance Triage

Scope:

- Let the successful Blue Dragon opening-scene process continue.
- Compare trace-heavy proof behavior against a trace-off speed sample.
- Check whether the speed wall is more likely CPU/JIT/audio or GPU/Vulkan.

Commands:

- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode Capture -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonLiveCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 300 -HideAndroidOsd true -HidNopConnected true -HidNopButtonSequence "start@45000:3000;a@68000:3000;a@86000:3000;a@106000:3000;start@135000:2500;a@145000:2500;start@175000:2500;a@185000:2500;start@215000:2500;a@225000:2500;start@255000:2500;a@265000:2500" -EmitInlineMmioChecks true -GpuTraceSwap true -GpuTracePacketBudget 260 -GpuInterruptOnRingIdle false -GpuBlueDragonKickWaitToken false -VulkanTraceCopyState true -VulkanTraceCopyStateBudget 90 -VulkanTraceDrawState true -VulkanTraceDrawStateBudget 160 -VulkanTraceDrawShaderFilter "0ABADD9DA4373CBA,57B736C8B5D4E953,B02CC5F55AD0D140,9567C79307ACC6F5" -VulkanTraceTextureSourceChecksum true -VulkanTraceTextureSourceChecksumBudget 140 -VulkanTraceTextureSourceShaderFilter "0ABADD9DA4373CBA,57B736C8B5D4E953,B02CC5F55AD0D140,9567C79307ACC6F5" -VulkanTraceSwapSharedMemoryChecksum true -VulkanTraceSwapSharedMemoryChecksumBudget 40 -VulkanForceSigned2101010UnormFallback true -VulkanForce2101010Rgba8Fallback false`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonLiveCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -HideAndroidOsd true -HidNopConnected true -HidNopButtonSequence "start@45000:3000;a@68000:3000;a@86000:3000;a@106000:3000" -VulkanForceSigned2101010UnormFallback true -VulkanForce2101010Rgba8Fallback false`
- `adb -s c3ca0370 shell "top -H -b -n 1 -p 25646 | head -80"`

Validation:

- `scratch/thor-debug/20260519-145818-*` showed Shu rendered in-engine.
- `scratch/thor-debug/20260519-150037-*` showed opening credits over the scene.
- `scratch/thor-debug/20260519-150129-*` is not a valid speed sample because
  Android force-stopped the emulator process and relaunched `LauncherActivity`.
- `scratch/thor-debug/20260519-150821-*` is the trace-off speed sample and
  stayed alive in `EmulatorActivity`.
- No native fatal, AndroidRuntime, `VK_ERROR_DEVICE_LOST`, or GPU-hung lines
  were found in the trace-off sample.

Findings:

- Trace-heavy proof runs are very expensive and should not be treated as speed
  samples.
- Trace-off sample still feels slow.
- Live thread sample from process `25646` showed:
  - Main guest CPU thread: `81.4%`
  - XMA decoder: `44.4%`
  - GPU command thread: `14.8%`
  - Draw thread: `3.7%`
- Adreno/Vulkan is rendering real frames; the immediate performance wall is
  CPU/JIT/audio/debug overhead first, GPU second.

Conclusion:

- Next speed lane: build a no-trace perf workflow, sample hot threads/functions,
  test a cheaper audio path for bring-up, and focus A64 backend dispatch/hot
  PPC functions before deeper Vulkan optimization.

## 2026-05-19 15:13-15:46 EDT - Quiet Speed Lane And Audio Probe

Scope:

- Build a faster Blue Dragon speed-sampling loop for Thor.
- Remove high-volume debug log overhead from speed captures.
- Test whether an experimental XMA fast-silence path materially improves the
  2 FPS feeling.

Files changed:

- `android/android_studio_project/app/src/main/java/jp/xenia/emulator/EmulatorActivity.java`
- `src/xenia/apu/xma_context.cc`
- `src/xenia/apu/xma_context.h`
- `src/xenia/gpu/command_processor.cc`
- `src/xenia/gpu/gpu_flags.cc`
- `src/xenia/gpu/gpu_flags.h`
- `src/xenia/gpu/texture_cache.cc`
- `src/xenia/gpu/vulkan/vulkan_pipeline_cache.cc`
- `tools/thor/thor_build.ps1`
- `tools/thor/thor_xenia_debug.ps1`
- `AGENTS.md`
- `README.md`
- `docs/research/20260519-154636-blue-dragon-speed-lane.md`
- `docs/worklogs/20260519.md`

Commands:

- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 130 -PerfSampleSeconds "60,120"`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 130 -PerfSampleSeconds "60,120" -XmaFastSilence true`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode Capture -DeviceSerial c3ca0370`

Validation:

- PowerShell parse checks passed for `thor_xenia_debug.ps1` and
  `thor_build.ps1`.
- Full deploy passed after fixing a linker issue in the fast-silence patch.
- `scratch/thor-debug/20260519-153505-*` is the quiet speed baseline.
- `scratch/thor-debug/20260519-154141-*` is the explicit
  `xma_fast_silence` sample.
- `scratch/thor-debug/20260519-154546-*` reached the opening scene after the
  fast-silence loading capture.
- No AndroidRuntime fatal, native fatal signal, `VK_ERROR_DEVICE_LOST`, or
  GPU-hung line was found in the quiet speed samples.

Findings:

- `LaunchBlueDragonSpeedCapture` now provides a repeatable no-live-logcat speed
  run with timed thread/process/device snapshots.
- XMA context kick/disable logging dropped from 2948 lines to 0.
- Known XMA/GPU log spam dropped to 0 in the quiet lane, and filtered logcat
  shrank from megabytes to about 18 KB.
- The main guest CPU thread remains pinned around one full core; the speed wall
  is still CPU/A64 backend first.
- `xma_fast_silence` is safe enough as an opt-in experiment and still reached
  the opening scene, but it did not solve the overall speed wall.
- `thor_build.ps1` previously installed stale APKs after a failed Gradle native
  build; it now throws on nonzero Gradle or ADB exit codes.

Conclusion:

- Use `LaunchBlueDragonSpeedCapture` for speed work, not trace-heavy live
  captures.
- Keep `xma_fast_silence` opt-in only.
- Next real performance work should profile and improve the A64 backend/hot
  guest function path.

## 2026-05-19 15:46-16:04 EDT - Translation Report To Agent Rules

Scope:

- Mine the Xbox 360 to Thor Max translation report into durable repo agent
  instructions.
- Make the next Blue Dragon speed work less one-fix-at-a-time by capturing the
  broad A64/DBT counters and lane rules the report recommends.

Files changed:

- `AGENTS.md`
- `docs/worklogs/20260519.md`
- `docs/research/20260519-153016-xbox360-thormax-translation-report.md`

Commands:

- `Get-Content -Path AGENTS.md -TotalCount 260`
- `Get-Content -Path docs\worklogs\20260519.md -Tail 120`
- `git status --short --branch`
- `rg -n "^(##|#)|Blue Dragon|Current|Performance|A64|ARM64|speed|trace|GPU" AGENTS.md`
- `Get-Content -Path AGENTS.md | Select-Object -Skip 260 -First 260`
- `Get-Item docs\research\20260519-153016-xbox360-thormax-translation-report.md | Format-List FullName,Length,LastWriteTime`
- `Get-Date -Format "yyyy-MM-dd HH:mm:ss zzz"`

Findings:

- The report says the current Blue Dragon wall is CPU/A64 dispatch, helper
  fallbacks, guest-to-host transitions, XMA/audio cost, and debug overhead
  first; GPU/Adreno work should stay focused until evidence points back to
  rendering.
- Trace-heavy correctness runs and trace-off speed runs must stay separate.
- The next useful broad pass is cheap speed-lane instrumentation for guest
  function/block entries, helper categories, code-cache links/misses, indirect
  branches, recompiles, and top guest PCs.
- `apu=nop` and `xma_fast_silence` are comparison lanes only, not correctness
  paths.

Conclusion:

- Updated `AGENTS.md` with the 2026-05-19 translation strategy digest, speed
  capture metadata requirements, A64 DBT instrumentation priorities, and the
  rule to keep broad GPU traces out of the speed lane.

## 2026-05-19 16:04-16:20 EDT - A64 Speed Profile Counters

Scope:

- Add the first low-noise A64 backend profiler recommended by the translation
  report.
- Build, deploy, and run Blue Dragon with the profiler enabled.

Files changed:

- `android/android_studio_project/app/src/main/java/jp/xenia/emulator/EmulatorActivity.java`
- `src/xenia/cpu/backend/a64/a64_assembler.cc`
- `src/xenia/cpu/backend/a64/a64_backend.cc`
- `src/xenia/cpu/backend/a64/a64_backend.h`
- `src/xenia/cpu/backend/a64/a64_emitter.cc`
- `src/xenia/cpu/backend/a64/a64_emitter.h`
- `src/xenia/cpu/backend/a64/a64_function.cc`
- `src/xenia/cpu/backend/a64/a64_function.h`
- `tools/thor/thor_xenia_debug.ps1`
- `AGENTS.md`
- `docs/research/20260519-162000-a64-speed-profile-counters.md`
- `docs/worklogs/20260519.md`

Commands:

- `git status --short --branch`
- `rg -n "DEFINE_(bool|int32|uint32|int64)|DECLARE_(bool|int32|uint32|int64)|cvar|a64_|arm64_|CompiledCall|Trace|helper|Helper|Dispatch|indirect|code cache|CodeCache|Resolve" src\xenia\cpu\backend\a64 src\xenia\cpu\backend\arm64 src\xenia\cpu -g "*.cc" -g "*.h"`
- `Get-Content -Path src\xenia\cpu\backend\a64\a64_backend.cc | Select-Object -First 920`
- `Get-Content -Path src\xenia\cpu\backend\a64\a64_emitter.cc | Select-Object -First 760`
- `git diff --check`
- PowerShell parser check for `tools\thor\thor_xenia_debug.ps1`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 100 -PerfSampleSeconds "45,90" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 16 -Arm64SpeedProfileMinDelta 1`

Validation:

- `git diff --check` passed.
- `thor_xenia_debug.ps1` parsed successfully.
- Native core build passed for Android `arm64-v8a` and `x86_64`.
- Full deploy built and installed `app-github-debug.apk` on Thor.
- Profiled Blue Dragon capture:
  `scratch/thor-debug/20260519-161600-*`.
- Metadata recorded APK SHA-256
  `FA6BC83AF068D1A94D66969BD449D76DFC109B9542F61A387827F0A4F362C080`.
- No AndroidRuntime fatal, native fatal signal, `VK_ERROR_DEVICE_LOST`,
  GPU-hung, tombstone, or ANR line was found in the profile capture.

Findings:

- New default-off cvars:
  `arm64_speed_profile_interval_ms`,
  `arm64_speed_profile_top_functions`, and
  `arm64_speed_profile_min_delta`.
- The Thor script now forwards those cvars and raises `log_level` to `1` only
  for profiled speed captures so warning-level profile summaries appear.
- Final profile interval:
  - `funcs=5750`
  - `entry_delta=239863955`
  - `direct=237498961/2065602607`
  - `indirect=2518769/8423002`
  - `extern=357748/3495432`
  - `resolves=584/5750`
  - `resolve_misses=0/0`
- Hottest final-interval functions:
  `__restgprlr_29`, `__savegprlr_29`, `8248B040`, and known draw-wait
  function `8246B408`.

Conclusion:

- The next CPU speed target is guest call overhead around Blue Dragon's
  draw-wait loop and the PPC save/restore compiler helpers, not a broad GPU
  trace.

## 2026-05-19 16:26-16:31 EDT - Thor Donor Skill Audit

Scope:

- Audit the Vita3K-Thor and RPCSX UI Android Thor repo skill folders for ideas
  that speed up xenia-thor's Blue Dragon / AYN Thor ARM64 loop.
- Adapt the useful patterns without copying donor skill bodies or scripts.
- Make the Xenia difference explicit: AYN Thor ARM64 first, Windows only as a
  control lane when a specific semantics question needs it.

Files changed:

- `AGENTS.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `.agents/skills/xenia-thor-experiment-gate/SKILL.md`
- `.agents/skills/xenia-blue-dragon-route-capture/SKILL.md`
- `.agents/skills/xenia-thor-war-room/SKILL.md`
- `.agents/skills/aarch64-snapdragon-jit-port/SKILL.md`
- `.agents/skills/xbox360-xenon-ppc-architect/SKILL.md`
- `.agents/skills/xenia-vulkan-adreno-renderdoc/SKILL.md`
- `docs/research/20260519-162652-thor-agent-skill-donor-audit.md`
- `docs/worklogs/20260519.md`

Commands:

- `git status --short --branch`
- `rg --files .agents\skills`
- Opened GitHub listings for:
  `https://github.com/noeldvictor/Vita3K-Thor/tree/master/.agents/skills`
  and
  `https://github.com/noeldvictor/rpcsx-ui-android-thor/tree/master/.agents/skills`
- Sparse-cloned donor skill folders under `%TEMP%\xenia-thor-skill-donors`.
- `git -C <donor> rev-parse HEAD`
- `rg --files <donor>\.agents\skills`
- Frontmatter/headings audit of donor `SKILL.md` files.
- Read focused donor skills for experiment gates, perf profiler, Android loop,
  experiment ledger, screenshot burst, Windows/Android A/B, codegen hotpath,
  ADB operator, Ghidra static lane, and debug RAG.
- `rg -n "screenshot|screencap|burst|Capture" tools .agents app src -g "*.ps1" -g "*.md" -g "*.kt" -g "*.java"`
- `rg -n "Arm64SpeedProfile|speed_profile|LaunchBlueDragonSpeedCapture|PerfSample|simpleperf|gfxinfo|SurfaceFlinger" tools src app docs .agents -g "*.ps1" -g "*.cc" -g "*.h" -g "*.md" -g "*.java"`
- `rg --files | rg "quick_validate|skill|validate"`
- `Get-Date -Format "yyyyMMdd-HHmmss"`

Findings:

- Vita3K-Thor has useful patterns for experiment gates, Android proof loops,
  performance profiling, input automation, Ghidra escalation, and durable debug
  memory.
- RPCSX UI Android Thor has useful patterns for ADB safety, experiment ledgers,
  screenshot bursts, scene routing, Windows/Android comparisons, Ghidra static
  lanes, Vulkan audits, and codegen hotpath work.
- Xenia should not import the donor Windows-first instinct as default. The
  priority here is ARM64 on Thor; Windows is a control microscope.
- No repo-local `quick_validate.py` was found, so this slice uses frontmatter
  and readability checks for the new skills.

Conclusion:

- Added three repo skills: A64 speed hotpath, experiment gate, and Blue Dragon
  route capture.
- Updated the war-room and expert skills so the next Blue Dragon speed pass
  starts with the clean Thor A64 profiler, records a one-variable experiment,
  and avoids falling back into broad trace-heavy guessing.

## 2026-05-19 17:04-17:19 EDT - A64 GPR/LR Helper Inline

Scope:

- Inline the PPC compiler `__savegprlr_*` and `__restgprlr_*` ABI helpers in the
  A64 backend.
- Preserve restore-helper LR mismatch behavior by tail-jumping through the
  normal code-cache path when the restored LR is not the current guest return.
- Validate the change on the Blue Dragon quiet speed route.

Files changed:

- `src/xenia/cpu/backend/a64/a64_emitter.cc`
- `src/xenia/cpu/backend/a64/a64_emitter.h`
- `android/android_studio_project/app/src/main/java/jp/xenia/emulator/EmulatorActivity.java`
- `tools/thor/thor_xenia_debug.ps1`
- `docs/research/20260519-170744-a64-gprlr-helper-inline.md`
- `docs/worklogs/20260519.md`
- `AGENTS.md`

Commands:

- `git diff --check`
- PowerShell parser check for `tools\thor\thor_xenia_debug.ps1`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 100 -PerfSampleSeconds "45,90" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 20 -Arm64SpeedProfileMinDelta 1 -A64InlineGprLrHelpers true`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 155 -PerfSampleSeconds "75,140" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 20 -Arm64SpeedProfileMinDelta 1 -A64InlineGprLrHelpers true`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 155 -PerfSampleSeconds "75,140" -A64InlineGprLrHelpers false`

Validation:

- Native core build passed.
- Full deploy passed and installed on Thor.
- First inline restore attempt reached no fatal crash but stayed black:
  `scratch/thor-debug/20260519-170818-*`.
- Same APK with `-A64InlineGprLrHelpers false` reached the visible opening
  scene: `scratch/thor-debug/20260519-171136-*`.
- Corrected inline restore with LR mismatch tail-jump reached the visible
  opening scene: `scratch/thor-debug/20260519-171625-*`.
- Corrected APK SHA-256:
  `E188217B67097FF866006B7115FCB47B3B13AD044550082A6D75DEDDA794BCDB`.
- No `AndroidRuntime`, fatal signal, `VK_ERROR_DEVICE_LOST`, GPU-hung,
  tombstone, or ANR line was found in the corrected capture.

Findings:

- `__savegprlr_*` and `__restgprlr_*` disappeared from the Blue Dragon top
  profile after the corrected inline helper path.
- The old always-return restore shortcut was wrong; `__restgprlr_*` must keep
  the old LR mismatch / tail branch semantics.
- Late corrected profile still shows the draw-wait pair `8248B040` /
  `8246B408` as the dominant wall.
- FPR and VMX helper families are now visible smaller helper-inline candidates.

Conclusion:

- Keep `a64_inline_gprlr_helpers` default-on with the runtime LR mismatch
  check.
- Next speed lane: inspect `8246B408` / `8248B040` and consider FPR/VMX helper
  inlining as the next broad helper-family cleanup.

## 2026-05-19 17:25-17:35 EDT - Thor Max Translation Target Mental Model

Scope:

- Check the user's "Thor Max is a good translation-emulation target" model
  against current web sources.
- Make the durable repo guidance explicit: translation/HLE first, not
  cycle-accurate Xbox 360 hardware emulation.

Files changed:

- `AGENTS.md`
- `docs/research/20260519-172519-thor-max-translation-target.md`
- `docs/worklogs/20260519.md`

Commands:

- Web search for AYN Thor Max specs, Snapdragon 8 Gen 2 Vulkan/API support,
  Xbox 360 hardware specs, and Xenia translation/HLE architecture references.
- `Get-Content AGENTS.md | Select-Object -First 280`
- `Get-ChildItem docs\research | Sort-Object LastWriteTime -Descending | Select-Object -First 10 Name,LastWriteTime`
- `Get-Content docs\worklogs\20260519.md | Select-Object -Last 120`
- `Get-Date -Format "yyyyMMdd-HHmmss"`

Findings:

- Thor Max is a strong research target for the xenia-thor approach: Snapdragon
  8 Gen 2, Adreno 740, Vulkan 1.3 support, 16 GB RAM in Max configurations,
  active cooling, Android, microSD, and handheld controls.
- The "about 20x" intuition is useful for hardware-headroom motivation, but not
  a literal speed promise. Effective performance is governed by the A64 DBT,
  endian/VMX/FP lowering, guest-host transitions, kernel timing/event HLE,
  shader/EDRAM/resolve translation, and Adreno driver behavior.
- The durable model should be:
  PowerPC guest code -> AArch64 JIT/DBT,
  Xenos GPU work -> Vulkan/SPIR-V on Adreno,
  Xbox OS/device services -> HLE with focused correctness probes.

Conclusion:

- Added a `Thor Max Translation Model` section to `AGENTS.md`.
- Added the timestamped source-backed research note so future speed work stays
  focused on translation/HLE bottlenecks and validated Thor captures.

## 2026-05-19 17:33 EDT - A64 PPC Thread-Field Leaf Inline

Scope:

- Reduce Blue Dragon's draw-wait direct-call overhead by inlining tiny PPC leaf
  helpers matching `lwz r11,D(r13); lwz r3,D(r11); blr`.

Files changed:

- `src/xenia/cpu/backend/a64/a64_emitter.cc`
- `src/xenia/cpu/backend/a64/a64_emitter.h`
- `android/android_studio_project/app/src/main/java/jp/xenia/emulator/EmulatorActivity.java`
- `tools/thor/thor_xenia_debug.ps1`
- `AGENTS.md`
- `docs/research/20260519-173329-a64-ppc-thread-field-leaf-inline.md`
- `docs/worklogs/20260519.md`

Experiment:

- If the A64 backend inlines tiny PPC current-thread field helpers, then
  Blue Dragon's `8248B040` profile entries and direct-call deltas should fall
  because the hot draw-wait loop will no longer pay a full guest function call
  to read `r13 + 0x100 -> +0x14C`.

Baseline:

- `scratch/thor-debug/20260519-171625-*`
- Late profile top pair: `8248B040` delta `89,485,052`, `8246B408` delta
  `89,410,658`.

Initial findings:

- Prior focused dump shows `8248B040` is exactly:
  `lwz r11, 0x100(r13); lwz r3, 0x14C(r11); blr`.
- The new inline is pattern-based, non-tail-call only, preserves `r11` and
  `r3`, and has rollback cvar
  `a64_inline_ppc_thread_field_leaf_helpers`.

Validation:

- `git diff --check` passed with normal CRLF warnings only.
- PowerShell parser check for `tools\thor\thor_xenia_debug.ps1` passed.
- Native core build passed.
- Full deploy passed and installed on Thor.
- Speed capture:
  `scratch/thor-debug/20260519-173541-*`
- APK SHA-256:
  `81499688A9C48C82934F0D8E17A5A4CA49B7D4C8D7009C1E5B7E9CE3EA789918`
- Screenshot reached the visible Blue Dragon opening-scene route.
- No `AndroidRuntime`, fatal signal, `VK_ERROR_DEVICE_LOST`, GPU hung,
  tombstone, or ANR line was found.
- Metadata confirmed:
  `a64_inline_ppc_thread_field_leaf_helpers=true`.

Result:

- `8248B040` disappeared from the top A64 speed profile, confirming the inline
  matched and removed that tiny helper call from the draw-wait hot path.
- Final interval top function became only `8246B408`, delta `315,647,367`.
- Final interval summary:
  `funcs=6126 entry_delta=317645761 h2g=3670/36802 g2h=360979/4494956 direct=317380254/1804216996 indirect=273952/9804910 extern=349526/4385228 resolves=107/6127 resolve_misses=0/0`.
- The higher raw `8246B408` count is expected: removing the callee lets the
  real busy-wait loop spin faster instead of splitting the profile between
  `8246B408` and `8248B040`.

Conclusion:

- Keep `a64_inline_ppc_thread_field_leaf_helpers` default-on.
- Next speed lane is `8246B408` itself: either general A64 compare/branch and
  context-barrier lowering, or a focused wait/yield semantics experiment if
  evidence shows the game is burning CPU while waiting for GPU/token progress.

## 2026-05-19 17:48 EDT - Repo-Local Ghidra OODA Loop

Scope:

- Find the local Ghidra install.
- Create the repo-local Ghidra/OODA skill.
- Create ignored Ghidra scratch folders.
- Update AGENTS so future skills stay repo-local only.

Files changed:

- `.agents/skills/xenia-ghidra-ooda-loop/SKILL.md`
- `.gitignore`
- `AGENTS.md`
- `tools/thor/ghidra_headless_import.ps1`
- `docs/research/20260519-174855-ghidra-ooda-loop.md`
- `docs/worklogs/20260519.md`

Commands:

- Read global `skill-creator` instructions for skill creation format.
- Read existing Ghidra debug skill as a reference only.
- `git status --short --branch`
- Searched common paths and drives for `analyzeHeadless.bat`.
- Found Ghidra at:
  `C:\Users\leanerdesigner\Documents\SteamPortableTools\toolchains\ghidra_12.0.4_PUBLIC`.
- Ran `analyzeHeadless.bat` and confirmed it prints headless analyzer usage.
- Created ignored folders:
  `scratch\ghidra\native`,
  `scratch\ghidra\guest`,
  `scratch\ghidra\generated-jit`,
  `scratch\ghidra\reports`,
  `scratch\ghidra\scripts`.

Findings:

- `/scratch/` was already ignored. `.gitignore` now documents that this covers
  local Ghidra, device-capture, extracted guest-code, screenshot, and generated
  static-analysis artifacts.
- `tools\thor\ghidra_headless_import.ps1` can now auto-detect the discovered
  local Ghidra install while still honoring explicit overrides.
- The new repo-local skill should be used for the current `8246B408` wall:
  runtime capture first, tiny address queue second, smallest Ghidra import
  third, one next experiment last.

Validation:

- `git diff --check` passed with normal CRLF warnings only.
- PowerShell parser check for `tools\thor\ghidra_headless_import.ps1` passed.
- `quick_validate.py .agents\skills\xenia-ghidra-ooda-loop` passed.
- Ghidra no-analysis native import smoke test passed:
  `powershell -ExecutionPolicy Bypass -File tools\thor\ghidra_headless_import.ps1 -ProjectDir scratch\ghidra\native -ProjectName xenia-thor-native-smoke -NoAnalysis`.
- Smoke import auto-detected:
  `C:\Users\leanerdesigner\Documents\SteamPortableTools\toolchains\ghidra_12.0.4_PUBLIC\support\analyzeHeadless.bat`.
- Ghidra loaded `libxenia-app.so` as `AARCH64:LE:64:v8A:default` into ignored
  `scratch\ghidra\native\xenia-thor-native-smoke`.

## 2026-05-19 18:46 EDT - Blue Dragon Speed-Loop Noise Budget

Scope:

- Keep the current fastest Blue Dragon route reproducible on Thor.
- Reduce speed-capture log overhead without hiding the A64 speed profiler.
- Preserve rollback knobs for draw-wait and mount-cache experiments.

Files changed:

- `android/android_studio_project/app/src/main/java/jp/xenia/emulator/EmulatorActivity.java`
- `src/xenia/cpu/backend/a64/a64_backend.cc`
- `src/xenia/cpu/backend/a64/a64_backend.h`
- `src/xenia/cpu/backend/a64/a64_seq_memory.cc`
- `src/xenia/gpu/command_processor.cc`
- `src/xenia/gpu/gpu_flags.cc`
- `src/xenia/gpu/gpu_flags.h`
- `src/xenia/gpu/graphics_system.cc`
- `src/xenia/kernel/xboxkrnl/xboxkrnl_io.cc`
- `tools/thor/thor_xenia_debug.ps1`
- `AGENTS.md`
- `docs/research/20260519-184650-blue-dragon-speed-loop-noise-budget.md`
- `docs/worklogs/20260519.md`

Commands:

- `git diff --check`
- PowerShell parser check for `tools\thor\thor_xenia_debug.ps1`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 155 -PerfSampleSeconds "75,140" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 20 -Arm64SpeedProfileMinDelta 1 -A64InlinePpcThreadFieldLeafHelpers true -Arm64BlueDragonDrawWaitProbe true`

Findings:

- `arm64_blue_dragon_draw_wait_probe_stride` is now routed and defaults to `1`,
  preserving the known every-hit draw-wait probe behavior unless a stride is
  explicitly requested.
- `arm64_blue_dragon_draw_wait_inline_tick_step` is routed and default-off.
  Initial step-`1` captures did not improve the route or counters.
- `gpu_unknown_register_log_budget` and
  `xboxkrnl_nt_create_file_fail_log_budget` are routed through Android and the
  Thor script. Blue Dragon speed defaults set both to `0`.
- Speed defaults no longer force `MountCache=false`, so `-MountCache true`
  remains available as an explicit A/B lane. The first 110s mount-cache test
  was not a speed win for the current route.
- Latest validation capture `scratch/thor-debug/20260519-184120-*` reached the
  visible opening scene.
- APK SHA-256:
  `C50C9C40EF73AF84F8675388EBD7A0BD491D0DB1F1ED6C03B2ACF191832001E9`
- Filtered log line count: `648`; `NtCreateFile failed` lines: `0`;
  unknown-register lines: `0`.
- Final profile still points at guest PC `8246B408`:
  `funcs=6136 entry_delta=291310454 h2g=3661/36746 g2h=435891/4857227 direct=290962320/1681928431 indirect=359649/9768935 extern=353892/4350451`.

Validation:

- `git diff --check` passed with normal CRLF warnings only.
- PowerShell parser check passed.
- Native core build passed.
- Full deploy passed and installed on Thor.
- The 155s Blue Dragon speed capture had no fatal signal, AndroidRuntime,
  `VK_ERROR_DEVICE_LOST`, GPU-hung, tombstone, or ANR marker.

Next actions:

- Use the quieter speed lane as the baseline for future Blue Dragon A/B runs.
- Treat `8246B408` as the next wall: either identify the wait condition with
  focused PPC/Ghidra work, or reduce broad A64 dispatch/direct-call cost before
  another title-specific timing hack.

## 2026-05-19 19:20 EDT - Blue Dragon Draw-Wait Fastpath

Scope:

- Add a default-off direct A64 body for Blue Dragon's known draw wait function
  `8246B408`.
- Validate whether it preserves the current visible-opening route on AYN Thor.
- Compare bad timing shortcuts against the real-clock path.

Files changed:

- `android/android_studio_project/app/src/main/java/jp/xenia/emulator/EmulatorActivity.java`
- `src/xenia/cpu/backend/a64/a64_backend.cc`
- `src/xenia/cpu/backend/a64/a64_emitter.cc`
- `src/xenia/cpu/backend/a64/a64_emitter.h`
- `tools/thor/thor_xenia_debug.ps1`
- `AGENTS.md`
- `docs/research/20260519-192038-blue-dragon-draw-wait-fastpath.md`
- `docs/worklogs/20260519.md`

Commands:

- `git diff --check`
- PowerShell parser check for `tools\thor\thor_xenia_debug.ps1`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 155 -PerfSampleSeconds "75,140" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 20 -Arm64SpeedProfileMinDelta 1 -A64InlinePpcThreadFieldLeafHelpers true -Arm64BlueDragonDrawWaitProbe true -Arm64BlueDragonDrawWaitFastpath true -Arm64BlueDragonDrawWaitInlineTickStep 1`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 155 -PerfSampleSeconds "75,140" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 20 -Arm64SpeedProfileMinDelta 1 -A64InlinePpcThreadFieldLeafHelpers true -Arm64BlueDragonDrawWaitProbe true -Arm64BlueDragonDrawWaitFastpath true`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 110 -PerfSampleSeconds "60,100" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 20 -Arm64SpeedProfileMinDelta 1 -A64InlinePpcThreadFieldLeafHelpers true -Arm64BlueDragonDrawWaitProbe true -Arm64BlueDragonDrawWaitFastpath true -Arm64BlueDragonDrawWaitProbeStride 4096`

Findings:

- `arm64_blue_dragon_draw_wait_fastpath` is now routed and default-off.
- Fastpath + inline tick step `1` stayed alive but black-screen parked after a
  `5000`-entry draw-wait burst.
- Fastpath + real KTHREAD clock probe reached the visible opening scene:
  `scratch/thor-debug/20260519-191314-*`.
- That useful path reduced the generated body size for `8246B408` from `1632`
  bytes to about `448` bytes, but pushed cost into guest-to-host clock updates.
- Fastpath + real clock probe stride `4096` stayed alive but black-screen parked
  after a `4096`-entry burst.

Validation:

- `git diff --check` passed with normal CRLF warnings only.
- PowerShell parser check passed.
- Native core build passed.
- Full deploy passed and installed on Thor.
- The 155s real-clock fastpath capture had no fatal signal, AndroidRuntime,
  `VK_ERROR_DEVICE_LOST`, GPU-hung, tombstone, ANR, unknown-register, or
  `NtCreateFile failed` markers.

Next actions:

- Keep the fastpath default-off until it is a clear speed win.
- Replace the per-entry native clock thunk with a cheap generated-code guest
  uptime source or move the update closer to the original PPC load-site path.

## 2026-05-19 19:33 EDT - Blue Dragon Host-Counter Time Fastpath

Scope:

- Remove the native clock-thunk wall exposed by the `8246B408` fastpath.
- Validate whether generated A64 can derive guest-shaped milliseconds from
  `CNTVCT_EL0` / `CNTFRQ_EL0` on Thor.
- Push Blue Dragon beyond boot/loading toward visible rendered content.

Files changed:

- `android/android_studio_project/app/src/main/java/jp/xenia/emulator/EmulatorActivity.java`
- `src/xenia/cpu/backend/a64/a64_backend.cc`
- `src/xenia/cpu/backend/a64/a64_backend.h`
- `src/xenia/cpu/backend/a64/a64_emitter.cc`
- `tools/thor/thor_xenia_debug.ps1`
- `AGENTS.md`
- `docs/research/20260519-193313-blue-dragon-host-counter-time.md`
- `docs/worklogs/20260519.md`

Commands:

- `git diff --check`
- PowerShell parser check for `tools\thor\thor_xenia_debug.ps1`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 110 -PerfSampleSeconds "60,100" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 20 -Arm64SpeedProfileMinDelta 1 -A64InlinePpcThreadFieldLeafHelpers true -Arm64BlueDragonDrawWaitProbe true -Arm64BlueDragonDrawWaitFastpath true -Arm64BlueDragonDrawWaitFastpathHostCounterTime true`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "90,160" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 20 -Arm64SpeedProfileMinDelta 1 -A64InlinePpcThreadFieldLeafHelpers true -Arm64BlueDragonDrawWaitProbe true -Arm64BlueDragonDrawWaitFastpath true -Arm64BlueDragonDrawWaitFastpathHostCounterTime true -MountCache true`
- `Start-Sleep -Seconds 90; powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode Capture -DeviceSerial c3ca0370`

Findings:

- `arm64_blue_dragon_draw_wait_fastpath_host_counter_time` builds and deploys.
- The 110s host-counter probe reached the Blue Dragon loading spinner with
  `8246B408` compiled to about `472` bytes and without the prior per-entry
  native clock-thunk storm.
- The 180s host-counter + `MountCache true` run reached the Microsoft Game
  Studios opening scene, and the follow-up capture
  `scratch/thor-debug/20260519-193236-screenshot.png` showed rendered
  Blue Dragon opening/game content with the main character visible.
- No fatal signal, AndroidRuntime, `VK_ERROR_DEVICE_LOST`, GPU-hung, tombstone,
  or ANR markers appeared in the proof captures.
- `MountCache true` removed the `ResolvePath(cache:\...) failed` noise seen in
  the host-counter no-cache probe.

Validation:

- `git diff --check` passed with normal CRLF warnings only.
- PowerShell parser check passed.
- Native core build passed.
- Full deploy passed and installed on Thor.
- On-device proof captures:
  `scratch/thor-debug/20260519-192458-*`,
  `scratch/thor-debug/20260519-192732-*`, and
  `scratch/thor-debug/20260519-193236-*`.

Next actions:

- Continue Blue Dragon with
  `-Arm64BlueDragonDrawWaitFastpath true -Arm64BlueDragonDrawWaitFastpathHostCounterTime true -Arm64BlueDragonDrawWaitProbe true -MountCache true`.
- Turn the remaining `8246B408` generated A64 busy-wait into an event/yield or
  lower-cost loop without regressing the visible route.
- Consider inlining the helper families now visible behind the wait:
  `__savevmx_124`, `__restvmx_124`, `__savefpr_27`, and `__restfpr_27`.

## 2026-05-19 20:00 EDT - Blue Dragon Wait Yield and Timeout Probes

Scope:

- Add default-off wait-yield, wait-sleep, and timeout controls for the
  `8246B408` Blue Dragon fastpath.
- Test whether those controls move the Thor route closer to title without
  crashing.

Files changed:

- `android/android_studio_project/app/src/main/java/jp/xenia/emulator/EmulatorActivity.java`
- `src/xenia/cpu/backend/a64/a64_backend.cc`
- `src/xenia/cpu/backend/a64/a64_backend.h`
- `src/xenia/cpu/backend/a64/a64_emitter.cc`
- `tools/thor/thor_xenia_debug.ps1`
- `AGENTS.md`
- `docs/research/20260519-200005-blue-dragon-wait-yield-timeout-probes.md`
- `docs/worklogs/20260519.md`

Validation:

- `git diff --check` passed with normal CRLF warnings only.
- PowerShell parser check passed for `tools\thor\thor_xenia_debug.ps1`.
- Native core build passed.
- Full deploy passed on Thor.

Runs:

- `scratch/thor-debug/20260519-194044-*`:
  `-Arm64BlueDragonDrawWaitFastpathNativeYieldStride 8192` was stable, reached
  Microsoft Game Studios at 180s, and follow-up
  `scratch/thor-debug/20260519-194617-screenshot.png` reached the child-on-rock
  scene. It did not remove the `8246B408` wall.
- `scratch/thor-debug/20260519-194811-*`:
  `-Arm64BlueDragonDrawWaitFastpathNativeYieldStride 8192 -Arm64BlueDragonDrawWaitFastpathNativeSleepUs 100`
  parked on black screen with later profile intervals at `entry_delta=0`.
- `scratch/thor-debug/20260519-195422-*`:
  `-Arm64BlueDragonDrawWaitFastpathTimeoutMs 1000` was stable, reached
  Microsoft Game Studios at 180s, and follow-up
  `scratch/thor-debug/20260519-195931-screenshot.png` reached the child-on-rock
  scene. It was not a clear speed win.

Next actions:

- Keep using the host-counter fastpath without native sleep.
- Use static/runtime analysis to identify the wait producer or target the next
  helper-family hotpaths behind `8246B408`.

## 2026-05-19 20:42 EDT - A64 FPR/VMX Helper Inline

Scope:

- Inline the FPR and VMX compiler ABI save/restore helpers that appeared behind
  the Blue Dragon draw-wait wall.
- Validate split and combined helper inlining on Thor.
- Update the speed lane notes so the next loop targets `8246B408` directly.

Files changed:

- `android/android_studio_project/app/src/main/java/jp/xenia/emulator/EmulatorActivity.java`
- `src/xenia/cpu/backend/a64/a64_emitter.cc`
- `src/xenia/cpu/backend/a64/a64_emitter.h`
- `tools/thor/thor_xenia_debug.ps1`
- `AGENTS.md`
- `docs/research/20260519-204252-a64-fpr-vmx-helper-inline.md`
- `docs/worklogs/20260519.md`

Commands:

- `git diff --check`
- PowerShell parser check for `tools\thor\thor_xenia_debug.ps1`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "90,160" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 20 -Arm64SpeedProfileMinDelta 1 -A64InlineFprHelpers true -A64InlineVmxHelpers true -A64InlinePpcThreadFieldLeafHelpers true -Arm64BlueDragonDrawWaitProbe true -Arm64BlueDragonDrawWaitFastpath true -Arm64BlueDragonDrawWaitFastpathHostCounterTime true -MountCache true`

Findings:

- The first FPR-only and VMX-only probes parked because guest stack offset
  subtraction happened after converting to a host pointer.
- Fixing the address math to subtract in 32-bit guest address space before
  applying membase made both split probes stable.
- VMX helper inlining also needs 16-byte effective-address alignment and the
  helper `r11 = -16` side effect.
- The combined FPR+VMX run `scratch/thor-debug/20260519-203701-*` reached the
  Microsoft Game Studios opening scene on Thor with APK SHA-256
  `5977CC990F4F53F414188AEE34CAEDD71FDDD7464064E95962403487CAD2CAA3`.
- The late helper entries disappeared from the top speed profile, but
  `8246B408` remains the dominant CPU wall. Final combined interval top entry:
  `8246B408`, `delta=104749562`.
- A no-explicit-helper run after flipping C++ defaults
  `scratch/thor-debug/20260519-204503-*` parked on black because the existing
  device config still stored `a64_inline_fpr_helpers = false` and
  `a64_inline_vmx_helpers = false`.
- `tools/thor/thor_xenia_debug.ps1` now passes the split helper extras as
  `true` by default; pass explicit `false` values for rollback probes.
- Script-default proof run `scratch/thor-debug/20260519-205129-*` launched
  without manually passing FPR/VMX switches, recorded both metadata values as
  `true`, reached the Blue Dragon loading spinner, and did not show FPR/VMX
  helper names in the filtered speed profile.
- GPU work is not the next broad target from this evidence: final thread sample
  showed GPU Commands around 3.8% while the main/XThread CPU lanes were hot.

Validation:

- `git diff --check` passed with normal CRLF warnings only.
- PowerShell parser check passed.
- Native core build passed.
- Full deploy passed on Thor.
- On-device proof captures:
  `scratch/thor-debug/20260519-201721-*`,
  `scratch/thor-debug/20260519-202108-*`,
  `scratch/thor-debug/20260519-202453-*`,
  `scratch/thor-debug/20260519-203233-*`,
  `scratch/thor-debug/20260519-203448-*`, and
  `scratch/thor-debug/20260519-203701-*`.
- Device config caveat capture:
  `scratch/thor-debug/20260519-204503-*`.
- Script-default proof capture:
  `scratch/thor-debug/20260519-205129-*`.

Next actions:

- Treat `a64_inline_fpr_helpers` and `a64_inline_vmx_helpers` as current
  default-on A64 backend speed cleanup, but rely on the Thor script extras for
  existing devices with stale persisted config files.
- Generate focused PPC/static evidence for `8246B408` and nearby producer or
  caller candidates before adding another title-specific shortcut.
- Keep the Blue Dragon speed lane trace-off and CPU-focused unless a capture
  starts showing GPU Commands or GPU VSync as the top wall.

## 2026-05-19 21:16 EDT - Blue Dragon Draw-Wait Caller Sampler

Scope:

- Replace broad compiled-call tracing with a narrow sampler for the hot
  `8246B408` draw-wait function.
- Identify the guest caller that creates the remaining Blue Dragon wait wall.
- Capture a focused PPC dump for that caller loop.

Files changed:

- `android/android_studio_project/app/src/main/java/jp/xenia/emulator/EmulatorActivity.java`
- `src/xenia/cpu/backend/a64/a64_backend.cc`
- `src/xenia/cpu/backend/a64/a64_backend.h`
- `src/xenia/cpu/backend/a64/a64_emitter.cc`
- `src/xenia/cpu/backend/a64/a64_emitter.h`
- `tools/thor/thor_xenia_debug.ps1`
- `AGENTS.md`
- `docs/research/20260519-211650-blue-dragon-draw-wait-caller.md`
- `docs/worklogs/20260519.md`

Commands:

- `git diff --check`
- PowerShell parser check for `tools\thor\thor_xenia_debug.ps1`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonLiveCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 120 -LogLevel 1 -HideAndroidOsd true -HidNopConnected true -HidNopButtonSequence "start@45000:3000;a@68000:3000;a@86000:3000;a@106000:3000" -DisassembleFunctions false -A64InlinePpcThreadFieldLeafHelpers true -Arm64BlueDragonDrawWaitProbe true -Arm64BlueDragonDrawWaitFastpath true -Arm64BlueDragonDrawWaitFastpathHostCounterTime true -Arm64BlueDragonDrawWaitCallerProfile true -Arm64BlueDragonDrawWaitCallerProfileStride 4096 -Arm64BlueDragonDrawWaitCallerProfileBudget 80 -MountCache true -GpuUnknownRegisterLogBudget 0 -XboxkrnlNtCreateFileFailLogBudget 0`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonLiveCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 45 -LogLevel 2 -HideAndroidOsd true -HidNopConnected true -HidNopButtonSequence "start@45000:3000" -DisassembleFunctions true -DisassembleFunctionFilter "8246E618,8246B408,8248B040,8246DBB0" -A64InlinePpcThreadFieldLeafHelpers true -Arm64BlueDragonDrawWaitProbe true -Arm64BlueDragonDrawWaitFastpath true -Arm64BlueDragonDrawWaitFastpathHostCounterTime true -MountCache true -GpuUnknownRegisterLogBudget 0 -XboxkrnlNtCreateFileFailLogBudget 0`

Findings:

- The old broad compiled-call trace was too intrusive for this hot polling
  route; it parked before producing useful evidence.
- The new default-off caller sampler records guest LR every configured stride
  only inside `8246B408`.
- Capture `scratch/thor-debug/20260519-210937-*` reached the Blue Dragon loading
  spinner and logged 80/80 caller samples as LR `8246E6A4`
  (`sub_8246E618+8C`).
- Sampled registers were stable: `r1 701BF8E0`, `r3 701BF930`, `r29 00000003`,
  `r30 0000000F`, `r31 4000E880`.
- Capture `scratch/thor-debug/20260519-211237-*` reached the Blue Dragon title /
  press START state and dumped `8246E618`.
- `8246E618` sets up a wait state with `8246B258`, loops on
  `8246B408(wait_state)`, rechecks progress through `[r31+0x2A10]`,
  `[r31+0x2A1C]`, and `r30`, then cleans up with `8246B288`.

Validation:

- The sampler route produced useful caller evidence without crashing.
- The focused dump route still reached the title screen with caller profiling
  disabled, so the sampler patch did not break the known route when idle.

Next actions:

- Dump/inspect `8246B258` and `8246B288` before attempting a full `8246E618`
  caller-loop fastpath.
- If setup/cleanup are simple enough, prototype a title-specific `8246E618`
  generated-body fastpath or loop-level low-overhead backoff.
- Keep native sleeps out of this path unless a future wait-producer analysis
  proves they are safe.

## 2026-05-19 21:33 EDT - Inline Draw-Wait At Hot Caller

Scope:

- Inspect the setup and cleanup functions around the `8246E618` caller loop.
- Add a default-off cvar that inlines the validated `8246B408` wait predicate
  at the hot `8246E618 -> 8246B408` call site.
- Validate whether the inline route stays alive and what profile wall appears
  behind `8246B408`.

Files changed:

- `android/android_studio_project/app/src/main/java/jp/xenia/emulator/EmulatorActivity.java`
- `src/xenia/cpu/backend/a64/a64_backend.cc`
- `src/xenia/cpu/backend/a64/a64_emitter.cc`
- `src/xenia/cpu/backend/a64/a64_emitter.h`
- `tools/thor/thor_xenia_debug.ps1`
- `AGENTS.md`
- `docs/research/20260519-213330-blue-dragon-inline-draw-wait-caller.md`
- `docs/worklogs/20260519.md`

Commands:

- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonLiveCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 55 -LogLevel 2 -HideAndroidOsd true -HidNopConnected true -HidNopButtonSequence "start@45000:3000" -DisassembleFunctions true -DisassembleFunctionFilter "8246B258,8246B288,8246E618,8246B408" -A64InlinePpcThreadFieldLeafHelpers true -Arm64BlueDragonDrawWaitProbe true -Arm64BlueDragonDrawWaitFastpath true -Arm64BlueDragonDrawWaitFastpathHostCounterTime true -MountCache true -GpuUnknownRegisterLogBudget 0 -XboxkrnlNtCreateFileFailLogBudget 0`
- `git diff --check`
- PowerShell parser check for `tools\thor\thor_xenia_debug.ps1`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 130 -PerfSampleSeconds "70,115" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 20 -Arm64SpeedProfileMinDelta 1 -A64InlinePpcThreadFieldLeafHelpers true -Arm64BlueDragonDrawWaitProbe true -Arm64BlueDragonDrawWaitFastpath true -Arm64BlueDragonDrawWaitFastpathHostCounterTime true -Arm64BlueDragonDrawWaitInlineInCaller true -MountCache true`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonLiveCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 75 -LogLevel 1 -HideAndroidOsd true -HidNopConnected true -HidNopButtonSequence "start@45000:3000;a@68000:3000" -DisassembleFunctions false -A64InlinePpcThreadFieldLeafHelpers true -Arm64BlueDragonDrawWaitProbe true -Arm64BlueDragonDrawWaitFastpath true -Arm64BlueDragonDrawWaitFastpathHostCounterTime true -Arm64BlueDragonDrawWaitInlineInCaller true -MountCache true -GpuUnknownRegisterLogBudget 0 -XboxkrnlNtCreateFileFailLogBudget 0`
- `Start-Sleep -Seconds 45; powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode Capture -DeviceSerial c3ca0370`
- `Start-Sleep -Seconds 80; powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode Capture -DeviceSerial c3ca0370`

Findings:

- `8246B258` is tiny wait-state setup, but `8246B288` is cleanup/stat
  accounting and may call through `[object+0x330C]`, so a full `8246E618`
  replacement is riskier than inlining only the hot wait predicate.
- Added `arm64_blue_dragon_draw_wait_inline_in_caller`, routed through Android
  and the Thor script as `-Arm64BlueDragonDrawWaitInlineInCaller`.
- The inline call route requires the base `8246B408` fastpath and only fires for
  current guest function `8246E618` and direct call target `8246B408`.
- Speed capture `scratch/thor-debug/20260519-212544-*` reached the loading
  spinner and removed `8246B408` from the top speed-profile entries.
- The newly exposed hot set includes `827294CC`, `8272A3A4`, `8272A8E8`,
  `826C5620`, `826BF770`, and critical-section / IRQL helpers.
- Live route `scratch/thor-debug/20260519-212846-*` plus follow-ups
  `20260519-213117-*` and `20260519-213255-*` reached the Microsoft Game
  Studios scene with inline-in-caller enabled.

Validation:

- `git diff --check` passed with normal CRLF warnings only.
- PowerShell parser check passed.
- Native core build passed.
- Full deploy passed on Thor.
- The inline route had no searched fatal signal, AndroidRuntime crash,
  `VK_ERROR_DEVICE_LOST`, GPU-hung, or ANR markers in the checked captures.

Next actions:

- Keep `arm64_blue_dragon_draw_wait_inline_in_caller` default-off until a
  same-route A/B proves a real time-to-scene speed win.
- Use it as a profiling tool to expose the hot functions behind `8246B408`.
- Classify `827294CC`, `8272A3A4`, `8272A8E8`, `826C5620`, and `826BF770`
  before adding the next shortcut.

## 2026-05-19 21:43 EDT - Hot Helper Classification and Memcpy Fastpath

Scope:

- Classify the hot functions exposed by the inline draw-wait run.
- Add a default-off native fastpath for Blue Dragon's hot `826BF770` byte-copy
  helper.
- Validate that the combined inline draw-wait + memcpy route still launches on
  Thor.

Files changed:

- `android/android_studio_project/app/src/main/java/jp/xenia/emulator/EmulatorActivity.java`
- `src/xenia/cpu/backend/a64/a64_backend.cc`
- `src/xenia/cpu/backend/a64/a64_emitter.cc`
- `src/xenia/cpu/backend/a64/a64_emitter.h`
- `tools/thor/thor_xenia_debug.ps1`
- `AGENTS.md`
- `docs/research/20260519-214300-blue-dragon-hot-helper-fastpaths.md`
- `docs/worklogs/20260519.md`

Commands:

- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonLiveCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 95 -LogLevel 2 -HideAndroidOsd true -HidNopConnected true -HidNopButtonSequence "start@45000:3000;a@68000:3000" -DisassembleFunctions true -DisassembleFunctionFilter "827294CC,8272A3A4,8272A8E8,826C5620,826BF770,8273EF84,8273EF74,8273F7B4,8273F7C4" -A64InlinePpcThreadFieldLeafHelpers true -Arm64BlueDragonDrawWaitProbe true -Arm64BlueDragonDrawWaitFastpath true -Arm64BlueDragonDrawWaitFastpathHostCounterTime true -Arm64BlueDragonDrawWaitInlineInCaller true -MountCache true -GpuUnknownRegisterLogBudget 0 -XboxkrnlNtCreateFileFailLogBudget 0`
- `git diff --check`
- PowerShell parser check for `tools\thor\thor_xenia_debug.ps1`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 130 -PerfSampleSeconds "70,115" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 20 -Arm64SpeedProfileMinDelta 1 -A64InlinePpcThreadFieldLeafHelpers true -Arm64BlueDragonDrawWaitProbe true -Arm64BlueDragonDrawWaitFastpath true -Arm64BlueDragonDrawWaitFastpathHostCounterTime true -Arm64BlueDragonDrawWaitInlineInCaller true -Arm64BlueDragonMemcpyFastpath true -MountCache true`

Findings:

- `826BF770` is a byte-copy / `memcpy`-shaped helper.
- `826C5620` is ASCII case-insensitive string-compare shaped.
- `RtlEnterCriticalSection`, `RtlLeaveCriticalSection`,
  `KeRaiseIrqlToDpcLevel`, and `KfLowerIrql` are single-`sc 2`
  syscall/import thunks into HLE.
- `827294CC` is a compact jump-table dispatch helper.
- `8272A3A4` and `8272A8E8` look like byte/bitstream packing or decompression
  loops.
- Added `arm64_blue_dragon_memcpy_fastpath`, routed through Android and
  `tools\thor\thor_xenia_debug.ps1` as `-Arm64BlueDragonMemcpyFastpath`.
- Combined speed capture `scratch/thor-debug/20260519-214026-*` reached the
  loading spinner with APK SHA-256
  `340CB68339C9AA6E74BE6030B3446ACDA3BDF9FA59DB102F31F952DCCCEC73A3`.
- `826BF770` still appears in the top speed profile because the current profiler
  counts entries, not elapsed time; this does not prove the body fastpath failed.
- Perf at 115s still showed Main Thread and GPU Commands both around one full
  core, so the route is now mixed CPU/GPU.

Validation:

- `git diff --check` passed with normal CRLF warnings only.
- PowerShell parser check passed.
- Native core build passed.
- Full deploy passed on Thor.
- The combined speed capture had no searched fatal signal, AndroidRuntime crash,
  `VK_ERROR_DEVICE_LOST`, GPU-hung, or ANR markers.

Next actions:

- Keep `arm64_blue_dragon_memcpy_fastpath` default-off until same-route A/B
  proves a real time-to-scene win.
- Add a better timing/cost lane, or use simpleperf, before judging function-body
  fastpaths by entry-count profiles.
- Consider a default-off `826C5620` string-compare fastpath after the next A/B.
