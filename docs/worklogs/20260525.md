# 2026-05-25 Worklog

## Blue Dragon fpscr CFG Writeback Audit

- Added a default-off `arm64_blue_dragon_fpscr_cfg_writeback_audit` skeleton for
  Blue Dragon `82287788`. The patch counts static fpscr sites, dynamic
  load/store traffic, CFG/external writeback stores, required call writeback
  PCs, and payload materialization without changing generated behavior.
- Wired the flag through Android launch extras and `tools/thor/thor_xenia_debug.ps1`.
- `NativeCore` and `FullDeploy` passed. Installed APK SHA256:
  `754EC8F2F04A71F45603EFFA79A8E6843CCBADE7A8B201DC295337E7E5FA390A`.
- Route-clean Thor capture:
  `scratch/thor-debug/20260525-132931-*`. The screenshot reached the visible
  opening sky/dragon-wing route and the fatal-marker search was clean.
- Final audit counters confirmed the plan is dynamic:
  `loads=2153538`, `stores=2153538`,
  `cfg_transition_stores=2116145`, `external_transition_stores=37393`,
  `required_call_writebacks=698088`,
  `call_82287EDC=678946`, `call_82287EE4=9571`, `call_82288220=9571`,
  `call_82287ED4=0`, `payload_materializations=0`,
  `unclassified_stores=0`.
- Final body-time still points at CPU/JIT work, not a Vulkan wall:
  `82282490=33811022`, `82281D28=7646617`, `82287788=3872874`.
  Final perf was Main Thread `92.3%`, GPU Commands `11.5%`, Draw Thread `3.8%`.

Next: do not claim a speed win or run a quiet fpscr speed A/B yet. Either do a
source/codegen design pass for a default-off fpscr CFG carrier payload that
preserves writebacks at `82287ED4`, `82287EDC`, `82287EE4`, and `82288220`, or
return to caller-local edge-variant storage for `82282490:82282598 -> 82287788`.

## 82287788 fpscr CFG Carrier Design Tool

- Added `tools/thor/thor_a64_fpscr_cfg_carrier_design.ps1`, an offline
  source-and-counter design auditor for the future `82287788` fpscr carrier.
- The tool parses the latest route-clean fpscr CFG audit row from
  `scratch/thor-debug/20260525-132931-speed-logcat-filtered.txt`, checks the
  A64 direct/tail call ABI, confirms the existing f1 carrier is callee-local,
  and verifies fpscr load/store/call-writeback counter coverage.
- Validation passed:
  `powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_a64_fpscr_cfg_carrier_design.ps1`.
- Reported dynamic contract: `loads=2153538`, `stores=2153538`,
  `cfg_transition_stores=2116145`, `external_transition_stores=37393`,
  `required_call_writebacks=698088`, `payload_materializations=0`, and
  `unclassified_stores=0`.
- Design conclusion: no behavior patch or quiet fpscr speed A/B yet. The safe
  behavior shape is a default-off callee-local fpscr dirty stack slot with
  normal-entry fallback and exact writebacks at `82287ED4`, `82287EDC`,
  `82287EE4`, and `82288220`, or a return to caller-local edge-variant storage
  for `82282490:82282598 -> 82287788`.

Next: implement only a default-off `arm64_blue_dragon_fpscr_cfg_carrier_fastpath`
skeleton with materialization/writeback/bailout counters, or switch lanes back
to caller-local edge-variant storage. Do not run a quiet fpscr speed A/B until
payload materialization is intentionally nonzero and all writebacks remain
accounted.

## Mature A64 Backend Research / Skill Update

- Paused the half-started fpscr carrier skeleton and switched to the user's
  requested research/process lane.
- Added
  `docs/research/20260525-143937-mature-a64-emulator-backend-patterns.md`.
  Primary-source review covered Dolphin `JitArm64`, PPSSPP ARM64 JIT,
  Dynarmic ARM64 backend/design docs, FEX development docs, DuckStation, and
  Arm AAPCS64.
- Main finding: mature mobile emulator speed is not a Vulkan-first fix for the
  current Blue Dragon wall. The common A64 pattern is register-cache quality,
  static host-register contracts, block linking, fastmem/fault fallback,
  helper ABI discipline, vector/FP state contracts, and deterministic backend
  tests.
- Updated `.agents/skills/xenia-a64-speed-hotpath/SKILL.md` with a Mature A64
  Backend Gate that must run before more repeated narrow PC-gated fastpaths.
- Updated `.agents/skills/aarch64-snapdragon-jit-port/SKILL.md` with a mature
  backend checklist covering register allocation, static registers, block
  linking, fastmem, helper ABI, NEON/FPCR/FPSCR, and tests.
- Updated `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md` so
  stale heartbeat/autopilot work pivots to the backend maturity gate when
  Main Thread/A64 generated-code remains the wall.

Next: build a source/offline A64 maturity gap report for the current
`82282490 -> 82287788` route before another Thor speed patch, unless a fresh
capture changes the wall away from Main Thread/A64 generated code.

## A64 Mature Backend Gap Report Tool

- Added `tools/thor/thor_a64_mature_backend_gap_report.ps1`, a deterministic
  source/offline report for the new mature-backend gate.
- Validation command:
  `powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_a64_mature_backend_gap_report.ps1`.
- Report written to
  `docs/research/20260525-145216-a64-mature-backend-gap-report.md`.
- The tool confirms the route is still CPU/JIT-heavy and ranks the next lanes:
  guest register/cache design first, `82282490:82282598 -> 82287788`
  edge-variant storage second, fpscr CFG carrier skeleton third, and
  fastmem/Vulkan only after fresh evidence moves the wall.
- Key source findings: A64 already has fixed x19/x20/x21 host registers for
  backend context, PPC context, and membase, but only x22-x28 are general
  allocatable GPRs; existing context cache/live-in-r1 probes are weak negative
  evidence; direct calls and resolve paths exist but the hot edge remains
  blocked by singleton normal-entry/indirection contracts.

Next: build a structural register-cache gap report for `82282490 -> 82287788`
that joins HIR context traffic, register allocation/spill pressure, helper-call
clobbers, and the previous negative context-cache/local-slot/pinned-r1 probes.

## 82282490 -> 82287788 Register Cache Gap Report

- Added `tools/thor/thor_a64_register_cache_gap_report.ps1`, a deterministic
  offline report that fuses `82282490` HIR state-span traffic, GPR promotion
  candidates, the `82282598 -> 82287788` roundtrip audit, A64 source register
  contracts, and prior negative context-cache/local-slot/live-in/carrier
  guardrails.
- Validation command:
  `powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_a64_register_cache_gap_report.ps1`.
- Report written to
  `docs/research/20260525-150348-82282490-82287788-register-cache-gap.md`.
- Key output: `82282490` has `GPR=1108`, `VMX=528`, `CR=345`,
  `whole_gpr_loads=546`, `whole_gpr_stores=562`, `context_barriers=213`, top
  state-cache candidates `r[1]` score `220` and `r[11]` score `169`, and source
  still gives only seven allocatable A64 GPRs (`x22-x28`) after fixed
  `x19/x20/x21`.
- Edge output: `8228252C -> 82287788` has `calls_total=1691272`,
  `body_ticks_total=5653971`, and `ticks_per_call=4`. The live edge payloads
  are visible: `r[3]` is mutable/high-risk, `f[1]` is read-only/medium-risk but
  already not speed-proven, `fpscr` is mutable/very-high-risk with required
  writebacks, and `lr` is call-link state.
- Decision: no generated behavior patch from this report. The next patch should
  be a default-off guest-state register-cache audit/design skeleton at the
  HIR/A64 register-allocation boundary, or caller-local/side-table edge-variant
  storage for `82282490:82282598 -> 82287788` with normal-entry fallback and
  payload counters.

Next: do not rerun the old `r[1]`, stvewx, `f[1]`, or fpscr behavior A/Bs.
Implement only a counter-first structural state-cache or edge-variant storage
skeleton, then build/deploy/capture on Thor only after runtime behavior changes.

## A64 Guest-State Register Cache Design Tool

- Cleared stale read-only Codex/app git probes that were blocking heartbeat
  continuations, then stayed in the offline evidence lane.
- Added `tools/thor/thor_a64_guest_state_cache_design.ps1`, a deterministic
  source/HIR design report for the next structural guest-state cache slice.
- Validation command:
  `powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_a64_guest_state_cache_design.ps1`.
- Report written to
  `docs/research/20260525-152958-a64-guest-state-cache-design.md`.
- Key output: no structural `arm64_guest_state_register_cache` skeleton exists
  yet; `82282490` still has `GPR=1108`, `whole_gpr_loads=546`,
  `whole_gpr_stores=562`, top candidates `r[1]` score `220` and `r[11]`
  score `169`, and hot edge `8228252C -> 82287788` at
  `calls_total=1691272`.
- Source boundary read: current `ContextPromotionPass` is per-block mem2reg,
  `RegisterAllocationPass` is per-block and spills to locals, the A64
  emit-time context cache resets per block, and A64 exposes only seven
  allocatable GPRs after fixed `x19/x20/x21`.
- Decision: no generated behavior patch. The next code slice should be a
  default-off counter-only `arm64_guest_state_register_cache_audit`, likely in
  or adjacent to `ContextPromotionPass`, with no store elision, no context-load
  replacement, no payload materialization, and counters for clean/dirty hit
  opportunities, misses, flush reasons, external visibility, and estimated
  spill pressure.

Next: implement that counter-only guest-state cache audit skeleton for clean
INT64 `r[1]` / `r[11]` with an `82282490` function filter, validate with
NativeCore, then run a route-clean Thor audit before any behavior change or
quiet speed A/B.

## A64 Guest-State Register Cache Audit Skeleton

- Implemented default-off `arm64_guest_state_register_cache_audit` and
  `arm64_guest_state_register_cache_audit_function` in `ContextPromotionPass`.
- Scope is counter-only and behavior-neutral: no HIR load replacement, no store
  elision, no payload materialization, and normal fallback remains active for
  every candidate.
- Initial tracked slots are clean INT64 `r[1]` and `r[11]`, matching the
  structural register-cache design report. The audit logs candidate loads and
  stores, clean/dirty possible hits, miss reasons, flush reasons, estimated
  spill pressure, per-slot totals, `normal_fallback`, and
  `payload_materializations_allowed=0 behavior_changed=0`.
- Added Thor launch plumbing:
  `-Arm64GuestStateRegisterCacheAudit true` and
  `-Arm64GuestStateRegisterCacheAuditFunction 82282490`, plus the matching
  Android `EmulatorActivity` cvar forwarding.
- Research note:
  `docs/research/20260525-154617-arm64-guest-state-register-cache-audit.md`.
- Validation:
  `git diff --check` passed with expected CRLF warnings only;
  `tools\thor\thor_build.ps1 -Mode NativeCore` passed;
  `tools\thor\thor_build.ps1 -Mode ApkShell` passed.

Next: full-deploy this APK and run a route-clean Thor audit capture scoped to
`82282490`. Do not patch register-cache behavior or run a quiet speed A/B until
the audit rows prove useful hit volume and manageable flush/spill pressure.

## A64 Guest-State Register Cache Audit Capture

- Start/end: 2026-05-25 15:52-16:02 EDT.
- Branch: `master`.
- FullDeploy command:
  `powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`.
- First capture command used
  `-Arm64GuestStateRegisterCacheAuditFunction 82282490` and reached the visible
  opening sky / dragon-wing route, but emitted no guest-state register-cache
  audit rows. This proved the old bare filter spelling was a harness problem,
  not useful counter evidence.
- Corrected capture command used
  `-Arm64GuestStateRegisterCacheAuditFunction 0x82282490` with delayed
  body-time comparators `82282490,82281D28,82287788`.
- Corrected capture paths:
  `scratch/thor-debug/20260525-155824-speed-logcat.txt`,
  `scratch/thor-debug/20260525-155824-speed-logcat-filtered.txt`,
  `scratch/thor-debug/20260525-155824-meta.txt`,
  `scratch/thor-debug/20260525-155824-screenshot.png`, and
  `scratch/thor-debug/20260525-155824-perf-final.txt`.
- APK SHA:
  `4ED7B6FE876F5ADCB7D11F70BDC42AC4D7A1BF916D680E4B22BEA861069440D6`.
- Fatal-marker search was clean.
- Audit row landed for `82282490` with `candidate_loads=778`,
  `candidate_stores=285`, `clean_hits_possible=357`,
  `dirty_hits_possible=247`, `normal_fallback=1063`,
  `estimated_spill_pressure=2`, `payload_materializations_allowed=0`, and
  `behavior_changed=0`.
- Per-slot totals: `r1` was `433/11/322/4/444`
  loads/stores/clean_hits/dirty_hits/fallback, while `r11` was
  `345/274/35/243/619`.
- Final body-time rows kept the same CPU/JIT wall:
  `82282490=31080764`, `82281D28=7471709`, `82287788=3379246`,
  `82282388=2165366`, and `82282410=2084225`.
- Final perf still had Main Thread at `100%` and GPU Commands at `7.6%`, so
  this is not a Vulkan pivot.
- Research note:
  `docs/research/20260525-160247-arm64-guest-state-register-cache-audit-capture.md`.
- Harness update: corrected the previous audit note and local skills to use the
  `0x82282490` function-filter spelling for guest-PC filters.

Next: implement a default-off, function-filtered `r1` clean-load replacement
prototype for `0x82282490` only, with no store elision, no `r11` dirty caching,
no payload materialization, and normal fallback preserved. First proof should
be a route-clean Thor correctness/counter capture, not a quiet speed A/B.

## r1 Clean-Load Source Gate

- Start/end: 2026-05-25 16:13-16:15 EDT.
- Source-gated the planned same-block `r1` clean-load prototype before editing
  runtime behavior.
- `ContextPromotionPass` runs the new
  `arm64_guest_state_register_cache_audit` before `PromoteBlock`, and
  `PromoteBlock` already rewrites same-block `LOAD_CONTEXT` to `ASSIGN` when a
  prior in-block context value exists.
- This means the route-clean audit row's `clean_hits_possible=357` and `r1`
  `clean_hits=322` are pre-promotion upper bounds, not proof of net-new
  generated-code savings.
- Research note:
  `docs/research/20260525-161334-r1-clean-load-source-gate.md`.

Next: do not implement the same-block `r1` clean-load replacement unchanged.
Add or run a default-off post-`PromoteBlock` residual audit for `0x82282490`
that counts remaining `r1` / `r11` load/store traffic, residual clean/dirty
opportunities, boundary flush reasons, and spill pressure with behavior
unchanged. If residual `r1` volume collapses, return to caller-local/side-table
edge-variant storage for `82282490:82282598 -> 82287788`.

## A64 Guest-State Register Cache Residual Audit Skeleton

- Start/end: 2026-05-25 16:19-16:22 EDT.
- Implemented default-off
  `arm64_guest_state_register_cache_residual_audit` and
  `arm64_guest_state_register_cache_residual_audit_function`.
- The residual audit runs after normal `PromoteBlock` and dead-store removal,
  reusing the existing `r1` / `r11` guest-state register-cache counters while
  keeping `payload_materializations_allowed=0` and `behavior_changed=0`.
- Existing pre-promotion audit rows remain named
  `A64 guest-state register-cache audit`; residual rows are named
  `A64 guest-state register-cache residual audit`.
- Added Android launch forwarding and Thor flags:
  `-Arm64GuestStateRegisterCacheResidualAudit true` and
  `-Arm64GuestStateRegisterCacheResidualAuditFunction 0x82282490`.
- Research note:
  `docs/research/20260525-162250-arm64-guest-state-register-cache-residual-audit.md`.
- Validation:
  PowerShell parser for `tools\thor\thor_xenia_debug.ps1` passed;
  `tools\thor\thor_build.ps1 -Mode NativeCore` passed;
  `tools\thor\thor_build.ps1 -Mode ApkShell` passed with only the existing
  Android Gradle plugin warning and Java deprecation notes.

Next: run FullDeploy, then a route-clean Thor residual audit capture using
`-Arm64GuestStateRegisterCacheResidualAudit true` and
`-Arm64GuestStateRegisterCacheResidualAuditFunction 0x82282490`, with delayed
body-time comparators `82282490,82281D28,82287788`. Do not patch generated
behavior or run a quiet speed A/B until those residual counters are captured.

## Cemu Android Comparison Lane

- Start/end: 2026-05-25 16:27-16:28 EDT.
- Checked current public Cemu sources:
  `https://github.com/cemu-project/Cemu`,
  `https://github.com/SSimco/Cemu`, and
  `https://github.com/SapphireRhodonite/Cemu`.
- Updated `AGENTS.md` with a Cemu / Wii U Android comparison lane for
  xenia-thor, with an explicit clean-room/license guardrail: learn architecture,
  profiling, Android packaging, input, and settings UX patterns from public
  emulator work, but do not copy MPL-2.0 Cemu source into the BSD-style Xenia
  repo without an explicit file-level license plan.
- Captured the hardware comparison rule: Wii U and Xbox 360 similarities make
  Cemu Android useful design pressure, but Xenia must still validate every
  borrowed idea on Thor because Xenon SMT, VMX128, Xenos EDRAM/resolve behavior,
  kernel/HLE contracts, and synchronization pressure differ.
- Captured the current pivot rule: Blue Dragon remains CPU/JIT-bound on Thor,
  so do not use Cemu Android performance as a reason to pivot to broad Vulkan
  work unless future captures show GPU Commands/present/submit/pipeline stalls
  overtaking the main-thread wall.
- Added the Android UX mandate: handheld-facing settings, per-game profiles,
  controller mapping, import/export, safe defaults, expert research-cvar page,
  reliable AYN Thor built-in controls, external USB/Bluetooth controllers,
  dead zones, trigger handling, rumble, and an input test screen.
- Research note:
  `docs/research/20260525-162738-cemu-android-comparison-lane.md`.

Next: ask the user whether the first UX slice should be the minimal Thor
built-in controller fix, a Cemu-style settings/profile page, or both. Continue
the Blue Dragon speed lane separately with the residual register-cache audit
capture.

## DBT Research Triage And Cemu-Style Settings

- Start/end: 2026-05-25 16:45-16:46 EDT.
- Verified the user's DBT research list against public sources where possible:
  Elevator static translation, Partial Cross-Compilation / Mixed Execution,
  USENIX ATC 2019 learning-based DBT rules, Arancini, AtoMig, mainline Cemu,
  and the SSimco Android Cemu fork.
- Updated `AGENTS.md` with a Binary Translation Research Triage section:
  static translation is a long-term offline specialization/checking lane;
  partial cross-compilation maps to hot function/edge variants with normal-entry
  fallback; learning-based rule generation needs symbolic equivalence and tests;
  Arancini/AtoMig reinforce ARM weak-memory discipline; host/GPU latency
  research reinforces the current no-broad-Vulkan-pivot rule.
- Recorded the user's UX preference in `AGENTS.md`: build Cemu-style
  settings/profile UX, including GPU, audio, controller/input, runtime, and
  expert/research config surfaces.
- Started the first Android settings scaffold:
  `SettingsActivity`, `XeniaAndroidSettings`, a launcher Settings entry, and a
  persistent launch-argument bundle for GPU/audio/controller/runtime cvars.
- Game launch now reads saved settings rather than hard-coding only
  `gpu=vulkan`, `cpu=arm64`, `apu=nop`, and `hid=android`.
- Checked Thor CPU features over ADB: AYN Thor / `kalama` exposes `asimd`,
  `asimddp`, `i8mm`, `bf16`, `asimdfhm`, `fcma`, `crc32`, and related features,
  but no `sve` / `sve2`. Updated `AGENTS.md` with a proof-gated VMX128 inline
  patching lane that prioritizes NEON/AdvSIMD and dot-product/I8MM only when
  opcode semantics match.
- Research note:
  `docs/research/20260525-164554-dbt-research-and-cemu-style-settings.md`.

Next: validate `ApkShell`, then deploy to Thor and verify Settings opens,
persists profile changes, and launches Blue Dragon with the selected bundle.

## Mobile TBR GPU Memory Locality Thesis

- Start/end: 2026-05-25 16:50-16:52 EDT.
- Researched Diya Joseph's 2024 PhD thesis,
  `Improving Memory Access Efficiency for Real-time Rendering in Tile-based GPU
  Architectures`.
- Added
  `docs/research/20260525-165028-mobile-tbr-gpu-memory-thesis.md`.
- Decision: useful for the future Vulkan/Adreno counter lane, but not a reason
  to pivot Blue Dragon away from A64/JIT while Thor captures still show Main
  Thread as the wall.
- Updated `AGENTS.md` with a Mobile TBR / GPU Memory Locality lane covering
  render-pass load/store behavior, resolve bytes, texture upload/copy bytes,
  descriptor/pipeline churn, frame-to-frame resource reuse, synchronization
  stalls, and Adreno/AGI bandwidth/cache counters.

Next: keep current Blue Dragon speed work on A64 guest-state/register-cache,
VMX128 lowering, and edge/function variants until captures show a GPU wall.

## A64 Guest-State Register Cache Residual Audit Capture

- Start/end: 2026-05-25 16:57-17:03 EDT.
- Branch: `master`.
- FullDeploy passed and installed commit `85c422d2b`.
- APK SHA:
  `18097BA09F0C50596DED67D4251E4401FC7D458AA5EF7AAC7365B362D14CD19A`.
- Capture command:
  `powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -Arm64GuestStateRegisterCacheResidualAudit true -Arm64GuestStateRegisterCacheResidualAuditFunction 0x82282490 -Arm64SpeedProfileBodyTimeFilter "82282490,82281D28,82287788" -Arm64SpeedProfileBodyTimeAfterMs 120000 -StopAppAfterCapture true`.
- Capture paths:
  `scratch/thor-debug/20260525-165936-speed-logcat.txt`,
  `scratch/thor-debug/20260525-165936-speed-logcat-filtered.txt`,
  `scratch/thor-debug/20260525-165936-meta.txt`,
  `scratch/thor-debug/20260525-165936-screenshot.png`, and
  `scratch/thor-debug/20260525-165936-perf-final.txt`.
- The screenshot reached the visible opening sky / dragon-wing route, and the
  fatal-marker search was clean.
- Residual audit row for `82282490`:
  `candidate_loads=174`, `candidate_stores=139`,
  `clean_hits_possible=0`, `dirty_hits_possible=0`,
  `normal_fallback=313`, `estimated_spill_pressure=2`,
  `payload_materializations_allowed=0`, `behavior_changed=0`.
- Per-slot totals:
  `r1=107/11/0/0/118` and `r11=67/128/0/0/195`
  loads/stores/clean_hits/dirty_hits/fallback.
- Final body-time stayed CPU/JIT-heavy:
  `82282490=29927708`, `82281D28=7155788`, `82287788=3162326`,
  `82282388=2071249`, and `82282410=1994466`.
- Final perf stayed Main Thread-bound:
  Main Thread `96.1%`, GPU Commands `11.5%`, XMA Decoder `7.6%`,
  Draw Thread `3.8%`.
- Research note:
  `docs/research/20260525-170307-arm64-register-cache-residual-audit-capture.md`.

Next: do not implement the stale same-block `r1` clean-load replacement,
`r11` dirty caching, store elision, or a quiet speed A/B from this lane. Return
to caller-local or side-table edge-variant payload storage for
`82282490:82282598 -> 82287788`, with normal-entry fallback and counter-only
payload/materialization diagnostics first.

## Thor Hardware Acceleration Menu

- Start/end: 2026-05-25 17:13-17:13 EDT.
- Researched current hardware-acceleration options for AYN Thor / Snapdragon 8
  Gen 2 / Adreno 740 using Arm ACLE, Khronos Vulkan guide/tutorial material,
  Android GPU Inspector docs, Qualcomm Snapdragon Profiler and Game Toolkit
  docs, Qualcomm GSR2 notes, Partial Cross-Compilation DBT work, Box64 Dynarec
  notes, and the mobile TBR thesis.
- Added
  `docs/research/20260525-171305-thor-hardware-acceleration-menu.md`.
- Updated `AGENTS.md` with the current hardware plan: prioritize A64/JIT
  generated-code quality and caller-local/side-table edge variants while Main
  Thread remains the wall; use Thor's NEON/dot/I8MM/CRC/LSE features only after
  opcode/source/counter proof; push only Xenos-like bulk graphics transforms to
  Adreno after counters show CPU-side graphics cost.
- Current "get GPU work to GPU" translation: add a measured graphics-work audit
  for texture deswizzle/endian/tile transforms, EDRAM resolves, format
  conversions, vertex fetch unpack, clears/copies, uploads, readbacks, queue
  waits, barriers, render-pass load/store behavior, and pipeline churn before a
  default-off Vulkan compute or render-pass experiment.

Next: main speed lane remains the `82282490:82282598 -> 82287788`
caller-local/side-table payload probe. Parallel GPU lane should be a counter
audit for CPU-side graphics bulk transforms, not a broad Vulkan rewrite.

## Edge-Variant Payload-Scope Audit

- Start/end: 2026-05-25 17:27-17:32 EDT.
- Branch: `master`.
- Added behavior-neutral payload-scope counters to the default-off
  `arm64_blue_dragon_edge_variant_audit` for exact edge
  `82282490:82282598 -> 82287788`.
- The patch only marks `A64BackendContext` while the hot-edge call is active,
  clears it after return, and counts active/inactive `f[1]` reads plus active
  call kills. It does not materialize payloads, alter generated guest behavior,
  replace the normal `82287788` entry, or change the global indirection slot.
- Extended `tools/thor/thor_a64_edge_variant_storage_plan.ps1` with optional
  marker / active-read counter parsing and source checks.
- `NativeCore`, `FullDeploy`, the PowerShell parser check, and `git diff --check`
  passed.
- Capture command:
  `powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -Arm64BlueDragonEdgeVariantAudit true -Arm64SpeedProfileBodyTimeFilter "82282490,82287788,82281D28" -Arm64SpeedProfileBodyTimeAfterMs 120000 -StopAppAfterCapture true`.
- Capture paths:
  `scratch/thor-debug/20260525-172734-speed-logcat.txt`,
  `scratch/thor-debug/20260525-172734-speed-logcat-filtered.txt`,
  `scratch/thor-debug/20260525-172734-meta.txt`,
  `scratch/thor-debug/20260525-172734-screenshot.png`, and
  `scratch/thor-debug/20260525-172734-perf-final.txt`.
- APK SHA:
  `12E4BC4F69BA1D0C4A4A249F64C74BD81FB802569B46122644C9242A57DA8CB1`.
- The screenshot reached the visible opening sky / dragon-wing route, and the
  fatal-marker search was clean.
- Final edge counters:
  `eligible_calls=687023`, `normal_fallbacks=687023`,
  `indirection_fallbacks=687023`, `variant_misses=687023`,
  `payload_materializations=0`, `marker_sets=687023`,
  `marker_clears=687023`, `active_f1_reads=2050899`,
  `inactive_f1_reads=3250`, and `active_call_kills=1722545`.
- Derived ratios:
  `normal_fallback_share=100.00%`, `active_f1_reads_per_call=2.99`, and
  `active_call_kills_per_call=2.51`.
- Final body-time stayed CPU/JIT-heavy:
  `82282490=34497956`, `82281D28=7587167`, `82287788=4153275`,
  `82282388=2202765`, and `82282410=2117213`.
- Final perf stayed Main Thread-bound:
  Main Thread `96.2%`, XMA Decoder `11.1%`, Audio Worker `7.4%`,
  GPU Commands `7.4%`, Draw Thread `0.0%`.
- Research note:
  `docs/research/20260525-173225-edge-variant-payload-scope-audit.md`.

Next: do not run a quiet speed A/B or materialize an edge payload from this
audit-only patch. Add per-PC active `f[1]` read / kill-window attribution for
the same marker, or use this marker as safe caller-local scope proof in a
broader `82282490` / `82287788` state-roundtrip design.

## Edge-Variant Per-PC Attribution

- Start/end: 2026-05-25 17:43-17:56 EDT.
- Branch: `master`.
- Added per-PC active `f[1]` read counters and per-PC active call-kill counters
  to the default-off `arm64_blue_dragon_edge_variant_audit` for exact edge
  `82282490:82282598 -> 82287788`.
- This remains behavior-neutral: no payload materialization, no normal-entry
  replacement, no global indirection-slot change, and normal fallback remains
  the only behavior.
- Extended `tools/thor/thor_a64_edge_variant_storage_plan.ps1` to parse the new
  optional per-PC counters while preserving compatibility with older logs.
- `NativeCore`, `FullDeploy`, PowerShell parser validation, and `git diff --check`
  passed.
- First capture `scratch/thor-debug/20260525-174834-*` was invalid for counter
  evidence: fatal-marker search was clean, but the final screenshot was black
  and edge counters stayed zero.
- Retry capture `scratch/thor-debug/20260525-175231-*` used APK SHA
  `FC9CE27AA7B7040CC741417B8B225B351A9624F43BCA444B4CA02480D57068E8`, reached
  the visible opening sky / dragon-wing route, and had a clean fatal-marker
  search.
- Final edge counters:
  `eligible_calls=675279`, `normal_fallbacks=675279`,
  `payload_materializations=0`, `marker_sets=675279`,
  `marker_clears=675279`, `active_f1_reads=2015839`,
  `inactive_f1_reads=3054`, and `active_call_kills=1693099`.
- Active `f[1]` read sites:
  `82287798=675279`, `82287828=333248`, `82287CF8=332673`,
  `82287D10=332673`, `82287D8C=332673`, `82287F1C=9293`, and
  `82287A1C/82287A2C/82287AA4/82287EA8=0`.
- Active kill sites:
  `8228778C=675279`, `82287854=333248`, `82287EDC=665986`,
  `82287EE4=9293`, `82288220=9293`, and `82287ED4=0`.
- Final body-time stayed CPU/JIT-heavy:
  `82282490=34275577`, `82281D28=7553858`, `82287788=4474801`,
  `82282388=2191723`, and `82282410=2109329`.
- Final perf stayed Main Thread-bound:
  Main Thread `100%`, GPU Commands `11.5%`, Draw Thread `7.6%`,
  XMA Decoder `7.6%`, Audio Worker `3.8%`.
- Research note:
  `docs/research/20260525-175612-edge-variant-pc-attribution.md`.

Next: do not materialize payloads or run a quiet speed A/B from this audit-only
patch. Split the `f[1]` kill taxonomy by source/HIR review: helper-preserved
vs child/unknown-call kills, keeping `fpscr` out of the read-only `f[1]` lane.

## Edge f[1] Kill Taxonomy Audit

- Start/end: 2026-05-25 18:38-18:50 EDT.
- Branch: `master`.
- Added behavior-neutral `f[1]` kill taxonomy counters to the default-off
  `arm64_blue_dragon_edge_variant_audit` for exact edge
  `82282490:82282598 -> 82287788`.
- Split the long edge-variant audit logcat row into base, taxonomy, `f1-sites`,
  and `kill-sites` rows because the first route-clean taxonomy capture
  `scratch/thor-debug/20260525-183812-*` proved useful taxonomy totals but
  clipped the late kill-site fields at the logcat tail.
- Updated `tools/thor/thor_a64_edge_variant_storage_plan.ps1` to merge the split
  rows while preserving old single-row log compatibility.
- Classification used for active call-kill PCs:
  helper-preserved `8228778C` / `82287EE4`, child-preserved `82287854` /
  `82287ED4`, return/exit `82287EDC` / `82288220`.
- Validation passed: PowerShell parser check,
  `thor_a64_edge_variant_storage_plan.ps1` on old and new logs,
  `git diff --check` with only CRLF warnings, `NativeCore`, and `FullDeploy`.
- Route capture command:
  `powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 180 -PerfSampleSeconds "120,150,175" -Arm64BlueDragonEdgeVariantAudit true -Arm64SpeedProfileBodyTimeFilter "82282490,82287788,82281D28" -Arm64SpeedProfileBodyTimeAfterMs 120000 -StopAppAfterCapture true`.
- Route-clean capture paths:
  `scratch/thor-debug/20260525-184539-speed-logcat.txt`,
  `scratch/thor-debug/20260525-184539-speed-logcat-filtered.txt`,
  `scratch/thor-debug/20260525-184539-meta.txt`,
  `scratch/thor-debug/20260525-184539-screenshot.png`, and
  `scratch/thor-debug/20260525-184539-perf-final.txt`.
- APK SHA:
  `DEDD20333729C3E6B44CC1E423357147F0A9149D034C6D0A2DB0237AB8BF5C61`.
- The screenshot reached the visible opening sky / dragon-wing route and the
  fatal-marker search was clean.
- Final edge counters:
  `eligible_calls=710511`, `normal_fallbacks=710511`,
  `payload_materializations=0`, `marker_sets=710511`,
  `marker_clears=710511`, `active_f1_reads=2121019`,
  `inactive_f1_reads=3436`, and `active_call_kills=1781437`.
- Taxonomy counters:
  `helper_preserved_calls=720290`, `child_preserved_calls=350636`,
  `return_exit_calls=710511`, and `unknown_call_kills=0`.
- Active `f[1]` read sites:
  `82287798=710511`, `82287828=350636`, `82287CF8=350031`,
  `82287D10=350031`, `82287D8C=350031`, `82287F1C=9779`, and the
  other four audited read PCs were zero.
- Active kill sites:
  `8228778C=710511`, `82287854=350636`, `82287ED4=0`,
  `82287EDC=700732`, `82287EE4=9779`, and `82288220=9779`.
- Final body-time stayed CPU/JIT-heavy:
  `82282490=39738917`, `82281D28=8067997`, `82287788=4878256`,
  `82282388=2341735`, and `82282410=2253406`.
- Final perf remained Main Thread-bound:
  Main Thread `103%`, XMA Decoder `42.3%`, GPU Commands `7.6%`,
  Draw Thread `0.0%`.
- Research note:
  `docs/research/20260525-184957-edge-f1-kill-taxonomy.md`.

Next: do not run a quiet speed A/B or materialize an `f[1]`-only payload from
this taxonomy patch. Use the zero-unknown-kill result as input to a broader
caller-local / side-table edge payload-storage design for
`82282490:82282598 -> 82287788`, with `fpscr` kept in the separate CFG
writeback lane.
