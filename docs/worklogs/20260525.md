# 2026-05-25 Worklog

## Blue Dragon fpscr CFG Writeback Audit

- Added a default-off `arm64_blue_dragon_fpscr_cfg_writeback_audit` skeleton for
  Blue Dragon `82287788`. The patch counts static fpscr sites, dynamic
  load/store traffic, CFG/external writeback stores, required call writeback
  PCs, and payload materialization without changing generated behavior.
- Wired the flag through Android launch extras and `tools/thor/thor_xenia_debug.ps1`.
- `NativeCore` and `FullDeploy` passed. Installed APK SHA256:
  `754EC8F2F04A71F45603EFFA79A8E6843CCBADE7A8B201DC295337E7E5FA390A`.
- Route-clean Thor capture:
  `scratch/thor-debug/20260525-132931-*`. The screenshot reached the visible
  opening sky/dragon-wing route and the fatal-marker search was clean.
- Final audit counters confirmed the plan is dynamic:
  `loads=2153538`, `stores=2153538`,
  `cfg_transition_stores=2116145`, `external_transition_stores=37393`,
  `required_call_writebacks=698088`,
  `call_82287EDC=678946`, `call_82287EE4=9571`, `call_82288220=9571`,
  `call_82287ED4=0`, `payload_materializations=0`,
  `unclassified_stores=0`.
- Final body-time still points at CPU/JIT work, not a Vulkan wall:
  `82282490=33811022`, `82281D28=7646617`, `82287788=3872874`.
  Final perf was Main Thread `92.3%`, GPU Commands `11.5%`, Draw Thread `3.8%`.

Next: do not claim a speed win or run a quiet fpscr speed A/B yet. Either do a
source/codegen design pass for a default-off fpscr CFG carrier payload that
preserves writebacks at `82287ED4`, `82287EDC`, `82287EE4`, and `82288220`, or
return to caller-local edge-variant storage for `82282490:82282598 -> 82287788`.

## 82287788 fpscr CFG Carrier Design Tool

- Added `tools/thor/thor_a64_fpscr_cfg_carrier_design.ps1`, an offline
  source-and-counter design auditor for the future `82287788` fpscr carrier.
- The tool parses the latest route-clean fpscr CFG audit row from
  `scratch/thor-debug/20260525-132931-speed-logcat-filtered.txt`, checks the
  A64 direct/tail call ABI, confirms the existing f1 carrier is callee-local,
  and verifies fpscr load/store/call-writeback counter coverage.
- Validation passed:
  `powershell -NoProfile -ExecutionPolicy Bypass -File tools\thor\thor_a64_fpscr_cfg_carrier_design.ps1`.
- Reported dynamic contract: `loads=2153538`, `stores=2153538`,
  `cfg_transition_stores=2116145`, `external_transition_stores=37393`,
  `required_call_writebacks=698088`, `payload_materializations=0`, and
  `unclassified_stores=0`.
- Design conclusion: no behavior patch or quiet fpscr speed A/B yet. The safe
  behavior shape is a default-off callee-local fpscr dirty stack slot with
  normal-entry fallback and exact writebacks at `82287ED4`, `82287EDC`,
  `82287EE4`, and `82288220`, or a return to caller-local edge-variant storage
  for `82282490:82282598 -> 82287788`.

Next: implement only a default-off `arm64_blue_dragon_fpscr_cfg_carrier_fastpath`
skeleton with materialization/writeback/bailout counters, or switch lanes back
to caller-local edge-variant storage. Do not run a quiet fpscr speed A/B until
payload materialization is intentionally nonzero and all writebacks remain
accounted.

## Mature A64 Backend Research / Skill Update

- Paused the half-started fpscr carrier skeleton and switched to the user's
  requested research/process lane.
- Added
  `docs/research/20260525-143937-mature-a64-emulator-backend-patterns.md`.
  Primary-source review covered Dolphin `JitArm64`, PPSSPP ARM64 JIT,
  Dynarmic ARM64 backend/design docs, FEX development docs, DuckStation, and
  Arm AAPCS64.
- Main finding: mature mobile emulator speed is not a Vulkan-first fix for the
  current Blue Dragon wall. The common A64 pattern is register-cache quality,
  static host-register contracts, block linking, fastmem/fault fallback,
  helper ABI discipline, vector/FP state contracts, and deterministic backend
  tests.
- Updated `.agents/skills/xenia-a64-speed-hotpath/SKILL.md` with a Mature A64
  Backend Gate that must run before more repeated narrow PC-gated fastpaths.
- Updated `.agents/skills/aarch64-snapdragon-jit-port/SKILL.md` with a mature
  backend checklist covering register allocation, static registers, block
  linking, fastmem, helper ABI, NEON/FPCR/FPSCR, and tests.
- Updated `.agents/skills/xenia-blue-dragon-full-speed-autopilot/SKILL.md` so
  stale heartbeat/autopilot work pivots to the backend maturity gate when
  Main Thread/A64 generated-code remains the wall.

Next: build a source/offline A64 maturity gap report for the current
`82282490 -> 82287788` route before another Thor speed patch, unless a fresh
capture changes the wall away from Main Thread/A64 generated code.
