# Worklog 2026-05-17

## 12:38-12:59 - Initial clone, ARM64 scaffold, and Android build baseline

Branch: `codex/thor-arm-agent-docs`

Commands run:

- `git --version`
- `git status --short --branch`
- `git remote -v`
- `git clone git@github.com:noeldvictor/xenia-thor.git xenia-thor`
- `rg --files`
- `git log -5 --oneline --decorate`
- `git submodule status`
- `python xenia-build --help`
- `python xenia-build premake --help`
- `python xenia-build setup --help`
- `python xenia-build premake --target_os=android`
- `python xenia-build setup --target_os=android`
- `.\gradlew.bat :app:assembleGithubDebug`
- `subst X: "C:\Users\leanerdesigner\Documents\New project 8\xenia-thor"` followed by Gradle build from `X:\android\android_studio_project`

Findings:

- The parent workspace was an empty Git repo, so the SSH clone was placed in `xenia-thor`.
- Origin is `git@github.com:noeldvictor/xenia-thor.git`.
- Current upstream head after clone is `95a5c3ee2 [GPU/WGF] Bind EDRAM to resolve dumping via ByteAddressBuffer`.
- Submodules are present but not initialized after clone.
- `python xenia-build premake --target_os=android` failed before setup because `third_party/premake-core` was missing.
- `python xenia-build setup --target_os=android` initialized submodules and generated Android NDK makefiles successfully, including `xenia-cpu-backend-arm64.prj.Android.mk`.
- Android scaffolding exists under `android/android_studio_project`.
- Android Gradle config already includes ARM64 and x86_64 ABIs.
- Current emulator CPU backend support is x64-only; no `src/xenia/cpu/backend/arm64` exists.
- Android app build excludes `xenia_main.cc` with a TODO for the emulator itself.
- Android launcher currently exposes the Vulkan trace viewer and window demo, not a normal game launch path.
- Direct Gradle build from the original path failed because `ndk-build` reported `build\xenia.wks.Android.mk` as unknown even though the file existed. The workspace path contains spaces.
- Gradle build from a temporary no-space `subst` drive succeeded.
- APK output: `android/android_studio_project/app/build/outputs/apk/github/debug/app-github-debug.apk`.

Files added:

- `AGENTS.md`
- `docs/porting/android-arm64-thor-plan.md`
- `docs/research/20260517-124117-thor-max-android-arm64-port-research.md`
- `docs/worklogs/20260517.md`
- `src/xenia/cpu/backend/arm64/arm64_backend.h`
- `src/xenia/cpu/backend/arm64/arm64_backend.cc`
- `src/xenia/cpu/backend/arm64/premake5.lua`

Files changed:

- `premake5.lua`
- `src/xenia/app/premake5.lua`
- `src/xenia/cpu/cpu_flags.cc`
- `src/xenia/cpu/processor.cc`
- `src/xenia/emulator.cc`

Code changes:

- Added a non-functional ARM64 backend scaffold that logs a clear not-implemented error and fails initialization.
- Added `xenia-cpu-backend-arm64` Premake project.
- Linked the ARM64 backend project for `architecture:ARM64`.
- Added ARM64 backend selection in `Emulator::Setup` behind `XE_ARCH_ARM64`.
- Added `arm64` to CPU backend flag help text.
- Removed an unused unconditional `x64_backend.h` include from `processor.cc`.

Validation:

- `python xenia-build setup --target_os=android` succeeded.
- Direct Gradle build from the original workspace path failed due to the `ndk-build` path-with-spaces issue.
- Gradle build through temporary `X:` subst drive succeeded.
- Built APK: `android/android_studio_project/app/build/outputs/apk/github/debug/app-github-debug.apk`, size 29,688,826 bytes.

Sources checked:

- AYN Thor official product page.
- Android NDK ABI docs.
- Android Vulkan native engine docs.
- Qualcomm Snapdragon 8 Gen 2 product page.
- AsmJit and VIXL AArch64 code generation docs.

Blockers:

- Native Android build should be run from a no-space path, or the workspace should be moved/checked out into a no-space path.
- Need device-specific answers for exact Thor Max storage SKU and first test content target.
- Need an AArch64 backend strategy decision before guest execution work begins.

Next actions:

- Add Thor deploy/debug helper.
- Install APK on Thor.
- Launch Vulkan window demo.
- Start focused research comparing AsmJit vs VIXL for the ARM64 backend.

## 14:22-14:28 - Thor install, Vulkan smoke test, and skill creation

Branch: `codex/thor-arm-agent-docs`

User answers:

- Target device is Thor Max.
- First milestone is Vulkan game running on Thor.
- User approved an external AArch64 JIT/emitter dependency after research.
- Likely first legally owned retail test title: Blue Dragon.

Commands run:

- `adb devices`
- `adb -s c3ca0370 shell getprop ...`
- `adb -s c3ca0370 shell pm list features`
- `adb -s c3ca0370 shell dumpsys SurfaceFlinger`
- `adb -s c3ca0370 install -r -d android\android_studio_project\app\build\outputs\apk\github\debug\app-github-debug.apk`
- `adb -s c3ca0370 logcat -c`
- `adb -s c3ca0370 shell am start -n jp.xenia.emulator.github.debug/jp.xenia.emulator.WindowDemoActivity`
- `cmd /c "adb -s c3ca0370 exec-out screencap -p > thor-window-demo.png"`
- `python C:\Users\leanerdesigner\.codex\skills\.system\skill-creator\scripts\init_skill.py ayn-thor-xenia-debug --path C:\Users\leanerdesigner\.codex\skills --resources scripts,references ...`
- `python C:\Users\leanerdesigner\.codex\skills\.system\skill-creator\scripts\quick_validate.py C:\Users\leanerdesigner\.codex\skills\ayn-thor-xenia-debug`
- Web research against official AsmJit docs/repo/license and VIXL Android/GitHub/GitLab/license sources.

Findings:

- ADB device `c3ca0370` is connected.
- Device reports `AYN Thor`, Android 13 / SDK 33, platform `kalama`.
- SurfaceFlinger reports `Qualcomm, Adreno (TM) 740`.
- Xenia logs report Vulkan instance API 1.3.0, Adreno 740 device API 1.3.128, Qualcomm proprietary driver, and a 1920x970 swapchain.
- APK install succeeded.
- `WindowDemoActivity` launched and remained alive as pid `17973`.
- Screenshot `thor-window-demo.png` shows ImGui demo rendering; metrics show about 59.5 FPS.

Files added:

- `docs/research/20260517-142224-thor-vulkan-device-baseline.md`
- `docs/research/20260517-142224-arm64-jit-emitter-asmjit-vixl.md`
- `tools/thor/thor_xenia_debug.ps1`

Local Codex skill added:

- `C:\Users\leanerdesigner\.codex\skills\ayn-thor-xenia-debug`
- Validation passed with `quick_validate.py`.

Blockers:

- Vulkan demo is not game execution.
- Android game launch/file picker flow is not implemented.
- ARM64 CPU backend is still a scaffold with no AArch64 JIT.
- Need the path/location of the user's legal Blue Dragon dump.

Decision:

- Prefer VIXL for the first AArch64 emitter spike; keep AsmJit as fallback.

Next actions:

- Add Android game launch flow for `.xex` / `.iso` selection or a temporary developer launch argument path.
- Begin VIXL integration spike and AArch64 code-cache/cache-flush prototype.

## 14:29-14:45 - Android game launch shell, ADB helper expansion, and Thor validation

Branch: `codex/thor-arm-agent-docs`

Commands run:

- `git diff --check`
- `python C:\Users\leanerdesigner\.codex\skills\.system\skill-creator\scripts\quick_validate.py C:\Users\leanerdesigner\.codex\skills\ayn-thor-xenia-debug`
- `rg -n ... AGENTS.md docs C:\Users\leanerdesigner\.codex\skills\ayn-thor-xenia-debug -S`
- Gradle build through temporary `X:` subst drive:
  `.\gradlew.bat :app:assembleGithubDebug`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -DeviceSerial c3ca0370 -Mode Install`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -DeviceSerial c3ca0370 -Mode LaunchLauncher`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -DeviceSerial c3ca0370 -Mode LaunchEmulator -Target /sdcard/Download/blue-dragon/default.xex`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -DeviceSerial c3ca0370 -Mode LaunchWindowDemo`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -DeviceSerial c3ca0370 -Mode Capture`
- `Get-FileHash android\android_studio_project\app\build\outputs\apk\github\debug\app-github-debug.apk -Algorithm SHA256`

Findings:

- Android launcher screenshot `scratch\thor-debug\20260517-144226-screenshot.png` shows `LAUNCH GAME`, `GPU TRACE VIEWER`, and `WINDOW DEMO`.
- `EmulatorActivity` can now convert simple ADB extras into the Bundle expected by native Android cvar parsing.
- Direct emulator launch writes app-private storage paths, content root, and cache root to logcat, then fails at the intended ARM64 backend boundary:
  `ARM64 CPU backend selected, but the AArch64 JIT is not implemented yet.`
- The direct emulator run exits without an Android Java/native crash.
- Fresh `WindowDemoActivity` run still renders on Thor, with Xenia logging Vulkan 1.3, Adreno 740, Qualcomm proprietary driver, and a 1920x970 swapchain.
- APK install succeeded on `c3ca0370`.
- APK: `android\android_studio_project\app\build\outputs\apk\github\debug\app-github-debug.apk`
- APK size: 46,050,080 bytes.
- APK SHA256: `1882AA58CA4A880C42F90E092ED3796B71BC8C14C69EE1C6FA64B7711F0D6B88`.

Files added:

- `android/android_studio_project/app/src/main/java/jp/xenia/emulator/EmulatorActivity.java`
- `android/android_studio_project/app/src/main/res/layout/activity_emulator.xml`

Files changed:

- `android/android_studio_project/app/src/main/AndroidManifest.xml`
- `android/android_studio_project/app/src/main/java/jp/xenia/emulator/LauncherActivity.java`
- `android/android_studio_project/app/src/main/res/layout/activity_launcher.xml`
- `android/android_studio_project/app/src/main/res/values/strings.xml`
- `tools/thor/thor_xenia_debug.ps1`
- `AGENTS.md`
- `docs/porting/android-arm64-thor-plan.md`
- `C:\Users\leanerdesigner\.codex\skills\ayn-thor-xenia-debug\SKILL.md`
- `C:\Users\leanerdesigner\.codex\skills\ayn-thor-xenia-debug\references\xenia-thor-android.md`
- `C:\Users\leanerdesigner\.codex\skills\ayn-thor-xenia-debug\scripts\thor_xenia_debug.ps1`

Validation:

- `git diff --check` produced only CRLF normalization warnings.
- Skill validation passed.
- `:app:assembleGithubDebug` passed.
- APK installed on Thor.
- Launcher and window demo screenshots captured under `scratch\thor-debug`.
- Emulator launch log captured at `scratch\thor-debug\20260517-144245-logcat.txt`.

Blockers:

- Native Android game execution is still blocked on the AArch64 JIT/backend implementation.
- The first real content run needs the user's legal Blue Dragon dump path or a tiny legal homebrew/test `.xex`.

Next actions:

- Start VIXL integration spike for executable-memory allocation, write/protect/flush/call on Android ARM64.
- Add the first minimal generated-code smoke test before attempting retail game boot.

## 14:46-14:54 - Blue Dragon target automation and Ghidra skill setup

Branch: `codex/thor-arm-agent-docs`

User update:

- Legal Blue Dragon content is on the Thor SD card under `roms/xbox360`.
- User asked to automate debug, create skills as needed, use ADB, use Ghidra, and mark the README as an extremely unstable vibecoded fork.

Commands run:

- `adb -s c3ca0370 shell "ls -la /storage ..."`
- `adb -s c3ca0370 shell "find /storage/2664-21DE/roms/xbox360 ..."`
- `where.exe ghidraRun.bat; where.exe analyzeHeadless.bat; where.exe adb`
- `Get-ChildItem C:\Users\leanerdesigner\AppData\Local\ghidra ...`
- `python C:\Users\leanerdesigner\.codex\skills\.system\skill-creator\scripts\init_skill.py xenia-ghidra-android-debug ...`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -DeviceSerial c3ca0370 -Mode FindContent`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -DeviceSerial c3ca0370 -Mode LaunchBlueDragon`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -DeviceSerial c3ca0370 -Mode Capture`
- PowerShell parse checks for `tools\thor\thor_xenia_debug.ps1` and `tools\thor\ghidra_headless_import.ps1`.
- Skill validation for `ayn-thor-xenia-debug` and `xenia-ghidra-android-debug`.

Findings:

- SD card mount is `/storage/2664-21DE`.
- Blue Dragon Disc 1 target:
  `/storage/2664-21DE/roms/xbox360/Blue Dragon.m3u/Blue Dragon (USA, Europe) (En,Fr) (Disc 1).iso`
- `FindContent` finds Blue Dragon Disc 1, Disc 2, and Disc 3.
- `LaunchBlueDragon` now handles spaces in the target path and starts `EmulatorActivity` with extras.
- Captured filtered log at `scratch\thor-debug\20260517-145321-logcat-filtered.txt`.
- Blue Dragon launch reaches the current expected blocker:
  `ARM64 CPU backend selected, but the AArch64 JIT is not implemented yet.`
- No `ghidraRun.bat` or `analyzeHeadless.bat` is currently on PATH.
- `C:\Users\leanerdesigner\AppData\Local\ghidra` exists, but only cache folders were found there, not a Ghidra launcher.

Files added:

- `docs/porting/android-thor-debug-automation.md`
- `tools/thor/ghidra_headless_import.ps1`
- `C:\Users\leanerdesigner\.codex\skills\xenia-ghidra-android-debug\SKILL.md`
- `C:\Users\leanerdesigner\.codex\skills\xenia-ghidra-android-debug\agents\openai.yaml`
- `C:\Users\leanerdesigner\.codex\skills\xenia-ghidra-android-debug\references\workflow.md`
- `C:\Users\leanerdesigner\.codex\skills\xenia-ghidra-android-debug\scripts\ghidra_headless_import.ps1`

Files changed:

- `README.md`
- `AGENTS.md`
- `tools/thor/thor_xenia_debug.ps1`
- `docs/worklogs/20260517.md`
- `C:\Users\leanerdesigner\.codex\skills\ayn-thor-xenia-debug\SKILL.md`
- `C:\Users\leanerdesigner\.codex\skills\ayn-thor-xenia-debug\references\xenia-thor-android.md`
- `C:\Users\leanerdesigner\.codex\skills\ayn-thor-xenia-debug\scripts\thor_xenia_debug.ps1`

Validation:

- Repo PowerShell scripts parse successfully.
- Both local skills pass `quick_validate.py`.
- `FindContent`, `LaunchBlueDragon`, and `Capture` were tested on Thor.

Blockers:

- Ghidra install path needs to be provided or installed before headless imports can run.
- Blue Dragon cannot execute further until the AArch64 backend/JIT exists.

Next actions:

- Point `GHIDRA_HOME` or `-AnalyzeHeadless` at the real Ghidra install.
- Start a native `libxenia-app.so` Ghidra project for ARM64 backend and Android bridge analysis.
- Begin the VIXL executable-code smoke test path.

## 14:55-15:06 - Android launcher skin, OSD shell, and Thor game-picker default

Branch: `codex/thor-arm-agent-docs`

User update:

- User asked for a nicer Android logo, Dolphin/Azahar-like Android GUI, a Dolphin-style OSD layer rather than ImGui for the main product UI, and `AGENTS.md` updates.
- User then asked to default the file picker to the Thor SD-card `roms/xbox360` folder.

Commands run:

- Gradle debug builds through temporary `X:` subst drive:
  `.\gradlew.bat :app:assembleGithubDebug`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -DeviceSerial c3ca0370 -Mode Install`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -DeviceSerial c3ca0370 -Mode LaunchLauncher`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -DeviceSerial c3ca0370 -Mode Capture`
- `adb -s c3ca0370 shell input tap 720 450`

Files changed:

- `AGENTS.md`
- `android/android_studio_project/app/src/main/AndroidManifest.xml`
- `android/android_studio_project/app/src/main/java/jp/xenia/emulator/EmulatorActivity.java`
- `android/android_studio_project/app/src/main/java/jp/xenia/emulator/LauncherActivity.java`
- `android/android_studio_project/app/src/main/res/layout/activity_launcher.xml`
- `android/android_studio_project/app/src/main/res/layout/activity_emulator.xml`
- `android/android_studio_project/app/src/main/res/values/strings.xml`

Files added:

- `android/android_studio_project/app/src/main/res/values/colors.xml`
- `android/android_studio_project/app/src/main/res/values/styles.xml`
- `android/android_studio_project/app/src/main/res/drawable/ic_xenia_thor_mark.xml`
- `android/android_studio_project/app/src/main/res/drawable/ic_action_play.xml`
- `android/android_studio_project/app/src/main/res/drawable/ic_action_trace.xml`
- `android/android_studio_project/app/src/main/res/drawable/ic_action_window.xml`
- `android/android_studio_project/app/src/main/res/drawable/launcher_background.xml`
- `android/android_studio_project/app/src/main/res/drawable/launcher_card.xml`
- `android/android_studio_project/app/src/main/res/drawable/launcher_card_primary.xml`
- `android/android_studio_project/app/src/main/res/drawable/launcher_chip.xml`
- `android/android_studio_project/app/src/main/res/drawable/launcher_icon_plate.xml`
- `android/android_studio_project/app/src/main/res/drawable/osd_panel.xml`
- `android/android_studio_project/app/src/main/res/drawable/osd_badge.xml`
- `android/android_studio_project/app/src/main/res/drawable/osd_warning.xml`

Findings:

- First launcher layout had a `0dp` text column without weight, causing only the mark to appear. Fixed and revalidated on device.
- The launcher now shows a dark Xenia Thor Android shell with a new mark, Vulkan/ARM64/Unstable chips, and three controller-friendly rows.
- `EmulatorActivity` now has a native Android OSD overlay above the render surface for title, target path, backend status, and current AArch64 JIT warning.
- The game picker now sets `DocumentsContract.EXTRA_INITIAL_URI` to removable SD-card document id `2664-21DE:roms/xbox360`.
- Device tap test on `Launch Game` opened Android DocumentsUI directly at `disk > roms > xbox360`.
- Screenshot proof: `scratch\thor-debug\20260517-150558-screenshot.png`.

Validation:

- `:app:assembleGithubDebug` passed after UI changes.
- APK installed on Thor.
- Launcher screenshot after density fix: `scratch\thor-debug\20260517-150538-screenshot.png`.
- DocumentsUI default-folder screenshot: `scratch\thor-debug\20260517-150558-screenshot.png`.

Blockers:

- OSD values are still mostly static Java-side text; native runtime event plumbing is future work.
- Actual game execution remains blocked on AArch64 JIT/backend implementation.

Next actions:

- Add native-to-Java OSD update hooks for FPS, title ID, setup errors, and shader/JIT events.
- Add launcher settings/library rows after CPU backend work begins.

## 15:23 - Blue Dragon Disc 1 launch request

Branch: `codex/thor-arm-agent-docs`

Command run:

- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -DeviceSerial c3ca0370 -Mode LaunchBlueDragon`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -DeviceSerial c3ca0370 -Mode Capture`

Target:

- `/storage/2664-21DE/roms/xbox360/Blue Dragon.m3u/Blue Dragon (USA, Europe) (En,Fr) (Disc 1).iso`

Result:

- Android started `jp.xenia.emulator.github.debug/jp.xenia.emulator.EmulatorActivity` with extras.
- Xenia initialized app-private storage/config/cache roots.
- Launch stopped at the expected current backend boundary:
  `ARM64 CPU backend selected, but the AArch64 JIT is not implemented yet.`

Artifacts:

- Full log: `scratch\thor-debug\20260517-152311-logcat.txt`
- Filtered log: `scratch\thor-debug\20260517-152311-logcat-filtered.txt`
- Screenshot: `scratch\thor-debug\20260517-152311-screenshot.png`

## 15:37-16:30 - ARM64 HIR interpreter bring-up and Blue Dragon opcode chase

Branch: `codex/thor-arm-agent-docs`, then moved to `master` at 16:31 per user branch policy.

User update:

- User said Blue Dragon needs to work and later asked for a smarter debug system, Ghidra usage, RenderDoc usage, master-only work, and frequent commits/pushes.

Commands run:

- Multiple Gradle builds through temporary no-space `subst` drive:
  `.\gradlew.bat :app:assembleGithubDebug`
- Multiple install/launch/capture loops:
  `tools\thor\thor_xenia_debug.ps1 -DeviceSerial c3ca0370 -Mode Install`
  `tools\thor\thor_xenia_debug.ps1 -DeviceSerial c3ca0370 -Mode LaunchBlueDragon`
  `tools\thor\thor_xenia_debug.ps1 -DeviceSerial c3ca0370 -Mode Capture`
- ADB process cleanup:
  `adb -s c3ca0370 shell am force-stop jp.xenia.emulator.github.debug`
  `adb -s c3ca0370 shell am force-stop net.rpcsx.easy`

Files changed:

- `src/xenia/cpu/backend/arm64/arm64_assembler.cc`
- `src/xenia/cpu/backend/arm64/arm64_function.cc`
- `src/xenia/cpu/backend/arm64/arm64_function.h`

Findings:

- Blue Dragon Disc 1 now loads `game:\default.xex` and reports disc metadata:
  `Disc Number / Total: 1 / 3`.
- The old wall of `failed to find function` errors is gone for the tested path.
- The temporary ARM64 backend now uses a slow HIR interpreter fallback so Blue Dragon can execute guest functions while a real AArch64 JIT/emitter is designed.
- Implemented or expanded interpreter handling for direct/indirect/extern calls, locals, context load/store, memory load/store, atomics, MMIO, `ADD_CARRY`, `MUL_HI`, vector compare, vector unpack/swizzle/extract/splat/permute, vector shift masks, float/vector arithmetic, and unary math (`ABS`, `SQRT`, `RSQRT`, `RECIP`, `POW2`, `LOG2`).
- Raising the debug interpreter step cap allowed the real long loop in `826BFD0C` to progress; earlier `500000` steps was too low for a 4 MB guest initialization loop.
- Another emulator process (`net.rpcsx.easy`) polluted one capture and stole focus; future Thor runs should use `StopNoise` first.

Artifacts:

- `scratch\thor-debug\20260517-160122-logcat.txt`
- `scratch\thor-debug\20260517-160441-logcat.txt`
- `scratch\thor-debug\20260517-160856-logcat.txt`
- `scratch\thor-debug\20260517-161329-logcat.txt`
- `scratch\thor-debug\20260517-161736-logcat.txt`
- `scratch\thor-debug\20260517-162037-logcat.txt`
- `scratch\thor-debug\20260517-162255-logcat.txt`
- `scratch\thor-debug\20260517-162643-logcat.txt`

Latest valid blocker before switching to debug-system work:

- After vector compare cleared, the run reached `OPCODE_RSQRT` (`81`) in guest function `82491298`.
- A unary math block was added after that blocker and built successfully, but the immediate follow-up run was invalid because Xenia did not stay alive and logcat contained only device noise.

Validation:

- `:app:assembleGithubDebug` passed after each interpreter slice.
- Blue Dragon runs repeatedly reached Vulkan/Adreno swapchain creation and guest-code execution before hitting the next missing HIR operation.

Blockers:

- The interpreter is intentionally slow and rough. It is for bring-up only, not a compatibility or performance solution.
- A real AArch64 backend/emitter is still required.
- Some vector semantics are approximate and need correctness tests before relying on game behavior.

Next actions:

- Continue opcode/correctness chase only after the debug system can produce clean, separated captures.
- Use Ghidra for native/guest address investigation when logs point to a concrete native or PPC address.
- Start VIXL or equivalent AArch64 emitter spike once the interpreter has identified the hottest required HIR surface.

## 16:31-16:45 - Smart Thor debug system and master-only policy

Branch: `master`

Commands run:

- `git switch master`
- Android Vulkan layer research using Android Developers documentation, accessed 2026-05-17.

Files added:

- `tools/thor/thor_build.ps1`
- `tools/thor/thor_renderdoc.ps1`
- `docs/porting/thor-smart-debug-system.md`
- `docs/research/20260517-163129-android-vulkan-layer-renderdoc-ghidra-debug.md`

Files changed:

- `README.md`
- `AGENTS.md`
- `android/android_studio_project/app/src/main/AndroidManifest.xml`
- `docs/porting/android-thor-debug-automation.md`
- `docs/worklogs/20260517.md`
- `tools/thor/thor_xenia_debug.ps1`
- Local skill: `C:\Users\leanerdesigner\.codex\skills\ayn-thor-xenia-debug\SKILL.md`

Findings:

- Android's current Vulkan layer documentation supports per-app GPU debug settings through `enable_gpu_debug_layers`, `gpu_debug_app`, `gpu_debug_layers`, and `gpu_debug_layer_app`.
- For Android 11+ targets, the manifest can opt into graphics layer injection via `com.android.graphics.injectLayers.enable`.

Changes:

- Added `thor_build.ps1` with separate lanes:
  - `FullApk` for native plus APK.
  - `NativeCore` for C++/Vulkan/CPU backend only.
  - `ApkShell` for Java/XML/resources/manifest only.
- Added `thor_renderdoc.ps1` with `Status`, `EnablePerApp`, `EnableGlobal`, `Disable`, and `PullCaptures` modes.
- Added `StopNoise` and metadata output to `thor_xenia_debug.ps1`.
- Strengthened README/AGENTS disclaimers that this is an extremely unstable local research fork, not official Xenia, and fork-specific issues should not be sent to upstream maintainers.
- Documented master-only branch policy and frequent commit/push cadence.

Next actions:

- Validate PowerShell script parsing and the `ApkShell` build lane.
- Commit and push `master`.

Validation update:

- PowerShell parser checks passed for:
  - `tools\thor\thor_build.ps1`
  - `tools\thor\thor_renderdoc.ps1`
  - `tools\thor\thor_xenia_debug.ps1`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode ApkShell` passed in 2 seconds and skipped the native external build lane as intended.

## 17:55-18:15 - Blue Dragon ARM64 interpreter continuation

Branch: `master`

Commands run:

- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -DeviceSerial c3ca0370 -Mode FullDeploy`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -DeviceSerial c3ca0370 -Mode StopNoise`
- `adb -s c3ca0370 shell am force-stop jp.xenia.emulator.github.debug`
- `adb -s c3ca0370 logcat -c`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -DeviceSerial c3ca0370 -Mode LaunchBlueDragon`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -DeviceSerial c3ca0370 -Mode Capture`
- `adb -s c3ca0370 shell top -H -b -n 1 -p 11356`
- `adb -s c3ca0370 shell ps -T -p 11356`

Files changed:

- `src/xenia/cpu/backend/arm64/arm64_function.cc`
- `src/xenia/cpu/backend/arm64/arm64_assembler.cc`
- `tools/thor/thor_xenia_debug.ps1`
- `docs/research/20260517-180818-blue-dragon-arm64-interpreter-jit-wall.md`
- `docs/worklogs/20260517.md`
- `AGENTS.md`
- `README.md`

Findings:

- Fixed `tools/thor/thor_xenia_debug.ps1 -Mode Capture` after PowerShell rejected the read-only `$PID` variable name.
- The 17:57 Thor run reached `OPCODE_MIN` in guest function `82286BC0`; scalar/vector min/max support cleared that wall.
- The 18:00 Thor run reached `OPCODE_SET_ROUNDING_MODE` in guest function `826CBAC0`; PPC FPSCR dynamic rounding, HIR `round`, and float-to-int conversion handling cleared that wall.
- The 18:05 Thor run did not hit another missing HIR opcode during capture.
- Blue Dragon remained in `EmulatorActivity` with process `11356`.
- Live thread sampling showed active guest execution under the interpreter:
  `Main XThread` around 103% CPU, plus additional active guest XThreads.
- Live logcat later reported:
  `ARM64 interpreter slow function 826E53A8: step 500000, pc 126, lr 826E53DC, ctr 826E52E8`.
- The visible `AArch64 JIT pending` text is static Java OSD text, not the native failure line. The real blocker is that the ARM64 backend is still an interpreter scaffold and is too slow for a visible game frame.

Artifacts:

- `scratch\thor-debug\20260517-175745-logcat.txt`
- `scratch\thor-debug\20260517-175745-logcat-filtered.txt`
- `scratch\thor-debug\20260517-175745-meta.txt`
- `scratch\thor-debug\20260517-175745-screenshot.png`
- `scratch\thor-debug\20260517-180025-logcat.txt`
- `scratch\thor-debug\20260517-180025-logcat-filtered.txt`
- `scratch\thor-debug\20260517-180025-meta.txt`
- `scratch\thor-debug\20260517-180025-screenshot.png`
- `scratch\thor-debug\20260517-180553-logcat.txt`
- `scratch\thor-debug\20260517-180553-logcat-filtered.txt`
- `scratch\thor-debug\20260517-180553-meta.txt`
- `scratch\thor-debug\20260517-180553-screenshot.png`
- `scratch\thor-debug\20260517-181152-logcat.txt`
- `scratch\thor-debug\20260517-181152-logcat-filtered.txt`
- `scratch\thor-debug\20260517-181152-meta.txt`
- `scratch\thor-debug\20260517-181152-screenshot.png`

Validation:

- `NativeCore` passed after the interpreter rounding/min-max changes.
- `FullDeploy` passed and installed the APK on `c3ca0370`.
- `LaunchBlueDragon` started the SD-card Disc 1 ISO directly.
- `Capture` wrote full logcat, filtered logcat, metadata, and screenshot.
- Follow-up `FullDeploy` installed APK SHA-256
  `DED6C1267875EB0AEB7AA07D0B9A3FBD383CEAA28F20C2B6E2AF479CE997A571`.
- The 18:11 metadata correctly preserved the Blue Dragon target path after the
  `last-target.txt` script fix.
- The 18:11 filtered log stayed readable enough to expose the real late signal:
  repeated slow interpreter hot functions `822710D0`, `82285470`, and
  `82285AB0`, with no new missing-opcode crash in the short verification window.

Blockers:

- The interpreter is useful for correctness discovery but cannot meet the user's
  "Vulkan game running" milestone.
- A real AArch64 JIT/emitter path is required next.
- The Android OSD status is still static and should be driven by native runtime
  events.

Next actions:

- Start AArch64 emitter/JIT research and spike, likely using VIXL unless a
  dated research note rejects it.
- Use Ghidra guest PPC analysis on `826E53A8` if the hot loop needs a concrete
  wait-condition explanation before JIT work.
- Keep using `NativeCore`, `ApkShell`, and `FullDeploy` lanes to avoid rebuilding
  unrelated parts while debugging on Thor.

## 2026-05-17 18:35 EDT - Public ARM64 / Android Fork Audit

Goal:

- Audit public Xenia ARM64 / Android repositories that may accelerate the Thor
  Max native Android port.
- Separate real AArch64 backend candidates from Android-shell or unrelated
  search hits.
- Record the import decision before copying code.

Commands:

- GitHub API repository searches for `xenia android`, `xenia fork:true arm64`,
  `xenia fork:true a64`, `xenia cpu backend a64`, and related terms.
- `git ls-remote --heads` for upstream, `wmarti/xenia-mac`,
  `has207/xenia-edge`, `Wunkolo/xenia`, `aenu1/ax360e`,
  `joetheshmoe/xenia-winarm`, and Android search-result repos.
- Shallow blob-filtered clones under `scratch\research`.
- `git ls-tree`, `git grep`, and selected `git show` checks for A64 files,
  opcode table coverage, Android glue, CMake/premake wiring, and license files.

Files changed:

- `docs/research/20260517-183520-xenia-arm64-fork-audit.md`
- `docs/worklogs/20260517.md`
- `AGENTS.md`

Findings:

- `has207/xenia-edge` is the best primary compiler source: active 2026-05-17
  fork, xbyak_aarch64 backend, CMake wiring, POSIX code cache, and 121 unique
  `EMITTER_OPCODE_TABLE` opcode entries.
- `aenu1/ax360e` is the best Android app-side reference: real Android project,
  nested Xenia Canary, A64 backend, Android CMake glue, SAF/document-file
  access, Android HID, Adreno options, and `libadrenotools`.
- `wmarti/xenia-mac`, `Wunkolo/xenia`, and `joetheshmoe/xenia-winarm` remain
  useful for A64 backend provenance and ARM64 portability work.
- `edilsongg/xenia-arm64` and `fillips67fissile/xenia-arm64-backend` appear to
  reuse Wunkolo ARM64 branch hashes.
- `SbossTreeSitty`, `Xenia-creator2025/Xenia-emulator-android`, and the
  `ProyectoXenia` repos do not provide useful AArch64 JIT code.

Decision:

- Start the import spike from `has207/xenia-edge` xbyak_aarch64 A64 backend.
- Use AX360E as a reference for Android glue only, after file-level license
  checks.
- Keep the current interpreter scaffold as fallback while the JIT comes online.
- Do not wholesale import any unclear app tree or vendored dependency set.

## 2026-05-17 18:40 EDT - xbyak_aarch64 Dependency Pin

Goal:

- Start the A64 import with the smallest useful dependency-only slice.
- Match the assembler dependency commit used by the best `xenia-edge` backend
  candidate before importing backend code.

Commands:

- `git submodule add https://github.com/fujitsu/xbyak_aarch64.git third_party/xbyak_aarch64`
- `git -C third_party/xbyak_aarch64 checkout 59c6a7e7368cd2dbb07c46abe83239ace4d58849`
- `git -C third_party/xbyak_aarch64 log -1 --date=iso --format=...`

Files changed:

- `.gitmodules`
- `third_party/xbyak_aarch64`
- `docs/worklogs/20260517.md`
- `AGENTS.md`

Findings:

- `has207/xenia-edge` stores `third_party/xbyak_aarch64` as a submodule gitlink
  at `59c6a7e7368cd2dbb07c46abe83239ace4d58849`.
- That commit resolves in `fujitsu/xbyak_aarch64` as
  `fix: intialise memory before use in conditional`, dated 2025-09-10.
- The checked dependency carries an Apache-2.0 `LICENSE`.

Decision:

- Keep this as a dependency-only checkpoint.
- Next backend slice should wire a premake/CMake target or import a minimal
  A64 emitter skeleton, not both at once.

## 2026-05-17 18:46 EDT - xbyak_aarch64 Premake Wiring

Goal:

- Make the pinned `third_party/xbyak_aarch64` submodule visible to the Android
  native build before adding emitter code.

Files changed:

- `third_party/xbyak_aarch64.lua`
- `premake5.lua`
- `src/xenia/cpu/backend/arm64/premake5.lua`
- `docs/worklogs/20260517.md`

Decision:

- Link `xenia-cpu-backend-arm64` against a small static `xbyak_aarch64`
  third-party target.
- Keep the first generated-code patch separate from dependency/build plumbing.

## 2026-05-17 19:10 EDT - ARM64 Mini JIT Checkpoint

Goal:

- Move Blue Dragon past the pure interpreter wall by adding the first strict
  AArch64 generated-code path while preserving interpreter fallback.

Files changed:

- `src/xenia/cpu/backend/arm64/arm64_jit.h`
- `src/xenia/cpu/backend/arm64/arm64_jit.cc`
- `src/xenia/cpu/backend/arm64/arm64_assembler.cc`
- `src/xenia/cpu/backend/arm64/arm64_function.h`
- `src/xenia/cpu/backend/arm64/arm64_function.cc`
- `docs/worklogs/20260517.md`

Implementation notes:

- Added a mini xbyak_aarch64 stack-slot emitter for integer HIR, context
  loads/stores, locals, memory helpers, branches, returns, compares, select,
  basic arithmetic, div/mul-high helpers, byte swap, count-leading-zero, and
  memset.
- `Arm64Function` now prefers a compiled function pointer when available and
  falls back to the existing HIR interpreter if generated code returns failure.
- The mini JIT is enabled only on real `XE_ARCH_ARM64`; x86_64 Android builds
  keep interpreter behavior.

Verification:

- Regenerated Android NDK makefiles with
  `python xenia-build setup --target_os=android` so `arm64_jit.cc` is included.
- `tools\thor\thor_build.ps1 -Mode NativeCore -DeviceSerial c3ca0370` passed
  for `arm64-v8a` and `x86_64`.

Next actions:

- Assemble/deploy the APK, launch Blue Dragon Disc 1 on the Thor, and capture
  logcat/screenshot evidence.
- If the generated code crashes or the interpreter still dominates the hot
  loop, use the first failing opcode/function address from logcat to expand the
  emitter narrowly.

## 2026-05-17 19:24 EDT - ARM64 Mini JIT Call Support

Goal:

- Keep more Blue Dragon guest functions in generated AArch64 code instead of
  rejecting every direct, indirect, conditional, or extern call.

Evidence from Thor run:

- APK hash `6EB7A6218D008D6A6D1258B0E5F1B44D93BC2FF32CD33FF0F7A96DD0E3AC0C84`
  at commit `6efc46556` launched Blue Dragon Disc 1 on the Thor.
- Capture: `scratch/thor-debug/20260517-191154-*`.
- The run created the Vulkan swapchain and guest threads, compiled 80 mini-JIT
  functions, but important functions still fell back on call opcodes 7-11.
- The same run surfaced a concrete title blocker:
  missing extern handler `XeKeysConsolePrivateKeySign`.

Implementation notes:

- Added mini-JIT support for `SET_RETURN_ADDRESS`, direct calls, conditional
  direct calls, indirect calls, conditional indirect calls, and extern calls.
- Generated call paths now invoke C++ helpers that mirror the interpreter's
  guest/host function dispatch and possible-return handling.
- Compiled `Arm64Function` calls now return the generated-code result directly;
  a runtime call failure after side effects should not re-run the whole guest
  function through the interpreter.
- Fixed large-stack AArch64 SP adjustment to use the extended-register form
  accepted by xbyak_aarch64.

Verification:

- `tools\thor\thor_build.ps1 -Mode NativeCore -DeviceSerial c3ca0370` passed
  for `arm64-v8a` and `x86_64`.

Next actions:

- Deploy and relaunch Blue Dragon to see whether call-capable generated code
  reduces the slow interpreter loops.
- Research or implement a narrow `XeKeysConsolePrivateKeySign` behavior stub if
  it remains the next title-blocking missing export.

## 2026-05-17 19:21 EDT - ARM64 Mini JIT Rotate-Left Support

Goal:

- Move Blue Dragon's hot `826BFD0C` shuffle/write loop out of the slow HIR
  interpreter.

Evidence from Thor run:

- APK hash `084C2FDEE698B282EF37D2249A3D5FB5437AC927AD02FA2D9004CC285AFCFBCC`
  at commit `dd77842ae` launched Blue Dragon Disc 1 on the Thor.
- Capture: `scratch/thor-debug/20260517-191813-*`.
- Direct/indirect/extern call opcodes were no longer the first mini-JIT wall.
- The current hot slow function was `826BFD0C`; its optimized HIR contains four
  `OPCODE_ROTATE_LEFT` instructions and then byte/word stores.
- Opcode mapping check: `98` = `OPCODE_ROTATE_LEFT`; `111` =
  `OPCODE_ATOMIC_COMPARE_EXCHANGE`.

Implementation notes:

- Added `OPCODE_ROTATE_LEFT` emission to the ARM64 mini JIT.
- Constant 32-bit and 64-bit rotates emit native AArch64 `ror` with the inverse
  immediate, which covers the observed Blue Dragon `i64` constants.
- Variable or narrow integer rotates use a C++ helper with explicit zero-shift
  handling to avoid undefined shifts.

Verification:

- `tools\thor\thor_build.ps1 -Mode NativeCore -DeviceSerial c3ca0370` passed
  for `arm64-v8a` and `x86_64`.

Next actions:

- Deploy and relaunch Blue Dragon to confirm `826BFD0C` compiles and find the
  next blocker on-device.
