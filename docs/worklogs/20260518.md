# Worklog - 2026-05-18

## 2026-05-18 09:12-09:21 EDT - Blue Dragon PPC Global Reference Scanner

Goal:

- Replace the noisy crash-time string search for `0x82785548` with an
  instruction-aware PPC reference scanner so the Thor run can tell whether any
  translated guest function directly reads or writes the null Blue Dragon
  global.

Branch and commit:

- Branch: `master`.
- Starting commit: `989609c0b Add Thor guest store watch telemetry`.

Commands run:

- `git status --short --branch`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode ApkShell`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -DeviceSerial c3ca0370 -Mode Install`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -DeviceSerial c3ca0370 -Mode LaunchBlueDragon -MountCache true -DisassembleFunctions true -Arm64GuestStoreWatch 82785548`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -DeviceSerial c3ca0370 -Mode Capture`
- `powershell -ExecutionPolicy Bypass -File tools\arm64\hir_coverage_report.ps1 -LogPath scratch\thor-debug\20260518-091952-logcat.txt`

Files changed:

- `AGENTS.md`
- `src/xenia/emulator.cc`
- `src/xenia/kernel/xboxkrnl/xboxkrnl_io.cc`
- `tools/arm64/hir_coverage_report.ps1`
- `docs/research/20260518-092052-blue-dragon-ppc-global-ref-scan.md`

Implementation notes:

- Added a crash-time PPC direct-reference scanner over translated function
  debug disassembly.
- The scanner parses PPC instruction words from Xenia's source-disasm lines,
  tracks simple constant address formation through `addis`, `addi`, `ori`, and
  `or` register copies, and resolves D-form / DS-form load-store effective
  addresses.
- Replaced the previous raw text search for `0x5548(` with a decoded search for
  exact guest address `0x82785548`.
- Extended `tools/arm64/hir_coverage_report.ps1` to summarize `PPC global ref`
  lines.
- Added focused `NtCreateFile success` telemetry for `\pack` and `\!necessity`
  paths so archive/package opens can be compared against the later font misses.
- Updated `AGENTS.md` to mention PPC global-reference scanner hits in the HIR
  coverage report output.

Validation:

- Native core build passed.
- APK shell build passed.
- APK install to Thor serial `c3ca0370` passed.
- Blue Dragon Disc 1 capture set:
  `scratch/thor-debug/20260518-091952-logcat.txt`,
  `scratch/thor-debug/20260518-091952-logcat-filtered.txt`,
  `scratch/thor-debug/20260518-091952-meta.txt`,
  `scratch/thor-debug/20260518-091952-screenshot.png`.
- Path telemetry capture set:
  `scratch/thor-debug/20260518-092516-logcat.txt`,
  `scratch/thor-debug/20260518-092516-logcat-filtered.txt`,
  `scratch/thor-debug/20260518-092516-meta.txt`,
  `scratch/thor-debug/20260518-092516-screenshot.png`.
- HIR coverage report on the capture passed and now includes a `PPC Global
  References` section.

Findings:

- Blue Dragon still crashes at guest PC `0x826A23E8`.
- `0x82785548` is still `0x00000000` at crash.
- The decoded PPC global-reference scanner found exactly one translated direct
  reference to `0x82785548`:
  `0x826A23DC lwz r11, 0x5548(r11)` in function `0x826A23C8-0x826A23F4`.
- It found zero translated direct stores to `0x82785548`.
- `D:\pack\!necessity.ipk` opens successfully before the crash. Earlier pack
  files such as `D:\pack\gimmick.iph`, `D:\pack\gimmick.ipk`,
  `D:\pack\map\gimmick.iph`, and `D:\pack\map\gimmick.ipk` also open
  successfully.
- The same run still shows the failed font opens under
  `D:\!necessity\font\...` immediately before the crash.

Blockers:

- The initializer that should seed `0x82785548` either was not translated before
  the crash, was not reached, uses a non-direct address path, or belongs to a
  native/module initialization path outside the scanned translated PPC
  functions.
- `D:\!necessity\font\...` file misses remain suspicious. The raw pack archive
  opens, so this now looks more like missing archive virtual-path setup or a
  skipped game-side archive initializer than a missing ISO file.

Next actions:

- Add targeted function/return tracing around `0x821903B8`, `0x822710D0`,
  `0x826A23C8`, and any functions that open `\pack` or `\!necessity`.
- Trace reads and return values around the successful `D:\pack\!necessity.ipk`
  open and the later failed `D:\!necessity\font\...` opens.

## 2026-05-18 09:52-10:18 EDT - Blue Dragon SoundBank Null Thunk Fix

Branch: `master`

Goal:

- Determine whether the `D:\pack\!necessity.ipk` open/read path was actually
  causing the `0x826A23E8` null thunk crash, then keep pushing toward Blue
  Dragon title on the AYN Thor.

Commands run:

- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode ApkShell`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -DeviceSerial c3ca0370 -Mode Install`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -DeviceSerial c3ca0370 -Mode LaunchBlueDragon -MountCache true -DisassembleFunctions true -Arm64GuestStoreWatch 82785548`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -DeviceSerial c3ca0370 -Mode Capture`
- `powershell -ExecutionPolicy Bypass -File tools\arm64\hir_coverage_report.ps1 -LogPath scratch\thor-debug\20260518-100921-logcat.txt`
- `adb -s c3ca0370 shell top -H -p 30849 -n 1 -b`
- `adb -s c3ca0370 shell ps -T -p 30849`

Files changed:

- `src/xenia/kernel/xboxkrnl/xboxkrnl_io.cc`
- `src/xenia/emulator.cc`
- `src/xenia/apu/nop/nop_audio_system.cc`
- `docs/research/20260518-100510-blue-dragon-soundbank-null-thunk.md`
- `docs/worklogs/20260518.md`
- `AGENTS.md`

Implementation notes:

- Added focused `NtReadFile` / `NtReadFileScatter` telemetry for
  `!necessity` paths.
- Added a Blue Dragon crash caller probe that prints static strings and object
  words around the `0x826A23C8` null thunk crash.
- Added a real silent `NopAudioDriver` for Android `apu=nop` launches. It
  accepts submitted frames, releases the audio semaphore, and lets
  `NopAudioSystem::CreateDriver` return `X_STATUS_SUCCESS`.

Validation:

- Native core build passed.
- APK shell build passed.
- APK install to Thor serial `c3ca0370` passed.
- Pre-fix caller probe capture:
  `scratch/thor-debug/20260518-100510-logcat.txt`.
- Post-fix captures:
  `scratch/thor-debug/20260518-100921-logcat.txt`,
  `scratch/thor-debug/20260518-101159-logcat.txt`.
- HIR coverage report on the post-fix capture found no guest crash PCs.

Findings:

- `D:\pack\!necessity.ipk` opens and reads successfully:
  3,125,248 bytes read with status `00000000`.
- The crash strings identify the failing path as
  `Sound::SOUNDBANK::Load XACTCreateSoundBank()`, not the later font path
  misses.
- The sound-bank object starts with `0x4B424453` (`KBDS`).
- Before the fix, Android `apu=nop` could not create an audio driver because
  `NopAudioSystem::CreateDriver` returned `X_STATUS_NOT_IMPLEMENTED`.
- After the silent-driver fix, Blue Dragon writes the previously null global:
  `826A2550@826A2598->82785548`.
- The old guest crash at `0x826A23E8` did not recur in the 10:09 or 10:11
  captures.
- Blue Dragon now creates draw and sound threads, but the visible Android OSD
  still shows only the static `AArch64 JIT pending` badge.

Blockers:

- Blue Dragon is not at title yet.
- The active CPU consumers after the fix are `Audio Worker` and `Draw Thread`.
- The current performance wall is slow ARM64 interpreter execution, especially
  functions `82393310`, `82393640`, `823F2568`, and `822B54C0`.

Next actions:

- Add targeted slow-function disassembly/source-offset logging for the current
  hot functions.
- Increase useful mini-JIT rejection detail for hot functions after the initial
  rejection budget is exhausted.
- Prioritize AArch64 mini-JIT coverage for the hot draw/audio path before more
  file-system debugging.

## 2026-05-18 10:18-10:24 EDT - Silent Nop Audio Pacing Check

Branch: `master`

Goal:

- Reduce the post-fix `Audio Worker` spin without regressing Blue Dragon's
  progress past sound-bank initialization.

Commands run:

- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode ApkShell`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -DeviceSerial c3ca0370 -Mode Install`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -DeviceSerial c3ca0370 -Mode LaunchBlueDragon -MountCache true -DisassembleFunctions true -Arm64GuestStoreWatch 82785548`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -DeviceSerial c3ca0370 -Mode Capture`
- `adb -s c3ca0370 shell top -H -p 3887 -n 1 -b`
- `adb -s c3ca0370 shell top -H -p 5585 -n 1 -b`
- `powershell -ExecutionPolicy Bypass -File tools\arm64\hir_coverage_report.ps1 -LogPath scratch\thor-debug\20260518-101858-logcat.txt`

Files changed:

- `src/xenia/apu/nop/nop_audio_system.cc`
- `docs/research/20260518-100510-blue-dragon-soundbank-null-thunk.md`
- `docs/worklogs/20260518.md`

Validation:

- Native core build passed.
- APK shell build passed.
- APK install passed.
- 5 ms pacing capture:
  `scratch/thor-debug/20260518-101858-logcat.txt`.
- 16 ms experiment capture:
  `scratch/thor-debug/20260518-102213-logcat.txt`.

Findings:

- The 5 ms silent audio pacing still reaches the fixed path: draw thread,
  sound thread, and `826A2550@826A2598->82785548` store-watch hit.
- The 16 ms experiment reduced apparent `Audio Worker` CPU but delayed the
  same slow-function milestones, so it is worse for the current title-screen
  push.
- Kept the 5 ms nop-audio pacing as the better bring-up compromise.

Blockers:

- Blue Dragon still does not visibly reach title.
- The current wall remains AArch64 mini-JIT coverage for hot interpreter
  functions, not audio driver creation.

Next actions:

- Add source-offset/opcode details to slow-function logs.
- Target mini-JIT support for the hottest draw/audio interpreter functions.

## 2026-05-18 10:24-11:13 EDT - Blue Dragon ARM64 Scalar Mini-JIT Push

Branch: `master`

Goal:

- Keep pushing Blue Dragon Disc 1 toward title on the AYN Thor by shrinking the
  ARM64 mini-JIT fallback set and separating scalar CPU blockers from the next
  vector/graphics wall.

Commands run:

- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -RepoRoot "C:\Users\leanerdesigner\Documents\New project 8\xenia-thor" -DeviceSerial c3ca0370 -Mode LaunchBlueDragon -Arm64GuestStoreWatch 82785548`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -RepoRoot "C:\Users\leanerdesigner\Documents\New project 8\xenia-thor" -DeviceSerial c3ca0370 -Mode Capture`
- `rg "ARM64 JIT fallback" scratch\thor-debug\20260518-111309-logcat.txt`

Files changed:

- `src/xenia/cpu/backend/arm64/arm64_function.cc`
- `src/xenia/cpu/backend/arm64/arm64_function.h`
- `src/xenia/cpu/backend/arm64/arm64_assembler.cc`
- `src/xenia/cpu/backend/arm64/arm64_jit.cc`
- `src/xenia/cpu/backend/arm64/arm64_code_cache.cc`
- `docs/research/20260518-111343-blue-dragon-arm64-mini-jit-vector-wall.md`
- `docs/worklogs/20260518.md`
- `AGENTS.md`
- `README.md`

Implementation notes:

- Added HIR opcode names, slow-function instruction windows, and stored
  mini-JIT rejection reasons to the ARM64 interpreter/JIT logging path.
- Added raw scalar float slot support for load/store/local/context/memory
  operations.
- Added helper-backed scalar mini-JIT support for float cast/convert,
  add/sub/mul/div, mul-add/mul-sub, neg/abs/sqrt, compare, `IS_NAN`,
  `ATOMIC_COMPARE_EXCHANGE`, and MMIO load/store.
- Raised `arm64_mini_jit_max_stack_bytes` and added large stack-slot
  addressing for temporary frames.
- Replaced generated stack zero-fill loops with a helper call to avoid bloated
  AArch64 output.
- Added `arm64_jit_code_cache_mb` and defaulted the research code cache to
  128 MiB for longer Thor captures.

Validation:

- Native core build passed after each patch group.
- Full deploy passed and installed to Thor serial `c3ca0370`.
- Captures:
  - `scratch/thor-debug/20260518-105521-*`
  - `scratch/thor-debug/20260518-110323-*`
  - `scratch/thor-debug/20260518-110845-*`
  - `scratch/thor-debug/20260518-111309-*`
- Latest APK SHA-256:
  `E0D689EFAEEF54516BC1D3ECE4020CC0D49ECEE93DC88D78B9E1ED1A931E9FCA`.

Findings:

- The old soundbank null-thunk crash remains fixed; the store watch still hits
  `826A2550@826A2598->82785548`.
- The earlier scalar fallback chain moved from float context load/store, to
  float assignment, to `OPCODE_CONVERT`, to float cast/binary, and then out of
  the hot-loop path.
- Blue Dragon consistently reaches later `D:\pack\*.iph` and `D:\pack\*.ipk`
  loading, including `effect.iph`, `battle.ipk`, `database.ipk`, `script.ipk`,
  `packmem_us.ipk`, and `battle_start.ipk`.
- Latest fallback table is now vector-heavy:
  `load_memory vec128`, `store_context vec128`, `load_context vec128`,
  `OPCODE_LOAD_VECTOR_SHL`, `OPCODE_LOAD_VECTOR_SHR`, and `OPCODE_UNPACK`.
- No Android crash, tombstone, or guest crash PC appeared in the latest
  capture.
- The screenshot still shows only the Android OSD and static
  `AArch64 JIT pending` badge; Blue Dragon is not at title yet.

Blockers:

- The mini-JIT still uses a 64-bit slot model, which cannot safely hold
  `VEC128_TYPE` values.
- Remaining unsupported vector opcodes are:
  - `27`: `OPCODE_LOAD_VECTOR_SHL`
  - `28`: `OPCODE_LOAD_VECTOR_SHR`
  - `109`: `OPCODE_UNPACK`
- Need distinguish whether the next visible stall is vector interpreter speed,
  GPU presentation, or both.

Next actions:

- Add a real mini-JIT slot-layout table with 16-byte aligned vec128 slots.
- Track local value types so vec128 locals are safe.
- Implement helper-backed or NEON-backed vec128 load/store/context paths,
  `LOAD_VECTOR_SHL`, `LOAD_VECTOR_SHR`, and `UNPACK`.
- Add thread PC/progress sampling during long captures to prove whether the
  guest is CPU-spinning or waiting on graphics/storage.

## 2026-05-18 11:13-11:52 EDT - Blue Dragon Vec128 Mini-JIT Fallback-Zero Push

Branch: `master`

Goal:

- Continue pushing Blue Dragon Disc 1 toward title on the AYN Thor by clearing
  the vector-heavy ARM64 mini-JIT fallback wall exposed by the scalar pass.

Commands run:

- `git status --short --branch`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -RepoRoot "C:\Users\leanerdesigner\Documents\New project 8\xenia-thor" -DeviceSerial c3ca0370 -Mode LaunchBlueDragon -Arm64GuestStoreWatch 82785548`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -RepoRoot "C:\Users\leanerdesigner\Documents\New project 8\xenia-thor" -DeviceSerial c3ca0370 -Mode Capture`
- `rg -n "ARM64 JIT fallback|unsupported opcode|unsupported .*type|signal|F DEBUG|Fatal signal|tombstone|crash" scratch\thor-debug\20260518-115022-logcat.txt scratch\thor-debug\20260518-115022-logcat-filtered.txt`

Files changed:

- `src/xenia/cpu/backend/arm64/arm64_assembler.cc`
- `src/xenia/cpu/backend/arm64/arm64_function.cc`
- `src/xenia/cpu/backend/arm64/arm64_function.h`
- `src/xenia/cpu/backend/arm64/arm64_jit.cc`
- `docs/research/20260518-115238-blue-dragon-arm64-vec128-mini-jit-fallback-zero.md`
- `docs/worklogs/20260518.md`
- `AGENTS.md`
- `README.md`

Implementation notes:

- Added a real mini-JIT stack slot layout with 16-byte aligned vec128 slots.
- Carried HIR local type information into the ARM64 `Program` so local vec128
  values do not share the old scalar-only slot model.
- Added helper-backed vec128 support for local/context/memory load/store,
  constant vector operands, raw vec128 copies, vector shifts, unpack, extract,
  splat, permute, swizzle, dot3/dot4, vector conversions, vector compares,
  vector add/sub, vector select, vec128 unary operations, and vec128
  mul-add/sub.
- Kept this as a correctness-first mini-JIT lane; helpers are not final NEON
  codegen.

Validation:

- Native core builds passed after each patch group.
- Full deploys passed and installed to Thor serial `c3ca0370`.
- Captures:
  - `scratch/thor-debug/20260518-112417-*`
  - `scratch/thor-debug/20260518-112859-*`
  - `scratch/thor-debug/20260518-113514-*`
  - `scratch/thor-debug/20260518-114058-*`
  - `scratch/thor-debug/20260518-114733-*`
  - `scratch/thor-debug/20260518-115022-*`
- Latest APK SHA-256:
  `D391A883C0F4EDDA23DECC2134DB3DF2FF10D8C608AD673FB7056DF05A0F7054`.

Findings:

- Fallbacks moved from vec128 slot load/store and `UNPACK`, to
  permute/swizzle/extract/dot-product, to splat/dot4/vector-convert, to
  round/vector-compare/vector-add/rsqrt/recip, to vec128 select.
- The latest 90-second capture has zero `ARM64 JIT fallback`, unsupported
  opcode/type, native crash, tombstone, or fatal-signal lines.
- The store watch still hits `826A2550@826A2598->82785548`.
- Blue Dragon still reaches later pack loading, including `battle.ipk`,
  `database.ipk`, `script.ipk`, `packmem_us.ipk`, and `battle_start.ipk`.
- Screenshot still shows only the Android shell/OSD and static
  `AArch64 JIT pending` badge. This badge is stale Java UI, not proof that
  the mini-JIT failed.

Blockers:

- Blue Dragon is not visibly at title yet.
- The current wall has moved from first-order HIR mini-JIT coverage to
  post-pack-load progress: guest CPU progress, render submission, Vulkan
  presentation, or Android OSD masking.

Next actions:

- Add runtime OSD state from native code so screenshots show real backend/JIT
  status.
- Add post-pack-load progress sampling: guest thread PCs, hot guest functions,
  command processor progress, draw submits, and swapchain presents.
- Use RenderDoc/AGI only after normal logs show first real guest render
  submission.

## 2026-05-18 11:52-13:31 EDT - Blue Dragon KTHREAD Timer And GPU Watchdog

Branch: `master`

Goal:

- Continue pushing Blue Dragon Disc 1 toward title on the AYN Thor Max after
  the ARM64 vec128 mini-JIT fallback-zero capture.
- Separate CPU/timer waits, kernel event waits, GPU ring progress, and game D3D
  watchdog failures so the loop is faster than guessing.

Commands run:

- `git status --short --branch`
- `adb devices`
- `adb -s c3ca0370 shell getprop ro.product.model`
- `adb -s c3ca0370 shell getprop ro.build.version.sdk`
- `rg -n "0x01C4|0x01C5|CP_RB|read_ptr_index_|UpdateWritePointer|InitializeRingBuffer|ReadRegister\(|WriteRegister\(" src/xenia/gpu`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragon -DeviceSerial c3ca0370 ...`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode Capture -DeviceSerial c3ca0370`
- `rg -n "Blue Dragon draw wait probe|The GPU is hung|CP_RB_RPTR|CP_RB_WPTR|Fatal signal|tombstone|ARM64 JIT fallback|unsupported opcode|unsupported .*type" scratch\thor-debug\20260518-*.txt`
- `git clone --depth 1 --filter=blob:none https://github.com/xenia-canary/xenia-canary.git scratch\upstream\xenia-canary`
- Focused PPC dumps through `-DisassembleFunctions true -DisassembleFunctionFilter "8246DBB0,8246B408,8246E6A4,82466C00"`

Files changed:

- `src/xenia/cpu/backend/arm64/arm64_function.cc`
- `src/xenia/cpu/backend/arm64/arm64_function.h`
- `src/xenia/cpu/cpu_flags.cc`
- `src/xenia/cpu/cpu_flags.h`
- `src/xenia/cpu/ppc/ppc_translator.cc`
- `src/xenia/gpu/command_processor.cc`
- `src/xenia/gpu/d3d12/d3d12_command_processor.cc`
- `src/xenia/gpu/gpu_flags.cc`
- `src/xenia/gpu/gpu_flags.h`
- `src/xenia/gpu/graphics_system.cc`
- `src/xenia/gpu/shared_memory.cc`
- `src/xenia/gpu/shared_memory.h`
- `src/xenia/gpu/vulkan/vulkan_command_processor.cc`
- `src/xenia/kernel/xboxkrnl/xboxkrnl_threading.cc`
- `android/android_studio_project/app/src/main/java/jp/xenia/emulator/EmulatorActivity.java`
- `tools/thor/thor_xenia_debug.ps1`
- `docs/research/20260518-133100-blue-dragon-kthread-gpu-watchdog.md`
- `docs/worklogs/20260518.md`
- `AGENTS.md`
- `README.md`

Implementation notes:

- Added focused guest-function disassembly filtering through
  `disassemble_function_filter`.
- Added ARM64 compiled-call sampling and a Blue Dragon draw-wait probe for guest
  function `8246B408`.
- Added kernel wait and event tracing cvars for post-pack-load waits.
- Added `arm64_update_kthread_time`, which writes current guest uptime into the
  running guest KTHREAD `+0x58` before ARM64 function dispatch.
- Mirrored command processor ring read/write pointers into MMIO registers
  `0x01C4` / `0x01C5`.
- Added Canary-inspired `clear_memory_page_state` support and ring-buffer
  zeroing on initialization.
- Added switchable research-only GPU experiments:
  `gpu_interrupt_on_ring_idle` and `gpu_blue_dragon_kick_wait_token`.

Validation:

- Full deploys passed and installed to Thor serial `c3ca0370`.
- Captures:
  - `scratch/thor-debug/20260518-123221-*`
  - `scratch/thor-debug/20260518-123609-*`
  - `scratch/thor-debug/20260518-124355-*`
  - `scratch/thor-debug/20260518-124812-*`
  - `scratch/thor-debug/20260518-125319-*`
  - `scratch/thor-debug/20260518-125759-*`
  - `scratch/thor-debug/20260518-130625-*`
  - `scratch/thor-debug/20260518-131640-*`
  - `scratch/thor-debug/20260518-131928-*`
  - `scratch/thor-debug/20260518-132520-*`
- Latest APK SHA-256:
  `C5EAA2B63D0ED10852567E7ABA9F9D78A33A748E4A835F1462D93775A01E0F40`.

Findings:

- The KTHREAD timer fix moved Blue Dragon past the earlier draw wait where
  `global_tick` stayed zero.
- After that fix, Blue Dragon enters the game's D3D watchdog and prints:
  `The GPU is hung! D3D version is 3529.0 retail, kernel is 65535, frame is 0`.
- CP ring pointer mirroring fixed the obviously stale watchdog dump:
  `CP_RB_RPTR` now matches `CP_RB_WPTR` at `0x1f/0x1f` and `0x25/0x25`.
- The watchdog still fires, so stale ring read pointer was not the only issue.
- Focused PPC dumps show the draw wait loop polls the graphics user-data object
  `4000E880`, token pointer `object + 0x2A10`, saved token at wait-state `+0x8`,
  and KTHREAD uptime.
- The Blue Dragon token-kick experiment did increment token pointer `FFCA3000`,
  but the wait state captured the new value and timed out again. Token movement
  alone is not enough.

Blockers:

- Blue Dragon still does not reach title.
- Current wall is a D3D/GPU completion or presentation contract: command ring
  pointers drain, but the game still considers frame 0 hung.
- No `XE_SWAP` / first visible frame proof appeared in the latest captures.

Next actions:

- Main lane: broaden ARM64 JIT/HIR coverage in batches, using coverage reports
  and audited AArch64 fork references, then use Thor runs as checkpoints.
- GPU lane: instrument `VdGetSystemCommandBuffer`, `VdSwap`, PM4 swap packets,
  GPU interrupt callback dispatch, and frame-close state before adding more
  hacks.
- Keep `gpu_interrupt_on_ring_idle` and `gpu_blue_dragon_kick_wait_token`
  default-off and research-only.

## 2026-05-18 13:31-13:48 EDT - ARM64 HIR Gap Batch

Branch: `master`

Goal:

- Stop guessing whether Blue Dragon is blocked by missing ARM64 HIR opcodes.
- Broaden ARM64 mini-JIT and interpreter switch coverage in one batch, then use
  Thor as the checkpoint.

Commands run:

- `git status --short --branch`
- `adb devices`
- `adb shell getprop ro.product.model`
- `adb shell getprop ro.build.version.sdk`
- `powershell -ExecutionPolicy Bypass -File tools\arm64\hir_coverage_report.ps1 -Latest 8 -OutPath scratch\arm64-coverage-20260518-after-watchdog.md`
- ARM64 HIR opcode switch audit against `opcodes.h`, `arm64_jit.cc`, and
  `arm64_function.cc`
- `powershell -ExecutionPolicy Bypass -File tools\arm64\arm64_jit_gap_report.ps1 -OutPath docs\research\20260518-134452-arm64-jit-gap-batch.md`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragon -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode Capture -DeviceSerial c3ca0370`
- `rg -n "ARM64 JIT fallback|unsupported opcode|unimplemented HIR opcode|The GPU is hung|VdSwap|XE_SWAP" scratch\thor-debug\20260518-134757-*.txt`

Files changed:

- `src/xenia/cpu/backend/arm64/arm64_jit.cc`
- `src/xenia/cpu/backend/arm64/arm64_function.cc`
- `tools/arm64/arm64_jit_gap_report.ps1`
- `docs/research/20260518-134452-arm64-jit-gap-batch.md`
- `docs/research/20260518-134832-arm64-jit-gap-device-checkpoint.md`
- `docs/worklogs/20260518.md`
- `AGENTS.md`
- `README.md`

Implementation notes:

- Added helper-backed ARM64 mini-JIT support for vector min/max, vector shifts,
  vector arithmetic shift, vector rotate-left, vector average, insert, pack,
  and atomic exchange.
- Added interpreter support for `DID_SATURATE`, insert, pack, and unpack
  `PACK_TYPE_ULONG_4202020`.
- Added `tools/arm64/arm64_jit_gap_report.ps1` to keep opcode switch coverage
  and recent Thor log signals together in a dated Markdown report.

Validation:

- Initial native-core build caught and fixed a missing `FloatFromBits` helper in
  `arm64_function.cc`.
- Native-core build passed after the fix.
- Full deploy passed and installed APK SHA-256
  `DB7EDE68FFA04C5FF6BBBB46546CD2734D098AEC00070FF91096FA5614AB1B64`.
- Blue Dragon captures:
  - `scratch/thor-debug/20260518-134558-*`
  - `scratch/thor-debug/20260518-134757-*`
- Latest gap report says all 113 HIR opcodes have ARM64 mini-JIT switch
  coverage and interpreter switch coverage.

Findings:

- The first post-batch Blue Dragon capture showed native mini-JIT compile
  activity and pack-file loading, with no immediate missing-op or native crash.
- The longer capture still hit the game's D3D watchdog at frame 0:
  `The GPU is hung! D3D version is 3529.0 retail, kernel is 65535, frame is 0`.
- Ring pointers were still drained and matching in the watchdog dump:
  `CP_RB_RPTR: 0x0000001f`, `CP_RB_WPTR: 0x0000001f`, then `0x25/0x25`.
- Guest symbol traces include `VdSwap`, but there is still no `XE_SWAP` or
  visible game frame proof.

Blockers:

- Blue Dragon still does not reach title.
- The current wall is no longer a missing ARM64 HIR switch-case surface; it is
  GPU/D3D completion, guest-visible swap/present behavior, or a deeper ARM64
  correctness/performance issue that manifests as the game's GPU watchdog.

Next actions:

- Instrument `VdSwap`, guest present/swap calls, PM4 packet submission, and
  Vulkan command processor frame-close state.
- Fix the static Java `AArch64 JIT pending` badge so screenshots reflect real
  native runtime status.
- Keep moving broad backend correctness forward, but use the GPU watchdog as
  the next main debug lane.

## 2026-05-18 13:48-14:12 EDT - Swap Trace And Broad ARM64 Conversion Audit

Branch: `master`

Goal:

- Stop debugging Blue Dragon one Thor crash at a time.
- Add explicit swap/present tracing to prove whether the game reaches guest
  `VdSwap`, PM4 `XE_SWAP`, or Vulkan `IssueSwap`.
- Build a broader x64-to-ARM64 conversion audit so missing backend families and
  infrastructure are visible before the next Thor milestone run.

Commands run:

- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragon -DeviceSerial c3ca0370 -GpuTraceSwap true`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode Capture -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragon -DeviceSerial c3ca0370 -GpuTraceSwap true -GpuInterruptOnRingIdle true`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode Capture -DeviceSerial c3ca0370`
- `rg -n "GPU swap trace|VdSwap|XE_SWAP|Vulkan IssueSwap|The GPU is hung|CP_RB_RPTR|CP_RB_WPTR" scratch\thor-debug\20260518-*.txt`
- `powershell -ExecutionPolicy Bypass -File tools\arm64\arm64_conversion_audit.ps1 -OutPath docs\research\20260518-140436-arm64-x64-conversion-audit.md -LatestLogs 10`

Files changed:

- `src/xenia/gpu/gpu_flags.cc`
- `src/xenia/gpu/gpu_flags.h`
- `src/xenia/gpu/command_processor.cc`
- `src/xenia/gpu/vulkan/vulkan_command_processor.cc`
- `src/xenia/kernel/xboxkrnl/xboxkrnl_video.cc`
- `src/xenia/cpu/backend/arm64/arm64_backend.cc`
- `android/android_studio_project/app/src/main/java/jp/xenia/emulator/EmulatorActivity.java`
- `android/android_studio_project/app/src/main/res/layout/activity_emulator.xml`
- `android/android_studio_project/app/src/main/res/values/strings.xml`
- `tools/thor/thor_xenia_debug.ps1`
- `tools/arm64/arm64_conversion_audit.ps1`
- `docs/research/20260518-140436-arm64-x64-conversion-audit.md`
- `docs/research/20260518-140712-blue-dragon-swap-trace.md`
- `docs/worklogs/20260518.md`
- `AGENTS.md`

Validation:

- Full deploy passed and installed APK SHA-256
  `F32E2E8888B9EF5CBDC7E654B1716B384F96C3D3EED5BF6181D85955C8334D85`.
- Final full deploy after the ARM64 startup log wording change passed and
  installed APK SHA-256
  `358E381CF27C20576E5B19CDF816774F1FBE6629E1640D5CED4E619B67045478`.
- Blue Dragon capture with swap tracing:
  `scratch/thor-debug/20260518-135816-*`.
- Blue Dragon capture with swap tracing plus ring-idle interrupt simulation:
  `scratch/thor-debug/20260518-140109-*`.
- The Android OSD now reports `AArch64 mini-JIT research` instead of the stale
  `AArch64 JIT pending` badge.

Findings:

- Runtime swap trace reaches engine initialization and ring setup, but no
  explicit runtime `GPU swap trace: VdSwap`, `XE_SWAP`, or Vulkan `IssueSwap`.
- Import/symbol listings mention `VdSwap`, but that is not runtime call proof.
- Both Blue Dragon runs still hit the D3D frame-0 watchdog with drained,
  matching ring pointers at `0x1f/0x1f`, then `0x25/0x25`.
- Enabling the current ring-idle interrupt experiment did not move the game to
  runtime swap/present.
- The broad conversion audit shows all 113 HIR opcodes have ARM64 mini-JIT and
  interpreter switch cases, but x64-grade backend infrastructure is still
  missing: host-to-guest thunk, guest-to-host thunk, resolve thunk, breakpoint
  support, exception/signal callback path, code-cache indirection/commit
  metadata, unwind data, and broad native lowering to replace 52 helper-call
  sites.

Blockers:

- Blue Dragon still does not reach title.
- The biggest process blocker is no longer a single missing opcode; it is the
  lack of a systematic ARM64 conversion harness and backend parity checklist.

Next actions:

- Batch backend conversion before the next Thor milestone: ABI/thunks, code
  cache metadata, debugger/exception services, scalar/control/memory native
  lowering, vector native lowering, then guest-function replay tests.
- Add compiled-vs-interpreter and x64-vs-ARM64 differential tests so Thor is a
  checkpoint instead of the only test.
- Keep `gpu_trace_swap` default-off and use it for GPU milestone captures.

## 2026-05-18 14:13-14:26 EDT - ARM64 Working Fork Reality Check

Branch: `master`

Goal:

- Answer whether any ARM64 Xenia fork actually works.
- Clarify whether to keep the helper mini-JIT scaffold or pivot directly to a
  real AArch64 emitter backbone.

Commands run:

- Web research for current Xenia ARM64 / Android / Apple forks.
- `Get-Date -Format 'yyyyMMdd-HHmmss K'`
- `git status --short --branch`

Files changed:

- `AGENTS.md`
- `docs/research/20260518-142302-arm64-xenia-working-forks.md`
- `docs/worklogs/20260518.md`

Findings:

- aX360e is the strongest Android evidence: it is public on Google Play, claims
  ARM64/Vulkan support, is based on Xenia `arm64-backend`/xenia-canary, and has
  a public GitHub repository.
- XeniOS/xenia-mac are Apple-focused evidence that ARM64 Xenia-derived code can
  reach game states, but their platform layer is Metal/iOS/macOS, not Android
  Vulkan.
- Xenia Edge is the most useful technical branch to study for A64 backend
  shape, especially Wunkolo-derived A64 backend work.
- No checked source proves a clean, drop-in Android ARM64 backend that would
  make Blue Dragon title-ready on Thor immediately.

Decision:

- Keep the helper mini-JIT as a bring-up scaffold/fallback.
- Start reshaping toward a real AArch64 emitter backbone now.
- Prioritize correctness coverage, with targeted native hot-path work to avoid
  Blue Dragon watchdogs caused only by CPU slowness.
- Study/import from aX360e, Xenia Edge, and XeniOS only with clean license and
  attribution.
