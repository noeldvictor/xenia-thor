# Worklog - 2026-05-18

## 2026-05-18 09:12-09:21 EDT - Blue Dragon PPC Global Reference Scanner

Goal:

- Replace the noisy crash-time string search for `0x82785548` with an
  instruction-aware PPC reference scanner so the Thor run can tell whether any
  translated guest function directly reads or writes the null Blue Dragon
  global.

Branch and commit:

- Branch: `master`.
- Starting commit: `989609c0b Add Thor guest store watch telemetry`.

Commands run:

- `git status --short --branch`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode ApkShell`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -DeviceSerial c3ca0370 -Mode Install`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -DeviceSerial c3ca0370 -Mode LaunchBlueDragon -MountCache true -DisassembleFunctions true -Arm64GuestStoreWatch 82785548`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -DeviceSerial c3ca0370 -Mode Capture`
- `powershell -ExecutionPolicy Bypass -File tools\arm64\hir_coverage_report.ps1 -LogPath scratch\thor-debug\20260518-091952-logcat.txt`

Files changed:

- `AGENTS.md`
- `src/xenia/emulator.cc`
- `src/xenia/kernel/xboxkrnl/xboxkrnl_io.cc`
- `tools/arm64/hir_coverage_report.ps1`
- `docs/research/20260518-092052-blue-dragon-ppc-global-ref-scan.md`

Implementation notes:

- Added a crash-time PPC direct-reference scanner over translated function
  debug disassembly.
- The scanner parses PPC instruction words from Xenia's source-disasm lines,
  tracks simple constant address formation through `addis`, `addi`, `ori`, and
  `or` register copies, and resolves D-form / DS-form load-store effective
  addresses.
- Replaced the previous raw text search for `0x5548(` with a decoded search for
  exact guest address `0x82785548`.
- Extended `tools/arm64/hir_coverage_report.ps1` to summarize `PPC global ref`
  lines.
- Added focused `NtCreateFile success` telemetry for `\pack` and `\!necessity`
  paths so archive/package opens can be compared against the later font misses.
- Updated `AGENTS.md` to mention PPC global-reference scanner hits in the HIR
  coverage report output.

Validation:

- Native core build passed.
- APK shell build passed.
- APK install to Thor serial `c3ca0370` passed.
- Blue Dragon Disc 1 capture set:
  `scratch/thor-debug/20260518-091952-logcat.txt`,
  `scratch/thor-debug/20260518-091952-logcat-filtered.txt`,
  `scratch/thor-debug/20260518-091952-meta.txt`,
  `scratch/thor-debug/20260518-091952-screenshot.png`.
- Path telemetry capture set:
  `scratch/thor-debug/20260518-092516-logcat.txt`,
  `scratch/thor-debug/20260518-092516-logcat-filtered.txt`,
  `scratch/thor-debug/20260518-092516-meta.txt`,
  `scratch/thor-debug/20260518-092516-screenshot.png`.
- HIR coverage report on the capture passed and now includes a `PPC Global
  References` section.

Findings:

- Blue Dragon still crashes at guest PC `0x826A23E8`.
- `0x82785548` is still `0x00000000` at crash.
- The decoded PPC global-reference scanner found exactly one translated direct
  reference to `0x82785548`:
  `0x826A23DC lwz r11, 0x5548(r11)` in function `0x826A23C8-0x826A23F4`.
- It found zero translated direct stores to `0x82785548`.
- `D:\pack\!necessity.ipk` opens successfully before the crash. Earlier pack
  files such as `D:\pack\gimmick.iph`, `D:\pack\gimmick.ipk`,
  `D:\pack\map\gimmick.iph`, and `D:\pack\map\gimmick.ipk` also open
  successfully.
- The same run still shows the failed font opens under
  `D:\!necessity\font\...` immediately before the crash.

Blockers:

- The initializer that should seed `0x82785548` either was not translated before
  the crash, was not reached, uses a non-direct address path, or belongs to a
  native/module initialization path outside the scanned translated PPC
  functions.
- `D:\!necessity\font\...` file misses remain suspicious. The raw pack archive
  opens, so this now looks more like missing archive virtual-path setup or a
  skipped game-side archive initializer than a missing ISO file.

Next actions:

- Add targeted function/return tracing around `0x821903B8`, `0x822710D0`,
  `0x826A23C8`, and any functions that open `\pack` or `\!necessity`.
- Trace reads and return values around the successful `D:\pack\!necessity.ipk`
  open and the later failed `D:\!necessity\font\...` opens.

## 2026-05-18 09:52-10:18 EDT - Blue Dragon SoundBank Null Thunk Fix

Branch: `master`

Goal:

- Determine whether the `D:\pack\!necessity.ipk` open/read path was actually
  causing the `0x826A23E8` null thunk crash, then keep pushing toward Blue
  Dragon title on the AYN Thor.

Commands run:

- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode ApkShell`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -DeviceSerial c3ca0370 -Mode Install`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -DeviceSerial c3ca0370 -Mode LaunchBlueDragon -MountCache true -DisassembleFunctions true -Arm64GuestStoreWatch 82785548`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -DeviceSerial c3ca0370 -Mode Capture`
- `powershell -ExecutionPolicy Bypass -File tools\arm64\hir_coverage_report.ps1 -LogPath scratch\thor-debug\20260518-100921-logcat.txt`
- `adb -s c3ca0370 shell top -H -p 30849 -n 1 -b`
- `adb -s c3ca0370 shell ps -T -p 30849`

Files changed:

- `src/xenia/kernel/xboxkrnl/xboxkrnl_io.cc`
- `src/xenia/emulator.cc`
- `src/xenia/apu/nop/nop_audio_system.cc`
- `docs/research/20260518-100510-blue-dragon-soundbank-null-thunk.md`
- `docs/worklogs/20260518.md`
- `AGENTS.md`

Implementation notes:

- Added focused `NtReadFile` / `NtReadFileScatter` telemetry for
  `!necessity` paths.
- Added a Blue Dragon crash caller probe that prints static strings and object
  words around the `0x826A23C8` null thunk crash.
- Added a real silent `NopAudioDriver` for Android `apu=nop` launches. It
  accepts submitted frames, releases the audio semaphore, and lets
  `NopAudioSystem::CreateDriver` return `X_STATUS_SUCCESS`.

Validation:

- Native core build passed.
- APK shell build passed.
- APK install to Thor serial `c3ca0370` passed.
- Pre-fix caller probe capture:
  `scratch/thor-debug/20260518-100510-logcat.txt`.
- Post-fix captures:
  `scratch/thor-debug/20260518-100921-logcat.txt`,
  `scratch/thor-debug/20260518-101159-logcat.txt`.
- HIR coverage report on the post-fix capture found no guest crash PCs.

Findings:

- `D:\pack\!necessity.ipk` opens and reads successfully:
  3,125,248 bytes read with status `00000000`.
- The crash strings identify the failing path as
  `Sound::SOUNDBANK::Load XACTCreateSoundBank()`, not the later font path
  misses.
- The sound-bank object starts with `0x4B424453` (`KBDS`).
- Before the fix, Android `apu=nop` could not create an audio driver because
  `NopAudioSystem::CreateDriver` returned `X_STATUS_NOT_IMPLEMENTED`.
- After the silent-driver fix, Blue Dragon writes the previously null global:
  `826A2550@826A2598->82785548`.
- The old guest crash at `0x826A23E8` did not recur in the 10:09 or 10:11
  captures.
- Blue Dragon now creates draw and sound threads, but the visible Android OSD
  still shows only the static `AArch64 JIT pending` badge.

Blockers:

- Blue Dragon is not at title yet.
- The active CPU consumers after the fix are `Audio Worker` and `Draw Thread`.
- The current performance wall is slow ARM64 interpreter execution, especially
  functions `82393310`, `82393640`, `823F2568`, and `822B54C0`.

Next actions:

- Add targeted slow-function disassembly/source-offset logging for the current
  hot functions.
- Increase useful mini-JIT rejection detail for hot functions after the initial
  rejection budget is exhausted.
- Prioritize AArch64 mini-JIT coverage for the hot draw/audio path before more
  file-system debugging.

## 2026-05-18 10:18-10:24 EDT - Silent Nop Audio Pacing Check

Branch: `master`

Goal:

- Reduce the post-fix `Audio Worker` spin without regressing Blue Dragon's
  progress past sound-bank initialization.

Commands run:

- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode ApkShell`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -DeviceSerial c3ca0370 -Mode Install`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -DeviceSerial c3ca0370 -Mode LaunchBlueDragon -MountCache true -DisassembleFunctions true -Arm64GuestStoreWatch 82785548`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -DeviceSerial c3ca0370 -Mode Capture`
- `adb -s c3ca0370 shell top -H -p 3887 -n 1 -b`
- `adb -s c3ca0370 shell top -H -p 5585 -n 1 -b`
- `powershell -ExecutionPolicy Bypass -File tools\arm64\hir_coverage_report.ps1 -LogPath scratch\thor-debug\20260518-101858-logcat.txt`

Files changed:

- `src/xenia/apu/nop/nop_audio_system.cc`
- `docs/research/20260518-100510-blue-dragon-soundbank-null-thunk.md`
- `docs/worklogs/20260518.md`

Validation:

- Native core build passed.
- APK shell build passed.
- APK install passed.
- 5 ms pacing capture:
  `scratch/thor-debug/20260518-101858-logcat.txt`.
- 16 ms experiment capture:
  `scratch/thor-debug/20260518-102213-logcat.txt`.

Findings:

- The 5 ms silent audio pacing still reaches the fixed path: draw thread,
  sound thread, and `826A2550@826A2598->82785548` store-watch hit.
- The 16 ms experiment reduced apparent `Audio Worker` CPU but delayed the
  same slow-function milestones, so it is worse for the current title-screen
  push.
- Kept the 5 ms nop-audio pacing as the better bring-up compromise.

Blockers:

- Blue Dragon still does not visibly reach title.
- The current wall remains AArch64 mini-JIT coverage for hot interpreter
  functions, not audio driver creation.

Next actions:

- Add source-offset/opcode details to slow-function logs.
- Target mini-JIT support for the hottest draw/audio interpreter functions.

## 2026-05-18 10:24-11:13 EDT - Blue Dragon ARM64 Scalar Mini-JIT Push

Branch: `master`

Goal:

- Keep pushing Blue Dragon Disc 1 toward title on the AYN Thor by shrinking the
  ARM64 mini-JIT fallback set and separating scalar CPU blockers from the next
  vector/graphics wall.

Commands run:

- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -RepoRoot "C:\Users\leanerdesigner\Documents\New project 8\xenia-thor" -DeviceSerial c3ca0370 -Mode LaunchBlueDragon -Arm64GuestStoreWatch 82785548`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -RepoRoot "C:\Users\leanerdesigner\Documents\New project 8\xenia-thor" -DeviceSerial c3ca0370 -Mode Capture`
- `rg "ARM64 JIT fallback" scratch\thor-debug\20260518-111309-logcat.txt`

Files changed:

- `src/xenia/cpu/backend/arm64/arm64_function.cc`
- `src/xenia/cpu/backend/arm64/arm64_function.h`
- `src/xenia/cpu/backend/arm64/arm64_assembler.cc`
- `src/xenia/cpu/backend/arm64/arm64_jit.cc`
- `src/xenia/cpu/backend/arm64/arm64_code_cache.cc`
- `docs/research/20260518-111343-blue-dragon-arm64-mini-jit-vector-wall.md`
- `docs/worklogs/20260518.md`
- `AGENTS.md`
- `README.md`

Implementation notes:

- Added HIR opcode names, slow-function instruction windows, and stored
  mini-JIT rejection reasons to the ARM64 interpreter/JIT logging path.
- Added raw scalar float slot support for load/store/local/context/memory
  operations.
- Added helper-backed scalar mini-JIT support for float cast/convert,
  add/sub/mul/div, mul-add/mul-sub, neg/abs/sqrt, compare, `IS_NAN`,
  `ATOMIC_COMPARE_EXCHANGE`, and MMIO load/store.
- Raised `arm64_mini_jit_max_stack_bytes` and added large stack-slot
  addressing for temporary frames.
- Replaced generated stack zero-fill loops with a helper call to avoid bloated
  AArch64 output.
- Added `arm64_jit_code_cache_mb` and defaulted the research code cache to
  128 MiB for longer Thor captures.

Validation:

- Native core build passed after each patch group.
- Full deploy passed and installed to Thor serial `c3ca0370`.
- Captures:
  - `scratch/thor-debug/20260518-105521-*`
  - `scratch/thor-debug/20260518-110323-*`
  - `scratch/thor-debug/20260518-110845-*`
  - `scratch/thor-debug/20260518-111309-*`
- Latest APK SHA-256:
  `E0D689EFAEEF54516BC1D3ECE4020CC0D49ECEE93DC88D78B9E1ED1A931E9FCA`.

Findings:

- The old soundbank null-thunk crash remains fixed; the store watch still hits
  `826A2550@826A2598->82785548`.
- The earlier scalar fallback chain moved from float context load/store, to
  float assignment, to `OPCODE_CONVERT`, to float cast/binary, and then out of
  the hot-loop path.
- Blue Dragon consistently reaches later `D:\pack\*.iph` and `D:\pack\*.ipk`
  loading, including `effect.iph`, `battle.ipk`, `database.ipk`, `script.ipk`,
  `packmem_us.ipk`, and `battle_start.ipk`.
- Latest fallback table is now vector-heavy:
  `load_memory vec128`, `store_context vec128`, `load_context vec128`,
  `OPCODE_LOAD_VECTOR_SHL`, `OPCODE_LOAD_VECTOR_SHR`, and `OPCODE_UNPACK`.
- No Android crash, tombstone, or guest crash PC appeared in the latest
  capture.
- The screenshot still shows only the Android OSD and static
  `AArch64 JIT pending` badge; Blue Dragon is not at title yet.

Blockers:

- The mini-JIT still uses a 64-bit slot model, which cannot safely hold
  `VEC128_TYPE` values.
- Remaining unsupported vector opcodes are:
  - `27`: `OPCODE_LOAD_VECTOR_SHL`
  - `28`: `OPCODE_LOAD_VECTOR_SHR`
  - `109`: `OPCODE_UNPACK`
- Need distinguish whether the next visible stall is vector interpreter speed,
  GPU presentation, or both.

Next actions:

- Add a real mini-JIT slot-layout table with 16-byte aligned vec128 slots.
- Track local value types so vec128 locals are safe.
- Implement helper-backed or NEON-backed vec128 load/store/context paths,
  `LOAD_VECTOR_SHL`, `LOAD_VECTOR_SHR`, and `UNPACK`.
- Add thread PC/progress sampling during long captures to prove whether the
  guest is CPU-spinning or waiting on graphics/storage.

## 2026-05-18 11:13-11:52 EDT - Blue Dragon Vec128 Mini-JIT Fallback-Zero Push

Branch: `master`

Goal:

- Continue pushing Blue Dragon Disc 1 toward title on the AYN Thor by clearing
  the vector-heavy ARM64 mini-JIT fallback wall exposed by the scalar pass.

Commands run:

- `git status --short --branch`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -RepoRoot "C:\Users\leanerdesigner\Documents\New project 8\xenia-thor" -DeviceSerial c3ca0370 -Mode LaunchBlueDragon -Arm64GuestStoreWatch 82785548`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -RepoRoot "C:\Users\leanerdesigner\Documents\New project 8\xenia-thor" -DeviceSerial c3ca0370 -Mode Capture`
- `rg -n "ARM64 JIT fallback|unsupported opcode|unsupported .*type|signal|F DEBUG|Fatal signal|tombstone|crash" scratch\thor-debug\20260518-115022-logcat.txt scratch\thor-debug\20260518-115022-logcat-filtered.txt`

Files changed:

- `src/xenia/cpu/backend/arm64/arm64_assembler.cc`
- `src/xenia/cpu/backend/arm64/arm64_function.cc`
- `src/xenia/cpu/backend/arm64/arm64_function.h`
- `src/xenia/cpu/backend/arm64/arm64_jit.cc`
- `docs/research/20260518-115238-blue-dragon-arm64-vec128-mini-jit-fallback-zero.md`
- `docs/worklogs/20260518.md`
- `AGENTS.md`
- `README.md`

Implementation notes:

- Added a real mini-JIT stack slot layout with 16-byte aligned vec128 slots.
- Carried HIR local type information into the ARM64 `Program` so local vec128
  values do not share the old scalar-only slot model.
- Added helper-backed vec128 support for local/context/memory load/store,
  constant vector operands, raw vec128 copies, vector shifts, unpack, extract,
  splat, permute, swizzle, dot3/dot4, vector conversions, vector compares,
  vector add/sub, vector select, vec128 unary operations, and vec128
  mul-add/sub.
- Kept this as a correctness-first mini-JIT lane; helpers are not final NEON
  codegen.

Validation:

- Native core builds passed after each patch group.
- Full deploys passed and installed to Thor serial `c3ca0370`.
- Captures:
  - `scratch/thor-debug/20260518-112417-*`
  - `scratch/thor-debug/20260518-112859-*`
  - `scratch/thor-debug/20260518-113514-*`
  - `scratch/thor-debug/20260518-114058-*`
  - `scratch/thor-debug/20260518-114733-*`
  - `scratch/thor-debug/20260518-115022-*`
- Latest APK SHA-256:
  `D391A883C0F4EDDA23DECC2134DB3DF2FF10D8C608AD673FB7056DF05A0F7054`.

Findings:

- Fallbacks moved from vec128 slot load/store and `UNPACK`, to
  permute/swizzle/extract/dot-product, to splat/dot4/vector-convert, to
  round/vector-compare/vector-add/rsqrt/recip, to vec128 select.
- The latest 90-second capture has zero `ARM64 JIT fallback`, unsupported
  opcode/type, native crash, tombstone, or fatal-signal lines.
- The store watch still hits `826A2550@826A2598->82785548`.
- Blue Dragon still reaches later pack loading, including `battle.ipk`,
  `database.ipk`, `script.ipk`, `packmem_us.ipk`, and `battle_start.ipk`.
- Screenshot still shows only the Android shell/OSD and static
  `AArch64 JIT pending` badge. This badge is stale Java UI, not proof that
  the mini-JIT failed.

Blockers:

- Blue Dragon is not visibly at title yet.
- The current wall has moved from first-order HIR mini-JIT coverage to
  post-pack-load progress: guest CPU progress, render submission, Vulkan
  presentation, or Android OSD masking.

Next actions:

- Add runtime OSD state from native code so screenshots show real backend/JIT
  status.
- Add post-pack-load progress sampling: guest thread PCs, hot guest functions,
  command processor progress, draw submits, and swapchain presents.
- Use RenderDoc/AGI only after normal logs show first real guest render
  submission.

## 2026-05-18 11:52-13:31 EDT - Blue Dragon KTHREAD Timer And GPU Watchdog

Branch: `master`

Goal:

- Continue pushing Blue Dragon Disc 1 toward title on the AYN Thor Max after
  the ARM64 vec128 mini-JIT fallback-zero capture.
- Separate CPU/timer waits, kernel event waits, GPU ring progress, and game D3D
  watchdog failures so the loop is faster than guessing.

Commands run:

- `git status --short --branch`
- `adb devices`
- `adb -s c3ca0370 shell getprop ro.product.model`
- `adb -s c3ca0370 shell getprop ro.build.version.sdk`
- `rg -n "0x01C4|0x01C5|CP_RB|read_ptr_index_|UpdateWritePointer|InitializeRingBuffer|ReadRegister\(|WriteRegister\(" src/xenia/gpu`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragon -DeviceSerial c3ca0370 ...`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode Capture -DeviceSerial c3ca0370`
- `rg -n "Blue Dragon draw wait probe|The GPU is hung|CP_RB_RPTR|CP_RB_WPTR|Fatal signal|tombstone|ARM64 JIT fallback|unsupported opcode|unsupported .*type" scratch\thor-debug\20260518-*.txt`
- `git clone --depth 1 --filter=blob:none https://github.com/xenia-canary/xenia-canary.git scratch\upstream\xenia-canary`
- Focused PPC dumps through `-DisassembleFunctions true -DisassembleFunctionFilter "8246DBB0,8246B408,8246E6A4,82466C00"`

Files changed:

- `src/xenia/cpu/backend/arm64/arm64_function.cc`
- `src/xenia/cpu/backend/arm64/arm64_function.h`
- `src/xenia/cpu/cpu_flags.cc`
- `src/xenia/cpu/cpu_flags.h`
- `src/xenia/cpu/ppc/ppc_translator.cc`
- `src/xenia/gpu/command_processor.cc`
- `src/xenia/gpu/d3d12/d3d12_command_processor.cc`
- `src/xenia/gpu/gpu_flags.cc`
- `src/xenia/gpu/gpu_flags.h`
- `src/xenia/gpu/graphics_system.cc`
- `src/xenia/gpu/shared_memory.cc`
- `src/xenia/gpu/shared_memory.h`
- `src/xenia/gpu/vulkan/vulkan_command_processor.cc`
- `src/xenia/kernel/xboxkrnl/xboxkrnl_threading.cc`
- `android/android_studio_project/app/src/main/java/jp/xenia/emulator/EmulatorActivity.java`
- `tools/thor/thor_xenia_debug.ps1`
- `docs/research/20260518-133100-blue-dragon-kthread-gpu-watchdog.md`
- `docs/worklogs/20260518.md`
- `AGENTS.md`
- `README.md`

Implementation notes:

- Added focused guest-function disassembly filtering through
  `disassemble_function_filter`.
- Added ARM64 compiled-call sampling and a Blue Dragon draw-wait probe for guest
  function `8246B408`.
- Added kernel wait and event tracing cvars for post-pack-load waits.
- Added `arm64_update_kthread_time`, which writes current guest uptime into the
  running guest KTHREAD `+0x58` before ARM64 function dispatch.
- Mirrored command processor ring read/write pointers into MMIO registers
  `0x01C4` / `0x01C5`.
- Added Canary-inspired `clear_memory_page_state` support and ring-buffer
  zeroing on initialization.
- Added switchable research-only GPU experiments:
  `gpu_interrupt_on_ring_idle` and `gpu_blue_dragon_kick_wait_token`.

Validation:

- Full deploys passed and installed to Thor serial `c3ca0370`.
- Captures:
  - `scratch/thor-debug/20260518-123221-*`
  - `scratch/thor-debug/20260518-123609-*`
  - `scratch/thor-debug/20260518-124355-*`
  - `scratch/thor-debug/20260518-124812-*`
  - `scratch/thor-debug/20260518-125319-*`
  - `scratch/thor-debug/20260518-125759-*`
  - `scratch/thor-debug/20260518-130625-*`
  - `scratch/thor-debug/20260518-131640-*`
  - `scratch/thor-debug/20260518-131928-*`
  - `scratch/thor-debug/20260518-132520-*`
- Latest APK SHA-256:
  `C5EAA2B63D0ED10852567E7ABA9F9D78A33A748E4A835F1462D93775A01E0F40`.

Findings:

- The KTHREAD timer fix moved Blue Dragon past the earlier draw wait where
  `global_tick` stayed zero.
- After that fix, Blue Dragon enters the game's D3D watchdog and prints:
  `The GPU is hung! D3D version is 3529.0 retail, kernel is 65535, frame is 0`.
- CP ring pointer mirroring fixed the obviously stale watchdog dump:
  `CP_RB_RPTR` now matches `CP_RB_WPTR` at `0x1f/0x1f` and `0x25/0x25`.
- The watchdog still fires, so stale ring read pointer was not the only issue.
- Focused PPC dumps show the draw wait loop polls the graphics user-data object
  `4000E880`, token pointer `object + 0x2A10`, saved token at wait-state `+0x8`,
  and KTHREAD uptime.
- The Blue Dragon token-kick experiment did increment token pointer `FFCA3000`,
  but the wait state captured the new value and timed out again. Token movement
  alone is not enough.

Blockers:

- Blue Dragon still does not reach title.
- Current wall is a D3D/GPU completion or presentation contract: command ring
  pointers drain, but the game still considers frame 0 hung.
- No `XE_SWAP` / first visible frame proof appeared in the latest captures.

Next actions:

- Main lane: broaden ARM64 JIT/HIR coverage in batches, using coverage reports
  and audited AArch64 fork references, then use Thor runs as checkpoints.
- GPU lane: instrument `VdGetSystemCommandBuffer`, `VdSwap`, PM4 swap packets,
  GPU interrupt callback dispatch, and frame-close state before adding more
  hacks.
- Keep `gpu_interrupt_on_ring_idle` and `gpu_blue_dragon_kick_wait_token`
  default-off and research-only.

## 2026-05-18 13:31-13:48 EDT - ARM64 HIR Gap Batch

Branch: `master`

Goal:

- Stop guessing whether Blue Dragon is blocked by missing ARM64 HIR opcodes.
- Broaden ARM64 mini-JIT and interpreter switch coverage in one batch, then use
  Thor as the checkpoint.

Commands run:

- `git status --short --branch`
- `adb devices`
- `adb shell getprop ro.product.model`
- `adb shell getprop ro.build.version.sdk`
- `powershell -ExecutionPolicy Bypass -File tools\arm64\hir_coverage_report.ps1 -Latest 8 -OutPath scratch\arm64-coverage-20260518-after-watchdog.md`
- ARM64 HIR opcode switch audit against `opcodes.h`, `arm64_jit.cc`, and
  `arm64_function.cc`
- `powershell -ExecutionPolicy Bypass -File tools\arm64\arm64_jit_gap_report.ps1 -OutPath docs\research\20260518-134452-arm64-jit-gap-batch.md`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragon -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode Capture -DeviceSerial c3ca0370`
- `rg -n "ARM64 JIT fallback|unsupported opcode|unimplemented HIR opcode|The GPU is hung|VdSwap|XE_SWAP" scratch\thor-debug\20260518-134757-*.txt`

Files changed:

- `src/xenia/cpu/backend/arm64/arm64_jit.cc`
- `src/xenia/cpu/backend/arm64/arm64_function.cc`
- `tools/arm64/arm64_jit_gap_report.ps1`
- `docs/research/20260518-134452-arm64-jit-gap-batch.md`
- `docs/research/20260518-134832-arm64-jit-gap-device-checkpoint.md`
- `docs/worklogs/20260518.md`
- `AGENTS.md`
- `README.md`

Implementation notes:

- Added helper-backed ARM64 mini-JIT support for vector min/max, vector shifts,
  vector arithmetic shift, vector rotate-left, vector average, insert, pack,
  and atomic exchange.
- Added interpreter support for `DID_SATURATE`, insert, pack, and unpack
  `PACK_TYPE_ULONG_4202020`.
- Added `tools/arm64/arm64_jit_gap_report.ps1` to keep opcode switch coverage
  and recent Thor log signals together in a dated Markdown report.

Validation:

- Initial native-core build caught and fixed a missing `FloatFromBits` helper in
  `arm64_function.cc`.
- Native-core build passed after the fix.
- Full deploy passed and installed APK SHA-256
  `DB7EDE68FFA04C5FF6BBBB46546CD2734D098AEC00070FF91096FA5614AB1B64`.
- Blue Dragon captures:
  - `scratch/thor-debug/20260518-134558-*`
  - `scratch/thor-debug/20260518-134757-*`
- Latest gap report says all 113 HIR opcodes have ARM64 mini-JIT switch
  coverage and interpreter switch coverage.

Findings:

- The first post-batch Blue Dragon capture showed native mini-JIT compile
  activity and pack-file loading, with no immediate missing-op or native crash.
- The longer capture still hit the game's D3D watchdog at frame 0:
  `The GPU is hung! D3D version is 3529.0 retail, kernel is 65535, frame is 0`.
- Ring pointers were still drained and matching in the watchdog dump:
  `CP_RB_RPTR: 0x0000001f`, `CP_RB_WPTR: 0x0000001f`, then `0x25/0x25`.
- Guest symbol traces include `VdSwap`, but there is still no `XE_SWAP` or
  visible game frame proof.

Blockers:

- Blue Dragon still does not reach title.
- The current wall is no longer a missing ARM64 HIR switch-case surface; it is
  GPU/D3D completion, guest-visible swap/present behavior, or a deeper ARM64
  correctness/performance issue that manifests as the game's GPU watchdog.

Next actions:

- Instrument `VdSwap`, guest present/swap calls, PM4 packet submission, and
  Vulkan command processor frame-close state.
- Fix the static Java `AArch64 JIT pending` badge so screenshots reflect real
  native runtime status.
- Keep moving broad backend correctness forward, but use the GPU watchdog as
  the next main debug lane.

## 2026-05-18 13:48-14:12 EDT - Swap Trace And Broad ARM64 Conversion Audit

Branch: `master`

Goal:

- Stop debugging Blue Dragon one Thor crash at a time.
- Add explicit swap/present tracing to prove whether the game reaches guest
  `VdSwap`, PM4 `XE_SWAP`, or Vulkan `IssueSwap`.
- Build a broader x64-to-ARM64 conversion audit so missing backend families and
  infrastructure are visible before the next Thor milestone run.

Commands run:

- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragon -DeviceSerial c3ca0370 -GpuTraceSwap true`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode Capture -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragon -DeviceSerial c3ca0370 -GpuTraceSwap true -GpuInterruptOnRingIdle true`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode Capture -DeviceSerial c3ca0370`
- `rg -n "GPU swap trace|VdSwap|XE_SWAP|Vulkan IssueSwap|The GPU is hung|CP_RB_RPTR|CP_RB_WPTR" scratch\thor-debug\20260518-*.txt`
- `powershell -ExecutionPolicy Bypass -File tools\arm64\arm64_conversion_audit.ps1 -OutPath docs\research\20260518-140436-arm64-x64-conversion-audit.md -LatestLogs 10`

Files changed:

- `src/xenia/gpu/gpu_flags.cc`
- `src/xenia/gpu/gpu_flags.h`
- `src/xenia/gpu/command_processor.cc`
- `src/xenia/gpu/vulkan/vulkan_command_processor.cc`
- `src/xenia/kernel/xboxkrnl/xboxkrnl_video.cc`
- `src/xenia/cpu/backend/arm64/arm64_backend.cc`
- `android/android_studio_project/app/src/main/java/jp/xenia/emulator/EmulatorActivity.java`
- `android/android_studio_project/app/src/main/res/layout/activity_emulator.xml`
- `android/android_studio_project/app/src/main/res/values/strings.xml`
- `tools/thor/thor_xenia_debug.ps1`
- `tools/arm64/arm64_conversion_audit.ps1`
- `docs/research/20260518-140436-arm64-x64-conversion-audit.md`
- `docs/research/20260518-140712-blue-dragon-swap-trace.md`
- `docs/worklogs/20260518.md`
- `AGENTS.md`

Validation:

- Full deploy passed and installed APK SHA-256
  `F32E2E8888B9EF5CBDC7E654B1716B384F96C3D3EED5BF6181D85955C8334D85`.
- Final full deploy after the ARM64 startup log wording change passed and
  installed APK SHA-256
  `358E381CF27C20576E5B19CDF816774F1FBE6629E1640D5CED4E619B67045478`.
- Blue Dragon capture with swap tracing:
  `scratch/thor-debug/20260518-135816-*`.
- Blue Dragon capture with swap tracing plus ring-idle interrupt simulation:
  `scratch/thor-debug/20260518-140109-*`.
- The Android OSD now reports `AArch64 mini-JIT research` instead of the stale
  `AArch64 JIT pending` badge.

Findings:

- Runtime swap trace reaches engine initialization and ring setup, but no
  explicit runtime `GPU swap trace: VdSwap`, `XE_SWAP`, or Vulkan `IssueSwap`.
- Import/symbol listings mention `VdSwap`, but that is not runtime call proof.
- Both Blue Dragon runs still hit the D3D frame-0 watchdog with drained,
  matching ring pointers at `0x1f/0x1f`, then `0x25/0x25`.
- Enabling the current ring-idle interrupt experiment did not move the game to
  runtime swap/present.
- The broad conversion audit shows all 113 HIR opcodes have ARM64 mini-JIT and
  interpreter switch cases, but x64-grade backend infrastructure is still
  missing: host-to-guest thunk, guest-to-host thunk, resolve thunk, breakpoint
  support, exception/signal callback path, code-cache indirection/commit
  metadata, unwind data, and broad native lowering to replace 52 helper-call
  sites.

Blockers:

- Blue Dragon still does not reach title.
- The biggest process blocker is no longer a single missing opcode; it is the
  lack of a systematic ARM64 conversion harness and backend parity checklist.

Next actions:

- Batch backend conversion before the next Thor milestone: ABI/thunks, code
  cache metadata, debugger/exception services, scalar/control/memory native
  lowering, vector native lowering, then guest-function replay tests.
- Add compiled-vs-interpreter and x64-vs-ARM64 differential tests so Thor is a
  checkpoint instead of the only test.
- Keep `gpu_trace_swap` default-off and use it for GPU milestone captures.

## 2026-05-18 14:13-14:26 EDT - ARM64 Working Fork Reality Check

Branch: `master`

Goal:

- Answer whether any ARM64 Xenia fork actually works.
- Clarify whether to keep the helper mini-JIT scaffold or pivot directly to a
  real AArch64 emitter backbone.

Commands run:

- Web research for current Xenia ARM64 / Android / Apple forks.
- `Get-Date -Format 'yyyyMMdd-HHmmss K'`
- `git status --short --branch`

Files changed:

- `AGENTS.md`
- `docs/research/20260518-142302-arm64-xenia-working-forks.md`
- `docs/worklogs/20260518.md`

Findings:

- aX360e is the strongest Android evidence: it is public on Google Play, claims
  ARM64/Vulkan support, is based on Xenia `arm64-backend`/xenia-canary, and has
  a public GitHub repository.
- XeniOS/xenia-mac are Apple-focused evidence that ARM64 Xenia-derived code can
  reach game states, but their platform layer is Metal/iOS/macOS, not Android
  Vulkan.
- Xenia Edge is the most useful technical branch to study for A64 backend
  shape, especially Wunkolo-derived A64 backend work.
- No checked source proves a clean, drop-in Android ARM64 backend that would
  make Blue Dragon title-ready on Thor immediately.

Decision:

- Keep the helper mini-JIT as a bring-up scaffold/fallback.
- Start reshaping toward a real AArch64 emitter backbone now.
- Prioritize correctness coverage, with targeted native hot-path work to avoid
  Blue Dragon watchdogs caused only by CPU slowness.
- Study/import from aX360e, Xenia Edge, and XeniOS only with clean license and
  attribution.

## 2026-05-18 14:26-14:49 EDT - First aX360e / Xenia Edge Import Slice

Branch: `master`

Goal:

- Take the highest-leverage clean pieces from aX360e and Xenia Edge right now
  without blindly pasting a mismatched full backend.

Commands run:

- `git status --short --branch`
- `git ls-remote --heads https://github.com/aenu1/ax360e.git`
- `git ls-remote --heads https://github.com/has207/xenia-edge.git`
- `git clone --depth 1 --filter=blob:none --branch edge https://github.com/has207/xenia-edge.git scratch\upstream\xenia-edge`
- `git clone --depth 1 --filter=blob:none --branch main https://github.com/aenu1/ax360e.git scratch\upstream\ax360e`
- `git -C scratch\upstream\ax360e sparse-checkout init --no-cone`
- `git -C scratch\upstream\ax360e sparse-checkout set --stdin`
- `git -C scratch\upstream\ax360e checkout -f main`
- `git diff --check`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore -DeviceSerial c3ca0370`
- `python xenia-build premake --target_os=android`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`
- `Get-FileHash android\android_studio_project\app\build\outputs\apk\github\debug\app-github-debug.apk -Algorithm SHA256`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragon -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode Capture -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\arm64\arm64_conversion_audit.ps1 -OutPath scratch\arm64-import-slice-audit.md -LatestLogs 3`

Files changed:

- `android/android_studio_project/app/src/main/AndroidManifest.xml`
- `premake5.lua`
- `src/xenia/cpu/backend/arm64/arm64_backend.cc`
- `src/xenia/cpu/backend/arm64/arm64_backend.h`
- `docs/research/20260518-143943-arm64-upstream-import-slice.md`
- `docs/worklogs/20260518.md`
- `AGENTS.md`

Implementation notes:

- Adapted Xenia Edge's ARM64 breakpoint direction into the current Thor
  backend: patch generated ARM64 code with `BRK #0`, restore original
  instructions on uninstall, and route illegal-instruction traps to
  `Processor::OnThreadBreakpointHit`.
- Added temporary page-protection switching and instruction-cache flushing
  around ARM64 breakpoint patches because the Thor code cache may be RX outside
  Android `rwx_debug` bring-up mode.
- Imported aX360e's immediately useful Android hardening shape:
  frame pointers, unwind tables, 16 KiB max page size, and disabled native heap
  pointer tagging.

Validation:

- Native core build passed for `arm64-v8a` and `x86_64`.
- Android makefile regeneration passed.
- Full deploy passed and installed to Thor serial `c3ca0370`.
- APK SHA-256:
  `4CE19E505D48CF50B5B3BC9C3699EC39750CE0EF033F721DBFA309AAAC7DF4AB`.
- Blue Dragon Disc 1 regression capture:
  `scratch/thor-debug/20260518-144723-*`.
- No `Fatal signal`, tombstone, or native crash signature was found in that
  capture.

Findings:

- `xenia-edge` branch `edge` is easy to clone and has the full A64 backend
  source family.
- `aX360e` hits Windows long-path checkout failures if cloned whole into the
  current workspace, so sparse checkout is the right local audit mode.
- aX360e's Android side is useful, but SAF/audio/HID/AdrenoTools should be
  imported as separate, attributed batches rather than dumped into the current
  Java/native shell.
- Blue Dragon still reaches the same frame-0 D3D watchdog, with drained
  `CP_RB_RPTR/WPTR` values `0x1f/0x1f` and then `0x25/0x25`. This import did
  not move the game to title and did not introduce a new native crash.
- The follow-up conversion audit no longer lists breakpoint/exception callback
  support as a gap. The remaining big missing pieces are `HostToGuestThunk`,
  `GuestToHostThunk`, `ResolveFunctionThunk`, code-cache indirection/commit
  metadata, unwind/data placement, and 52 helper-call sites in the mini-JIT.

Next actions:

- Port Edge's backend context/thunk skeleton behind the helper mini-JIT entry
  path.
- Add code-cache indirection metadata and resolve-thunk scaffolding.
- Start replacing helper-heavy scalar/control/memory mini-JIT paths with Edge
  A64 emitter/sequences.

## 2026-05-18 14:51 EDT - Edge A64 Code Cache BRK Fill

Branch: `master`

Goal:

- Take one more safe Xenia Edge A64 cache behavior before the larger code-cache
  and thunk import.

Files changed:

- `src/xenia/cpu/backend/arm64/arm64_code_cache.cc`
- `docs/research/20260518-145131-arm64-code-cache-edge-brk-fill.md`
- `docs/worklogs/20260518.md`
- `AGENTS.md`

Implementation notes:

- Mirrored Xenia Edge's A64 habit of filling unwritten generated-code memory
  with `BRK #0` (`0xD4200000`).
- Existing placed code still overwrites and flushes its committed range; the
  new fill is a guard for padding and uncommitted cache space.

Validation:

- `git diff --check` passed with only CRLF normalization warnings.
- Native core build passed for `arm64-v8a` and `x86_64`:
  `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore -DeviceSerial c3ca0370`.
- Full deploy passed and installed commit `9be7323b8` on Thor:
  `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`.
- APK SHA-256:
  `9434C3D97859FC61C2986D764378EB0EE6E945AECCE4BDAE646E9F01A1697954`.
- Blue Dragon Disc 1 regression capture:
  `scratch/thor-debug/20260518-145425-*`.
- The latest capture shows ARM64 backend initialization and guest mini-JIT
  compilation, with no `Fatal signal`, tombstone, `SIGILL`, or native crash
  signature found by the searched log patterns.
- Blue Dragon still stops at the known D3D frame-0 watchdog with drained
  `CP_RB_RPTR/WPTR` values `0x1f/0x1f`, then `0x25/0x25`.

## 2026-05-18 15:07-15:21 EDT - ARM64 Thunk / Code Cache Scaffold

Branch: `master`

Goal:

- Add the next Edge-shaped ARM64 backend/code-cache runway: thunk slots,
  resolve helper, indirection table metadata, data placement, and commit
  tracking, while keeping the current helper mini-JIT ABI alive.

Commands run:

- `git status --short --branch`
- `rg -n "host_to_guest|guest_to_host|ResolveFunction|AddIndirection|PlaceData|CommitExecutableRange" ...`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\arm64\arm64_conversion_audit.ps1 -OutPath scratch\arm64-thunk-cache-scaffold-audit.md -LatestLogs 4`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`
- `Get-FileHash android\android_studio_project\app\build\outputs\apk\github\debug\app-github-debug.apk -Algorithm SHA256`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragon -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode Capture -DeviceSerial c3ca0370`
- `rg -n "ARM64 code cache allocated|indirection=|Fatal signal|tombstone|SIGSEGV|SIGILL|mprotect failed|The GPU is hung|CP_RB_RPTR|CP_RB_WPTR" scratch\thor-debug\20260518-152107-logcat*.txt`

Files changed:

- `src/xenia/cpu/backend/arm64/arm64_backend.cc`
- `src/xenia/cpu/backend/arm64/arm64_backend.h`
- `src/xenia/cpu/backend/arm64/arm64_code_cache.cc`
- `src/xenia/cpu/backend/arm64/arm64_code_cache.h`
- `docs/research/20260518-151702-arm64-thunk-cache-scaffold.md`
- `docs/worklogs/20260518.md`
- `AGENTS.md`

Implementation notes:

- Added ARM64 thunk type slots and getters for `HostToGuestThunk`,
  `GuestToHostThunk`, and `ResolveFunctionThunk`, matching the x64/Edge
  backend surface.
- Added a simple ARM64 `ResolveFunction` helper for future resolve-thunk use.
- Made ARM64 `CommitExecutableRange` forward into the code cache.
- Added an encoded sparse indirection table, external 64-bit target side table,
  default target encoding, `AddIndirection64`, `PlaceData`, placement overloads,
  unwind placeholder metadata, and generated-code commit tracking.
- Current helper mini-JIT guest-code placement now updates indirection metadata,
  but current execution still uses the existing helper mini-JIT ABI.

Validation:

- `git diff --check` passed with only CRLF normalization warnings.
- Native core build passed for `arm64-v8a` and `x86_64`.
- Conversion audit now reports no backend or code-cache capability-name gaps in
  the audit script, but still reports 52 helper mini-JIT call sites.
- Full deploy built and installed the APK on Thor after the final guard update.
- APK SHA-256:
  `860FA95A862220425406E1D51CBD3DDAA49EC1B35EEA846EEE87117D20E469C2`.
- Blue Dragon Disc 1 regression capture:
  `scratch/thor-debug/20260518-152107-*`.
- The capture shows ARM64 code cache startup with an indirection table address,
  ARM64 backend initialization, and guest mini-JIT compilation.
- No `Fatal signal`, tombstone, `SIGSEGV`, `SIGILL`, or indirection
  `mprotect failed` line was found in the searched log output.
- Blue Dragon still stops at the known D3D frame-0 watchdog with drained
  `CP_RB_RPTR/WPTR` values `0x1f/0x1f`, then `0x25/0x25`.

Next actions:

- Generate actual ARM64 host-to-guest, guest-to-host, and resolve thunks behind
  a cvar, using the Edge A64 thunk emitter/stack-layout shape as the reference.
- Move direct and indirect guest calls onto the indirection path in the native
  emitter path only; keep the current helper mini-JIT fallback until the ABI is
  proven.
- Start reducing the 52 helper-call sites by opcode family with differential
  tests before expecting Blue Dragon to move.

## 2026-05-18 15:22-15:28 EDT - Generated ARM64 Transition Thunks

Branch: `master`

Goal:

- Generate actual ARM64 host-to-guest, guest-to-host, and resolve thunks in the
  ARM64 backend, using the Edge A64 thunk shape as the next infrastructure
  batch while keeping the current Thor helper mini-JIT path stable.

Commands run:

- `git status --short --branch`
- `git diff -- src/xenia/cpu/backend/arm64/arm64_backend.cc`
- `rg -n "ARM64 transition thunks generated|ARM64 code cache allocated|The GPU is hung|CP_RB_RPTR|CP_RB_WPTR|Fatal signal|tombstone|SIGSEGV|SIGILL|mprotect failed" scratch\thor-debug\20260518-152749-logcat.txt scratch\thor-debug\20260518-152749-logcat-filtered.txt`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`
- `Get-FileHash android\android_studio_project\app\build\outputs\apk\github\debug\app-github-debug.apk -Algorithm SHA256`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragon -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode Capture -DeviceSerial c3ca0370`

Files changed:

- `src/xenia/cpu/backend/arm64/arm64_backend.cc`
- `docs/research/20260518-152759-arm64-generated-transition-thunks.md`
- `docs/worklogs/20260518.md`
- `AGENTS.md`

Implementation notes:

- Added a local `Arm64ThunkEmitter` using `xbyak_aarch64`.
- Generated host-to-guest, guest-to-host, and resolve-function thunks into the
  ARM64 code cache during backend initialization.
- Set the ARM64 indirection table default target to the generated resolve
  thunk.
- Committed the force-return range through the ARM64 code cache.
- Logged generated thunk addresses at startup so device captures can prove the
  thunks exist in executable memory.

Validation:

- `git diff --check` passed with only CRLF normalization warnings.
- Native core build passed for `arm64-v8a` and `x86_64`.
- Full deploy passed and installed to Thor serial `c3ca0370`.
- APK SHA-256:
  `186EA0502949D8AF4ACB5691C70563E42E0956A6006440AAC8F913CF43FB022D`.
- Blue Dragon Disc 1 regression capture:
  `scratch/thor-debug/20260518-152749-*`.
- The capture shows ARM64 code-cache allocation and generated transition-thunk
  addresses:
  `host_to_guest=0000007734805020`,
  `guest_to_host=0000007734805050`,
  `resolve=0000007734805070`.
- No `Fatal signal`, tombstone, `SIGSEGV`, `SIGILL`, or indirection
  `mprotect failed` line was found in the searched log output.

Findings:

- Generated ARM64 transition thunks work as startup infrastructure on Thor.
- Current helper mini-JIT execution is still not routed through these thunks.
- Blue Dragon still stops at the known D3D frame-0 watchdog with drained
  `CP_RB_RPTR/WPTR` values `0x1f/0x1f`, then `0x25/0x25`.

Blockers:

- The full Edge A64 transition ABI is still missing backend context,
  stackpoint/vector save details, unwind data, exception recovery, and native
  emitter call routing.
- The 52 helper-call mini-JIT surface remains the main backend correctness and
  performance backlog.

Next actions:

- Wire direct and indirect native guest calls to the indirection/resolve path
  behind a controlled backend flag.
- Start reducing helper-call opcode families with differential tests before
  expecting Blue Dragon to move toward title.

## 2026-05-18 15:36-15:44 EDT - aX360e Fixed High-Memory Layout Import

Branch: `master`

Goal:

- Stop polishing the helper mini-JIT path for this slice and import a concrete
  Android ARM64 layout idea from aX360e: fixed high guest memory plus matching
  high ARM64 code-cache and indirection windows.

Commands run:

- `git status --short --branch`
- `git -C scratch\upstream\ax360e rev-parse HEAD`
- `git diff --no-index --stat scratch\upstream\xenia-edge\src\xenia\cpu\backend\a64 scratch\upstream\ax360e\app\src\main\cpp\xenia-canary\src\xenia\cpu\backend\a64`
- `rg -n "mmap_address_high|execute_address_high|XE_PLATFORM_AX360E|adrenotools" scratch\upstream\ax360e\app\src\main\cpp`
- `git diff --check`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`
- `Get-FileHash android\android_studio_project\app\build\outputs\apk\github\debug\app-github-debug.apk -Algorithm SHA256`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragon -DeviceSerial c3ca0370 -MmapAddressHigh 8`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode Capture -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\arm64\arm64_conversion_audit.ps1 -OutPath scratch\arm64-ax360e-high-map-audit.md -LatestLogs 4`

Files changed:

- `android/android_studio_project/app/src/main/java/jp/xenia/emulator/EmulatorActivity.java`
- `src/xenia/memory.cc`
- `src/xenia/cpu/backend/arm64/arm64_code_cache.cc`
- `tools/thor/thor_xenia_debug.ps1`
- `docs/research/20260518-154350-ax360e-fixed-high-memory-layout.md`
- `docs/worklogs/20260518.md`
- `AGENTS.md`

Implementation notes:

- Added `mmap_address_high`, default `0`, so existing dynamic guest memory
  mapping remains the default.
- When set to `1..124`, guest memory first tries
  `mmap_address_high << 32` before falling back to the old search.
- ARM64 code cache now tries aX360e-shaped fixed addresses when
  `mmap_address_high` is set:
  indirection at `((mmap_address_high + 2) << 32) | 0x80000000` and generated
  code at `((mmap_address_high + 2) << 32) | 0xA0000000`.
- Added Android intent propagation and Thor script flag:
  `-MmapAddressHigh 8`.

Validation:

- `git diff --check` passed with only CRLF normalization warnings.
- Native core build passed for `arm64-v8a` and `x86_64`.
- Full deploy passed and installed to Thor serial `c3ca0370`.
- APK SHA-256:
  `28CF832C88A1F6AFB96CF5D5571A5702D55A53E9A60D2B4A81E08B965EF8A2A7`.
- Blue Dragon capture with `-MmapAddressHigh 8`:
  `scratch/thor-debug/20260518-154216-*`.
- Longer Blue Dragon capture with the same process:
  `scratch/thor-debug/20260518-154338-*`.
- Device logs prove fixed high guest memory and ARM64 code-cache placement:
  `guest=0000000800000000`,
  `code=0000000AA0000000`,
  `indirection=0000000A80000000`,
  `host_to_guest=0000000AA0000020`,
  `guest_to_host=0000000AA0000050`,
  `resolve=0000000AA0000070`.
- No native `Fatal signal`, tombstone, `SIGSEGV`, `SIGILL`, or indirection
  `mprotect failed` line was found in the searched output.

Findings:

- Thor accepts the aX360e-style fixed high mapping with `mmap_address_high=8`.
- This gives the native AArch64 emitter/import path a cleaner address-layout
  target than the previous fully dynamic cache.
- Blue Dragon still hits the known D3D frame-0 watchdog with drained
  `CP_RB_RPTR/WPTR` values `0x1f/0x1f`, then `0x25/0x25`.
- The `VdSwap` line in the latest log remains a symbol/import listing, not
  explicit runtime `GPU swap trace: VdSwap` proof.

Blockers:

- This is still infrastructure. It does not replace the helper mini-JIT with
  the real aX360e/Edge A64 emitter.
- The next import needs to consume the fixed high layout in real guest
  direct/indirect call code, guest trampolines, or broader native lowering.

Next actions:

- Import guest trampoline API/allocation from aX360e/Edge with fallback to the
  current kernel trampoline behavior.
- Start moving the native ARM64 call path to the indirection layout in a
  coherent emitter slice, not through more helper mini-JIT polishing.

## 2026-05-18 15:57-16:00 EDT - aX360e Guest Trampoline Import

Branch: `master`

Goal:

- Pull more real aX360e/Edge ARM64 backend infrastructure into this fork
  instead of continuing one-off Thor fixes. Specifically, import the guest
  trampoline API/allocation path needed by the full A64 emitter family.

Commands run:

- `git status --short --branch`
- `git fetch --depth 1 origin main` in `scratch/upstream/ax360e`
- `git fetch --depth 1 origin edge` in `scratch/upstream/xenia-edge`
- `rg -n "class Backend|GuestTrampoline|CreateGuestTrampoline|FreeGuestTrampoline|InitializeBackendContext|class Arm64Backend|CommitExecutableRange|guest_to_host" src\xenia\cpu\backend src\xenia\kernel -g "*.h" -g "*.cc"`
- `Get-Content scratch\upstream\ax360e\app\src\main\cpp\xenia-canary\src\xenia\cpu\backend\a64\a64_backend.h`
- `Get-Content scratch\upstream\ax360e\app\src\main\cpp\xenia-canary\src\xenia\cpu\backend\a64\a64_backend.cc`
- `git diff --check`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`
- `Get-FileHash -Algorithm SHA256 android\android_studio_project\app\build\outputs\apk\github\debug\app-github-debug.apk`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragon -DeviceSerial c3ca0370 -MmapAddressHigh 8`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode Capture -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\arm64\arm64_conversion_audit.ps1 -OutPath scratch\arm64-ax360e-trampoline-audit.md -LatestLogs 4`

Files changed:

- `src/xenia/base/bit_map.h`
- `src/xenia/base/bit_map.cc`
- `src/xenia/cpu/backend/backend.h`
- `src/xenia/cpu/thread_state.cc`
- `src/xenia/cpu/backend/arm64/arm64_backend.h`
- `src/xenia/cpu/backend/arm64/arm64_backend.cc`
- `docs/research/20260518-155736-ax360e-guest-trampolines.md`
- `docs/worklogs/20260518.md`
- `AGENTS.md`

Implementation notes:

- Added aX360e `BitMap::AcquireFromBack()` support for long-lived allocations.
- Added aX360e-style backend hooks for backend context lifecycle, guest
  rounding, pseudo stacktraces, and guest trampoline creation/freeing.
- Added `GuestTrampolineGroup` as a backend-level shared trampoline owner.
- Switched `ThreadState` to allocate a 256-byte backend-context prefix before
  `PPCContext`, matching aX360e's A64 backend assumption.
- Added ARM64 guest trampoline generation using the aX360e/Edge `movz/movk`
  immediate sequence plus `br x9`, instruction-cache flush, and ARM64
  `AddIndirection64`.
- This slice does not yet wire `KernelModule::GenerateTrampoline` to backend
  trampolines. That is intentionally left for a separate behavior change.

Validation:

- `git diff --check` passed with only CRLF normalization warnings.
- Native core build passed for `arm64-v8a` and `x86_64`.
- Full deploy passed and installed to Thor serial `c3ca0370`.
- APK SHA-256:
  `88B1942C34BF7CA14805617E0963C7A75415381EDD78A48828838709A30A747E`.
- Blue Dragon capture with `-MmapAddressHigh 8`:
  `scratch/thor-debug/20260518-155837-*`.
- Device log proves fixed high memory, fixed high code cache, generated
  transition thunks, and the new executable trampoline pool:
  `trampolines=000000775BD04000-000000775BF24000`.
- No native `Fatal signal`, tombstone, `SIGSEGV`, or `SIGILL` line was found
  in the searched capture.

Findings:

- The aX360e/Edge guest trampoline infrastructure can initialize on Thor inside
  our current `arm64` backend.
- Blue Dragon still stops at the known D3D frame-0 watchdog:
  `The GPU is hung! D3D version is 3529.0 retail, kernel is 65535, frame is 0`.
- The latest ARM64 conversion audit still reports 52 mini-JIT helper symbols
  and 52 helper call sites. The helper-heavy surface remains the big backlog.

Blockers:

- The imported trampoline machinery is not yet consumed by kernel/runtime
  callback paths.
- The full Edge/aX360e A64 emitter ABI is still missing backend context use,
  native direct/indirect call routing, stackpoint/vector save details, and
  broader native opcode lowering.

Next actions:

- Wire backend guest trampolines into one kernel/runtime callback path with
  clear Thor proof, or start importing the full A64 emitter backbone in
  buildable opcode families.
- Keep using `-MmapAddressHigh 8` for aX360e-layout Thor runs until proven
  harmful.

## 2026-05-18 16:08-16:12 EDT - aX360e ARM64 Backend Context Scaffold

Branch: `master`

Goal:

- Continue pulling buildable aX360e A64 backend pieces. After adding the
  256-byte context prefix and guest trampoline API, add the ARM64 backend
  context data that the real A64 emitter expects.

Commands run:

- `rg -n "GetGuestTickCountPointer|XE_ARCH_ARM64|WriteStatusReg|fpcr" src\xenia\base src\xenia\cpu\backend\arm64 scratch\upstream\ax360e\app\src\main\cpp\xenia-canary\src\xenia\cpu\backend\a64 -g "*.h" -g "*.cc"`
- `git diff --check`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`
- `Get-FileHash -Algorithm SHA256 android\android_studio_project\app\build\outputs\apk\github\debug\app-github-debug.apk`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragon -DeviceSerial c3ca0370 -MmapAddressHigh 8`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode Capture -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\arm64\arm64_conversion_audit.ps1 -OutPath scratch\arm64-ax360e-backend-context-audit.md -LatestLogs 4`

Files changed:

- `src/xenia/base/clock.h`
- `src/xenia/base/clock.cc`
- `src/xenia/cpu/backend/arm64/arm64_backend.h`
- `src/xenia/cpu/backend/arm64/arm64_backend.cc`
- `docs/research/20260518-160807-ax360e-arm64-backend-context.md`
- `docs/worklogs/20260518.md`
- `AGENTS.md`

Implementation notes:

- Added `Clock::GetGuestTickCountPointer()` for future native A64 code that
  wants a direct guest tick pointer.
- Added `Arm64ReserveHelper`, `Arm64BackendStackpoint`, and
  `Arm64BackendContext`, constrained to the 256-byte prefix before
  `PPCContext`.
- Implemented ARM64 backend context initialization, deinitialization, reentry
  preparation, and `SetGuestRoundingMode`.
- ARM64 FPCR writes are guarded behind `XE_ARCH_ARM64`; x86_64 Android builds
  still compile and update PPC `fpscr` state without emitting ARM64 assembly.

Validation:

- `git diff --check` passed with only CRLF normalization warnings.
- Native core build passed for `arm64-v8a` and `x86_64`.
- Full deploy passed and installed to Thor serial `c3ca0370`.
- APK SHA-256:
  `D7349937301314D7C7E96F2E7A613682271B78380F5F58AC9A35C5D928649787`.
- Blue Dragon captures with `-MmapAddressHigh 8`:
  `scratch/thor-debug/20260518-160908-*` and
  `scratch/thor-debug/20260518-160947-*`.
- Device log still shows fixed high guest memory, fixed high code cache, and
  executable trampoline pool:
  `trampolines=000000775A7BF000-000000775A9DF000`.
- No native `Fatal signal`, tombstone, `SIGSEGV`, or `SIGILL` line was found
  in the searched capture.

Findings:

- The aX360e-style ARM64 backend context can initialize on Thor without a new
  native startup crash.
- Blue Dragon still hits the same D3D frame-0 watchdog, so this is emitter
  groundwork rather than game progress.

Blockers:

- Host-to-guest and guest-to-host thunks still do not use `x19` as backend
  context like the full Edge/aX360e A64 ABI.
- The helper-heavy mini-JIT still has 52 helper symbols/call sites.

Next actions:

- Import the full A64 transition ABI only together with the native function
  prolog/epilog expectations that make `x19` backend context safe.
- Start landing Edge/aX360e native emitter families rather than adding more
  helper-only opcode cases.

## 2026-05-18 16:14-16:45 EDT - Hard Swap To aX360e A64 Backend

Branch: `master`

Goal:

- Stop iterating on the old helper-backed ARM64 mini-JIT scaffold and replace
  the active Android ARM64 CPU path with the donor aX360e/Edge-style A64
  backend family.

Commands run:

- `git status --short --branch`
- Internet check for public aX360e project/repo status.
- `python xenia-build premake --target_os=android`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragon -DeviceSerial c3ca0370 -MmapAddressHigh 8`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode Capture -DeviceSerial c3ca0370`
- `adb -s c3ca0370 shell "top -b -n 1 | grep jp.xenia"`
- `adb -s c3ca0370 shell "ps -T -p 2338 | head -80"`

Files changed:

- `src/xenia/cpu/backend/a64/*`
- `src/xenia/cpu/backend/arm64/*`
- `src/xenia/cpu/backend/code_cache_base.h`
- `src/xenia/base/platform.h`
- `src/xenia/base/platform_arm64.h`
- `src/xenia/base/platform_arm64.cc`
- `src/xenia/cpu/hir/opcodes.h`
- `src/xenia/cpu/hir/opcodes.inl`
- `src/xenia/cpu/hir/instr.h`
- `src/xenia/cpu/hir/instr.cc`
- `android/android_studio_project/app/src/main/java/jp/xenia/emulator/EmulatorActivity.java`
- `android/android_studio_project/app/src/main/res/layout/activity_emulator.xml`
- `android/android_studio_project/app/src/main/res/values/strings.xml`
- `docs/research/20260518-164150-ax360e-a64-hard-swap.md`
- `docs/worklogs/20260518.md`
- `AGENTS.md`
- `README.md`

Implementation notes:

- Removed the active old `arm64` mini-JIT source files and replaced the project
  with donor `a64` sources copied from the local aX360e checkout.
- Kept `arm64_backend.h` as a wrapper alias so the rest of this fork can still
  refer to `arm64::Arm64Backend`.
- Imported donor `CodeCacheBase` and ARM64 platform feature helpers.
- Added compatibility shims for this fork's logging API, cvar type set,
  Capstone ARM64 naming, HIR helper methods/opcodes, C++17 bit reinterpretation,
  POSIX unwind placement, and the missing donor XexModule info-cache MMIO path.
- Updated the Android OSD label from the stale mini-JIT wording to
  `aX360e A64 backend research`, and stopped forwarding removed mini-JIT debug
  cvars from `EmulatorActivity`.
- Added A64 `OPCODE_MUL_HI` I8/I16/I32 sequences to match the donor I64
  sequence and this fork's HIR signatures.

Validation:

- `python xenia-build premake --target_os=android` passed.
- First `NativeCore` run found donor integration breaks in Capstone naming and
  POSIX unwind placement.
- Second `NativeCore` run built `arm64-v8a` and found an x86_64 compile issue
  in `platform_arm64.cc`.
- Final `NativeCore` run passed for Android `arm64-v8a` and `x86_64`.
- `FullDeploy` passed and installed to Thor serial `c3ca0370`.
- Blue Dragon Disc 1 launched from:
  `/storage/2664-21DE/roms/xbox360/Blue Dragon.m3u/Blue Dragon (USA, Europe) (En,Fr) (Disc 1).iso`.
- Runtime sequence-gap captures:
  - `scratch/thor-debug/20260518-164610-*`: Capstone ARM64 disassembly startup
    was fatal.
  - `scratch/thor-debug/20260518-164734-*`: next gap was `is_true`.
  - `scratch/thor-debug/20260518-165135-*`: next gap was `dot_product_4`.
  - `scratch/thor-debug/20260518-165422-*`: next gap was `mul_hi`.
  - `scratch/thor-debug/20260518-165821-*`: no new missing-sequence crash;
    game boot continued into asset loads.
  - `scratch/thor-debug/20260518-165930-*`: process still alive after a longer
    wait; screenshot still black with Android OSD.
- Latest APK SHA-256:
  `A4F3C0B036352CD0894E89AF26B037F31CBD2BD5F274C07C9B5B2D778BF6009E`.

Findings:

- This is a much faster development lane than the one-operation mini-JIT loop:
  the repo now builds the real donor A64 backend surface instead of a helper
  scaffold.
- The hard swap now gets past the earlier missing-sequence crash chain and into
  live Blue Dragon execution: `default.xex` loads, guest XThreads start, Vulkan
  creates a 1920x970 swapchain, and the game opens many `D:\pack\*.iph` /
  `D:\pack\*.ipk` assets.
- The current blocker is no longer "A64 backend crashes instantly"; it is
  black-screen / draw-thread progress. The process stays alive, CPU is about
  118%, and `ps -T` shows `Draw Thread` runnable.

Blockers:

- The donor XexModule info-cache path is not imported; recorded-MMIO handling
  is disabled for now.
- No title-screen proof yet. Latest screenshots show only the app OSD over a
  black frame.
- Need to prove whether Blue Dragon reaches `VdSwap`, `PM4_XE_SWAP`, Vulkan
  `IssueSwap`, or a draw-wait loop after the pack-file load burst.

Next actions:

- Relaunch with `gpu_trace_swap=true` and wait/draw tracing enabled.
- If swap traces appear, move to RenderDoc/AGI capture.
- If swap traces do not appear, instrument the draw thread, GPU ring pointers,
  and the Blue Dragon frame-completion wait path before making more GPU hacks.

## 2026-05-18 17:53 - Blue Dragon PM4 Alias Fix And Next Crash

Scope:

- Continue the Thor Max Blue Dragon Disc 1 bring-up on `master`.
- Keep this clearly research-only: this fork is not official Xenia, not support
  material for upstream, and not a compatibility claim.

Implementation:

- Added A64 compiled-function entry tracing with filters, budget controls, guest
  thread filters, and delayed tracing.
- Added a Blue Dragon draw-wait probe that updates `KTHREAD+0x58` at the known
  draw-thread wait timeout load site `8246B474`.
- Added launch forwarding for inline MMIO, GPU packet trace budgets, A64 trace
  options, draw-wait probing, and ARM64 guest store watches.
- Added PM4 packet tracing around ring-buffer execution and swap-related video
  exports.
- Added ARM64 guest store watches that can watch exact guest addresses or ranges
  and log raw/little-endian plus guest-visible big-endian values.
- Fixed Android POSIX file mappings to use `MAP_SHARED` and return `nullptr` on
  `MAP_FAILED`.
- Fixed POSIX guest heap commit to `mprotect` the existing shared guest memory
  views instead of remapping them anonymously with `AllocFixed`.
- Added defensive exception logging for unmapped A64 code-cache thunk crashes and
  PM4 `EVENT_WRITE_SHD` / `WAIT_REG_MEM` payloads for the next device run.

Validation:

- `tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370` passed
  after the physical alias fix and installed on Thor.
- `scratch/thor-debug/20260518-173814-*` proved the guest CPU wrote real PM4
  dwords to virtual ring alias `BFC50000` while the GPU command processor still
  read zeros from physical alias `1FC50000`.
- `scratch/thor-debug/20260518-174358-*` proved the alias fix worked: the GPU
  command processor read real packets including `PM4_ME_INIT`,
  `PM4_INDIRECT_BUFFER`, `PM4_EVENT_WRITE_SHD`, `PM4_DRAW_INDX_2`, and
  `PM4_INVALIDATE_STATE`.
- `scratch/thor-debug/20260518-174624-*` with the old interrupt/token nudges
  disabled processed real packets and then hit Blue Dragon's draw-thread GPU
  hang debug path at guest `8247A4C8`.
- `scratch/thor-debug/20260518-174806-*` with a token-area store watch showed:
  `FFCA3000 = 00000001`, `FFCA3004 = BF64FFFC`, and
  `VdSetSystemCommandBufferGpuIdentifierAddress(FFCA3008)`. That run reached a
  later A64 generated-code exception path on the Audio Worker.

Findings:

- The biggest blocker was not another missing A64 operation. Android heap commit
  was breaking the Xbox physical/virtual memory alias model, so the GPU thread
  was executing a zeroed command buffer.
- With aliases fixed, Blue Dragon now submits real PM4 and reaches draw packets.
- The older `gpu_interrupt_on_ring_idle` / `gpu_blue_dragon_kick_wait_token`
  hacks are no longer a good default because real ring traffic exists now and
  forced interrupts can trigger Blue Dragon's own D3D debugbreak path.
- The current walls are now higher quality: GPU completion/writeback semantics
  and an A64 generated-code thunk/branch failure, not empty-ring bring-up.

Next actions:

- Rebuild with the new `EVENT_WRITE_SHD`, `WAIT_REG_MEM`, and A64 thunk-crash
  logs.
- Confirm exactly which PM4 event writes target the Blue Dragon token/writeback
  area.
- If the A64 thunk crash is a failed `ResolveFunction`, log the guest target and
  fix the branch/indirection path instead of adding another title hack.

## 2026-05-18 18:00-18:14 EDT - Blue Dragon Watchdog Retest And Repo Skills

Scope:

- Continue Blue Dragon Disc 1 on Thor Max after the Android physical alias and
  POSIX code-cache commit fixes.
- Create repo-only skills to make the next debugging loop less one-crash-at-a-time.

Commands:

- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragon -DeviceSerial c3ca0370 -EmitInlineMmioChecks true -Arm64BlueDragonDrawWaitProbe true -GpuTraceSwap true -GpuTracePacketBudget 180 -GpuInterruptOnRingIdle false -GpuBlueDragonKickWaitToken false -Arm64GuestStoreWatch FFCA3000-FFCA3020 -Arm64GuestStoreWatchBudget 120`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode Capture -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragon -DeviceSerial c3ca0370 -EmitInlineMmioChecks true -Arm64BlueDragonDrawWaitProbe true -GpuTraceSwap true -GpuTracePacketBudget 260 -GpuEarlyPrimaryReadPointerWriteback true -GpuInterruptOnRingIdle false -GpuBlueDragonKickWaitToken false -XboxkrnlIgnoreGuestDebugBreakpoints true -Arm64GuestStoreWatch FFCA3000-FFCA3020 -Arm64GuestStoreWatchBudget 120`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode Capture -DeviceSerial c3ca0370`
- `python C:\Users\leanerdesigner\.codex\skills\.system\skill-creator\scripts\quick_validate.py .agents\skills\xenia-thor-war-room`
- `python C:\Users\leanerdesigner\.codex\skills\.system\skill-creator\scripts\quick_validate.py .agents\skills\xbox360-xenon-ppc-architect`
- `python C:\Users\leanerdesigner\.codex\skills\.system\skill-creator\scripts\quick_validate.py .agents\skills\aarch64-snapdragon-jit-port`
- `python C:\Users\leanerdesigner\.codex\skills\.system\skill-creator\scripts\quick_validate.py .agents\skills\xenia-vulkan-adreno-renderdoc`

Files changed:

- `.agents/skills/xenia-thor-war-room/SKILL.md`
- `.agents/skills/xbox360-xenon-ppc-architect/SKILL.md`
- `.agents/skills/aarch64-snapdragon-jit-port/SKILL.md`
- `.agents/skills/xenia-vulkan-adreno-renderdoc/SKILL.md`
- `AGENTS.md`
- `docs/worklogs/20260518.md`
- Native/Android debug files already in the current dirty set:
  `src/xenia/gpu/command_processor.*`, `src/xenia/gpu/gpu_flags.*`,
  `src/xenia/kernel/xboxkrnl/xboxkrnl_debug.cc`,
  `android/android_studio_project/app/src/main/java/jp/xenia/emulator/EmulatorActivity.java`,
  `tools/thor/thor_xenia_debug.ps1`, and the A64/memory instrumentation files
  from the prior slice.

Validation:

- `FullDeploy` passed twice and installed on Thor.
- `scratch/thor-debug/20260518-180029-*`: code-cache zero-instruction storm is
  gone. Blue Dragon reaches real PM4, texture creation/loading, two runtime
  `GPU swap trace: VdSwap` calls, then hits the D3D watchdog and SIGTRAP.
- `scratch/thor-debug/20260518-180640-*`: with
  `GpuEarlyPrimaryReadPointerWriteback=true`, early primary read-pointer
  writeback logs fire before indirect buffers and guest-visible ring progress is
  better, but Blue Dragon still hits the same D3D watchdog after two `VdSwap`
  calls. APK SHA-256 was
  `ACEDE51483336CDEC53A237CCB6CCB72A0C0F54F2FC486CA6CE717758325EB60`.
- All four repo-only skill folders passed `quick_validate.py`.

Findings:

- The POSIX code-cache commit fix worked: the old unmapped code-cache
  zero-instruction crash did not recur.
- The current wall is higher quality and clearly GPU/watchdog related:
  real `DRAW_INDX_2`, `EVENT_WRITE_SHD`, `COND_WRITE`, memory waits, texture
  traffic, and runtime `VdSwap` happen before the watchdog.
- The early read-pointer experiment alone is not enough to satisfy Blue
  Dragon's D3D hang detector.
- `XboxkrnlIgnoreGuestDebugBreakpoints=true` did not suppress the final
  SIGTRAP, so the trap is likely emitted through the PPC/HIR/A64 debugbreak
  path or `break_on_debugbreak`, not only the `DbgBreakPoint` export.
- Repo-only skills now define the planned collaboration shape:
  war-room coordinator plus Xenon PPC, AArch64 Snapdragon JIT, and
  Vulkan/Adreno/RenderDoc specialists.

Blockers:

- No visible title-screen output yet; screenshots still return to the Thor
  launcher after the fatal SIGTRAP.
- Need to prove whether the generated `VdSwap` command buffer is actually
  executed as `PM4_XE_SWAP`, and if not, why the ring path skips or delays it.
- Need to decide whether to route guest debugbreak suppression through
  `break_on_debugbreak=false` for research runs or fix an A64 trap handler gap.

Next actions:

- Use `.agents/skills/xenia-thor-war-room/SKILL.md` as the default next-loop
  coordinator.
- In the GPU specialist lane, trace the exact primary ring dwords around the
  `VdSwap` buffers `BF65BBA0` and `BF69635C` and prove whether the command
  processor reaches `PM4_XE_SWAP`.
- In the A64 specialist lane, add/forward a `break_on_debugbreak=false` launch
  option or catch the A64 debugbreak trap only for documented research runs.
- Commit and push this validated slice on `master`.

## 2026-05-18 18:14-18:31 EDT - A64 Trap Parity And First Stable Blue Dragon Swaps

Scope:

- Stop Blue Dragon's guest watchdog/debugbreak path from killing the Android
  process during research runs.
- Prove whether the generated `VdSwap` command buffers are actually executed by
  the Vulkan command processor on Thor Max.

Commands:

- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragon -DeviceSerial c3ca0370 -EmitInlineMmioChecks true -Arm64BlueDragonDrawWaitProbe true -GpuTraceSwap true -GpuTracePacketBudget 512 -GpuEarlyPrimaryReadPointerWriteback true -GpuInterruptOnRingIdle false -GpuBlueDragonKickWaitToken false -BreakOnDebugbreak false -XboxkrnlIgnoreGuestDebugBreakpoints true -Arm64GuestStoreWatch FFCA3000-FFCA3020 -Arm64GuestStoreWatchBudget 120`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode Capture -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragon -DeviceSerial c3ca0370 -GpuEarlyPrimaryReadPointerWriteback true -GpuInterruptOnRingIdle false -GpuBlueDragonKickWaitToken false -BreakOnDebugbreak false -XboxkrnlIgnoreGuestDebugBreakpoints true`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode Capture -DeviceSerial c3ca0370`
- `adb -s c3ca0370 shell input keyevent 108`
- `adb -s c3ca0370 shell input keyevent 96`

Files changed:

- `src/xenia/cpu/backend/a64/a64_emitter.cc`
- `src/xenia/gpu/command_processor.cc`
- `src/xenia/kernel/xboxkrnl/xboxkrnl_video.cc`
- `android/android_studio_project/app/src/main/java/jp/xenia/emulator/EmulatorActivity.java`
- `tools/thor/thor_xenia_debug.ps1`
- `docs/worklogs/20260518.md`

Validation:

- `git diff --check` passed with only normal CRLF warnings.
- `FullDeploy` passed and installed on Thor. APK SHA-256 for the tested build
  was `FC4B01BBFABA6A6FAE8014326CA46EA92B5A0D69C2FFA23D99AB7019FCEA1DFB`.
- `scratch/thor-debug/20260518-182431-*` with heavy swap tracing reached many
  `VdSwap`, `XE_SWAP`, `Vulkan IssueSwap begin/end`, and
  `Vulkan guest output callback submitted` lines instead of dying in SIGTRAP.
- `scratch/thor-debug/20260518-182700-*` with trace mostly disabled kept PID
  `13527` alive for the full 90-second capture and continued to emit frequent
  `XE_SWAP` and XMA audio activity.
- `scratch/thor-debug/20260518-182904-*` after A/Start input still showed a
  live process and ongoing swap/audio activity, but the Android screenshot
  remained black behind the Java OSD.

Findings:

- A64 was handling every guest `TRAP` as a raw `brk`. Matching the x64 behavior
  for debug-print and debug-break trap classes lets `break_on_debugbreak=false`
  suppress the fatal guest watchdog break during research runs.
- Blue Dragon now reaches a stable present loop on Thor: the generated VdSwap
  buffers are executed as `PM4_XE_SWAP`, Vulkan `IssueSwap` completes, and the
  guest-output callback submits written 1280x720 frames.
- The current wall is no longer "crashes before swap." It is now "guest frames
  are submitted but the Android capture remains black," which belongs to either
  guest frontbuffer contents, video/FMVs, or Android/Vulkan presenter painting.

Next actions:

- Add a narrow frontbuffer/presenter checksum or capture probe so we can tell
  whether the guest output image itself is black or whether the Android
  swapchain/presenter path is failing to display nonblack frames.
- Reduce noisy XMA/swap logs for normal bring-up runs and keep the heavy trace
  flags opt-in.

## 2026-05-18 18:31-18:37 EDT - Blue Dragon Frontbuffer Checksum Probe

Scope:

- Add an opt-in Android/ADB flag to sample the physical frontbuffer passed to
  `PM4_XE_SWAP`.
- Determine whether the black Thor screenshot is caused by Android/Vulkan
  presentation or by the guest providing black/empty output buffers.

Commands:

- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragon -DeviceSerial c3ca0370 -GpuTraceSwapFrontbufferChecksum true -GpuTraceSwapFrontbufferChecksumBudget 16 -GpuEarlyPrimaryReadPointerWriteback true -GpuInterruptOnRingIdle false -GpuBlueDragonKickWaitToken false -BreakOnDebugbreak false -XboxkrnlIgnoreGuestDebugBreakpoints true`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragon -DeviceSerial c3ca0370 -GpuTraceSwapFrontbufferChecksum true -GpuTraceSwapFrontbufferChecksumBudget '-1' -GpuEarlyPrimaryReadPointerWriteback true -GpuInterruptOnRingIdle false -GpuBlueDragonKickWaitToken false -BreakOnDebugbreak false -XboxkrnlIgnoreGuestDebugBreakpoints true`

Files changed:

- `src/xenia/gpu/command_processor.cc`
- `src/xenia/gpu/gpu_flags.cc`
- `src/xenia/gpu/gpu_flags.h`
- `android/android_studio_project/app/src/main/java/jp/xenia/emulator/EmulatorActivity.java`
- `tools/thor/thor_xenia_debug.ps1`
- `docs/worklogs/20260518.md`

Validation:

- `git diff --check` passed with only normal CRLF warnings.
- `FullDeploy` passed and installed on Thor. APK SHA-256 for the tested build
  was `499554FBA91EAF3B311A6486526EE3225487052A4EB40CBCF1D3BCCFF50F7010`.
- `scratch/thor-debug/20260518-183344-*` captured the limited checksum run.
- `scratch/thor-debug/20260518-183534-*` captured the unlimited checksum run.

Findings:

- Blue Dragon repeatedly swaps between physical frontbuffers `1CA1C000` and
  `1CDB4000` at `1280x720`.
- Every sampled frontbuffer line reported `nonzero=0` with the same all-zero
  checksum, while the process stayed alive and continued `VdSwap`, `XE_SWAP`,
  `Vulkan IssueSwap`, texture-load, and XMA audio activity.
- The current wall is upstream of Android presentation: the guest-visible
  frontbuffers submitted to swap are still empty. Next debugging should target
  Xenos render-target resolve/copy/writeback into those frontbuffer ranges.

Next actions:

- Add opt-in resolve/copy destination tracing around render target cache and
  packet paths, with specific focus on writes or uploads targeting the
  `1CA1C000` and `1CDB4000` frontbuffer ranges.
- Keep the checksum probe available for confirming when a future change
  finally produces nonzero guest output.

## 2026-05-18 19:07-19:23 EDT - Vulkan Presenter Split Probe

Scope:

- Fix one suspicious Vulkan shared-memory barrier for compute writes.
- Add default-off Thor research knobs to separate APK/UI, presenter, swap
  texture, and render-target resolve problems.
- Re-run Blue Dragon Disc 1 on AYN Thor Max and classify the black screen.

Files changed:

- `src/xenia/gpu/vulkan/vulkan_shared_memory.cc`
- `src/xenia/gpu/vulkan/vulkan_command_processor.cc`
- `src/xenia/gpu/vulkan/vulkan_command_processor.h`
- `src/xenia/gpu/vulkan/vulkan_render_target_cache.cc`
- `src/xenia/gpu/gpu_flags.cc`
- `src/xenia/gpu/gpu_flags.h`
- `android/android_studio_project/app/src/main/java/jp/xenia/emulator/EmulatorActivity.java`
- `tools/thor/thor_xenia_debug.ps1`
- `docs/research/20260518-192311-blue-dragon-presenter-split.md`

Commands:

- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragon -DeviceSerial c3ca0370 -VulkanPresentRecentResolveOnSwap true -VulkanTraceCopyState true -VulkanTraceCopyStateBudget 260 -VulkanTraceResolve true -VulkanTraceResolveBudget 260 -VulkanTraceResolveChecksum true -VulkanTraceResolveChecksumBudget 64 -VulkanTraceSwapSharedMemoryChecksum true -VulkanTraceSwapSharedMemoryChecksumBudget 24 -GpuTraceSwap true -GpuTracePacketBudget 220 -GpuEarlyPrimaryReadPointerWriteback true -GpuInterruptOnRingIdle false -GpuBlueDragonKickWaitToken false -BreakOnDebugbreak false -XboxkrnlIgnoreGuestDebugBreakpoints true`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragon -DeviceSerial c3ca0370 -VulkanDebugSolidGuestOutput true -GpuTraceSwap true -GpuTracePacketBudget 120 -GpuEarlyPrimaryReadPointerWriteback true -GpuInterruptOnRingIdle false -GpuBlueDragonKickWaitToken false -BreakOnDebugbreak false -XboxkrnlIgnoreGuestDebugBreakpoints true`

Validation:

- `git diff --check` passed with only normal CRLF warnings.
- `FullDeploy` passed and installed on Thor.
- APK SHA-256 for the resolve-present build was
  `39ABA8565FB3D06627274BC2C93A067B6367D9ECE556E5DCF3B976B53D20266D`.
- APK SHA-256 for the solid-output proof build was
  `96CCADF7FBDE0A99A7E492BB02F9A9D86C1B356CCDB8A40F699C0035AE96FBD3`.
- Captures:
  - `scratch/thor-debug/20260518-190743-*`
  - `scratch/thor-debug/20260518-191850-*`
  - `scratch/thor-debug/20260518-192246-*`

Findings:

- The shared-memory compute-write barrier now uses
  `VK_ACCESS_SHADER_WRITE_BIT`. This is a real correctness fix, but it did not
  by itself make the Blue Dragon frontbuffers nonzero.
- The default-off `vulkan_present_recent_resolve_on_swap` probe successfully
  made `IssueSwap` use recent full-size resolve candidate `1DC14000+00398000`
  as format `7` instead of the empty frontbuffers `1CA1C000` / `1CDB4000`.
- The resolve candidate override still produced a black screenshot, so the
  first candidate is not useful visible output or is being decoded as black.
- The default-off `vulkan_debug_solid_guest_output` probe rendered bright
  magenta through the same Blue Dragon `RefreshGuestOutput` path. The Android
  surface, Vulkan swapchain, and presenter are alive.

Next actions:

- Stop blaming Android UI/presentation for the Blue Dragon black screen.
- Add a better resolve-candidate classifier before presenting candidates:
  sample real color entropy, skip all-zero and clear-looking resolves, and log
  candidate format/base/pitch/clear-state together.
- Inspect why the game presents `1CA1C000` / `1CDB4000` while useful-looking
  render-target resolve traffic goes elsewhere, especially whether a missing
  resolve, copy, or guest texture-address translation step should populate the
  actual VdSwap frontbuffers.

## 2026-05-18 19:26 EDT - Post-Commit Blue Dragon Swap Checksum Baseline

Scope:

- Re-run the pushed presenter-split build with heavier checksum tracing to
  capture a clean `master` baseline after commit `8cc760e0c`.
- Confirm whether the game ever writes nonzero data into the actual VdSwap
  frontbuffers after the Vulkan shared-memory barrier fix and presenter probes.

Command:

- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragon -DeviceSerial c3ca0370 -VulkanTraceCopyState true -VulkanTraceCopyStateBudget 500 -VulkanTraceResolve true -VulkanTraceResolveBudget 500 -VulkanTraceResolveChecksum true -VulkanTraceResolveChecksumBudget '-1' -VulkanTraceSwapSharedMemoryChecksum true -VulkanTraceSwapSharedMemoryChecksumBudget 32 -GpuTraceSwap true -GpuTracePacketBudget 160 -GpuEarlyPrimaryReadPointerWriteback true -GpuInterruptOnRingIdle false -GpuBlueDragonKickWaitToken false -BreakOnDebugbreak false -XboxkrnlIgnoreGuestDebugBreakpoints true`

Validation:

- Capture: `scratch/thor-debug/20260518-192604-*`.
- Branch/head: `master` at `8cc760e0c`.
- APK SHA-256:
  `96CCADF7FBDE0A99A7E492BB02F9A9D86C1B356CCDB8A40F699C0035AE96FBD3`.
- Target:
  `/storage/2664-21DE/roms/xbox360/Blue Dragon.m3u/Blue Dragon (USA, Europe) (En,Fr) (Disc 1).iso`.

Findings:

- Android focus stayed on `EmulatorActivity`; the process stayed alive as PID
  `14738`.
- The actual VdSwap frontbuffers `1CA1C000` and `1CDB4000` stayed all-zero at
  every sampled swap.
- Full-size `k_24_8` depth-like buffers at `1D87C000` and `1D88F000` were
  nonzero with `first_nonzero_value=00FFFFFF`, so the guest/render loop is not
  completely idle.
- The repeating full-size color candidates such as `1DC14000` are
  clear-looking `k_2_10_10_10` traffic rather than useful visible title output.
- XMA audio context activity continued throughout the capture, which suggests
  the current wall may be an intro/video/loading/render-output path rather than
  a simple crash-before-title failure.

Next actions:

- Run a low-noise longer session with gamepad-equivalent A/Start inputs and no
  checksum flood to see whether Blue Dragon advances past the black intro path.
- If it remains black, instrument draw/render-target state rather than the
  Android presenter: color write masks, active color RTs, clear values, and copy
  or resolve destinations immediately before each `PM4_XE_SWAP`.

## 2026-05-18 19:46-19:50 EDT - Nop HID Pulse Proof and Adreno Format Triage

Scope:

- Add a research-only connected controller mode to Android `hid=nop`.
- Add delayed/held nop button pulses so Thor runs can fire Start/A after the
  guest reaches later startup timing.
- Re-run Blue Dragon Disc 1 with the delayed pulse and classify whether the
  black screen is an input problem.

Files changed:

- `src/xenia/hid/hid_flags.cc`
- `src/xenia/hid/hid_flags.h`
- `src/xenia/hid/nop/nop_input_driver.cc`
- `src/xenia/hid/nop/nop_input_driver.h`
- `src/xenia/app/xenia_main.cc`
- `android/android_studio_project/app/src/main/java/jp/xenia/emulator/EmulatorActivity.java`
- `tools/thor/thor_xenia_debug.ps1`

Commands:

- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragon -DeviceSerial c3ca0370 -HidNopConnected true -HidNopButtons 'start,a' -HidNopButtonsDelayMs 45000 -HidNopButtonsHoldMs 3000 -GpuEarlyPrimaryReadPointerWriteback true -GpuInterruptOnRingIdle false -GpuBlueDragonKickWaitToken false -BreakOnDebugbreak false -XboxkrnlIgnoreGuestDebugBreakpoints true`

Validation:

- `git diff --check` passed with only normal CRLF warnings.
- `NativeCore` passed.
- `FullDeploy` passed and installed on Thor.
- APK SHA-256:
  `99D4A0438D59C1E8474E111B272A580C3D4B2BA4FE253458B83D1A9F45FDB083`.
- Captures:
  - `scratch/thor-debug/20260518-194653-*`
  - `scratch/thor-debug/20260518-194736-*`

Findings:

- The explicit `hid=nop` path was skipping `InputDriver::Setup()`. Calling
  `Setup()` there plus polling-path logging now proves the research controller
  launch cvars are active.
- Short capture proof line:
  `Nop HID research controller active via setup: user=0 connected=true buttons='start,a' delay_ms=45000 hold_ms=3000`.
- The 85 second delayed-pulse run stayed alive and did not crash:
  `VdSwap=285`, `XE_SWAP=285`, `FATAL=0`, `AndroidRuntime=0`, `GPU is hung=0`.
- The 85 second screenshot is still black apart from Android/system/OSD UI:
  sampled center `0/1296` nonblack with midpoint `0,0,0`.
- Because simulated Start/A was proven active and did not visibly advance the
  screen, the near-term wall is no longer basic Android input wiring.
- The retained log repeatedly reports Adreno texture format pressure, including
  `k_2_10_10_10 (signed)` being unsupported as its preferred Vulkan format.

Next actions:

- Add a research fallback for Adreno devices where signed
  `A2B10G10R10_SNORM` texture sampling is unavailable.
- Re-run Blue Dragon with the same delayed pulse and compare screenshot/log
  against the `20260518-194653` baseline.
- If the fallback does not move visible output, instrument active color
  render-target writes immediately before `PM4_XE_SWAP`.
