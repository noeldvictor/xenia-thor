# Worklog 2026-09-20

## Deploy

- Installed on the Thor (c3ca0370, USB) at 11:30:06 with the MCP `xenia_install` tool
  (ApkShellDeploy, 12 s). APK sha256 `d0e905dc4fb0ea0c…`, identical on device. Built from master
  `a06e7508d7`: the port pass, the compile overlay fix, the toggle control surface, the MCP server.
- Not launched. The user tests it.
- Fixed the install tool's hash check: sha256sum on Git Bash prefixes a backslash, which made the
  tool report a false mismatch.

## Upstream sweep, ARM64 and Adreno 740

- Edge: 21 new commits, an XNA user-mode series plus 3 standalone CPU fixes. No performance work.
  Canary, XenDroid, xenia-project, and the forks: 0 new.
- Online: two Android forks found are not sources (a manifest repo and a dead 2024 fork).
- Mesa Turnip: 167 driver commits since our bundled build. No headline Adreno 740 performance commit;
  three LRZ and GMEM fence changes may move a number. Note: `docs/research/20260920-upstream-sweep-arm64-adreno.md`.
- Decision: update the bundled Turnip to 20260919 and measure. Queue the 3 edge fixes.
