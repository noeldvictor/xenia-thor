# 2026-05-20 Worklog

## Blue Dragon Thor Debug Loop

- Added `arm64_speed_profile_thread_snapshot` for the A64 speed profiler.
  - Generated A64 prologues now store the current guest function and incoming
    guest return address in `A64BackendContext`.
  - Speed-profile intervals can log each XThread's `last_fn`, `last_ret`,
    LR/CTR/R1/R3/R4.
  - The snapshot path retries the processor debug lock for short lock pressure
    and reports `last_global_owner_sys_tid` plus PPC global-lock breadcrumbs if
    it still cannot sample.
- Added `Processor::TryQueryThreadDebugInfos` so debug sampling does not block
  the profiler thread indefinitely.
- Added lightweight PPC global-lock owner breadcrumbs in `ppc_frontend`.
- Added generic global critical-region last-owner breadcrumb for profiler
  skip diagnostics.
- Added Android all-files debug access:
  - Manifest now declares `MANAGE_EXTERNAL_STORAGE`.
  - `tools/thor/thor_build.ps1` reapplies the `MANAGE_EXTERNAL_STORAGE` app-op
    after install/deploy when a device serial is provided.
- Changed the Thor debug launcher default to `mount_cache=true`.

## Thor Validation

- Build/deploy passed repeatedly with `tools/thor/thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`.
- Bad launch proof: `scratch/thor-debug/20260520-003245-*`.
  - After reinstall, storage app-op was rejected and the route stayed at
    `funcs=0`; this was a tooling/permission problem, not an A64 problem.
- Storage re-armed and verified:
  - `cmd appops get jp.xenia.emulator.github.debug MANAGE_EXTERNAL_STORAGE`
    returned `allow`.
  - Blue Dragon ISO path was visible on `/storage/2664-21DE/...`.
- Thread snapshot proof: `scratch/thor-debug/20260520-004503-*`.
  - Snapshots successfully logged guest thread PCs/registers at 15s and 30s.
  - Final sample still showed main guest CPU and XMA/GPU work dominating.
- Cache-mounted proof: `scratch/thor-debug/20260520-004656-*`.
  - `ResolvePath(cache:\pack)` errors were gone.
  - Route black-idled by the 45s interval with `entry_delta=0`, all threads
    sleeping, and a black screenshot.
- Title proof: `scratch/thor-debug/20260520-005304-*`,
  `scratch/thor-debug/20260520-010150-*`, and
  `scratch/thor-debug/20260520-011006-*`.
  - `20260520-005304` reached the visible Blue Dragon `press START` screen with
    wait trace and thread snapshots active.
  - `20260520-010150` reached `press START` in a 45s no-wait-trace live
    capture.
  - `20260520-011006` reached `press START` through the new
    `LaunchBlueDragonTitleCapture` mode with thread snapshots on and timed
    screenshots disabled.
  - The route remains timing-sensitive: `20260520-010454`,
    `20260520-010651`, and `20260520-010820` black-idled before the title loop
    with similar cvars when thread snapshots were off or timed screenshots were
    active.

## Tooling Changes

- Added `tools/thor/thor_xenia_debug.ps1 -Mode LaunchBlueDragonTitleCapture`.
  - Defaults to 45 seconds.
  - Avoids auto START/A input and the forced signed 10:10:10:2 Vulkan fallback
    used by the speed lane.
  - Adds optional `-TitleScreenshotSeconds` for timed screenshots; default is
    `0` because mid-run screencaps can perturb the timing-sensitive title route.
  - Records `title_screenshot_seconds` in capture metadata.
- Added `arm64_speed_profile_thread_snapshot_on_idle`.
  - Routed through Android intent extras and `tools/thor/thor_xenia_debug.ps1`
    as `-Arm64SpeedProfileThreadSnapshotOnIdle`.
  - It waits for at least one active A64 profile interval, then logs one thread
    snapshot only if a later interval goes idle.
  - This is intended for black-idle captures where always-on thread snapshots
    change early title-route timing.
- Build/deploy validation for the idle snapshot lane passed with
  `tools/thor/thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`.
- Idle snapshot lane proof: `scratch/thor-debug/20260520-012015-*`.
  - APK SHA-256:
    `FB1FA6AF9EACC02755FA7E3CAFF23E57FEC15AEB6BA1048B39D3FCF456813905`.
  - `-Arm64SpeedProfileThreadSnapshotOnIdle true`, no always-on snapshots, no
    timed screenshots.
  - Route reached the visible `press START` title screen and counters stayed
    active through the 45s interval, so the idle snapshot did not need to fire.
- Added first-pass Android HID for the AYN Thor built-in controller:
  - New Android HID driver reports XInput user 0 as connected.
  - `EmulatorActivity` forwards Android gamepad key, stick, trigger, and hat
    events to native code.
  - `tools/thor/thor_xenia_debug.ps1` now defaults normal launches to
    `-HidDriver android`; the Blue Dragon speed lane switches itself back to
    `-HidDriver nop` for scripted START/A automation.
  - Research note:
    `docs/research/20260520-012811-ayn-thor-controller-default.md`.
  - Validation passed: native Android build, full APK build, install to Thor
    serial `c3ca0370`, Blue Dragon launch with `hid=android`, and synthetic
    `KEYCODE_BUTTON_START` down/up observed in logcat.
- Captured a fresh Blue Dragon speed baseline:
  - `scratch/thor-debug/20260520-110440-*`.
  - All Blue Dragon A64 fastpaths were off, so the old `8246B408` draw-wait
    wall dominated again with hundred-million-entry intervals.
  - Simpleperf also showed XMA decoder and cold A64 translation cost, but
    `xma_fast_silence` remains opt-in because it trades audio correctness for
    speed.
- Captured the same route with the validated A64 speed pack:
  - `scratch/thor-debug/20260520-110714-*`.
  - Reached the Blue Dragon loading spinner at 95 seconds with no searched
    fatal markers.
  - `8246B408` disappeared from the top speed-profile rows.
  - `8272A3A4` is again the next body-time wall.
- Updated `Use-BlueDragonSpeedDefaults` so the speed lane enables the validated
  A64 speed pack by default while preserving explicit command-line overrides.
  Research note:
  `docs/research/20260520-111034-blue-dragon-speed-pack-default.md`.
- Validated the defaulted speed pack with
  `scratch/thor-debug/20260520-111324-*`; without manually passing the speed
  flags, metadata showed the pack enabled and the 20 second screenshot reached
  Blue Dragon's `press START` title screen.
- Added generic A64 guest memory offset lowering in
  `src/xenia/cpu/backend/a64/a64_seq_util.h`:
  - avoids redundant `mov w0,w0`;
  - returns immediately for zero offsets;
  - uses AArch64 immediate adds for small constant offsets;
  - uses shifted-immediate adds for page-aligned constants that fit
    `add #imm, lsl #12`;
  - keeps the old scratch-register path for other constants.
- Native-core build and full deploy passed for the safe offset-lowering APK.
- Safe offset-lowering proof: `scratch/thor-debug/20260520-124856-*`.
  - APK SHA-256:
    `07BEBC73E8984A73820C66A02B876632D939E31D764CD233195AA936226EB39C`.
  - No searched fatal markers; metadata stayed in `EmulatorActivity` with PID
    `24691`.
  - `8272A3A4` shrank to `code_size=14612` from the older speed-pack baseline
    around `15952`; `8272A8E8` shrank to `5892` from around `6412`.
  - Final screenshot was black, so this is process/route proof rather than a
    title or loading-spinner visual proof.
- Rejected a broader wrapped-immediate `ADD`/`SUB` experiment:
  `scratch/thor-debug/20260520-124314-*`.
  - It shrank `8272A3A4` further to `14532` and `8272A8E8` to `5876`, but the
    emulator process exited cleanly around 41 seconds before the scripted route
    completed.
  - Later launcher/file-picker activity in that capture was script fallout, so
    the experiment was removed.
- Shifted-immediate follow-up proof:
  `scratch/thor-debug/20260520-125303-*`.
  - APK SHA-256:
    `07C80969A22E135C429C0F0628C87B0A68D736AECE1A3469DC6EBB9198F30526`.
  - No searched fatal markers; metadata stayed in `EmulatorActivity` with PID
    `26982`.
  - `8272A3A4` remained `code_size=14612` and `8272A8E8` remained
    `code_size=5892`; the shifted form did not affect the current Blue Dragon
    hot blocks but is safe generic lowering.
- Research note:
  `docs/research/20260520-125511-a64-offset-lowering-speed.md`.

## Next

- Treat the next correctness target as Blue Dragon post-title route
  determinism after `mount_cache=true`, not as a helper-fastpath speed issue.
- Compare title-success captures against black-idle captures around
  `cache:\pack\item_rec.*`, file-thread waits, and the 30-45s A64 thread
  snapshots.
- Continue speed work at `8272A3A4` with full-region/static proof or generic
  A64 lowering improvements; avoid partial `8272A8B4` HIR-block splices and
  broad wrapped-immediate integer lowering until semantics are proven.

## 13:13-13:48 EDT - A64 kernel extern transition-cost pass

Branch: `master`

Commands run:

- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -DeviceSerial c3ca0370 -Mode LaunchBlueDragonSpeedCapture -LiveCaptureSeconds 90 -PerfSampleSeconds "30,60,90" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 25 -Arm64SpeedProfileMinDelta 1 -Simpleperf false`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -DeviceSerial c3ca0370 -Mode LaunchBlueDragonSpeedCapture -LiveCaptureSeconds 75 -PerfSampleSeconds "30,60,75" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 20 -Arm64SpeedProfileMinDelta 1 -Simpleperf false`

Files changed:

- `src/xenia/cpu/processor.h`
- `src/xenia/cpu/backend/a64/a64_backend.h`
- `src/xenia/cpu/backend/a64/a64_backend.cc`
- `src/xenia/cpu/backend/a64/a64_emitter.h`
- `src/xenia/cpu/backend/a64/a64_emitter.cc`
- `tools/thor/thor_xenia_debug.ps1`
- `AGENTS.md`
- `docs/research/20260520-134854-a64-kernel-extern-fastpaths.md`
- `docs/worklogs/20260520.md`

What changed:

- Added a small A64 high-frequency kernel export inline lane gated by
  `a64_inline_kernel_high_frequency_exports`.
- Exposed `Processor::irql_` to the A64 backend context for direct IRQL
  exchange in generated code.
- Inlined `KeRaiseIrqlToDpcLevel`.
- Inlined safe `RtlEnterCriticalSection` and `RtlTryEnterCriticalSection`
  cases with host fallback, and only recursive `RtlLeaveCriticalSection`
  release.
- Added `-A64InlineKernelHighFrequencyExports` to the Thor debug launcher and
  capture metadata.

Thor validation:

- Initial broad spinlock inline attempt:
  `scratch/thor-debug/20260520-132805-*`.
  - Regressed into an idle route; removed.
- IRQL-only baseline:
  `scratch/thor-debug/20260520-133239-*`.
  - No searched fatal markers; guest execution kept moving through 75 seconds.
- Rtl/IRQL fastpath proof:
  `scratch/thor-debug/20260520-134010-*`.
  - No searched fatal markers; metadata stayed in `EmulatorActivity`, PID
    `16777`.
  - Reached the Blue Dragon loading spinner.
  - Guest-to-host deltas were lower than the IRQL-only baseline.
- Guarded one-CAS spinlock experiment:
  `scratch/thor-debug/20260520-134410-*`.
  - Reproduced the black-idle signature:
    `entry_delta=0` after the 45 second interval.
  - Removed before final deploy.
- Final stable proof after spinlock rollback:
  `scratch/thor-debug/20260520-134646-*`.
  - APK SHA-256:
    `5D60BD98548642CEE488FB2AB9C3C7478619CD700687A8C2A28C17C381E31100`.
  - No searched fatal markers; metadata stayed in `EmulatorActivity`, PID
    `20858`.
  - Guest execution stayed active through 75 seconds.
  - Compared to the IRQL-only baseline, speed-profile guest-to-host deltas were
    lower by roughly 14-33% at matched intervals.

Findings:

- This is a real transition-cost reduction, but not a 30 FPS fix.
- Rtl functions still rank high by entry count because the guest still calls
  them; many calls now avoid the expensive host thunk.
- `KfLowerIrql` is not a safe simple inline because it calls
  `XThread::CheckApcs()`.
- Spinlock export inlines are currently unsafe for the Blue Dragon route,
  even with a guarded host fallback on busy acquire.

Next:

- Keep the Rtl/IRQL fastpaths.
- Do not reattempt spinlock inlines without a separate one-variable proof and
  idle diagnostics.
- Use A64 body-time/simpleperf for the next wall: generated guest hot loops,
  remaining thunk-safe kernel exports, and XMA/audio cost probes.

## 14:29-14:48 EDT - Wrapped Addi Lowering Rejected

Branch: `master`

Commands run:

- `git -C scratch\upstream\ax360e fetch --depth 1 origin main`
- `git diff --no-index --stat scratch\upstream\ax360e\app\src\main\cpp\xenia-canary\src\xenia\cpu\backend\a64 src\xenia\cpu\backend\a64`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -DeviceSerial c3ca0370 -Mode LaunchBlueDragonSpeedCapture -LiveCaptureSeconds 100 -PerfSampleSeconds "45,90,100" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 25 -Arm64SpeedProfileMinDelta 1 -Arm64SpeedProfileBodyTimeFilter "8272A3A4,8272A8E8,826C5620,827294CC,826BF770,8246A928,820C1708,82154E38,826BED98" -Simpleperf true -SimpleperfStartSecond 70 -SimpleperfSeconds 12 -SimpleperfFrequency 1000 -SimpleperfCallGraph fp`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -DeviceSerial c3ca0370 -Mode LaunchBlueDragonSpeedCapture -LiveCaptureSeconds 60 -PerfSampleSeconds "45,60" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 25 -Arm64SpeedProfileMinDelta 1 -Arm64SpeedProfileBodyTimeFilter "8272A3A4,8272A8E8,826C5620,827294CC,826BF770,8246A928,820C1708,82154E38,826BED98" -Arm64SpeedProfileThreadSnapshotOnIdle true`

Files changed:

- `AGENTS.md`
- `docs/research/20260520-144829-a64-wrapped-addi-bisect.md`
- `docs/worklogs/20260520.md`

Findings:

- aX360e's A64 donor code does not already fix wrapped `ADD`/`SUB`
  immediates. It still materializes large constants into scratch registers for
  the relevant integer paths.
- Baseline capture `scratch/thor-debug/20260520-142915-*` stayed active through
  100 seconds with APK SHA
  `5D60BD98548642CEE488FB2AB9C3C7478619CD700687A8C2A28C17C381E31100`.
- Broad I32/I64 `ADD`/`SUB` wrapped-immediate rewrite:
  `scratch/thor-debug/20260520-143752-*`.
  - APK SHA:
    `056085154CF2120B2013672AC1228C5C22A88CAAEF42DBEA22589E72DE1C9903`.
  - No fatal marker; stayed in `EmulatorActivity`.
  - Regressed into idle counters from about 45 seconds onward.
  - `8272A3A4` shrank to `code_size=13020`, but the route was invalid.
- Narrow `ADD_I32`-only rewrite:
  `scratch/thor-debug/20260520-144309-*`.
  - APK SHA:
    `4B127F9FDD538A7DCEC890379E385B9CDFEF717DF881B47048F3D25BEBBB7494`.
  - No fatal marker; stayed in `EmulatorActivity`.
  - Reproduced idle counters by about 45 seconds.
  - `8272A3A4` stayed at `code_size=13100`, so this did not even shrink the
    main hot block.
- Restored source, rebuilt, redeployed, and reran:
  `scratch/thor-debug/20260520-144638-*`.
  - APK SHA returned to
    `5D60BD98548642CEE488FB2AB9C3C7478619CD700687A8C2A28C17C381E31100`.
  - Guest counters kept moving through 60 seconds.
  - 60 second `top -H` showed Main Thread about 80.7%, XMA Decoder about
    42.3%, and GPU Commands about 19.2%.

Decision:

- No wrapped-immediate code change was kept.
- Do not reattempt this as a default lowering based on code-size shrink alone.
- Next pass should add a default-off lowering audit that records guest
  function, opcode, and constant for candidate immediate rewrites before any
  more A64 `ADD`/`SUB` conversion is enabled on Thor.

## 14:54-15:10 EDT - ADD_I64 Wrapped-Immediate Fastpath

Branch: `master`

Commands run:

- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -DeviceSerial c3ca0370 -Mode LaunchBlueDragonSpeedCapture -LiveCaptureSeconds 60 -PerfSampleSeconds "45,60" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 20 -Arm64SpeedProfileMinDelta 1 -Arm64SpeedProfileBodyTimeFilter "8272A3A4,8272A8E8,826C5620,827294CC,826BF770,8246A928,820C1708,82154E38,826BED98" -Arm64AddSubImmAudit true -Arm64AddSubImmAuditBudget 128`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -DeviceSerial c3ca0370 -Mode LaunchBlueDragonSpeedCapture -LiveCaptureSeconds 35 -PerfSampleSeconds "30" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 12 -Arm64SpeedProfileMinDelta 1 -Arm64AddSubImmAudit true -Arm64AddSubImmAuditBudget 128`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -DeviceSerial c3ca0370 -Mode LaunchBlueDragonSpeedCapture -LiveCaptureSeconds 60 -PerfSampleSeconds "45,60" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 20 -Arm64SpeedProfileMinDelta 1 -Arm64AddI64WrappedImmFastpath true`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -DeviceSerial c3ca0370 -Mode LaunchBlueDragonSpeedCapture -LiveCaptureSeconds 60 -PerfSampleSeconds "45,60" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 20 -Arm64SpeedProfileMinDelta 1`

Files changed:

- `android/android_studio_project/app/src/main/java/jp/xenia/emulator/EmulatorActivity.java`
- `src/xenia/cpu/backend/a64/a64_backend.cc`
- `src/xenia/cpu/backend/a64/a64_emitter.h`
- `src/xenia/cpu/backend/a64/a64_sequences.cc`
- `tools/thor/thor_xenia_debug.ps1`
- `AGENTS.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `docs/research/20260520-144829-a64-wrapped-addi-bisect.md`
- `docs/research/20260520-151030-a64-add-i64-wrapped-immediate-fastpath.md`
- `docs/worklogs/20260520.md`

What changed:

- Added default-off wrapped-immediate audit cvars and Thor launcher flags:
  `arm64_add_sub_imm_audit`,
  `arm64_add_sub_imm_audit_function`, and
  `arm64_add_sub_imm_audit_budget`.
- Fixed the Android ADB-extra whitelist so those cvars actually reach native
  code through `EmulatorActivity`.
- Added `arm64_add_i64_wrapped_imm_fastpath`, plus an exact function filter,
  to lower `ADD_I64 reg, reg, -N` as `SUB #N` when `N <= 4095`.
- Promoted that narrow `ADD_I64` fastpath to the default after two Thor route
  proofs.

Thor validation:

- `scratch/thor-debug/20260520-150200-*`
  - APK SHA:
    `1B3F57020D2EF87D219F3919658137AE2F70777FD8F2E3056FC5C80AAF58E5DB`.
  - Confirmed audit cvar crossed Java into C++.
  - Logged 128 candidates; useful set was `ADD_I64` wrapped small-negative
    constants such as `-1`, `-4`, `-0x10`, and `-0x70`.
- `scratch/thor-debug/20260520-150447-*`
  - APK SHA:
    `22A39CCF19F56690AFF3A03054C8F45A959E5B4CBE5D97878E844ECB3F99E520`.
  - Fastpath explicitly enabled.
  - No searched fatal markers; process stayed in `EmulatorActivity`.
  - Guest counters stayed active through 60 seconds.
  - `8272A3A4` shrank to `code_size=12772`.
- `scratch/thor-debug/20260520-150652-*`
  - APK SHA:
    `E40C7E514C3C0F1E6F4F570DE74677CCE0A3D71EAC347011E744814D78FABBA4`.
  - Fastpath enabled by default through the canonical speed command.
  - No searched fatal markers; process stayed in `EmulatorActivity`.
  - Guest counters stayed active through 60 seconds.
  - First active interval: `entry_delta=3868019`, `8272A3A4 code_size=12772`.
  - Final interval: `entry_delta=2035655`.

Findings:

- The previous broad wrapped-immediate rewrite failed because it touched unsafe
  width/opcode combinations; the route-proven useful slice is only `ADD_I64`
  wrapped small-negative constants.
- Compared to no-fastpath/no-body-time run `20260520-150004-*`, `8272A3A4`
  shrank from `12852` to `12772` bytes.
- This is a small generic codegen win and not enough to move Blue Dragon from
  2 FPS to playable by itself.

Next:

- Leave `ADD_I64` wrapped-immediate lowering default-on, with rollback via
  `-Arm64AddI64WrappedImmFastpath false`.
- Keep broad `ADD_I32`, `SUB_I32`, and `SUB_I64` rewrites blocked until an
  audit plus Thor route proof clears each shape separately.
- Add source offset or HIR instruction identity to the audit before the next
  immediate-lowering expansion.

## 15:12-15:22 EDT - I64 Logical-Immediate Lowering

Branch: `master`

Commands run:

- `git diff --no-index --stat -- scratch/upstream/ax360e/app/src/main/cpp/xenia-canary/src/xenia/cpu/backend/a64 src/xenia/cpu/backend/a64`
- `git diff --no-index -U5 -- scratch/upstream/ax360e/app/src/main/cpp/xenia-canary/src/xenia/cpu/backend/a64 src/xenia/cpu/backend/a64`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 60 -PerfSampleSeconds "45" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 20 -Arm64SpeedProfileMinDelta 1`

Files changed:

- `src/xenia/cpu/backend/a64/a64_sequences.cc`
- `AGENTS.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `docs/research/20260520-152237-a64-i64-logical-immediate-lowering.md`
- `docs/worklogs/20260520.md`

What changed:

- Added a local 64-bit A64 logical-immediate recognizer in the A64 sequence
  file instead of editing the `third_party/xbyak_aarch64` submodule.
- Routed `AND_I64`, `AND_NOT_I64` with constant second operand, `OR_I64`, and
  `XOR_I64` through direct logical-immediate emission when the constant is
  encodable.
- Kept the old materialize-to-`x0` fallback for non-encodable constants.

Thor validation:

- Capture: `scratch/thor-debug/20260520-152048-*`.
- APK SHA:
  `444BDC1AFDED5EEA933D8FA988FA7FB8DE77B5DB045FB28E7DCCF44E35EFDC03`.
- No searched fatal markers.
- Process stayed in `EmulatorActivity`.
- Guest counters stayed active through 60 seconds.
- Final interval: `funcs=4390`, `entry_delta=2056735`.
- Final `top -H`: Main Thread about 84.6%, XMA Decoder about 46.1%, GPU
  Commands about 15.3%.

Findings:

- aX360e/Edge donor comparison showed the Thor backend already carries the
  donor A64 tree plus local instrumentation; the useful work now is completing
  generic lowering gaps and proving them on Thor.
- The 32-bit logical paths already had helper-style immediate lowering, but the
  64-bit paths still materialized every constant.
- Compared with previous default proof `scratch/thor-debug/20260520-150652-*`,
  `8272A3A4` shrank from `code_size=12772` to `12652`, and `8272A8E8` shrank
  from `5628` to `5600`.

Next:

- Keep this generic I64 logical-immediate lowering.
- Continue scanning hot generated functions for scratch-materialization gaps
  before doing any more title-specific splices.

## 15:22-15:27 EDT - AND_NOT Logical-Immediate Follow-Up

Branch: `master`

Commands run:

- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 60 -PerfSampleSeconds "45" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 20 -Arm64SpeedProfileMinDelta 1`

Files changed:

- `src/xenia/cpu/backend/a64/a64_sequences.cc`
- `AGENTS.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `docs/research/20260520-152733-a64-and-not-logical-immediate-lowering.md`
- `docs/worklogs/20260520.md`

What changed:

- Lowered `AND_NOT_I8`, `AND_NOT_I16`, and `AND_NOT_I32` with a constant
  second operand through the existing 32-bit `and_imm` helper as
  `src1 & ~const`.
- This emits a direct A64 logical-immediate `AND` when the inverted mask is
  encodable and falls back through the helper otherwise.

Thor validation:

- Capture: `scratch/thor-debug/20260520-152601-*`.
- APK SHA:
  `4663F004F97C5314813479ABE9B483E5762BA3CB990C1BA0CF9B622F2DEA8FA7`.
- No searched fatal markers.
- Process stayed in `EmulatorActivity`.
- Guest counters stayed active through 60 seconds.
- Final interval: `funcs=4393`, `entry_delta=2057688`.

Findings:

- Route stayed healthy.
- Current hottest code sizes did not improve beyond the preceding I64 logical
  pass: `8272A3A4` stayed at `12652`, and `8272A8E8` stayed at `5600`.
- Keep this as a generic codegen cleanup, not a measured Blue Dragon speed win.

Next:

- Add a translation-time audit/counter for immediate-lowering helper hits before
  doing more broad generic lowering, so the next pass can target shapes that
  actually fire in hot functions.

## 15:27-15:46 EDT - Logical Immediate Audit For 8272A3A4

Branch: `master`

Commands run:

- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 45 -PerfSampleSeconds "40" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 20 -Arm64SpeedProfileMinDelta 1 -Arm64ImmediateLoweringAudit true -Arm64ImmediateLoweringAuditFunction 0x8272A3A4 -Arm64ImmediateLoweringAuditBudget 256`
- `git diff --check`

Files changed:

- `src/xenia/cpu/backend/a64/a64_backend.cc`
- `src/xenia/cpu/backend/a64/a64_sequences.cc`
- `android/android_studio_project/app/src/main/java/jp/xenia/emulator/EmulatorActivity.java`
- `tools/thor/thor_xenia_debug.ps1`
- `AGENTS.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `docs/research/20260520-154650-a64-immediate-lowering-audit.md`
- `docs/worklogs/20260520.md`

What changed:

- Added default-off `arm64_immediate_lowering_audit`,
  `arm64_immediate_lowering_audit_function`, and
  `arm64_immediate_lowering_audit_budget` cvars.
- Routed logical immediate helpers through an audit hook that logs function,
  opcode, width, constant side, masked immediate, and chosen action.
- Added Android extra forwarding and Thor script parameters for the audit.
- Fixed Thor script integer-extra conversion so high guest addresses such as
  `0x8272A3A4` pass through Android `--ei` as signed int32 values.

Thor validation:

- Capture: `scratch/thor-debug/20260520-154135-*`.
- APK SHA:
  `CF97908B37B50BBBC46DCD7F08B5F2B984055C24C48E8AFA92740E36B2C08FB3`.
- No searched fatal markers.
- Process stayed in `EmulatorActivity`.
- Startup log confirmed:
  `A64 immediate lowering audit enabled: function=8272A3A4 budget=256`.
- First active interval: `funcs=3604`, `entry_delta=3874470`.
- Hot code sizes stayed at `8272A3A4 code_size=12652` and
  `8272A8E8 code_size=5600`.

Findings:

- The filtered `8272A3A4` audit emitted 68 rows: 38 `AND_I8` and 30 `AND_I64`.
- All 68 rows were already `logical_imm yes action logical-imm`.
- There were zero `mov+reg` logical-immediate misses in this function.
- The final interval idled with trace logging active, so this run is
  diagnostic evidence only, not a speed comparison.

Next:

- Commit and push this audit tooling before another risky pass.
- Run the audit on `8272A8E8` or with no function filter and a small budget to
  find actual logical-immediate misses.
- If logical constants stay clean, add the next audit family around shifts,
  rotates, selects, or address-generation lowering.

## 15:46-15:57 EDT - Zero Logical-Immediate Cleanup

Branch: `master`

Commands run:

- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 45 -PerfSampleSeconds "40" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 20 -Arm64SpeedProfileMinDelta 1 -Arm64ImmediateLoweringAudit true -Arm64ImmediateLoweringAuditFunction 0x8272A8E8 -Arm64ImmediateLoweringAuditBudget 256`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 45 -PerfSampleSeconds "40" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 20 -Arm64SpeedProfileMinDelta 1 -Arm64ImmediateLoweringAudit true -Arm64ImmediateLoweringAuditBudget 256`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 45 -PerfSampleSeconds "40" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 20 -Arm64SpeedProfileMinDelta 1 -Arm64ImmediateLoweringAudit true -Arm64ImmediateLoweringAuditBudget 256`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 60 -PerfSampleSeconds "45" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 20 -Arm64SpeedProfileMinDelta 1`
- `git diff --check`

Files changed:

- `src/xenia/cpu/backend/a64/a64_sequences.cc`
- `AGENTS.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `docs/research/20260520-155700-a64-zero-logical-immediate-fastpath.md`
- `docs/worklogs/20260520.md`

What changed:

- Added zero-constant special cases to the shared logical immediate helpers:
  `AND reg, 0` emits zero, while `OR/XOR reg, 0` emits identity.
- Extended immediate-lowering audit actions so rows can say `identity` or
  `zero`, not only `logical-imm` or `mov+reg`.

Thor validation:

- `8272A8E8` targeted audit `scratch/thor-debug/20260520-154657-*` had 9 rows:
  7 `AND_I64`, 2 `AND_I8`, all `logical-imm`.
- Global before-change audit `scratch/thor-debug/20260520-154815-*` had 256
  rows: 210 `logical-imm`, 46 `mov+reg`. Of those misses, 44 were
  `OR_I32 imm 0`.
- Post-change audit `scratch/thor-debug/20260520-155154-*` had 256 rows:
  210 `logical-imm`, 44 `identity`, 2 `mov+reg`.
- Quiet proof `scratch/thor-debug/20260520-155321-*`:
  - APK SHA:
    `8C74D16929429E4BCD6DAF732A5C81DABE266F333D5081EA5F5B7A38B3A204F8`.
  - No searched fatal markers.
  - Process stayed in `EmulatorActivity`.
  - Final interval: `funcs=4393`, `entry_delta=2032022`.
  - Final `top -H`: Main Thread about 76.9%, XMA Decoder about 42.3%, GPU
    Commands about 19.2%.

Findings:

- The zero-identity cleanup removed the audited `OR_I32 imm 0` scratch path.
- This is generic codegen cleanup and route-safe, but not a measured FPS jump.
- Remaining first-budget misses are two `AND_I64` masks:
  `0x6001007D` and `0xEF`.

Next:

- Commit and push the zero cleanup.
- Investigate the two remaining `AND_I64` masks only with a separate audit and
  proof, or pivot to a higher-value lowering family such as shifts, rotates,
  selects, or address generation.

## 16:00-16:12 EDT - A64 Zero-Select Cleanup

Branch: `master`

Commands run:

- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 100 -PerfSampleSeconds "45,90" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 20 -Arm64SpeedProfileMinDelta 1`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 70 -PerfSampleSeconds "45" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 20 -Arm64SpeedProfileMinDelta 1`

Files changed:

- `src/xenia/cpu/backend/a64/a64_sequences.cc`
- `AGENTS.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `docs/research/20260520-161130-a64-zero-select-fastpath.md`
- `docs/worklogs/20260520.md`

What changed:

- Integer `SELECT_I8`, `SELECT_I16`, `SELECT_I32`, and `SELECT_I64` now detect
  zero constants and use `wzr` / `xzr` directly in `csel`.
- This avoids materializing `0` into `w1`, `w2`, `x1`, or `x2` for hot HIR
  shapes such as `select shift_too_large, 0, shifted_value`.

Thor validation:

- Baseline quiet proof before this pass:
  `scratch/thor-debug/20260520-155321-*`, APK SHA
  `8C74D16929429E4BCD6DAF732A5C81DABE266F333D5081EA5F5B7A38B3A204F8`,
  final interval `funcs=4393`, `entry_delta=2032022`,
  `8272A3A4 code_size=12652`, `8272A8E8 code_size=5600`.
- First proof `scratch/thor-debug/20260520-160530-*`, APK SHA
  `9E56D859F4717D3F8D8FB13B6CF4407C41F4ACCC91C885632CF1778086ADD14B`,
  had no searched fatal markers and shrank `8272A3A4` to `12540`, but later
  intervals went idle. Treat this as timing-sensitive negative evidence.
- Repeat proof `scratch/thor-debug/20260520-160757-*`, same APK SHA, had no
  searched fatal markers and stayed active through the 70-second capture.
  Later interval: `funcs=4704`, `entry_delta=5447909`.
- Measured code-size deltas: `8272A3A4 12652 -> 12540`, `8272A8E8 5600 ->
  5592`.

Findings:

- This is a small route-proven generic lowering cleanup for the repeated Blue
  Dragon shift/select pattern.
- The final screenshot remains black, so this is CPU/codegen progress, not a
  visible title-screen progress claim.

Next:

- Re-run body-time or block profiling after this cleanup to choose the next
  `8272A3A4` target.
- Do not fuse the whole variable-shift/select pattern without a HIR-aware
  peephole or a very narrow proof.

## 16:13-16:18 EDT - A64 Unsigned Zero-Compare Fold

Branch: `master`

Commands run:

- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 70 -PerfSampleSeconds "45" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 20 -Arm64SpeedProfileMinDelta 1`

Files changed:

- `src/xenia/cpu/backend/a64/a64_sequences.cc`
- `AGENTS.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `docs/research/20260520-161650-a64-unsigned-zero-compare-fold.md`
- `docs/worklogs/20260520.md`

What changed:

- Added a narrow A64 integer compare fold for unsigned zero-bound comparisons:
  `x < 0` and `0 > x` emit false; `x >= 0` and `0 <= x` emit true.
- This removes `cmp` + `cset` from repeated CR flag patterns in the Blue Dragon
  hot HIR.

Thor validation:

- Baseline after zero-select cleanup:
  `scratch/thor-debug/20260520-160757-*`, APK SHA
  `9E56D859F4717D3F8D8FB13B6CF4407C41F4ACCC91C885632CF1778086ADD14B`,
  active through 70 seconds, later interval `funcs=4704`,
  `entry_delta=5447909`, `8272A3A4 code_size=12540`,
  `8272A8E8 code_size=5592`.
- Proof `scratch/thor-debug/20260520-161344-*`, APK SHA
  `F4934E774C4CA14709F0FE228FF52CE503ED61E812722F6007E87132D9A225FB`,
  had no searched fatal markers and stayed active through the 70-second
  capture.
- Proof later interval: `funcs=4687`, `entry_delta=5421420`.
- Code-size deltas versus the zero-select baseline:
  `8272A3A4 12540 -> 12432`, `8272A8E8 5592 -> 5552`.

Findings:

- This is a route-proven generic cleanup and a direct hit on the hot CR compare
  rows seen in `8272A3A4` and `8272A8E8`.
- The route still has black screenshots, so this is CPU/codegen progress only.

Next:

- Run body-time profiling again with the post-cleanup APK to pick the next real
  wall-time target.

## 16:27-16:35 EDT - A64 Compare-To-Context Store Peephole

Branch: `master`

Commands run:

- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 60 -PerfSampleSeconds "45" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 20 -Arm64SpeedProfileMinDelta 1`

Files changed:

- `src/xenia/cpu/backend/a64/a64_sequences.cc`
- `AGENTS.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `docs/research/20260520-163450-a64-compare-store-context-peephole.md`
- `docs/worklogs/20260520.md`

What changed:

- `STORE_CONTEXT_I8/I16` and `STORE_LOCAL_I8/I16` now use `wzr` directly for
  masked zero constants.
- Added a narrow A64 sequence peephole for always-false unsigned zero compares
  whose result is used only by the immediately following `store_context`.
- The peephole turns the Blue Dragon CR flag shape `compare_ult value, 0`;
  `store_context flag, result` into a direct zero context store.

Thor validation:

- Zero-store-only proof `scratch/thor-debug/20260520-162727-*`, APK SHA
  `A430D4C2E14701987CF21E87002AF4218002CF919CA49B684F6434784110ADF9`, had no
  searched fatal markers and reached the Blue Dragon Voice Language menu, but
  `8272A3A4` stayed at `12432` and `8272A8E8` stayed at `5552`.
- Peephole proof `scratch/thor-debug/20260520-163134-*`, APK SHA
  `D04FA8B9BEA65A1008BAFC3C59031415E3A68CC9BC8A140DA83DBCF61EFAEA6D`, had no
  searched fatal markers and reached the Blue Dragon Voice Language menu.
- First active interval: `funcs=3651`, `entry_delta=3872135`.
- Later interval: `funcs=4393`, `entry_delta=2043246`.
- Code-size deltas versus the unsigned zero-compare baseline:
  `8272A3A4 12432 -> 12332`, `8272A8E8 5552 -> 5520`.
- Final `top -H`: Main Thread about 84.6%, XMA Decoder about 38.4%, GPU
  Commands about 15.3%; thermal status 0.

Findings:

- The zero-store cleanup is safe but not enough by itself.
- The peephole is a real generic lowering win for CR context-store churn.
- We still need larger wins, likely register-cached PPC GPR/CR lowering or a
  fully proven `8272A3A4` region fastpath. This is not yet playable speed.

Next:

- Commit and push this safe A64 cleanup.
- Use a fresh body-time capture before choosing the next large target.

## 16:35-16:42 EDT - Post-Peephole Body-Time And Rejected UGT/EQ Pair

Branch: `master`

Commands run:

- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 85 -PerfSampleSeconds "70" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 20 -Arm64SpeedProfileMinDelta 1 -Arm64SpeedProfileBodyTimeFilter "8272A3A4,8272A8E8,826C5620,827294CC,826BF770,8246A928,820C1708,82154E38,826BED98"`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 60 -PerfSampleSeconds "45" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 20 -Arm64SpeedProfileMinDelta 1`

Files changed:

- `AGENTS.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`
- `docs/research/20260520-164105-a64-ugt-eq-context-pair-nohit.md`
- `docs/worklogs/20260520.md`

What changed:

- Ran a post-commit body-time capture for `6fb53f1d2`.
- Tried an uncommitted local peephole to share one compare between adjacent
  `COMPARE_UGT x, 0` and `COMPARE_EQ x, 0` context stores.
- Reverted that local code because the clean proof showed no hot-function
  shrink.

Thor validation:

- Body-time capture `scratch/thor-debug/20260520-163540-*`, APK SHA
  `D04FA8B9BEA65A1008BAFC3C59031415E3A68CC9BC8A140DA83DBCF61EFAEA6D`,
  had no searched fatal markers. Final screenshot showed the Blue Dragon
  loading spinner. Final body interval had `8272A3A4` as body top:
  `body_ticks_delta=11968390`, `entries_delta=893698`,
  `ticks_per_entry=13`; `827294CC` was next at `1020270` body ticks.
- Rejected pair proof `scratch/thor-debug/20260520-163945-*`, APK SHA
  `618BC8C0F5B55000C2C2ED557B0A6401F2CC4980A3B3A403CEDAC3000DE14FFB`, had no
  searched fatal markers and reached the Blue Dragon Voice Language menu.
- The rejected pair proof kept `8272A3A4 code_size=12332` and
  `8272A8E8 code_size=5520`, identical to the pushed
  compare-to-context-store peephole proof.

Findings:

- `8272A3A4` is still the body-time wall after the proven peephole.
- The `UGT/EQ` context-pair matcher is not worth keeping without an audit that
  proves exact hits in the hot function.

Next:

- Commit/push these negative notes.
- Next real speed target should be a larger `8272A3A4` loop strategy:
  register-cached PPC GPR/CR lowering or a fully proven native region fastpath.

## 17:02-17:06 EDT - A64 UGT/EQ CR Branch Peephole

Branch: `master`

Commands run:

- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 95 -PerfSampleSeconds "70" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 20 -Arm64SpeedProfileMinDelta 1 -Arm64SpeedProfileBodyTimeFilter "8272A3A4,8272A8E8,826C5620,827294CC,826BF770"`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 70 -PerfSampleSeconds "60" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 20 -Arm64SpeedProfileMinDelta 1`

Files changed:

- `src/xenia/cpu/backend/a64/a64_sequences.cc`
- `docs/research/20260520-170621-a64-ugt-eq-cr-branch-peephole.md`
- `docs/worklogs/20260520.md`
- `AGENTS.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`

What changed:

- Added a guarded A64 matcher for PPC CR `GT` plus `EQ` store pairs:
  `COMPARE_UGT` followed by a same-operand `COMPARE_EQ`, both storing to
  adjacent CR bytes.
- The matcher also consumes an immediately following branch on the EQ result,
  emitting one `cmp`, `cset HI`, `strb GT`, `cset EQ`, `strb EQ`, and a direct
  `b.eq` / `b.ne`.
- Guards require exact CR byte offsets and no unexpected uses, so this does not
  revive the earlier rejected no-hit pair.

Thor validation:

- NativeCore and FullDeploy succeeded.
- Body-time proof `scratch/thor-debug/20260520-170226-*`, APK SHA
  `A7ED0CE9547441992F0EAC297EDE425FA81D97BA6AFB5DDE5E44EB155C9F5E5F`, had no
  searched fatal markers and showed the Blue Dragon loading spinner.
- Final body interval kept `8272A3A4` as the wall:
  `body_ticks_delta=19940185`, `entries_delta=1277068`,
  `ticks_per_entry=15`, `code_size=12544`.
- Clean proof `scratch/thor-debug/20260520-170433-*` had no searched fatal
  markers and stayed active through the final A64 interval.
- Clean code-size deltas versus the previous clean proof:
  `8272A3A4 12332 -> 12296`, `8272A8E8 5520 -> 5508`.
- Final clean interval: `funcs=4695`, `entry_delta=5436916`; top entries were
  `827294CC delta=1365881`, `8272A3A4 delta=903231`,
  `8272A8E8 delta=564994`.

Findings:

- This is a real measured CPU/codegen win in the same hot loop, but it is still
  small.
- The remaining wall is byte-loop/context churn in `8272A3A4`, not GPU.

Next:

- Commit and push this safe peephole.
- Move to a larger `8272A3A4` strategy: register-cached CR/GPR lowering or a
  fully proven native state-machine fastpath around the `8272A8B4` loop.

## 17:10-17:13 EDT - Rejected Generic Compare-Branch Fusion

Branch: `master`

Commands run:

- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 70 -PerfSampleSeconds "60" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 20 -Arm64SpeedProfileMinDelta 1`

Files changed:

- `docs/research/20260520-171256-a64-compare-branch-black-idle.md`
- `docs/worklogs/20260520.md`
- `AGENTS.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`

What changed:

- Tried an uncommitted local matcher for integer `COMPARE_*` immediately
  followed by a single-use branch, lowering it as `cmp` plus `b.cond`.
- Reverted the code before commit because the Thor route black-idled.

Thor validation:

- NativeCore and FullDeploy succeeded.
- Capture `scratch/thor-debug/20260520-171056-*`, APK SHA
  `8E63DC6A0D634A8E0F88820A557CD9C922F512D890F172E07F2DF3E1DF28D88C`, had no
  searched fatal markers.
- First active interval: `funcs=3653`, `entry_delta=3872644`.
- Second interval dropped to `entry_delta=94899`.
- Later intervals at `17:11:45`, `17:12:00`, and `17:12:15` all had
  `entry_delta=0`.
- Final screenshot was black.
- First interval hot code sizes were unchanged from the committed UGT/EQ proof:
  `8272A3A4=12296`, `8272A8E8=5508`, `827294CC=324`, `826BF770=248`.

Findings:

- This broad compare-branch fusion is not route-clean.
- Do not retry it without a hit audit, condition-polarity tests, and an idle
  snapshot explaining the black frame.

Next:

- Commit/push the negative note only.
- Return to larger `8272A3A4` work, not generic branch fusion.

## 17:13-17:32 EDT - Edge Reservation Helper Import, Default-Off

Branch: `master`

Commands run:

- `git ls-remote https://github.com/aenu1/ax360e.git refs/heads/main`
- `git ls-remote https://github.com/has207/xenia-edge.git refs/heads/edge`
- `git -C scratch\upstream\xenia-edge fetch --depth 1 origin edge`
- `git -C scratch\upstream\ax360e fetch --depth 1 origin main`
- `git -C scratch\upstream\xenia-edge diff --stat 302034249048dc9d664b12b6a3560a6d95933803 eee166febd27dfdffc556c4474e1aa49f2b4ccc5 -- src\xenia\cpu\backend\a64`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 95 -PerfSampleSeconds "70" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 20 -Arm64SpeedProfileMinDelta 1 -Arm64SpeedProfileBodyTimeFilter "8272A3A4,8272A8E8,826C5620,827294CC,826BF770"`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 70 -PerfSampleSeconds "60" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 20 -Arm64SpeedProfileMinDelta 1 -Arm64SpeedProfileBodyTimeFilter "8272A3A4,8272A8E8,826C5620,827294CC,826BF770"`

Files changed:

- `src/xenia/cpu/backend/a64/a64_backend.cc`
- `src/xenia/cpu/backend/a64/a64_seq_memory.cc`
- `tools/thor/thor_xenia_debug.ps1`
- `docs/research/20260520-173242-edge-reservation-helper-import.md`
- `docs/worklogs/20260520.md`
- `AGENTS.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`

What changed:

- Refreshed the donor checkouts:
  - aX360e `main` remains `2b4c889e8f849d07ede83334fa0b323f5c647828`.
  - Xenia Edge `edge` is now `eee166febd27dfdffc556c4474e1aa49f2b4ccc5`.
- Imported Edge-style A64 reservation helper functions and helper pointer
  wiring:
  - `TryAcquireReservationHelper`
  - `ReservedStore32Helper`
  - `ReservedStore64Helper`
- Added `arm64_global_reservation_helpers`, default `false`, so the donor
  helper path can be enabled only for focused PPC sync experiments.
- Added `-Arm64GlobalReservationHelpers true` support to the Thor debug script.

Thor validation:

- NativeCore and FullDeploy succeeded.
- Default-on experiment `scratch/thor-debug/20260520-172518-*`, APK SHA
  `5F62F4975DF6A33669DC08D1940615F83B0905BB58B3E06B32778756BB2EED94`, had no
  searched fatal markers but black-idled after the early burst:
  `entry_delta=3849847`, then `171433`, then `0` in later intervals.
- Default-off gated build `scratch/thor-debug/20260520-173018-*`, APK SHA
  `1E9D57B65AE7CE7E86D54254010C3E981D1C20C760CCE9FB7F0CF34A1D72E639`, used
  the legacy inline reservation lowering and preserved the expected body-time
  code-size shape in the first active interval:
  `8272A3A4=12544`, `8272A8E8=5756`, `827294CC=412`, `826BF770=296`.

Findings:

- The latest Edge donor delta is useful for backend maturity, but not a Blue
  Dragon speed win.
- aX360e/Edge do not appear to contain a hidden large missing opcode table; the
  active gap is lower-level generated-code quality around PPC state,
  branch/CR lowering, block linking, and hot function structure.
- Keep the donor reservation helpers as an opt-in experiment. Do not make them
  default-on for Blue Dragon without a route-clean proof.

Next:

- Commit and push the opt-in donor import and negative evidence.
- Resume larger `8272A3A4` work: full-region proof or an audit that counts CR,
  GPR, endian memory, branch, and context round-trips in the hot generated
  function.

## 17:33-18:05 EDT - PowerPC To ARM64 Source Harvest

Branch: `master`

Commands run:

- Web search/opened primary sources for Dolphin `JitArm64`, QEMU TCG/PPC/A64,
  RPCS3 PPU/LLVM/AArch64, and IBM PowerPC branch docs.
- `git clone --depth 1 --filter=blob:none --sparse https://github.com/dolphin-emu/dolphin.git scratch\upstream\dolphin`
- `git -C scratch\upstream\dolphin sparse-checkout set Source/Core/Core/PowerPC`
- `git clone --depth 1 --filter=blob:none --sparse https://github.com/qemu/qemu.git scratch\upstream\qemu`
- `git -C scratch\upstream\qemu sparse-checkout set tcg/aarch64 target/ppc docs/devel`
- `git clone --depth 1 --filter=blob:none --sparse https://github.com/RPCS3/rpcs3.git scratch\upstream\rpcs3`
- `git -C scratch\upstream\rpcs3 sparse-checkout set rpcs3/Emu/Cell rpcs3/Emu/CPU`
- `rg -n "JitArm64|RegCache|StoreCRRegisters|JumpIfCRFieldBit|WriteExit|BlockCache" scratch\upstream\dolphin\Source\Core\Core\PowerPC\JitArm64`
- `rg -n "Direct block chaining|lookup_and_goto_ptr|cpu_env|atomic|exclusive|gen_bcond|lwarx|stwcx" scratch\upstream\qemu\docs\devel scratch\upstream\qemu\target\ppc`
- `rg -n "PPUTranslator|LLVM|AArch64|raddr|rtime|reservation" scratch\upstream\rpcs3\rpcs3\Emu`
- `git diff --check`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore`

Files changed:

- `docs/research/20260520-180132-powerpc-to-arm64-source-harvest.md`
- `docs/worklogs/20260520.md`
- `AGENTS.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`

Findings:

- Dolphin `JitArm64` is the closest public PPC-to-AArch64 reference. The key
  lesson is not a magic opcode table; it is pinned PPC state/membase registers,
  GPR/FPR/CR caching, branch/LR/CTR lowering, and flushes based on exact
  liveness.
- QEMU TCG is useful for translation-block chaining, helper read/write
  metadata, explicit PPC CR globals, and reservation/atomic semantics.
- RPCS3 PPU/LLVM/AArch64 is useful as a second-backbone idea for hot function
  LLVM experiments and Ghidra/OODA function/module analysis.
- Dolphin/QEMU/RPCS3 are GPL-family references. Use them for design, audits,
  and semantics unless this fork makes a deliberate license-compatible import
  decision.
- IBM PowerPC branch docs reinforce why broad compare/branch fusions are risky:
  CR/LR/CTR and BO/BI semantics need exact guards.
- Validation: `git diff --check` passed with only normal line-ending warnings,
  and `NativeCore` rebuilt successfully for Android `arm64-v8a` and `x86_64`.

Next:

- Make the next speed sprint an `8272A3A4` state-traffic audit.
- Count context loads/stores, CR traffic, LR/CTR traffic, helper calls, direct
  exits, indirect exits, endian swaps, and dispatcher returns.
- Use that audit to build the first Dolphin-style CR/GPR context cache in the
  A64 backend.

## 18:05-18:24 EDT - A64 Context Traffic Audit For 8272A3A4

Branch: `master`

Commands run:

- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 55 -PerfSampleSeconds "45" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 20 -Arm64SpeedProfileMinDelta 1 -Arm64SpeedProfileBodyTimeFilter "8272A3A4" -Arm64ContextTrafficAudit true -Arm64ContextTrafficAuditFunction 0x8272A3A4 -Arm64ContextTrafficAuditBudget 4`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 50 -PerfSampleSeconds "40" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 20 -Arm64SpeedProfileMinDelta 1 -Arm64SpeedProfileBodyTimeFilter "8272A3A4" -Arm64ContextTrafficAudit true -Arm64ContextTrafficAuditFunction 0x8272A3A4 -Arm64ContextTrafficAuditBudget 4`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 50 -PerfSampleSeconds "40" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 20 -Arm64SpeedProfileMinDelta 1 -Arm64SpeedProfileBodyTimeFilter "8272A3A4" -Arm64ContextTrafficAudit true -Arm64ContextTrafficAuditFunction 0x8272A3A4 -Arm64ContextTrafficAuditBudget 4`

Files changed:

- `src/xenia/cpu/backend/a64/a64_backend.cc`
- `src/xenia/cpu/backend/a64/a64_emitter.cc`
- `src/xenia/cpu/backend/a64/a64_emitter.h`
- `src/xenia/cpu/backend/a64/a64_sequences.cc`
- `android/android_studio_project/app/src/main/java/jp/xenia/emulator/EmulatorActivity.java`
- `tools/thor/thor_xenia_debug.ps1`
- `docs/research/20260520-182253-a64-context-traffic-audit.md`
- `docs/worklogs/20260520.md`
- `AGENTS.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`

What changed:

- Added default-off A64 context-traffic audit cvars:
  `arm64_context_traffic_audit`,
  `arm64_context_traffic_audit_function`, and
  `arm64_context_traffic_audit_budget`.
- Exposed the audit through the Thor debug launcher and Android
  `EmulatorActivity` launch-argument bridge.
- Added a compile-time HIR scan in the A64 emitter for selected functions. It
  logs PPC context loads/stores, CR/GPR/LR/CTR classes, local/memory traffic,
  compare-derived context stores, branches/calls, and top PPCContext offsets.
- Added a generic exact `LT/GT/EQ` PPC CR compare-store triplet peephole in
  `a64_sequences.cc`.

Thor validation:

- NativeCore built successfully for Android `arm64-v8a` and `x86_64`.
- `scratch/thor-debug/20260520-181535-*` proved the launcher metadata was set,
  but native did not enable the audit because `EmulatorActivity.java` was not
  yet forwarding the new cvars.
- `scratch/thor-debug/20260520-181744-*` proved the audit bridge and emitted
  the first `8272A3A4` state-traffic summary.
- `scratch/thor-debug/20260520-182128-*`, APK SHA
  `44C846620EF331CDDC58E54C1DA0DAF06EEE6730C1E583117DA3BC5F0F4CC91C`,
  stayed alive and reached the Blue Dragon Voice Language menu.

Findings:

- `8272A3A4` compile-time HIR state traffic:
  - `blocks=54`, `instrs=2467`
  - `context_loads=255`, `context_stores=442`
  - `ppc_loads lr/ctr/gpr/cr/xer/fpr/vmx/runtime=0/0/255/0/0/0/0/0`
  - `ppc_stores=6/0/252/183/1/0/0/0`
  - `store_src cmp/const/select=183/18/0`
  - `barriers ctx/mem=85/0`
  - top loads: `0x108`, `0x118`, `0x028`, `0x110`, `0x0F8`, `0x0D8`,
    `0x078`, `0x070`
  - top stores: CR6 bytes `0xA3C..0xA3E`, then GPR slots `0x078`, `0x070`,
    `0x038`, `0x068`, `0x040`
- The triplet peephole is route-clean but not a measured `8272A3A4` win:
  `8272A3A4 code_size` stayed `12544`.
- The final 40-second perf sample showed real CPU activity at the Voice
  Language menu: `Main Thread` about `76.9%`, `XMA Decoder` about `42.3%`,
  and `GPU Commands` about `15.3%`.

Next:

- Add a hit audit for CR compare-store shapes so the exact `0xA3C..0xA3E`
  pattern is known before another peephole.
- Start a narrow A64 GPR context-cache experiment for the hottest offsets in
  `8272A3A4`.
- Treat the 85 context barriers as the flush/aliasing problem before broad
  Dolphin-style PPC state caching.

## 18:24-18:40 EDT - CR Shape Audit And Relaxed Compare-Store Peepholes

Branch: `master`

Commands run:

- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 50 -PerfSampleSeconds "40" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 20 -Arm64SpeedProfileMinDelta 1 -Arm64SpeedProfileBodyTimeFilter "8272A3A4" -Arm64ContextTrafficAudit true -Arm64ContextTrafficAuditFunction 0x8272A3A4 -Arm64ContextTrafficAuditBudget 4`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 50 -PerfSampleSeconds "40" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 20 -Arm64SpeedProfileMinDelta 1 -Arm64SpeedProfileBodyTimeFilter "8272A3A4" -Arm64ContextTrafficAudit true -Arm64ContextTrafficAuditFunction 0x8272A3A4 -Arm64ContextTrafficAuditBudget 4`
- `rg -n "A64 context traffic audit|A64 speed profile|8272A3A4|Fatal signal|AndroidRuntime|VK_ERROR_DEVICE_LOST|GPU is hung|ANR|tombstone|crash|signal" scratch\thor-debug -g "20260520-183741-*"`

Files changed:

- `src/xenia/cpu/backend/a64/a64_emitter.cc`
- `src/xenia/cpu/backend/a64/a64_sequences.cc`
- `docs/research/20260520-184020-a64-cr-shape-relaxed-peephole.md`
- `docs/worklogs/20260520.md`
- `AGENTS.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`

What changed:

- Extended the context-traffic audit with CR store source-opcode summaries and
  exact CR shape counters for `LT/GT/EQ`, `UGT/EQ`, and CR6 `IS_FALSE` update
  forms.
- Relaxed the A64 `LT/GT/EQ` triplet and `UGT/EQ` pair peepholes so compare
  values can have later users. The fused code still emits each `cset` into the
  original HIR value register before skipping adjacent compare/store work.

Thor validation:

- NativeCore built successfully for Android `arm64-v8a` and `x86_64`.
- FullDeploy installed successfully on Thor.
- Pre-relax capture `scratch/thor-debug/20260520-183455-*` showed the missing
  hit reason: `cr_shapes triplet=60/0 signed/unsigned=10/50 gt_eq=50/9 cr6=0/0`
  with top CR stores
  `0xA3E:cmp_eq:61,0xA3C:cmp_ult:51,0xA3D:cmp_ugt:51`.
- Post-relax capture `scratch/thor-debug/20260520-183741-*` had no searched
  fatal markers and shrank `8272A3A4` from `12544` to `12196` bytes and
  `8272A8E8` from `5508` to `5356` bytes.

Findings:

- The earlier triplet peephole was not firing because the only-use guard was
  too strict for Blue Dragon's HIR. The compare values are still safe to
  materialize because later users can read the same assigned A64 value regs.
- This is a real generated-code shrink, but not yet a visible speed or route
  fix. The post-relax run still black-idled later, and the 40-second screenshot
  was black.
- Remaining `8272A3A4` waste is still dominated by context traffic:
  255 context loads, 442 context stores, 85 context barriers, and hot GPR slots.

Next:

- Add emitted-hit counters for the relaxed CR peepholes so future captures can
  distinguish HIR shape counts from selected-code hits.
- Start the narrow GPR context-cache experiment for the hottest `8272A3A4`
  offsets, with context-barrier flush rules.

## 18:40-19:29 EDT - Context Cache Negative And Kernel Spinlock Fastpaths

Branch: `master`

Commands run:

- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 50 -PerfSampleSeconds "40" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 20 -Arm64SpeedProfileMinDelta 1 -Arm64SpeedProfileBodyTimeFilter "8272A3A4" -Arm64ContextValueCache true -Arm64ContextTrafficAudit true -Arm64ContextTrafficAuditFunction 0x8272A3A4 -Arm64ContextTrafficAuditBudget 4`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 70 -PerfSampleSeconds "40,65" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 20 -Arm64SpeedProfileMinDelta 1 -A64InlineKernelSpinlockExports true -A64InlineKfLowerIrql true -Arm64ContextValueCache false`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 55 -PerfSampleSeconds "40" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 20 -Arm64SpeedProfileMinDelta 1 -A64InlineKernelSpinlockExports true -A64InlineKfLowerIrql false -Arm64ContextValueCache false`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode NativeCore`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_build.ps1 -Mode FullDeploy -DeviceSerial c3ca0370`
- `powershell -ExecutionPolicy Bypass -File tools\thor\thor_xenia_debug.ps1 -Mode LaunchBlueDragonSpeedCapture -DeviceSerial c3ca0370 -LiveCaptureSeconds 55 -PerfSampleSeconds "40" -Arm64SpeedProfileIntervalMs 15000 -Arm64SpeedProfileTopFunctions 20 -Arm64SpeedProfileMinDelta 1`

Files changed:

- `src/xenia/cpu/backend/a64/a64_backend.cc`
- `src/xenia/cpu/backend/a64/a64_emitter.cc`
- `android/android_studio_project/app/src/main/java/jp/xenia/emulator/EmulatorActivity.java`
- `tools/thor/thor_xenia_debug.ps1`
- `docs/research/20260520-192930-a64-context-cache-and-spinlock-fastpaths.md`
- `docs/worklogs/20260520.md`
- `AGENTS.md`
- `.agents/skills/xenia-a64-speed-hotpath/SKILL.md`

What changed:

- Added a default-off `arm64_context_value_cache` probe for block-local PPC GPR
  context load reuse.
- Added default-on A64 inline fastpaths for
  `KeAcquireSpinLockAtRaisedIrql`, `KeTryToAcquireSpinLockAtRaisedIrql`, and
  `KeReleaseSpinLockFromRaisedIrql`.
- Added default-off `a64_inline_kf_lower_irql` as an opt-in probe because the
  direct IRQL restore skips native APC delivery.
- Forwarded the kernel-inline toggles and context-cache toggle through Android
  launch arguments and `tools/thor/thor_xenia_debug.ps1` metadata.

Thor validation:

- NativeCore built successfully for Android `arm64-v8a` and `x86_64`.
- FullDeploy installed successfully on Thor.
- Context cache probes `scratch/thor-debug/20260520-191256-*` and
  `scratch/thor-debug/20260520-191641-*` reported `8272A3A4` load hits
  `255/0`, so the cache is not a speed win.
- `scratch/thor-debug/20260520-192530-*`, with `a64_inline_kf_lower_irql=true`,
  black-idled after the early burst.
- `scratch/thor-debug/20260520-192739-*`, with `a64_inline_kf_lower_irql=false`,
  stayed active and reached the Blue Dragon Voice Language menu.
- Final default capture `scratch/thor-debug/20260520-193232-*`, APK SHA
  `3C89CD3DBEC4FBD9735D29A10178F24BA584D1B9C2BD7EEEBE9C5C1156FB4159`, also
  stayed active and reached the Blue Dragon Voice Language menu.

Findings:

- The simple block-local context cache found no same-block `8272A3A4` reloads
  from live host regs despite seeing 240 cacheable stores. The real answer is a
  cross-block PPC state cache with exact helper/exit/barrier flush rules.
- Spinlock inlining reduced native guest-to-host trips while keeping the route
  alive. Compared with the previous healthy route, the 15-30s interval moved
  from `g2h=263236` to `g2h=206432`, and the 30-45s interval moved from
  `g2h=322262` to `g2h=244721`.
- `KfLowerIrql` is still hot, but the naive inline is unsafe as a default.

Next:

- Keep `a64_inline_kf_lower_irql=false` unless doing a deliberate A/B probe.
- Attack `RtlEnterCriticalSection` / `RtlLeaveCriticalSection` fallback cost.
- Start the real PPC state-cache design rather than adding more block-local
  context-cache machinery.
