# Worklog 2026-09-18

## CLAUDE.md split and STE rewrite

- User directive: all future language in ASD-STE100 Simplified Technical English with five rules. Codified in
  CLAUDE.md section 1 and in a memory file.
- CLAUDE.md went from 11,790 lines (1,005,032 bytes) to 286 lines. The old file is archived verbatim at
  `docs/research/20260918-claude-md-archive.md` (11,799 lines with a 9-line preamble). Header index at
  `docs/research/20260918-claude-md-archive-index.md` (593 lines).
- The new CLAUDE.md holds: language rule, mission, read-first map, 15 dated standing directives, device
  hygiene, measurement rules, the source table with sweep marks, current state, build and git rules.
- Correction found during the fork scan: our a64 backend is the canary donor of 2026-05-18 (Herman S.,
  xbyak_aarch64), not Wunkolo's oaknut backend. The source table says so.

## Upstream triage

- Remotes added: `upstream` (xenia-project), `xendroid`, `wunkolo`, `xeniamac`, `arm64fork`, `cassar`.
  Edge tracking corrected to branch `edge`. All 60 open canary PR heads fetched to `refs/remotes/canary/pr/N`.
- Four read-only scans by subagents. Results in `docs/research/20260918-upstream-triage.md`.

| source | scanned | kept |
|---|---|---|
| xenia-edge since 01efb80ed | 298 | 69 |
| xenia-canary since fork base | 950 | 52 |
| open canary PRs | 60 | 16 |
| forks (Wunkolo, xenia-mac, IhorHarayda, XenDroid branches) | 398 | 7 |
| xenia-project master | 0 | 0 |
| XenDroid main (rfandango) | 0 | 0 |

- Merged shortlist: Tier 1 has 22 clean stability picks. Tier 2 has 30 manual stability ports. Tier 3 has
  12 speed items. Tier 4 lists GPU rendering correctness items.
- No code was cherry-picked. The user approves the tiers before any port.

## Commands

```
git fetch canary edge xendroid upstream wunkolo xeniamac arm64fork cassar
git log --format='%h|%cd|%an|%s' --date=short --no-merges 01efb80ed..edge/edge
git log --format='%h|%cd|%an|%s' --date=short master..canary/canary_experimental
git show <hash> | git apply --check
```

## Ports landed

- The user approved all four tiers. 90 commits landed on master after `29ad2fcb1c`.
- Tier 1: 22 of 22. Tier 2: 28 of 30 (2 not applicable). Tier 3: 9 landed, 2 already present, 5 deferred.
  Tier 4: 21 landed, 6 not applicable, 4 not attempted. Details and hashes in
  `docs/research/20260918-upstream-triage.md`, section "Port status".
- Builds: Windows MSVC xenia.exe pass (394 s). Android native core NDK pass (435 s).
- x64 PPC corpus: 169,117 cases, 14,333 failures before and after, identical failure sets. 0 regressions.
- Six port agents ran in isolated worktrees before the user set the subagent limit. Their 49 commits
  were cherry-picked onto master by patch id and the worktrees removed.
- The user set two standing rules this session: STE language (CLAUDE.md section 1) and the subagent
  limit (CLAUDE.md directive 16).

## Next actions

1. Device test list in the triage note, section "Port status". The Thor was not used today.
2. Deferred items: futex global mutex, a64 MMIO fault recording (needs XexInfoCache), CR6 native tests,
   async shader compilation, audio pacing.

## Deploy

- Installed on the Thor (c3ca0370, USB) at 16:45:36 with `thor_build.ps1 -Mode FullDeploy`.
  APK `app-github-debug.apk`, 49,351,262 bytes, sha256 prefix `5b7d37413b67487a`, same on device.
  Built from master `a29ec786db`. Not launched.
- ApkShellDeploy failed on a duplicate `libxenia-app.so`: the mode staged a copy into jniLibs while
  Gradle already merges the cxx output when the native task is skipped. Fixed in `thor_build.ps1`:
  the mode now skips the native task with nothing staged and stages only when the APK has no
  library. Verified: ApkShell builds in 2 s with `lib/arm64-v8a/libxenia-app.so` present.

## Compile overlay, MCP server, no-cvar rule

- User report: the "Compiling game code" overlay did not show, and the compile used little power.
  Log evidence from the 17:05 Banjo-Kazooie launch: the native precompile ran and logged progress
  (5,120 of ~33,414 functions at +80 s, about 64 functions per second); the app's UI watchdog logged
  the main thread wedged at +4 s (sync and async messages both stalled, Java stack idle in
  nativePollOnce); the process was force-stopped by another process at 17:07:41.
- Compile speed: the LLVM object-cache path holds one compile lock per function, so a cold compile is
  single-threaded by design. A new APK means a cold object cache. Not from today's ports.
- Fix landed: the overlay now polls a native `PrecompileStatus` (src/xenia/cpu/precompile_status.h)
  through `nativeGetAotProgress` instead of tailing logcat from inside the app. The main-thread wedge
  is still open; `xenia_backtrace` (debuggerd -b) is the tool to read its native stack.
- New: `tools/mcp/xenia_thor_mcp.py`, a stdio MCP server over adb, registered in `.mcp.json`,
  documented in CLAUDE.md section 9. 17 tools: status, preflight, launch, stop, logs, AOT progress,
  fps, screenshot, backtrace, threads, memory, config get and set, build, install, git head.
- User rule (CLAUDE.md directive 17): no more cvars. Ship behavior as app defaults; test only what the
  play button runs. `xenia_launch` sends the launcher's VIEW intent and passes no extras.
- Installed on the Thor at 17:2x: APK sha256 prefix `3926a36242993e39`. The overlay test launch was
  interrupted by the user before it ran. Not verified on device yet.
